---
title: MiniMax Sparse Attention for Million-Token Contexts (GitHub Repo)
source: https://github.com/MiniMax-AI/MSA?utm_source=tldrai
author: []
published: ''
created: '2026-06-16'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 05c62ebc8024c924
source_type: news_media
tldr: MiniMax 开源 MSA 稀疏注意力项目，专为 NVIDIA SM100 设计，支持百万 token 上下文
objective_summary: MiniMax 在 GitHub 开源了 MSA（MiniMax Sparse Attention）项目，包含密集 FlashAttention
  和稀疏 top-k 注意力 CUDA 内核，专为 NVIDIA SM100 GPU 设计。项目提供 csrc JIT 和 CuTe-DSL 双栈实现，
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - MiniMax
  technologies:
  - MSA
  - FlashAttention
  - SM100
  - CuTe-DSL
  - NVFP4
  - FP8
  - BF16
  - FP4
  - CUTLASS
  - JIT
  key_people: []
key_logic_flow:
- MiniMax 开源了 MSA（MiniMax Sparse Attention）项目，这是一个专为 NVIDIA SM100 GPU 设计的稀疏注意力实现，支持百万
  token 上下文。
- 项目包含两个 JIT 编译栈：csrc 栈提供密集 FMHA 和 sparse_topk_select 索引器，CuTe-DSL 栈提供完整的稀疏注意力前向和分页
  FP8 解码。
- msa 支持 BF16、FP8、NVFP4 和 FP4 多种精度格式，其中 NVFP4 和 FP4 仅用于稀疏预填充路径。
- 安装依赖包括 CUDA Toolkit（nvcc ≥ 12.x）、Python ≥ 3.10、Linux x86_64 以及 NVIDIA SM100 GPU（计算能力
  10.0）。
- 用户可通过 kernels 库或直接 pip 安装使用，首次运行 JIT 编译 sparse_topk_select 需 30 秒至数分钟。
- 项目采用 MIT 许可证，包含对 NVIDIA CUTLASS 子模块的依赖，子模块和第三方代码保留其原始许可证。
impact_score:
  score: 6.5
  reason: MiniMax 开源 MSA 是稀疏注意力领域重要的工程贡献，专为 NVIDIA SM100 设计，支持百万 token 上下文。其价值在于：1）提供了密集
    FlashAttention 和稀疏 top-k 注意力的生产级 CUDA 实现，JIT 双栈架构（csrc + CuTe-DSL）体现了扎实的工程深度；2）MIT
    许可证降低了社区使用门槛。但硬件绑定（SM100 独占，计算能力 10.0）严重限制了适用范围——当前绝大多数开发者无法使用，行业冲击力局限在小众的高端 GPU
    优化圈层。对比 FlashAttention 当年发布时跨架构的普适性影响，MSA 更像是对未来硬件的提前布局。综合评定 6.5 分：对长上下文推理优化领域有实质推动，但短期行业影响因硬件限制而明显收窄。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: SM100 硬件独占限制较大，但百万 token 稀疏注意力的 MIT 开源实现是实打实的工程干货，多精度格式（BF16/FP8/NVFP4/FP4）支持尤其令
    LLM 推理优化开发者关注
hype_assessment:
  level: low
  reason: 项目为纯工程代码仓库发布，README 没有任何 PR 式夸张用词（无『革命性』『颠覆』等表述），提供了完整的安装说明、API 示例、测试套件和基准脚本，技术细节透明。MIT
    开源许可证进一步证明是务实的技术分享而非概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: CuTe-DSL 与 csrc 双栈 JIT 编译架构，在 SM100 上同时实现密集 FlashAttention
    和稀疏 top-k 注意力，并通过 bridge 适配器统一 API 调用路径；支持 BF16/FP8/NVFP4/FP4 多精度混合，稀疏预填充路径独享
    NVFP4/FP4 4-bit 精度，为百万 token 级推理的显存与计算效率提供了底层工程突破。
  business_model: MIT 开源许可证有助于 MiniMax 在 AI 基础设施领域建立技术声望，吸引顶尖 CUDA 工程人才，并通过生态影响力反哺其闭源模型产品的商业竞争力，是一种『以开源工程能力换品牌势能』的策略。
engineering_complexity: production_ready
compound_value:
  score: 6.5
  reason: MSA 直击大模型推理的核心瓶颈——注意力机制随序列长度二次方增长的计算成本，并通过硬件-算法协同设计（为 SM100 定制 sparse top-k
    attention）给出了工程化解决方案。从复利效应看：一方面，长上下文是行业确定性趋势（从 128K 到 1M+ token），MSA 一旦成为事实上的
    SM100 注意力库，其 JIT 编译生态和 CUDA 内核积累会产生持续的网络效应和性能迭代优势；但另一方面，风险同样显著——(1) 完全锁定 NVIDIA
    SM100 单一代际架构，若 NVIDIA 下一代架构变更或 AMD/定制芯片竞争力提升，这部分 kernel 投资可能无法迁移；(2) MIT 开源协议意味着
    MiniMax 无法直接通过许可证变现，价值主要流向 NVIDIA（硬件溢价）和下游模型厂商，MiniMax 自身捕获有限；(3) 注意力机制本身可能在算法层面被颠覆（如线性注意力、状态空间模型），稀疏注意力的长期必要性并非铁板钉钉。综上，MSA
    有潜力成为长上下文训练/推理的关键基础设施组件，但其复利效应受制于硬件锁定和开源商业化的天然局限，给予 6.5 分。
value_capture_layer: hardware_compute
moat_impact: creates_new_moat
key_beneficiaries:
- NVIDIA
- MiniMax
- Hugging Face
competitive_casualty:
- 专有稀疏注意力算法厂商
- 非 SM100 硬件生态（AMD、Intel 等）
- 传统长上下文 RAG 中间件
market_opportunities:
- 面向已部署 NVIDIA SM100（Blackwell）GPU 的企业，可直接集成 MSA 实现百万 token 级稀疏注意力，大幅降低长上下文推理的显存占用和计算延迟
- 开源社区可基于 MSA 的双栈架构（csrc JIT + CuTe-DSL）开发适配自家模型的稀疏注意力微调方案，尤其是在法律文档、代码库、基因组学等需要超长上下文的垂直场景
- 云服务商可为 Blackwell 实例用户预装 MSA 并包装为托管推理服务，形成 GPU 实例的差异化增值能力
risk_matrix:
  regulatory: 无
  technological: 项目强绑定 NVIDIA SM100（计算能力 10.0）GPU，无法在 Hopper 及更早架构上运行，若 Blackwell
    出货不及预期或出现更优稀疏注意力方案（如来自 NVIDIA 官方 CUTLASS 生态），该项目的技术生命力将被严重压缩
  competitive: NVIDIA 自身可能在未来 CUTLASS/CuTE 版本中推出官方稀疏注意力内核，直接覆盖 MSA 功能；Meta、Google
    等大厂也有各自稀疏注意力开源方案（如 FlashAttention 生态），生态位竞争激烈
  ethical: 无
  additional:
  - MIT 许可证下捆绑的 NVIDIA CUTLASS 子模块和第三方代码保留各自原始许可证，企业在商业集成时需逐一排查第三方许可兼容性
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: monitor
---

**MSA** (`fmha_sm100`

) ships dense FlashAttention and sparse top-k attention
kernels for **NVIDIA SM100**. Two JIT-compiled stacks
share one Python package:

Algorithm reference: MiniMax Sparse Attention paper.


| Stack | Path | What it gives you |
|---|---|---|
csrc JIT |
`python/fmha_sm100/csrc/` |
Dense FMHA (`fmha_sm100` , `fmha_sm100_plan` ) + `sparse_topk_select` indexer, compiled from Jinja templates by `jit.py` at runtime. |
CuTe-DSL |
`python/fmha_sm100/cute/` |
Full sparse attention (forward + paged FP8 decode, BF16 / FP8 / NVFP4 / FP4), compiled at runtime via `cute.compile` . |
Bridge |
`python/fmha_sm100/sparse_fmha_adapter.py` |
Adapts the `fmha_sm100` API to call `sparse_atten_func` for sparse prefill paths. |


License: MIT.Self-authored files carry`SPDX-License-Identifier: MIT`

. See LICENSE and NOTICE. Bundled / derived third-party code retains its own license — see Third-party licenses.

**GPU**: NVIDIA SM100.**Toolchain**: CUDA Toolkit with`nvcc`

on`PATH`

(or`CUDA_HOME`

/`CUDA_PATH`

set).**Python**: ≥ 3.10.**OS**: Linux x86_64 (aarch64 untested; JIT builds may need small Makefile edits on WSL).

Quick sanity check before installing:

```
nvcc --version # expect ≥ 12.x
nvidia-smi --query-gpu=compute_cap --format=csv | grep "10.0" # confirm SM100
python -c "import sys; print(sys.version_info[:2])" # ≥ (3, 10)
```

To quickly get started using MSA kernels, you can use the `kernels`

library:

```
# make sure `kernels` is installed: `pip install -U kernels`
from kernels import get_kernel
kernel_module = get_kernel("MiniMaxAI/msa", version=0)
sparse_atten_func = kernel_module.sparse_atten_func
sparse_atten_func(...)
```

Check out the kernel on the Hugging Face Hub here.

```
# --recursive pulls the NVIDIA CUTLASS submodule (python/fmha_sm100/cutlass/),
# whose headers are required for JIT/AOT compilation.
git clone --recursive https://github.com/MiniMax-AI/MSA.git msa
cd msa
# If you cloned without --recursive:
# git submodule update --init --recursive
pip install . # standard install (works from a wheel too)
# or
pip install -e . # editable install for development
```

This pulls in the CuTe-DSL stack via `nvidia-cutlass-dsl`

and `quack-kernels`

;
the csrc kernels are JIT-compiled at first import from sources shipped inside
the package.

Run a small CUDA smoke test. **The first run JIT-compiles sparse_topk_select,
which takes 30 s – a few minutes on a cold nvcc cache** — this is normal, not
a hang. Subsequent runs hit the JIT cache and finish in seconds.

`python tests/smoke/test_sparse_topk_forced.py`

```
import torch
from fmha_sm100 import fmha_sm100, fmha_sm100_plan, sparse_topk_select
# Page size and top-k for the sparse prefill path.
page_size, topk = 128, 16
# Dense proxy pass: compute per-block max score from a cheap Q slice.
proxy_plan = fmha_sm100_plan(
qo_lens, kv_lens, proxy_q.shape[1],
num_kv_heads=1,
page_size=page_size,
output_maxscore=True,
)
_, max_score = fmha_sm100(
proxy_q, proxy_k_pages, proxy_v_pages, proxy_plan,
kv_indices=kv_indices,
output_o=False,
output_maxscore=True,
)
# max_score -> sparse KV block indexes.
kv_block_indexes = sparse_topk_select(
max_score.contiguous(), topk, num_valid_pages=num_pages,
)
# Sparse attention with the selected blocks.
sparse_plan = fmha_sm100_plan(
qo_lens, kv_lens, q.shape[1],
num_kv_heads=k_pages.shape[1],
page_size=page_size,
kv_block_num=topk,
)
out, _ = fmha_sm100(
q, k_pages, v_pages, sparse_plan,
kv_indices=kv_indices,
kv_block_indexes=kv_block_indexes,
)
```

For block-sparse prefill with CSR metadata, the FP4 indexer, NVFP4 K/V, and
the paged FP8 decode wrapper, see the **CuTe-DSL deep dive**:

```
# Fast smoke tests.
python -m pytest tests/smoke -q
# API and end-to-end integration tests.
python -m pytest tests/integration -q
python tests/integration/test_proxy_kv_e2e.py
# Large regression suites.
python tests/regression/test_correctness.py
python tests/regression/test_sparse_attn.py
# CuTe-DSL forward-only sparse attention.
cd python/fmha_sm100/cute
python -m pytest test_sparse_atten.py -q
```

`benchmarks/bench_sparse_attention_ops.py`

covers dense prefill, paged
prefill, sparse prefill, dense decode, paged decode, sparse decode, in
`fp8`

and `bf16`

(`nvfp4`

is sparse-prefill only).

`python benchmarks/bench_sparse_attention_ops.py --help # full flag list`

Common invocations (output is TSV):