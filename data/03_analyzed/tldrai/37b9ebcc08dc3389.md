---
title: The Session You Cannot Take With You (18 minute read)
source: https://earendil.com/posts/session-portability/?utm_source=tldrai
author: []
published: ''
created: '2026-08-01'
manifest_dates:
- '2026-08-01'
- '2026-08-02'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 37b9ebcc08dc3389
source_type: news_media
tldr: 文章批判推理 API 越来越多地返回只能由原服务商解密的状态（加密推理 token、隐藏搜索证据、压缩上下文、加密子代理消息等），使会话不再可移植。作者提出检验会话所有权的五项测试（检视、导出、重放、审计、删除），并指出
  OpenAI Responses API 与 Gemini Interactions API 默认在服务端存储会话，使本地转录稿退化为指向服务端数据的指针。
objective_summary: earendil.com 的开发者团队撰写此文，分析当前推理 API 正在丧失会话可移植性的问题。他们指出 OpenAI 等提供商通过返回加密推理
  token、隐藏 web 搜索结果、仅提供商可解的压缩上下文等方式，使客户端持有的转录稿只是会话的部分视图。作者提出检视、导出、重放、审计、删除五项测试来判断会话所有权，并批评
  Responses API 默认存储响应至少 30 天、Gemini Interactions API 默认开启 store 的做法。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies:
  - OpenAI
  technologies:
  - Responses API
  - Gemini Interactions API
  - prompt caching
  - Zero Data Retention
  key_people: []
key_logic_flow:
- 推理 API 原本承诺会话转录稿可由用户保存、回放或交给其他模型，但如今越来越多地返回与提供商绑定且不可移植的状态。
- 不可移植的功能包括加密推理 token、客户端不可见的搜索结果、仅原提供商可解密的压缩上下文、隐藏的子代理指令以及存于服务端的会话 ID。
- 作者提出会话可移植性的五项测试：检视模型所见、导出自包含存档、重放语义等价上下文、审计系统行为原因以及删除服务端副本。
- 文章指出所谓 encrypted_content 实为提供商密封状态，密钥由提供商掌控，加密并不对提供商隐藏数据，反而对用户隐藏数据。
- 'OpenAI Responses API 默认存储响应对象至少 30 天，文章建议使用 store: false 使其更接近 completions 的行为。'
- 新的 Gemini Interactions API 也做出类似选择，默认将 store 设为 true，把会话状态保存在服务端。
object_mentions:
- object_type: product
  name: Responses API
  canonical_name: OpenAI Responses API
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 文章指出 OpenAI 的 Responses API 默认存储响应对象，其文档说明响应对象默认保留至少 30 天。
  - '文章建议开发者使用 store: false 参数使其更接近 completions 的行为，数据不会存储在 OpenAI 服务器上。'
  article_id: 37b9ebcc08dc3389
- object_type: product
  name: Gemini Interactions API
  canonical_name: Gemini Interactions API
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 文章指出新的 Gemini Interactions API 也做出了类似选择，默认将 store 设为 true，把会话状态保存在服务端。
  article_id: 37b9ebcc08dc3389
extract_result: success
impact_score:
  score: 4.5
  reason: '评分依据：该文并非产品发布、论文发表或融资事件，而是针对 OpenAI Responses API 与 Gemini Interactions
    API 默认在服务端存储会话状态的批判性技术分析，短期不会直接改变竞争格局或市场份额。但其提出的''provider-sealed state''（提供商密封状态）概念与五项可移植性检验框架精准命中了开发者对
    API 锁定和数据主权的真实焦虑，可能短期影响部分开发者的 API 选型决策（如主动设置 store: false），并在技术舆论场引发对推理 API 会话所有权与锁定问题的讨论。综合判断属于中低冲击的行业舆论事件。评分：4.5。'
sentiment: negative
developer_sentiment:
  tone: skeptical
  primary_focus: 会话可移植性与数据所有权——本地转录稿正在退化为指向服务端状态的指针，开发者对提供商锁定与不透明加密状态高度警惕
hype_assessment:
  level: low
  reason: '判定依据：文章通篇未使用''颠覆''''革命''等 PR 式夸张措辞，而是基于可验证的 API 实际行为（Responses API 默认保留响应对象至少
    30 天、Gemini Interactions API 默认 store: true、encrypted_content 的密钥由提供商掌控）展开具体批评，并给出代码化的可移植性定义与五项可操作检验框架，属于实打实的行业批评与架构原则探讨，无概念炒作成分。判定：low。'
information_entropy: high
domain_disruption:
  technical_innovation: 本文未提出新的技术架构，核心贡献在于概念层面的破局：首次系统性地定义了'provider-sealed state'（提供商密封状态），戳破
    encrypted_content 的营销话术——加密并不对提供商隐藏数据，反而对用户隐藏数据；并提出会话可移植性五项检验框架（检视、导出、重放、审计、删除），为开发者评估
    API 会话所有权提供了可操作的判定标准。
  business_model: '若该理念被开发者广泛接受，将抑制推理 API 提供商通过服务端会话 ID、加密推理 token、压缩上下文等机制构筑切换成本、锁定客户的商业模式倾向，倒逼
    OpenAI、Google 等将 store: false / Zero Data Retention 从可选项推向更主流的默认配置，重塑''用户数据主权
    vs 平台锁定''的 API 商业平衡。'
engineering_complexity: conceptual
compound_value:
  score: 6.0
  reason: 该事件揭示了一个结构性趋势：推理 API 正从"可移植转录稿"走向"提供商密封状态"，会话可移植性成为模型竞争的关键变量。若这一批判能催生类似
    MCP 之于工具那样的"可移植会话格式"标准，将沉淀为智能体中间件层的基础设施，具备明显的复利效应——未来的 Agent 应用无论底层模型如何切换，都依赖一个中立的会话载体。但目前仍停留在理论呼吁层面，尚无实际协议或产品落地；且
    OpenAI/Google 的商业激励天然指向锁定（默认 30 天存储、store:true、加密 token 都强化切换成本），标准的形成需要开源社区、监管和下游开发者三方合力，存在较大不确定性。因此给到
    6 分——有潜力成为细分赛道基础设施，但需持续验证是否有实质协议与产品承接。
value_capture_layer: agent_middleware
moat_impact: strengthens_monopoly
key_beneficiaries:
- OpenAI
- Google
- LangChain
- Mistral
competitive_casualty:
- 小型 AI 应用开发者
- OpenRouter
- 多模型切换型 Agent 平台
market_opportunities:
- 开发者可基于文中提出的会话可移植性五项测试（检视/导出/重放/审计/删除），构建跨推理商的会话导出与重放中间件，帮助企业实现多模型无缝迁移而不依赖原厂商解密状态
- '建议关注主打零数据保留（store: false）与本地完整审计日志的企业级 AI 网关/代理产品机会，将合规留痕与数据主权作为差异化卖点'
- 开源社区可推动标准化的自包含会话存档格式与可移植协议，降低对单一推理商的依赖，契合数据主权日益受重视的企业客户需求
risk_matrix:
  regulatory: 欧盟 GDPR 的删除权/数据可携带权与 AI Act 透明度义务可能约束推理商默认服务端存储与密封状态做法；企业在数据出境和留存审计上或面临合规与追责压力
  technological: 推理 API 持续将会话状态绑定到服务端会削弱多模型编排与迁移能力、加剧供应商锁定；但开源本地推理与开放协议可能反向形成技术替代
  competitive: OpenAI、Google 等巨头通过密封状态与服务端会话 ID 构建生态锁定，挤压第三方工具链生存空间；主打可移植性的新服务与开源模型可差异化突围
  ethical: 所谓 encrypted_content 实为向用户隐藏数据而非保护用户，削弱用户对自身会话数据的知情权与事后审计能力，AI 决策可追溯性随之下降
  additional:
  - 企业数据主权风险：含商业机密的会话状态留存于第三方服务端，一旦供应商调整策略或停止服务，历史会话可能无法迁移、恢复或完整归档
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: product
  name: Responses API
  canonical_name: OpenAI Responses API
  url: null
  positioning: OpenAI 面向开发者的新一代对话与推理响应接口，默认将响应对象存储在服务端，与旧版 completions 接口的无状态行为形成对比。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用 OpenAI 推理 API 的开发者
  - 依赖会话转录稿做本地归档的 AI 应用团队
  product_signal: '该接口默认保留响应对象至少 30 天，可通过 store: false 关闭存储，使其行为更接近无状态的 completions。'
  market_signal: 文章批评推理 API 日益返回与提供商绑定的不可移植状态，反映行业默认存储会话的趋势正在引发开发者反弹。
  differentiation: 相比不存储数据的旧版 completions 接口，Responses API 默认开启服务端存储，使本地转录稿退化为指向服务端数据的指针。
  watch_reason: 'OpenAI 作为行业风向标，其 Responses API 的存储与加密默认值正在塑造推理 API 的会话所有权范式，直接关系到开发者的数据合规与工作流可移植性，值得持续跟踪其默认值演进与
    store: false 的采用情况。'
  risk_notes:
  - '默认存储至少 30 天可能违反企业数据合规要求，开发者需显式传入 store: false 才能规避。'
  - 加密推理 token 的密钥由提供商掌控，加密并不向用户提供真正的数据控制权。
  score: 7.0
  article_ids:
  - 37b9ebcc08dc3389
  evidence_snippets:
  - 文章指出 OpenAI 的 Responses API 默认存储响应对象，其文档说明响应对象默认保留至少 30 天。
  - '文章建议开发者使用 store: false 参数使其更接近 completions 的行为，数据不会存储在 OpenAI 服务器上。'
- object_type: product
  name: Gemini Interactions API
  canonical_name: Gemini Interactions API
  url: null
  positioning: Google 推出的新一代会话式推理接口，默认将 store 设为 true，把会话状态保存在服务端，延续了推理 API 的会话存储趋势。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - Gemini API 开发者
  - 关注会话数据存储位置与合规性的 AI 应用团队
  product_signal: '该接口默认开启 store: true，把会话状态保存在服务端，与 OpenAI Responses API 的默认存储行为保持一致。'
  market_signal: Gemini Interactions API 与 OpenAI Responses API 同步选择默认服务端存储，显示头部推理厂商正在集体转向不可移植的会话模型。
  differentiation: '与 OpenAI Responses API 一样默认存储会话，但文章未提及 Google 提供类似 store: false
    的无状态替代选项。'
  watch_reason: Gemini Interactions API 作为 Google 的旗舰会话接口，其默认服务端存储与 OpenAI 的举措共同定义了推理
    API 会话所有权的行业惯例，影响开发者对数据控制与可移植性的预期，值得跟踪其后续是否提供关闭存储的选项。
  risk_notes:
  - 默认开启 store 意味着会话状态依赖服务端保存，用户本地转录稿将失去独立可移植性。
  - 文章信息有限，尚不清楚该接口是否提供与 Responses API 相当的关闭存储参数。
  score: 6.0
  article_ids:
  - 37b9ebcc08dc3389
  evidence_snippets:
  - 文章指出新的 Gemini Interactions API 也做出了类似选择，默认将 store 设为 true，把会话状态保存在服务端。
---

# The Session You Cannot Take With You

The original promise of an inference API was wonderfully simple: send some input, receive some output. If you kept both, you had the conversation. You could inspect it, archive it, replay it, or give it to a different model.

That abstraction was never completely true. For instance prompt caches
live on somebody else's GPUs, tokenization differs
between models, and sampling is not reproducible (and quite intentionally so).
But the *semantic record* of a session in the form of a transcript could still
belong to the user. A transcript should contain the instructions, messages,
tool calls and tool results. Another sufficiently capable model might not
continue identically, but it could understand what happened and take over.

Inference APIs are frustratingly moving away from that property, at least somewhat. They increasingly return a mixture of text and provider-bound state that is very intentionally non-portable.

- reasoning tokens that are billed to the user but returned only as opaque, encrypted blobs, with useless summaries at best
- web searches where the model sees source material the client never sees
- compacted context that only the original provider can decrypt
- subagent instructions and messages hidden from the application running the agents in the form of encrypted payloads
- file, vector-store, container, and cache references that cannot be resolved anywhere else.
- response and conversation state that is entirely keyed by IDs that are stored fully on the provider's servers

Each feature comes with a basic justification that's trivial for a provider to come up with, along with good arguments for why this is good for the user. Together all of these things change the ownership reality of an AI session: the transcript on your machine is no longer your session but a partial view of a session whose operational state belongs to an inference provider and not you.

We are not fans of this direction, and we want to talk a bit about what it means to you, as a user, and what it means to us, as people developing tools in this space.

## A Practical Test for Session Ownership

By a portable session we do not mean that switching from one model to another must produce the same next token. That's a given because models have different capabilities, trained personalities, context windows, and ways of working with tools. And well, it's all quite nondeterministic anyway. Portability means something more modest:

```
const transcript = session.export();
revokeCredentials(oldProvider);
session = newProvider.continueFrom(transcript);
```


The archive should contain enough intelligible information for another model to continue the work. It should not require the old provider to dereference an ID, decrypt a blob, remember a search result, or reconstruct a summary.

This gives us five useful tests:

**Inspection:**Can the user see what the model saw, what tools did, and what agents told each other?**Export:**Is the session self-contained, apart from ordinary artifacts that can also be downloaded?**Replay:**Can another implementation reconstruct a semantically equivalent context?**Audit:**Can a human explain why the system took an action after the fact?**Deletion:**Can the user identify and remove every server-side copy on which the session depends?

A response ID is not a transcript (as the data is stored on the server), a ciphertext is not user-controlled stated (as the user cannot decrypt it), a list of citations is not the evidence that was placed in the model's context by a search result (as you cannot typically fetch the same data as the model did).

## Encryption for Whom?

The naming and marketing around these features can be misleading.
`encrypted_content`

sounds like a privacy feature under the user's control.
Usually it is a capsule that the client cannot read and only the provider can
open. The provider chooses the keys, decrypts the content for its own models,
and defines where the data can be replayed.

A better term is **provider-sealed state**.

Provider sealing can have a real privacy benefit. OpenAI, for example, can
return encrypted reasoning to a client using `store: false`

, then decrypt it in
memory on the next request without persisting the intermediate state. That is
better than requiring server-side conversation storage, particularly for Zero
Data Retention customers. But, remember, there is not really anything that
needs encryption to begin with!

This encryption does not hide the data from the inference provider but it hides it from you.

## Stored Conversations Turn a Transcript into a Pointer

OpenAI's Responses API stores responses by default. Its documentation says
response objects are retained for at least 30 days by default. `store: false`

is available
and should be used, as it makes it work more like completions: the data is not
stored on OpenAI's servers.

The new Gemini Interactions API has made a similar choice. It defaults to
`store: true`