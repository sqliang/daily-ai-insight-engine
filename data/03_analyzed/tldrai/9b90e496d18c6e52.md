---
title: Wall Attention (GitHub Repo)
source: https://github.com/tilde-research/wall-attention-release?utm_source=tldrai
author: []
published: ''
created: '2026-06-04'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 9b90e496d18c6e52
source_type: news_media
tldr: Tilde Research 开源 Wall Attention——一种带逐通道逐时间步乘法衰减的新注意力机制变体
objective_summary: Tilde Research 于 GitHub 开源 Wall Attention 的 Triton 内核实现，包含训练/预填充和解码两个内核，支持
  GQA、逐通道衰减、注意力汇聚、滑动窗口和变长序列打包，附带完整的数值正确性测试。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Tilde Research
  technologies:
  - Wall Attention
  - Triton
  - FlashAttention
  - GQA
  key_people: []
key_logic_flow:
- Wall Attention 的核心创新是在 QK 内积中引入逐通道、逐时间步的乘法衰减门控（g参数），区别于标准注意力机制。
- 项目提供两个 Triton 内核：训练/预填充内核（融合前向+反向，FlashAttention 风格流式 softmax）和解码内核（单步生成，使用预缩放 KV
  缓存）。
- 解码阶段通过 build_wall_kv_cache 将衰减折叠到键中，利用逐块锚点保证长上下文数值稳定性，每次生成只需一次小型 GEMV 操作。
- 支持 GQA（查询头数可超过 KV 头数）、可选标量门控 g_scalar、注意力汇聚、滑动窗口和变长序列打包。
- 所有内核路径均通过 Eager PyTorch 参考实现的数值正确性检验，g/g_scalar 梯度通过中心有限差分验证。
impact_score:
  score: 6.0
  reason: Wall Attention 提出了一种新颖的注意力机制变体——在 QK 内积中引入逐通道、逐时间步的乘法衰减门控（g参数），并提供了高质量的
    Triton 内核实现（训练/预填充融合内核+高效解码内核）。作为 MIT 开源项目，其工程实现扎实（数值正确性验证、GQA/滑动窗口/变长序列支持、预缩放
    KV 缓存优化），有潜力被社区采纳。但该机制尚未在大规模模型训练中得到验证，也未展示与标准注意力在同等规模下的对比基准，短期行业冲击有限——它本质上是为社区提供一个新构建模块，而非立即改变主流注意力范式。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 逐通道衰减门控能否在长上下文和大规模训练中稳定收敛并超越标准注意力
hype_assessment:
  level: low
  reason: 文章和仓库描述技术内容扎实，没有使用'颠覆性''革命性'等夸大的 PR 话术。提供了完整的 Triton 内核源码、数值正确性测试（含有限差分梯度验证）、解码流式循环示例和清晰的
    API 文档。这是实打实的技术贡献而非概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 在 QK 内积中引入逐通道、逐时间步的乘法衰减门控（g参数），使模型可以学习每个通道在不同时间步上的注意力衰减模式，这是对标准点积注意力的一种结构性增强。解码阶段通过
    build_wall_kv_cache 将衰减折叠到键中，并利用逐块锚点（per-chunk anchor）保证长上下文时 exp2 数值范围可控，每次生成仅需一次小型
    GEMV 操作——这是工程创新与算法创新的结合。
  business_model: MIT 开源协议，无直接商业模式。但该注意力变体若被推理引擎（如 vLLM、TensorRT-LLM）或框架采纳，可降低长上下文推理的解码成本，间接影响
    AI 推理基础设施的商业竞争格局。
engineering_complexity: prototype
compound_value:
  score: 6.5
  reason: Wall Attention 作为带逐通道逐时间步乘法衰减的新型注意力变体，在技术上有实质创新：解码阶段通过预缩放 KV 缓存将单步生成降为小型
    GEMV 操作，对长上下文场景有显著效率优势。Tilde Research 以 MIT 协议开源，附带经过有限差分验证的 Triton 内核和完整的数值正确性测试（训练+解码），工程质量高于普通研究代码。但注意力机制赛道已有多方案（FlashAttention、MLA、Mamba、RWKV）争夺生态位，Wall
    Attention 尚未获得社区广泛采用或主流框架集成。长期来看，其 MIT 协议和 Triton 实现有利于被集成，但单一注意力变体难以建立平台级护城河，复利效应受限于技术栈薄层定位。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- Tilde Research
- 开源 AI 社区
- 长上下文模型开发者
- NVIDIA
competitive_casualty:
- 闭源注意力机制优化服务商
market_opportunities:
- 长上下文大语言模型的推理优化——Wall Attention 的解码内核通过预缩放 KV 缓存将生成成本降低为单次小 GEMV 操作，在支持百万级上下文窗口的服务中有直接落地价值
- 边缘设备上的高效推理部署——该机制将逐时间步衰减折叠到键缓存中，消除了前缀重计算，特别适合内存和算力受限的端侧场景
- 新模型架构预训练集成——开发者可将 Wall Attention 作为一个可微分的注意力变体嵌入新的大模型训练流程，配合 GQA 和滑动窗口实现更高效的模型设计
risk_matrix:
  regulatory: 无
  technological: Wall Attention 面临 FlashAttention、MLA、状态空间模型等主流方案的生态竞争，若未被 HuggingFace
    Transformers、vLLM 等框架集成，可能难以获得广泛采用，存在架构被边缘化的风险
  competitive: 注意力机制优化赛道极为拥挤（Triton 生态已有 flash-linear-attention、FlashAttention 等成熟方案），OpenAI
    和 DeepSeek 等巨头的专有优化方案并不开源，Wall Attention 在吸引社区贡献和生态构建上存在挑战
  ethical: 无（该注意力机制本身不引入新的伦理风险，但任何大模型能力提升都可能间接加剧深度伪造或信息操纵风险，属于所有注意力变体的共性问题）
  additional:
  - 该实现高度依赖特定硬件（CUDA GPU）和 Triton 编译器，对 AMD 或 Apple Silicon 等非 NVIDIA 生态的支持缺失可能限制其扩散半径
confidence:
  impact: medium
  compound: low
  hype: low
actionable_insight: monitor
---

Wall Attention is an attention variant with a **per-channel, per-timestep multiplicative decay** baked into the QK inner product. Where standard attention scores a pair

See the blog for more information: https://blog.tilderesearch.com/blog/wall-attn

This repo packages the two kernels used in practice, each on its own:

-
**Training / prefill**(`wall_attn`

): a fused forward + backward Triton kernel (FlashAttention-style streaming softmax) with analytic gradients for$q, k, v, g$ . -
**Decode**(`wall_attn_decode`

): a single-step kernel that reads a**pre-rescaled KV cache**, so per-token generation costs one small GEMV-like pass instead of recomputing the prefix.

```
# Using uv (recommended)
uv sync
source .venv/bin/activate
# or with pip
pip install -e .
```

```
import torch
from wall_attn import wall_attn
B, T, H, HQ, K, V = 2, 1024, 4, 8, 64, 64 # GQA: HQ query heads, H kv heads
q = torch.randn(B, T, HQ, K, device="cuda", dtype=torch.bfloat16, requires_grad=True)
k = torch.randn(B, T, H, K, device="cuda", dtype=torch.bfloat16, requires_grad=True)
v = torch.randn(B, T, H, V, device="cuda", dtype=torch.bfloat16, requires_grad=True)
g = torch.randn(B, T, HQ, K, device="cuda", dtype=torch.bfloat16, requires_grad=True) * 0.02
o = wall_attn(q, k, v, g, scale=K**-0.5) # [B, T, HQ, V]
o.sum().backward()
```

Optional arguments: `g_scalar`

([B, T, HQ] FoX-style additive gate), `sink_bias`

([HQ] attention sink), `window_size`

(sliding window), and `cu_seqlens`

(varlen packing, requires `B == 1`

).

Build the pre-rescaled cache once at prefill, then decode one token at a time:

```
import torch
from fla.ops.utils.constant import RCP_LN2
from fla.ops.utils.cumsum import chunk_global_cumsum
from wall_attn import build_wall_kv_cache, wall_attn_decode
C = 64 # cache chunk size (anchor granularity)
P = chunk_global_cumsum(g, scale=RCP_LN2) # [B, T, HQ, K] prefix
k_tilde, r_cache = build_wall_kv_cache(k, P, chunk_size=C)
o, _ = wall_attn_decode(
q=q[:, -1:], # current query [B, 1, HQ, K]
v=v, # cached values [B, T_kv, H, V]
p_curr=P[:, -1:], # prefix at the current row
k_tilde=k_tilde, # pre-rescaled keys [B, T_kv, HQ, K]
r_cache=r_cache, # per-chunk anchors [B, ceil(T_kv/C), HQ, K]
sink_bias=None,
scale=K**-0.5,
cache_chunk_size=C,
)
```

`build_wall_kv_cache`

folds the decay into the keys (`k_tilde[j] = k[j] · exp2(R_c − P[j])`

) using a per-chunk anchor `R_c`

, so the decode kernel never re-accumulates the prefix. See `tests/test_decode.py::test_decode_streaming_matches_full_forward`

for the full append-as-you-go serving loop.

```
wall_attn/
├── __init__.py # public API
├── training.py # forward/backward Triton kernels + autograd Function + wall_attn()
├── decode.py # single-step decode kernel + build_wall_kv_cache()
└── reference.py # eager PyTorch reference (correctness oracle)
tests/
├── test_training.py # parity + analytic gradients (finite-difference checked)
└── test_decode.py # decode == prefill forward, streaming, cache shapes
```


**GQA**: query heads`HQ`

may exceed kv heads`H`

(`HQ % H == 0`

).**Per-channel decay**with exact analytic gradient, plus an optional`g`

**scalar gate**`g_scalar`

.**Attention sink**(`sink_bias`

),**sliding window**(`window_size`

), and**varlen packing**(`cu_seqlens`

).**Pre-rescaled decode cache**for cheap autoregressive generation, numerically stable to long context (per-chunk anchors keep`exp2`

bounded).- BF16/FP32 inputs; autotuned block sizes for Hopper / Ampere.

`pytest # requires a CUDA GPU`

Every kernel path is checked against the eager `wall_attn_reference`

, and the `g`

/ `g_scalar`

gradients are verified against central finite differences. The decode kernel is checked to reproduce the training forward token-for-token, including a streaming generation loop.

The Triton kernels build on the parallel-attention machinery from flash-linear-attention (MIT). We thank the FLA team for their excellent work on efficient attention.

MIT, see LICENSE.