---
title: Today, we are releasing verifiers v1 (3 minute read)
source: https://threadreaderapp.com/thread/2076447247693402301.html?utm_source=tldrai
author: []
published: ''
created: '2026-07-15'
manifest_dates:
- '2026-07-15'
description: TLDR AI 每日头条
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 10c20df18a1fe34a
source_type: news_media
tldr: verifiers v1 正式发布，这是一套面向现代智能体强化学习和评估的环境堆栈重构方案，将环境拆解为任务集、测试框架和运行时三层，支持可扩展的编码和计算机使用任务，并可直接接入
  PRIME-RL 进行训练。
objective_summary: 2026年7月17日，一个未具名团队发布了 verifiers v1，对智能体 RL 和评估的环境堆栈进行全面重构。verifiers
  v1 将环境分解为 taskset、harness 和 runtime 三层，通过受管制的拦截服务器代理 harness 与推理服务器之间的请求，实时记录消息
  DAG 轨迹，使轨迹大小从 O(n²) 降至 O(n)。该方案可直接接入 PRIME-RL 进行训练，已用于所有内部生产运行：在 GLM-4.5-Air 模型上的
  ScaleSWE 任务测试中，实现 4 分钟内完成步骤、35 轮 rollout，6 台 H200 节点 2 天完成 1000 步训练。同时，RL Residency
  Cohort I 产出了 CARLA-Env、RLM 和 INTELLECT-3（106B MoE 模型）等多项成果。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - GLM (Zhipu AI)
  technologies:
  - verifiers v1
  - PRIME-RL
  - MoE
  - GLM-4.5-Air
  - INTELLECT-3
  - RLM
  - ScaleSWE
  - CARLA-Env
  - Message DAG
  key_people:
  - '@myainotez'
  - '@a1zhang'
key_logic_flow:
- verifiers v1 正式发布，将环境堆栈重构为 taskset、harness 和 runtime 三层，支持编码和计算机使用等复杂智能体任务的大规模运行。
- verifiers v1 引入受管制的拦截服务器，代理 harness 与推理服务器间的请求，实时记录轨迹并支持训练和重写以缓解奖励作弊问题。
- 轨迹系统升级为消息 DAG 结构，每条消息仅存储一次，轨迹大小从 O(n²) 降至 O(n)，使长程智能体 rollout 和路由器回放变得可行。
- verifiers v1 可直接接入 PRIME-RL 进行训练，已在内部生产中用于 GLM-4.5-Air 模型，在 ScaleSWE 任务上实现 4 分钟内完成步骤、35
  轮 rollout、6 台 H200 节点 2 天完成 1000 步训练。
- RL Residency Cohort I 多项成果公布，包括基于 CARLA 模拟器的具身 RL 环境 CARLA-Env、通过持久 Python REPL
  变量进行上下文折叠的 RLM，以及 106B MoE 模型 INTELLECT-3。
- INTELLECT-3 是基于 GLM-4.5-Air Base 的 106B 参数 MoE 模型，采用 SFT 和 RL 两阶段训练，在 512 块 H200
  GPU 集群上耗时两个月完成，在数学、代码和推理任务上达到同规模最优。
extract_result: success
object_mentions:
- object_type: project
  name: verifiers v1
  canonical_name: verifiers v1
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - verifiers v1 是对环境堆栈的全面重构，将环境分解为 taskset、harness 和 runtime 三层，支持编码和计算机使用等复杂智能体任务。
  - verifiers v1 通过受管制的拦截服务器代理 harness 与推理服务器之间的请求，实时记录消息 DAG 轨迹以支持训练和重写。
  - verifiers v1 可直接接入 PRIME-RL 进行训练，已用于所有内部生产运行，包括 GLM-4.5-Air 模型的 ScaleSWE 任务训练。
  article_id: 10c20df18a1fe34a
- object_type: project
  name: PRIME-RL
  canonical_name: PRIME-RL
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - verifiers v1 可直接接入 PRIME-RL 进行训练，PRIME-RL 是一个可扩展的异步 RL 训练器。
  - PRIME-RL 与 verifiers、沙箱和计算编排共同构成 INTELLECT-3 的训练堆栈。
  article_id: 10c20df18a1fe34a
- object_type: project
  name: CARLA-Env
  canonical_name: CARLA-Env
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - CARLA-Env 是一个基于 CARLA 模拟器的开源具身 RL 环境，提供高保真物理、传感器和可配置城市场景。
  - CARLA-Env 由 @myainotez 创建，是 RL Residency Cohort I 的成果之一。
  article_id: 10c20df18a1fe34a
- object_type: project
  name: RLM
  canonical_name: RLM
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - RLM 于 2025 年 10 月由 @a1zhang 首次提出，模型通过持久 Python REPL 中的变量访问其输入。
  - RLM 是一种简单灵活的上下文折叠形式，不依赖有损摘要，模型可将上下文主动委托给 Python 脚本和子 LLM。
  article_id: 10c20df18a1fe34a
- object_type: model
  name: INTELLECT-3
  canonical_name: INTELLECT-3
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - INTELLECT-3 是一个 106B 参数的 MoE 模型，基于 GLM-4.5-Air Base 经 SFT 和 RL 两阶段训练而成。
  - INTELLECT-3 在数学、代码和推理任务上达到同规模最优性能，在 512 块 H200 GPU 集群上耗时两个月完成训练。
  article_id: 10c20df18a1fe34a
- object_type: model
  name: GLM-4.5-Air
  canonical_name: GLM-4.5-Air
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - GLM-4.5-Air 模型在 ScaleSWE 任务上使用 verifiers v1 进行训练，实现 4 分钟内完成步骤、35 轮 rollout 的训练效率。
  - GLM-4.5-Air Base 作为 INTELLECT-3 的基座模型，用于后续 SFT 和 RL 训练。
  article_id: 10c20df18a1fe34a
- object_type: dataset
  name: ScaleSWE
  canonical_name: ScaleSWE
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - verifiers v1 使用 ScaleSWE 任务对 GLM-4.5-Air 进行训练，展示了其在软件工程评估任务中的训练效率。
  article_id: 10c20df18a1fe34a
- object_type: project
  name: Harbor
  canonical_name: Harbor
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - verifiers v1 支持通过复用 Harbor 等其他框架来创建 taskset，实现跨框架兼容。
  article_id: 10c20df18a1fe34a
impact_score:
  score: 7.2
  reason: verifiers v1 解决了一个长期困扰智能体强化学习的核心工程瓶颈：长程 rollout 的 trace 存储复杂度从 O(n²) 降为
    O(n) 的消息 DAG 结构。这不是渐进式优化，而是架构级别的突破——使得智能体在多轮交互、路由重放、多模态数据场景下的训练变得可行。三层分解（taskset/harness/runtime）提供了组合灵活性，可以直接复用
    Harbor 等已有框架。实证数据也有说服力：6 个 H200 节点、2 天完成 1000 步、步长<4 分钟的 ScaleSWE 训练，以及 106B MoE
    模型的全栈生产验证。这不是日常更新，但也不是范式转移——它在智能体 RL 基础设施这个细分领域有重大意义，对局部竞争格局有重塑力。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 消息 DAG 将 trace 存储从 O(n²) 降为 O(n)，使长程智能体 rollout 训练变得实用
hype_assessment:
  level: medium
  reason: 文章整体技术描述较为扎实，给出了具体的架构设计（三层分解、拦截服务器、消息 DAG）、性能数据（6 节点 2 天完成 1000 步）和模型结果（INTELLECT-3
    106B MoE）。没有出现 '颠覆'、'革命性' 等空洞词汇。但 'overhaul'、'modern era' 等措辞有一定包装色彩，且展示的 RL Residency
    成果清单（持续学习、自动化研究、材料科学等）偏宽泛，与 verifiers v1 核心能力的关联不够紧密，有借势宣传的嫌疑。
information_entropy: high
domain_disruption:
  technical_innovation: 消息 DAG 架构是核心突破：将 trace 存储从 O(n²) 降至 O(n)，每条消息只存一次，任一 root→leaf
    路径都是可训练的连续样本，原生支持超越上下文窗口的长程训练。加上三层环境分解架构（taskset/harness/runtime）和拦截服务器实时 trace
    记录，实现了智能体 RL 训练基础设施的系统级重构。
  business_model: 开源发布 verifiers v1 可能推动智能体 RL 训练基础设施的标准化，降低长程智能体训练的技术门槛。与 PRIME-RL、Sandbox
    等工具链的集成形成完整的训练栈，有潜力成为智能体 RL 领域的类似 HuggingFace 生态的基建层。INTELLECT-3 的 106B MoE 模型展示了全栈自研能力，对云服务商和模型供应商的竞争格局产生影响。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: verifiers v1 的核心价值在于将智能体强化学习的环境堆栈重构为 taskset-harness-runtime 三层分解架构，并引入消息
    DAG 将 trace 规模从 O(n²) 降为 O(n)，从根本上解决了长程智能体 rollout 的存储和训练效率问题。这使其有潜力成为智能体 RL 训练与评估的标配基础设施。已在大规模生产环境验证（1K
    步训练在 6 个 H200 节点上 2 天完成，INTELLECT-3 在 512 块 H200 上耗时两月），技术壁垒明确。但作为 v1 版本，生态采用率和竞争对手跟进仍需观察，且
    PRIME-RL 的耦合程度可能限制其独立发展空间。综合考虑，长期复利价值较高但尚需生态验证。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- Zhipu AI
- NVIDIA
- PRIME-RL 生态项目
- 智能体 RL 研究社区
competitive_casualty:
- 传统 RL 框架（RLlib 等）
- 闭源智能体评估平台
- 缺乏自建 RL 基础设施的中小 AI 实验室
market_opportunities:
- 基于 verifiers v1 开源的三层架构（taskset/harness/runtime），AI 团队可大幅降低智能体强化学习训练基础设施的构建成本，快速复用现有任务集和评估框架，聚焦模型优化而非工程重复造轮子
- 消息 DAG 将 trace 规模从 O(n²) 降至 O(n) 的突破，使长程多模态智能体 rollout 成为可行，可催生面向复杂多轮交互场景的智能体训练数据产品与工具链创业机会
- RL Residency 在具身环境（CARLA-Env）、自动化 AI 研究、材料科学等方向的实践成果，为垂直行业（自动驾驶、科研自动化、GPU 编程教育）的
  RL 应用落地提供了可参照的技术路径
risk_matrix:
  regulatory: 智能体自主决策系统可能落入 EU AI Act 对高风险 AI 系统的监管范畴；自主代码执行环境（Docker/沙箱）若被用于攻击性操作可能引发合规审查
  technological: verifiers v1 深度集成 PRIME-RL 训练框架和特定运行时环境，若生态中出现更优替代方案或 PRIME-RL 停止维护，迁移成本较高；当前验证主要集中在
    GLM 系列模型，跨模型框架的通用性有待更多实践验证
  competitive: 头部 AI 实验室（OpenAI、DeepMind、Anthropic）均有自研的 RL 基础设施且能力封闭，verifiers v1
    作为开源方案需持续吸引社区贡献来维持竞争力，避免被巨头的主导生态边缘化
  ethical: 智能体自主编码和执行计算机操作带来了安全滥用风险（如自动生成恶意代码）；奖励黑客（reward hacking）问题虽被提及有缓解策略但仍需在实际部署中持续警惕
  additional: []
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: verifiers v1
  canonical_name: verifiers v1
  url: null
  positioning: 面向智能体强化学习和评估的环境堆栈重构方案，将环境分解为 taskset、harness 和 runtime 三层，支持编码和计算机使用等复杂智能体任务的大规模运行。
  technical_signal: 消息 DAG 轨迹系统将轨迹大小从 O(n²) 降至 O(n)，使长程智能体 rollout 和路由器回放成为可行方案。
  adoption_signal: 已用于所有内部生产运行，在 GLM-4.5-Air 模型上实现 4 分钟内完成步骤、35 轮 rollout、2 天完成 1000
    步训练。
  ecosystem_relevance: 可直接接入 PRIME-RL 进行训练，并支持复用 Harbor 等框架创建 taskset，具备良好的跨框架兼容性。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: verifiers v1 是对智能体 RL 环境堆栈的全面重构，通过三层解耦和消息 DAG 轨迹系统解决了长程 rollout 的规模瓶颈，已在内部生产中验证其高效性，是推动大规模智能体
    RL 训练的关键基础设施项目。
  risk_notes:
  - 全新架构在更广泛社区中的采用度和外部兼容性仍有待时间验证。
  - 消息 DAG 在极端复杂多智能体场景下的稳定性和可维护性尚不明确。
  score: 9.0
  article_ids:
  - 10c20df18a1fe34a
  evidence_snippets:
  - verifiers v1 是对环境堆栈的全面重构，将环境分解为 taskset、harness 和 runtime 三层，支持编码和计算机使用等复杂智能体任务。
  - verifiers v1 通过受管制的拦截服务器代理 harness 与推理服务器之间的请求，实时记录消息 DAG 轨迹以支持训练和重写。
  - verifiers v1 可直接接入 PRIME-RL 进行训练，已用于所有内部生产运行，包括 GLM-4.5-Air 模型的 ScaleSWE 任务训练。
- object_type: project
  name: PRIME-RL
  canonical_name: PRIME-RL
  url: null
  positioning: 可扩展的异步 RL 训练框架，与 verifiers、沙箱和计算编排共同构成大规模智能体模型的完整训练堆栈。
  technical_signal: 作为异步 RL 训练器，支持与 verifiers v1 环境堆栈的深度集成，能够驱动 106B 参数 MoE 模型的大规模训练。
  adoption_signal: 已作为 INTELLECT-3（106B MoE 模型）训练堆栈的核心组件，在 512 块 H200 GPU 集群上经过两个月验证。
  ecosystem_relevance: 与 verifiers 环境库、沙箱容器基础设施和计算编排系统共同构成完整的 RL 训练堆栈，是该生态的关键连接层。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: PRIME-RL 是连接 verifiers 环境堆栈与大规模模型训练的核心 RL 训练器，已成功支撑 106B 参数 MoE 模型的端到端训练，是智能体
    RL 训练基础设施的重要组件，值得持续跟踪其训练能力和生态扩展。
  risk_notes:
  - 作为与 verifiers 深度绑定的框架，其独立于该生态系统的通用性有限。
  - 在更大规模集群和更复杂多任务场景下的扩展性仍需进一步验证。
  score: 7.0
  article_ids:
  - 10c20df18a1fe34a
  evidence_snippets:
  - verifiers v1 可直接接入 PRIME-RL 进行训练，PRIME-RL 是一个可扩展的异步 RL 训练器。
  - PRIME-RL 与 verifiers、沙箱和计算编排共同构成 INTELLECT-3 的训练堆栈。
- object_type: project
  name: CARLA-Env
  canonical_name: CARLA-Env
  url: null
  positioning: 基于 CARLA 模拟器的开源具身 RL 环境，提供高保真物理、传感器和可配置城市场景，用于训练和评估决策智能体。
  technical_signal: 提供高保真物理模拟和传感器仿真，支持可配置城市场景，为具身智能体提供逼真的训练和评估环境。
  adoption_signal: 由 RL Residency Cohort I 产出，作为开源项目可为社区提供标准化的具身 RL 实验平台。
  ecosystem_relevance: 填补了开源具身 RL 环境中高保真驾驶模拟场景的空白，与 verifiers 环境库生态互补。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: CARLA-Env 将成熟的 CARLA 模拟器与 RL 训练流程深度整合，为具身智能研究提供了开箱即用的标准化环境，是 RL Residency
    的重要产出，值得关注其在具身智能领域的应用进展。
  risk_notes:
  - 基于驾驶模拟器，其场景多样性可能受限于城市交通领域，通用性有待扩展。
  - 作为 RL Residency 初期产出，项目的长期维护和社区活跃度尚待观察。
  score: 5.0
  article_ids:
  - 10c20df18a1fe34a
  evidence_snippets:
  - CARLA-Env 是一个基于 CARLA 模拟器的开源具身 RL 环境，提供高保真物理、传感器和可配置城市场景。
  - CARLA-Env 由 @myainotez 创建，是 RL Residency Cohort I 的成果之一。
- object_type: project
  name: RLM
  canonical_name: RLM
  url: null
  positioning: 一种通过持久 Python REPL 实现上下文折叠的模型架构，不依赖有损摘要，让模型主动将上下文处理委托给代码和子模型。
  technical_signal: 模型可通过持久 Python REPL 中的变量访问其输入，用代码检查和转换变量，并通过工具将部分上下文委托给子 LLM 处理。
  adoption_signal: 由 @a1zhang 于 2025 年 10 月首次提出，是 RL Residency Cohort I 的成果之一，展示了上下文折叠的新思路。
  ecosystem_relevance: 提出了一种不依赖有损摘要的上下文折叠新范式，与 verifiers 和 RL 训练生态可协同推进长上下文智能体研究。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: RLM 提出的上下文折叠方法让模型主动管理上下文而非被动处理全部输入，在长上下文场景下具有显著的理论优势，是对传统上下文处理方式的一种突破性替代方案，值得持续跟踪其实际效果和社区采用情况。
  risk_notes:
  - 该架构在处理超长上下文时的实际性能和在复杂任务中的稳定性仍有待验证。
  - 依赖子 LLM 和 Python REPL 可能引入额外的延迟和系统可靠性风险。
  score: 6.0
  article_ids:
  - 10c20df18a1fe34a
  evidence_snippets:
  - RLM 于 2025 年 10 月由 @a1zhang 首次提出，模型通过持久 Python REPL 中的变量访问其输入。
  - RLM 是一种简单灵活的上下文折叠形式，不依赖有损摘要，模型可将上下文主动委托给 Python 脚本和子 LLM。
- object_type: project
  name: Harbor
  canonical_name: Harbor
  url: null
  positioning: 一个支持创建智能体评估任务集（taskset）的框架，verifiers v1 可通过复用 Harbor 实现跨框架兼容的任务创建。
  technical_signal: 作为可被 verifiers 复用的 taskset 创建框架，支持跨框架兼容的任务集定义，降低了生态迁移成本。
  adoption_signal: null
  ecosystem_relevance: 作为 verifiers 生态中跨框架兼容的关键桥梁，降低了从其他环境框架迁移到 verifiers 的门槛。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Harbor 作为 verifiers 生态中实现跨框架兼容的桥梁组件，其复用能力直接影响社区采用 verifiers 的迁移成本，但其自身发展状态和独立生态价值尚需更多信息来评估。
  risk_notes:
  - 证据仅来自 verifiers v1 的兼容性提及，Harbor 自身的活跃度和社区基础不明。
  - 作为被引用的辅助框架，其独立价值和长期维护前景尚不清晰。
  score: 3.0
  article_ids:
  - 10c20df18a1fe34a
  evidence_snippets:
  - verifiers v1 支持通过复用 Harbor 等其他框架来创建 taskset，实现跨框架兼容。
---

Today, we are releasing verifiers v1 — an overhaul of our environment stack for the modern era of agentic RL and evals.

We decompose environments into a taskset, a harness, and a runtime.

Run complex agentic tasks like coding and computer use at scale, in any harness.

The decomposition allows you to:

- Create tasksets, either verifiers-native or by re-using other frameworks such as Harbor
- Evaluate and train in any harness (e.g. Codex, mini-SWE-agent) or bring your own
- Run them in different runtimes, e.g. subprocesses, Docker or sandboxes

For example, run Nemotron 3 Ultra on Terminal-Bench 2 under Codex by authoring a simple config and launch it with our CLI.

The central piece is the verifiers-managed interception server which proxies the requests between the harness and the inference server.

It records traces on the fly, which allows for training and rewriting (e.g. to mitigate reward hacks).

These traces are now message DAGs: every message is stored exactly once.

Now, trace sizes are O(n) in turns instead of O(n²), which makes long horizon agentic rollouts feasible, especially for router replay and multimodal data.

Rollouts aren't linear. Compaction and subagents create branches in the graph — and every root→leaf branch is a contiguous, trainable sample.

One trace, N training samples. Long-horizon training past the context window, natively.

verifiers v1 plugs straight into prime-rl for training.

We have been using v1 internally for all our production runs.

In this run, we train GLM-4.5-Air on ScaleSWE tasks with under-4-minute steps and 35-turn rollouts, completing 1K steps in 2 days on just 6 H200 nodes.

Over the past months, Cohort I of our RL Residency has been shipping.

Highlights
- continual learning
- automating AI research (from GPU programming to RL itself)
- embodied environments
- multi-agent systems
- materials science discovery

CARLA-Env – @myainotez

An open-source embodied RL environment based on the CARLA simulator. It provides high-fidelity physics, sensors, and configurable urban scenarios for training and evaluating decision-making agents.

A dataset and RL environment based on the book “Programming Massively Parallel Processors,” focused on CUDA and GPU programming skills. Includes verifiable coding exercises and a frontier eval based on it.

First introduced by @a1zhang in Oct 2025, the RLM has access to its inputs through a variable in a persistent Python REPL.

The model can inspect & transform that variable with code, and pipe parts of it into sub-LLMs with tools without ever loading the potentially huge input data into its context.

RLMs are a simple, flexible form of context folding that doesn't depend on lossy summarization.

Instead, the model proactively delegates context to:

- Python scripts (search, filter, transform)
- Sub-LLMs (fresh instances) for parallel work
- Iterative answer edits until it's actually correct

Introducing INTELLECT-3: Scaling RL to a 100B+ MoE model on our end-to-end stack

Achieving state-of-the-art performance for its size across math, code and reasoning

Built using the same tools we put in your hands, from environments & evals, RL frameworks, sandboxes & more

INTELLECT-3 is a 106B parameter Mixture-of-Experts model trained with both SFT and RL on top of the GLM 4.5 Air Base model.

Both stages, including multiple ablations, were carried out on a 512-GPU H200 cluster over the course of two months.

Our Training Stack

+ PRIME-RL: Our scalable, asynchronous RL trainer
+ Verifiers: Our unified library used for hundreds of envs and evals on the Environments Hub
+ Sandboxes: Custom container infra optimized for agentic RL
+ Compute: Orchestration & observability for 512 H200s