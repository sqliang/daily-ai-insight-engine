---
title: MSLK kernel reference (Website)
source: https://lucasb.eyer.be/lab/mslk/?utm_source=tldrai
author: []
published: ''
created: '2026-08-04'
manifest_dates:
- '2026-08-04'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: f24024981ddcc394
source_type: news_media
tldr: MSLK 是一个面向 Transformer 工作负载的融合 GPU 内核库，涵盖注意力、低精度 GEMM、量化、MoE 路由与卷积，统一通过 torch.ops.mslk.*
  接口调用，支持 CUDA 13.0 与 ROCm 7.1 两种安装方式。
objective_summary: lucasb.eyer.be 发布了 MSLK 内核参考文档。MSLK 是一组融合 GPU 内核，覆盖 fMHA 注意力、低精度
  GEMM（BF16/FP8/FP4/MX/INT）、量化、MoE 路由与卷积，统一通过 torch.ops.mslk.* 访问。库提供自动后端分发机制，并包含
  partial autograd 注意力合并、显式数据移动的 MoE 底层 API，以及 BaselineMoE 与 MetaShufflingMoE 组合模块。安装提供
  CUDA 13.0 与 ROCm 7.1 的 pip wheel。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - MSLK
  - fMHA
  - GEMM
  - FP8
  - BF16
  - MoE
  - MLA
  - CUTLASS
  - FlyDSL
  - MXFP4
  - NVFP4
  key_people:
  - Lucas Beyer
key_logic_flow:
- MSLK 是一个面向 Transformer 工作负载的融合 GPU 内核库，涵盖注意力、低精度 GEMM、量化、MoE 路由和卷积，主要通过 torch.ops.mslk.*
  访问。
- 库采用自动分发机制，根据输入 dtype、head 维度、mask、dropout 与硬件选择后端，仅在需要特定架构、paged KV、split-K 或确定性行为时才手动指定后端。
- MSLK 通过 pip 安装，提供 CUDA 13.0 与 ROCm 7.1 两种 wheel，导入 mslk 后加载 mslk.so，导入域模块后注册对应的
  torch.ops.mslk 条目。
- 注意力 API memory_efficient_attention 支持 partial autograd 与 merge_attentions_autograd，通过
  log-sum-exp 数学重加权，等效于对拼接 K/V 序列计算注意力。
- MoE 底层 API 将数据移动显式化，包括 index_shuffling、gather_scale_dense_tokens 与 scatter_add_dense_tokens，并提供
  BaselineMoE 与 MetaShufflingMoE 组合模块。
- 库内所有签名遵循统一轴约定，张量最后一维的 stride 必须为 1，变长批次打包为 B=1 并用 mask 携带序列元数据。
object_mentions:
- object_type: project
  name: MSLK
  canonical_name: MSLK
  url: https://lucasb.eyer.be/lab/mslk/
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - MSLK 是一个面向 Transformer 工作负载的融合 GPU 内核库，涵盖注意力、低精度 GEMM、量化、MoE 路由与卷积，主要通过 torch.ops.mslk.*
    接口调用。
  - MSLK 提供自动后端分发机制，可根据输入 dtype、head 维度、mask、dropout 与硬件选择内核实现，仅在需要特定架构或确定性行为时手动指定后端。
  - MSLK 的安装支持 CUDA 13.0 与 ROCm 7.1 的 pip wheel，导入 mslk 后加载 mslk.so，导入域模块后注册对应的 torch.ops.mslk
    条目。
  article_id: f24024981ddcc394
extract_result: success
impact_score:
  score: 6.5
  reason: 该库以 torch.ops.mslk.* 统一暴露注意力、低精度 GEMM（FP8/FP4/MX/INT）、量化、MoE 路由与卷积等 Transformer
    全栈融合内核，并实现基于 dtype、head 维度、mask、dropout 与硬件的自动后端分发，同时提供 CUDA 13.0 与 ROCm 7.1 双平台
    wheel——这是内核生态中少见的一站式基础设施型发布，可能改变 xFormers/FlashAttention/CUTLASS 传统分工的局部竞争格局；但当前仅是文档参考页发布，缺少公开基准数据与生产级验证，尚未到范式转移级别，故给
    6.5 分。
sentiment: positive
developer_sentiment:
  tone: skeptical
  primary_focus: 缺少公开基准，开发者关注其自动分发后端相对 FlashAttention/xFormers 的真实性能与生产成熟度
hype_assessment:
  level: low
  reason: 页面为纯技术参考文档，通篇是 API 签名、轴约定、安装命令与可运行示例，未出现'颠覆''革命性'等 PR 措辞；partial autograd
    注意力合并（log-sum-exp 重加权）与 MoE 显式数据移动等描述均有具体实现逻辑支撑，属于干货型发布，判定为低水分。
information_entropy: high
domain_disruption:
  technical_innovation: 以统一 torch.ops.mslk.* 命名空间覆盖注意力/低精度 GEMM/量化/MoE/卷积全栈，自动分发机制按
    dtype、head 维、mask、dropout 与硬件动态选择后端，避免逐算子手写调度；其中 partial autograd 注意力通过 log-sum-exp
    数学重加权合并分块结果，等价于对拼接 K/V 序列的完整注意力且保持反向传播可导，为长序列/无限上下文推理提供了新思路；MoE 底层 API 将 index_shuffling、gather/scatter
    等数据移动显式化并配 BaselineMoE 与 MetaShufflingMoE 组合模块，工程抽象层次清晰。
  business_model: 作为基础设施型开源内核库，短期不直接冲击商业模式，但以 pip wheel 按 CUDA/ROCm 分发并统一到 torch.ops
    接口的做法，可能削弱对单一厂商内核栈（如 NVIDIA CUTLASS）的绑定，推动跨硬件生态标准化；若性能经验证，可能被 PyTorch 生态吸收为 xFormers/FlashAttention
    的替代选项，重塑第三方内核库的供给格局。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 从资本视角看，MSLK 具备成为 Transformer 内核层潜在基础设施的复利逻辑：1) 覆盖面广（fMHA/低精度 GEMM/量化/MoE/卷积），且统一通过
    torch.ops.mslk.* 接入，与 PyTorch 生态天然兼容，降低开发者迁移成本；2) 同时提供 CUDA 13.0 与 ROCm 7.1 双平台
    wheel，踩中 AMD 软件生态补强的结构性趋势，有望在 NVIDIA 之外的第二极硬件生态中建立心智；3) 作者 Lucas Beyer 学术信誉强，偏研究驱动的内核库往往能借论文/社区传播获得早期采纳。但内核层的长期复利价值高度依赖生态采用率，当前
    FlashAttention/Triton/CUTLASS 已深度嵌入 PyTorch 原生路径与各大推理引擎，MSLK 既无明确的性能基准优势证明，也面临被既有库吸收或
    PyTorch 原生内核替代的风险。因此判定为'有潜力成为细分基础设施但需持续验证'的 6 分档位，而非 8 分以上基石级——核心变量在于未来 12 个月能否进入
    vLLM/主流训练框架的默认内核路径。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- MSLK 项目
- PyTorch
- AMD
- vLLM
- 开源大模型开发者
competitive_casualty:
- 专有内核优化商业公司
- FlashAttention 注意力内核独占地位
- NVIDIA CUDA 软件锁定优势
market_opportunities:
- AI 基础设施团队可评估将 MSLK 的 FP4/MXFP4 等低精度量化 GEMM 与注意力内核集成到自有推理栈，其自动后端分发机制（按 dtype/head
  维度/硬件选后端）可作为低精度推理引擎的参考设计，降低端侧与数据中心推理的显存与算力成本
- MoE 路由与数据移动的底层 API（index_shuffling/gather_scale_dense_tokens/scatter_add_dense_tokens）为
  DeepSeek、Mixtral 类稀疏模型的高效推理提供了可直接借鉴的实现范式，是构建 MoE 推理服务或 token 调度优化器的良好起点
- ROCm 7.1 官方 wheel 意味着 AMD GPU 生态可获得与 CUDA 对等的融合内核能力，在国产芯片与异构算力选型背景下，值得评估其作为跨平台低精度推理后端的可行性
risk_matrix:
  regulatory: 内核库属于基础软件，短期无直接监管风险；但需留意 FP8/FP4 等低精度算力内核若被纳入芯片级出口管制或高端算力限制，其分发与使用存在潜在合规变量
  technological: 该库为个人实验室项目（lucasb.eyer.be），非 PyTorch 官方产品，维护可持续性存疑；且正面竞争 FlashAttention、NVIDIA
    CUTLASS、OpenAI Triton 及 PyTorch 原生 SDPA/低精度支持，一旦官方标准内核性能追平或 Triton 生态成熟，MSLK 可能被边缘化
  competitive: 融合内核赛道竞争极为激烈：CUTLASS、Triton、vLLM PagedAttention、FlashMLA 等均提供同类能力且生态更成熟、有大厂背书；MSLK
    缺乏社区基础与生产验证，存在被生态挤压的显著风险
  ethical: 无
  additional:
  - 未明确开源许可证与商业支持路径，生产环境采用存在法律与长期维护风险
  - 高度依赖特定硬件架构（如 Hopper/Blackwell 的 FP8/FP4/MXFP4 指令集），对长尾 GPU 与消费级硬件的兼容性尚未验证
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: MSLK
  canonical_name: MSLK
  url: https://lucasb.eyer.be/lab/mslk/
  positioning: 定位为面向 Transformer 工作负载的融合 GPU 内核库，覆盖注意力、低精度 GEMM、量化、MoE 路由与卷积，通过 torch.ops.mslk.*
    统一访问。
  technical_signal: 以自动后端分发为核心，根据输入 dtype、head 维度、mask 与硬件选择内核，并提供 partial autograd
    注意力合并与显式数据移动的 MoE 底层 API。
  adoption_signal: 提供 CUDA 13.0 与 ROCm 7.1 双平台 pip wheel 安装，可同时覆盖 NVIDIA 与 AMD GPU，降低内核库接入与评估门槛。
  ecosystem_relevance: 以 torch.ops.mslk.* 形式深度集成 PyTorch 生态，覆盖注意力、低精度 GEMM、量化、MoE
    路由与卷积等主流 Transformer 组件。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 自动后端分发与手动指定后端的双模式设计，配合 partial autograd 注意力合并及 MetaShufflingMoE，构成区别于常规内核库的工程特色。
  watch_reason: MSLK 以统一 torch.ops.mslk.* 接口覆盖 Transformer 关键工作负载，自动后端分发、partial autograd
    注意力合并与 MetaShufflingMoE 设计具有工程参考价值，值得持续跟踪其在 CUDA 13.0 与 ROCm 7.1 双平台下的演进。
  risk_notes:
  - 作为个人实验室项目，维护持续性、版本稳定性与上游依赖风险尚不明确。
  - 参考文档未披露基准测试与性能对比数据，实际加速收益有待独立验证。
  score: 7.0
  article_ids:
  - f24024981ddcc394
  evidence_snippets:
  - MSLK 是一个面向 Transformer 工作负载的融合 GPU 内核库，涵盖注意力、低精度 GEMM、量化、MoE 路由与卷积，主要通过 torch.ops.mslk.*
    接口调用。
  - MSLK 提供自动后端分发机制，可根据输入 dtype、head 维度、mask、dropout 与硬件选择内核实现，仅在需要特定架构或确定性行为时手动指定后端。
  - MSLK 的安装支持 CUDA 13.0 与 ROCm 7.1 的 pip wheel，导入 mslk 后加载 mslk.so，导入域模块后注册对应的 torch.ops.mslk
    条目。
---

### Fused attention

Begin with automatic dispatch. Reach for explicit backends only when you need a specific architecture, paged KV layout, split-K, or deterministic behavior.

Start with memory_efficient_attention →MSLK is a library of fused GPU kernels for transformer workloads: attention, low-precision GEMM, quantization, MoE routing, and convolution. Most of it is reached through `torch.ops.mslk.*`

after `import mslk`

. This page documents every public surface and, more importantly, tells you which one to call.

Pick the workload family you are working on — each card opens the reference filtered to that domain.

fMHA dispatch, masks, architecture backends, tree attention, MLA, and paged KV.

BF16, FP8, FP4, MX, INT8, INT4, grouped, batched, and mixed-input matrix products.

FP8, MXFP4, NVFP4, INT4, and MXFP6 conversion, packing, scaling, and dequantization.

Routing, shuffling, token gather/scatter, fused activations, and composed expert layers.

The low-precision FP8 3D convolution operator.

FlyDSL JIT/AOT, device detection, architecture checks, and test decorators.

Kernel launch, accessors, tuning, timing, source context, and CUTLASS helpers.

Begin with automatic dispatch. Reach for explicit backends only when you need a specific architecture, paged KV layout, split-K, or deterministic behavior.

Start with memory_efficient_attention →Choose a scale granularity that matches the GEMM family: tensor, row, block, group, MXFP4, NVFP4, or packed INT4.

See the FP8 pipeline →MSLK exposes the routing pieces independently and also includes baseline and Meta-shuffling MoE layers for composed execution.

Open the routing API →These are deliberately small, copyable paths through the major public surfaces.

```
# CUDA 13.0 wheel
pip install mslk --index-url https://download.pytorch.org/whl/cu130
# ROCm 7.1 wheel
pip install mslk \
--index-url https://download.pytorch.org/whl/rocm7.1/ \
--extra-index-url https://pypi.org/simple
```


`import mslk`

loads `mslk.so`

. Import a domain such as `mslk.gemm`

or `mslk.moe`

before calling its `torch.ops.mslk`

entries so Python-side registrations are installed.

```
import torch
from mslk.attention import fmha
B, M, H, K = 2, 2048, 32, 128
q = torch.randn(B, M, H, K, device="cuda", dtype=torch.bfloat16)
k = torch.randn_like(q)
v = torch.randn_like(q)
out = fmha.memory_efficient_attention(
q, k, v,
attn_bias=fmha.LowerTriangularMask(),
)
# out: [B, M, H, K]
```


Automatic dispatch evaluates the input dtype, head dimension, mask, dropout, gradient requirements, and hardware. Supply `op=(FwOp, BwOp)`

only when deliberately pinning a backend.

```
import torch
import mslk.gemm
from mslk.quantize.triton.fp8_quantize import quantize_fp8_row
x = torch.randn(1024, 4096, device="cuda", dtype=torch.bfloat16)
w = torch.randn(4096, 4096, device="cuda", dtype=torch.bfloat16)
xq, x_scale = quantize_fp8_row(x)
wq, w_scale = quantize_fp8_row(w)
out = torch.ops.mslk.f8f8bf16_rowwise(
xq, wq, x_scale, w_scale
)
# Conceptually: dequant(xq) @ dequant(wq).T → BF16
```


Most MSLK GEMMs take activations `[M,K]`

and weights `[N,K]`

, then compute `X @ W.T`

. Keep scale layout paired with the quantizer that produced it.

```
import torch
from mslk.attention.fmha.merge_training import (
memory_efficient_attention_partial_autograd,
merge_attentions_autograd,
)
B, Mq, Mkv, H, K = 1, 128, 1024, 16, 128
q = torch.randn(B, Mq, H, K, device="cuda", dtype=torch.bfloat16)
k = torch.randn(B, Mkv, H, K, device="cuda", dtype=torch.bfloat16)
v = torch.randn_like(k)
k0, k1 = k.chunk(2, dim=1)
v0, v1 = v.chunk(2, dim=1)
p0 = memory_efficient_attention_partial_autograd(q, k0, v0)
p1 = memory_efficient_attention_partial_autograd(q, k1, v1)
out = merge_attentions_autograd(p0, p1)
```


Each partial carries its output and log-sum-exp. The merge reweights chunks mathematically, so it is equivalent to attention over the concatenated K/V sequence.

```
import torch
import mslk.moe
T, D, E = 256, 512, 8
x = torch.randn(T, D, device="cuda", dtype=torch.bfloat16)
routing_scores = torch.softmax(
torch.randn(T, E, device="cuda"), dim=-1
)
counts, experts, tokens = torch.ops.mslk.index_shuffling(
routing_scores, top_k=1
)
expert_x = mslk.moe.gather_scale_dense_tokens(
x, tokens, experts, routing_scores
)
# Replace this identity with grouped expert GEMMs + silu_mul.
expert_y = expert_x
out = torch.zeros_like(x)
mslk.moe.scatter_add_dense_tokens(out, expert_y, tokens)
```


The low-level API makes data movement explicit. For a composed module, use `BaselineMoE`

or the top-1-only `MetaShufflingMoE`

.

Every signature on this page describes tensors with the same axis letters. Learn them once and the 400 entries below stop needing individual explanation. One rule holds nearly everywhere: **the last dimension must have stride 1**, even when the others are non-contiguous.

Variable-length batches are packed into `B=1`

with sequence metadata carried by the mask instead.