---
title: Escha-W2 (Hugging Face Repo)
source: https://huggingface.co/EschaLabs/Qwen3.6-35B-A3B-Escha-W2?utm_source=tldrai
author: []
published: ''
created: '2026-07-31'
manifest_dates:
- '2026-07-31'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 9ce5f79345d7e2e5
source_type: news_media
tldr: Escha-W2 是 Qwen3.6-35B-A3B（256 专家 MoE）的 2-bit 量化版本，磁盘占用 12.3GB，可在 16GB/24GB
  消费级 GPU 上通过 OpenAI 兼容 API 本地部署。文章详细对比 sglang 与 zml 两套运行时引擎的性能差异，并给出安装与验证步骤。
objective_summary: EschaLabs 发布了 Escha-W2，这是 Qwen3.6-35B-A3B（256 专家 MoE 模型）的 2-bit
  量化构建，打包为可通过 OpenAI 兼容 /v1 API 在本地服务的模型仓库。模型磁盘占用 12.3GB，最低可在 16GB 显存显卡上运行，官方推荐使用
  24GB 显存；模型与运行时分仓存放，提供 sglang 和 zml 两套引擎。2026-07-27 的实测显示 zml 在长回答贪心解码下比 sglang 快
  2-26%，但温度大于 0 时性能明显下降。文章还说明 torch 必须固定 2.9.x 等安装要点与堆栈自检命令。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - EschaLabs
  - Hugging Face
  technologies:
  - MoE
  - 2-bit quantization
  - SGLang
  - ZML
  - OpenAI-compatible API
  - CUDA
  key_people: []
key_logic_flow:
- Escha-W2 是 Qwen3.6-35B-A3B 的 2-bit 量化版本，采用 256 专家的 MoE 架构，磁盘占用 12.3GB，最低可在 16GB
  显存显卡上运行，24GB 显存为官方推荐配置。
- 模型通过 OpenAI 兼容的 /v1 HTTP API（端口 30000）对外提供服务，运行时与模型分仓存放，独立仓库为 EschaLabs/escha-runtime-qwen3moe。
- 官方建议默认使用 sglang 引擎，因为它是页面所有性能数字的测量基准，也是唯一支持并发、工具调用、结构化输出和推理解析器的引擎。
- zml 引擎为单二进制运行时，无需任何 Python 环境，在长回答（≥1k tokens）贪心解码下比 sglang 快 2-26%，但温度大于 0 时解码反而慢约
  2.15 倍，且需要 24GB 显存。
- 安装时 torch 必须固定为 2.9.x 版本，否则 escha._C 因 ABI 链接 libtorch 会出现 undefined symbol 错误，启动前需用两条
  sanity-check 命令验证堆栈。
object_mentions:
- object_type: model
  name: EschaLabs/Qwen3.6-35B-A3B-Escha-W2
  canonical_name: Escha-W2
  url: https://huggingface.co/EschaLabs/Qwen3.6-35B-A3B-Escha-W2
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Escha-W2 是 Qwen3.6-35B-A3B 的 2-bit 量化构建，采用 256 专家的 MoE 架构，磁盘占用 12.3GB，可在 16GB
    或 24GB 消费级 GPU 上本地运行。
  - 该仓库只存放模型权重，运行时位于独立仓库 EschaLabs/escha-runtime-qwen3moe，通过 OpenAI 兼容的 /v1 API 在端口
    30000 提供服务。
  article_id: 9ce5f79345d7e2e5
- object_type: project
  name: EschaLabs/escha-runtime-qwen3moe
  canonical_name: Escha runtime
  url: https://huggingface.co/EschaLabs/escha-runtime-qwen3moe
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 运行时仓库 EschaLabs/escha-runtime-qwen3moe 按引擎分目录，sglang/ 目录包含 escha wheel 和 serve.sh
    启动脚本，负责服务并发与结构化输出。
  - zml/ 目录提供单二进制运行时，无需 Python 和 CUDA 工具包，14 秒即可完成安装，适合单用户生成长回答的场景。
  article_id: 9ce5f79345d7e2e5
- object_type: project
  name: SGLang
  canonical_name: SGLang
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 官方建议默认使用 sglang 引擎，它是页面所有性能数字的测量基准，也是唯一支持并发、工具调用、结构化输出和推理解析器的引擎。
  article_id: 9ce5f79345d7e2e5
- object_type: project
  name: ZML
  canonical_name: ZML
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - zml 引擎是单二进制运行时，无需 Python 环境，在长回答贪心解码上比 sglang 快 2-26%，但温度大于 0 时解码速度反而慢约 2.15 倍。
  article_id: 9ce5f79345d7e2e5
- object_type: model
  name: Qwen3.6-35B-A3B
  canonical_name: Qwen3.6-35B-A3B
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - Escha-W2 的基础模型是 Qwen3.6-35B-A3B，一款拥有 256 个专家的 MoE 模型，量化后整个模型在磁盘上仅占 12.3GB 空间。
  article_id: 9ce5f79345d7e2e5
extract_result: success
impact_score:
  score: 5.0
  reason: 先看依据：该事件把 35B 级 256 专家 MoE 模型通过 2-bit 超低位量化压缩到 12.3GB，使 16GB/24GB 消费级显卡可本地推理，并配套
    OpenAI 兼容 API 与 sglang/zml 双运行时，附带跨显卡的详细实测数据，属于工程完成度很高的实用化发布，会在本地推理圈层显著改变工具选型与部署门槛。但它并非范式转移——超低位量化与
    MoE 稀疏激活结合此前已有 Qwen Escha 等系列探索，且 2-bit 质量损失、zml 仅贪心解码加速（温度>0 反而慢 2.15 倍）、16GB
    显存下长上下文 HTTP 500 等局限使其短期难以重塑整体行业格局。综合评定为中等偏上的行业冲击力。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 2-bit 量化质量损失与本地部署便捷性之间的权衡，以及 sglang/zml 双运行时的选型复杂度
hype_assessment:
  level: low
  reason: 通篇是硬核实测数据与工程细节：不同显卡（5090/3090/5080/4090/5060 Ti）上 2-26% 的加速差异、温度>0 时 2.15
    倍降速、16GB 显存长提示词的 HTTP 500 阈值、torch 必须锁定 2.9.x 否则 ABI 静默失败的坑、两条 sanity-check 命令等。作者主动披露
    zml 贪心解码独占、sglang 全功能等限制，无'颠覆''革命'等 PR 滥用词汇，属于实打实的技术文档，水分极低。
information_entropy: high
domain_disruption:
  technical_innovation: 对 256 专家 MoE 采用 2-bit 专家权重 + 混合 2/3-bit 投影 + int8 稠密层的超低位量化方案（eschamoe），将
    35B 级模型压至 12.3GB，使 16GB 消费级显卡即可本地推理；zml 运行时以单二进制、零 Python 依赖形态将 16 步解码融合为单次 GPU
    派发并在设备端做 argmax，在贪心解码长回答场景实现最高 26% 的加速，是本地推理工程层面的实质性突破。
  business_model: 强化'本地优先'推理路线：以 OpenAI 兼容 API 将迁移成本降到最低，让个人开发者与企业可用消费级/入门级 GPU 私有化部署中型
    MoE，省去按 token 计费的云端 API 费用并规避数据出境风险；长期看可能分流部分云端推理收入，同时壮大 sglang/zml 等开源运行时的本地部署生态。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: 该事件代表'本地化推理民主化'这一长期趋势的关键一步：将 35B 级 256 专家 MoE 模型以 2-bit 量化压到 12.3GB、在 16/24GB
    消费级显卡上跑通 OpenAI 兼容 API，具备真实的复利底色——每一代新基座模型都会催生更极致的量化与运行时优化，技术资产可沿'更小、更快、更省显存'的方向持续累积。但需清醒认识到其局限：其一，Escha-W2
    是典型的'点状产品'，量化产物随 Qwen 基座迭代快速过期，模型构建本身不具备持久价值，真正的沉淀在量化方法与运行时（eschamoe/SGLang/ZML）而非该仓库；其二，2-bit
    量化在温度>0 采样下性能显著劣化、16GB 卡上长提示易 500，质量与稳定性折损限制了其大规模替代云端 API 的可能；其三，赛道拥挤且进入门槛低，GGUF/AWQ/llama.cpp
    等既有生态已建立用户习惯，EschaLabs 的执行力虽强但技术护城河并不排他。综合判断：有潜力成为本地推理生态的基础组件之一，但需持续验证其能否把'先发效率优势'转化为'生态锁定'，故落于
    4-7 分区间中位。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- EschaLabs
- Alibaba (Qwen)
- SGLang
- ZML
- Hugging Face
- NVIDIA 消费级 GPU 生态
competitive_casualty:
- 云端开源模型推理服务商（Together AI、Fireworks、Groq 类轻负载场景）
- 传统量化工具链（bitsandbytes、GGUF 的同类替代场景）
- 中小型闭源 API 提供商
market_opportunities:
- 创业者可借鉴 2-bit 极端量化 + MoE 的技术路线，为金融、医疗、法律等数据敏感行业提供在消费级 GPU（16GB/24GB 显存）上本地化私有部署的开源模型方案，满足数据不出域的合规需求
- zml 单二进制、免 Python 环境、14 秒安装的运行时设计思路，可迁移到边缘计算设备、AI 一体机或军工/政务等离线环境推理产品，是嵌入式本地推理的差异化切入点
- 模型仓库与运行时分仓存储 + OpenAI 兼容 /v1 API 的发布模式，使现有 OpenAI API 应用可零改动切换到本地推理，围绕该模式构建本地推理托管与管理工具链存在商业机会
risk_matrix:
  regulatory: 模型基于 Apache-2.0 的 Qwen3.6 开源底座，但 2-bit 量化构建是否完整履行上游再分发与许可条款义务需核实；面向终端应用落地时需留意
    AI Act 等对开源权重模型及衍生服务的透明度义务，以及未来开源模型出口管制收紧的政策风险
  technological: 2-bit 极端量化（2-bit experts + int8 dense）存在显著精度损失与安全对齐弱化风险；torch 必须锁定
    2.9.x 的 ABI 依赖是脆弱工程约束，wheel 未声明 torch 依赖导致裸装即失败，可复现性差；eschamoe 为私有量化格式，面临被 GGUF/AWQ/GPTQ
    等主流生态格式取代的架构淘汰风险
  competitive: Ollama/llama.cpp/vLLM 等成熟生态已覆盖消费级 GPU 量化推理场景，Escha-W2 只能靠 2-bit 极致压缩差异化；zml
    引擎在 temperature>0 时解码慢约 2.15 倍、16GB 显存下长提示词直接返回 HTTP 500，短板明显；Qwen 底座快速迭代会使该定制构建快速过时
  ethical: 2-bit 量化可能显著削弱模型的安全对齐与拒答能力，叠加本地部署绕过 API 层内容审核，放大了有害内容生成、偏见放大与深度伪造误用风险；极端量化引入的精度损失还可能加剧对特定群体的歧视性输出
  additional:
  - 安装链路脆弱（torch 需手动固定 2.9.x，two multi-GB 下载后仍可能 import 失败），对非专业用户门槛过高
  - 文章脚注 [^ctxcap] 暗示 16GB 显存下并发与上下文存在未充分披露的取舍细节，技术文档完整性存疑
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
---

**Escha-W2** is a 2-bit quantized build of **Qwen3.6-35B-A3B**, a Mixture-of-Experts model with
256 experts, packaged with everything needed to serve it locally through an **OpenAI-compatible
HTTP API**. (The runtime and served model id keep the `escha`

name — see *Connecting a client*.)

The whole thing is **12.3 GB on disk** and runs on a **single 24 GB consumer GPU** — or on a
**16 GB card** (e.g. RTX 5060 Ti), where you trade concurrency *or* context, not both[^ctxcap].

| Base model | Qwen3.6-35B-A3B (MoE, 256 experts) |
| Quantization | 2-bit experts (`eschamoe` ; mixed 2/3-bit per projection), int8 dense layers |
| Size on disk | 12.3 GB |
| Minimum GPU | 16 GB VRAM (fewer concurrent streams, or less context — your choice[^ctxcap]), 24 GB recommended; NVIDIA Ampere (sm_80) → Blackwell (sm_120) |
| Platform | Linux x86-64, glibc ≥ 2.28 (Ubuntu 20.04+, RHEL/Rocky/Alma 8+) — the wheel is `manylinux_2_28` |
| CUDA | an NVIDIA driver — no CUDA toolkit needed (`ptxas` ships inside `triton` , a PyTorch dependency) |
| Python | 3.12, for the SGLang engine — the ZML engine needs no Python at all |
| Interface | OpenAI-compatible `/v1` on port 30000 |

| Path | What it is |
|---|---|
`*.safetensors` , `config.json` , `tokenizer*` , `vocab.json` , `merges.txt` , `*.jinja` |
the quantized weights, tokenizer and config (at the repo root) |
`opencode.json` |
example client config (see Connecting a client) |
`LICENSE` , `THIRD_PARTY_LICENSES/` |
Apache-2.0 plus the upstream license texts |

This repo holds **only the model**. The **runtimes** live in a separate repo,
**EschaLabs/escha-runtime-qwen3moe**,
one directory per engine: ** sglang/** (the

`escha`

wheel + `serve.sh`

— servers, concurrency,
tools, structured output) and **(a single-binary runtime — no Python, no dependencies, and the stronger single-user decode on most cards).**

`zml/`

**Use sglang/ unless you have a specific reason not to.** It is the engine every number on this
page was measured with, and the only one that supports concurrency, tool calling, structured
output and a reasoning parser.

Choose `zml/`

when you want a single-user box with **no Python at all** — one binary, no venv, no
CUDA toolkit, 14-second install — and you generate long answers. It is the only way to run this
model without a Python environment.

Two things decide whether that trade is worth it on **your** box:

**The decode lead is card-dependent.**On answers ≥1k tokens, measured 2026-07-27:**+15–26%**on an RTX 5090 or 3090,**+8–11%**on a 5080,**+2–5%**on a 4090 (against a fully tuned SGLang), and a**tie**on a 5060 Ti. ZML loses short replies on every card, and its first start compiles graphs — 75–145 s on a 4090, minutes on a 16 GB card, versus ~33 s for SGLang.**The lead is greedy-only.**ZML's fast path fuses 16 decode steps per GPU dispatch with on-device argmax; any`temperature > 0`

falls back to a per-token loop that is**~2.15× slower**(RTX 4090, 512-token answer: 218 tok/s at`temperature: 0`

vs 102 at`0.6`

). Send`"temperature": 0`

for the quoted numbers — including with`opencode.json`

below, which sets`0.6`

. The SGLang engine samples at full speed.


ZML needs the 24 GB it asks for.On 16 GB cards long prompts fail with an opaque`HTTP 500`

— measured from ≥2,048 tokens on a 5080 and from ~700 on a 5060 Ti. Use the SGLang engine on 16 GB.

Install the **Escha runtime** first (wheel +
`serve.sh`

+ full detail, including two requirements that fail **quietly**) — then download this
model and serve it:

```
python3.12 -m venv .venv && source .venv/bin/activate
pip install -U pip wheel "huggingface_hub[cli]"
# torch MUST be pinned to 2.9.x. `escha._C` is ABI-linked to libtorch and the wheel does
# NOT declare a torch dependency, so a bare ">=2.9.0" resolves to 2.11 and `import escha`
# then fails with `undefined symbol: _ZN3c10...` — after two multi-GB downloads.
pip install "torch==2.9.*" --index-url https://download.pytorch.org/whl/cu128
# 1. runtime: fetch the sglang/ engine dir (wheel + serve.sh), install the wheel.
# Use the glob — a pinned wheel filename goes stale on every rebuild.
hf download EschaLabs/escha-runtime-qwen3moe --include "sglang/*" --local-dir .
pip install ./sglang/escha-*.whl # pulls transformers>=5.8 + the full dep closure
# 2. this model:
hf download EschaLabs/Qwen3.6-35B-A3B-Escha-W2 --local-dir ./escha-w2
MODEL=./escha-w2 bash sglang/serve.sh
```


Sanity-check the stack before serving — the first line must print three `True`

s:

```
python -c "import torch, escha, sglang; print(torch.cuda.is_available(), hasattr(torch.ops.escha, 'escham_moe_linear'), bool(sglang.__version__))"
python -c "import escha; print(escha.__version__)" # paste this into bug reports
```


Then:

```
curl -s http://127.0.0.1:30000/v1/models | python3 -m json.tool
```