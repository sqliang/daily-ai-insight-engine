---
title: Microsoft's Full-bandwidth Transformers (26 minute read)
source: https://arxiv.org/abs/2608.08888?utm_source=tldrai
author: []
published: ''
created: '2026-08-18'
manifest_dates:
- '2026-08-18'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 76f2082937a5889f
source_type: news_media
tldr: 微软在 arXiv 发表论文提出全带宽变换器（Full-bandwidth Transformer）：每个解码步骤将上一步的顶层隐状态与采样 token
  嵌入经门控线性单元融合后反馈为下一步输入，从而加宽垂直反馈通道。1B 参数模型最高训练 400B tokens，在验证损失、数学与代码生成、指令微调等任务上优于标准
  Transformer，且每 token 解码开销可忽略。
objective_summary: 微软在 arXiv 发表论文提出全带宽变换器（Full-bandwidth Transformer）。论文指出传统自回归 Transformer
  的垂直反馈通道过窄，仅采样 token 返回栈底而顶层隐状态被丢弃。该方法通过潜变量反馈，将上一步顶层隐状态与采样 token 嵌入经门控线性单元融合后作为下一步输入，同时保留标准架构、KV
  缓存和语言建模目标。为不破坏并行教师强制训练，作者采用调度式多遍目标函数，在预训练后期引入反馈并混合少量更深反馈遍。研究者在 1B 参数模型上最高训练 400B
  tokens，结果显示该结构在验证损失、5-shot 评估、数学与代码生成、指令微调上均优于标准 Transformer，能以约 1.5 倍更少的训练 token
  达到相近性能，并能生成更短的推理轨迹。
event_type: infrastructure_update
epistemic_status: theoretical_claim
entities:
  companies:
  - Microsoft
  technologies:
  - Full-bandwidth transformer
  - Latent feedback
  - Gated linear unit
  - KV cache
  key_people: []
key_logic_flow:
- 自回归 Transformer 沿水平（跨生成 token）和垂直（穿透模型深度）两个轴计算，密集注意力提供了广泛的水平历史访问，但垂直反馈通道狭窄，仅采样 token
  返回栈底而顶层隐状态被丢弃。
- 全带宽变换器通过潜变量反馈加宽垂直通道：每个解码步骤将上一层的顶层隐状态与采样 token 嵌入经门控线性单元融合后作为下一步输入。
- 为保留并行教师强制训练，作者设计调度式多遍目标函数，在预训练后期引入潜变量反馈，并混合一小部分更深反馈遍以稳定训练。
- 作者训练了 1B 参数的全带宽变换器，最高达 400B tokens，发现潜变量反馈改善了验证损失、5-shot 语言模型评估、数学与代码生成以及指令微调性能。
- 以可忽略的每 token 解码开销，全带宽变换器匹配或接近用约 1.5 倍更多 token 训练的标准变换器，并能以相同或更好的精度生成更短的推理轨迹。
object_mentions:
- object_type: paper
  name: Full-bandwidth transformer
  canonical_name: Full-bandwidth transformer
  url: https://arxiv.org/abs/2608.08888
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 全带宽变换器在每个解码步骤将上一层的顶层隐状态与采样 token 嵌入经门控线性单元融合后反馈为下一步输入，从而加宽垂直反馈通道。
  - 作者训练了 1B 参数的全带宽变换器，最高达 400B tokens，发现潜变量反馈改善了验证损失、数学与代码生成以及指令微调性能。
  - 以可忽略的每 token 解码开销，全带宽变换器能匹配或接近用约 1.5 倍更多 token 训练的标准变换器，并能生成更短的推理轨迹。
  article_id: 76f2082937a5889f
extract_result: success
impact_score:
  score: 7.0
  reason: 该论文提出了自回归 Transformer 架构层面的实质改进——通过潜变量反馈加宽垂直信息通道，在保留标准架构、KV 缓存与语言建模目标的前提下，实现约
    1.5 倍训练 token 效率提升且解码开销可忽略。若结论在更大规模上成立，将直接改变大模型预训练的成本结构，属于可被主流训练框架快速吸收的架构级进展，足以影响局部竞争格局；但它仍是单篇论文、仅
    1B 参数规模验证、未发布代码与模型，尚未达到范式转移级别，因此评分落在重要进展区间而非颠覆区间。综合评定 7.0 分。
sentiment: positive
developer_sentiment:
  tone: skeptical
  primary_focus: 调度式多遍训练目标的复杂度，以及 1B 规模结论能否在更大模型与更长训练上稳定复现
hype_assessment:
  level: low
  reason: 论文措辞克制严谨，使用'match or approach'等限定性表述而非夸大承诺，报告了验证损失、5-shot 评估、数学/代码生成与指令微调等多维度的真实对比实验，并诚实披露了为保留并行教师强制而引入的调度式多遍训练这一训练复杂度代价。标题'Full-bandwidth'虽有概念包装色彩，但整体属于实打实的技术干货，未见'颠覆''革命性'等
    PR 滥用词汇。判定为低炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 核心突破在于拓宽自回归解码的垂直反馈通道：将上一步顶层隐状态与采样 token 嵌入经门控线性单元融合后作为下一步输入，使未显式言说的计算能带着全新的深度预算重新进入堆栈，同时通过调度式多遍目标函数在预训练后期引入潜变量反馈以维持并行教师强制训练。这一机制为模型提供了跨解码步骤的循环计算路径，也解释了为何能生成更短的推理轨迹——非显式计算不必全部摊在单步深度上完成。
  business_model: 约 1.5 倍训练 token 效率提升直接对应预训练算力成本的显著下降，且由于保留标准 KV 缓存与架构，现役推理服务栈无需改造即可部署，降低了采纳门槛。若该机制被主流开源训练框架或云厂商内置，可能改变大模型训练的成本结构与定价空间，对依赖算力投入的头部玩家构成效率竞赛压力。
engineering_complexity: prototype
compound_value:
  score: 5.5
  reason: 从 VC 视角拆解：1) 价值本质——全带宽变换器是训练效率型架构改进，宣称以约 1.5× 更少 token 达到相当性能，对应前沿模型训练端数千万美元算力开支的缩减，具备明确的经济价值；2)
    复利路径——若被主流模型厂商采纳并标准化（类比 RoPE、GQA 的演进路径），将沉淀为行业级训练技术，价值随时间累积；3) 核心风险——当前仅在 1B/400B
    token 规模验证，未触及 10B+ 前沿训练，独立复现缺失，且调度式多遍目标函数增加训练流水线复杂度，大厂采纳意愿存疑；4) 格局判断——属于'效率补丁'而非颠覆性新平台，即便落地也是存量玩家受益而非催生新物种。综合评分处于'有潜力成为细分基建但需持续验证'区间，尚未到
    8 分以上的基石级复利强度。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- Microsoft
- OpenAI
- Anthropic
- Google DeepMind
- Meta AI
- DeepSeek
competitive_casualty:
- 依赖专有高效训练架构构建护城河的模型厂商
- 以长推理轨迹为差异化卖点的推理产品/服务商
market_opportunities:
- 模型训练团队可深入研究该潜变量反馈机制，将其作为降低预训练 token 消耗的手段，对算力预算有限的中小团队有望带来约 1.5 倍的训练成本节约
- 推理服务与边缘部署供应商可关注其生成更短推理轨迹且精度相当的特性，用于降低推理延迟与每 token 成本，在长思维链推理和 Agent 多步决策场景具备商业价值
- AI Infra 与训练框架厂商可预研对调度式多遍目标函数和潜变量反馈的原生工程支持，抢占下一代高效 Transformer 架构工程化的先发优势
risk_matrix:
  regulatory: 无
  technological: 论文仅验证 1B 参数规模，未证明可在更大模型上 scale，存在规模外推失败风险；调度式多遍目标函数增加训练复杂度并可能带来训练不稳定；未发布代码与复现实验，理论声称尚未经同行评审
  competitive: 若头部大厂（尤其微软）将该技术快速内化至闭源模型，开源社区可能形成代差；训练效率提升若被广泛采用将降低模型训练门槛，加剧同质化竞争与价格战
  ethical: 潜变量反馈使更多计算发生在不可口语化的隐状态通道中，可能降低推理过程的可解释性与可审计性；对深层隐状态的依赖增强可能放大训练数据中的隐蔽偏差
  additional:
  - 复现风险：未发布代码与权重，实际工程集成（训练框架支持、分布式并行适配）成本未知
  - 调度式多遍训练的超参敏感性可能显著增加实际采用的工程复杂度与调参周期
confidence:
  impact: medium
  compound: low
  hype: low
actionable_insight: deep_dive
---

# Computer Science > Artificial Intelligence

# Title:Full-bandwidth transformer

View PDF HTML (experimental)Abstract:Autoregressive transformers compute along two axes: horizontally across generated tokens, and vertically through model depth. Dense attention gives each token broad horizontal access to the past, but the vertical feedback channel between decoding steps remains narrow: only the sampled token returns to the bottom of the stack, while the top-layer hidden state is discarded. We introduce the \emph{full-bandwidth transformer}, which widens this channel with \emph{latent feedback}: at each decoding step, the previous top-layer hidden state is fused with the sampled token embedding through a gated linear unit and fed back as the next input. Latent feedback lets non-verbalized computation re-enter the stack with a renewed depth budget, while preserving the standard transformer architecture, KV cache, and language-modeling objective. To train full-bandwidth transformers without losing parallel teacher forcing, we use a scheduled multi-pass objective that introduces latent feedback late in pretraining and mixes a small fraction of deeper feedback passes for stability. We train 1B-parameter full-bandwidth transformers up to 400B tokens and find that latent feedback improves validation loss, 5-shot language-model evaluation, math and coding generation, and instruction-tuned performance. With negligible per-token decoding overhead, full-bandwidth transformers match or approach standard transformers trained with roughly $1.5\times$ more tokens, and manage to produce shorter reasoning traces at equal or better accuracy.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.