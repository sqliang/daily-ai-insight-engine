---
title: Graphsignal (GitHub Repo)
source: https://github.com/graphsignal/graphsignal-profiler?utm_source=tldrai
author: []
published: ''
created: '2026-06-25'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: ba63161e45fe82c7
source_type: news_media
tldr: Graphsignal 发布生产级推理分析平台，支持 GPU/CUDA 性能监控
objective_summary: Graphsignal 推出开源推理性能分析平台，提供连续 profiling 时间线、LLM 生成逐步骤追踪、系统级硬件指标和错误监控，支持通过
  CLI 或 Python API 接入，兼容 CUDA 12.x 和 13.x。
event_type: framework_tools
epistemic_status: pr_statement
entities:
  companies:
  - Graphsignal
  technologies:
  - CUDA
  - CUPTI
  - vLLM
  key_people: []
key_logic_flow:
- Graphsignal 是一个面向生产环境的推理性能分析平台，用于优化 AI 模型在不同引擎和硬件上的性能表现。
- 该工具提供连续高分辨率 profiling 时间线，展示推理工作负载的操作持续时间和资源利用率。
- 支持 LLM 生成追踪，包含每步耗时、token 吞吐量和延迟分解等细粒度指标。
- 提供推理引擎和硬件（CPU、GPU、加速器）的系统级指标监控。
- 支持设备级故障和推理错误监控，以及 AI 代理的推理遥测数据采集。
- 通过 graphsignal-run CLI 命令或 graphsignal.watch() Python API 接入，依赖 CUPTI 低开销 API 采集
  CUDA kernel 活动，分析上传在侧车进程中完成。
extract_result: success
impact_score:
  score: 4.5
  reason: Graphsignal 是一个面向生产环境的推理性能分析平台，定位清晰但属于渐进式工具改进而非范式突破。它填补了 LLM 推理领域生产级 profiling
    工具的空白——现有的 NVIDIA Nsight 等工具偏开发调试，缺乏对 LLM 生成逐步骤追踪、token 吞吐量等 AI 工作负载特有指标的连续监控能力。开源（GitHub）降低了采用门槛，CLI
    一键接入 vLLM 的设计对 MLOps 团队有实际吸引力。但该领域已有 Datadog/New Relic 等 APM 厂商的 AI 监控模块、以及 Braintrust/Arize
    等 AI observability 平台竞争，Graphsignal 作为初创产品要突破格局仍需时间。综合判断：实用工具级影响力，局部改变推理优化工作流，但非行业级地震。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: CUDA kernel profiling 在 LLM 生产推理中的实际可观测性效果
hype_assessment:
  level: low
  reason: README 描述客观务实，未出现 '颠覆'、'革命性' 等 PR 滥用词汇。功能描述具体（高分辨率 profiling 时间线、LLM 生成追踪、每步耗时和
    token 吞吐量），架构细节透明（基于 CUPTI 低开销 API、侧车进程上传分析数据），并明确声明不记录 prompt/completion 内容。文档风格偏向技术工程师工具文档，而非营销宣传，水分较低。
information_entropy: medium
domain_disruption:
  technical_innovation: 基于 CUPTI 低开销 API 采集 CUDA kernel 活动，侧车进程分离分析负载与推理主进程，在保持生产环境低性能损耗的前提下提供
    LLM 生成逐步骤追踪和 token 吞吐量细粒度指标。类似 NVIDIA Nsight 的 profiling 能力下放到生产环境，集成到 CLI/Python
    API 的轻量接入方式降低了使用门槛。
  business_model: 采用开源核心（GitHub）+ 云平台（API key 绑定 graphsignal.com 账户）的开放核心模式，与 Datadog
    AI Observability 等闭源商业 APM 形成差异化定位。通过免费账户拉新、企业版变现的路径清晰，但面临同赛道 Arize/Braintrust
    等开源可观测平台的竞争。
engineering_complexity: production_ready
compound_value:
  score: 6.5
  reason: 推理性能分析是 AI 从实验走向生产的关键基础设施层，需求随推理规模增长具有长期复利效应。Graphsignal 以开源 + CUPTI 低开销
    GPU kernel 采集构建技术壁垒，侧车进程架构保障生产级低影响，差异化定位在低层硬件级 profiling（而非 LLM 应用层 eval），与传统
    APM 和 LLM 可观测性工具形成互补。但需警惕：1）该类能力可能被云平台（AWS SageMaker、GCP Vertex AI）或推理引擎（vLLM、TGI）原生集成而稀释独立性；2）AI
    可观测性赛道已拥挤（LangFuse、Arize AI、Weights & Biases、Helicone），差异化护城河需持续加码；3）开源商业化的变现路径尚待验证。目前处于『细分赛道潜在基础设施』阶段，3-5
    年内价值取决于社区生态粘性和能否向平台化演进。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Graphsignal
- NVIDIA
- vLLM 用户与生态
competitive_casualty:
- 传统 APM 厂商（DataDog、New Relic）
- 闭源高价 AI 可观测性平台
market_opportunities:
- 企业可基于Graphsignal的开源能力构建内部AI推理基础设施的可观测性平台，尤其针对GPU资源利用率和推理延迟进行精细化成本优化
- LLM推理服务商可将Graphsignal集成到推理管线中，通过逐步骤token吞吐量和延迟分解定位瓶颈，直接降低GPU算力开销
- AI可观测性工具厂商可借鉴其基于CUPTI的低开销profiling架构，将GPU级性能监控能力集成到现有产品中形成差异化优势
risk_matrix:
  regulatory: 使用CUPTI采集GPU内核活动可能受美国出口管制规则约束，跨境部署时需评估对特定硬件/地区的合规风险
  technological: 深度绑定NVIDIA CUDA/CUPTI生态，若推理硬件市场向AMD ROCm、Apple Metal或定制ASIC多元化发展，该工具的核心profiling能力将受限
  competitive: AI推理监控赛道已有Langfuse、Helicone、Weights & Biases等成熟竞品，Graphsignal的GPU级profiling虽形成差异化，但生态集成度和品牌认知度尚待验证
  ethical: 虽声明不记录prompt/completion内容，但生产环境全链路遥测（含硬件级指标）仍需建立完善的数据治理和访问控制机制以防敏感信息泄露
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
---

Graphsignal is a production-scale inference profiling platform that helps engineers optimize AI performance across models, engines, GPUs, and other accelerators. It provides essential visibility across the inference stack, including:

- Continuous, high-resolution profiling timelines exposing operation durations and resource utilization across inference workloads.
- LLM generation tracing with per-step timing, token throughput, and latency breakdowns for major inference frameworks.
- System-level metrics for inference engines and hardware (CPU, GPU, accelerators).
- Error monitoring for device-level failures and inference errors.
- Inference telemetry for AI agents to identify bottlenecks and drive targeted improvements across the inference stack.

Learn more at graphsignal.com.

```
UV_TOOL_BIN_DIR=/usr/local/bin uv tool install 'graphsignal[cu12]' # CUDA 12.x
# or
UV_TOOL_BIN_DIR=/usr/local/bin uv tool install 'graphsignal[cu13]' # CUDA 13.x
```

If you prefer a single environment, or you use the `graphsignal.watch()`

Python API (which requires `graphsignal`

importable by your application), install it directly into your workload's environment instead:

```
pip install 'graphsignal[cu12]' # CUDA 12.x
# or
pip install 'graphsignal[cu13]' # CUDA 13.x
```

Wrap your launch command with `graphsignal-run`

:

```
export GRAPHSIGNAL_API_KEY=<my-api-key>
graphsignal-run vllm serve <model> --port 8001
```

Environment variables read by the profiler:

| Variable | Purpose |
|---|---|
`GRAPHSIGNAL_API_KEY` (required) |
Your account API key. |
`GRAPHSIGNAL_TAG_<KEY>=<value>` |
Arbitrary tag attached to all signals (e.g. `GRAPHSIGNAL_TAG_DEPLOYMENT=us-prod` ). |

Sign up for a free account at graphsignal.com; you'll find the API key in Settings / API Keys.

See the Profiler CLI reference for the full set of options.

Applications that bootstrap themselves can call `graphsignal.watch()`

from Python instead — see the Profiler API reference.

See integration documentation for libraries and inference engines:

Log in to Graphsignal to monitor and analyze your application.

Install the Graphsignal skill to let your AI coding agent (Claude Code, Codex, or Gemini) fetch and analyze signal context directly from your agent. See AI Optimization for setup instructions.

The profiler has minimal impact on production performance. CUDA kernel activity is collected via CUPTI with low-overhead APIs, and analysis and upload happen in the sidecar process.

The profiler only establishes outbound connections to `api.graphsignal.com`

to send data; inbound connections or commands are not possible.

Content and sensitive information, such as prompts and completions, are not recorded.

If something doesn't look right, report it to our support team via your account.

In case of connection issues, please make sure outgoing connections to `https://api.graphsignal.com`

are allowed.