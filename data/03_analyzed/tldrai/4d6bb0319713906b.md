---
title: smevals (GitHub Repo)
source: https://github.com/prime-radiant-inc/smevals?utm_source=tldrai
author: []
published: ''
created: '2026-08-04'
manifest_dates:
- '2026-08-04'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 4d6bb0319713906b
source_type: news_media
tldr: smevals 是一个用于对小模型和大模型运行评测的框架，支持通过 uv tool 或 pip 安装。它定义了 Eval、Task、Config、Run、Grader、Check
  等组件，将评测组织为可复现的目录结构，并用可配置的评分器对多次运行结果进行打分。
objective_summary: prime-radiant-inc 在 GitHub 上发布了 smevals 评测框架，用户可通过 uv tool install
  smevals 或 pip install smevals 安装。框架以 Eval 为核心概念，将评测拆解为 Task、Config、Run、Grader、Check
  等组件，其中 Run 是使用 Runner 将单个 Task 对单个 Config 执行一次的不可变记录。每个 Run 可被多个 Grader 反复评分，因网络等错误而失败的
  Run 不会计入成绩。文章以让模型写俳句的评测为例，展示了 eval.yaml 目录结构与配置文件的组织方式。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - prime-radiant-inc
  technologies:
  - LLM
  - evals
  - Codex
  - Pi
  key_people: []
key_logic_flow:
- smevals 是面向小模型和大模型的评测框架，可通过 uv tool install smevals 或 pip install smevals 安装，也可用
  uvx smevals --help 查看帮助。
- 框架的核心概念是 Eval，即一组用于评估模型特定能力（如 text-to-SQL）的 Task 集合，相关 Eval 可进一步分组为 Suites。
- Run 是使用 Runner 将单个 Task 对单个 Config 执行一次的不可变记录，同一 Task 与 Config 可执行多次以应对非确定性结果，smevals
  run -n 5 表示每个 Task 最多执行五次。
- Runner 以非零状态退出的 Run 被标记为失败，失败 Run 仅保留用于调试，从不参与评分、不进入报告，也不计入 -n 目标数量。
- Grader 由按顺序执行的 Check 序列及结果合并规则组成，对 Run 应用后生成 Grade；Check 由 Checker 执行，可包含文本包含判断或渲染
  SVG 后由 LLM 评判等复杂断言。
- 一个 Eval 是包含 eval.yaml 的目录，示例用让模型写俳句的评测展示了 tasks、configs、graders、checkers、runs 等子目录的推荐组织方式。
object_mentions:
- object_type: project
  name: smevals
  canonical_name: smevals
  url: https://github.com/prime-radiant-inc/smevals
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - smevals 是一个用于对小模型和大模型运行评测的框架，可通过 uv tool install smevals 或 pip install smevals
    安装。
  - smevals 以 Eval 为核心概念，将评测拆解为 Task、Config、Run、Grader、Check 等组件，并用目录结构组织评测。
  - 文章以让模型写俳句的评测为例，展示了 smevals 中 eval.yaml 文件及 tasks、configs、graders、checkers 子目录的写法。
  article_id: 4d6bb0319713906b
- object_type: product
  name: Codex
  canonical_name: Codex
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - smevals 的 Runner 可以是直接向模型发送提示的 CLI 程序，也可以构建在 Codex 或 Pi 等智能体框架之上。
  article_id: 4d6bb0319713906b
- object_type: product
  name: Pi
  canonical_name: Pi
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - smevals 的 Runner 可以是直接向模型发送提示的 CLI 程序，也可以构建在 Codex 或 Pi 等智能体框架之上。
  article_id: 4d6bb0319713906b
extract_result: success
impact_score:
  score: 3.0
  reason: 作为 LLM 评测领域的新开源框架，smevals 在工程设计上确有亮点（Run 与 Grader 解耦、失败 Run 自动排除、Check 可复用的
    CLI 化抽象），但评测工具链赛道已相当拥挤（OpenAI Evals、Promptfoo、DeepEval、Ragas 等），且发布方 prime-radiant-inc
    并非头部机构、无社区声量背书。短期影响主要局限于给开发者的评测工具选型增加一个候选，难以改变局部竞争格局，故评分为 3.0。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: Run/Grader 解耦设计带来的评测复现性与可重评分能力，以及它在成熟 eval 生态中的差异化价值
hype_assessment:
  level: low
  reason: 文章是对 GitHub README 的技术性转述，通篇为框架组件概念（Eval/Task/Run/Grader/Check）与目录结构的客观说明，未出现'颠覆''革命性''SOTA'等
    PR 滥用词汇，也没有夸大的性能数据或用户规模宣传，属于实打实的工程发布，水分较低。
information_entropy: medium
domain_disruption:
  technical_innovation: 核心设计创新在于将 Run（不可变的执行证据）与 Grader（评分逻辑）彻底解耦：同一 Task 与 Config
    可多次执行生成多条 Run，且可事后更换 Grader 对既有 Run 反复评分而无需重新执行模型调用；Check/Checker 抽象将断言从简单文本包含扩展到可渲染
    SVG 后交 LLM 视觉评判的复合流程，并通过共享工作目录实现跨 Check 的产物传递。这种'证据与评分分离'的架构对提升评测可复现性有一定工程参考价值，但并非范式级突破。
  business_model: 无直接商业模式，属纯开源工具发布；潜在路径是被商业化的 eval/MLOps 平台（如 LangSmith、Weights &
    Biases）吸收其设计理念或集成为底层评测引擎，或成为团队内部评测基础设施的轻量选择。
engineering_complexity: production_ready
compound_value:
  score: 6.0
  reason: 评测是 AI 应用与 Agent 走向生产环境的必经环节，具备典型的'评测资产复利'效应：团队一旦围绕 smevals 沉淀出 eval suites（Task/Config/Grader
    体系），更换工具的成本会随套件规模增长而上升，形成资产层面的切换粘性。设计上采用'目录即评测'（eval-as-code）的可复现结构、模型与 harness
    无关（兼容 Codex、Pi 等任意 CLI Runner），并妥善处理了非确定性多次运行、失败 Run 不计分、不重跑即可换 Grader 重评分等工程痛点，定位精准，契合小型模型在成本约束下被严格验证的产业趋势。但当前赛道拥挤（OpenAI
    Evals、Promptfoo、LangSmith、Braintrust、DeepEval 等均在争夺），smevals 处于极早期，生态未建立、缺乏与主流
    CI/可观测平台的集成、也无标杆客户背书，能否沉淀为细分基础设施仍需社区采纳度验证。综合给予 6.0 分，属'潜力型但需持续验证'区间。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- prime-radiant-inc
- DeepSeek
- Mistral
- Anthropic
- OpenAI
competitive_casualty:
- Braintrust
- LangSmith
- Confident AI
- 闭源评测平台
- 人工评测/QA 服务商
market_opportunities:
- 工程团队可借鉴 smevals 将 Run（执行留档）与 Grader（评分）解耦的设计，重构自家 LLM 评测管线，实现评分标准迭代时无需重跑模型，显著降低回归评测成本。
- 面向小型/端侧模型的垂直评测仍有差异化空间，可基于 smevals 这类轻量框架封装行业专属任务集（如 text-to-SQL、代码生成），对外提供模型选型与质量验收服务。
- smevals 的可扩展 Checker 模式（如渲染 SVG 后由 LLM 评判）可启发多模态输出（图表、界面截图、设计稿）的自动化质检产品，切入 AI 应用测试与
  QA 工具市场。
risk_matrix:
  regulatory: 无
  technological: 评测框架赛道拥挤，smevals 面临 OpenAI Evals、DeepEval、promptfoo、lm-evaluation-harness
    等成熟方案的替代压力；且依赖 uv/pip 生态与新兴 Runner（Codex、Pi）接口，接口稳定性与长期演进存在不确定性。
  competitive: 开源评测工具同质化严重，OpenAI、LangChain、Weights & Biases、Braintrust 等大厂与头部创业公司均在布局
    evals，新框架易被生态挤压或边缘化，商业化变现路径模糊。
  ethical: 内置 LLM-as-judge 检查器存在裁判模型偏见与评分不一致风险，可能导致评测结果系统性偏差；同时公开评测集存在被针对性过拟合（数据污染）的隐患。
  additional:
  - 单点维护风险：项目由 prime-radiant-inc 单一组织发布，社区生态尚不成熟，版本早期存在弃维护或设计大改的风险。
confidence:
  impact: medium
  compound: low
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: smevals
  canonical_name: smevals
  url: https://github.com/prime-radiant-inc/smevals
  positioning: smevals 是一个开源评测框架，可对小模型与大模型运行评测，以 Eval 为核心概念，通过 Task、Config、Run、Grader、Check
    等组件将评测组织为可复现的目录结构，并用可配置评分器对多次运行结果打分。
  technical_signal: 框架以 Run 作为执行一次 Task 与 Config 组合的不可变记录，失败 Run 仅保留用于调试且从不参与评分，同一
    Task 与 Config 可执行多次以应对非确定性结果。
  adoption_signal: 项目通过 uv tool install 或 pip install 即可安装，并支持 uvx smevals --help
    快速体验，文章展示了完整的入门示例与目录结构规范。
  ecosystem_relevance: 该项目兼容 Codex、Pi 等 agent harness 作为 Runner，并支持 llm CLI 等可执行程序接入，与现有
    AI 评测与 agent 工具链生态互补。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 与既有评测框架相比，smevals 强调将评测组织为磁盘上的可复现目录结构，并允许同一 Run 被不同 Grader 反复评分，这种解耦设计具有差异化。
  watch_reason: smevals 将评测拆解为可复现的目录结构与组件化组件，使同一 Run 可被不同 Grader 反复评分而无需重新执行，这种设计在评测可追溯性与灵活复评方面具有价值。项目来自
    prime-radiant-inc，尚处早期，值得跟踪其在 agent 评测领域的采用与演进。
  risk_notes:
  - 项目发布初期社区采用量未知，成熟度与长期维护可持续性仍需观察。
  - 评测结果依赖 Runner 与 Checker 的可靠性，复杂断言如 LLM 评判 SVG 可能引入不稳定因素。
  score: 5.0
  article_ids:
  - 4d6bb0319713906b
  evidence_snippets:
  - smevals 是一个用于对小模型和大模型运行评测的框架，可通过 uv tool install smevals 或 pip install smevals
    安装。
  - smevals 以 Eval 为核心概念，将评测拆解为 Task、Config、Run、Grader、Check 等组件，并用目录结构组织评测。
  - 文章以让模型写俳句的评测为例，展示了 smevals 中 eval.yaml 文件及 tasks、configs、graders、checkers 子目录的写法。
---

A framework for running evals against small (and large) models

`uv tool install smevals`

Or `pip install smevals`

, or just `uvx smevals --help`

.

The top-level concept is an **Eval**: a collection of Tasks used to determine how good a particular model or model-and-harness configuration is at a specific high-level capability, such as text-to-SQL, drawing a pelican riding a bicycle, or evaluating whether an implementation satisfies a provided specification.

Evals can optionally be grouped into **Suites** of related Evals, primarily as a mechanism for organizing them on disk.

An **Eval** is a collection of **Tasks**. These are the individual exercises that a model must complete for its abilities to be evaluated.

A **Config** describes the setup used to attempt Tasks. It specifies a model and may include model parameters, system prompts, tools and other settings.

To gather evidence, we create a **Run**. A Run is the immutable record of executing one Task against one Config using a **Runner**. A Runner is a reusable CLI program that may send prompts directly to a model or build on an agent harness such as Codex or Pi.

The same Task and Config can be executed multiple times, producing multiple Runs to help account for non-deterministic results - `smevals run -n 5`

tops each Task up to five Runs. Each Run includes a timestamp to help track these.

A Run whose Runner exits non-zero is a **failed Run**: a harness-level error such as a network failure, not evidence about the model. Failed Runs stay on disk for debugging but are never graded, are excluded from reports, and do not count towards `-n`

targets.

Once we have gathered Runs, we apply a **Grader** to each Run to produce a **Grade**. A Grader is a configured sequence of **Checks**, plus rules for combining their results into that Grade.

Checks are individual assertions or measurements. Some may be simple, such as “does the output contain this text?” Others may be more complex, such as “render this SVG to an image and have an LLM judge assess it”. Each Check names the **Checker** that performs it, along with configuration for that Checker such as patterns, rubrics or expected values. A Check can be marked as required, in which case its failure halts the Grader and skips the remaining Checks.

A **Checker** is a named operation *or* a reusable CLI program that implements one kind of Check. `contains`

and `xml-valid`

are named operations, `../checkers/render-svg`

might be a custom program. The same Checker can be used by many Checks across many Graders. The Checks in a Grader execute in order and share a working directory, so a Checker may create files - such as a rendered image - which are kept with the Grade as artifacts and available to later Checks in the sequence.

A **Grade** is the result of applying a Grader to a Run. It records the result of each Check and may contain an overall pass/fail outcome and/or a numeric score. Grades can also include additional notes which are not used for scoring but may help interpret the results in the future.

We may later change the Grader we use to evaluate Runs without executing the Runs again. A single Run can therefore be evaluated multiple times, producing multiple Grades using different Graders.

An Eval is any directory containing an `eval.yaml`

file:

```
my-eval/
├── eval.yaml # name and description
├── tasks/ # one YAML file per Task
├── configs/ # one YAML file per Config
├── graders/ # one YAML file per Grader
├── checkers/ # custom Checker executables (by convention)
├── run-llm # Runner executable (any name, any location)
└── runs/ # created by smevals run - never edit by hand
```


Here's how to structure a complete Eval that asks models to write haikus and grades them on their structure.

The Eval consists of five files.

`my-eval/eval.yaml`

defines a name and description:

```
name: haiku
description: >-
Can the model write a haiku on demand? Graded on structure:
the reply must be exactly three lines.
```

An Eval must have one or more Tasks. Each of these is defined as a `tasks/*.yaml`

YAML file.

`my-eval/tasks/pelicans.yaml`

must have a `name`

; a `prompt`

is the common case, but any other keys are allowed and are passed to the Runner as environment variables:

```
name: pelicans
prompt: Write a haiku about pelicans. Reply with only the haiku, three lines.
```

An Eval also needs at least one Config, defined in `configs/*.yaml`

. If there is just one of these it should be called `default`

.

`my-eval/configs/default.yaml`

- the Config named `default`

is used when no `-c`

option is passed to `smevals run`

.

`runner`

specifies a path to an executable program relative to this file:

```
name: default
runner: ../run-llm
model: gpt-4.1-mini
```

Here's that Runner script:

`my-eval/run-llm`

- This one uses the llm CLI, but any executable honoring the contract below works. Make it executable with `chmod +x`

: