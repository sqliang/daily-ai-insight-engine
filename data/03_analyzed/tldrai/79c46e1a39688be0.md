---
title: Speculative Programmatic Tool Calling (12 minute read)
source: https://alexzhang13.github.io/blog/2026/spec-ptc/?utm_source=tldrai
author: []
published: ''
created: '2026-08-26'
manifest_dates:
- '2026-08-26'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 79c46e1a39688be0
source_type: news_media
tldr: 作者Alex Zhang提出投机式程序化工具调用(sPTC)技术，在harness生成token的同时预先启动高延迟工具调用（如子LLM调用），用缓存输出加速以代码生成为主的harness；在8xH100节点上实测递归语言模型(RLM)推理约1-1.2倍加速，代码已开源至GitHub。
objective_summary: Alex Zhang在个人博客提出投机式程序化工具调用(sPTC)技术，灵感来自CPU投机执行与LLM投机解码，核心做法是在harness仍在生成token时预先启动部分生成的REPL调用中的工具调用，若最终完整代码调用了这些工具则直接返回缓存结果。该技术主要适用于以代码执行为动作空间的harness，尤其针对递归语言模型(RLM)中的子LLM或子agent等高延迟工具调用。作者在8xH100
  80B节点上的vLLM服务器上，使用Qwen3-30B-A3B-Instruct-0527在OOLONG和OOLONG-Pairs数据集上实测RLM加速约1-1.2倍。相关实现代码已开源至GitHub仓库alexzhang13/spec-ptc。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - sPTC (Speculative Programmatic Tool Calling)
  - RLM (Recursive Language Model)
  - REPL
  - vLLM
  - speculative decoding
  - CodeAct
  key_people:
  - Alex Zhang
key_logic_flow:
- 作者提出投机式程序化工具调用(sPTC)技术，其灵感来自CPU投机执行与LLM投机解码，核心是在harness生成token期间预先启动部分生成的REPL调用中的工具调用，并在完整代码确认调用时返回缓存输出。
- 该技术主要适用于以代码为动作空间的harness（如CodeAct式或代码模式），尤其对RLM中的子LLM或子agent等高延迟工具调用收益最明显，因为这类调用通常是延迟瓶颈。
- 基准测试在8xH100 80B节点上的vLLM服务器上进行，使用Qwen3-30B-A3B-Instruct-0527在OOLONG和OOLONG-Pairs数据集上测得RLM加速约1-1.2倍。
- 设计上为REPL中需要被投机的工具调用提供hook契约，既能指定哪些工具应被投机（如子LLM调用而非过于昂贵的子RLM调用），也能对依赖内存变量而非字面量的工具调用进行投机。
- 加速收益分为两类场景：本地运行LLM时投机可提升算术强度以缓解内存瓶颈，大规模服务系统中投机可让工具执行与主上下文生成或慢速REPL调用相互重叠。
object_mentions:
- object_type: project
  name: alexzhang13/spec-ptc
  canonical_name: alexzhang13/spec-ptc
  url: https://github.com/alexzhang13/spec-ptc
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章明确给出投机式程序化工具调用的关联代码仓库地址 https://github.com/alexzhang13/spec-ptc，并说明实现细节简单且容易在代码库中理解。
  article_id: 79c46e1a39688be0
- object_type: project
  name: RLM (Recursive Language Model)
  canonical_name: RLM (Recursive Language Model)
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者在博客中回顾其此前关于递归语言模型(RLM)的研究，强调代码在REPL中是系统唯一需要的工具，其余所有工具都应作为该代码工具中的函数。
  article_id: 79c46e1a39688be0
- object_type: dataset
  name: OOLONG
  canonical_name: OOLONG (trec-coarse)
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者在8xH100 80B节点上运行的vLLM服务器上，使用OOLONG(trec-coarse, 132k)数据集对RLM运行时的投机式程序化工具调用进行基准测试，每次实验重复运行5次。
  article_id: 79c46e1a39688be0
- object_type: dataset
  name: OOLONG-Pairs
  canonical_name: OOLONG-Pairs
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者同时使用OOLONG-Pairs(32k)数据集进行实验，并在4并发与8并发两种设置下报告模型平均子调用次数与轮次，以给出相对运行情况的参照。
  article_id: 79c46e1a39688be0
- object_type: model
  name: Qwen3-30B-A3B-Instruct-0527
  canonical_name: Qwen3-30B-A3B-Instruct-0527
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 作者使用Qwen3-30B-A3B-Instruct-0527模型，分别在temperature=0.7与temperature=0.0两种设置下测试，以控制RLM运行因轨迹不同带来的速度方差。
  article_id: 79c46e1a39688be0
- object_type: project
  name: vLLM
  canonical_name: vLLM
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 基准测试运行在由8块H100 80B组成的节点上的vLLM服务器中，用于衡量投机式程序化工具调用对递归语言模型推理时间的加速效果。
  article_id: 79c46e1a39688be0
extract_result: success
impact_score:
  score: 3.5
  reason: 这是一个个人博客提出的技术技巧分享，而非重大产品发布或行业范式创新。虽然投机式程序化工具调用(sPTC)将CPU投机执行与LLM投机解码思想迁移到agent
    harness设计，思路在RLM/CodeAct优化方向上颇具新意，且附带可读的开源代码实现，但实测加速仅约1-1.2倍，收益幅度有限；适用范围局限于以代码为动作空间的harness，当前还只是理论性主张，未经社区大规模验证或产业采用。对agent推理优化小圈子有参考价值，但短期行业冲击力有限，故给予3.5分。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: 投机式工具调用的实际加速收益有限（约1-1.2倍），且仅适用于代码动作为主的harness，应用边界较窄
hype_assessment:
  level: low
  reason: 作者以实证数据说话，如实报告1-1.2倍的有限加速，并主动说明收益高度依赖工具延迟、生成token数、服务引擎负载与模型运行轨迹等条件，同时明确标注适用场景局限。全文未见'颠覆''革命性'等PR滥用词汇，属于诚实的技术报告，无概念炒作成分。
information_entropy: high
domain_disruption:
  technical_innovation: 将CPU投机执行与LLM投机解码的思想迁移至agent harness设计，提出在harness生成token期间预先启动部分REPL调用中的高延迟工具调用（如子LLM/子agent），待完整代码确认调用时直接返回缓存输出以消除串行等待；同时设计了可指定哪些工具应被投机、并支持依赖内存变量（而非字面量）的工具调用的hook契约机制，是对agent系统级延迟优化的新尝试。
  business_model: 对商业模式影响为间接且长远：若该技术走向成熟，可降低代码驱动型agent（RLM、CodeAct）推理链路中的高延迟瓶颈，进而优化agent平台的单次任务时延与算力成本结构，对以agent调用量计费的SaaS服务商有潜在成本优化空间；但当前仅为研究原型，尚无直接商业落地或生态重塑力。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 评估逻辑：① 行业趋势上，AI Agent 规模化部署的核心瓶颈正从'模型能力'转向'推理延迟与工具调用成本'，子 agent/子 LLM 等高延迟工具调用是明确的延迟瓶颈，投机式执行是对这一瓶颈的系统性解法，方向判断成立；②
    技术类比上，投机解码在 1-2 年内从论文成为 vLLM/TensorRT-LLM 等主流 serving 引擎的标配，sPTC 将同一逻辑延伸至 agent
    执行层，具备成为 agent 推理优化标准模式的条件，底层思想有复利性；③ 实证层面，OOLONG 上实测 1-1.2x 加速幅度温和但真实，且随工具延迟占比上升收益会放大（子
    agent 调用越贵收益越高），逻辑自洽；④ 主要风险：当前为个人开源技术提案，无商业载体与团队，大概率被 LangGraph/vLLM/smolagents
    等框架吸收为内置能力导致价值稀释，加速倍数高度依赖 workload，通用性未经更大范围验证。综合判断：有潜力沉淀为 agent 基础设施层的通用优化范式，但处于早期验证阶段且价值捕获路径不清晰，故评
    6.0 分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- vLLM
- LangChain
- Hugging Face
- OpenAI
- Anthropic
competitive_casualty:
- 传统 RPA 厂商
- 闭源专有 Agent 编排平台
- 低代码 AI 工作流工具
market_opportunities:
- Agent 推理优化方向创业者可基于 sPTC 思路开发投机式工具调用中间件，为 CodeAct 风格或递归语言模型(RLM) harness 提供通用加速能力
- 提供 agent/RLM 托管服务的平台可将 sPTC 内置于推理调度层，通过缓存高延迟子模型/子 agent 调用的输出，降低端到端延迟并提升单节点并发吞吐
- 在本地多卡 LLM 部署场景（如私有化推理）中，开发者可将 sPTC 与投机解码结合，缓解主上下文解码的内存瓶颈、提升算术强度，形成推理性能优化服务方向
risk_matrix:
  regulatory: 技术本身无直接监管风险，但投机式缓存若涉及外部 API（搜索、第三方子模型服务）响应的留存，需注意数据保护法规与 API 使用条款合规；未实际执行的投机调用产生的计量费用也可能引发成本归属争议
  technological: 该事件为理论性主张（认识论状态：theoretical_claim），实测加速仅 1-1.2 倍且高度依赖工具延迟、token 数量与
    harness 轨迹；若主流推理引擎（如 vLLM）或前沿模型服务商未来原生集成类似调度优化，独立 sPTC 方案的技术价值将被稀释，且代码已开源导致该方法缺乏技术壁垒
  competitive: 主要竞争风险来自大型 AI 基础设施厂商将投机调度能力内置到推理引擎或 agent 框架，挤压独立中间件与第三方优化服务的生存空间；同时投机解码与调度类优化迭代迅速，早期方案易被后发者快速追赶并超越
  ethical: 投机式预执行工具调用若从只读子 LLM 调用推广到有副作用的工具，可能触发本不应执行的动作，造成无效计算、缓存污染甚至意外副作用；基于部分生成代码的缓存输出在完整代码路径不一致时可能返回陈旧或错误结果，影响一致性与可追溯性
  additional:
  - 正确性风险：投机缓存需建立失效与一致性校验机制，否则完整 REPL 路径与投机路径分歧时会输出过期结果
  - 成本风险：投机预启动的子模型/子 agent 调用即使最终未被代码采用，也已产生 GPU 或 API 费用，在付费模型服务上可能形成额外开销
confidence:
  impact: medium
  compound: low
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: alexzhang13/spec-ptc
  canonical_name: alexzhang13/spec-ptc
  url: https://github.com/alexzhang13/spec-ptc
  positioning: 投机式程序化工具调用(sPTC)的开源实现，在harness生成token期间预先启动REPL中的高延迟工具调用，以缓存输出加速以代码生成为主的harness推理。
  technical_signal: 设计为REPL中可投机工具调用提供hook契约，可指定哪些工具应被投机，并支持对依赖内存变量的调用进行投机。
  adoption_signal: 在8xH100 80B节点的vLLM服务器上，用Qwen3-30B-A3B-Instruct-0527在OOLONG和OOLONG-Pairs数据集上实测RLM推理加速约1-1.2倍。
  ecosystem_relevance: 面向CodeAct式或代码模式的harness，尤其适用于递归语言模型(RLM)中高延迟的子LLM或子agent调用，属于AI推理与agent编排优化工具链。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 投机式程序化工具调用将CPU投机执行与LLM投机解码的思路迁移到harness工具调用层，为以代码为动作空间的agent推理提速提供了新范式；实现已开源且代码简短，实测RLM加速约1-1.2倍，值得跟踪其在更多harness与更大规模场景下的收益验证。
  risk_notes:
  - 实测加速幅度高度依赖工具延迟、token数量与服务负载，1-1.2倍的结果未必能泛化到生产环境。
  - 若投机启动的工具调用最终未被完整代码使用，会产生无效计算与资源浪费，需要谨慎设计投机策略。
  score: 7.0
  article_ids:
  - 79c46e1a39688be0
  evidence_snippets:
  - 文章明确给出投机式程序化工具调用的关联代码仓库地址 https://github.com/alexzhang13/spec-ptc，并说明实现细节简单且容易在代码库中理解。
  - 作者在8xH100 80B节点的vLLM服务器上，使用Qwen3-30B-A3B-Instruct-0527在OOLONG和OOLONG-Pairs数据集上实测RLM推理加速约1-1.2倍。
  - 该技术主要适用于以代码执行为动作空间的harness，尤其对递归语言模型中的子LLM或子agent等高延迟工具调用收益最明显。
- object_type: project
  name: RLM (Recursive Language Model)
  canonical_name: RLM (递归语言模型)
  url: null
  positioning: 递归语言模型(RLM)是一种以REPL中代码作为系统唯一工具的agent范式，所有其他工具都作为该代码工具中的函数，代码成为主要动作空间。
  technical_signal: RLM将REPL代码视为系统唯一需要的工具，其余所有工具都作为该代码工具中的函数，代码执行成为agent的主要动作空间。
  adoption_signal: 作者此前已围绕RLM展开研究并撰文，本文以RLM为基准场景验证sPTC，在OOLONG与OOLONG-Pairs数据集上完成了加速实测。
  ecosystem_relevance: RLM是sPTC技术的主要应用场景，其子LLM或子agent调用通常构成高延迟瓶颈，投机式工具调用正是针对这一瓶颈设计。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: RLM作为以代码执行为唯一动作空间的agent范式，是sPTC投机式工具调用的核心验证场景，其子LLM调用延迟瓶颈具有普遍性；跟踪RLM演进有助于判断代码驱动agent推理优化的方向。
  risk_notes:
  - RLM在本文中属于作者个人研究背景中的概念，缺乏独立于该博客生态的广泛采用证据，需借助外部信源交叉验证其影响力。
  score: 5.0
  article_ids:
  - 79c46e1a39688be0
  evidence_snippets:
  - 作者在博客中回顾其此前关于递归语言模型(RLM)的研究，强调代码在REPL中是系统唯一需要的工具，其余所有工具都应作为该代码工具中的函数。
  - RLM中的子LLM或子agent调用往往具有高延迟，通常是依赖代码执行的harness中的延迟瓶颈。
---

Speculative programmatic tool calling is a class of techniques for overlapping tool call computation with the code being generated by a harness.

The associated code is here: https://github.com/alexzhang13/spec-ptc

It is no surprise from my previous writing and work on Recursive Language Models (RLMs) that I believe (1) code in a REPL is the only “tool” a system needs; (2) all other tools should be functions in that code tool. When code becomes the primary action space of your system, you need to start thinking about overlapping these specialized tool calls with the code being generated and executed.

Inspired by speculative execution in CPUs and speculative decoding in LLMs, a relatively simple and useful trick I’m proposing in harness design is **speculative programmatic tool calling** (**sPTC**), in which we speculate and pre-launch tool calls from partially generated REPL calls as the harness is still generating tokens, rather than waiting for it to finish the entire generation. In particular, this is mostly useful when the tool of interest is a sub-LLM or sub-agent call, such as in the RLM. If the fully generated REPL actually ends up calling these tools, they immediately return with their cached outputs from the speculated call.

A key observation is that LLM tools, such as sub-agents or search APIs, are often high-latency and are usually the bottleneck in harnesses that rely on code execution as actions. Furthermore, the actual generation of the main context is often also a significant latency bottleneck that blocks intermediate calls from happening.

For the rest of this blog, I’ll mostly be discussing inference time savings with respect to an RLM, but note that this generally applies to harnesses that generate code (i.e. CodeAct-like or “code mode” harnesses). There are two obvious areas where you save time:

On locally running LLMs running one or a few chat instances at a time, your inference engine is often highly memory-bound from decoding just the main context, and speculation can help increase the arithmetic intensity. For high-volume serving systems (e.g. if you’re using a frontier lab or model router API), batched requests are abstracted away to various, likely disjoint serving engines, so the gain is purely from either overlapping computation with the main context, or overlapping execution time with a slow REPL call.

I’ll start by highlighting some basic runtime numbers, although you can probably reason through the runtime benefit. The actual implementation details are quite simple and easy to follow in the codebase, which I will talk about in the latter half of the blog.

You should keep in mind that it’s very difficult to estimate the exact speed-ups because it’s highly dependent on the latency of the tools, the number of tokens generated, the load of your serving engine, and the actual choices the harness makes. We can isolate some components (e.g. fix a program) to highlight obvious cases that don’t really need benchmarking to understand, but the simplest example is just to replicate a setting and harness we know and benchmark the runtimes averaged over many samples.

We run below on OOLONG (trec-coarse, 132k) and OOLONG-Pairs (32k) from the original paper, with a node of 8xH100 80B running a vLLM server. We tested two cases with `Qwen3-30B-A3B-Instruct-0527`

, one with the standard temperature=0.7 and one with temperature=0.0 to control the variance of these RLM runs, whose speed is highly dependent on the trajectory it takes. We run each experiment 5 times. We report the average number of sub-calls and turns to give a relative sense for what each model is doing as well, and report across a setting where we have 4 concurrent runs, and 8 concurrent runs over the same serving engine.

The speed-ups for the RLM are generally on the order of 1-1.2x. We also tested a more “deterministic” suite of LM programs where we’d observe hypothetical or realistic runtime speed-ups in the codebase, but we omit them here because they are too specific.

The code is quite short and readable, but I’ll talk a bit about the design philosophy below.

The high-level design is to have imported tool calls in our REPL have a hook that can be called earlier and replaced with a cached output when needed. We want a library and frontend contract that allows us to define (1) what tools should be speculated and what shouldn’t (e.g. maybe we want sub-LLM calls to be speculated, but a sub-RLM call is too costly); and (2) a mechanism for speculating on tool calls whose inputs rely on variables in memory that were computed previously, rather than just literals.

At a high-level, the type of contract we want is a hook around functions we want speculated (e.g. the sub-call `llm_query()`