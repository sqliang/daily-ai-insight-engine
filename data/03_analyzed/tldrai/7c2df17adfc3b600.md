---
title: 'Model Genome: Fingerprinting Whether an LLM Was Trained From Scratch or Derived
  (8 minute read)'
source: https://huggingface.co/blog/mayafree/model-dna?utm_source=tldrai
author: []
published: ''
created: '2026-08-11'
manifest_dates:
- '2026-08-11'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 7c2df17adfc3b600
source_type: news_media
tldr: 文章提出 Model Genome 方法，通过 config.json 架构指纹、tokenizer 词表重叠率与线性 CKA 等公开文件证据，客观判断
  LLM 是从零训练还是基于开源权重派生，并提供覆盖韩国九家机构基座模型的在线演示 Model Genome Korea。
objective_summary: Hugging Face 博客作者提出 Model Genome（模型基因组）指纹方法，通过读取 transformers 检查点的
  config.json 与 tokenizer.json，从架构字段、词表重叠率和线性 CKA 三个层面判断 LLM 是从零训练还是派生自开源基座。实测显示多款商用模型与
  Qwen2.5、Llama-3.1、DeepSeek-V3 等开源架构完全一致；某模型架构匹配 Qwen2.5-7B 但词表重叠仅 0.38。从零训练模型与候选基座的
  CKA 接近零，继续预训练派生模型约 0.25，仅略高于无关模型基线 0.21。该工具的背景是 2026 年 7 月底多家机构发布对标 DeepSeek 的自研模型（如
  LG K-EXAONE 2.0 750B）。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  - LG
  - Alibaba
  - Meta
  - DeepSeek
  - Mistral
  technologies:
  - LLM
  - VLM
  - MoE
  - CKA
  - BPE
  - transformers
  - config.json
  - tokenizer.json
  key_people: []
key_logic_flow:
- Model Genome 方法通过读取 transformers 检查点公开的 config.json，用 model_type、hidden_size、num_hidden_layers
  等少量字段构成判别性的架构指纹。
- 实测多款商业模型与开源基座架构完全一致，例如某 7B 模型形状精确匹配 Qwen2.5-7B，某 MoE 模型匹配 DeepSeek-V3。
- 词表重叠率采用 min 归一化分母，某模型架构完全匹配 Qwen2.5-7B 但词表重叠仅约 0.38，属于'外国大脑、自创语言'的情况，而部分 VLM 直接复用基座词表（重叠
  1.000）。
- 逐行余弦相似度因 Transformer 隐藏空间的旋转不变性无法区分模型血缘，作者改用具备旋转与各向同性缩放不变性的线性 CKA 比较表示。
- 实测从零训练模型与候选基座 CKA 接近零，而继续预训练派生模型约为 0.25，仅略高于同族无关模型基线约 0.21，说明 CKA 对派生侧判别力有限。
- 文章背景是 2026 年 7 月底多家机构发布对标 DeepSeek 的自研模型（如 LG K-EXAONE 2.0 750B），知乎相关讨论浏览量超过 270
  万。
object_mentions:
- object_type: project
  name: Model Genome
  canonical_name: Model Genome
  url: https://huggingface.co/blog/mayafree/model-dna
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章提出 Model Genome 指纹方法，依据 config.json 与 tokenizer.json 等公开文件客观回答模型是从零训练还是派生而来。
  article_id: 7c2df17adfc3b600
- object_type: product
  name: Model Genome Korea
  canonical_name: Model Genome Korea
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 作者将同一套衡量标准应用于韩国九家机构的公开基座模型，并提供了可在线试用的 Model Genome Korea 演示。
  article_id: 7c2df17adfc3b600
- object_type: model
  name: LG K-EXAONE 2.0
  canonical_name: LG K-EXAONE 2.0
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 2026 年 7 月底多家实验室发布对标 DeepSeek 的自研模型，例如 LG 的 K-EXAONE 2.0（750B 参数），并在中文社区引发从零训练还是派生的讨论。
  article_id: 7c2df17adfc3b600
extract_result: success
impact_score:
  score: 6.8
  reason: 该事件并非新模型发布或重大融资，但 Hugging Face 提出的 Model Genome 方法切中了当前 LLM 行业‘自研’宣传泛滥与开源权重派生关系不透明的痛点，并提供可复现的公开文件审计工具与在线演示。短期内会显著影响模型
    provenance、开源许可证合规、第三方评测与社区舆论，对韩国及中国相关模型的品牌信任造成局部冲击，但尚未达到范式转移级别。
sentiment: mixed
developer_sentiment:
  tone: skeptical
  primary_focus: 公开文件指纹能否可靠区分‘从零训练’与‘开源基座派生’，尤其是线性 CKA 对继续预训练模型的判别力有限（0.25 vs 无关模型
    0.21）以及架构相似是否等同于派生的伦理与证据强度争议。
hype_assessment:
  level: medium
  reason: 文章使用‘DNA lineage test’、‘fingerprinting’、‘objectively answerable’等带有科普和 PR
    色彩的比喻，但核心提供了可运行的代码、具体指标（min-overlap tokenizer、线性 CKA）和韩国模型实测案例，并非纯粹概念炒作；不过作者也承认
    CKA 对派生侧判别力较弱，说明‘客观判定’的表述存在一定程度的包装。
information_entropy: high
domain_disruption:
  technical_innovation: 提出基于 transformers 检查点公开文件（config.json 架构字段、tokenizer.json
    词表、embed_tokens.weight）的模型血缘推断框架：用少量超参数构成架构指纹、用 min 归一化词表重叠率检测‘外国大脑自创语言’、用具备旋转与各向同性缩放不变性的线性
    CKA 比较权重表示，形成了不依赖私有训练日志的模型谱系审计方法。
  business_model: 对模型厂商‘自研基座’营销话语与开源权重许可证合规形成外部监督压力，可能重塑模型资产评估、并购尽调与开源生态信任机制；同时可能催生第三方模型审计、认证服务或社区驱动的模型血缘数据库需求。
engineering_complexity: prototype
compound_value:
  score: 5.5
  reason: Model Genome 提供了一套基于公开检查点的模型血缘鉴定方法，对 AI 生态的透明度、开源归属与合规审计有长期基础设施价值。然而它本质上是一套开放的法医/分析方法论和演示工具，而非可规模化收费的独占产品；长期复利效应取决于是否被纳入模型发布标准、许可证审计或学术引用体系。若
    Hugging Face 将其固化为平台能力，可增强其作为开源模型枢纽的地位，但独立商业化路径不清晰，因此介于'细分赛道基础设施'与'一次性工具'之间。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Hugging Face
- Alibaba
- Meta
- DeepSeek
- Mistral
competitive_casualty:
- LG
- 宣称'自研基座'但实际派生的模型厂商
- 依赖模型血统不透明性的闭源模型营销方
market_opportunities:
- 模型血缘与血统验证工具存在商业化空间，可面向投资机构、监管机构和企业采购部门提供 LLM 血统审计服务
- 开源模型合规审计赛道将迎来需求增长，尤其是验证衍生模型是否遵守原基座模型的开源许可证条款
- 创业者可开发面向 M&A 与技术尽调的模型指纹 SaaS，帮助买方识别'自研'声明背后的真实技术来源
risk_matrix:
  regulatory: 全球主要司法辖区（如欧盟 AI Act）正强化 AI 系统透明度与来源披露要求，'从零训练'与'基于开源权重派生'的表述若被证实误导，可能触发虚假宣传、许可证合规及监管处罚风险
  technological: 线性 CKA 对继续预训练派生模型的判别力有限（约 0.25 仅略高于无关模型基线 0.21），且存在被对抗性重初始化、词表混淆或架构微调等反取证手段绕过的可能
  competitive: 开源生态信任受损可能加剧，标榜'自研基座'的公司将面临更严苛的公众与同行审查，而真正从零训练的实验室则可能获得稀缺性溢价
  ethical: 模型血统曝光可能引发舆论审判、企业声誉危机及地域化技术民族主义情绪（如中韩社区对 LG 等机构的争议），并可能放大对'LLM 洗白'现象的公开指责
  additional:
  - 许可证与知识产权纠纷风险：若派生模型未遵守原开源权重许可条款，可能触发法律诉讼或社区抵制
  - 投资者尽调风险：AI 初创公司的技术估值高度依赖'是否自研基座'，模型基因组方法可能重塑估值假设
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: Model Genome
  canonical_name: Model Genome
  url: https://huggingface.co/blog/mayafree/model-dna
  positioning: 通过公开检查点文件为大型语言模型提供客观血缘鉴定的开源指纹方法。
  technical_signal: 结合 config.json 架构字段、tokenizer 词表重叠率与线性 CKA 三层证据判断模型是否派生。
  adoption_signal: 已覆盖韩国九家机构的公开基座模型，并在 Hugging Face 博客发布可复现的实现代码。
  ecosystem_relevance: 针对 2026 年 7 月多家机构发布自研 DeepSeek 对标模型的行业争议提供可验证工具。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 随着开源基座被大量商用化，Model Genome 提供了低成本的客观审计手段，有助于推动模型血统披露透明化。
  risk_notes:
  - CKA 对继续预训练派生模型的判别力有限，可能无法区分同族派生与无关模型。
  score: 8.0
  article_ids:
  - 7c2df17adfc3b600
  evidence_snippets:
  - 文章提出 Model Genome 指纹方法，依据 config.json 与 tokenizer.json 等公开文件客观回答模型是从零训练还是派生而来。
- object_type: product
  name: Model Genome Korea
  canonical_name: Model Genome Korea
  url: null
  positioning: 面向韩国 LLM 与 VLM 基础模型的在线血缘检测演示工具。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 模型审计人员
  - 开源社区研究者
  - AI 合规与透明度关注者
  product_signal: 可在线上传或比对韩国九家机构公开基座模型的架构、词表与表示相似度。
  market_signal: 回应近期韩国及全球自研大模型争议，提供可交互的透明度验证入口。
  differentiation: 将通用 Model Genome 方法落地到特定区域生态，提供一键式在线演示。
  watch_reason: 作为区域模型透明度基础设施的先行者，该演示可能成为评估韩国大模型真实血缘的常用入口。
  risk_notes:
  - 仅覆盖九家韩国机构，样本范围有限；演示依赖公开检查点文件可用性。
  score: 7.0
  article_ids:
  - 7c2df17adfc3b600
  evidence_snippets:
  - 作者将同一套衡量标准应用于韩国九家机构的公开基座模型，并提供了可在线试用的 Model Genome Korea 演示。
---

#### Model Genome Korea

DNA lineage test of Korean LLM & VLM foundation models

`config.json`

), `config`

+ tokenizer remain the primary evidence. We applied the exact same yardstick to the public foundation models of nine Korean organizations. Try it live: Model Genome Korea.
Building a large language model on top of an open-weight base (Qwen, Llama, DeepSeek, Mistral) is a legitimate, industry-standard practice. But it is *different* from training a foundation model from scratch — and vendors do not always make the distinction explicit. When several labs released DeepSeek-rivaling "self-developed" models in late July 2026 (e.g. LG K-EXAONE 2.0, 750B), the debate spilled into Chinese tech communities as well — a Zhihu thread (→ link) crossed 2.7M views. The natural question followed: **from scratch, or derived?**

This is answerable, objectively, from public files. Here is how.

Every `transformers`

checkpoint ships a `config.json`

. A handful of fields form a surprisingly discriminative signature:

`model_type`

`vocab_size`

`hidden_size`

`intermediate_size`

`num_hidden_layers`

`num_attention_heads`

/ `num_key_value_heads`

```
import requests
def arch_fingerprint(repo):
c = requests.get(f"https://huggingface.co/{repo}/resolve/main/config.json",
headers={"User-Agent": "genome/1.0"}).json()
return {k: c.get(k) for k in
("model_type", "vocab_size", "hidden_size",
"intermediate_size", "num_hidden_layers",
"num_attention_heads", "num_key_value_heads")}
```


The shape tuple `(hidden_size, intermediate_size, num_hidden_layers, heads, kv)`

is effectively a fingerprint of the reference architecture. When a model's tuple **matches a foreign open-weight exactly**, that is strong evidence the architecture was adopted rather than designed independently. Examples we measured:

| Model | shape (h · i · L · heads · kv) | Exact match |
|---|---|---|
| a 7B commercial model | 3584 · 18944 · 28 · 28 · 4 | Qwen2.5-7B |
| a 72B commercial model | 8192 · 29568 · 80 · 64 · 8 | Qwen2.5-72B |
| a 14B VLM | 5120 · 17408 · 40 · 40 · 8 | Qwen3-14B |
| an 8B model | 4096 · 14336 · 32 · 32 · 8 | Llama-3.1-8B |
| a MoE model | 7168 · 18432 · 61 · (moe 2048) | DeepSeek-V3 |

A single coincidental field means nothing; five simultaneously is a fingerprint.

Architecture alone can mislead. A model can copy a foreign *architecture* but train a genuinely new tokenizer, or vice-versa. The tokenizer is measured directly from `tokenizer.json`

, comparing the vocabulary sets with a min-overlap ratio:

```
def vocab_set(repo):
j = requests.get(f"https://huggingface.co/{repo}/resolve/main/tokenizer.json").json()
v = j["model"]["vocab"] # BPE: {token: id}
return set(v.keys())
def tok_overlap(a, b):
A, B = vocab_set(a), vocab_set(b)
return len(A & B) / min(len(A), len(B)) # 1.0 == subset
```


This immediately surfaces things `config`

hides. One model matched **Qwen2.5-7B's architecture exactly**, yet its tokenizer overlapped Qwen by only ~0.38 — a **"foreign brain, own language"** case: the architecture was adopted, but a new Korean tokenizer was trained. Conversely, some VLMs reused a base tokenizer verbatim (overlap = 1.000), confirming a straight fine-tune.

A practical trap:

`min(|A|,|B|)`

in the denominator (not the union) is what makes areducedvocabulary that is a strict subset of a larger one score ~1.0 — the correct signal for "carved out of the base."

The gold-standard question is: **were the weights trained from scratch, or continued-pretrained on a foreign base?** This is where two instructive traps live.

The naive idea: load `embed_tokens.weight`

from both models, and for shared tokens, average the row-wise cosine similarity. If they share lineage, embeddings should be similar.

They are not — *even when they obviously share lineage*. We measured near-zero mean cosine for **both** a known from-scratch model **and** a known Llama-derivative. The reason is **rotational invariance**: a Transformer's hidden space has no privileged basis, so two models can encode identical information under an arbitrary orthogonal rotation. Row-wise cosine sees rotation as dissimilarity. It cannot distinguish lineage.

**Linear CKA (Centered Kernel Alignment)** is rotation- and isotropic-scale-invariant, so it is the right tool for comparing representations:

```
import torch
def linear_cka(X, Y):
# X: (n, d1), Y: (n, d2) — SAME token order (shared vocab)
X = X - X.mean(0, keepdim=True)
Y = Y - Y.mean(0, keepdim=True)
num = (X.T @ Y).norm() ** 2
den = (X.T @ X).norm() * (Y.T @ Y).norm()
return (num / den).item()
```


A from-scratch model scored **near-zero CKA** against its candidate base — clean evidence of independent pretraining. But a continued-pretrained derivative scored only modestly higher (≈0.25) — barely above the baseline between two *unrelated* models of the same family (≈0.21). Large-scale training reshapes embeddings enough that CKA loses discriminative power on the *derivative* side.