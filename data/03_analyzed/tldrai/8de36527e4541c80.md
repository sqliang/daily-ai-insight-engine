---
title: Gemini Distillation Service (17 minute read)
source: https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/distillation?utm_source=tldrai
author: []
published: ''
created: '2026-07-29'
manifest_dates:
- '2026-07-29'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 8de36527e4541c80
source_type: news_media
tldr: 谷歌云推出 Gemini Distillation Service 早期访问版，允许企业用 gemini-3.1-pro 教师模型的输出与内部推理路径，蒸馏训练
  gemini-2.5-flash 学生模型。该服务仅需纯提示词 JSONL 数据集，无需人工标注答案，目标是在降低延迟和成本的同时提升小模型推理能力。
objective_summary: 谷歌云发布 Gemini Distillation Service（早期访问），让企业用户以 gemini-3.1-pro 为教师模型、gemini-2.5-flash
  为学生模型进行知识蒸馏。与仅使用最终文本输出的标准 SFT 不同，该服务同时利用教师模型的原始推理路径。蒸馏使用纯提示词数据集，教师模型在训练过程中自动生成目标输出，用户无需提供预期答案。任务需在
  us-central1 区域运行，须先向 Google 销售申请白名单并启用 Agent Platform API。蒸馏超参数包括 epochCount、learningRateMultiplier
  和 candidateCount，并支持从已蒸馏检查点继续连续微调。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Google Cloud
  technologies:
  - Model Distillation
  - SFT
  - Agent Platform API
  - JSONL
  key_people: []
key_logic_flow:
- Gemini Distillation Service 允许用户利用大型教师模型生成的输出与内部推理路径，训练更小、更高效的'学生'模型，以兼顾推理深度与生产级效率。
- 早期访问阶段支持的教师模型为 gemini-3.1-pro，学生模型为 gemini-2.5-flash。
- 该服务适用于高吞吐低延迟应用、缺乏人工标注数据、复杂推理任务以及教师模型显著优于基础学生模型的场景。
- 数据集采用 JSON Lines 格式的纯提示词数据集，存放在 Cloud Storage，教师模型在蒸馏过程中自动生成目标输出，无需提供预期答案。
- 蒸馏任务需配置教师模型的 candidateCount，以及学生训练的 epochCount、learningRateMultiplier 等超参数，并通过 REST
  版 Agent Platform API 提交到 us-central1 区域。
- 任务完成后学生模型自动注册到模型注册表并创建专用预测端点，同时支持仅从先前已蒸馏检查点进行连续微调。
object_mentions:
- object_type: product
  name: Gemini Distillation Service
  canonical_name: Gemini Distillation Service
  url: https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/distillation
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Gemini Distillation Service 允许用户训练一个更小、更高效的学生模型，该模型使用更大、能力更强的教师模型生成的输出与推理模式。
  - 早期访问期间，该服务仅支持以 gemini-3.1-pro 为教师模型、gemini-2.5-flash 为学生模型进行蒸馏训练。
  article_id: 8de36527e4541c80
- object_type: model
  name: gemini-3.1-pro
  canonical_name: gemini-3.1-pro
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 早期访问期间蒸馏服务支持的教师模型为 gemini-3.1-pro，其生成的目标输出与内部推理路径用于训练学生模型。
  - 在蒸馏任务示例中，教师模型被指定为 gemini-3.1-pro-preview，并通过 candidateCount 参数控制其响应生成数量。
  article_id: 8de36527e4541c80
- object_type: model
  name: gemini-2.5-flash
  canonical_name: gemini-2.5-flash
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 早期访问期间蒸馏服务支持的学生模型为 gemini-2.5-flash，蒸馏目标是在满足延迟与预算约束的同时接近 Pro 级模型的推理能力。
  - 连续微调仅支持以同一基础学生模型 gemini-2.5-flash 蒸馏出的先前检查点。
  article_id: 8de36527e4541c80
- object_type: product
  name: Gemini Enterprise Agent Platform
  canonical_name: Gemini Enterprise Agent Platform
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 蒸馏任务需在 Google Cloud 项目中启用 Agent Platform API，并拥有 Agent Platform Administrator
    这一 IAM 角色。
  - 蒸馏完成后，新的学生模型会自动注册到 Gemini Enterprise Agent Platform 模型注册表，并创建专用端点用于预测。
  article_id: 8de36527e4541c80
extract_result: success
impact_score:
  score: 6.5
  reason: 该服务将原本属于前沿实验室的知识蒸馏能力（尤其是利用教师模型内部推理路径的'推理蒸馏'）产品化为企业可直接调用的托管服务，并创新性地支持纯提示词数据集，消除了
    SFT 依赖人工标注的最大落地瓶颈。对企业而言，'用 Pro 的推理能力、付 Flash 的推理成本'直接改变了生产环境大模型部署的成本结构，属于改变局部竞争格局的重要产品发布。但受限于早期访问（需销售白名单）、仅支持
    gemini-3.1-pro→gemini-2.5-flash 单一模型对、且任务限定 us-central1 区域，短期覆盖面和生态冲击力有限，尚不构成范式转移。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 纯提示词蒸馏 + 利用教师内部推理路径，能否真正把 Pro 级推理能力压缩进 Flash 模型
hype_assessment:
  level: low
  reason: 本文为官方技术文档，通篇是具体可执行的技术规格（JSONL 数据集格式、candidateCount/epochCount 等超参数、区域与白名单限制、IAM
    权限），无任何'颠覆''革命性'等 PR 滥用措辞，并如实标注了早期访问的限制条件，属于实打实的干货而非概念包装。
information_entropy: high
domain_disruption:
  technical_innovation: 与标准 SFT 仅使用最终文本输出不同，该服务同时利用教师模型的内部推理路径（raw thoughts）进行知识迁移，将
    DeepSeek-R1 等实验室验证过的'推理蒸馏'路线工程化为企业级托管服务；纯提示词数据集设计让教师模型在训练过程中自动生成目标输出，直接解决了企业缺乏高质量人工标注数据的落地难题，并支持从已蒸馏检查点继续连续微调。
  business_model: 以'模型压缩即服务'切入企业模型定制化市场：企业可用 Flash 级推理成本获得 Pro 级推理能力，显著降低生产环境的推理账单；同时将蒸馏这一原本高度依赖专业
    ML 团队的能力门槛大幅拉低，强化对 Google Agent Platform 生态的绑定，形成'蒸馏成本换平台粘性'的商业模式。
engineering_complexity: production_ready
compound_value:
  score: 7.0
  reason: 投资逻辑推演：该事件的核心价值在于把'模型蒸馏'从'需人工标注ground-truth + 自建工程团队'的重模式，转化为'纯提示词数据集 +
    托管流水线'的轻模式，并独家利用教师模型的内部推理路径（raw thoughts）而非仅最终文本输出——这是当前 AWS Bedrock / Azure AI
    尚未提供的差异化能力，构成'用Pro的推理深度换Flash的成本延迟'的明确生产价值主张。复利效应体现在：企业一旦将蒸馏后的学生模型接入高吞吐生产链路，即对
    Google Cloud 形成较强平台锁定（模型注册表 + 专用预测端点 + 从已蒸馏检查点连续微调），且随真实流量数据积累形成'蒸馏-部署-再微调'的迭代飞轮，数据越用越多、模型越用越专。但需审慎看待两点：其一，早期访问阶段仅支持
    Gemini 家族内部蒸馏（Pro→Flash），跨模型/跨生态能力未开放，天花板受限于 Gemini 生态；其二，开源侧（如 DeepSeek 等已验证小模型蒸馏路径）若将同等能力开源化，该服务
    3-5 年后的差异化窗口可能收窄。综合属于'细分赛道基础设施级潜力但需持续验证'区间，评 7.0。
value_capture_layer: cloud_platform
moat_impact: creates_new_moat
key_beneficiaries:
- Google Cloud
- Google DeepMind (Gemini)
- AI 应用企业（高吞吐、低延迟、成本敏感场景）
competitive_casualty:
- 数据标注与人工 SFT 服务商
- 独立微调/蒸馏服务创业公司
- AWS Bedrock 与 Azure AI（蒸馏与推理路径能力相对落后）
market_opportunities:
- 企业可将海量存量用户提示词日志（无需人工标注答案）直接蒸馏为垂直场景的 gemini-2.5-flash 小模型，在高吞吐低延迟场景下大幅降低生产级推理成本
- 提示词级蒸馏大幅降低了模型定制门槛，技术服务商可围绕'蒸馏即服务'提供数据清洗、候选输出筛选、学生模型评测等端到端托管方案
- 蒸馏得到的低成本推理底座适合批量 Agent 应用（客服、代码补全、日志分析），可与微调、RAG 组合形成分层模型策略
risk_matrix:
  regulatory: 蒸馏任务限定 us-central1 区域，提示词数据集常含用户请求与隐私信息，跨境存储与处理需满足 GDPR、数据出境等合规要求；早期访问白名单制与区域绑定加深对
    Google Cloud 的供应商依赖
  technological: 蒸馏质量受教师模型能力上限约束，且深度绑定当前 gemini-3.1-pro/2.5-flash 版本，模型迭代后需重复蒸馏；推理路径蒸馏的跨平台可移植性差，存在技术锁定风险
  competitive: Google 将'蒸馏'产品化为托管服务，挤压第三方微调/蒸馏工具商的生存空间；同时该能力仅限 Gemini 生态，Anthropic/OpenAI
    及国内大模型厂商若跟进类似服务，将引发价格与功能竞争
  ethical: 直接使用生产流量中的提示词蒸馏，可能固化教师模型已有的偏见与幻觉；教师模型内部推理路径若包含未过滤的敏感中间推理，存在被学生模型复现并放大的伦理风险
  additional:
  - 早期访问定价与配额尚未公开，蒸馏与推理成本存在不确定性；白名单申请与 Agent Platform API 依赖带来接入摩擦
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: product
  name: Gemini Distillation Service
  canonical_name: Gemini Distillation Service
  url: https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/distillation
  positioning: 谷歌云推出的企业级模型蒸馏服务，以 gemini-3.1-pro 为教师、gemini-2.5-flash 为学生，用纯提示词数据集训练小模型，兼顾推理深度与生产级效率。
  technical_signal: 区别于仅用最终文本输出的标准 SFT，该服务同时利用教师模型的内部推理路径迁移深度推理，并支持 candidateCount
    等参数控制教师生成质量。
  adoption_signal: null
  ecosystem_relevance: 服务内置于 Gemini Enterprise Agent Platform，蒸馏完成的学生模型自动注册模型注册表并创建专用预测端点，与谷歌云模型管理生态深度集成。
  target_users:
  - 高吞吐低延迟应用开发者
  - 缺乏人工标注数据的企业团队
  - 需要复杂推理能力但受预算约束的生产环境用户
  - 希望通过小模型承接 Pro 级推理能力的谷歌云客户
  product_signal: 提供纯提示词 JSONL 数据集的蒸馏能力，教师模型在训练中自动生成目标输出，无需人工标注，支持多轮对话、系统指令与超参数配置，并可连续微调。
  market_signal: 该服务处于早期访问阶段，需经 Google 销售申请白名单并在 us-central1 运行，瞄准企业推理降本增效需求，作为闭源
    API 之外的模型优化路径补充。
  differentiation: 与标准 SFT 仅用最终文本不同，该服务利用教师模型的原始推理路径迁移深层推理能力，并以纯提示词数据集免除人工标注，形成独特竞争点。
  watch_reason: 企业级模型蒸馏正成为平衡推理质量与成本的关键技术路径，该服务首创性地将教师推理路径与纯提示词数据集结合，若早期访问验证有效，可能重塑企业模型部署的成本结构。
  risk_notes:
  - 服务处于早期访问阶段，仅支持 us-central1 区域且需销售白名单，可用性与覆盖范围有限。
  - 学生模型质量直接受限于教师输出，candidateCount 等生成参数配置不当可能影响蒸馏效果。
  - 该服务对推理能力保留程度的实际效果尚缺乏公开基准与客户验证。
  score: 7.0
  article_ids:
  - 8de36527e4541c80
  evidence_snippets:
  - Gemini Distillation Service 允许用户训练一个更小、更高效的学生模型，该模型使用更大、能力更强的教师模型生成的输出与推理模式。
  - 早期访问期间，该服务仅支持以 gemini-3.1-pro 为教师模型、gemini-2.5-flash 为学生模型进行蒸馏训练。
---

The **Gemini Distillation Service** (Distillation) allows users to
train a smaller, more efficient "student" model that uses the outputs and
reasoning patterns of a larger, more capable "teacher" model. While frontier
models define the cutting edge of AI, they can be over-provisioned for specific
enterprise use cases. Distillation bridges this gap, enabling
production-grade efficiency (lower latency and cost) while allowing smaller
models to achieve a deeper level of reasoning.

Unlike standard supervised fine-tuning (SFT) which only uses the final text output, distillation leverages:

**Teacher responses**: The final textual output.**Raw thoughts**: The internal reasoning paths generated by the teacher model.

## Supported models

The following models are supported for Distillation during early access:

**Teacher model**:`gemini-3.1-pro`

**Student model**:`gemini-2.5-flash`


## Suitable use cases

Distillation is recommended over standard prompting or supervised fine-tuning (SFT) in the following scenarios:

**High-volume, latency-sensitive applications**: When your application requires the reasoning capabilities of a Pro-tier model but must meet strict latency SLAs or budget constraints that necessitate a Flash-tier model.**Lack of ground-truth data (SFT is infeasible)**: When you have a large dataset of user prompts or queries, but lack the resources to manually label or generate high-quality ground-truth answers required for standard SFT.**Complex reasoning tasks**: Tasks involving multi-step logic, summarization of highly technical documents, or complex coding tasks where the base Flash model struggles, but the Pro model succeeds.**Significant performance gaps**: When the teacher model substantially outperforms the base student model on your specific task, providing a clear margin of knowledge to transfer during distillation.

## Prerequisites and project setup

Before starting a distillation job, ensure your Google Cloud environment is properly configured:

**Request access to the allowlist**: Ensure your Google Cloud project ID has been added to the allowlist for the Gemini Distillation Service early access. Contact your Google sales representative to get your project added to the allowlist.**Enable the API**: Enable the Agent Platform API in your Google Cloud project.**Set IAM role permissions**: You must have the Agent Platform Administrator (`roles/aiplatform.admin`

) IAM role.**Set the region**: Distillation jobs must be run in the`us-central1`

region.

## Dataset preparation

A key feature of this service is the use of **prompt-only datasets**. Because
the teacher model generates the target outputs during the distillation process,
you don't need to provide the expected answers.

### Dataset requirements

Datasets must be in JSON Lines (JSONL) format and stored in a Cloud Storage bucket. Each entry must adhere to the Gemini tuning dataset format in addition to the following:

**System instructions**: You can include an optional`systemInstruction`

field (with a 'system' role) to define system prompts.**Input**: The contents field (with a 'user' role) is required for the primary input.**Multi-turn prompts**: You can alternate between 'user' and 'model' roles, provided the final entry in the sequence is 'user'.

The following are two example `dataset.jsonl`

files:

```
{
"contents": [
{
"role": "user",
"parts": [
{
"text": "You're the artist here. Choose as many strands of thread as you like, as long as you're using three or more. Go for color combinations that you think would make a pretty pattern. Get creative! If you only use one color of thread, you won't be able to create a pattern.\n\nProvide a summary of the article in two or three sentences:\n\n"
}
]
}
]
},
```


```
{
"contents": [
{
"role": "user",
"parts": [
{
"text": "You're the artist here. Choose as many strands of thread as you like, as long as you're using three or more. Go for color combinations that you think would make a pretty pattern. Get creative! If you only use one color of thread, you won't be able to create a pattern.\n\nProvide a summary of the article in two or three sentences:\n\n"
}
]
},
{
"role": "model",
"parts": [
{
"text": "Choose several strands of embroidery thread in a variety of colors."
}
]
},
{
"role": "user",
"parts": [
{
"text": "You will need one egg (raw or hard boiled but hard boiled is best) and one spoon for each person participating in the race. You might even like to use dyed Easter eggs as something special for Easter. It's best to have this race on grass or some other soft surface, to give dropped eggs a chance!"
}
]
}
]
}
```


### Best practices

Use the following guidelines when creating your dataset:

**Size**: A minimum of 1,000 examples is recommended for noticeable quality improvement.**Diversity**: Ensure your prompts cover the edge cases and varying lengths expected in your production traffic.

## Configure the distillation request

A distillation job requires configuring both the teacher's generation behavior and the student's training hyperparameters.

### Configure the teacher model's generation behavior

You must define how the teacher model responds to your dataset. The quality of
the student model is directly bound by the quality of the teacher's output. To
configure the teacher model's generation behavior, set the `candidateCount`

:

`candidateCount`

: The number of response variations to generate. (Example:`4`

. Range`[1, 5]`

). If it is not specified in the request, a default value of`4`

will be used.

### Set the distillation hyperparameters

The distillation hyperparameters control the training process of the student model. For more information on hyperparameters in Gemini Enterprise Agent Platform, see the "Create a tuning job" section of the supervised fine-tuning guide.

The following hyperparameters must be set when creating a distillation job:

`epochCount`

: The number of times the student model will iterate over the dataset. (Example:`20`

. Range`[1, 100]`

). If not specified, a default value of`4`

is used.`learningRateMultiplier`

: Modifies the base learning rate of the student model. (Example:`2.0`

. Range`[0.25, 4]`

). If not specified, a default value of`1`

is used.

## Start the distillation job

During the early access period, you can submit and monitor distillation jobs using the REST version of the Agent Platform API. You can initiate a new distillation job or perform continuous tuning on an already distilled model checkpoint.

### Create a new distillation job

Create a JSON file named `request.json`

containing your job configuration. In
the following example, the teacher's generation configuration is nested within
the `hyperParameters`

field:

```
{
"description": "Distillation testing job.",
"baseModel": "gemini-2.5-flash",
"tunedModelDisplayName": "flash-distillation-run-1",
"distillationSpec": {
"promptDatasetUri": "gs://your-bucket/path/to/prompt_dataset.jsonl",
"validationDatasetUri": "",
"base_teacher_model": "gemini-3.1-pro-preview",
"hyperParameters": {
"epochCount": "20",
"learningRateMultiplier": 2.0,
"generation_config": {
"candidateCount": 5
}
}
}
}
```


Submit the job using `curl`

:

```
curl -X POST \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $(gcloud auth print-access-token)" \
https://us-central1-aiplatform.googleapis.com/v1beta1/projects/
````PROJECT_ID`/locations/us-central1/tuningJobs \
-d @request.json


### Perform continuous tuning

If you want to resume tuning from a previously distilled model checkpoint,
include the `preTunedModel`

block in your `request.json`

file. Continuous tuning
is only supported for previously distilled model checkpoints, with the same base
student model. Previously supervised fine-tuned model checkpoints (even with the
same base student model) aren't supported.

The following is an example of setting up continuous tuning for a previously distilled model checkpoint:

```
{
"description": "Continuous distillation testing job.",
"preTunedModel": {
"tunedModelName": "projects/
````YOUR_PROJECT_ID`/locations/us-central1/models/`PRETUNED_MODEL_ID`@1",
"checkpointId": "1",
"baseModel": "gemini-2.5-flash"
},
"tunedModelDisplayName": "flash-distillation-continuous",
"distillationSpec": {
"promptDatasetUri": "gs://your-bucket/path/to/prompt_dataset.jsonl",
"validationDatasetUri": "",
"base_teacher_model": "gemini-3.1-pro-preview",
"hyperParameters": {
"epochCount": "20",
"learningRateMultiplier": 2.0,
"generation_config": {
"candidateCount": 5,
}
}
}
}


Submit the payload using `curl`

:

```
curl -X POST \
-H "Content-Type: application/json" \
-H "Authorization: Bearer $(gcloud auth print-access-token)" \
https://us-central1-aiplatform.googleapis.com/v1beta1/projects/
````PROJECT_ID`/locations/us-central1/tuningJobs \
-d @request.json


## Monitor the distillation job

The submission response will return a job name containing your
`JOB_ID`. You can check the status of your job
(`state`

, errors, and final hyperparameters) by sending a GET request:

```
curl -X GET \
-H "Authorization: Bearer $(gcloud auth print-access-token)" \
-H "Content-Type: application/json; charset=utf-8" \
https://us-central1-aiplatform.googleapis.com/v1beta1/projects/
````PROJECT_ID`/locations/us-central1/tuningJobs/`JOB_ID`


You can also monitor the progress visually in the Google Cloud console by
navigating to **Agent Platform > Tuning** and choosing the
`us-central1`

region.

For this early access release, the Agent Platform Console UI has the following known limitations:

**Teacher sampling progress**: There is no progress widget for the teacher model sampling process. While the status may display "Running Prepare for tuning," the job is proceeding normally in the background.**Student tuning charts**: During the student model tuning stage, the UI provides charts for the Loss curve and total training text tokens.**Checkpoint table**: The UI displays a table of intermediate checkpoints and links to the generated Agent Platform prediction endpoint for evaluation. The "Epoch" column in this table displays '0' due to a known issue.

## Cancel the distillation job

To cancel an ongoing distillation job, do either of the following:

Use the Google Cloud console, changing the following URL:

`https://console.cloud.google.com/agent-platform/tuning/managed?project=`

`YOUR_PROJECT_ID`&vertex_ai_region=us-central1Replace

`YOUR_PROJECT_ID`with your project ID.Use

`curl`

to send a POST request to cancel the job:`curl -X POST \ -H "Authorization: Bearer $(gcloud auth print-access-token)" \ -H "Content-Type: application/json; charset=utf-8" \ https://us-central1-aiplatform.googleapis.com/v1beta1/projects/`

`YOUR_PROJECT_ID`/locations/us-central1/tuningJobs/`YOUR_JOB_ID`:cancelReplace the following:


with your project ID.`YOUR_PROJECT_ID`

with your job ID.`YOUR_JOB_ID`


## Evaluating the Result

After the distillation job successfully completes, the new student model is automatically registered in Gemini Enterprise Agent Platform Model Registry, and one or more dedicated endpoints are created to serve predictions. To evaluate the result, you locate the endpoint, send a prediction request, and finally evaluate.

To evaluate the result, do the following:

Send the following GET request to see the tuning job's status.

`curl -X GET \ -H "Authorization: Bearer $(gcloud auth print-access-token)" \ -H "Content-Type: application/json; charset=utf-8" \ https://us-central1-aiplatform.googleapis.com/v1beta1/projects/`

`YOUR_PROJECT_ID`/locations/us-central1/tuningJobs/`YOUR_JOB_ID`Replace the following:


with your project ID.`YOUR_PROJECT_ID`

with your job ID.`YOUR_JOB_ID`

Completed jobs display an

`endpoint`

field nested inside the`tunedModel`

object. Extract the

from the end of the returned path string (for example,`ENDPOINT_ID``projects/.../endpoints/`

). Note the endpoint ID.`YOUR_ENDPOINT_ID`Ensure that the tuning job completed successfully, because the endpoint isn't available while the tuning job is still running or failed. If the

`endpoint`

field is missing, then debug the tuning job by viewing the job's`state`

or`error`

keys.Create a JSON payload request named

`generate_content_request.json`

that contains your prompt:`{ "contents": { "role": "user", "parts": [ { "text": "hi, say something" } ] } }`

Use the following POST example to send a prediction request:

`curl -X POST \ -H "Content-Type: application/json" \ -H "Authorization: Bearer $(gcloud auth print-access-token)" \ https://us-central1-aiplatform.googleapis.com/v1beta1/projects/`

`YOUR_PROJECT_ID`/locations/us-central1/endpoints/`YOUR_ENDPOINT_ID`:generateContent \ -d @generate_content_request.jsonReplace the following:


: your project ID.`YOUR_PROJECT_ID`

: your job ID.`YOUR_JOB_ID`

: your endpoint ID.`ENDPOINT_ID`

Do the following to evaluate the results:

Run a holdout test set, using prompts not included in the training data, against your newly distilled model.

Compare the outputs against the base

`gemini-2.5-flash`

model to measure the quality improvements.Compare the outputs against the

`gemini-3.1-pro`

model to determine how closely the student approximates the teacher's reasoning.


## Limitations

The following table describes limitations for Distillation:

Distillation is subject to the following limitations:

**Model limitations**:- See the supported models

**Dataset restrictions**:**Volume limits**:- The maximum training set capacity is 50,000 examples.
- The source JSONL file size must not exceed 1 GB.

**Context window specifications**:- The service accommodates a maximum of 8,000 input tokens per entry. Distillation jobs are terminated if more than 10% of the provided entries exceed this established threshold.
- Teacher model sampling is limited to a maximum output of 24,000 tokens. In instances where the teacher model generates more than 24,000 tokens, the content is truncated at this limit, which may affect the performance of the student model.

**Modality**: Limited to text-based data. There is no support for multimodal inputs including video, imagery, or function calls.

**Configuration and hyperparameter limitations**- Adhere to the following boundaries when defining the distillationSpec
and its associated hyperParameters:
- Encryption: CMEK is unavailable for distillation tasks involving Google's first-party models.
`epochCount`

: Restricted to an integer value between 1 and 100.`learningRateMultiplier`

: Values must be within the floating-point range of`0.25`

-`4.0`

.


- Adhere to the following boundaries when defining the distillationSpec
and its associated hyperParameters:
**One-step distillation**: The teacher model sampling and student model tuning run in a single API call. If you have a large amount of data to sample, the same data has to be sampled again in the following tuning.

## Getting access

If you are interested in experimenting with Gemini Distillation Service, contact our Tuning Service team at cloud-ai-tuning-service-support@google.com to request access and project allowlisting.

To ensure optimal performance and resource management, we recommend creating a dedicated Google Cloud project for your distillation tasks. When reaching out to our team, provide your Project ID or project number to expedite the allowlisting process.

### Quota and Access Policies

The following quota and access policies are in effect:

Capacity: Projects recently added to our allowlist are provisioned with a default concurrent quota of 4. To prevent resource contention, we recommend using a separate project rather than one already running other Gemini tuning jobs.

Access period: Access is granted for an initial 30-day period.