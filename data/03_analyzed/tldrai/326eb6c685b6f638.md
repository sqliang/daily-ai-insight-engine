---
title: WavFlow Generates Audio Directly in Waveform Space (GitHub Repo)
source: https://github.com/facebookresearch/WavFlow?utm_source=tldrai
author: []
published: ''
created: '2026-05-22'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
id: 326eb6c685b6f638
source_type: news_media
tldr: Meta发布WavFlow，直接在原始波形空间从视频和文本生成音频，绕过潜在压缩。
objective_summary: Meta AI与Northeastern University研究团队于2026年发布WavFlow，一种直接在原始波形空间进行音频生成的方法。通过波形分块和振幅提升技术实现流匹配的稳定训练，在VGGSound和AudioCaps基准上与潜在空间方法性能持平。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Meta AI
  - Northeastern University
  - Facebook Research
  technologies:
  - WavFlow
  - waveform patchifying
  - amplitude lifting
  - flow matching
  - CLIP
  - Synchformer
  - MMAudio
  - JiT
  key_people:
  - Feiyan Zhou
  - Luyuan Wang
  - Shoufa Chen
  - Zhe Wang
  - Zhiheng Liu
  - Yuren Cong
  - Xiaohui Zhang
  - Fanny Yang
  - Belinda Zeng
key_logic_flow:
- WavFlow提出直接在原始波形空间生成音频的新范式，完全绕开潜在压缩步骤，实现端到端的波形生成
- 核心技术包括波形分块（waveform patchifying）将原始音频切分为可处理单元，以及振幅提升（amplitude lifting）稳定训练过程
- 通过直接x-prediction在原始音频上运行流匹配（flow matching），支持视频转音频（VT2A）和文本转音频（T2A）两种模态
- 在VGGSound视频转音频和AudioCaps文本转音频两个基准上，性能与主流潜在空间方法持平，证明端到端波形生成可行
- 代码已在GitHub开源（facebookresearch/WavFlow），提供完整安装脚本和推理配置，支持medium/large两种架构规模和16k/44k两种采样率
- 预训练模型权重因Meta组织政策限制暂未发布，团队表示正在基于全开源数据训练基础模型，当前用户需自行训练
pipeline_stage: fact_extracted
impact_score:
  score: 6.0
  reason: WavFlow提出了直接在原始波形空间进行音频生成的新范式，完全绕开潜在压缩，这在技术路线上具有探索性意义。但当前性能仅与主流潜在空间方法持平而非超越，且预训练权重因组织政策未发布，用户需自行训练，短期内难以形成实际产品冲击。综合来看属于重要学术贡献，改变局部研究方向，但尚未达到行业范式转移级别。
sentiment: positive
developer_sentiment:
  tone: skeptical
  primary_focus: 预训练模型权重因Meta组织政策限制无法发布，开发者需自行训练方可使用，实用门槛过高
hype_assessment:
  level: medium
  reason: 论文使用'paradigm'（范式）一词描述端到端波形生成路线，存在一定的学术包装意味；但实际性能声明诚实——仅'与潜在空间方法持平'(on par)，未宣称超越或颠覆。代码完整开源、配置文档详实，整体宣传克制，但'范式'表述略有拔高。
information_entropy: high
domain_disruption:
  technical_innovation: 通过波形分块（waveform patchifying）将原始音频切分为可处理单元，结合振幅提升（amplitude
    lifting）技术稳定流匹配训练过程，首次在原始44kHz波形空间实现端到端的视频/文本到音频生成，完全绕开编码器-解码器的潜在压缩环节。这简化了生成管线，避免了潜在空间的信息损失。
  business_model: 代码以MIT协议开源，支持视频转音频和文本转音频两种模态，架构可扩展（medium/large × 16k/44k四种配置）。但由于预训练权重未发布，当前商业模式影响有限。若后续开源基础模型权重落地，可能降低高质量音频生成的门槛，对配音、视频制作、游戏音效等SaaS工具链产生替代性影响。
engineering_complexity: prototype
compound_value:
  score: 4.5
  reason: WavFlow提出了一种绕过潜在压缩、直接在原始波形空间生成音频的新范式，理论上具备更高的保真度上限。但目前仅达到与潜在空间方法'持平'的性能水平，尚未建立显著优势；预训练权重因Meta政策限制未发布，社区需自行训练，大幅抬高采用门槛。作为Meta
    FAIR的开源研究项目，其长期价值取决于：(1)后续能否在性能上超越潜在方法；(2)全开源基础模型能否如期发布；(3)社区是否持续投入该范式。当前处于'概念验证'阶段，有潜力成为音频生成赛道的底层范式，但需持续验证，暂不具备复利确定性。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- Meta
- NVIDIA
- Hugging Face
competitive_casualty:
- ElevenLabs
- Stability AI
- 闭源音频生成平台
market_opportunities:
- 视频制作工具厂商可基于WavFlow的开源代码和训练方案，构建端到端视频配音功能，减少传统级联方案（视频理解→潜在空间生成→波形解码）的延迟和计算开销，尤其适用于短视频、直播等低延迟场景
- AI音频基础设施创业者可围绕原始波形空间生成范式，开发针对游戏音效、VR/AR空间音频、电影拟音等垂直场景的定制化微调方案，在潜在空间方法尚未覆盖的细分领域建立先发优势
- 音频生成领域研究人员可将波形分块（waveform patchifying）和振幅提升（amplitude lifting）技术迁移至其他原始信号生成任务（如EEG、地震波、传感器时序数据），探索端到端原始信号建模的跨领域应用
risk_matrix:
  regulatory: 无
  technological: 波形空间端到端生成范式存在被潜在空间方法持续超越的风险——若潜在空间方法在训练效率、推理速度和生成质量上持续领先，WavFlow的端到端优势可能不足以推动范式迁移；预训练权重缺失进一步削弱社区验证和迭代速度，增加技术路线被边缘化的可能
  competitive: Google DeepMind、OpenAI、Stability AI等均已布局音频生成赛道且拥有更强的算力和数据优势；Meta内部MMAudio等潜在空间方案可能形成内部竞争，分散团队资源和注意力；JiT等被WavFlow依赖的开源项目若停止维护将影响其技术栈稳定性
  ethical: 原始波形生成能力若扩展至语音合成场景，可能被滥用于深度伪造音频和语音克隆诈骗；目前WavFlow聚焦环境音效与视频同步，语音生成能力尚未暴露，伦理风险相对可控但需持续关注其能力边界扩展
  additional:
  - 预训练权重因Meta组织政策未发布，开源社区无法直接评估或使用，短期内严重限制生态扩散和实践验证
  - 完整训练流程对算力要求较高（medium/large两种架构规模×16k/44k两种采样率），中小团队和独立研究者难以复现，可能导致学术跟进集中在少数有算力资源的实验室
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: monitor
---

Feiyan Zhou1,2 ·
Luyuan Wang1 ·
Shoufa Chen1,* ·
Zhe Wang1 ·
Zhiheng Liu1 ·
Yuren Cong1 ·
Xiaohui Zhang1 ·
Fanny Yang1 ·
Belinda Zeng1

1 Meta AI · 2 Northeastern University

**WavFlow** introduces a paradigm for generating synchronized, high-fidelity audio from video and text inputs directly in the **raw waveform space**, bypassing latent compression entirely. Through *waveform patchifying* and *amplitude lifting*, WavFlow enables stable flow matching on raw audio via direct *x*-prediction. Evaluation on the VGGSound (VT2A) and AudioCaps (T2A) benchmarks shows that WavFlow delivers performance on par with established latent-based methods, proving that end-to-end waveform generation can match traditional frameworks in acoustic richness, fidelity, and synchronization.

|
## forest.mp4 |
## frog.mp4 |
|
## drum.mp4 |
## skateboard.mp4 |

See the

Project Pagefor 24+ samples and side-by-side benchmark comparisons.

```
git clone https://github.com/facebookresearch/WavFlow.git
cd WavFlow
bash scripts/setup.sh # creates conda env 'wavflow' and installs everything
conda activate wavflow
```

## Manual setup

```
conda create -n wavflow python=3.10 -y
conda activate wavflow
pip install -r requirements.txt
pip install -e . --no-deps
conda install -n wavflow -c conda-forge "ffmpeg<7" -y # for torio video decoding
```

All required external weights (CLIP, Synchformer, the empty-string CFG embedding) are downloaded or computed automatically on first run and cached under

`~/.cache/wavflow/`

.


⚠️ Due to organizational policy constraints, we are currently unable to release the production-trained checkpoints. We are working on a foundation checkpoint trained on fully open-source data; in the meantime you can train your own — see the training guide.

Once you have a trained checkpoint, run:

`bash scripts/launch/predict.sh [--gpu N] [--config PATH]`

The default config is `wavflow/configs/infer.yaml`

. The input CSV (`data.csv_path`

) accepts video, text, or both:

```
video_path,caption,video_exist,text_exist
/abs/path/sample1.mp4,a whistling rocket explodes,1,1 # video + text
/abs/path/sample2.mp4,birds chirping in a forest,1,1 # video + text
,a whistling rocket explodes,0,1 # text-only
/abs/path/sample3.mp4,,1,0 # video-only
```

## Configuration reference

| Flag / env | Default | Description |
|---|---|---|
`--gpu N` (or `GPU=N` ) |
`0` |
CUDA device index |
`--config PATH` (or `CONFIG_PATH=...` ) |
`wavflow/configs/infer.yaml` |
YAML config to load |
`WAVFLOW_ENV` |
`wavflow` |
conda env name to auto-activate |

Any extra positional argument is forwarded to `python -m wavflow.infer`

.

| Field | What to set |
|---|---|
`data.csv_path` |
the input CSV (above) |
`model.name` |
one of `medium_16k` , `medium_44k` , `large_16k` , `large_44k` (must match the trained ckpt) |
`model.ckpt_path` |
a `checkpoint_*.pth` (full ckpt) or `ema_epoch_*.pth` (EMA-only) |
`model.use_ema` |
`true` to load `model_ema1` from a full ckpt; `false` to use the live `model` weights |
`inference.duration_sec` / `target_sample_rate` |
output length and SR (must match model arch) |
`inference.cfg` , `num_steps` , `noise_scale` , `noise_shift` , `prediction_type` , `seed` |
sampling hyperparameters |
`inference.batch_size` |
rows per ODE batch |
`inference.trim_to_duration` |
trim output to `duration_sec` |
`output.output_dir` |
where wavs are written |
`output.loudness_norm` , `loudness_target_lufs` |
optional `pyloudnorm` post-processing |

`video_exist=0`

→ uses learned empty CLIP/Sync tokens (no video decode)`text_exist=0`

→ uses learned empty CLIP-text token (caption ignored)- Optional
`id`

column; otherwise the wav file name is derived from`Path(video_path).stem`

, falling back to`row_<idx>`

for text-only rows - Captions with commas must be quoted

The EMA tensor stored as `model_ema1`

is updated with `ema_decay = 0.9999`

per step. After only a few hundred / thousand steps it still contains random-init values and produces noise during inference. Set `model.use_ema: false`

(or pass an `ema_epoch_*.pth`

saved after enough steps) when sampling from a short / overfit run.

For feature extraction and training (single-node and multi-node), see **TRAINING.md**.

```
@misc{zhou2026wavflowaudiogenerationwaveform,
title={WavFlow: Audio Generation in Waveform Space},
author={Feiyan Zhou and Luyuan Wang and Shoufa Chen and Zhe Wang and Zhiheng Liu and Yuren Cong and Xiaohui Zhang and Fanny Yang and Belinda Zeng},
year={2026},
eprint={2605.18749},
archivePrefix={arXiv},
primaryClass={cs.SD},
url={https://arxiv.org/abs/2605.18749},
}
```

WavFlow builds on the open-source community. We gratefully acknowledge:

**MMAudio**— multimodal audio generation**JiT**— Just Image Transformer**Synchformer**— audio-visual synchronization