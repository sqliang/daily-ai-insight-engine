---
title: 'Bringing PyTorch Monarch to AMD GPUs: Single-Controller Distributed Training
  on ROCm (13 minute read)'
source: https://pytorch.org/blog/bringing-pytorch-monarch-to-amd-gpus-single-controller-distributed-training-on-rocm/?utm_source=tldrai
author: []
published: ''
created: '2026-07-08'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 35e3c45ac00b80ff
manifest_dates:
- '2026-07-08'
source_type: news_media
tldr: PyTorch Monarch 分布式训练框架正式移植到 AMD ROCm 平台，支持 AMD Instinct GPU 上的弹性容错训练。
objective_summary: PyTorch 团队将单控制器分布式训练框架 Monarch 移植到 AMD Instinct GPU 和 ROCm 平台，通过
  hipify_torch 转换 CUDA 代码为 HIP、链接 RCCL 替代 NCCL，并解决 ROCm 无静态链接库等跨平台问题，实现了在 AMD GPU
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - AMD
  - PyTorch Foundation
  technologies:
  - PyTorch Monarch
  - ROCm
  - CUDA
  - HIP
  - RCCL
  - NCCL
  - RDMA
  - Tokio
  - SLURM
  - Kubernetes
  - SkyPilot
  - DeepSeekV3-671B
  key_people: []
key_logic_flow:
- PyTorch Monarch 被移植到 AMD Instinct GPU 和 ROCm 平台，将单控制器分布式训练范式扩展到 CUDA 之外的硬件生态。
- 团队使用 hipify_torch 将 C++ 桥接代码从 CUDA 转换为 HIP，并链接 RCCL 替代 NCCL 实现集合通信，同时通过自动检测平台路由
  GPU 驱动 API。
- ROCm 不提供静态链接库（libamdhip64），与 NVIDIA 的 libcudart_static.a 不同，这一差异是移植过程中面临的关键跨平台工程挑战。
- Monarch 采用基于 actor 的运行时、进程网格抽象和异步执行模型，允许开发者从单个 Python 程序编排整个 GPU 集群。
- Monarch 的分层容错模型通过监督树在最低层级处理故障：演员拥有私有状态、崩溃不传播，本地重启秒级完成，仅在升级处理时需数分钟。
- 此前团队已在大规模集群上验证了 FP8 训练的准线性扩展能力，在 1024-GPU MI325 集群上使用 DeepSeekV3-671B 实现了 96.16%
  的扩展效率。
extract_result: success
impact_score:
  score: 6.0
  reason: 评分依据：该事件是 PyTorch 分布式训练框架 Monarch 从 CUDA-only 移植到 AMD ROCm 平台的基础设施更新。技术意义上，它解决了跨平台工程难题（静态链接差异、API
    路由、RCCL 替代 NCCL），并在 1024-GPU MI325 集群上验证了 96.16% 的扩展效率，证明了 AMD GPU 大规模训练的技术可行性。但其行业冲击力有限：Monarch
    本身并非当前最主流的分布式训练范式（FSDP、DeepSpeed 市占率更高），AMD Instinct GPU 在 AI 训练市场的份额仍远低于 NVIDIA。更重要的是，这是现有框架的横向移植而非范式创新，不会改变
    AI 训练的底层游戏规则。它对 AMD 生态和寻求摆脱 NVIDIA 锁定的用户具有实质意义，但对整个行业而言属于重要的生态拓展而非格局重塑。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: AMD ROCm 平台上的弹性容错分布式训练能力及单控制器编程模型
hype_assessment:
  level: low
  reason: 判定依据：文章整体呈现技术博客风格，没有使用 '颠覆'、'革命性' 等 PR 滥用词汇。内容聚焦于具体的工程实现细节——hipify_torch
    转换、RCCL 链接、静态库差异处理、监督树容错模型等，均为可验证的技术描述。96.16% 扩展效率引用了之前的工作成果而非空口承诺。整个文章信息密度高、技术诚实，不存在概念包装或过度宣传。
information_entropy: high
domain_disruption:
  technical_innovation: 将 PyTorch Monarch 的单控制器分布式训练范式（actor 运行时 + 分层容错监督树）从 CUDA
    移植到 ROCm 平台，解决了 HIP 运行时无静态链接库这一关键跨平台工程障碍，并通过 hipify_torch 自动转换和平台自动检测路由实现了 GPU
    驱动 API 的跨平台兼容。
  business_model: 降低了 AI 大规模训练对 NVIDIA 硬件的单点依赖，为 AMD Instinct GPU 进入企业级 AI 训练市场提供了成熟软件栈支撑，可能推动
    GPU 算力市场的竞争格局变化和定价优化。
engineering_complexity: production_ready
compound_value:
  score: 8.2
  reason: PyTorch Monarch 移植到 ROCm 是一个典型的'基础设施解锁'事件，具有显著的长期复利效应。首先，它打破了 NVIDIA CUDA
    在分布式训练领域的垄断——此前大规模分布式训练（千卡级别）几乎完全依赖 NCCL 和 CUDA 生态，AMD GPU 仅能用于单卡或小规模训练。Monarch
    通过 hipify_torch + RCCL 替代方案，使 AMD Instinct GPU 在 1024 卡规模验证了 96.16% 的扩展效率，证明 AMD
    在训练场景已具备与 NVIDIA 竞争的工程可行性。其次，Monarch 的分层容错模型（私有的 actor 状态、秒级本地重启、分钟级升级处理）解决了大规模训练的核心痛点——可靠性，这一架构创新不绑定具体硬件，具有跨平台复用的长期价值。第三，这是一个开源生态事件，PyTorch
    Foundation 作为中立方推动，不会造成新的供应商锁定，反而增加了整个 AI 训练基础设施的硬件弹性。潜在风险在于：ROCm 生态成熟度仍落后于 CUDA（如无静态链接库的工程坑），实际生产环境的广泛部署需要
    1-2 年验证周期。整体来看，如果 AI 训练规模继续按预期增长，硬件多样性将成为刚需，Monarch 作为跨平台分布式训练运行时，3-5 年大概率成为与
    NVIDIA NeMo 并列的基础设施选项，复利效应强。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- AMD
- PyTorch Foundation
- AWS (AMD Instinct instances)
- Lambda Labs
- CoreWeave
competitive_casualty:
- NVIDIA (CUDA software moat weakened)
- NVIDIA NeMo (proprietary distributed training framework)
market_opportunities:
- 企业 AI 团队可借此构建多供应商 GPU 策略，利用 AMD Instinct 集群运行 PyTorch Monarch 实现容错训练，降低对 NVIDIA
  硬件的单一依赖和成本压力
- 云服务商和 AI 基础设施公司可基于 Monarch on ROCm 推出弹性容错训练服务，作为 CUDA 生态之外的差异化卖点吸引价格敏感型客户
- AI 工程团队可探索将 Monarch 的分层容错模型（秒级本地重启）应用于现有训练流水线，显著降低大规模训练因硬件故障导致的计算浪费
risk_matrix:
  regulatory: AMD Instinct GPU 同样受美国对华出口管制影响（如 MI250/MI300 系列），该技术扩展可能面临地缘政治导致的硬件供应限制风险
  technological: ROCm 生态在软件成熟度、文档完善度和社区支持上仍落后于 CUDA；hipify_torch 转换路径可能引入长期维护成本和技术债务；ROCm
    不提供 HIP 运行时静态链接库，影响部署灵活性
  competitive: NVIDIA 在分布式训练领域仍占据绝对主导地位，FSDP、DeepSpeed 等成熟框架生态完善；AMD ROCm 的市场份额和开发者社区规模有限，Monarch
    on ROCm 短期内难以撼动 CUDA 生态的竞争格局
  ethical: 无
  additional:
  - AMD Instinct GPU 的实际供应量和可获取性有限，开发者可能面临硬件采购困难
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
---

### Featured projects

Training state-of-the-art large language models (LLMs) with billions of parameters requires distributed training across hundreds or thousands of GPUs. At this scale, hardware failures are not exceptional events—they are expected. A single GPU memory error, network partition, or node crash can bring down an entire training run that has been progressing for days or weeks. While our previous work demonstrated near-linear scaling of FP8 training at scale (achieving 96.16% scaling efficiency on a 1024-GPU MI325 cluster with DeepSeekV3-671B), the key challenge remains: reliability at scale.

To address these challenges, we have brought PyTorch Monarch to AMD Instinct GPUs with ROCm, expanding the single-controller model beyond CUDA environments and bringing this emerging runtime to a broader hardware ecosystem.

In this blog, we will explore the architecture of PyTorch Monarch, walk through the engineering effort required to port Monarch’s GPU runtime and distributed communication stack to ROCm, and demonstrate how the system dynamically recovers from node failures without halting the entire training job. By the end, you will understand how Monarch enables elastic, fault-tolerant distributed training on AMD GPUs and why this represents a significant step toward stable, large-scale AI infrastructure.

## The Challenge: Reliability at Scale

Traditional fault-tolerance strategies rely heavily on periodic checkpointing: saving the full model state to persistent storage at regular intervals. When a failure occurs, the entire job restarts from the last checkpoint. While conceptually simple, this approach has significant drawbacks.

Challenge |
Impact |
Checkpoint overhead |
Writing hundreds of gigabytes of model state to storage consumes time and I/O bandwidth. |
Wasted computation |
All progress since the last checkpoint is lost upon failure. |
Cluster idle time |
The entire cluster sits idle while the failed node is replaced and the job restarts. |
Scalability limits |
As cluster size grows, the probability of failure during any checkpoint interval increases. |

For truly large-scale training, scaling is not enough—training must also recover from failures. We need a more dynamic approach, one that allows healthy nodes to continue training while failed nodes recover and rejoin, minimizing wasted computation and maximizing GPU utilization. This is where PyTorch Monarch comes in.

## What is PyTorch Monarch?

PyTorch Monarch introduces a new distributed programming paradigm that enables developers to orchestrate entire GPU clusters from a single Python program. With its actor-based runtime, process mesh abstraction, and asynchronous execution model, Monarch simplifies large-scale distributed training and enables complex workflows that combine training, evaluation, and reinforcement learning within one unified script.

The architecture operates at multiple distinct levels:

**Python API**: A single-program interface where developers write simple Python code to get distributed GPU execution.**Monarch Runtime**: Manages actors and meshes, supervision trees, and tensor sharding.**Rust Runtime (Tokio)**: Ensures high performance and memory safety.**Infrastructure**: Integrates with RDMA, RCCL/NCCL, SLURM, Kubernetes, and SkyPilot.

*Figure 1: PyTorch Monarch architecture decoupling Python API from the Rust runtime and infrastructure.*

By decoupling the parallelism strategy used within each training replica from the fault-tolerance mechanism used across replicas, Monarch provides a cleaner fault-tolerance model. Failures are isolated (actors have private state, crashes do not propagate), hierarchical (handled at the lowest possible level), and recovery is fast (seconds for local restart, minutes only if escalated).

*Figure 2: Monarch’s hierarchical fault-handling model and supervision tree.*

## Porting Monarch to ROCm: Ecosystem Integration

Bringing Monarch to AMD GPUs required significant engineering effort to port the GPU runtime and distributed communication stack to ROCm.

We successfully implemented three main porting paths:

**Collective Communications**: We used`hipify_torch`

to convert the C++ bridge code from CUDA to HIP and linked against RCCL, which mirrors NCCL’s API.**GPU Memory Management**: We extended the build system to auto-detect the platform and route CUDA driver API calls through their HIP equivalents.**RDMA Integration**: Configuring`GPU_PLATFORM=rocm`

keeps the`libibverbs`

-based RDMA path intact while swapping the GPU-side bindings from CUDA to HIP for GPU-direct transfers.

*Figure 3: Porting Monarch from CUDA to ROCm via hipify_torch and auto-detection.*

Moreover, two cross-cutting issues shaped the port and deserve a closer look:

**No static link for the HIP runtime:**NVIDIA ships`libcudart_static.a`

, so the CUDA path links`cudart_static`

directly. ROCm ships no static equivalent for`libamdhip64`

, so the ROCm build links`amdhip64`