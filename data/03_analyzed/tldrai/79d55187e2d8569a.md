---
title: Short-Lived Credentials for AI Agents (12 minute read)
source: https://vercel.com/blog/the-end-of-credential-sprawl-for-agents?utm_source=tldrai
author: []
published: ''
created: '2026-08-27'
manifest_dates:
- '2026-08-27'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 79d55187e2d8569a
source_type: news_media
tldr: Vercel 宣布 Vercel Connect 正式全面可用（GA），以运行时按需申请、按任务限定作用域并自动过期的短期凭证替代长期 token，连接器生态已超
  100 个，并新增 RBAC、审计日志等治理能力。
objective_summary: Vercel 官方宣布 Vercel Connect 正式全面可用（GA）。该平台用运行时按需申请的短期凭证替代长期 token，凭证自动过期，作用域与身份均按请求粒度控制，应用不再存储供应商密钥。公开测试期间连接器生态已增至
  100 余个，GA 新增细粒度 RBAC、审计日志与 token 可观测性三项治理能力，并支持自定义 OAuth、API Key 以及具备 OAuth 能力的
  MCP 服务器作为连接器。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies:
  - Vercel
  - Slack
  - GitHub
  - Snowflake
  - Shopify
  - Notion
  - Workday
  - Linear
  - Salesforce
  - BuildPass
  - EF World Journeys USA
  technologies:
  - OIDC
  - OAuth
  - MCP
  - RBAC
  - API Key
  key_people:
  - Fraser Brown
  - Pat Dunn
key_logic_flow:
- Vercel Connect 今日正式全面可用（GA），在公开测试期间其连接器生态已增长至超过 100 个。
- 该平台用运行时按需申请、自动过期且按任务限定作用域的短期凭证，替代长期存在于环境中的 provider 密钥。
- 每次 Vercel 部署都携带 OIDC 身份，SDK 用该身份证明请求者，token 会自动刷新，应用不再需要存储供应商密钥。
- 凭证的作用域与身份均按请求粒度控制，例如 GitHub 可把 token 限制为单仓库只读，开源 GitHub Tools SDK 实现了这一模式。
- GA 新增三项治理能力：细粒度 RBAC 控制连接器管理权限、审计日志记录授权与连接器活动、token 与触发可观测性展示各项目使用情况。
- Vercel Connect 支持 100 余个预设连接器以及自定义 OAuth 与 API Key 认证，具备 OAuth 能力的 MCP 服务器也可作为连接器，并覆盖自定义环境、eve
  与 Chat SDK。
object_mentions:
- object_type: product
  name: Vercel Connect
  canonical_name: Vercel Connect
  url: https://vercel.com/connect
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Vercel Connect 今日正式全面可用，用运行时按需申请且自动过期的短期凭证替代长期 token，连接器生态已超过 100 个。
  - GA 新增细粒度 RBAC、审计日志以及 token 与触发可观测性三项治理能力，支持团队管控连接器并追踪访问使用情况。
  - Vercel Connect 支持自定义 OAuth 与 API Key 认证，任何具备 OAuth 能力的 MCP 服务器都可以充当连接器。
  article_id: 79d55187e2d8569a
- object_type: project
  name: GitHub Tools SDK
  canonical_name: GitHub Tools SDK
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章指出开源项目 GitHub Tools SDK 将该模式落地，选择 code-review 等预设即可生成仅含所需权限范围的短期 token。
  article_id: 79d55187e2d8569a
- object_type: product
  name: eve
  canonical_name: eve
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章提到 eve 开箱即用地支持 Vercel Connect，连接按每个 agent 声明，为 AI agent 提供连接能力。
  article_id: 79d55187e2d8569a
- object_type: product
  name: Chat SDK
  canonical_name: Chat SDK
  url: null
  confidence: low
  article_role: ecosystem_context
  evidence_snippets:
  - 文章指出 Chat SDK 支持将同一套连接模型带到对话式应用中，使聊天应用也能按需申请短期凭证。
  article_id: 79d55187e2d8569a
extract_result: success
impact_score:
  score: 6.5
  reason: 评分依据：该 GA 直击 AI agent 生态的普遍痛点——长期 token 泛滥与权限膨胀，把 OIDC 工作负载身份与按需短期凭证这套成熟机制产品化为平台能力，并配以
    100+ 连接器、RBAC、审计日志等治理能力，可能改变局部竞争格局（冲击传统 vault/密钥管理方案与 agent 鉴权框架），对 Vercel 生态内开发者有直接价值。但它本质是厂商平台功能的里程碑，底层
    OIDC/短期凭证技术早已是 AWS/GCP 的通用范式，并非全新范式突破，也未脱离 Vercel 平台边界，因此冲击力不足以进入 8 分以上的范式转移区间。综合评定
    6.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 用按需申请、自动过期、按请求限权的短期凭证替代长期 token，能否真正根治 agent 凭据泄露与权限膨胀问题，以及 100+
    连接器生态是否覆盖真实场景
hype_assessment:
  level: low
  reason: 判定依据：标题 'The End of Credential Sprawl' 属于典型的 PR 夸大话术，但正文给出了可验证的具体机制（每次部署携带
    OIDC 身份、SDK 运行时证明身份、token 自动刷新）、具体数字（连接器超 100 个）与具体治理能力（细粒度 RBAC、审计日志、token 可观测性），并以开源
    GitHub Tools SDK 佐证单仓库只读的限权实现；底层是标准 OIDC 技术而非噱头概念，整体水分不高，属于产品化落地而非概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 将 OIDC 工作负载身份与按需短期凭证模式系统化应用到 AI agent 场景：每次部署自动携带 OIDC 身份，SDK
    在运行时用该身份申请按任务限定作用域、自动刷新且无需存储供应商密钥的 token；凭证的作用域与身份均按请求粒度控制（如 GitHub 可限权到单仓库只读），并支持具备
    OAuth 能力的 MCP 服务器作为连接器，把 agent 访问外部系统的安全模型从'静态长期授权'推进到'动态按需授权'。
  business_model: 把凭据治理产品化为平台能力，使 Vercel 成为 agent 访问外部 SaaS 的控制平面：通过 RBAC、审计日志、可观测性与一键吊销沉淀治理价值，强化平台粘性与生态锁定；同时'平台签发短期凭证
    + 连接器市场'的模式为独立商业化与生态分成提供了新载体，可能推动整个行业从自建 token 轮换脚本转向平台化的短期凭证服务。
engineering_complexity: production_ready
compound_value:
  score: 7.0
  reason: Vercel Connect 押注的是 AI Agent 时代最核心的架构范式——用 OIDC 短时凭证替代长期 token，这一范式本身在 3-5
    年内几乎必然成为行业基石（云厂商 workload identity 已证明其正确性，Agent 的自主性与高频外部调用让它从'最佳实践'升级为'强制要求'）。其复利价值体现在两层：一是连接器生态的网络效应（100+
    连接器 × 用户数的双边飞轮，每新增一个连接器都提升平台对开发者的不可替代性），二是治理数据资产（RBAC、审计日志、token 可观测性沉淀的访问行为数据会随时间指数级增值，形成数据护城河）。但需保持审慎：它是'正确范式的某一实现'，且强绑定
    Vercel 平台——若 Vercel 在 Agent 运行时层争夺中未能成为默认入口，连接器生态的锁定效应将被削弱；同时 OAuth/OIDC 与 MCP
    等开放标准可能使该能力商品化。因此落在 4-7 区间的上沿：有潜力成为细分赛道基础设施，但需持续验证 Vercel 在 Agent 生态中的长期位置。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Vercel
- Anthropic
- GitHub
- Slack
- Snowflake
- Shopify
competitive_casualty:
- HashiCorp Vault
- 传统 RPA 厂商
- 独立 API 连接器初创公司
- 依赖长期凭证的 Agent 托管平台
market_opportunities:
- 建议关注 AI Agent 安全与身份管理赛道：可借鉴短期凭证+按需作用域模式，为多云/自建环境用户提供去平台化的凭证生命周期管理工具
- MCP 服务器与 SaaS 服务商可把自身 OAuth/API Key 能力接入 Vercel Connect 等连接器生态，借助平台的企业分发触达 agent
  工作流用户
- 企业安全团队可将 OIDC 工作负载身份+短期凭证模式迁移到内部 agent 架构，系统性减少长期 token 的泄漏面与轮换负担
risk_matrix:
  regulatory: 按需签发凭证与审计日志涉及身份数据和访问行为记录，面向欧盟市场需符合 GDPR 的数据最小化与日志保留要求；跨境分发凭证的企业还需关注数据驻留规定，整体合规风险中等偏低。
  technological: 该方案与 Vercel 部署的 OIDC 身份深度绑定，存在平台锁定与多云迁移障碍；AWS IAM Roles Anywhere、GCP
    Workload Identity、Azure 工作负载身份以及 SPIFFE/SPIRE 零信任网格构成潜在技术替代路径。
  competitive: AWS、Azure、Google Cloud 等云厂商与 HashiCorp Vault、Akeyless 等密钥管理厂商均在短期凭证/工作负载身份方向布局，Vercel
    Connect 面临巨头生态挤压；GitHub、Okta 等身份厂商也可能推出同类能力。
  ethical: Agent 以具名用户身份(subject)执行操作时，一次性授权流存在诱导授权(consent phishing)与被越权调用的风险；审计日志大规模采集代理行为细节可能引发员工隐私与过度监控争议。
  additional:
  - 平台绑定：连接器配置、RBAC 与审计能力深度耦合 Vercel 生态，后续迁出成本高
  - 大规模 Agent 并发按需签发凭证可能引入额外延迟与费用，需验证性能与成本边界
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: product
  name: Vercel Connect
  canonical_name: Vercel Connect
  url: https://vercel.com/connect
  positioning: Vercel 的连接器管理平台，以运行时按需申请、自动过期、按任务限定作用域的短期凭证替代长期 token，面向 AI agent 提供安全的外部服务访问。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - Vercel 平台上的 AI agent 开发者
  - 需要在多服务间安全调用 API 的团队
  product_signal: GA 新增细粒度 RBAC、审计日志与 token 可观测性，并支持 100 余个预设连接器及自定义 OAuth、API Key
    与 MCP 服务器接入。
  market_signal: 公开测试期间连接器生态已增至 100 余个，覆盖 Slack、GitHub、Snowflake 等主流开发者工具与 SaaS 服务，生态扩张势头明显。
  differentiation: 以 OIDC 身份证明请求者并按请求粒度控制凭证作用域，应用不再存储供应商密钥，区别于传统 vault 长期密钥管理方案。
  watch_reason: Vercel Connect 将短期凭证、按请求作用域与治理能力整合进部署平台，可能重塑 AI agent 连接外部服务的默认安全范式，其生态扩张与治理演进值得持续跟踪。
  risk_notes:
  - 凭证作用域精细度受限于服务商能力，GitHub 之外的服务未必支持单仓库只读级的细粒度控制。
  - 平台深度绑定 Vercel 部署生态，非 Vercel 用户的采用成本可能限制其市场覆盖范围。
  score: 8.0
  article_ids:
  - 79d55187e2d8569a
  evidence_snippets:
  - Vercel Connect 今日正式全面可用，用运行时按需申请且自动过期的短期凭证替代长期 token，连接器生态已超过 100 个。
  - GA 新增细粒度 RBAC、审计日志以及 token 与触发可观测性三项治理能力，支持团队管控连接器并追踪访问使用情况。
  - Vercel Connect 支持自定义 OAuth 与 API Key 认证，任何具备 OAuth 能力的 MCP 服务器都可以充当连接器。
- object_type: project
  name: GitHub Tools SDK
  canonical_name: GitHub Tools SDK
  url: null
  positioning: 开源 GitHub 工具 SDK，通过 code-review 等预设按需生成仅含所需权限范围的短期 token，落地按任务限定作用域的凭证模式。
  technical_signal: 将短期凭证模式落地为开源实现，按 code-review 等预设仅生成所需权限范围的 token，体现最小权限原则的工程化实践。
  adoption_signal: null
  ecosystem_relevance: 作为 Vercel Connect 的开源参考实现，展示按请求作用域凭证在 GitHub 场景下的落地方式，对开发者工具生态有示范意义。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为短期凭证模式在主流代码托管平台上的开源落地样本，其预设粒度设计与安全实践可能影响同类开发者工具的安全架构方向，值得跟踪后续演进。
  risk_notes:
  - 项目仅作为 Vercel Connect 的示例被提及，独立维护活跃度与社区规模尚不明确。
  - 依赖 Vercel Connect 生态，脱离该平台的通用性有待进一步验证。
  score: 5.0
  article_ids:
  - 79d55187e2d8569a
  evidence_snippets:
  - 文章指出开源项目 GitHub Tools SDK 将该模式落地，选择 code-review 等预设即可生成仅含所需权限范围的短期 token。
- object_type: product
  name: eve
  canonical_name: eve
  url: null
  positioning: Vercel 的文件系统优先框架，用于构建持久化 AI agent 应用，开箱即用地支持 Vercel Connect，为 agent
    提供声明式连接能力。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 构建 AI agent 的开发者
  - 需要声明式连接外部服务的 agent 应用团队
  product_signal: 开箱即用支持 Vercel Connect，连接按每个 agent 声明，为 AI agent 提供声明式的安全连接能力。
  market_signal: null
  differentiation: 采用文件系统优先设计并以 per-agent 声明式连接与 Vercel 平台深度集成，区别于需要自行管理凭证的 agent
    框架。
  watch_reason: eve 是 Vercel 面向 AI agent 应用的核心框架，与 Connect 深度集成，反映 Vercel 在 agent
    基础设施层的布局，其采纳进展值得持续跟踪。
  risk_notes:
  - eve 仍处早期阶段，公开证据有限，其独立价值与生态成熟度需要进一步观察。
  score: 5.0
  article_ids:
  - 79d55187e2d8569a
  evidence_snippets:
  - 文章提到 eve 开箱即用地支持 Vercel Connect，连接按每个 agent 声明，为 AI agent 提供连接能力。
---

Every useful agent reaches beyond your codebase. It posts to Slack, opens pull requests, queries Snowflake, or calls an internal API. That reach is what makes it valuable, and it's also where the risk lives, because for years, granting it meant provisioning a long-lived token and hoping it never leaked.

Vercel Connect replaces long-lived tokens with ones your code requests at runtime, scoped to the task and expiring on their own.

During the public beta, we've grown the ecosystem past 100 connectors, unified how they work, and added the governance capabilities teams need in production.

Today, Vercel Connect is generally available.

## Copy link to headingVaults don't fix long-lived tokens

Managing credentials has become its own workload. Teams write rotation scripts, copy secrets across environments, and share tokens between users. Putting a token in a vault made it harder to steal, but no less dangerous once stolen. It never expires, and no vault limits what a leaked credential can do.

Agents compounded the problem by touching more systems with greater autonomy, more often. Yet the tools to contain a secret haven't changed.

With Vercel Connect, your app never stores credentials. It requests one:

You register a connector once for a provider like Slack, GitHub, Snowflake, Shopify, or your own OAuth service

You attach it to the projects and environments that need it

Your code requests a token at runtime, and it refreshes automatically

Your app has no provider secret to commit by accident


`vercel connect create slack --name acme-slack`


Create a Slack connector named acme-slack

```
import { getToken } from '@vercel/connect';
const token = await getToken('slack/acme-slack', { subject: { type: 'app' },});
```


Request an app-level Slack token

Requesting a token doesn't require another secret. Every deployment on Vercel carries an OIDC identity, and the SDK uses it to prove who's asking.

## Copy link to headingWhat changes when access becomes a request

The difference shows up in the properties of the credential:

Credentials that used to sit in environments long after the work finished now expire on their own. Nothing lingers for an attacker to find.

Minting short-lived tokens instead of keeping provider credentials in paused sandboxes has removed a whole class of security risk for us.Fraser Brown BuildPass

Scoping happens per request. One step of an agent might read a repository, the next opens an issue, and each asks for only that.

How fine-grained a token can be depends on the service, and GitHub is the clearest example. Requests can restrict a token to a single repository with read-only permissions, rather than trusting a standing grant organization-wide.

The open-source GitHub Tools SDK puts this into practice. Choose a preset like `code-review`

, and it mints tokens with only the required scopes.

Identity is per request as well. Tokens act as the app by default, but pass a named user as the `subject`

and the token acts on their behalf, scoped to what they authorized during a one-time consent flow.

## Copy link to headingConnectors for the services you already use

Vercel Connect now ships with 100+ preset connectors for developer tools and SaaS providers like Notion, Shopify, and Workday, as well as managed connectors for Slack, GitHub, Linear, Salesforce, and more.

Your own services follow the same model via custom OAuth and API Key authentication, and any OAuth-capable MCP server can serve as a connector.

Finding and creating them is faster, too. Browse the full catalog on the Vercel website, create a connector in fewer steps, and manage everything from the dashboard, CLI, or API.

Vercel Connect allowed us to quickly deploy new AI agents and channel integrations to our teams, while saving us the time and headache of rolling our own secure token management.Pat Dunn EF World Journeys USA

## Copy link to headingGovernance that scales with your team

Teams adopting Vercel Connect need more than scoped tokens. They need to control who manages connectors and to track how access is used.

GA adds three capabilities:

Fine-grained RBAC controls who can create and manage connectors

Audit logs record authorization and connector activity

Token and trigger observability shows usage across projects


Together with per-environment attachment and one-command revocation, access becomes something you can inspect and prove.

When an auditor asks who had access to a system and when, the answer is a query against the audit log, not an investigation across projects and Slack threads.

## Copy link to headingWherever your agents run

Vercel Connect is available wherever you and your users are:

Custom Environments are supported, so a

`qa`

environment gets its own connector alongside production, preview, and developmenteve supports Connect out of the box, with connections declared per agent

Chat SDK support brings the same model to conversational apps