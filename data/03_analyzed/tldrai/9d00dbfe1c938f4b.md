---
title: Agent Plugins are the future of Agent Skills (13 minute read)
source: https://x.com/GoogleCloudTech/status/2087733334617063503?utm_source=tldrai
author: []
published: ''
created: '2026-08-15'
manifest_dates:
- '2026-08-15'
- '2026-08-16'
- '2026-08-17'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 9d00dbfe1c938f4b
source_type: news_media
tldr: Google 发布开放、厂商中立的 Agent Plugins 标准，将 Agent Skills 及其依赖的 MCP server 打包进单一可移植文件夹，供任意兼容客户端加载，Google
  已以核心维护者身份加入技术指导委员会。
objective_summary: Google Cloud 开发者关系工程师 Lavi Nigam 撰文介绍 Agent Plugins 规范，其目标是把 Agent
  Skills 指令与其依赖的 MCP server 打包为一个可移植文件夹，供任何兼容客户端加载。该规范采用封闭 schema，仅允许十个顶层字段，最小清单为
  manifest 与 mcp.json 两个文件。Google 已作为核心维护者加入技术指导委员会，并公布了详细规范文本。规范还界定了目录发现深度与占位符展开规则，同时禁止在
  headers 中放置凭据。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Google
  - Google Cloud
  technologies:
  - Agent Plugins
  - Agent Skills
  - MCP
  - ADK
  - Agents CLI
  - SKILL.md
  - mcp.json
  - OAuth
  key_people:
  - Lavi Nigam
key_logic_flow:
- Agent Skills 提供按需加载的指令文件夹，能让模型在任务匹配时才拉入专业知识，但一直没有解决技能的分发问题。
- 需要工具的技能由 SKILL.md 指令与 MCP server 两部分构成，此前二者的绑定只能写在 README 里，且不同客户端各有各的打包格式。
- Agent Plugins 将技能及其依赖的 MCP server 封装进单一可移植文件夹，并采用封闭 schema，只允许十个顶层字段，最小清单只需 manifest
  与 mcp.json 两个文件。
- Google 以核心维护者身份加入技术指导委员会，规范规定客户端只对 skills 目录做一级深度发现，mcp.json 需显式声明三种传输方式之一。
- 规范明确了 PLUGIN_ROOT 等占位符的展开范围与文本单遍展开规则，并禁止在 headers 中放置凭据，Agent Plugins 1.0.0 未定义可移植的
  OAuth 或凭据引用字段。
object_mentions:
- object_type: project
  name: Agent Plugins
  canonical_name: Agent Plugins
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Agent Plugins 是一个开放、厂商中立的标准，把 Agent Skills 以及它们依赖的 MCP server 打包进一个可移植文件夹，任何兼容客户端都能加载。
  - Google 加入技术指导委员会并担任核心维护者，文章称启动贴介绍了公告，而详细规范则承载了完整的实现细节。
  article_id: 9d00dbfe1c938f4b
- object_type: project
  name: google/skills
  canonical_name: google/skills
  url: https://github.com/google/skills
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者表示团队在 google/skills 仓库中重度使用 Agent Skills，并随 Agents CLI 一同发布了七个内置技能。
  article_id: 9d00dbfe1c938f4b
- object_type: product
  name: Agents CLI
  canonical_name: Agents CLI
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Agent Skills 随 Agents CLI 工具一起交付七个内置技能，文章以此作为 Agent Plugins 打包之前的实践案例。
  article_id: 9d00dbfe1c938f4b
- object_type: project
  name: Agent Skills
  canonical_name: Agent Skills
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - Agent Skills 提供按需的专业技能，即一整套指令文件夹，模型只在任务匹配时拉入，避免上下文窗口长期携带部署手册。
  article_id: 9d00dbfe1c938f4b
extract_result: success
impact_score:
  score: 7.0
  reason: Agent 工具互联互通是当前行业的核心痛点：Agent Skills 解决了按需加载指令的问题，但技能与 MCP server 的绑定长期依赖
    README 手抄，各客户端各搞一套打包格式。Agent Plugins 以厂商中立身份给出封闭 schema + 双文件最小清单的可移植打包标准，直击分发断层，具备成为
    Agent 生态'wheel 格式'的潜质，对工具链分发和开发者工作流有实质影响。但作为标准提议而非产品发布，其短期冲击力取决于 Anthropic/OpenAI
    等主要客户端是否跟进采纳，属于'重要标准发布、改变局部竞争格局'级别，尚未达到 ChatGPT 发布式的范式转移，故给 7.0 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 技能 + MCP server 的统一打包格式能否被各大 Agent 客户端采纳为跨厂商事实标准
hype_assessment:
  level: low
  reason: 标题 'future of Agent Skills' 带有 PR 色彩，但正文与规范文本包含大量硬约束：封闭 schema 仅允许十个顶层字段、客户端只做一级深度目录发现、占位符为文本单遍展开且不作用于
    command/headers、mcp.json 必须显式声明三种传输之一、1.0.0 明确不定义可移植 OAuth/凭据引用。这些边界规则说明规范经过了真实工程推敲而非概念包装，判定为实打实的干货。
information_entropy: high
domain_disruption:
  technical_innovation: 首次将 Agent Skills 指令与 MCP server 依赖绑定为单一可移植文件夹，以封闭 schema（十个顶层字段）+
    最小双文件清单（manifest + mcp.json）实现任意兼容客户端的统一加载；明确了仅对 skills/ 做一级发现、占位符（PLUGIN_ROOT
    等）只在 args/env/cwd 做单遍文本展开、禁止在 headers 放置凭据等边界规则，本质是把 Agent 工具分发从 README 约定升级为机器可读标准，类似
    Python wheel 之于库分发。
  business_model: 标准化后的插件可移植文件夹为 Agent 插件注册表/市场（类比 npm registry、VS Code Marketplace）奠定基础，有望催生技能分发与收费的新商业模式；同时
    Google 以核心维护者身份主导 TSC，可强化 Cloud 生态中 ADK 与 Agents CLI 的采用，而厂商中立设计降低了其他云厂商和客户端的采纳壁垒，属于生态卡位而非封闭锁定。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: 这是典型的协议/标准层投资逻辑，价值不在单一产品而在网络效应：技能作者、兼容客户端、分发渠道三方一旦形成正循环，Agent Plugins 有潜力成为
    Agent 技能分发的'事实标准'，类似 npm/OCI 之于软件业，具备强复利积累效应。关键加分项有三：一是 MCP 已被 OpenAI/Google/Microsoft/Anthropic
    等巨头采纳，Agent Plugins 补齐了 MCP 生态缺失的'分发'环节，站在已有势能之上而非从零造标准；二是打包格式与现有 Agent Skills
    目录结构向后兼容，迁移成本极低，被采纳概率显著提升；三是 Google 以核心维护者身份加入技术指导委员会，提供持续治理与推广资源。但风险同样明确：标准尚处早期，1.0.0
    未定义可移植的 OAuth/凭据引用，且 OpenAI 等存在竞争性封装格式的替代路径，市场仍可能碎片化，能否在 3-5 年后成为行业基石仍需持续验证，故给予细分赛道基础设施上沿的评分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Google Cloud
- Anthropic
- LangChain
- Agent 技能分发平台
- MCP 工具链厂商
competitive_casualty:
- OpenAI 封闭插件/技能生态
- 采用私有技能格式的闭源 Agent 平台
- 传统 RPA / 流程自动化厂商
market_opportunities:
- 技能分发层尚属空白，创业者可基于 Agent Plugins 标准抢先布局插件注册表/技能市场（类比 npm 之于代码），为各类 Agent 客户端提供可移植技能的发现、安装与版本管理服务
- 存在一站式迁移与合规工具链机会：开发将现有 Agent Skills + MCP server 自动打包为符合 Agent Plugins 规范的工具（CLI
  验证器、CI 集成、schema lint），帮助企业以近乎零成本完成技能资产的标准化封装
- 大型企业内部技能治理是落地场景：将散落在团队中的 MCP server 与指令文件夹统一打包为可移植插件，实现跨客户端复用、审计与访问控制，可延伸为私有的插件内网门户
risk_matrix:
  regulatory: 规范宣称厂商中立但由 Google 主导技术指导委员会，若与欧盟 AI Act 的互操作性要求或各云厂商自研标准冲突，可能出现标准分裂；1.0.0
    未定义可移植 OAuth/凭据引用字段，跨辖区部署时的身份认证与数据合规仍需企业自行承担
  technological: Agent Plugins 强绑定 MCP 与 Agent Skills 规范，若 MCP 协议自身演进（或 A2A 等竞争协议兴起）则打包格式随之下沉；封闭
    schema 仅允许十个顶层字段且无凭据引用，生产级认证能力明显滞后
  competitive: Anthropic 在 MCP 生态具主导话语权，微软 Copilot、亚马逊 Bedrock Agents 各有自建技能体系，各巨头可能维护私有打包格式；'厂商中立'能否兑现取决于非
    Google 客户端（Claude 客户端、微软 Copilot 等）是否真正兼容采用，存在生态挤压风险
  ethical: 插件分发放大了第三方供应链风险，恶意 MCP server 可借技能打包伪装传播；headers 作为明文包数据可被任何下载者读取，涉及隐私与敏感信息泄露；凭据字段缺失使身份安全设计滞后
  additional:
  - 供应链完整性：规范未定义插件签名/哈希校验机制，第三方分发渠道可能成为恶意代码注入与投毒的攻击面
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: Agent Plugins
  canonical_name: Agent Plugins
  url: null
  positioning: Agent Plugins 是一个开放、厂商中立的 Agent Skills 分发标准，将技能指令与其依赖的 MCP server 打包进单一可移植文件夹，供任意兼容客户端加载。
  technical_signal: 规范采用封闭 schema 仅允许十个顶层字段，最小清单只需 manifest 与 mcp.json 两个文件，并规定客户端只对
    skills 目录做一级深度发现。
  adoption_signal: Google 已以核心维护者身份加入技术指导委员会，正式推动该标准的落地与演进，并公开完整规范文本供社区实现。
  ecosystem_relevance: 该标准与 Agent Skills 和 MCP server 生态深度耦合，旨在统一各客户端各自发明的打包格式，解决技能分发碎片化问题。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Agent Plugins 直击 Agent Skills 分发缺失这一关键痛点，由 Google 主导并加入技术指导委员会，若被主流客户端采纳，将可能成为
    Agent 技能与工具分发的行业标准，值得持续跟踪其生态落地进度。
  risk_notes:
  - 规范 1.0.0 未定义可移植的 OAuth 或凭据引用字段，认证仍依赖客户端管理，可能限制含鉴权工具的插件分发。
  - 封闭 schema 与一级目录发现规则对已有大型技能库迁移不够友好，分类目录会被静默忽略且无错误提示。
  score: 8.0
  article_ids:
  - 9d00dbfe1c938f4b
  evidence_snippets:
  - Agent Plugins 是一个开放、厂商中立的标准，把 Agent Skills 以及它们依赖的 MCP server 打包进一个可移植文件夹，任何兼容客户端都能加载。
  - Google 加入技术指导委员会并担任核心维护者，文章称启动贴介绍了公告，而详细规范则承载了完整的实现细节。
- object_type: project
  name: google/skills
  canonical_name: google/skills
  url: https://github.com/google/skills
  positioning: google/skills 是 Google 团队重度使用 Agent Skills 的开源仓库，并随 Agents CLI 发布七个内置技能，作为
    Agent Plugins 打包实践的先行案例。
  technical_signal: 该仓库展示了 Agent Skills 的目录结构与 SKILL.md 指令组织方式，是理解技能打包前形态的直接参考实现。
  adoption_signal: 作者明确表示团队在 google/skills 仓库中重度使用 Agent Skills，表明该仓库已具备实际生产使用基础。
  ecosystem_relevance: 作为 Google 官方技能仓库，其内置技能将随 Agents CLI 分发，成为 Agent Plugins 生态中技能内容的主要供给来源。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: google/skills 是观察 Google Agent 技能生态演进的核心窗口，其仓库结构与内置技能数量变化可直接反映 Agent
    Plugins 标准的采纳进度，且对开发者学习技能编写有参考价值。
  risk_notes:
  - 该对象仅作为文章中的实践背景提及，缺少独立的仓库活跃度与社区规模信息，需结合 GitHub 实际数据进一步验证。
  score: 5.0
  article_ids:
  - 9d00dbfe1c938f4b
  evidence_snippets:
  - 作者表示团队在 google/skills 仓库中重度使用 Agent Skills，并随 Agents CLI 一同发布了七个内置技能。
- object_type: product
  name: Agents CLI
  canonical_name: Agents CLI
  url: null
  positioning: Agents CLI 是 Google 随 Agent Skills 一同交付的命令行工具，内置七个开箱即用的技能，是开发者体验 Agent
    Plugins 打包前形态的入口。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用 Google 开发工具链的 AI 应用开发者
  - 探索 Agent Skills 与 MCP 技能的开发者
  product_signal: Agents CLI 随工具交付七个内置技能，为开发者提供开箱即用的技能体验，并作为 Agent Plugins 打包能力之前的实践载体。
  market_signal: 该工具以内置技能形式降低 Agent 技能上手门槛，是 Google 推动 Agent 技能生态从规范走向开发者工具链落地的一环。
  differentiation: 与需要手动在 README 中绑定 SKILL.md 与 MCP server 的做法不同，Agents CLI 通过内置技能提供了更一体化的分发与使用体验。
  watch_reason: Agents CLI 作为 Agent Plugins 打包能力的配套分发工具，其内置技能与 CLI 演进可反映 Google Agent
    工具链的落地节奏，对开发者选型有直接参考意义。
  risk_notes:
  - 文章仅简要提及该工具随附七个技能，缺少功能范围、平台支持与社区采用情况等详细信息，需谨慎评估其成熟度。
  score: 4.0
  article_ids:
  - 9d00dbfe1c938f4b
  evidence_snippets:
  - Agent Skills 随 Agents CLI 工具一起交付七个内置技能，文章以此作为 Agent Plugins 打包之前的实践案例。
---

Agent Plugins is an open, vendor-neutral standard for packaging Agent Skills and the MCP servers they depend on into one portable folder that any compatible client can load. Google is joining the Technical Steering Committee as a Core Maintainer. The launch post covers the announcement, and the specification carries the detail. This is what we learned putting real skills through it.

Agent Skills gave agents on-demand expertise. A folder of instructions the model pulls in only when a task matches, so your context window isn't carrying a deployment runbook while you fix a CSS bug. We've leaned on that hard, in the google/skills repository and the seven skills that ship with Agents CLI.

What Skills never solved is distribution.

By @lavinigam, Developer Relations Engineer, Google Cloud

A skill that needs a tool is two artifacts. Instructions live in SKILL.md, the tool lives in an MCP server, and nothing ties them together. So the binding lived in a README: copy this here, add that JSON block there, a different snippet per client. Every client invented its own bundle format to fix it, so authors picked one and rewrote for the next.

Agent Plugins standardizes the box. The components inside it were already portable.

Here's what changes when your skill becomes a plugin, and why you've already built most of one:

The one file you're missing. Your folder is already the right shape.

Your tools travel with your expertise. mcp.json, and paths that survive the trip.

Components fail independently. A dead server doesn't take your skills down.

Client-specific behaviour without forking. The extension namespace.

One folder, every client. What we shipped, and where it actually runs.

The one file you're missing

If you've written an ADK skill, look at where it lives: skills/<name>/SKILL.md, with scripts/, references/ and assets/ underneath. Agent Plugins asks for skills/<dir>/SKILL.md and defers the inside of that folder to the Agent Skills specification. It's the same tree.

Here's a skill we'd already written, unchanged by packaging:

Note what line 1 of the Steps assumes: a running MCP server. That dependency is the thing packaging fixes.

The migration is one file at the root:

Both fields are required, and that's the whole minimum. Names run 1 to 64 characters of lowercase alphanumerics, hyphens and periods, must start and end alphanumeric, and can't contain -- or ... So acme.reports is fine, while My-Plugin, -start and has--double are not.

When you're ready to publish rather than test, the manifest takes metadata too. The schema is closed: ten top-level fields are permitted and nothing else (full field reference).

That's nine of the ten. The last is extensions, for client-specific data, which comes later. author also takes an optional email.

version should be SemVer and license should be SPDX, though a client won't reject you for malformed strings. Wrong JSON types are a different matter: a number where a string belongs is fatal even on an optional field. Exactly two schema violations are non-fatal, an unknown top-level field and an extensions value that isn't an object. Both get reported and ignored, and the plugin still loads. Everything else is fatal and the client refuses the whole package.

One rule catches people migrating to a large skills library. Discovery goes exactly one level deep: clients read the immediate subdirectories of skills/ and don't recurse (discovery rules). If you've been grouping skills into category folders, those skills stop existing the moment you package them, with no error explaining why.

Your tools travel with your expertise

mcp.json sits beside your manifest and declares the servers your skill needs. It carries only two top-level keys, and every server declares its transport explicitly:

Three transports exist, and the required fields differ (MCP server reference):

command is one executable token, not a shell command. Either a bare name resolved by platform search rules, or a plugin-relative path starting with ./. Placeholder expansion deliberately does not apply to it, so a bundled binary is found by that ./ path and nothing else.

The two placeholders are environment variables the client provides to stdio subprocesses, and the difference between them matters more than the names suggest:

Write into PLUGIN_ROOT and your state disappears the next time someone updates the plugin. Both expand in args, env values and cwd only. Not in environment keys, not in command, not in remote URLs, and not in headers. Expansion is textual and single-pass, so nothing nests.

Headers are literal package data that anyone who downloads your plugin can read, which is why the spec forbids putting credentials in them. Agent Plugins 1.0.0 defines no portable OAuth or credential-reference field at all. Authentication stays client-managed.

Components fail independently