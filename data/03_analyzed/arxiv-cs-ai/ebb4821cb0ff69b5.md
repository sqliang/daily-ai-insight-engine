---
title: 'ITNet: A Learnable Integral Transform That Subsumes Convolution, Attention,
  and Recurrence'
source: https://arxiv.org/abs/2606.19538
author:
- '[[Ashim Dhor, Rasel Mondal, Pin Yu Chen]]'
published: '2026-06-19'
created: '2026-06-19'
description: 'arXiv:2606.19538v1 Announce Type: new Abstract: Convolutional networks,
  recurrent networks, and transformers each encode different inductive biases -- locality,
  sequential memory, and content-dependent pairwise interaction -- and have remained
  mathematically distinct since their inception. We show that this fragmentation reflects
  not a fundamental diversity in how signals should be processed, but rather incomplete
  views of a single underlying mathematical object: a learnable integral transform.
  We introduce the Integral Transform Network (ITNet), a unified architecture built
  around a learnable kernel that depends jointly on positions and features. This kernel
  is implemented as a small neural network, specifically an MLP, that models pairwise
  interactions, enabling the model to adapt its behavior from data. We show that convolution,
  self-attention (including multi-head), and autoregressive recurrence (including
  LSTM, GRU, S4, and Mamba) arise as special cases under appropriate parameterizations,
  and that ITNet is a universal approximator of continuous operators. To make this
  practical, we develop tiled kernel fusion, importance-weighted Monte Carlo integration,
  and learned low-rank factorization, enabling efficient and scalable computation.
  A single ITNet architecture with a shared operator and lightweight modality-specific
  encoders matches or exceeds specialized baselines on ImageNet-1K , GLUE, ModelNet40,
  VQA\,v2 and NLVR2. The results demonstrate that a single learned interaction mechanism
  can recover the behavior of all three architectural families from data.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: ebb4821cb0ff69b5
source_type: academic_paper
tldr: ITNet提出可学习的积分变换核，统一了卷积、注意力与循环三大架构。
objective_summary: 研究人员提出ITNet（积分变换网络），通过一个由MLP实现的联合位置与特征的可学习核，将卷积、自注意力（含多头）和自回归循环（含LSTM、GRU、S4、Mamba）统一为同一数学框架的特殊情形，并在ImageNet-1K、GLUE、ModelNet40、VQA
event_type: infrastructure_update
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - ITNet
  - Integral Transform
  - Convolution
  - Self-Attention
  - RNN
  - LSTM
  - GRU
  - S4
  - Mamba
  - MLP
  - Monte Carlo Integration
  key_people: []
key_logic_flow:
- 卷积网络、循环网络和Transformer各自编码了不同的归纳偏置（局部性、序列记忆、内容相关的成对交互），但ITNet证明这些结构是一个可学习积分变换的不同参数化特例。
- ITNet的核心是一个由小型MLP实现的联合位置与特征的核函数，用于建模成对交互，从而使模型能从数据中自适应调整行为。
- ITNet是连续算子的通用逼近器，理论上能够逼近任意连续算子。
- 为实现高效可扩展计算，ITNet引入了三个关键技术：分块核融合、重要性加权蒙特卡洛积分和可学习的低秩分解。
- 单个ITNet架构配合共享算子和轻量级模态特定编码器，在图像分类（ImageNet-1K）、自然语言理解（GLUE）、3D点云（ModelNet40）和视觉问答（VQA
  v2、NLVR2）上达到或超过各领域专用基线。
impact_score:
  score: 7.5
  reason: 该论文提出了一个极具原创性的理论贡献——将卷积、自注意力和循环网络三大架构范式统一为可学习积分变换的特例，这在数学层面填补了长期以来的理论空白。不同于常规的架构改进（如新注意力变体），ITNet
    从算子层面完成了统一，并提供了通用逼近性证明。实验覆盖图像（ImageNet-1K）、文本（GLUE）、3D点云（ModelNet40）和多模态（VQA v2/NLVR2）四大领域，均达到或超过专用基线，初步验证了实用性。但由于该工作仍处于学术论文阶段，缺乏开源代码复现验证，也未经过工业级大规模训练（如
    LLM 预训练或视频理解）的检验，距离真正改变行业格局还需时间。综合评估，其理论冲击力接近范式迁移级别，但工程落地尚需兑现，故评 7.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 统一三大架构的理论优雅性 vs 工程落地的实际效率与可扩展性
hype_assessment:
  level: low
  reason: 论文标题和摘要中使用了 'subsumes'、'universal approximator' 等强宣称词汇，但论文在数学层面提供了严格的证明（积分变换核与各架构的等价性推导、通用逼近性证明），并在五个标准基准上提供了实验验证。三个效率优化技术（分块核融合、重要性加权蒙特卡洛积分、低秩分解）表明作者并非空谈理论，而是认真考虑了可扩展性。整体上，论文的
    PR 程度在学术论文的正常范围内，没有过度包装或无法验证的空洞宣称。
information_entropy: high
domain_disruption:
  technical_innovation: 提出可学习积分变换作为神经架构的统一数学框架，证明了卷积、自注意力、循环网络（包括 LSTM、GRU、S4、Mamba）均为其特例，并引入分块核融合、重要性加权蒙特卡洛积分和可学习低秩分解三项工程技术使其可扩展计算。这是自
    Transformer 以来最具理论深度的架构创新之一。
  business_model: 若该架构成熟落地，可能降低企业对多种专用架构的依赖——不再需要为不同任务维护 CNN/Transformer/RNN 三套技术栈和对应的硬件加速库，统一算子可简化
    MLOps 和推理基础设施。但短期内对现有商业格局影响有限，更多是学术研究方向的指引。
engineering_complexity: prototype
compound_value:
  score: 3.5
  reason: ITNet 在数学上统一了 CNN、Transformer 和 RNN 三大范式，这一理论贡献具有学术价值，但从 VC 视角评估，3-5 年内形成商业价值的概率较低：(1)
    可学习 MLP 核的计算开销远高于高度优化的专用 CUDA 算子（FlashAttention 等），论文引入的蒙特卡洛积分和低秩分解本质上是性能取舍，而非天然优势；(2)
    验证规模仅到 ImageNet-1K/GLUE，未展示在 GPT/LLaMA 千亿参数规模下的可扩展性，从中等规模实验到生产级部署存在巨大鸿沟；(3) 历史上类似的大一统理论尝试（Neural
    ODEs、HyperNetworks、Set Transformers）最终都未撼动专用架构的主导地位——根本原因在于硬件生态（GPU Tensor Core、NPU）针对
    matmul 的高度优化使 Transformer 具备了难以追赶的生态护城河。ITNet 更可能是对下一代架构设计的启发性贡献，而非独立的商业投资标的。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- DeepMind
- Google Research
- Meta AI
competitive_casualty: []
market_opportunities:
- ITNet的统一架构范式为AI基础设施供应商提供了设计通用加速器的机会，可开发面向积分变换算子的专用芯片或推理优化引擎，降低多架构部署的硬件成本与碎片化问题
- 创业团队可基于ITNet的分块核融合与蒙特卡洛积分技术，开发面向长序列处理（如文档理解、视频分析、长文本推理）的高效训练/推理中间件，填补Transformer在超长上下文场景的效率短板
- 对于AI工程团队，ITNet展示了一条减少架构选型成本的路径——同一套算子跨模态任务，可大幅降低技术栈复杂度与维护成本，建议密切关注开源实现进展并提前储备相关知识
risk_matrix:
  regulatory: 无
  technological: ITNet的可学习MLP核的端到端计算开销和实际可扩展性尚未在大规模生产环境中充分验证，存在理论优美但实践受限的风险；其蒙特卡洛积分策略在高精度要求场景下可能不如确定性方法稳定；论文当前为arXiv预印本，尚未经过同行评审，核心结论存在被后续工作证伪或弱化的可能
  competitive: 现有Transformer生态（CUDA内核、TPU/GPU算子优化、推理引擎、部署工具链）已被大型科技公司深度锁定，ITNet面临极高的生态转换成本与路径依赖阻力；同时Mamba、RWKV等线性复杂度架构也在争夺后Transformer时代的定义权，竞争格局尚不明朗
  ethical: 无
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
---

# Computer Science > Artificial Intelligence

# Title:ITNet: A Learnable Integral Transform That Subsumes Convolution, Attention, and Recurrence

View PDF HTML (experimental)Abstract:Convolutional networks, recurrent networks, and transformers each encode different inductive biases -- locality, sequential memory, and content-dependent pairwise interaction -- and have remained mathematically distinct since their inception. We show that this fragmentation reflects not a fundamental diversity in how signals should be processed, but rather incomplete views of a single underlying mathematical object: a learnable integral transform. We introduce the Integral Transform Network (ITNet), a unified architecture built around a learnable kernel that depends jointly on positions and features. This kernel is implemented as a small neural network, specifically an MLP, that models pairwise interactions, enabling the model to adapt its behavior from data. We show that convolution, self-attention (including multi-head), and autoregressive recurrence (including LSTM, GRU, S4, and Mamba) arise as special cases under appropriate parameterizations, and that ITNet is a universal approximator of continuous operators. To make this practical, we develop tiled kernel fusion, importance-weighted Monte Carlo integration, and learned low-rank factorization, enabling efficient and scalable computation. A single ITNet architecture with a shared operator and lightweight modality-specific encoders matches or exceeds specialized baselines on ImageNet-1K , GLUE, ModelNet40, VQA\,v2 and NLVR2. The results demonstrate that a single learned interaction mechanism can recover the behavior of all three architectural families from data.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.