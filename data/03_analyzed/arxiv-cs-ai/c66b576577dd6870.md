---
title: 'Memory in the Loop: In-Process Retrieval as ExtendedWorking Memory for Language
  Agents'
source: https://arxiv.org/abs/2607.05690
author:
- '[[Yusuf Khan, Carlo Lipizzi]]'
published: '2026-07-08'
created: '2026-07-08'
description: 'arXiv:2607.05690v1 Announce Type: new Abstract: Language agents run
  a loop - observe, reason, act - but the memory they reason over sits outside it:
  a store queried at most once per turn. We study the regime where memory moves inside
  the loop, read and written on every step. The obstacle has always been latency:
  networked stores answer in tens to hundreds of milliseconds, and in-loop retrieval
  can inflate end-to-end latency by up to 83x when retrieval is expensive. Prior work
  manages that cost rather than questioning it: serving-layer scheduling hides it,
  "memory-first" designs ration retrieval to once per turn. We argue latency is a
  property of where the store lives, not the in-loop pattern: an in-process store
  answers in ~100us, three orders of magnitude below the network regime, and at that
  speed the per-step tax collapses. By the extended-mind thesis''s parity principle,
  a store fast enough to be constantly and directly available becomes extended working
  memory, not a tool the agent merely consults. The premise is causal: holding a fixed
  per-turn memory-latency budget and varying only the store''s answer speed, redundant
  actions rise monotonically with latency - 0.0 of 12 at in-process speed, 7.2 of
  12 at a 110ms cloud round trip (gpt-5-nano, gpt-5-mini; exact permutation p=0.0079).
  We demonstrate the regime end-to-end: across four GPT-5-class models under a bounded
  window, recall improves from 0/5 to 3.6-4.8/5 with in-loop memory, store ops at
  p50 80-165us - though an instructed restate-every-reply baseline also solves it
  perfectly, at a token cost that grows with the working set. The store never lost
  a fact in any run (244 of 244 writes kept); every miss traces to the agent''s read
  policy, not the store. Our measurements also relocate the bottleneck: the dominant
  per-step cost is embedding (~200-400ms over the network); pairing the in-process
  store with a small local embedder returns the complete operation to a measured ~40us.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: c66b576577dd6870
manifest_dates:
- '2026-07-08'
source_type: academic_paper
tldr: 论文提出将记忆检索内置于语言代理的执行循环中，通过进程内存储（~100μs）替代网络存储，将冗余动作从7.2/12降至0/12。
objective_summary: 该研究通过对比实验证明，将记忆存储移入代理循环（进程内存储，约100μs延迟）相比网络存储（110ms延迟）能显著降低冗余动作（0/12
  vs 7.2/12，p=0.0079），并将回忆准确率从0/5提升至3.6-4.8/5。瓶颈在于嵌入计算（200-400ms），
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies: []
  technologies: []
  key_people: []
key_logic_flow:
- 语言代理的传统架构中，记忆存储位于执行循环之外，网络查询延迟在数十到数百毫秒级别，限制了每步的检索频率。
- 作者提出将记忆存储移入代理循环内部（in-process store），使代理每一步都能读写记忆，且延迟降至约100μs，较网络存储降低三个数量级。
- 在固定记忆延迟预算的实验中，冗余动作数量随延迟单调递增：进程内存储为0/12，网络存储（110ms）为7.2/12，统计显著（p=0.0079）。
- 在GPT-5系列模型上，循环内记忆将回忆准确率从0/5提升至3.6-4.8/5，244次写入全部成功，所有记忆缺失均源于代理的读取策略而非存储本身。
- 瓶颈定位：嵌入计算是主要开销（网络延迟200-400ms）；将进程内存储与本地小模型嵌入器结合可将完整操作降至约40μs。
specialized_tags:
  paper:
    paperTitle: 'Memory in the Loop: In-Process Retrieval as Extended Working Memory
      for Language Agents'
    authors: []
    affiliations: []
    venue: arXiv preprint
    codeUrl: null
    datasetUrl: null
    researchArea: Systems
    methodType: empirical
extract_result: success
impact_score:
  score: 6.5
  reason: 该论文对语言代理的记忆架构提出了根本性改进——将记忆存储移入执行循环内部，将延迟从网络存储的110ms降至进程内的~100μs（降幅达三个数量级）。实验数据扎实：冗余动作从7.2/12降至0/12（p=0.0079），回忆准确率从0/5提升至3.6-4.8/5。这不仅是性能优化，更是对当前主流代理架构（LangChain、AutoGPT等均采用外部网络存储）的范式质疑。核心洞察虽源于经典的'数据靠近计算'系统设计原则，但首次在语言代理场景下给出了因果证据链。评分6.5而非更高，因为这是一个工程架构的实质性优化，并非模型能力或理论范式的突破，影响力主要集中在代理工程圈而非整个AI行业。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 进程内存储将记忆检索延迟从百毫秒级降至微秒级，大幅减少代理冗余动作
hype_assessment:
  level: low
  reason: 论文无PR滥用词汇（如'颠覆'、'革命性'），采用了严谨的实验设计：控制变量法（固定每步记忆延迟预算，仅改变存储速度）、统计显著性检验（精确置换检验p=0.0079）、跨模型验证（GPT-5系列四种模型）、且诚实报告了当前瓶颈（嵌入计算200-400ms）并给出了缓解方案。属于典型的扎实系统论文风格。
information_entropy: high
domain_disruption:
  technical_innovation: 提出'循环内记忆'架构，将记忆存储从代理执行循环外部移入进程内部，使每步都能以~100μs延迟进行读写，较网络存储降低三个数量级。定位了瓶颈转移：传统观点认为网络延迟是主要开销，本文发现嵌入计算（200-400ms）才是真正瓶颈，并验证了本地小模型嵌入器可将完整操作压缩至~40μs。
  business_model: 无直接商业模式影响，但潜在商业化路径：可被嵌入主流AI代理框架（LangChain、CrewAI、AutoGPT等）作为默认记忆层的最佳实践，降低企业级代理部署中的冗余API调用成本（减少不必要的LLM重新推理），提升代理任务的可靠性和完成率。
engineering_complexity: prototype
compound_value:
  score: 7.5
  reason: 该论文揭示了一个关键的代理架构范式转变：将记忆存储从网络查询（~110ms）移入进程内（~100μs）并非渐进优化，而是让记忆从'偶发查询的工具'变为'持续可用的扩展工作记忆'。这种架构一旦被行业采纳，将成为所有语言代理的基础设计模式而非某个产品的特性。论文的实验数据非常扎实（p=0.0079，冗余动作从7.2/12降至0/12，244次写入零丢失），并且指明了下一个瓶颈（嵌入计算），为工程化落地提供了清晰路径。按3-5年的复利视角看，任何追求高质量Agent行为的系统都会采纳该范式，类似'RAG已死，内循环记忆永生'的格局重构。扣分点在于：这是学术论文，尚未产品化；当前仅验证了GPT-5系列模型；嵌入瓶颈的解决（本地小模型嵌入器）仍依赖硬件和模型压缩进展。综合来看，这是'未来Agent操作系统的基础组件'级别的价值评分。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- Anthropic
- LangChain
- LlamaIndex
- Apple
- Qualcomm
competitive_casualty:
- Pinecone
- Weaviate
- 传统向量数据库即服务厂商
- 云嵌入API提供商
market_opportunities:
- AI代理框架开发者可将进程内记忆架构集成到产品中，显著提升代理在多步推理任务中的执行效率与可靠性，形成差异化竞争力
- 本地小模型嵌入器与进程内存储结合方案可开辟边缘AI代理市场，为隐私敏感型行业（医疗、金融、法律）提供低延迟、本地的持久记忆能力
- 代理读取策略优化（而非存储本身）成为新的技术攻关方向，可催生出智能记忆管理中间件，帮助代理自主决策何时写入、读取和遗忘
risk_matrix:
  regulatory: 进程内记忆使代理运行数据更加本地化，可能规避部分跨境数据传输监管要求；但记忆持久化可能带来数据留存合规隐患，需确保符合GDPR等法规的"被遗忘权"要求
  technological: 嵌入计算（网络延迟200-400ms）仍是当前架构下主要瓶颈，本地小模型嵌入质量可能逊于大型云模型，存在精度与延迟的权衡风险；进程内存储对代理宿主内存有更高要求，大规模部署时可能受限
  competitive: OpenAI、Anthropic、Google等大型AI平台可能将类似内存管理机制内置于自有API和运行时环境，挤压第三方代理框架和中间件厂商的差异化空间
  ethical: 持续的进程内记忆可能记录敏感用户交互信息，若缺乏完善的遗忘和擦除机制，存在隐私泄露与信息固化风险；记忆持久化也可能放大代理偏见或锁定错误认知
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
paper_metadata:
  title: 'Memory in the Loop: In-Process Retrieval as Extended Working Memory for
    Language Agents'
  authors: []
  affiliations: []
  venue: arXiv preprint
  paper_url: https://arxiv.org/abs/2607.05690
  code_url: null
  dataset_url: null
research_problem:
  core_question: 将记忆检索移入语言智能体的执行循环内部（in-process），能否通过极低延迟的进程内存储实现扩展工作记忆，从而减少智能体的冗余行为？
  motivation: 当前语言智能体的记忆系统通常位于执行循环外部，每次交互仅查询一次存储，导致延迟高、冗余行为多。作者认为延迟是存储位置的属性而非循环模式本身的属性，通过将存储移至进程内可实现约100μs的响应时间，从而将记忆从"工具"转变为智能体持续可访问的"扩展工作记忆"。
  significance: fundamental
  gap_addressed: 现有工作将循环内检索的高延迟视为不可避免的代价，通过调度层优化或限制检索频率来管理成本，而非质疑延迟的根源。本文填补了"存储位置对智能体行为影响"的研究空白，证明了延迟的根本原因是存储位置而非循环模式，并基于扩展心灵理论的
    parity principle 提出了理论论证。
methodology:
  approach_summary: 本文提出将语言智能体的记忆存储移至进程内部（in-process store），使其在每个推理步骤都能被快速读写（约100μs），而非传统网络存储（数十至数百毫秒）。作者设计了因果实验：固定每轮记忆延迟预算，仅改变存储响应速度，测量冗余行为变化。实验涉及四个GPT-5级模型，在有限窗口下验证循环内记忆对召回率的提升。此外，作者发现嵌入计算（约200-400ms网络延迟）才是实际瓶颈，提出将进程内存储与小规模本地嵌入模型配对，将完整操作降至约40μs。
  novelty_type: architectural
  key_innovations:
  - 提出"Memory in the Loop"范式，将存储从智能体执行循环外部移至进程内部，实现每步约100μs的读写延迟
  - 通过严格因果实验证明：固定每轮记忆延迟预算下，冗余行为随延迟增加单调上升（进程内速度0/12 vs 云服务110ms下7.2/12）
  - 识别并重新定位了实际瓶颈：嵌入计算（~200-400ms）而非存储查找本身，提出进程内存储+本地小嵌入模型联合方案将完整操作降至约40μs
  - 基于扩展心灵理论的 parity principle 提出理论论证：足够快的存储成为智能体的扩展工作记忆，而非仅是其查询的工具
  inspiration_sources:
  - 扩展心灵理论（Extended Mind Thesis）的 parity principle
  - 认知科学中的工作记忆概念
  - 进程内计算与网络计算延迟差异的工程观察
  technical_depth: deeply_technical
experimental_rigor:
  benchmark_coverage: 实验覆盖了四个GPT-5级模型（gpt-5-nano, gpt-5-mini等），在有限上下文窗口下评估循环内记忆的召回性能。设计了合成任务（12步操作）来测量不同延迟设置下的冗余行为。同时测量了存储操作的百分位延迟（p50
    80-165μs）和嵌入延迟。
  baseline_comparison: adequate
  ablation_quality: adequate
  reproducibility_level: fully_reproducible
  claimed_improvement: 在12步任务中将冗余动作从7.2/12（110ms云往返）降至0.0/12（进程内速度）；在有限窗口下召回率从0/5提升至3.6-4.8/5
limitations_and_honesty:
  stated_limitations:
  - 每个遗漏都追溯到智能体的读取策略，而非存储本身——读取策略是限制因素
  - 存储操作在全部运行中从未丢失数据（244/244次写入全部保留）
  - '"复述每次回复"基线也能完美解决问题，但代价是随工作集增长而增加的token成本'
  reviewer_concerns:
  - 实验主要在合成任务上验证，缺乏在真实复杂智能体任务（如Web导航、代码生成）上的评估
  - 对"冗余行为"的定义和测量方式可能存在主观性
  - 进程内存储在大规模工作记忆集下的扩展性未得到充分讨论——是否仍能维持微秒级延迟
  - 与MemGPT等现有长期记忆系统的直接端到端对比缺失
  - 本地嵌入模型的质量对最终性能的影响未被系统分析
  overclaiming_assessment: honest
  generalization_concern: 实验主要在受限的合成任务上完成，对复杂多步推理、需要长期记忆的任务以及不同领域（如Web Agent、Code
    Agent）的泛化能力尚未验证。
industrial_relevance:
  applicable_domains:
  - 语言智能体系统
  - 本地化AI代理与边缘设备
  - 实时交互系统
  - 自主工作流应用
  - 高频记忆存取的机器人控制系统
  compute_requirements: commodity
  integration_readiness: needs_engineering
  cost_efficiency_analysis: 进程内存储方案将每次存储操作的延迟从网络级别的数十至数百毫秒降至约100微秒，降幅达三个数量级。结合本地小嵌入模型后，可将完整操作降至约40微秒。对于高频交互的智能体应用，这能大幅缩短端到端响应时间，同时避免云服务调用成本。但需考虑本地资源占用——进程内存储占用的RAM和本地嵌入模型的计算开销需要在具体部署中权衡。
related_work_context:
  closest_prior_works:
  - MemGPT（虚拟上下文管理）
  - RAG（检索增强生成）系统
  - Memory-first 智能体设计
  - 服务于调度层的延迟隐藏技术
  advancement_over_prior: 现有工作将循环内检索的高延迟视为既定事实，通过调度层优化（如延迟隐藏）或限制每轮检索次数来管理成本。本文从根本上挑战了这一假设，证明延迟是存储位置的函数而非循环模式的固有属性，并通过经验证据表明进程内存储可将延迟降低三个数量级，从而将记忆从外部工具转化为智能体的扩展工作记忆。
  opens_new_direction: true
  potential_follow_ups:
  - 进程内存储与现有智能体框架（如LangChain、AutoGPT）的集成方案
  - 大规模工作记忆集下进程内存储的性能边界研究
  - 自适应读取策略：让智能体学会何时以及如何最优地查询其扩展工作记忆
  - 结合进程内存储与本地嵌入模型的完整端到端系统构建
  - 在长周期复杂任务（如Web导航、软件开发）中验证Memory in the Loop范式的有效性
---

# Computer Science > Artificial Intelligence

# Title:Memory in the Loop: In-Process Retrieval as ExtendedWorking Memory for Language Agents

View PDF HTML (experimental)Abstract:Language agents run a loop - observe, reason, act - but the memory they reason over sits outside it: a store queried at most once per turn. We study the regime where memory moves inside the loop, read and written on every step. The obstacle has always been latency: networked stores answer in tens to hundreds of milliseconds, and in-loop retrieval can inflate end-to-end latency by up to 83x when retrieval is expensive. Prior work manages that cost rather than questioning it: serving-layer scheduling hides it, "memory-first" designs ration retrieval to once per turn. We argue latency is a property of where the store lives, not the in-loop pattern: an in-process store answers in ~100us, three orders of magnitude below the network regime, and at that speed the per-step tax collapses. By the extended-mind thesis's parity principle, a store fast enough to be constantly and directly available becomes extended working memory, not a tool the agent merely consults. The premise is causal: holding a fixed per-turn memory-latency budget and varying only the store's answer speed, redundant actions rise monotonically with latency - 0.0 of 12 at in-process speed, 7.2 of 12 at a 110ms cloud round trip (gpt-5-nano, gpt-5-mini; exact permutation p=0.0079). We demonstrate the regime end-to-end: across four GPT-5-class models under a bounded window, recall improves from 0/5 to 3.6-4.8/5 with in-loop memory, store ops at p50 80-165us - though an instructed restate-every-reply baseline also solves it perfectly, at a token cost that grows with the working set. The store never lost a fact in any run (244 of 244 writes kept); every miss traces to the agent's read policy, not the store. Our measurements also relocate the bottleneck: the dominant per-step cost is embedding (~200-400ms over the network); pairing the in-process store with a small local embedder returns the complete operation to a measured ~40us.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.