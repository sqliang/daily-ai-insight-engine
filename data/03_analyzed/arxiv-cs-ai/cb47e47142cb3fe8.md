---
title: 'Regimes: An Auditable, Held-Out-Gated Improvement Loop Demonstrated on LongMemEval
  with ActiveGraph'
source: https://arxiv.org/abs/2606.10241
author:
- '[[Yohei Nakajima]]'
published: '2026-06-10'
created: '2026-06-10'
description: 'arXiv:2606.10241v1 Announce Type: new Abstract: Autonomous improvement
  loops are hard to trust because the improvement process is usually external scaffolding
  bolted onto the agent: failures go unlogged, diagnoses cannot be replayed, and promote-or-discard
  decisions land in a side database rather than the agent''s own history. We show
  that an event-sourced agent runtime removes that friction and turns controlled improvement
  into a first-class workflow. When the agent''s state is a deterministic projection
  of an append-only event log, failures are recorded, a run replays exactly from its
  log, candidate patches scope to typed pipeline seams, gates are auditable, and every
  promotion or discard is itself an event. We demonstrate this with Regimes, a loop
  on the ActiveGraph runtime that diagnoses failed evaluations, proposes a repair
  at a pipeline point, and promotes it only after static checks, sandbox execution,
  in-sample evaluation, and held-out validation. The loop is target-agnostic: the
  same control flow runs against different tasks through a common interface. On LongMemEval-S
  the dominant failure is not retrieval but reconciliation: the evidence is already
  in the assembled context, yet the reader answers incorrectly. Across five seeded
  held-out splits, Regimes discovers reader-prompt repairs that improve final held-out
  accuracy by +0.05 to +0.10 in four splits and +0.01 in one over-promotion split;
  two splits are individually significant (seed 5 unadjusted for its sequential promotion
  structure), and the pooled count is descriptive only, since the splits share one
  500-question pool. The durable contributions are ActiveGraph as an auditable substrate
  that makes controlled improvement loops tractable, the held-out-gated loop it supports,
  the failure-regime taxonomy routing each failure to a pipeline location (whose marginal
  value over an unrouted baseline is the primary open question), and the prompt-as-discovery-probe
  hypothesis.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: cb47e47142cb3fe8
source_type: academic_paper
tldr: 该论文提出 Regimes 框架，在 ActiveGraph 事件溯源运行时上构建可审计的留出门控自主改进循环。在 LongMemEval-S 上验证发现主要失败模式是协调问题而非检索问题，通过自动发现的阅读器提示修复将准确率提升最高
  0.10。
objective_summary: 该论文提出 Regimes，一个在 ActiveGraph 事件溯源运行时上构建的可审计、留出门控的自主改进循环。Regimes
  诊断失败评估，将修复定位到类型化流水线接缝，并通过静态检查、沙盒执行、样本内评估和留出验证四道关卡后晋升修复。在 LongMemEval-S 基准测试上，论文发现主要失败模式是协调失败——证据已存在于上下文中但模型回答错误——而非检索失败。通过自动发现的阅读器提示修复，Regimes
  在五个留出分割中的四个上实现 +0.05 到 +0.10 的准确率提升，一个分割上提升 +0.01。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies: []
  key_people: []
key_logic_flow:
- 自主改进循环通常难以信任，因为改进过程是外部拼接的，失败未被记录，诊断无法重现，晋升决策也落在旁路数据库中。
- 论文提出基于事件溯源的智能体运行时 ActiveGraph，将智能体状态建模为仅追加事件日志的确定性投影，使失败记录、运行重放和晋升决策都成为可审计的第一类工作流。
- Regimes 是在 ActiveGraph 上构建的改进循环，它诊断失败评估，在类型化流水线接缝处提出修复，并通过静态检查、沙盒执行、样本内评估和留出验证四道关卡后晋升修复。
- 在 LongMemEval-S 上，论文发现最主要的失败模式不是检索失败而是协调失败——证据已在上下文中，但阅读器回答错误。
- Regimes 自动发现的阅读器提示修复在五个留出分割中的四个上将最终留出准确率提升了 +0.05 到 +0.10，在一个过晋升分割上提升了 +0.01。
- 论文的持久贡献包括：ActiveGraph 作为可审计基础、留出门控改进循环、将每个失败路由到流水线位置的失败模式分类法、以及提示即发现探针的假设。
extract_result: success
object_mentions:
- object_type: project
  name: ActiveGraph
  canonical_name: ActiveGraph
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - ActiveGraph 是一个基于事件溯源的智能体运行时，它将智能体状态建模为仅追加事件日志的确定性投影，使失败记录、运行重放和晋升决策都成为可审计的第一类工作流。
  - 论文将 ActiveGraph 视为核心贡献之一，认为它提供了使受控改进循环变得可行的可审计基础。
  article_id: cb47e47142cb3fe8
- object_type: project
  name: Regimes
  canonical_name: Regimes
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Regimes 是在 ActiveGraph 运行时上构建的改进循环，它诊断失败评估、在类型化流水线接缝处提出修复，并在静态检查、沙盒执行、样本内评估和留出验证后晋升修复。
  - Regimes 的目标无关，通过统一接口对不同的任务运行相同的控制流程，在 LongMemEval-S 上发现了阅读器提示修复并提升了准确率。
  article_id: cb47e47142cb3fe8
- object_type: dataset
  name: LongMemEval
  canonical_name: LongMemEval
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - LongMemEval-S 是论文使用的评估基准，论文发现其上最主要的失败模式不是检索失败而是协调失败——证据已在上下文中但阅读器回答错误。
  - 在 LongMemEval-S 的五个留出分割上，Regimes 发现的阅读器提示修复提升了最终留出准确率，四个分割提升 +0.05 到 +0.10，一个分割提升
    +0.01。
  article_id: cb47e47142cb3fe8
impact_score:
  score: 6.8
  reason: 该论文提出 Regimes，将事件溯源（Event Sourcing）引入代理改进循环，解决了自主改进系统缺乏可审计性和可复现性的根本信任问题。这是一个架构层面的优雅创新——使代理状态成为只增事件日志的确定性投影，从而让失败可记录、诊断可重放、决策可审计。但当前仅在一个基准（LongMemEval-S）上验证，准确率提升幅度为
    +0.05 到 +0.10，属于中等幅度改进。虽然方向正确且对代理框架设计有启发意义，但尚未形成行业范式转移，需要更多跨任务验证才能评估其通用性和鲁棒性。评分为
    6.8 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 事件溯源运行时使代理改进循环可审计可复现，从根本上解决自主代理的信任问题
hype_assessment:
  level: low
  reason: 论文语言客观克制，没有使用'颠覆性''革命性'等 PR 话术。明确标注了开放问题（如无路由基线的边际价值是主要待解决问题），实验数据透明，给出了完整的失败模式分类和分析。属于扎实的学术贡献，不存在过度包装。判定为低炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 将事件溯源（Event Sourcing）应用于代理运行时，使代理状态成为只增事件日志的确定性投影，从而让失败可记录、诊断可重放、提升/降级决策本身也成为日志中的事件。该架构让受控改进循环从外部脚手架变为一等工作流，并建立了失败机制分类法，将失败路由到管道中具体的代码位置进行修复。
  business_model: 无直接商业模式影响，但该技术若成熟将提升 AI 代理系统的可信度和可审计性，可能催生'可审计代理即服务'的差异化产品定位，尤其在金融合规、医疗记录等强监管领域具备商业价值。
engineering_complexity: prototype
compound_value:
  score: 7.5
  reason: 该论文提出的核心概念——事件溯源代理运行时（ActiveGraph）+ 保留门控改进循环——解决了企业级 Agent 落地的关键信任瓶颈。当前业界
    Agent 框架大多将改进过程视为外部脚手架（失败不记录、诊断不可回放、提升决策落在外部队列），Regimes 将这些变为一等工作流。从 VC 视角看，这属于
    Agent 基础设施层的'可信自改进'范式，一旦被主流框架采纳将成为事实标准。类比 LangChain 定义了 Agent 构建的编排层，Regimes 定义的可能是
    Agent '持续改进与合规'层。但纯学术论文形态意味着需要开源社区或创业公司做产品化封装，存在执行风险。综合而言，若该思路被 LangChain / AutoGPT
    / ActiveGraph 生态吸纳，可形成长期飞轮：更多 Agent → 更多改进循环数据 → 更强诊断能力 → 更高企业信任度。评分落点在 7.5，核心逻辑是'底层范式有价值
    + 产品化风险中等'。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- ActiveGraph
- LangChain
- Anthropic
- AutoGPT
- CrewAI
competitive_casualty:
- 传统 RPA 厂商（UiPath、Automation Anywhere）
- 黑盒式 Agent 平台（无可审计改进循环）
- 闭源 Agent 调试与评估工具
market_opportunities:
- 企业级 AI Agent 平台可借鉴 Regimes 的事件溯源架构，构建具备完整审计追溯能力的自主改进系统，满足金融、医疗等强监管行业对 AI 可解释性和可问责性的合规需求
- AI 可观测性与调试工具领域可基于其失败机制分类法（检索失败 vs 调合失败），开发针对 Agent 推理链的精准诊断产品，提升企业 Agent 排障效率
- 提示工程团队可集成其保留验证门控机制，构建提示词优化的泛化能力评估流水线，降低传统提示调优中的过拟合风险
risk_matrix:
  regulatory: Regimes 的事件溯源与多层门控设计天然契合欧盟 AI Act 等法规对高风险 AI 系统的可追溯性要求，但若自改进循环在部署后产生超出预期边界的行为，可能触发新的合规审查责任
  technological: 架构强依赖 ActiveGraph 运行时生态，若该框架未获业界广泛采用则影响力受限；实验仅在 LongMemEval 单一基准上验证，+0.05~+0.10
    的提升在更复杂任务上的泛化性尚未证明
  competitive: 面临 Anthropic、OpenAI、Google 等巨头 Agent 框架的生态挤压，以及 LangChain、CrewAI 等开源替代的竞争，作为学术原型在工程化和规模化落地方面存在显著差距
  ethical: 尽管设计了静态检查/沙箱执行/保留验证等多层门控，自主改进循环仍存在行为漂移的不可预测风险，审计完备性依赖事件日志的完整性，实际部署中日志缺失或偏差可能导致审计盲区
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: ActiveGraph
  canonical_name: ActiveGraph
  url: null
  positioning: ActiveGraph 是一个基于事件溯源的智能体运行时，将智能体状态建模为仅追加事件日志的确定性投影，为可审计的自主改进循环提供可信基础设施。
  technical_signal: 事件溯源架构使失败记录、运行重放和晋升决策都成为可审计的第一类工作流，从根本上解决了自主改进循环的可信任问题。
  adoption_signal: 作为 Regimes 改进循环的基础运行时在 LongMemEval-S 上成功验证，但尚未看到论文之外的广泛采用信号。
  ecosystem_relevance: 作为可审计智能体运行时的基础设施性工作，与 LLM 智能体可观测性、自主改进和安全对齐等前沿研究方向高度相关。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: ActiveGraph 通过事件溯源架构将智能体改进循环变为可审计的第一类工作流，为 LLM 智能体的自主改进提供了可信基础设施，其设计理念可能影响下一代智能体框架的架构方向。
  risk_notes:
  - 事件溯源的额外存储和重放开销在生产环境中的可行性尚未验证。
  - 目前仅在 LongMemEval-S 基准上验证，泛化到其他任务和领域的能力有待证明。
  - 与现有主流智能体框架（如 LangChain、AutoGPT 等）的集成兼容性不明确。
  score: 7.0
  article_ids:
  - cb47e47142cb3fe8
  evidence_snippets:
  - ActiveGraph 是一个基于事件溯源的智能体运行时，它将智能体状态建模为仅追加事件日志的确定性投影，使失败记录、运行重放和晋升决策都成为可审计的第一类工作流。
  - 论文将 ActiveGraph 视为核心贡献之一，认为它提供了使受控改进循环变得可行的可审计基础。
- object_type: project
  name: Regimes
  canonical_name: Regimes
  url: null
  positioning: Regimes 是在 ActiveGraph 运行时上构建的留出门控自主改进循环，通过诊断失败评估、定位类型化流水线接缝和四道晋升关卡，实现可审计的智能体持续改进。
  technical_signal: 在 LongMemEval-S 上发现主要失败模式是协调失败而非检索失败，通过自动发现的阅读器提示修复实现 +0.05 到
    +0.10 的准确率提升。
  adoption_signal: 论文在 LongMemEval-S 基准上取得积极结果，但作为新提出的框架尚未见到社区采用或第三方复现。
  ecosystem_relevance: 提出了失败模式分类法和提示即发现探针的新范式，对智能体自我改进、自动化调试和持续学习领域具有启发意义。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Regimes 提出了将自主改进循环从外部脚手架转变为内建可审计工作流的新范式，其留出门控机制和失败模式分类法为构建可信赖的自我改进智能体提供了可操作的框架。
  risk_notes:
  - 留出门控依赖足够的样本分割，在样本不足的场景下可能出现过晋升或欠晋升。
  - 自动发现提示修复的有效性可能随任务复杂度增加而递减。
  - 无路由基线的边际贡献作为主要开放问题尚未被量化验证。
  score: 8.0
  article_ids:
  - cb47e47142cb3fe8
  evidence_snippets:
  - Regimes 是在 ActiveGraph 运行时上构建的改进循环，它诊断失败评估、在类型化流水线接缝处提出修复，并在静态检查、沙盒执行、样本内评估和留出验证后晋升修复。
  - Regimes 的目标无关，通过统一接口对不同的任务运行相同的控制流程，在 LongMemEval-S 上发现了阅读器提示修复并提升了准确率。
---

# Computer Science > Artificial Intelligence

# Title:Regimes: An Auditable, Held-Out-Gated Improvement Loop Demonstrated on LongMemEval with ActiveGraph

View PDF HTML (experimental)Abstract:Autonomous improvement loops are hard to trust because the improvement process is usually external scaffolding bolted onto the agent: failures go unlogged, diagnoses cannot be replayed, and promote-or-discard decisions land in a side database rather than the agent's own history. We show that an event-sourced agent runtime removes that friction and turns controlled improvement into a first-class workflow. When the agent's state is a deterministic projection of an append-only event log, failures are recorded, a run replays exactly from its log, candidate patches scope to typed pipeline seams, gates are auditable, and every promotion or discard is itself an event. We demonstrate this with Regimes, a loop on the ActiveGraph runtime that diagnoses failed evaluations, proposes a repair at a pipeline point, and promotes it only after static checks, sandbox execution, in-sample evaluation, and held-out validation. The loop is target-agnostic: the same control flow runs against different tasks through a common interface. On LongMemEval-S the dominant failure is not retrieval but reconciliation: the evidence is already in the assembled context, yet the reader answers incorrectly. Across five seeded held-out splits, Regimes discovers reader-prompt repairs that improve final held-out accuracy by +0.05 to +0.10 in four splits and +0.01 in one over-promotion split; two splits are individually significant (seed 5 unadjusted for its sequential promotion structure), and the pooled count is descriptive only, since the splits share one 500-question pool. The durable contributions are ActiveGraph as an auditable substrate that makes controlled improvement loops tractable, the held-out-gated loop it supports, the failure-regime taxonomy routing each failure to a pipeline location (whose marginal value over an unrouted baseline is the primary open question), and the prompt-as-discovery-probe hypothesis.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.