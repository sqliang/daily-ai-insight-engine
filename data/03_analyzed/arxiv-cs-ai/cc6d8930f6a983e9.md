---
title: 'LinearKV: One Cached State Suffices for Position-Independent Caching in Hybrid
  LLMs'
source: https://arxiv.org/abs/2608.11231
author:
- '[[Yirui Liu, Ruoling Qi, Longwen Wang, Xuaner Wu, Jian Chen, Yuxin Jin, Jiawei
  Shao, Xuelong Li]]'
published: '2026-08-13'
created: '2026-08-13'
manifest_dates:
- '2026-08-13'
description: 'arXiv:2608.11231v1 Announce Type: new Abstract: LLM serving is increasingly
  accelerated by position-independent caching (PIC). Existing PIC methods, however,
  are built for full-attention models, where a token-indexed KV cache underlies its
  core operations: matching reusable token chunks, concatenating their KV entries,
  and selectively recomputing a few tokens to restore cross-chunk context. Hybrid
  LLMs break these primitives---they replace most attention layers with linear recurrences
  that expose only a fixed-size state, leaving no token-indexed KV to concatenate
  or to locally repair. This raises a natural question: can PIC benefit hybrid models,
  and what would it take? We present LinearKV, a training-free hybrid-PIC framework.
  Its key insight is a \emph{decoupled initialization}: each linear layer maps its
  $K$ matched local states to a single initial state, while full-attention layers
  concatenate their KV as before. LinearKV is therefore compatible with existing PIC
  methods, reusing their token selection and recomputation as-is. Under this framework,
  we find that a \emph{single cached state} suffices as the linear layer''s initializer.
  The algebraically principled alternative---composing all $K$ cached states into
  the exact full-prefix state, as concurrent work HYPIC does---is unnecessary and,
  on some architectures, even harmful. We compare the two across three hybrid models
  and three PIC selectors. On the two GDN models the two tie, both recovering most
  of full quality (up to $92\%$); on the Mamba-2 model, exact composition instead
  collapses under every selector---under EPIC, for instance, it recovers only $46.6\%$
  of full quality, versus $86.8\%$ for a single cached block initializer. A single
  state initializer is also cheaper, cutting time-to-first-token to $0.46\times$ full
  prefill versus a further $5$--$17\%$ overhead for exact composition; results hold
  across LongBench QA and RULER at 8K--32K.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: cc6d8930f6a983e9
source_type: academic_paper
tldr: 研究者提出 LinearKV，一种无需训练的位置无关缓存框架，使混合线性循环与全注意力的大语言模型都能复用现有 PIC 方法；实验表明，对线性层使用单个缓存状态初始化即可达到与精确组合相当的精度，且首
  token 延迟更低。
objective_summary: 论文针对混合 LLM（以线性循环层替代大部分注意力层）难以直接套用为全注意力模型设计的位置无关缓存（PIC）这一问题，提出训练自由的
  LinearKV 框架。其核心是解耦初始化：线性层将匹配到的 K 个局部状态映射为一个初始状态，全注意力层仍按原方式拼接 KV。作者在 GDN 与 Mamba-2
  等三种混合模型、三种 PIC 选择器上对比 LinearKV 与同期工作 HYPIC 的精确状态组合策略，发现单状态初始化在 GDN 上与精确组合持平，在 Mamba-2
  上显著优于精确组合，且将 time-to-first-token 降至全量预填充的 0.46 倍。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - PIC
  - Hybrid LLM
  - LinearKV
  - HYPIC
  - GDN
  - Mamba-2
  - EPIC
  - LongBench QA
  - RULER
  - KV cache
  - linear recurrence
  - full-attention
  key_people: []
key_logic_flow:
- 现有位置无关缓存（PIC）基于全注意力模型，依赖 token 索引的 KV 缓存来匹配可复用 token 块、拼接 KV 条目并选择性地重算少量 token。
- 混合 LLM 用固定大小的线性循环状态替换多数注意力层，没有可拼接或局部修复的 token 索引 KV，导致现有 PIC 方法失效。
- LinearKV 采用解耦初始化：线性层把 K 个匹配的局部状态映射为单一初始状态，全注意力层保持原有 KV 拼接，从而兼容现有 PIC 方法。
- 实验显示，线性层使用单个缓存状态足以恢复大部分完整质量（最高达 92%），而 HYPIC 的精确组合在 Mamba-2 上反而会在所有选择器下崩溃。
- 单状态初始化更便宜，time-to-first-token 降至全量预填充的 0.46 倍，并在 LongBench QA 与 RULER 的 8K-32K 长度上保持效果。
object_mentions:
- object_type: project
  name: LinearKV
  canonical_name: LinearKV
  url: https://arxiv.org/abs/2608.11231
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 论文提出 LinearKV，一种面向混合大语言模型的无训练位置无关缓存（PIC）框架，使线性循环层与全注意力层能统一复用现有 PIC 方法。
  - LinearKV 的关键设计是解耦初始化：线性层将匹配到的 K 个局部状态映射为单一初始状态，全注意力层仍按原方式拼接 KV。
  - 实验在三种混合模型和三种 PIC 选择器上进行，结果表明单个缓存状态即可恢复最高 92% 的完整质量，并将 time-to-first-token 降至全量预填充的
    0.46 倍。
  article_id: cc6d8930f6a983e9
- object_type: project
  name: HYPIC
  canonical_name: HYPIC
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - HYPIC 是同期工作，它通过代数方式将 K 个缓存状态组合成精确的前缀状态，作为线性层的初始化。
  - 论文在相同实验设置下对比 LinearKV 与 HYPIC，发现精确组合在 Mamba-2 模型上会在所有选择器下崩溃，仅能恢复 46.6% 的完整质量。
  article_id: cc6d8930f6a983e9
- object_type: project
  name: EPIC
  canonical_name: EPIC
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - EPIC 是论文评估的三种 PIC 选择器之一。
  - 在 Mamba-2 上，EPIC 选择器下 HYPIC 精确组合仅恢复 46.6% 的完整质量，而 LinearKV 单状态初始化恢复 86.8%。
  article_id: cc6d8930f6a983e9
extract_result: success
impact_score:
  score: 6.2
  reason: 该论文解决的是混合 LLM（线性循环+全注意力）推理中位置无关缓存（PIC）的关键适配问题：现有 PIC 依赖 token 索引的 KV 缓存，而线性层只有固定状态。LinearKV
    以无训练、单状态初始化的方式让现有 PIC 方法直接复用于混合架构，并在 GDN、Mamba-2 上取得与精确组合相当或更优的效果，同时将 time-to-first-token
    降至全量预填充的 0.46 倍。这一结果对长上下文推理和混合模型部署具有直接工程价值，可能很快被 vLLM/SGLang 等推理社区跟进。但短期内它仍是学术方案，影响力主要集中在
    serving 与状态空间模型社区，尚未形成产品或生态锁定，因此达不到范式转移级别。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 如何将线性循环层的单状态初始化与现有 PIC 选择器集成，以降低混合模型的首 token 延迟并避免精确状态组合在部分架构上的崩溃
hype_assessment:
  level: low
  reason: 文章属于 arXiv 学术论文，摘要和正文以问题定义、方法细节和实验对比为主，未使用“颠覆”“革命性”等营销词汇；虽然标题中的 "One Cached
    State Suffices" 以及“unnecessary and even harmful”“collapses”等表述具有一定观点色彩，但均有具体模型、选择器和
    8K-32K 评测数据支撑，整体水分较低。
information_entropy: high
domain_disruption:
  technical_innovation: 将 PIC 从全注意力模型扩展到混合线性循环/全注意力架构，提出解耦初始化策略：线性层把 K 个匹配的局部状态映射为单一初始状态，全注意力层保留原有
    KV 拼接；无需训练即可兼容现有 PIC 选择器，并证明单状态初始化足以恢复大部分完整质量，同时规避精确组合在 Mamba-2 上的崩溃问题。
  business_model: 对 LLM 推理即服务（Serving/SaaS）的成本结构有潜在影响，尤其是长上下文和混合架构（Mamba/GDN 类）模型的部署可降低预填充开销、提升响应速度；若被主流推理引擎集成，可推动混合模型在云
    API 和端侧推理中的采用，但目前仍为学术方案，需工程适配与生态集成。
engineering_complexity: prototype
compound_value:
  score: 7.2
  reason: LinearKV 切中一个正在放大的真实痛点：混合 LLM（linear recurrence + 稀疏/全注意力）被视为降低长上下文推理成本的关键路径，但现有位置无关缓存（PIC）无法直接套用会卡住工程落地。论文提出的解耦初始化与单缓存状态策略无需训练，可直接嵌入现有
    PIC 与推理引擎，实验在 GDN 与 Mamba-2 上证明效果不低于、甚至在 Mamba-2 上显著优于精确组合，同时将首 token 延迟降至全量预填充的
    0.46 倍。中长期看，若混合架构市场份额持续提升，LinearKV 很可能成为混合模型 serving stack 的标配缓存原语。但需注意：这是公开发表的算法，IP
    不可独占，价值会被推理框架（vLLM/SGLang）和云平台以成本优势形式吸收，难以单独支撑高壁垒公司；且混合架构尚未成为绝对主流，若全注意力模型通过推理
    scaling 继续主导，需求空间会被压缩。因此给予 7.2 分：高基础设施潜力，但价值捕获分散、存在被替代风险。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Mamba-2
- GDN
- vLLM
- SGLang
- AWS
- Google Cloud
- Microsoft Azure
competitive_casualty:
- HYPIC
- Dense full-attention LLMs
- Transformer-only inference stacks
market_opportunities:
- 混合架构（Mamba-2 / GDN 等）LLM 的推理服务商可将 LinearKV 的单状态初始化方案集成到 serving 栈中，在不重新训练模型的前提下降低首
  token 延迟并提升缓存复用率
- 面向长上下文场景的模型厂商可把解耦式 PIC 框架作为混合 LLM 的默认推理原语，以 0.46 倍预填充延迟支撑 8K-32K 的 QA 与检索任务
- 创业者可围绕混合 LLM 的位置无关缓存构建 serving 中间件或兼容层，解决线性循环状态与现有全注意力 PIC 方案之间的工程鸿沟
risk_matrix:
  regulatory: 无
  technological: 存在架构迭代风险：若未来全注意力模型在效率上反超混合线性模型，或 HYPIC 等精确组合方法在工程实现上得到显著优化，单状态初始化的优势可能被削弱；此外实验仅在
    GDN 与 Mamba-2 三类模型上验证，泛化到其它混合架构仍需验证
  competitive: vLLM、TensorRT-LLM、TGI 等主流推理框架若快速内置类似线性层 PIC 支持，将挤压独立工具或初创公司的生存空间；同时
    HYPIC 等同期工作可能分流学术与工程注意力
  ethical: 无
  additional:
  - 工程落地风险：生产级 serving 系统需同时维护线性状态的匹配、映射与全注意力 KV 拼接两套路径，增加系统复杂度与出错面
  - 选择器质量风险：LinearKV 复用现有 PIC 选择器，若选择器在真实业务分布上召回不足，单状态初始化的收益会同步下降
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: LinearKV
  canonical_name: LinearKV
  url: https://arxiv.org/abs/2608.11231
  positioning: 面向混合大语言模型的无训练位置无关缓存框架，通过解耦初始化让线性循环层与全注意力层统一复用现有 PIC 方法。
  technical_signal: 提出解耦初始化机制，将 K 个匹配的局部状态映射为单一初始状态，使线性循环层可兼容现有 PIC 选择器与局部重算流程。
  adoption_signal: 在 GDN 与 Mamba-2 三类混合模型及三种 PIC 选择器上验证，单状态初始化即可达到甚至超过精确组合效果。
  ecosystem_relevance: 补齐了线性循环/混合 LLM serving 生态中缺少位置无关缓存的关键一环，可显著降低长上下文首 token 延迟。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该方法无需训练即可让混合 LLM 复用现有 PIC 基础设施，在多个模型上验证可将首 token 延迟降至全量预填充的 0.46 倍，对长上下文推理部署有直接价值。
  risk_notes:
  - 目前仅基于论文实验验证，尚未公布开源代码或生产环境部署数据。
  - 单状态初始化在更复杂混合架构上的泛化性仍需更多独立复现验证。
  score: 8.0
  article_ids:
  - cc6d8930f6a983e9
  evidence_snippets:
  - 论文提出 LinearKV，一种面向混合大语言模型的无训练位置无关缓存（PIC）框架，使线性循环层与全注意力层能统一复用现有 PIC 方法。
  - LinearKV 的关键设计是解耦初始化：线性层将匹配到的 K 个局部状态映射为单一初始状态，全注意力层仍按原方式拼接 KV。
  - 实验在三种混合模型和三种 PIC 选择器上进行，结果表明单个缓存状态即可恢复最高 92% 的完整质量，并将 time-to-first-token 降至全量预填充的
    0.46 倍。
- object_type: project
  name: HYPIC
  canonical_name: HYPIC
  url: null
  positioning: 同期提出的混合模型位置无关缓存方案，通过代数方式将多个缓存状态组合成精确前缀状态以初始化线性层。
  technical_signal: 用代数方法将多个缓存状态精确组合为完整前缀状态，是验证单状态初始化是否足够的同期对照方法。
  adoption_signal: 论文以基线身份出现，实验显示其在 Mamba-2 的所有选择器下崩溃，实际采用前景受限。
  ecosystem_relevance: 代表了同期对混合模型 PIC 精确组合的探索，其失败案例凸显了单一状态方案在工程实现上的优势。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 LinearKV 的同期对照，HYPIC 揭示了精确组合在 Mamba-2 上的不稳定性，可帮助社区理解混合模型缓存初始化策略的设计边界。
  risk_notes:
  - 作为基线方案，在 Mamba-2 上所有选择器下崩溃，工程可行性存疑。
  - 尚未有独立复现或开源实现来验证其代数组合假设。
  score: 4.0
  article_ids:
  - cc6d8930f6a983e9
  evidence_snippets:
  - HYPIC 是同期工作，它通过代数方式将 K 个缓存状态组合成精确的前缀状态，作为线性层的初始化。
  - 论文在相同实验设置下对比 LinearKV 与 HYPIC，发现精确组合在 Mamba-2 模型上会在所有选择器下崩溃，仅能恢复 46.6% 的完整质量。
---

# Computer Science > Artificial Intelligence

# Title:LinearKV: One Cached State Suffices for Position-Independent Caching in Hybrid LLMs

View PDF HTML (experimental)Abstract:LLM serving is increasingly accelerated by position-independent caching (PIC). Existing PIC methods, however, are built for full-attention models, where a token-indexed KV cache underlies its core operations: matching reusable token chunks, concatenating their KV entries, and selectively recomputing a few tokens to restore cross-chunk context. Hybrid LLMs break these primitives---they replace most attention layers with linear recurrences that expose only a fixed-size state, leaving no token-indexed KV to concatenate or to locally repair. This raises a natural question: can PIC benefit hybrid models, and what would it take? We present LinearKV, a training-free hybrid-PIC framework. Its key insight is a \emph{decoupled initialization}: each linear layer maps its $K$ matched local states to a single initial state, while full-attention layers concatenate their KV as before. LinearKV is therefore compatible with existing PIC methods, reusing their token selection and recomputation as-is. Under this framework, we find that a \emph{single cached state} suffices as the linear layer's initializer. The algebraically principled alternative---composing all $K$ cached states into the exact full-prefix state, as concurrent work HYPIC does---is unnecessary and, on some architectures, even harmful. We compare the two across three hybrid models and three PIC selectors. On the two GDN models the two tie, both recovering most of full quality (up to $92\%$); on the Mamba-2 model, exact composition instead collapses under every selector---under EPIC, for instance, it recovers only $46.6\%$ of full quality, versus $86.8\%$ for a single cached block initializer. A single state initializer is also cheaper, cutting time-to-first-token to $0.46\times$ full prefill versus a further $5$--$17\%$ overhead for exact composition; results hold across LongBench QA and RULER at 8K--32K.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.