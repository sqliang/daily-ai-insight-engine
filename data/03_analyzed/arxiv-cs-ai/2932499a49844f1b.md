---
title: 'Nexus: Depth-Adaptive KV-Cache Splicing and Retrieval-Decoupled Tool Routing
  for Agentic LLMs on Unified Memory'
source: https://arxiv.org/abs/2608.20397
author:
- '[[Mustafa Arslan]]'
published: '2026-08-24'
created: '2026-08-24'
manifest_dates:
- '2026-08-24'
description: 'arXiv:2608.20397v1 Announce Type: new Abstract: Agentic large language
  models (LLMs) on the Model Context Protocol (MCP) re-encode verbose tool schemas
  every turn, so prefill - quadratic in sequence length - dominates time-to-first-token
  (TTFT) as the tool registry grows. Nexus''s primary lever is to decouple routing
  from the schema-prefill cost: an INT8 semantic lookaside buffer (SLB) with a calibrated
  cross-encoder margin gate selects tools by retrieval, and arguments are generated
  over a compressed textual signature (median 19 tokens) rather than over spliced
  key/value (KV) cache. This path is depth-independent: routing accuracy stays near
  89% as the registry scales to 250 tools - where a concatenate-all-schemas baseline
  overflows the context window entirely - and it reaches a first-argument token 1.66x
  sooner than a full-schema re-prefill at a ~80% main-context token saving. As a secondary,
  bounded lever we transplant a compiled schema KV block directly into the live context.
  This is fundamentally limited by rotary position embedding (RoPE) phase drift: an
  anchored splice is output-exact, but off-anchor placement corrupts attention, so
  beyond a threshold P=256 Nexus repairs the seam with a depth-adaptive suffix redecode
  that escalates to a full re-prefill. The resulting never-regress property is a guarantee
  on output fidelity (top-1 agreement, D_KL approx. 0) - not on latency, which can
  dip to 0.98x before converging to parity - alongside a 1.1-1.7x TTFT speedup at
  moderate depth that narrows to parity at deep context. Two negative results bound
  the design: the off-anchor RoPE fidelity boundary, and the failure of a reference-free
  drift gate to predict drift (Spearman rho = 0.193). All measurements are from one
  model tuple (Qwen2.5-14B-Instruct Q4_K_M) on Apple-silicon unified memory; the qualitative
  boundaries generalize, while the quantitative envelope is tuple-specific.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 2932499a49844f1b
source_type: academic_paper
tldr: Nexus 论文提出面向 MCP 智能体的检索式工具路由方案，用 INT8 语义缓冲区分隔路由与模式预填充，在 250 个工具规模下保持约 89% 路由准确率，并带来
  1.1-1.7 倍 TTFT 加速；二级手段是深度自适应 KV 缓存拼接，受 RoPE 相位漂移约束。
objective_summary: 该 arXiv 论文提出 Nexus 方案，针对基于 MCP 的智能体 LLM 每轮重编码冗长工具模式、使预填充耗时随工具注册表膨胀的问题。Nexus
  的主要手段是用 INT8 语义旁路缓冲区按检索结果选择工具，并在压缩文本签名（中位 19 个令牌）上生成参数，路由准确率在工具规模达 250 个时约 89%，首个参数令牌比全模式重预填充快
  1.66 倍，主上下文令牌节省约 80%。次级手段是移植已编译的模式 KV 块，但受 RoPE 相位漂移限制，超过阈值 P=256 时改用深度自适应后缀重解码修复接缝，以保证输出保真度。全部测量基于
  Qwen2.5-14B-Instruct Q4_K_M 在 Apple-silicon 统一内存上完成。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies:
  - Apple
  - Alibaba
  technologies:
  - MCP
  - KV-Cache
  - RoPE
  - INT8
  - TTFT
  - SLB
  key_people: []
key_logic_flow:
- 基于 MCP 的智能体 LLM 每轮重编码冗长工具模式，使平方级预填充成本随工具注册表增长而主导首个令牌时间（TTFT）。
- Nexus 通过 INT8 语义旁路缓冲区将工具路由与模式预填充成本解耦，路由准确率在工具规模扩展到 250 个时仍保持约 89%。
- 参数在压缩文本签名（中位 19 个令牌）上生成，首个参数令牌比全模式重预填充快 1.66 倍，主上下文令牌节省约 80%。
- 次级手段是把已编译的模式 KV 块直接移植进活动上下文，但受 RoPE 相位漂移限制，错位拼接会破坏注意力。
- 超过阈值 P=256 时 Nexus 用深度自适应后缀重解码修复接缝，必要时升级为完整重预填充，从而保证输出保真度（never-regress 属性）。
- 两项负结果界定设计边界：错位 RoPE 保真度边界，以及无参考漂移门无法预测漂移（Spearman rho=0.193）。
object_mentions:
- object_type: paper
  name: 'Nexus: Depth-Adaptive KV-Cache Splicing and Retrieval-Decoupled Tool Routing
    for Agentic LLMs on Unified Memory'
  canonical_name: Nexus
  url: https://arxiv.org/abs/2608.20397
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Nexus 提出用 INT8 语义旁路缓冲区按检索结果选择工具，从而将工具路由与模式预填充成本解耦。
  - Nexus 在工具注册表扩展到 250 个工具时路由准确率保持在约 89%，首个参数令牌比全模式重预填充快 1.66 倍。
  - Nexus 的二级手段是深度自适应 KV 缓存拼接，但受 RoPE 相位漂移限制，超过阈值 P=256 时用深度自适应后缀重解码修复。
  article_id: 2932499a49844f1b
- object_type: model
  name: Qwen2.5-14B-Instruct
  canonical_name: Qwen2.5-14B-Instruct
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 全部测量基于 Qwen2.5-14B-Instruct Q4_K_M 这一模型元组在 Apple-silicon 统一内存上进行，定量包络为元组特有。
  article_id: 2932499a49844f1b
- object_type: project
  name: Model Context Protocol (MCP)
  canonical_name: MCP
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 论文研究背景是使用 Model Context Protocol 的智能体 LLM，每轮都会重编码冗长的工具模式。
  article_id: 2932499a49844f1b
extract_result: success
impact_score:
  score: 5.5
  reason: 评分依据：该论文切入 MCP 智能体工具注册表膨胀导致预填充成本平方级上升、TTFT 恶化的真实工程痛点，提出的检索式工具路由 + KV 缓存拼接方案在
    250 个工具规模下保持约 89% 路由准确率并带来 1.1-1.7 倍 TTFT 加速，属于可落地的系统优化而非范式突破；且论文诚实披露了 RoPE 相位漂移边界与无参考漂移门失效两项负结果，学术可信度高。但量化结果仅来自单一模型（Qwen2.5-14B-Instruct
    在 Apple silicon 统一内存上），路由准确率本身不算顶尖，加速比在中深层上下文收敛到持平，短期内只会影响智能体框架与推理服务的局部实现选型，不改变行业竞争格局。综合评估为中等偏上的短期行业冲击力，故给
    5.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 检索式工具路由能否在真实 MCP 多工具负载下稳定复现 TTFT 收益，以及 KV 缓存拼接方案的可移植性
hype_assessment:
  level: low
  reason: 判定依据：全文未使用'颠覆'、'革命性'等 PR 滥用词汇，措辞克制（'bounded lever'、'never-regress'），主动披露两项负结果、明确单模型单硬件的量化边界，并区分定性可推广与定量不可外推，属于实打实的工程贡献，水分极低。
information_entropy: high
domain_disruption:
  technical_innovation: 核心创新是把工具路由与 schema 预填充成本解耦：用 INT8 语义旁路缓冲区（SLB）配合交叉编码器边距门做检索式工具选择，并在压缩文本签名（中位
    19 个 token）上生成参数，使路由成本不再随工具注册表规模平方级增长；次级杠杆是将已编译的 schema KV 块移植进活动上下文，并用深度自适应后缀重解码修复
    RoPE 相位漂移造成的接缝错位，以 never-regress 属性界定输出保真度边界。这是对 KV 缓存复用受位置编码约束的一次诚实工程刻画，检索-预填充解耦思路对工具型智能体具有通用借鉴价值。
  business_model: 论文无直接商业模式，但其方向对 MCP/智能体基础设施有潜在经济性影响：若被智能体框架或推理服务采纳，可显著降低多工具场景的首令牌延迟与
    token 消耗（主上下文 token 节省约 80%），改善 agent SaaS 的边际推理成本与交互体验，间接利好工具编排类平台与智能体托管服务的规模化经济性。
engineering_complexity: prototype
compound_value:
  score: 5.5
  reason: 从复利视角看，Nexus 直击 MCP 智能体生态中一个真实且随规模扩张而指数恶化的痛点：工具注册表膨胀导致每轮模式重编码的 prefill 成本平方级上升，成为
    TTFT 主导项。其核心思路——用检索式路由（INT8 语义旁路缓冲区）把路由决策与模式预填充成本解耦，辅以 KV 拼接缓解重编码——即使这篇论文的具体实现未被直接采用，方向也会被后续工程方案继承，属于智能体中间件层的基础性技术储备，具备细分赛道复利价值。但当前证据等级仅为
    theoretical_claim：全部定量测量集中在 Qwen2.5-14B + Apple silicon 单一组合，路由准确率 89% 在安全敏感场景仍有不可忽视的错误率，深层上下文时
    TTFT 收益收敛至 parity，且从论文到可工程化落地（检索索引构建、校准门调参、错误恢复）仍有明显距离。因此给 5.5 分，落在『有潜力成为细分基础设施、但需持续验证』区间中位偏上。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Apple
- Alibaba
- Anthropic
- LangChain
competitive_casualty:
- 传统 RPA 厂商
- 依赖全量预填充的闭源 Agent 平台
market_opportunities:
- 创业者可将 Nexus 的检索式工具路由与压缩签名参数生成范式封装为 MCP 智能体中间件或 SDK，帮助企业在工具注册表膨胀至数百个时降低首令牌延迟与推理成本
- 在统一内存与端侧设备（如 Apple silicon）上，围绕 KV-Cache 管理与语义旁路缓冲开发本地智能体加速方案，契合隐私敏感与离线场景的部署需求
- 建议 Agent 框架与推理引擎团队将'检索解耦式工具路由'作为默认设计模式集成，并结合语义缓存构建差异化的工具链能力
risk_matrix:
  regulatory: 无
  technological: 论文仅基于单一模型（Qwen2.5-14B-Instruct Q4_K_M）与单一硬件（Apple silicon 统一内存）验证，定量边界缺乏跨模型泛化证据；RoPE
    相位漂移边界与无参考漂移门失效（Spearman rho=0.193）两项负结果表明 KV 拼接技术成熟度有限；长上下文模型、新型位置编码或推理引擎原生前缀缓存的演进也可能从根本绕过该问题
  competitive: 主流推理框架（如 vLLM、SGLang）与云厂商推理平台若将工具缓存或检索式路由原生内置，将挤压独立中间件方案的生存空间；MCP 协议层自身若引入
    schema 压缩或缓存规范，也会削弱该方案的差异化价值
  ethical: 路由准确率约 89% 意味着工具规模达 250 个时存在约 11% 的工具误选风险，在自主执行场景可能导致错误操作或不当行为；检索式路由的排序机制也可能引入工具选择偏差，影响公平性与可控性
  additional:
  - '''never-regress'' 属性仅保证输出保真度而非延迟，论文承认在部分深度下延迟可回落至 0.98x，采用者需明确该性能边界'
confidence:
  impact: medium
  compound: low
  hype: low
actionable_insight: monitor
---

# Computer Science > Artificial Intelligence

# Title:Nexus: Depth-Adaptive KV-Cache Splicing and Retrieval-Decoupled Tool Routing for Agentic LLMs on Unified Memory

View PDF HTML (experimental)Abstract:Agentic large language models (LLMs) on the Model Context Protocol (MCP) re-encode verbose tool schemas every turn, so prefill - quadratic in sequence length - dominates time-to-first-token (TTFT) as the tool registry grows. Nexus's primary lever is to decouple routing from the schema-prefill cost: an INT8 semantic lookaside buffer (SLB) with a calibrated cross-encoder margin gate selects tools by retrieval, and arguments are generated over a compressed textual signature (median 19 tokens) rather than over spliced key/value (KV) cache. This path is depth-independent: routing accuracy stays near 89% as the registry scales to 250 tools - where a concatenate-all-schemas baseline overflows the context window entirely - and it reaches a first-argument token 1.66x sooner than a full-schema re-prefill at a ~80% main-context token saving. As a secondary, bounded lever we transplant a compiled schema KV block directly into the live context. This is fundamentally limited by rotary position embedding (RoPE) phase drift: an anchored splice is output-exact, but off-anchor placement corrupts attention, so beyond a threshold P=256 Nexus repairs the seam with a depth-adaptive suffix redecode that escalates to a full re-prefill. The resulting never-regress property is a guarantee on output fidelity (top-1 agreement, D_KL approx. 0) - not on latency, which can dip to 0.98x before converging to parity - alongside a 1.1-1.7x TTFT speedup at moderate depth that narrows to parity at deep context. Two negative results bound the design: the off-anchor RoPE fidelity boundary, and the failure of a reference-free drift gate to predict drift (Spearman rho = 0.193). All measurements are from one model tuple (Qwen2.5-14B-Instruct Q4_K_M) on Apple-silicon unified memory; the qualitative boundaries generalize, while the quantitative envelope is tuple-specific.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.