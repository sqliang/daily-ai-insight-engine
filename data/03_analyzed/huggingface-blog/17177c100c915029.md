---
title: Training and Finetuning Multi-Vector Embedding Models with Sentence Transformers
source: https://huggingface.co/blog/train-multi-vector-encoder
author: []
published: '2026-08-26'
created: '2026-08-27'
manifest_dates:
- '2026-08-27'
- '2026-08-28'
- '2026-08-29'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 17177c100c915029
source_type: tech_blog
tldr: Sentence Transformers 新增 MultiVectorEncoder，支持 ColBERT 风格的多向量后期交互检索模型的训练与微调。作者用单张
  RTX 3090 训练 14.5 小时得到的医疗检索模型，在评测中超过所有通用检索模型。
objective_summary: 官方博客介绍 Sentence Transformers 库新增的 MultiVectorEncoder，用于训练和微调 ColBERT
  风格的多向量检索模型，并提供从数据集、损失函数到评估器、训练器的完整流程。多向量模型为每个 token 保留一个向量，通过 MaxSim 算子计算查询与文档的匹配分数，保留比单向量压缩更细粒度的信号。作者在撰写博客期间用单张
  RTX 3090 训练 14.5 小时微调出 multi-vector-encoder/mLateOn-medical 模型，在医疗检索评测上超过稠密、稀疏、词法及多向量等各类通用检索模型。全部训练功能通过
  pip install -U "sentence-transformers[train]" 即可使用。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  technologies:
  - MultiVectorEncoder
  - ColBERT
  - MaxSim
  - Sentence Transformers
  - late interaction
  key_people: []
key_logic_flow:
- 多向量模型（又称后期交互或 ColBERT 风格模型）不像稠密模型那样把整段文本压缩成单个向量，而是为每个 token 保留一个小向量。
- 多向量模型使用 MaxSim 算子打分，让每个查询 token 找到最匹配的文档 token 并将分数求和，从而保留单向量需要平均掉的细粒度信号。
- 微调多向量模型涉及模型、数据集、损失函数、训练参数、评估器和训练器类等组件，文章逐一给出实用示例。
- 所有训练与微调流程只需 pip install -U "sentence-transformers[train]" 即可运行。
- 作者在写作本文期间用单张 RTX 3090 训练 14.5 小时得到 multi-vector-encoder/mLateOn-medical 模型，在医疗检索评测上优于他找到的所有通用检索模型。
object_mentions:
- object_type: project
  name: MultiVectorEncoder
  canonical_name: sentence-transformers MultiVectorEncoder
  url: https://github.com/UKPLab/sentence-transformers
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 本文介绍 Sentence Transformers 新增的 MultiVectorEncoder，用于 ColBERT 风格的后期交互检索。
  - 文章展示如何用 MultiVectorEncoder 微调多向量模型，使其在你自己的数据上超过通用检索器。
  article_id: 17177c100c915029
- object_type: project
  name: sentence-transformers
  canonical_name: sentence-transformers
  url: https://github.com/UKPLab/sentence-transformers
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 文章中的所有训练与微调流程都运行在 pip install -U "sentence-transformers[train]" 安装的库之上。
  article_id: 17177c100c915029
- object_type: model
  name: multi-vector-encoder/mLateOn-medical
  canonical_name: multi-vector-encoder/mLateOn-medical
  url: null
  confidence: medium
  article_role: primary_subject
  evidence_snippets:
  - 作者在单张 RTX 3090 上训练 14.5 小时得到的 multi-vector-encoder/mLateOn-medical 模型，在医疗检索评测上超过所有通用检索模型。
  article_id: 17177c100c915029
extract_result: success
impact_score:
  score: 6.8
  reason: 评分依据：这是主流嵌入库对多向量（ColBERT 式后期交互）检索模型训练能力的一次正式补全。此前多向量模型效果虽强，但微调需要定制实现延迟交互损失与
    MaxSim 打分链路，门槛较高；此次以 pip 一键安装 + 完整训练组件（模型/数据集/损失/评估器/训练器）将其工程化并纳入 Sentence Transformers，直接改变检索与
    RAG 工具链的局部竞争格局。作者用单张 RTX 3090 训练 14.5 小时即在医疗评测上超越所有通用稠密/稀疏/词法/多向量检索器，证明了低成本领域定制检索的可行性，这对垂直场景
    RAG 有实质性推动。但该范式本身（ColBERT/后期交互）已是 2020 年以来的成熟技术，此次属于能力民主化而非新范式诞生，影响面集中于检索/嵌入子领域而非全行业，故给予
    6.8 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 单张消费级 GPU（RTX 3090）14.5 小时即可微调出超越通用检索器的多向量领域模型，训练门槛大幅降低
hype_assessment:
  level: low
  reason: 判定依据：文章未滥用'颠覆''革命性'等 PR 词汇，核心卖点是'单卡 3090、14.5 小时、医疗评测超越所有通用模型'这一具体可复现的量化指标，并配以完整的训练组件讲解与评测方法。虽有'outperforms
    every general-purpose retrieval model I could find'这类作者个人化表述，但结论限定于特定医疗评测集且提供了方法论与配套库，整体属于干货型技术发布，炒作成分很低。
information_entropy: high
domain_disruption:
  technical_innovation: 本质突破不在算法架构（ColBERT 后期交互是既有范式），而在于工程化：将多向量模型的完整训练链路（token 级编码、MaxSim
    打分、延迟交互损失、评估器与训练器）封装进主流库，开发者无需自行实现复杂的分布式/内存管理即可微调；同时验证了消费级单卡即可训练出领域专用强检索模型的高效路径，可能加速多向量范式在
    RAG 系统中的实际采用。
  business_model: 领域专用检索模型微调门槛骤降，将削弱通用嵌入 API 在垂直场景（医疗、法律、代码等）的不可替代性，推动'通用 API 兜底 +
    领域微调定制'的混合商业化模式；同时强化 Hugging Face 在训练工具链和模型分发生态上的平台锁定效应，使下游检索厂商更依赖其生态。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: 从资本视角看，本事件的本质是'检索基础设施的民主化'：多向量（ColBERT 后期交互）检索此前是少数具备前沿工程能力团队的专属能力，如今被完整封装进
    Sentence Transformers 开源训练链路（数据→损失→评估→训练器），单张 RTX 3090、14.5 小时即可微调出超越通用检索模型（稠密/稀疏/词法/多向量）的领域模型。复利逻辑在于：检索是
    RAG 与 Agent 技术栈的刚性基础设施，训练门槛降低会推动更多垂直领域数据沉淀为专用模型，进而强化模型层与向量数据库层的生态锁定，并反向刺激对多向量索引存储与算力的需求——这是'数据飞轮+生态锁定'的复合复利。但需扣分验证两点风险：其一，多向量模型索引体积与推理延迟显著高于稠密检索，商用落地仍受成本约束，可能限制其成为主流范式；其二，长上下文模型与下一代检索范式（如代理式检索）可能削弱
    RAG 的必要性，构成范式级威胁。综合判断属于'细分赛道基础设施、需持续验证'区间，给 7.5 分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Hugging Face
- Weaviate
- Qdrant
- Vespa
- jina-ai
- AnswerAI
competitive_casualty:
- 商用稠密 embedding API（OpenAI text-embedding、Cohere Embed、Voyage AI）
- 依赖通用稠密检索的小型 RAG 服务商
- 传统词法检索企业搜索厂商（Elasticsearch）
market_opportunities:
- 企业可在医疗、法律、金融等垂直领域利用 MultiVectorEncoder 以单卡 GPU 在十余小时内微调专属多向量检索模型，显著提升 RAG 场景的召回质量，相比通用检索模型形成可量化的竞争优势
- 建议关注面向中小团队的『检索模型微调即服务』方向，将多向量训练的完整流程（数据准备、损失函数、评估器、训练器）封装为低门槛托管产品，降低垂直检索优化的使用门槛
- 检索质量是 RAG 落地成败的核心瓶颈，可围绕此能力开发面向企业知识库的检索优化咨询与工具链业务，或将其作为个人技能组合中区别于通用 Embedding 调优的差异化能力
risk_matrix:
  regulatory: 医疗等垂直领域的微调涉及敏感数据合规（如医疗数据隐私、训练语料授权），训练与部署需符合当地数据保护法规；工具本身暂无直接监管风险
  technological: 多向量模型为每个 token 保留向量，索引体积与推理开销显著高于稠密向量，存储与算力成本高；后续可能被性能提升的稠密模型、学习型稀疏模型或新一代检索范式部分替代
  competitive: Hugging Face 将能力纳入主流库后，云厂商、向量数据库（Pinecone、Weaviate 等）及 RAG 框架（LlamaIndex、Haystack）可能快速集成同类能力，挤压独立工具生态空间；RAGatouille/ColBERTv2
    等既有方案已先一步布局
  ethical: 领域微调训练数据可能存在偏见或数据投毒风险；医疗等高风险场景下检索错误可能误导关键决策，需引入人工审核与模型护栏机制
  additional:
  - 多向量索引规模增长带来的基础设施成本扩张风险，需在产品设计中权衡召回质量与部署成本的平衡
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: MultiVectorEncoder
  canonical_name: sentence-transformers MultiVectorEncoder
  url: https://github.com/UKPLab/sentence-transformers
  positioning: Sentence Transformers 库新增的 MultiVectorEncoder 训练模块，用于微调 ColBERT 风格的多向量后期交互检索模型，支持从零训练并在自有数据上超越通用检索器。
  technical_signal: 多向量模型为每个 token 保留独立向量，用 MaxSim 算子让查询 token 匹配最佳文档 token 并求和，保留单向量平均掉的细粒度匹配信号。
  adoption_signal: 训练能力随官方库发布，pip install -U "sentence-transformers[train]" 即可使用；作者用单张
    RTX 3090 训练 14.5 小时即微调出超越通用检索器的医疗模型。
  ecosystem_relevance: Sentence Transformers 是 Hugging Face 与 UKPLab 生态中广泛使用的嵌入库，MultiVectorEncoder
    补齐多向量模型训练能力，衔接检索评测、向量数据库等周边生态。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: MultiVectorEncoder 把 ColBERT 风格多向量模型的训练能力直接并入主流嵌入库，大幅降低微调门槛；实测单卡 14.5
    小时即可在医疗检索超越所有通用检索器，若被社区广泛采用，可能显著改变检索模型定制化格局，值得持续跟踪版本演进与社区采用情况。
  risk_notes:
  - 多向量模型索引体积显著大于单向量模型，存储与检索成本更高，可能限制在资源受限场景的落地。
  - 评测仅基于作者自建的医疗检索数据集，样本量与评测设计未公开，结论的普适性有待验证。
  - 该训练接口为新增功能，稳定性与社区实践积累仍不足，生产环境采用需谨慎评估。
  score: 7.0
  article_ids:
  - 17177c100c915029
  evidence_snippets:
  - 本文介绍 Sentence Transformers 新增的 MultiVectorEncoder，用于 ColBERT 风格的后期交互检索。
  - 文章展示如何用 MultiVectorEncoder 微调多向量模型，使其在你自己的数据上超过通用检索器。
---

`MultiVectorEncoder`

, for ColBERT-style late interaction retrieval, alongside a complete training approach for it. In this blogpost, I'll show you how to use it to finetune a multi-vector model that outperforms general-purpose retrievers on your data. This method can also train strong new multi-vector models from scratch. Everything below runs on `pip install -U "sentence-transformers[train]"`

.
Finetuning multi-vector models involves several components: the model itself, datasets, loss functions, training arguments, evaluators, and the trainer class. I'll have a look at each of these components, accompanied by practical examples of how they can be used for finetuning strong multi-vector models.

Lastly, in the Evaluation section, I'll show you that my finetuned multi-vector-encoder/mLateOn-medical model, trained in 14.5 hours on a single RTX 3090 alongside this blogpost, easily outperforms every general-purpose retrieval model I could find on my medical retrieval evaluation: dense, sparse, lexical, and multi-vector alike.

If you're interested in finetuning dense embedding models, sparse embedding models, or rerankers instead, then consider reading through my prior Training and Finetuning Embedding Models, Training and Finetuning Sparse Embedding Models, and Training and Finetuning Reranker Models blogposts.

This blogpost is about

trainingmulti-vector models. If you want to learn how tousethem, from loading and encoding to indexing in vector databases, see the companion Multi-Vector (Late Interaction) Embedding Models with Sentence Transformers blogpost.

- What are Multi-Vector models?
- Why Finetune?
- Training Components
- Model
- Dataset
- Loss Function
- Training Arguments
- Evaluator
- Trainer
- Evaluation
- Acknowledgements
- Additional Resources

A dense embedding model compresses a whole text into a single vector, and similarity is one dot product between two such summaries. A multi-vector model (also called a late-interaction or ColBERT-style model) skips that compression. It keeps **one small vector per token** and scores a query against a document with the MaxSim operator, where every query token finds its best-matching document token and the scores are summed. Token-level matching preserves exactly the fine-grained signals that a single vector has to average away, which usually means stronger retrieval, at the cost of a bigger index.

The companion Multi-Vector Embedding Models blogpost covers the architecture, encoding, scoring, and indexing in detail, so I'll keep this section short and get to the training.