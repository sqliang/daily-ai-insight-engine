---
title: 'Agentic Resource Discovery: Let agents search'
source: https://huggingface.co/blog/agentic-resource-discovery-launch
author: []
published: '2026-06-17'
created: '2026-06-18'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b59b3271b775356f
source_type: tech_blog
tldr: 微软、谷歌、Hugging Face 等联合发布 ARD 开放规范，让 AI 代理在运行时动态发现工具，无需预安装。
objective_summary: Microsoft、Google、GoDaddy、Hugging Face 等联合发布了 Agentic Resource Discovery
  (ARD) 开放规范草案，定义代理和工具的编目、索引和跨联邦注册表搜索标准。Hugging Face 发布了参考实现 Hugging Face
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - Microsoft
  - Google
  - GoDaddy
  - Hugging Face
  technologies:
  - ARD
  - MCP
  - A2A
  - Agentic Resource Discovery
  key_people: []
key_logic_flow:
- ARD 是一个开放规范草案，由 Microsoft、Google、GoDaddy、Hugging Face 等贡献者联合制定，定义了代理和工具在联邦注册表中的编目、索引和搜索标准。
- 当前代理工具模型是"先安装后使用"模式，开发者将 MCP 服务器 URL 硬编码到配置中，这种模式无法扩展到数千个临时交互场景。
- 另一种替代方案是将所有工具描述塞入 LLM 上下文窗口让模型选择，但受限于上下文预算且描述过于单薄。
- ARD 将选择过程移出 LLM：注册表用发布者身份、代表查询、合规证明和标签等更丰富的信号索引能力，客户端通过自然语言搜索，代理使用搜索结果调用工具。
- 规范定义了两个核心内容：静态清单格式 ai-catalog.json（发布者在已知 URL 托管能力信息）和动态注册表 API POST /search（提供实时排序的发现能力）。
- Hugging Face 发布了参考实现 Hugging Face Discover Tool，结合 Hub 的语义搜索和 Agent Skills，提供对数千个
  Gradio 应用、MCP 服务器和演示的搜索访问。
impact_score:
  score: 7.0
  reason: 该事件发布的是开放规范草案而非正式标准，但解决的痛点——代理工具发现机制——是当前整个 AI Agent 生态的关键瓶颈。目前 MCP/A2A
    等协议解决了连接问题，却缺乏发现层；ARD 补上了这一环，且由微软、谷歌、Hugging Face 等跨阵营联合推动，具备成为行业事实标准的潜力。短期直接影响有限（草案阶段），但对
    Agent 基础设施架构有长远塑造力。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 动态工具发现规范能否取代现有的硬编码 MCP 配置模式
hype_assessment:
  level: low
  reason: 文章以技术博客风格呈现，详细解释了规范的设计动机（安装优先模式的扩展性问题）、架构决策（将选择过程移出 LLM 上下文窗口）以及具体的两种接口格式。没有使用'颠覆''革命性'等
    PR 套话，Hugging Face 也明确标注参考实现为草案阶段。措辞务实。
information_entropy: high
domain_disruption:
  technical_innovation: 提出将工具选择逻辑从 LLM 上下文窗口剥离到独立的发现层，通过联邦注册表用发布者身份、代表查询、合规证明、标签等丰富信号索引能力，代理通过自然语言搜索在运行时动态发现工具——从'安装优先'范式转向'意图驱动搜索'范式。静态清单
    ai-catalog.json + 动态注册表 POST /search 的双层架构既支持轻量级静态发布也支持实时排序发现。
  business_model: 标准化发现层可能催生跨组织工具市场生态：任何公司可独立实现注册表，代理可跨联邦注册表搜索，类似互联网的 DNS 体系。这将打破目前各平台封闭的插件市场格局，使工具发现从垂直整合走向水平互联，对
    SaaS API 经济产生深远影响。
engineering_complexity: prototype
compound_value:
  score: 8.0
  reason: ARD 解决的是 AI Agent 生态扩张的关键瓶颈——工具运行时发现。当前'先安装后使用'模式需要开发者硬编码 MCP URL，无法扩展到千级临时交互场景；而将所有工具描述塞入
    LLM 上下文的方案又受限于预算。ARD 将选择逻辑移出 LLM，通过联邦注册表和意图驱动的自然语言搜索实现动态发现，这是一个根本性的架构改进。由 Microsoft、Google、GoDaddy、Hugging
    Face 等联合制定，具备强行业背书和网络效应：注册表越多价值越大（供给端），Agent 可用工具越丰富用户粘性越强（需求端）。一旦成为事实标准，它将像 DNS
    之于互联网一样作为 Agent 基础设施持久存在，复利效应显著。主要风险：仍为草案阶段，存在标准碎片化或 Adoption 不及预期的可能，但巨头联合背书大幅降低了这一风险。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Hugging Face
- Microsoft
- Google
- LangChain
- CrewAI
competitive_casualty:
- Zapier
- 闭源 Agent 平台
- RapidAPI
market_opportunities:
- 开发者可围绕 ARD 规范构建垂直行业的工具注册表（如金融合规、医疗诊断、法律检索），通过联邦搜索为专业领域的 AI Agent 提供动态工具发现能力，抢占生态位
- 企业可将现有 MCP 服务器或内部工具库接入 ARD 清单格式（ai-catalog.json），降低 Agent 集成的配置成本，实现从'预装模式'到'按需发现'的迁移，提升产品互操作性
- 第三方服务商可提供 ARD 注册表托管、合规认证审核、工具搜索质量优化等增值服务，面向中大型企业输出 Agent 工具治理解决方案
risk_matrix:
  regulatory: ARD 联邦注册表允许跨组织工具发现，可能引发责任归属争议（如 Agent 使用了注册表中恶意或不合规的工具时由谁担责）；若纳入欧盟 AI
    Act 的高风险分类，注册表运营方可能需要承担透明度义务和合规审查责任
  technological: ARD 当前为草案阶段，如果行业采纳率不足或出现更强势的竞争标准（如 OpenAI 推出类似但闭源的协议），该规范可能边缘化；同时其依赖的
    MCP/A2A 生态仍处于早期，成熟度和长期兼容性存在不确定性
  competitive: Microsoft、Google 等联合发起方可能各自推出带有平台锁定效应的商业化扩展，将开放的 ARD 规范转化为事实上的生态入口壁垒，挤压第三方独立注册表的生存空间
  ethical: 动态工具发现机制若缺乏严格的发布者身份核验和工具行为审计，可能导致 Agent 被恶意注册表中的投毒工具诱导执行高风险操作（如删除数据、越权访问），造成用户隐私泄露或安全事件
  additional: []
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
---

The Agentic Resource Discovery (ARD) specification is the discovery layer that sits in front of them. It is a draft, open specification developed by contributors from Microsoft, Google, GoDaddy, Hugging Face, and others, with broad participation across the industry. It defines how agents and tools are cataloged, indexed, and searched across federated registries, so an agent can find capabilities at runtime instead of needing them pre-installed. It is not a product or a marketplace. It is a shared standard that any company can implement independently, and that any agent or tool can participate in.

In this post, we'll explore the specification, how Hugging Face has implemented it, and how you can start building on ARD.

The current model for agent capabilities is install-first, use-later. A developer hardcodes an MCP server URL into a config file. A user connects a service to their AI app via a plugin and reuses it. This works for the handful of tools an agent uses every day, but it doesn't scale to thousands of ad-hoc surfaces.

The fallback is to dump every available tool description into the LLM's context window and let the model pick. This is limited by the context budget. There are search-based strategies here too, but the descriptions are often too thin to disambiguate well.

ARD moves selection outside the LLM. A registry indexes capabilities with richer signals such as publisher identity, representative queries, compliance attestations, and tags. It exposes a REST endpoint. The client searches in natural language, and the model invokes whatever the search returns. The shift is from manually installed, static catalogs to intent-based search that lets an agent find the right capability dynamically, and reach a growing ecosystem of MCP tools, A2A agents, and other services without pre-configuring each one.

The specification defines two things:

- A static manifest format called
`ai-catalog.json`

lets publishers host their capabilities at a well-known URL. - A dynamic registry API at
`POST /search`

provides live, ranked discovery.

The Hugging Face Discover Tool is our reference implementation of ARD. It provides search access to thousands of Skills, ML applications, and MCP Servers — on Hugging Face and across other ARD discovery services.

It works by combining the Hub's existing semantic search over Spaces, alongside our Agent Skills, and serving the results as ARD catalog entries. The Hub already hosts a catalog of Spaces running Gradio apps, MCP servers, and demos. Its semantic search supports an `agents=true`

flag that returns Spaces ranked by agent-oriented metadata, and Discover translates that search into the ARD specification.

The adapter applies two filters. First, the response includes only Spaces whose runtime stage is `RUNNING`

. Second, the response media type is driven by the request. Three media types are supported:

`application/ai-skill`

: the default. A generated`SKILL.md`

wrapping the Space's`agents.md`