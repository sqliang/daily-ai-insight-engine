---
title: Run a vLLM Server on HF Jobs in One Command
source: https://huggingface.co/blog/vllm-jobs
author: []
published: '2026-06-26'
created: '2026-06-26'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 5d2de9152522b89c
source_type: tech_blog
tldr: Hugging Face 推出通过一条命令在 HF Jobs 上运行 vLLM 推理服务器的功能，用户可使用 `hf jobs run` 指定 GPU
  型号、暴露端口，快速启动 OpenAI 兼容的模型推理端点，适合测试、评估和批量生成场景。
objective_summary: Hugging Face 在 2026 年 7 月发布了 `hf jobs run` 命令，允许用户通过单条命令在 HF Jobs
  基础设施上启动 vLLM 推理服务器。用户需安装 `huggingface_hub>=1.20.0` 并登录，指定 `--flavor`（如 a10g-large）和
  `--expose 8000` 参数即可运行官方 `vllm/vllm-openai` 镜像。启动后服务通过 HF 公共 Jobs 代理暴露，客户端使用 Hugging
  Face token 作为 Bearer token 即可通过 OpenAI 兼容 API 调用模型。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  technologies:
  - vLLM
  - OpenAI API
  key_people: []
key_logic_flow:
- Hugging Face 推出了 `hf jobs run` 命令，使用户能够以类似 `docker run` 的方式在 HF Jobs 基础设施上快速启动模型服务。
- 用户需要安装 `huggingface_hub>=1.20.0` 并通过 `hf auth login` 完成本地登录，同时需要有效的付款方式或预付费余额。
- 通过指定 `--flavor a10g-large`、`--expose 8000` 和 `--timeout 2h` 参数，可使用官方 `vllm/vllm-openai`
  镜像运行 `vllm serve` 命令启动推理服务器。
- 启动后服务会通过 HF 的公共 Jobs 代理暴露，用户获得一个格式为 `https://<job_id>--8000.hf.jobs` 的可访问 URL。
- vLLM 服务器提供 OpenAI 兼容的 API 接口，客户端使用 Hugging Face token 作为 Bearer token 即可发起请求，支持
  curl 和 OpenAI Python SDK 两种调用方式。
- 该功能适用于测试、评估和批量生成等非生产场景，生产级需求应使用 Hugging Face Inference Endpoints。
extract_result: success
object_mentions:
- object_type: project
  name: vLLM
  canonical_name: vllm-project/vllm
  url: https://github.com/vllm-project/vllm
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章使用官方 `vllm/vllm-openai` 镜像，通过 `vllm serve Qwen/Qwen3-4B --host 0.0.0.0 --port
    8000` 命令启动推理服务器。
  - vLLM 服务器提供 OpenAI 兼容的 API 接口，客户端可通过 curl 或 OpenAI Python SDK 调用模型的 chat completions
    功能。
  article_id: 5d2de9152522b89c
- object_type: product
  name: HF Jobs
  canonical_name: Hugging Face Jobs
  url: https://huggingface.co/jobs
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - '`hf jobs run` 命令允许用户在 HF Jobs 基础设施上启动容器化任务，按硬件使用分钟计费，需要付款方式或预付费余额。'
  - 通过 `--flavor a10g-large` 指定 GPU 型号、`--expose 8000` 暴露端口、`--timeout 2h` 设置超时时间，即可在
    Jobs 上运行 vLLM 推理服务。
  - 启动后服务通过 HF 公共 Jobs 代理暴露，URL 格式为 `https://<job_id>--8000.hf.jobs`，需使用 HF token
    进行认证访问。
  article_id: 5d2de9152522b89c
- object_type: project
  name: huggingface_hub
  canonical_name: huggingface/huggingface-hub
  url: https://github.com/huggingface/huggingface-hub
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 使用 `hf jobs run` 命令需要安装 `huggingface_hub>=1.20.0` 版本，通过 `pip install -U "huggingface_hub>=1.20.0"`
    安装。
  - 用户需要通过 `hf auth login` 完成本地登录认证，后续 API 调用使用 `hf auth token` 获取的 token 进行鉴权。
  article_id: 5d2de9152522b89c
impact_score:
  score: 5.5
  reason: 该功能本质上是将 vLLM 推理服务器的部署流程封装为一条 CLI 命令，借助 HF Jobs 的基础设施（Docker + GPU 调度 +
    公共代理 URL + Token 认证）实现零配置启动。对开发者而言，这大幅降低了原型验证和模型评测的摩擦成本——过去需要自行管理 GPU 实例、配置网络和认证，现在一条命令即可获得
    OpenAI 兼容的推理端点。但 vLLM 本身早已成熟，且 Hugging Face Inference Endpoints 也提供类似能力，因此该功能的增量价值主要在于'低成本快速试错'场景，而非范式级创新。在
    Hugging Face 平台化战略的语境下，这是补齐开发者工作流中'实验 → 生产'衔接环节的重要一步，对局部竞争格局（尤其是与 Replicate、Together
    AI 等推理平台的对比）有中等冲击。综合判定为 5.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 一键启动推理服务器的便捷性，以及公共代理 URL 带来的免运维体验
hype_assessment:
  level: low
  reason: 文章未使用'革命性''颠覆'等 PR 术语，而是以教程式行文清晰说明功能用法、前置条件和限制（需要支付方式、按分钟计费、超时设置等）。功能本身是务实的工程改进，定位明确——让开发者快速起模型做测试和评估，并诚实区分了
    Jobs 与 Inference Endpoints 的使用场景。判定为干货型发布。
information_entropy: medium
domain_disruption:
  technical_innovation: 无本质技术突破。该功能是已有技术的工程集成：HF Jobs（Docker 容器调度）+ vLLM（推理引擎）+ 反向代理认证层。技术亮点在于将多步操作（申请
    GPU、配置 Docker、暴露端口、设置认证）压缩为一条命令，属于工程体验优化。
  business_model: Hugging Face 通过 Jobs 提供按分钟计费的推理能力，在免费/低成本的实验性推理市场与 Replicate、Together
    AI 等形成差异化竞争。与 Inference Endpoints（生产级托管）形成阶梯式定价和服务体系，覆盖从实验到生产的完整链路，有助于锁定用户工作流在
    HF 生态内。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: 从长期复利角度评估，HF Jobs + vLLM 一键推理的价值在于'漏斗顶端的习惯养成效应'。HF 通过在平台内直接提供零摩擦的推理体验，将模型浏览者转化为平台计算用户，再逐步引导至高毛利
    Inference Endpoints。这种'试用→生产'的转化漏斗一旦形成规模，用户切换成本将显著增加——开发者的测试流水线、CI/CD 脚本、团队协作都绑定在
    HF 生态内。但评分上限受限于：1) vLLM 本身是开源中立技术，竞品（如 RunPod、Beam、Modal）可快速复制类似体验；2) GPU 算力本身是商品化资源，HF
    在此层的定价权有限；3) 并非核心模型层或框架层的突破。综合来看，它在 HF 平台内部具备强复利效应，但作为独立事件对行业格局的边际推动力为中等偏上。
value_capture_layer: cloud_platform
moat_impact: democratizes_access
key_beneficiaries:
- Hugging Face
- NVIDIA
- vLLM
- 开源模型开发者
competitive_casualty:
- Replicate
- Together AI
- Fireworks AI
- 自建推理基础设施
market_opportunities:
- 开发者可利用 HF Jobs 一键启动 vLLM 的能力，将模型评测和选型流程从数小时缩短至几分钟，显著降低推理验证的试错成本
- 创业团队可基于此功能构建轻量化的垂直场景推理服务（如客服、代码生成、文档分析），以按分钟计费替代长期 GPU 租赁，降低冷启动成本
- AI 教育机构可将 HF Jobs+vLLM 作为教学实践平台，让学生无需配置基础设施即能实验主流开源模型的推理效果
risk_matrix:
  regulatory: GPU 出口管制（如美国对中国的 A100/A10G 限制）可能影响部分地区的用户正常使用指定 flavor 实例；用户需自行确认所在地区的硬件合规性
  technological: vLLM 面临 SGLang、TensorRT-LLM、TGI 等推理框架的持续竞争，长期技术路线并非唯一选择，迁移成本需关注
  competitive: AWS SageMaker、Google Cloud Vertex AI、Replicate、Together AI 等平台均提供类似的一键模型部署服务，HF
    Jobs 的核心差异化在于与 Hugging Face 生态和模型库的深度集成
  ethical: 一键部署大幅降低了模型服务上线门槛，可能被用于深度伪造生成、有害内容传播等滥用场景，Hugging Face 的 token 认证机制是有限的防线
  additional:
  - 成本失控风险——按分钟计费且无内置预算上限，长时间运行或忘记终止任务可能导致意外高额账单
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: vLLM
  canonical_name: vllm-project/vllm
  url: https://github.com/vllm-project/vllm
  positioning: 高性能开源模型推理引擎，支持多种主流模型架构的高效推理，提供与 OpenAI 兼容的标准 API 接口。
  technical_signal: vLLM 已提供官方 Docker 镜像并与 Hugging Face Jobs 深度集成，支持通过单条命令快速启动推理服务。
  adoption_signal: 用户可通过 `hf jobs run` 命令在 Hugging Face 基础设施上直接部署 vLLM 服务，采用 GPU 按分钟计费模式。
  ecosystem_relevance: 与 Hugging Face 生态系统紧密集成，支持 HF token 认证和公共 Jobs 代理暴露，成为 HF 推理基础设施的默认引擎。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: vLLM 正从独立推理引擎演变为 Hugging Face 基础设施层的默认推理后端，其与 HF Jobs 的单命令集成模式降低了部署门槛，值得持续关注其生态影响。
  risk_notes:
  - 单命令部署方式明确标注为非生产场景，生产级需求需转向 Inference Endpoints。
  - 深度绑定 Hugging Face Jobs 基础设施，存在一定程度的平台锁定风险。
  score: 6.0
  article_ids:
  - 5d2de9152522b89c
  evidence_snippets:
  - 文章使用官方 `vllm/vllm-openai` 镜像，通过 `vllm serve Qwen/Qwen3-4B --host 0.0.0.0 --port
    8000` 命令启动推理服务器。
  - vLLM 服务器提供 OpenAI 兼容的 API 接口，客户端可通过 curl 或 OpenAI Python SDK 调用模型的 chat completions
    功能。
- object_type: product
  name: HF Jobs
  canonical_name: Hugging Face Jobs
  url: https://huggingface.co/jobs
  positioning: Hugging Face 推出的容器化任务托管服务，支持 GPU 按分钟计费，可通过 `hf jobs run` 命令一键启动推理任务。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - AI 开发者和研究人员
  - 需要快速测试和评估模型的团队
  - 需要批量生成离线结果的用户
  product_signal: 提供 `hf jobs run` 命令实现类似 `docker run` 的容器化体验，支持 `--flavor`、`--expose`、`--timeout`
    等灵活配置参数。
  market_signal: 该功能定位在测试、评估和批量生成等非生产场景，生产级推理需求由 Inference Endpoints 覆盖，形成清晰分层。
  differentiation: 与 Hugging Face 模型生态深度集成，使用 HF token 统一认证，无需用户管理 Kubernetes 或云基础设施即可快速部署推理服务。
  watch_reason: HF Jobs 将 Hugging Face 从模型仓库平台向推理基础设施服务延伸，其按分钟计费的 GPU 服务和单命令部署能力显著降低了模型服务化门槛，值得跟踪其演进方向。
  risk_notes:
  - 明确标注为非生产场景，生产推理仍需转向 Inference Endpoints。
  - 需要有效的付款方式或预付费余额，按硬件使用分钟计费可能产生成本。
  score: 7.0
  article_ids:
  - 5d2de9152522b89c
  evidence_snippets:
  - '`hf jobs run` 命令允许用户在 HF Jobs 基础设施上启动容器化任务，按硬件使用分钟计费，需要付款方式或预付费余额。'
  - 通过 `--flavor a10g-large` 指定 GPU 型号、`--expose 8000` 暴露端口、`--timeout 2h` 设置超时时间，即可在
    Jobs 上运行 vLLM 推理服务。
  - 启动后服务通过 HF 公共 Jobs 代理暴露，URL 格式为 `https://<job_id>--8000.hf.jobs`，需使用 HF token
    进行认证访问。
---

It's the quickest way to stand up a model for tests, evals, or batch generation. (If you're after a managed, production-ready service instead, that's what Inference Endpoints are for — more on when to pick which at the end.)

Here's the whole thing end to end.

- A payment method or a positive prepaid credit balance (Jobs is billed per‑minute by hardware usage).
`huggingface_hub >= 1.20.0`

:`pip install -U "huggingface_hub>=1.20.0"`

.- Logged in locally:
`hf auth login`

.

`hf jobs run`

is `docker run`

for HF infrastructure. We use the official `vllm/vllm-openai`

image, ask for a GPU with `--flavor`

, and expose vLLM's port with `--expose`

:

```
hf jobs run --flavor a10g-large --expose 8000 --timeout 2h \
vllm/vllm-openai:latest \
vllm serve Qwen/Qwen3-4B --host 0.0.0.0 --port 8000
```


`--expose 8000`

routes the container's port through HF's public jobs proxy (see the Serve Models guide for the full reference). The command prints the URL your server is reachable at:

```
✓ Job started
id: 6a381ca1953ed90bfb947332
url: https://huggingface.co/jobs/qgallouedec/6a381ca1953ed90bfb947332
Hint: Exposed ports are reachable at (requires an HF token with read access to the job):
https://6a381ca1953ed90bfb947332--8000.hf.jobs
```


`6a381ca1953ed90bfb947332`

is your job ID. Keep track of it, we'll need it. We'll use `<job_id>`

as a placeholder for it in the rest of the post.

Give it a couple of minutes to download weights and boot. When the logs show `Application startup complete`

, you're live.

vLLM speaks the OpenAI API, and every request just needs your HF token as a bearer token. The quickest way to hit it is curl:

```
curl https://<job_id>--8000.hf.jobs/v1/chat/completions \
-H "Authorization: Bearer $(hf auth token)" \
-H "Content-Type: application/json" \
-d '{
"model": "Qwen/Qwen3-4B",
"messages": [{"role": "user", "content": "Hello!"}],
"chat_template_kwargs": {"enable_thinking": false}
}'
```


which returns the usual OpenAI-style JSON, with `choices[0].message.content`

holding `"Hello! How can I assist you today? 😊"`

.

Or, from Python, point the OpenAI client at the exposed URL and pass the token as the API key:

```
from huggingface_hub import get_token
from openai import OpenAI
client = OpenAI(
base_url="https://<job_id>--8000.hf.jobs/v1",
api_key=get_token(),
)
resp = client.chat.completions.create(
model="Qwen/Qwen3-4B",
messages=[{"role": "user", "content": "Hello!"}],
extra_body={"chat_template_kwargs": {"enable_thinking": False}},
)
print(resp.choices[0].message.content)
```


```
Hello! How can I assist you today? 😊
```


Quick health check before you start: `curl https://<job_id>--8000.hf.jobs/v1/models -H "Authorization: Bearer $(hf auth token)"`

should list the model.