---
title: 别光给Agent加Tool了，它根本选不明白！复旦×通义提出全新CUA训练范式
source: https://www.qbitai.com/2026/05/427005.html
author:
- '[[Jay]]'
published: '2026-05-31'
created: '2026-06-01'
description: 下一代CUA训练范式
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 5924bbd53efe8a2d
source_type: news_media
tldr: 复旦×通义提出ToolCUA，一种解决Agent在GUI和Tool之间路径选择困难的CUA训练范式，8B模型超越Claude-4-Sonnet。
objective_summary: 复旦大学和通义实验室联合提出ToolCUA训练范式，通过数据合成和Tool-Bootstrapped RFT两阶段训练，解决CUA在GUI操作和工具调用混合动作空间中的路径选择问题。ToolCUA-8B在OSWorld-MCP基准上达到46.
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - 复旦大学
  - 通义实验室
  technologies:
  - ToolCUA
  - CUA
  - GUI
  - RFT
  - MLLM
  - OSWorld-MCP
  key_people: []
key_logic_flow:
- 复旦大学和通义实验室发现，给Agent同时添加GUI操作和工具调用能力后，模型在两者之间缺乏路径选择能力，导致准确率不升反降。
- 团队提出ToolCUA，一种面向GUI-Tool混合动作空间的CUA训练范式，核心目标是让模型学会动态选择GUI操作或工具调用。
- ToolCUA-8B在OSWorld-MCP基准上达到46.85%准确率，超过Claude-4-Sonnet（43.5%），逼近Claude-4.5-Sonnet（48.4%）。
- 第一阶段通过Interleaved GUI-Tool Trajectory Scaling Pipeline将已有的GUI-only数据转换为混合GUI-Tool轨迹，包含约4000个独特工具和18万步训练数据。
- 第二阶段执行Tool-Bootstrapped GUI RFT，通过warmup SFT和单轮RL为模型建立混合动作基础。
- 代码和模型权重已全面开源。
impact_score:
  score: 7.0
  reason: 这是一个针对 Agent 混合动作空间（GUI+Tool）路径选择难题的真实技术突破。核心贡献在于：1）揭示了反直觉现象——给 Agent 强行加
    Tool 反而降低准确率（Claude-4-Sonnet 从 47.7% 降至 43.5%），并正确定义了 optimal GUI-Tool path selection
    问题；2）提出 Interleaved GUI-Tool Trajectory Scaling Pipeline 将存量 GUI-only 数据转化为混合轨迹，解决了高质量混合数据的稀缺问题；3）ToolCUA-8B
    以 46.85% 在 OSWorld-MCP 上超越 Claude-4-Sonnet（43.5%），逼近 Claude-4.5-Sonnet（48.4%），验证了小参数模型通过针对性训练即可超越闭源大模型。该工作在学术研究层面具有较高影响力，可能成为
    CUA 训练范式的重要分支，但距离行业级范式转移（如 ChatGPT 发布）尚有距离，且基准覆盖范围和实际工程部署效果需进一步验证。综合评定 7.0。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 混合动作空间中 GUI 与 Tool 的路径选择训练方法，以及 8B 开源模型超越闭源大模型的性价比优势
hype_assessment:
  level: medium
  reason: 标题使用了「别光给Agent加Tool了，它根本选不明白！」等情绪化表达，具有一定的 PR 包装色彩。但文章内容提供了详实的实验数据（4个模型在有无
    Tool 条件下的准确率对比、180k steps 训练数据、4k unique tools 等），代码和权重已开源。问题定义本身扎实，实验结果可复现，因此认定为中等炒作——有一定包装但非空洞概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 提出 Interleaved GUI-Tool Trajectory Scaling Pipeline，通过 MLLM
    从纯 GUI 轨迹中抽象出 grounded tool library 并生成混合轨迹，配合 Tool-Bootstrapped RFT 两阶段训练，使小模型学会在
    GUI 操作和工具调用之间动态选择最优路径——这是首次系统性地解决混合动作空间中的路径选择问题，而非简单拼接两种能力。
  business_model: 开源模型权重降低了企业构建 CUA Agent 的门槛：8B 模型即可达到甚至超越闭源商用模型的水平，意味着中小企业不必依赖 API
    调用即可部署具备 GUI+Tool 混合能力的 Agent，可能加速 AI Agent 在自动化办公、RPA、浏览器自动化等场景的落地成本结构变化。
engineering_complexity: prototype
compound_value:
  score: 7.0
  reason: ToolCUA 解决了混合动作空间中 GUI 与 Tool 路径选择的根本性问题，这是 AI Agent 走向实用化的关键瓶颈。核心投资逻辑：①路径选择问题是永久性的，Agent
    永远需要在 GUI 操作与 API 调用之间做决策，这一问题不会随着模型规模增大而自动消失（论文已证明大模型加 Tool 后准确率反而下降）；②8B 模型超越
    Claude-4-Sonnet 验证了方法论的杠杆效应——通过训练范式创新而非堆参数，就能获得不对等的性能提升，这是 VC 最看重的'效率跃迁'逻辑；③背靠通义实验室（阿里）有持续的算力和数据资源投入，且代码/模型权重全面开源，可能成为
    Agent 训练的事实标准范式。风险点：学术界创新迭代极快，该范式可能在 6-12 个月内被新方法超越；开源模式导致商业价值捕获路径不清晰——作为研究贡献而非产品，直接货币化难度大。综合判断：有潜力成为
    Agent 训练领域的基础设施级方法，但需观察工业界采纳速度和持续迭代能力。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- 通义实验室 (Tongyi Lab)
- Alibaba Cloud
- 开源 Agent 生态
competitive_casualty:
- Anthropic (Claude-4-Sonnet)
- 闭源 CUA 训练方案提供商
- 依赖纯 GUI 规则的 RPA 厂商
market_opportunities:
- 开源模型 ToolCUA-8B 可直接用于企业级 RPA（机器人流程自动化）产品的智能化升级，替代传统基于规则的动作编排，实现 GUI 与 API 的自动路径选择，大幅降低运维成本
- 该训练范式中的数据合成 Pipeline（Interleaved GUI-Tool Trajectory Scaling）具有独立商业价值，可作为 SaaS 服务提供给需要将存量
  GUI-only 数据转化为混合轨迹的 AI 开发团队
- 建议关注 Agent 操作系统的中间件机会——在模型底层能力之上开发 GUI/Tool 调度层产品，为复杂办公自动化、软件测试、跨应用工作流提供通用决策引擎
risk_matrix:
  regulatory: 中美学界合作的开源模型可能面临 AI 出口管制与芯片禁令的合规审查，尤其是涉及高性能小模型（8B级）的跨境分发
  technological: 该方法依赖 MLLM 进行数据合成（合成工具库、轨迹生成），合成数据的质量边界尚不明确，在长尾或高度专业化的 GUI 场景中可能引入偏差或降级
  competitive: Anthropic（Claude-4.5-Sonnet，48.4%）和 OpenAI 等巨头在 CUA 赛道已有先发优势，且具备更丰富的真实用户交互数据，开源
    8B 方案可能被迅速吸收并超越
  ethical: GUI-Tool 混合 Agent 的低成本部署可能被滥用于自动化网络攻击（如 GUI 驱动的渗透测试、钓鱼操作），且大规模替代人工 GUI
    操作可能冲击传统客服与数据录入岗位
  additional: []
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: deep_dive
---

# 别光给Agent加Tool了，它根本选不明白！复旦×通义提出全新CUA训练范式

下一代CUA训练范式

复旦×通义团队 投稿

量子位 | 公众号 QbitAI


**给Agent同时接上GUI操作和工具调用，准确率反而下降了。**

模型根本不会在GUI和Tool之间选择。该点按钮的时候去调API，该调API的时候又死磕菜单，两头乱窜，越帮越忙。

为应对这一挑战，**复旦大学**和**通义实验室MobileAgent团队**联合提出**ToolCUA**，一个面向GUI-Tool混合动作空间的Computer Use Agent。

核心目标就一个：让模型学会什么时候走GUI，什么时候切Tool，什么时候不该调工具。

结果相当能打。

ToolCUA-8B在OSWorld-MCP上拿到**46.85%**准确率，超过**Claude-4-Sonnet**，逼近**Claude-4.5-Sonnet**。

**代码、模型权重已全面开源。**

# 混合动作空间下的路径困惑

传统的CUA主要依赖原子化GUI操作，例如点击、输入、拖拽、滚动。这类操作泛化性强，只要界面上能看到按钮，理论上模型就能点；但它也有明显短板：步骤长、误差容易累积，在复杂任务中很容易出现cascading errors。

相反，tool calls或API-based operations往往更高效、更精确。例如在LibreOffice里批量处理表格，GUI-only方案可能需要一串冗长的菜单点击和参数配置，而工具调用可能一个API就能完成。

看起来最自然的方案，是让Agent同时拥有GUI和Tool。但实验发现一个非常反直觉的事实：**直接把tools接到强模型上，并不会自动提升性能。**

在hybrid GUI-Tool action space中，Agent每一步都站在一个岔路口：左边是GUI，右边是Tool。GUI泛化强但慢，Tool快但依赖覆盖与上下文条件。如果模型缺少路径选择能力，就会出现两类典型失败:

**Tool underuse**：明明有更高效的工具，模型仍然几乎只走GUI路线。

**Tool overuse**：模型频繁调用工具，但调用时机不对、调用粒度不对，反而降低任务成功率。

论文将这个问题定义为**optimal GUI-Tool path selection**：在长程任务中动态决定何时使用GUI actions、何时调用tools，从而形成更高效、更可靠的执行路径。

上图左侧的表格直接给出了这个反直觉现象：一旦把tools接到强模型上，结果并不总是更好。

Qwen3VL-8B几乎不使用工具，平均tool calls只有0.003，准确率从29.0%降到28.2%；Qwen3VL-235B则明显更倾向于调用工具，平均tool calls达到6.10，步骤数从25.9降到17.4，但准确率反而从41.1%降到38.1%。

Claude系列同样说明了这一点。

Claude-4-sonnet在加入工具后步骤数从23.6降到19.2，但准确率从47.7%降到43.5%；Claude-4.5-sonnet的步骤数从23.3降到19.1，但准确率从61.9%降到48.4%。

这说明，混合动作空间真正难的不是有没有工具，而是**模型在GUI和Tool之间会不会选路**。

# 第一阶段：数据合成与Tool-Bootstrapped RFT

要让模型学会GUI-Tool path selection，首先需要高质量的interleaved GUI-Tool trajectories。但现实中，这类数据非常稀缺。

真实工具接口往往应用相关、覆盖不完整，而且维护成本高；而收集真实GUI-Tool混合轨迹又需要复杂的环境接入和人工标注。

已有GUI数据虽然规模很大，但大多是GUI-only trajectories，只教模型如何点击和输入，并没有告诉模型何时应该用工具替代冗长GUI操作。

ToolCUA的第一步，就是把这些GUI-only数据盘活，并顺势完成第一阶段的hybrid bootstrapping。

论文提出**Interleaved GUI-Tool Trajectory Scaling Pipeline**：从已有GUI轨迹出发，利用MLLM合成grounded tool library，再将GUI-only trajectories转换成interleaved GUI-Tool trajectories。

整个pipeline可以概括为三个步骤:

**1、Trajectory-aware synthetic tool library construction。**

对每条GUI轨迹，模型会分析任务目标、动作序列和截图描述，从真实操作流程中抽象出可调用的工具。

例如从Chrome设置流程中抽象出chrome_open_language_settings，从LibreOffice表格操作中抽象出读取工作簿信息、创建透视表等工具。

这些工具不是凭空生成的API模板，而是grounded in concrete trajectory behavior，也就是从真实GUI行为中抽象出来的工具能力。

**2、Tool trajectory generation with next-state grounding。**

给定合成工具库和原始GUI轨迹，MLLM生成一个功能等价的tool-only trajectory，并为每一步预测tool response。

随后通过next-state grounding，将工具执行效果锚定到原始GUI轨迹中的下一帧截图，验证工具步骤和可见状态变化是否一致。

**3、Interleaved GUI-Tool trajectory generation。**

最后，系统不会简单地把所有GUI操作都替换成工具，而是随机采样部分工具调用，再替换回对应GUI子序列，形成多种GUI与Tool交错的轨迹。

这个设计非常关键：它让模型看到不同tool availability下的决策边界，也自然产生GUI -> Tool和Tool -> GUI的critical switching steps。

最终，ToolCUA的数据中大约包括了4k个unique tools，覆盖fine-grained、mid-grained、coarse-grained多级粒度，大约有180k steps数据用于warmup SFT，还从critical steps中sample出5k条用于single-turn RL。

基于这些数据，ToolCUA进一步执行**Tool-Bootstrapped GUI RFT**。这一阶段的目标，不是直接学完整长程策略，而是先给模型打下一个可用的hybrid foundation。

具体来说，ToolCUA先在D_all上进行warmup SFT，学习多模态工具调用知识，包括工具用途、参数、返回结果，以及工具执行后的状态变化。