---
title: 至知研究院提出大模型可解释性新路线：拆权重，数据成本不到1%
source: https://www.qbitai.com/2026/08/473876.html
author:
- '[[思邈]]'
published: '2026-08-15'
created: '2026-08-15'
manifest_dates:
- '2026-08-15'
- '2026-08-16'
- '2026-08-17'
description: 理解大模型，无需再训练一个替代网络
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: d1d15223560c76d2
source_type: news_media
tldr: 至知创新研究院联合Safe AI Forum、牛津大学、斯坦福大学、清华大学提出稀疏权重分解（SWD），直接从预训练权重中拆出可独立干预的瓶颈单元，无需再训练替代网络；数据成本不到Transcoder等训练型基线的1%，已扩展到Qwen3.5-27B，并覆盖GPT-2
  Small全部48个权重矩阵。
objective_summary: 至知创新研究院（IQuest Research）联合Safe AI Forum、牛津大学、斯坦福大学、清华大学提出稀疏权重分解（SWD）方法，将稠密权重矩阵W分解为两个稀疏矩阵A和B，使共享的中间维度成为可独立评分、选择和消融的瓶颈单元，从而直接从预训练权重中抽取任务回路。在GPT-2
  Small单矩阵实验中，SWD仅用数千校准token即达到匹配替换保真度，数据成本不到Transcoder等训练型基线的1%；方法还扩展到Qwen2.5与Qwen3.5-27B，覆盖GPT-2
  Small全部48个注意力与MLP权重矩阵，并提供无需校准文本的zero-data版本。相关论文已发布在arXiv（编号2608.03913）。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies:
  - IQuest Research
  - Safe AI Forum
  - Oxford University
  - Stanford University
  - Tsinghua University
  technologies:
  - SWD
  - SVD
  - Transcoder
  - VPD-Recon-CI
  - NaNA
  key_people: []
key_logic_flow:
- 大模型机制可解释性研究中，理解已训练模型通常需要先训练一套新的稀疏表示，如Transcoder和稀疏特征模块，这带来额外训练成本，且替代模块未充分复现时会产生替换误差。
- 至知创新研究院联合多家机构提出稀疏权重分解（SWD），将稠密权重矩阵W分解为两个稀疏矩阵A和B，共享的m个中间维度构成可独立评分、选择和消融的rank-one瓶颈单元。
- 与SVD对比时，SVD成分的读写方向通常稠密，即使少量成分也可能连接几乎所有维度；SWD对每个单元的读写连接施加稀疏性，使少量单元对应少量活跃连接。
- 在GPT-2 Small第8层单矩阵实验中，SWD仅使用数千校准token即进入低CE误差区间，而Transcoder和VPD-Recon-CI等训练型基线需要约10^6量级token，SWD数据成本不到训练型基线的1%。
- SWD在GPT-2、Qwen2.5和Qwen3.5-27B上完成任务回路实验，并覆盖GPT-2 Small全部48个注意力与MLP权重矩阵，同时提供完全不需要校准文本的zero-data版本。
- 任务回路需同时满足充分性（只保留选中单元时任务仍存在）和必要性（移除时任务表现下降），SWD以更少的瓶颈单元和活跃连接达到相同目标。
object_mentions:
- object_type: paper
  name: Sparse Weight Decomposition
  canonical_name: Sparse Weight Decomposition (SWD)
  url: https://arxiv.org/abs/2608.03913
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 至知创新研究院联合Safe AI Forum、牛津大学、斯坦福大学和清华大学提出稀疏权重分解（SWD），直接从预训练权重中分解出可独立评分、选择和消融的瓶颈单元。
  - 论文显示，在达到匹配替换保真度时SWD使用的校准数据不到Transcoder等训练型基线的1%，并已扩展到Qwen3.5-27B模型。
  - SWD覆盖了GPT-2 Small全部48个注意力和MLP权重矩阵，并提供了完全不需要校准文本的zero-data版本。
  article_id: d1d15223560c76d2
- object_type: paper
  name: Transcoder
  canonical_name: Transcoder
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Transcoder和稀疏特征模块等方法会学习一组新的内部单元，用它们近似原模型某一层或某个模块的计算，再通过保留或移除这些单元寻找任务回路。
  article_id: d1d15223560c76d2
- object_type: paper
  name: VPD-Recon-CI
  canonical_name: VPD-Recon-CI
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 在GPT-2 Small第8层单矩阵实验中，Transcoder和VPD-Recon-CI等训练型基线需要约10^6量级的optimizer-replay
    token才能接近SWD的替换质量。
  article_id: d1d15223560c76d2
- object_type: paper
  name: NaNA
  canonical_name: NaNA
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 近期的NaNA等方法已经证明SVD成分可以用于任务排序和干预，但SVD成分的读方向和写方向通常都是稠密的。
  article_id: d1d15223560c76d2
- object_type: model
  name: GPT-2 Small
  canonical_name: GPT-2 Small
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - SWD在GPT-2 Small第8层mlp.c_proj单矩阵实验中仅使用数千个校准token就进入低CE误差区间，并在留出文本上验证了替换保真度。
  article_id: d1d15223560c76d2
- object_type: model
  name: Qwen3.5-27B
  canonical_name: Qwen3.5-27B
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - SWD方法在GPT-2、Qwen2.5和Qwen3.5-27B的任务回路实验中，通常以更少的瓶颈单元和活跃连接达到相同的充分性、必要性目标。
  article_id: d1d15223560c76d2
extract_result: success
impact_score:
  score: 7.0
  reason: 该论文把机制可解释性研究中'先训练一个替代网络'的固有范式转变为'直接从预训练权重做稀疏分解'，单矩阵替换数据成本比 Transcoder 等训练型基线低两个数量级，且已扩展到
    27B 量级模型并覆盖 GPT-2 Small 全部 48 个权重矩阵，短期内有望重塑该子领域的标准工作流、动摇训练型替代表示的地位；但认识论状态仍属理论主张、尚未被独立复现，影响范围主要集中在可解释性/AI
    安全子领域，对整体 AI 产业格局的短期冲击相对有限，故评分为 7.0。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 无需训练替代网络即可直接拆解权重做回路归因与消融，数据成本骤降两个数量级
hype_assessment:
  level: medium
  reason: 文章使用了'更直接的路线''真正的关键'等带有 PR 色彩的措辞，且'数据成本不到1%'这一口号式表述建立在单矩阵替换保真度的限定实验场景上，容易让读者高估其普适性；但论文本身提供了跨
    GPT-2/Qwen2.5/Qwen3.5-27B 的多尺度实验、与 full-rank SVD/Random-B 的对照消融及 zero-data 变体，干货充足，判定为存在一定包装而非严重概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 提出稀疏权重分解（SWD），将稠密权重矩阵 W 分解为两个稀疏因子 A、B，共享中间维度构成具备稀疏读写结构的 rank-one
    瓶颈单元，从而绕过训练替代网络，直接从预训练权重抽取、排序和消融任务回路；相比 SVD，SWD 在保持替换保真度的同时大幅压缩活跃连接，以数千校准 token
    达到训练型基线 10^6 token 的替换质量，并提供零校准数据的变体，是回路级可解释性在构造成本上的本质突破。
  business_model: 回路分析构造成本的量级下降有望推动 AI 安全审计、模型合规评估与内部风险治理等服务的规模化落地，SWD 可作为可解释性工具链（SaaS/API
    形态）的低成本底层引擎，降低对大规模校准数据与训练算力的依赖；但当前仍是学术方法，直接商业化路径尚不清晰。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 机制可解释性是 AI 安全、治理与监管的长期刚需，随模型规模扩大和政策收紧只会持续增值，SWD 具备复利效应的底层逻辑。其核心优势——无需训练替代网络、数据成本不到训练型基线的1%、提供zero-data版本——大幅降低了可解释性分析的门槛，使其具备从学术方法走向规模化部署工具的前提，这是成为细分赛道基础设施的关键条件。且方法已从GPT-2扩展到Qwen3.5-27B，覆盖全部48个权重矩阵，证明了一定的跨规模泛化能力。但作为VC需审慎：其一，当前仍处学术验证阶段（arXiv论文），距离被主流模型厂商采纳为生产级治理工具还有明显距离；其二，可解释性赛道竞争拥挤（SVD类方法如NaNA、SAE、Transcoder等训练型路线并行演进），SWD需在更多模型架构和真实干预场景下证明稳定优势才能胜出；其三，可解释性工具的商业变现路径在全球范围内尚未跑通，多为大模型厂商内部安全投入而非独立收入。综合判断：有潜力成为机制可解释性细分赛道的基础设施，但需1-2年持续验证社区采纳与跨模型推广，暂给6分而非更高。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- IQuest Research
- Safe AI Forum
- Anthropic
- Qwen（阿里巴巴）
- AI安全与治理研究社区
competitive_casualty:
- Transcoder等训练型可解释性方法
- VPD-Recon-CI等训练型替代表示方案
- 依赖大规模训练算力的可解释性服务商
market_opportunities:
- 创业者可基于SWD将大模型可解释性从'高成本研究项目'转变为'低成本服务'，面向金融、医疗、司法等强监管行业提供模型审计、行为归因与合规报告工具
- SWD的zero-data版本可催生'模型手术'类产品，帮助团队在不拥有训练数据的情况下对开源大模型进行回路级诊断、缺陷定位与定向行为修复
- AI安全厂商可将SWD的回路抽取与消融能力集成到红队评测与治理平台，快速定位幻觉、偏见等高风险回路，提升安全审计的自动化程度
risk_matrix:
  regulatory: 可解释性技术具有双重用途：既能支撑合规审计，也可能被用于定位并移除模型的安全护栏回路；涉及中国研究机构与美英高校的国际合作，可能受到出口管制与学术交流政策波动的影响
  technological: SWD目前仅为arXiv预印本和理论主张，未经同行评审与大规模独立复现；若在更大模型或更多任务上无法稳定复现，或出现更优的分解/稀疏化方法，该路线可能被快速替代
  competitive: Anthropic、DeepMind、OpenAI等实验室在机制可解释性上投入巨大，开源社区可能迅速复刻并超越SWD，单纯方法本身难以构成持久竞争壁垒
  ethical: 回路定位技术若被滥用于定向移除价值对齐、安全拒绝等关键回路，可能放大模型滥用风险；对稀疏单元的不当干预还可能产生未预期的行为副作用
  additional:
  - 论文声称覆盖Qwen3.5-27B与GPT-2 Small全部48个权重矩阵，需独立验证复现，存在结果被后续研究挑战的可能性
  - 地缘政治紧张可能影响该中美国际合作项目的后续开源与研究进程
confidence:
  impact: medium
  compound: low
  hype: medium
actionable_insight: deep_dive
---

# 至知研究院提出大模型可解释性新路线：拆权重，数据成本不到1%

理解大模型，无需再训练一个替代网络

允中 发自 凹非寺

量子位 | 公众号 QbitAI


大模型机制可解释性研究中，一直存在一个颇具反差的现实：

为了理解一个已经训练好的模型，研究者往往需要先训练一套新的稀疏表示。

Transcoder、稀疏特征模块等方法会学习一组新的内部单元，用它们近似原模型某一层或某个模块的计算，再通过保留、移除这些单元来寻找任务回路。

这些方法推动了机制可解释性的发展，但也带来了一笔不小的额外成本：新的表示需要数据和优化，而且一旦替代模块没有充分复现原模块，后续分析就可能同时解释模型行为和替换误差。

说白了，研究者想打开一个黑箱，却往往需要先训练另一个“小黑箱”。

问题不只是训练成本。任务回路需要找到一组可以独立干预的内部计算：只保留它们时，相关能力仍然存在；移除它们时，模型表现则明显下降。

训练型替代表示通常还需要针对不同模型、层和checkpoint分别拟合，使大规模、系统性的回路分析更加困难。

更理想的方案应当同时保持原模型行为、提供可独立干预的单元，并能以较低成本直接从已有权重中构造。

**至知创新研究院（IQuest Research）**与Safe AI Forum、牛津大学、斯坦福大学、清华大学的合作者提出了一条更直接的路线：

不再训练一套独立替代网络，而是从现有的预训练权重中直接“拆”出可干预单元。


这套方法名为**稀疏权重分解**（Sparse Weight Decomposition，SWD）。

它把一个稠密权重矩阵分解为两个稀疏矩阵，并把二者共享的中间维度变成可独立评分、选择和消融的瓶颈单元。研究者由此可以直接在权重上抽取任务回路。

论文给出了三个值得关注的结果：

- 在匹配替换保真度的单矩阵实验中，SWD使用的数据
**不到Transcoder等训练型基线的1%**； - 在GPT-2、Qwen2.5和Qwen3.5-27B的任务回路实验中，SWD通常以
**更少的瓶颈单元和活跃连接**达到相同的充分性、必要性目标； - 方法不仅扩展到了27B模型，还覆盖了GPT-2 Small
**全部48个注意力和MLP权重矩阵**，并提供了完全不需要校准文本的zero-data版本。

论文地址：https://arxiv.org/abs/2608.03913

△SWD方法概览。预训练模型中的稠密权重被分解为两个稀疏因子，共享中间坐标成为可独立评分、选择和消融的瓶颈单元。

# 直接把权重拆成“稀疏路径”

SWD的出发点很简单。对于预训练模型中的稠密权重矩阵W，寻找两个稀疏矩阵A和B，使得

W ≈ AB.

A和B共享m个中间维度。第i个维度先通过A的第i列从输入中读出一个标量，再通过B的第i行写向输出。这样，一列和一行共同构成一个瓶颈单元，也就是一条固定的rank-one读写路径。

可以把它想象成在一张极其密集的交通网络中增加一组“中转站”：每个中转站只连接少量入口和出口。研究者不仅可以看到一条路径从哪里读取、向哪里写入，还可以单独关闭它，观察模型行为会发生什么变化。

真正困难的地方，是如何在大幅减少连接的同时，仍然保留原权重对模型激活的作用。SWD使用少量校准文本产生的层输入，优化分解前后的输出误差；求解过程中交替更新两个因子，通过硬阈值维持非零预算，并重新拟合保留下来的权重值。分解完成后，每个瓶颈单元都可以像稀疏特征一样参与任务归因、排序和消融，但不需要再训练一套独立的神经替代网络。

△从权重分解到回路抽取。Step 1将稠密权重分解为稀疏因子A和B；Step 2按任务归因对瓶颈单元排序，并选择top-k单元组成任务回路。

# 已经有SVD，为什么还需要SWD？

既然目标是直接分解权重，一个自然的问题是：为什么不直接使用奇异值分解（SVD）？

SVD同样可以把一个矩阵表示为rank-one成分之和，而且不需要训练数据。近期的NaNA等方法也已经证明，SVD成分可以用于任务排序和干预。

但对回路分析而言，单元数量少，并不等于真正的计算路径少。

SVD成分的读方向和写方向通常都是稠密的。即使只保留少数成分，它们仍可能连接几乎所有输入和输出维度。

SWD则进一步把稀疏性施加到每个单元的读写连接上：少量单元对应的同时也是少量活跃连接。

团队还构造了两个能够精确还原GPT-2原权重的稠密对照：保留全部奇异值的full-rank SVD，以及采用随机正交基的Random-B。

两种分解都能在数值上精确复现原矩阵，但在相同的归因和消融流程下，需要更多活跃连接才能达到与SWD相同的任务表现。

这组对照表明，SWD的优势并不是“把矩阵拆开”这么简单，真正关键的是**每个单元都具有稀疏的读写结构**。

△精确稠密分解对照。Full-rank SVD与Random-B都能精确还原原始权重，但需要比SWD更多的活跃连接才能达到相同的充分性或必要性要求。

# 不到1%的数据，仍能保持模型行为

在进行回路分析之前，首先要回答一个更基础的问题：把原权重换成稀疏分解之后，模型还是原来的模型吗？

团队使用留出文本上的交叉熵差值（CE delta）衡量替换保真度，并使用KL散度和替换位置的激活重构误差进行补充验证。CE delta越接近0，说明替换后的模型行为越接近原模型。

在GPT-2 Small第8层mlp.c_proj的单矩阵实验中，SWD仅使用数千个校准token就进入低CE误差区间；Transcoder和VPD-Recon-CI等训练型基线则需要约10⁶量级的optimizer-replay token才能接近这一替换质量。同样的趋势随后出现在Qwen2.5-0.5B、1.5B、3B和Qwen3.5-27B上。

综合论文中的单矩阵比较，在达到匹配替换保真度时，SWD使用的数据不到训练型替代表示的1%。这意味着，大量数据和长时间替代模块训练并不是获得高保真回路单元的必要前提。直接从预训练权重出发，也可以构造足够忠实的干预表面。

△GPT-2 Small单矩阵替换的CE delta随数据量变化。SWD使用少量校准数据即可进入低误差区域。

△Qwen2.5-3B与Qwen3.5-27B的单矩阵替换结果。横轴为构造替换表示所使用的token数，纵轴为相对稠密模型的CE delta。

# 保留时能支撑任务，移除时会破坏任务

高保真地复现原矩阵，只能说明所有瓶颈单元合在一起能够工作。真正的回路还必须满足两个更严格的条件：只保留少量选中单元时，任务行为仍然存在；只移除这些单元时，任务表现会明显下降。前者是充分性测试，后者是必要性测试。

团队先在独立训练划分上使用一阶任务归因对候选单元排序，再构造从top-1、top-2到top-k的嵌套回路，并在留出测试集上评估。回路成本同时使用两种口径衡量：选中了多少瓶颈单元，以及这些单元实际包含多少非零读写连接。