---
title: 一次吃下一本书！百度开源新OCR，作者疑似前DeepSeek研究员
source: https://www.qbitai.com/2026/06/439464.html
author:
- '[[林樾]]'
published: '2026-06-28'
created: '2026-06-28'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: bd27b18eda87ca56
source_type: news_media
tldr: 百度开源Unlimited OCR模型，通过参考滑动窗口注意力实现长文档连续解析，在OmniDocBench上刷新SOTA
objective_summary: 百度于2026年6月开源了Unlimited OCR模型，其核心创新是参考滑动窗口注意力（R-SWA）机制，将视觉Token固定保留、输出Token仅保留最近128个历史窗口，KV
  Cache大小恒定。在OmniDocBench v1.5上以93.23%超越DeepSeek OCR 6.
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - 百度
  - DeepSeek
  - 智谱
  technologies:
  - Unlimited OCR
  - R-SWA
  - OCR
  - KV Cache
  - OmniDocBench
  key_people: []
key_logic_flow:
- 传统OCR处理长文档采用逐页处理+结果拼接的for-loop方案，KV Cache随文档长度膨胀，限制生成长度和推理速度。
- Unlimited OCR提出参考滑动窗口注意力（R-SWA），视觉Token作为固定参考信息始终保留，输出端只保留最近128个历史Token参与注意力计算。
- R-SWA将KV Cache设计为固定长度队列，每生成新Token移出最旧状态、补入新状态，显存占用和计算开销不随文档长度增长。
- 在OmniDocBench v1.5上Unlimited OCR综合得分93.23%，较DeepSeek OCR提升6.22%；v1.6版本进一步达到93.92%，刷新SOTA。
- 模型一次性输入40页以上内容仍保持稳定，Distinct-35指标达96.90%，编辑距离始终低于0.1069。
- 生成6000个Token时推理速度较DeepSeek OCR提升约35%，延迟基本保持稳定，未出现长文档场景下的延迟飙升。
extract_result: success
impact_score:
  score: 6.5
  reason: 该事件的技术核心R-SWA机制在KV Cache管理上确有原创性，解决了长文档OCR场景下的显存膨胀和延迟飙升问题，OmniDocBench上6.22%的提升证明了其有效性。但OCR本身是成熟领域，R-SWA的核心理念（固定窗口+分离参考Token）在LLM长上下文优化中已有类似思路（如StreamingLLM的窗口+KVCache回收），并非范式级突破。加之百度在AI开源社区的影响力有限，此次发布短期内会冲击OCR细分赛道格局（尤其与DeepSeek
    OCR的直接竞争），但难以改变AI行业整体走向。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: R-SWA机制在真实长文档（40页以上）场景中是否能稳定复现benchmark水准
hype_assessment:
  level: medium
  reason: 标题'一次吃下一本书'属于典型的PR包装语言，具有夸张色彩；但技术正文提供了完整的benchmark数据和消融实验，R-SWA的创新性确实存在。文章多次强调'SOTA'和'超越DeepSeek
    OCR'，有对标营销意图，不过整体而言技术干货多于概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 参考滑动窗口注意力（R-SWA）将视觉Token作为固定参考信息始终保留在注意力计算中，输出端仅保留最近128个历史Token，KV
    Cache设计为固定长度队列，实现显存和计算开销不随文档长度增长。这一设计突破了传统OCR逐页拼接的for-loop架构，在长文档连续解析场景下推理速度提升约35%，编辑距离稳定低于0.1069。
  business_model: 百度选择开源策略，在OCR成为AI数据入口（PDF/合同/票据→Token）的关键节点抢占生态位。与DeepSeek、智谱的国产OCR竞逐将加速'逐页拼接'方案的淘汰，推动RAG和文档智能等下游应用体验质变。但需关注开源协议的商业友好度，若附加限制则生态影响力会受限。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: Unlimited OCR 的核心创新 R-SWA（参考滑动窗口注意力）从根本上解决了长文档 OCR 的 KV Cache 膨胀问题，将显存和计算开销从随文档长度线性增长变为恒定，这是一个架构级的突破。OCR
    正在从传统工具转变为 AI 数据入口层——企业海量非结构化数据（PDF、合同、票据、报告）需要从像素转化为模型可理解的 Token，而该模型填补了'一次性连续阅读数十页文档'的关键空白。R-SWA
    作为一种'用遗忘替代扩容'的反共识长上下文思路，其思想可能溢出到更广泛的模型架构设计中，影响面不限于 OCR。开源策略加速了社区采用和生态反馈循环。但扣分项在于：Baidu
    在开源社区的长期投入力度和品牌信任度有待验证；R-SWA 能否被更广泛模型生态采纳尚不确定；OCR 赛道竞争激烈（DeepSeek、智谱、Google 等均在重兵投入）。综合来看，有潜力成为文档解析层的基础设施，但需要看到后续迭代和社区采用数据才能判定为长期基石。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- Baidu
- DeepSeek
- AI Agent 框架（LangChain、CrewAI）
- 企业文档处理平台
- 开源 AI 社区
competitive_casualty:
- 传统商业 OCR API 供应商（ABBYY、Google Cloud Vision OCR）
- 基于逐页拼接的 for-loop 文档解析方案
- 闭源文档理解中间件厂商
market_opportunities:
- 企业可将Unlimited OCR集成到文档数字化流水线中，实现合同、票据、书籍等长文档的一键式连续解析，大幅降低人工录入成本
- R-SWA注意力机制的恒定KV Cache设计可迁移至其他长上下文场景（如代码生成、对话系统），创业团队可基于该思路开发领域专用高效推理框架
- 基于开源版本进行垂直行业微调（医疗病历、法律卷宗、金融报表），提供高精度文档结构化SaaS服务，抢占非结构化数据向LLM可消费数据转化的市场红利
risk_matrix:
  regulatory: 处理企业内部长文档（合同、票据、个人数据）时，存在数据跨境传输、隐私合规（如《个人信息保护法》）风险；开源模型若被用于批量扫描敏感文档，可能引发监管审查
  technological: R-SWA固定128历史窗口的设计在极端超长文档或高度格式嵌套的场景下可能存在信息遗忘，学术界可能提出更优的注意力压缩方案导致架构迭代风险
  competitive: OCR赛道群雄逐鹿——DeepSeek OCR、智谱GLM-OCR均持续迭代，且各巨头均将OCR视为数据入口战略性布局，价格战和功能同质化竞争激烈
  ethical: 长文档连续OCR能力可能被大规模用于盗版书籍数字化、未经授权的文档采集和商业机密批量提取，加剧版权侵权和数据隐私风险
  additional:
  - 百度开源策略可能怀有生态锁定意图，下游开发者若深度依赖其模型框架，未来面临许可证变更或能力供货限制的供应链风险
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
---

# 一次吃下一本书！百度开源新OCR，作者疑似前DeepSeek研究员

henry 发自 凹非寺

量子位 | 公众号 QbitAI


国产开源OCR又刷新SOTA！

刚刚，百度开源了全新的OCR新模型——

**Unlimited OCR**。

它主打一口气读完几十页长文档，并在OmniDocBench上刷新SOTA，整体成绩超过此前的DeepSeek OCR。

与传统OCR处理长文档时“一页一页读，再把结果拼回去”的思路不同，Unlimited OCR这次模仿了一种酷似人类抄录员的工作方式：

**不再死记硬背前面已经抄写过的内容，而是只保留当前工作需要的信息和进度。**

基于此，它能够像人一样连续阅读整本书，而不是每读完一页就中断一次，再从下一页重新开始。


更关键的是，借助创新的**参考滑动窗口注意力（Reference Sliding Window Attention，R-SWA）机制**，即便文档越来越长，显存占用和注意力计算开销也几乎不会增长。

这是怎么做到的？

# for-loop只是权宜之计

要理解Unlimited OCR，得先看看传统OCR是怎么处理超长文档的。

简单来说，OCR（光学字符识别，Optical Character Recognition）就是把图片里的内容读出来，再转换成Markdown等可编辑文本。

在以往的处理流程中，一张图片会先进入编码器，被压缩成一串视觉Token；随后解码器再一个字一个字地把内容写出来。

但问题在于，传统的OCR解码器每生成一个新Token，它都需要回头查看之前生成过的所有Token，再决定下一个字该写什么。

于是文档越长，需要回看的历史内容就越多，KV Cache持续膨胀，显存占用和注意力计算开销也随之增长。

最终，无论是生成长度还是推理速度，都会受到限制。

这也是为什么现有OCR系统很难一次读完几十页文档，通常只能采用“逐页处理+结果拼接”的方案：

**每读完一页就重置上下文，最后再由外部程序把结果重新拼起来。**

这种for-loop式方案虽然工程上可行，但本质上只是权宜之计，而Unlimited OCR想解决的，正是进一步扩展模型在长程任务的表现。

# 参考滑动窗口注意力

那，问题来了。

如果不采用逐页重置的for-loop方案，模型又该如何在保持连续阅读状态的同时，避免KV Cache随着文档长度无限增长？

Unlimited OCR给出的答案，不是让模型记住更多东西，而是让模型学会像人一样“遗忘”。而这，就引出了它最核心的创新点：**参考滑动窗口注意力**。

为了说明参考滑动窗口注意力的设计思路，研究举了一个非常形象的例子：**人类阅读长文档其实并不是全量回溯的。**

比如抄写一本书时，你不会每写一个字都重新翻阅前面几十页。

你只会保留当前的阅读状态，以及刚刚写下的一小段内容，用来确认没有跳行、没有漏字，更久远的信息则会逐渐淡出记忆。

论文将这种机制称为**软遗忘（Soft Forgetting）**。

受此启发，参考滑动窗口注意力应运而生。

对于每个待生成Token，模型始终关注全部参考Token（Reference Tokens），也就是视觉Token和提示词；

与此同时，在输出端只保留最近n个历史Token（默认128个）参与注意力计算。

就好像你抄书的时候，原书始终摊开在桌面上，可以随时查看完整内容；

而手边只保留最近写下的几行字，用来追踪当前进度。更早的内容则自然淡出工作记忆。

这样一来，模型既能持续看到完整图像，又能依靠局部历史信息判断当前解析到了哪里。

此外，这里值得一提的，还有Unlimited OCR的KV Cache管理方式。

论文将KV Cache设计成一个固定长度的队列。每生成一个新Token，最旧的一部分状态就会自动移出，新状态再补进来。

因此，无论最终生成几千还是几万个Token，KV Cache规模始终保持恒定，显存占用和计算成本都不会继续增长。

这也是R-SWA与其他注意力机制最大的区别。

相比全注意力（Full Attention），后者的KV Cache会随着解码不断膨胀，而R-SWA始终保持固定大小。

相比传统滑动窗口注意力（SWA），后者会把视觉Token和文本Token一起放进窗口，随着窗口不断滑动，早期视觉信息会逐渐被挤出；解码越长，对原图的感知就越模糊。

而R-SWA则将视觉Token单独保留下来，让它们始终作为参考信息存在，不参与滑动窗口更新。

换句话说，图像始终保持清晰，发生滑动的只有输出文本本身。

正如上图所示，传统OCR越读越慢，而Unlimited OCR基本保持匀速运行，这正是R-SWA的价值所在。

# 实验验证

在实验部分，研究团队采用OmniDocBench v1.5和v1.6评估模型的文档解析能力，并额外构建了覆盖2页至40页以上文档的内部测试集，专门考察其长文档连续解析能力。

首先来看整体成绩。

在OmniDocBench v1.5上，Unlimited OCR取得了**93.23%**的综合得分，相比DeepSeek OCR提升**6.22%**。

在最新的v1.6版本中，成绩进一步达到93.92%，刷新当前SOTA。

在**长文本解析能力**方面，即便一次性输入40页以上内容，模型依然能够保持稳定表现。

其Distinct-35指标达到96.90%，编辑距离（Edit Distance）始终维持在0.1069以下。

换句话说，随着文档长度不断增长，模型并没有出现明显的内容混淆或解析质量下降。

除了精度提升，Unlimited OCR还带来了**推理效率**上的改善。

由于采用恒定KV Cache设计，模型的推理开销不会随着文档页数持续增长。在生成6000个Token时，其推理速度（TPS）相比DeepSeek OCR提升约35%。

与此同时，调用延迟（Latency）基本保持稳定，没有出现长文档场景下常见的延迟飙升现象。

# OCR之外：一种新的长上下文思路

如果把最近一年的时间线串起来看，会发现一个有意思的现象：越来越多头部模型公司开始重兵投入OCR。

从DeepSeek年初发布OCR2，到智谱开源GLM-OCR，再到百度这次推出Unlimited OCR，大家争夺的早已不是单纯的文字识别能力，而是新的数据入口。

互联网数据正在被快速消耗，而企业真正有价值的数据，其实大量沉睡在PDF、合同、报告、票据和扫描件里。

对于人类来说，这些是信息，但对于模型来说，它们只是像素。

OCR的意义，就是把这些像素重新变成模型能够理解和推理的Token。因此，OCR正在从一个传统工具，逐渐变成AI时代最重要的数据入口之一。

不过，如果只把Unlimited OCR看成一个OCR模型，可能就低估了它。

过去两年，行业面对长上下文问题时，最主流的思路一直是扩容。

128K、1M、10M，上下文窗口不断变长；各种长上下文优化方案，本质上也都在思考如何让模型记住更多信息。

而R-SWA反着来。