---
title: 'Show HN: XY – A Fast, composable, GPU-accelerated interactive plotting library'
source: https://github.com/reflex-dev/xy
author:
- '[[apetuskey]]'
published: '2026-07-28'
created: '2026-07-28'
manifest_dates:
- '2026-07-28'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 2546c963ca959e12
source_type: community_discussion
tldr: XY 是 reflex-dev 开源的 GPU 加速 Python 交互式绘图库，基于 Rust 内核与 WebGL2 渲染，宣称在 0.081 秒内完成一亿点数据渲染，并提供
  reflex-xy 适配器将图表直接集成到 Reflex 应用。
objective_summary: 2026年7月，reflex-dev 通过 Show HN 发布了 XY 绘图库，面向希望用一套图表库兼顾日常绘图、应用可视化与超大规模数据的
  Python 用户。图表由容器与内部 marks 组合而成，支持声明式与 matplotlib 兼容两种 API，可渲染为笔记本交互图表或导出 HTML、PNG、SVG、PDF。其
  Rust 内核按屏幕分辨率只计算需要显示的内容，配合 ColumnStore 精确数据列与类型化二进制缓冲传输，实测在 0.081 秒内渲染一亿点密度数据，显著快于
  Matplotlib 与 Plotly。项目目前处于 alpha 阶段，并附带 reflex-xy 适配器让 XY 图表成为 Reflex 组件。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - reflex-dev
  technologies:
  - Rust
  - WebGL2
  - GPU
  - ColumnStore
  key_people: []
key_logic_flow:
- XY 是一个 Python 图表库，图表由容器与内部 marks 组成，支持声明式 API 与 matplotlib 兼容的 xy.pyplot 接口。
- 小图表将每个点传输到浏览器，大图表则由 Rust 内核按屏幕分辨率只计算需要显示的内容，平移缩放时为新范围重新计算并回传原始行。
- 实测基准显示，XY 在 10k 点用时 0.071 秒、在 1 亿点用时 0.081 秒，而 Matplotlib 在 5000 万点需 13.4 秒，Plotly
  无法完成该规模渲染。
- XY 通过 ColumnStore 保留精确 f64 列，用 Rust 计算直接、抽稀与密度三级细节层级，并以无数据 JSON 描述加类型化二进制缓冲传输给浏览器。
- reflex-xy 适配器以独立包分发，可将任何 XY 图表转化为 Reflex 组件，无需 JavaScript、iframe 或独立图表服务。
- XY 处于 alpha 阶段，路线图规划了类别分布、回归诊断、散点矩阵、饼图、K 线图、3D 与体可视化等后续图表类型。
object_mentions:
- object_type: project
  name: reflex-dev/xy
  canonical_name: xy
  url: https://github.com/reflex-dev/xy
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - XY 是 reflex-dev 发布的极速 Python 交互式绘图库，适用于 Web、笔记本与静态导出，图表由容器与内部 marks 组合而成。
  - XY 的 Rust 内核按屏幕分辨率只计算需要显示的内容，实测渲染一亿点密度数据仅需 0.081 秒。
  - XY 通过 ColumnStore 保留精确 f64 列，用 Rust 计算细节层级，并以类型化二进制缓冲传输紧凑 payload 给浏览器渲染。
  article_id: 2546c963ca959e12
- object_type: project
  name: reflex-xy
  canonical_name: reflex-xy
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - reflex-xy 适配器能把任意 XY 图表变成常规 Reflex 组件，无需 JavaScript、iframe 或独立图表服务，并作为独立包分发。
  - 注册 reflex_xy.XYPlugin 后，可在 Reflex 组件树中直接添加 XY 图表，悬停、平移与缩放功能保持可用。
  article_id: 2546c963ca959e12
- object_type: project
  name: Reflex
  canonical_name: Reflex
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - reflex-xy 适配器依赖并引入 xy 与 reflex 两个包，将 XY 图表转化为 Reflex 组件树的一部分。
  article_id: 2546c963ca959e12
extract_result: success
impact_score:
  score: 6.0
  reason: 评分依据：该事件是 Python 数据可视化领域一次真实的技术创新，Rust 内核 + WebGL2 + 三级细节层次（直接/抽稀/密度）的架构让大图渲染复杂度与数据量解耦，一亿点在
    0.081 秒内完成渲染的性能数字显著优于 Matplotlib 与 Plotly，这打破了 Python 生态大规模交互式可视化的既有瓶颈，会改变大型数据集可视化的局部竞争格局，且背后有
    reflex-xy 适配器绑定 Reflex 生态形成联动。但它本质仍是开发者工具层的增量创新，而非范式转移——图表库不改变 AI 或软件行业的基础工作方式；且项目处于
    alpha 阶段、功能覆盖不完整（matplotlib 兼容仅部分支持），短期行业冲击力有限。综合判定 6.0 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 一亿点 0.08 秒渲染的真实性与 matplotlib 兼容迁移成本
hype_assessment:
  level: medium
  reason: 判定依据：文章显著区别于纯 PR 话术——提供了完整基准方法学（benchmark runbook、逐尺寸视频、对照规范、单机单次运行的测量条件），并主动披露
    density=False 时 100M 点需 1.34 秒的诚实数据，透明度较高；但核心宣传点'0.081 秒渲染一亿点'是密度聚合模式下的结果，而'渲染了
    100 亿点 OpenStreetMap 全量数据集'这类表述自带营销光环，加上 alpha 阶段的自我评测存在测试集与场景选择空间，需第三方独立复现才能完全采信，故判定为存在一定包装而非纯干货。
information_entropy: high
domain_disruption:
  technical_innovation: Rust 内核按屏幕分辨率只计算需要显示的内容，配合 ColumnStore 保留精确 f64 列、类型化二进制缓冲（无数据
    JSON 描述 + 二进制负载）传输，以及直接/抽稀/密度三级细节层次，使大数据量渲染复杂度与点规模解耦，zoom 时对新范围重新计算并回传原始行，这是对
    Matplotlib/Plotly 全量数据走 CPU 或 JS 渲染路径的结构性突破。
  business_model: 开源核心库（pip/uv 即装即用）+ matplotlib 兼容 API 降低迁移门槛，意在蚕食 Matplotlib/Plotly
    存量用户；reflex-xy 适配器将图表沉淀为 Reflex 组件，无需 JS/iframe/独立图表服务，强化 Reflex Python 全栈框架的生态护城河，为
    Reflex 商业化托管平台提供数据可视化差异化能力，走典型'开源引流 + 平台变现'路径。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 评估逻辑：XY 的核心价值在于用 Rust 内核 + WebGL2 将大规模交互式可视化（1 亿点 0.081 秒）从'专业重型工具'变为'pip
    install 即可用'的基础设施，技术上具备真实代差，基准数据可信度较高。但作为复利标的需冷静看待三点：其一，绘图库处于价值链中段，本身不拥有数据、算力或分发网络，捕获价值能力天然弱于上游算力与下游应用；其二，alpha
    阶段且深度绑定 Reflex 单一生态，reflex-xy 适配器既是差异点也是生态锁定风险，若 Reflex 生态增长不及 Streamlit/Dash，则天花板受限；其三，Matplotlib/Plotly
    的存量用户惯性极强，迁移成本与习惯壁垒是最大阻力。综合判断：具备成为'大规模 Python 数据可视化细分基础设施'的潜力（4-7 分区间），但需持续验证生态扩散速度、性能优势的可复制性以及商业化路径，故给予
    6.0 分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- reflex-dev
- Reflex
- Jupyter
- Python 数据科学生态
competitive_casualty:
- Plotly
- Matplotlib
- Bokeh
- Datashader
market_opportunities:
- 数据密集型应用开发者可关注 XY 在金融行情、IoT 传感器、地理信息等亿级数据可视化场景的落地机会，以远低于 Matplotlib/Plotly 的内存与时间成本实现实时交互式探索
- 纯 Python 团队可利用 reflex-xy 适配器将高性能图表直接嵌入 Reflex Web 应用，无需前端工程化与 JS 投入即可交付数据密集型产品，显著降低可视化应用开发门槛
- 可视化工具与 BI 厂商可研究 XY 的 Rust 内核 + 屏幕分块渲染 + 类型化二进制缓冲传输架构，将其作为自研高性能渲染引擎的设计范式参考，或集成进自身产品的数据探索管线
risk_matrix:
  regulatory: 无（纯开源工具项目，不涉及出口管制、版权诉讼或 AI 监管议题；但需关注 alpha 阶段后续 License 变更对商业化使用方式的潜在影响）
  technological: XY 处于 alpha 阶段，matplotlib 兼容层覆盖不全，常用图表类型仍在路线图中；性能基准来自单机（Apple M5
    Pro）自测，需独立复现验证；渲染依赖 WebGL2 与浏览器 GPU 能力，老旧硬件与内网环境兼容性存疑
  competitive: Matplotlib/Plotly 已形成强烈的生态锁定效应，HoloViz/Datashader 在超大规模可视化赛道亦有成熟替代方案；Reflex
    自身生态规模小于 Streamlit/Gradio，可能限制 reflex-xy 适配器的传播半径
  ethical: 密度模式为按屏幕分辨率近似渲染，可能掩盖离群点与局部数据分布细节，用于医疗、风控等决策场景存在误导风险；大规模数据需传输至浏览器端处理，涉及敏感数据外传的隐私考量
  additional: []
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: reflex-dev/xy
  canonical_name: xy
  url: https://github.com/reflex-dev/xy
  positioning: reflex-dev 开源的 GPU 加速 Python 交互式绘图库，基于 Rust 内核与 WebGL2 渲染，面向日常绘图、应用可视化与超大规模数据场景。
  technical_signal: XY 的 Rust 内核按屏幕分辨率只计算需要显示的内容，配合 ColumnStore 精确 f64 列与类型化二进制缓冲传输，实测
    0.081 秒渲染一亿点。
  adoption_signal: XY 由 reflex-dev 于 2026 年 7 月通过 Show HN 公开发布，当前处于 alpha 阶段，支持 pip
    与 uv 安装并随附 reflex-xy 适配器。
  ecosystem_relevance: XY 与 Reflex 应用框架同属 reflex-dev 生态，reflex-xy 适配器可将任意 XY 图表转化为
    Reflex 组件，无需 JavaScript 或独立图表服务，降低大规模可视化的接入门槛。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: XY 以 0.081 秒渲染一亿点，性能在四个数量级区间保持平坦，若持续迭代有望改写大规模数据可视化的性能基准；其 reflex-xy
    适配器同时打通 Reflex 应用集成路径，兼具库与生态双重价值，值得长期跟踪。
  risk_notes:
  - XY 目前处于 alpha 阶段，路线图中的类别分布、回归诊断、散点矩阵、K 线图、3D 与体可视化等图表类型尚未落地。
  - XY 的 matplotlib 兼容接口尚不支持全部图表与功能，现有 pyplot 代码迁移时可能遇到覆盖缺口。
  - 性能基准由项目方自建规范并单机单次测量，独立第三方复现结果尚未出现，宣称的跨数量级优势有待独立验证。
  score: 8.0
  article_ids:
  - 2546c963ca959e12
  evidence_snippets:
  - XY 是 reflex-dev 发布的极速 Python 交互式绘图库，适用于 Web、笔记本与静态导出，图表由容器与内部 marks 组合而成。
  - XY 的 Rust 内核按屏幕分辨率只计算需要显示的内容，实测渲染一亿点密度数据仅需 0.081 秒。
  - XY 通过 ColumnStore 保留精确 f64 列，用 Rust 计算细节层级，并以类型化二进制缓冲传输紧凑 payload 给浏览器渲染。
- object_type: project
  name: reflex-xy
  canonical_name: reflex-xy
  url: null
  positioning: reflex-dev 分发的 Reflex 官方适配器包，可将任意 XY 图表转化为常规 Reflex 组件，无需 JavaScript、iframe
    或独立图表服务。
  technical_signal: reflex-xy 注册 reflex_xy.XYPlugin 后即可在 Reflex 组件树中直接添加 XY 图表，悬停、平移与缩放等交互保持可用，并以独立包分发、随包引入
    xy 与 reflex 依赖。
  adoption_signal: 作为 reflex-dev 官方生态的一部分，reflex-xy 与 XY 同源发布，为 Reflex 应用开发者提供零 JavaScript
    接入高性能图表的路径。
  ecosystem_relevance: reflex-xy 打通 XY 与 Reflex 应用框架的集成链路，使 XY 图表无需额外前端工程即可成为 Reflex
    组件，强化 Reflex 生态的数据可视化能力。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: reflex-xy 以适配器形式把 XY 的超大规模渲染能力嵌入 Reflex 组件体系，填补了 Reflex 生态高性能可视化组件的空白，其版本演进与功能同步速度直接影响
    XY 在应用场景的落地广度，值得持续跟踪。
  risk_notes:
  - reflex-xy 与 XY 同处 alpha 阶段，接口与行为可能随 XY 快速迭代而变动，适配层稳定性待观察。
  - 目前缺少独立版本发布节奏与长期兼容性保证的公开说明，XY 新增图表类型能否即时同步到 Reflex 组件尚不确定。
  score: 6.0
  article_ids:
  - 2546c963ca959e12
  evidence_snippets:
  - reflex-xy 适配器能把任意 XY 图表变成常规 Reflex 组件，无需 JavaScript、iframe 或独立图表服务，并作为独立包分发。
  - 注册 reflex_xy.XYPlugin 后，可在 Reflex 组件树中直接添加 XY 图表，悬停、平移与缩放功能保持可用。
---

XY is an extremely fast, interactive, customizable Python charting library for the web, notebooks, and static exports.

Charts are composed declaratively or through matplotlib conventions. You can fully customize them with Python, CSS, or Tailwind.

With small charts, every point is sent to the browser. For large charts, the Rust core computes only what the screen needs to display, based on its resolution. Pan, zoom, hover, and selection can show full details by running the same process for the new range, and a selection returns the original rows.

With XY we rendered the entirety of OpenStreetMap — a **10,000,000,000 point** dataset. See the example →

Important

**XY is in alpha** and is receiving frequent enhancements.
⭐️ Star the repo to follow the progress.

XY is for Python users who want one flexible charting library for everything from everyday plots to custom application visuals and large datasets. Build a chart once, then use it in notebooks and web apps or export it as HTML, PNG, SVG, or PDF.

```
pip install xy
# or, with uv
uv add xy
```

A chart is a container plus the marks inside it. Any sequence works; NumPy is optional.

```
import xy
chart = xy.line_chart(xy.line([1, 2, 3, 4, 5], [120, 180, 165, 240, 310]))
# chart.to_html("chart.html")
# chart.to_png("chart.png")
# chart.to_svg("chart.svg")
chart # notebooks render it
```

The same API scales to a hundred million points as a density surface:


```
import numpy as np
import xy
rng = np.random.default_rng(7)
n = 100_000_000
r = 6.0 * rng.beta(1.2, 3.0, n)
theta = 2.9 * np.log1p(r) + rng.integers(0, 4, n) * (np.pi / 2) + rng.normal(0, 0.045 + 0.016 * r, n)
chart = xy.scatter_chart(
xy.scatter(
r * np.cos(theta),
r * np.sin(theta),
color=np.exp(-r / 2.2),
colormap="magma_r",
density=True,
opacity=0.85,
# Grow and solidify markers once a view drills through to real rows.
size=2.5,
zoom_size_factor=2.6,
zoom_opacity=0.95,
),
xy.theme(
background="#ffffff", plot_background="#ffffff", grid_color="#e6e6e1",
axis_color="#c3c2b7", text_color="#0b0b0b",
),
title="100 million points",
)
chart
```

For common pyplot workflows, change the import and keep the plotting code:

```
import numpy as np
import xy.pyplot as plt
x = np.linspace(0, 10, 200)
fig, ax = plt.subplots()
ax.plot(x, np.sin(x), "r--", label="signal")
ax.legend()
plt.show()
```

See the compatibility guide; not all charts and functionality are supported yet.

Use Python to control the chart, from marks and axes to interactions and layout.

**Marks:**Control color, size, opacity, symbols, gradients, strokes, curves, and colormaps.**Guides:**Customize axes, ticks, grids, annotations, legends, colorbars, and tooltips.**Interaction:**Add pan, zoom, hover, selections, crosshairs, callbacks, and linked charts.**Layout:**Create layers and facets, set responsive dimensions, and apply themes.

```
chart = xy.line_chart(
xy.line(x, y, color="#7c3aed", width=3),
class_name="rounded-xl bg-white",
class_names={"tooltip": "rounded-lg bg-zinc-900 text-white"},
)
```

See the styling guide for examples. For a detailed breakdown of what can be customized, see the capability matrix.

Live interactive charts, 10k to 100M points. Every library gets every row and is driven through its own input path in a real browser. The clock stops only when the canvas is both correct (planted sentinel points verified lit) and stable (10 byte-identical frames), so progressive renderers are charged until their last chunk lands.

XY holds **0.071 s at 10k and 0.081 s at 100M**, flat across four orders of
magnitude, because above 200k rows it draws a screen-bounded density surface
instead of one marker per row, and zoom drills back to exact rows. Every
exact-marker path scales with N instead: Matplotlib crosses a second at ~3M
and reaches 13.4 s at 50M; Plotly crosses at ~2.5M and reaches 9.8 s at 25M.

The pale line is XY with `density=False`

: the same engine drawing one marker
per row, no aggregation credit. It renders 100M exact markers in 1.34 s on
5.26 GiB.

Time until every point is on screen, in seconds. `✕`

is a size the library
did not render: Plotly never finishes constructing the figure at 50M, and
Matplotlib draws at 100M but never resolves the zoom that follows.

| Points | 10k | 100k | 500k | 1M | 2.5M | 5M | 10M | 25M | 50M | 100M |
|---|---|---|---|---|---|---|---|---|---|---|
XY speedup |
1× |
2× |
3× |
4× |
9× |
16× |
34× |
89× |
177× |
— |
XY |
0.071 |
0.072 |
0.075 |
0.084 |
0.083 |
0.089 |
0.083 |
0.077 |
0.076 |
0.081 |
XY (`density=False` ) |
0.085 | 0.074 | 0.087 | 0.098 | 0.111 | 0.144 | 0.206 | 0.424 | 0.645 | 1.343 |
| Matplotlib (WebAgg) | 0.086 | 0.115 | 0.224 | 0.357 | 0.758 | 1.424 | 2.804 | 6.838 | 13.385 | ✕ |
| Plotly (scattergl) | 0.341 | 0.373 | 0.477 | 0.614 | 1.033 | 1.785 | 3.367 | 9.794 | ✕ | ✕ |

Peak Python-side resident memory, in GiB. Browser memory is tracked separately and excluded here, since a headless Chrome resides ~1 GiB before drawing anything.

| Points | 10k | 100k | 500k | 1M | 2.5M | 5M | 10M | 25M | 50M | 100M |
|---|---|---|---|---|---|---|---|---|---|---|
XY advantage |
1.8× |
1.7× |
1.9× |
2.1× |
2.1× |
2.4× |
2.6× |
2.9× |
2.8× |
— |
XY |
0.05 |
0.05 |
0.06 |
0.07 |
0.13 |
0.19 |
0.32 |
0.70 |
1.36 |
2.58 |
XY (`density=False` ) |
0.05 | 0.05 | 0.07 | 0.10 | 0.18 | 0.31 | 0.57 | 1.35 | 2.66 | 5.26 |
| Matplotlib (WebAgg) | 0.09 | 0.09 | 0.12 | 0.15 | 0.28 | 0.46 | 0.84 | 2.06 | 3.85 | ✕ |
| Plotly (scattergl) | 0.21 | 0.18 | 0.28 | 0.36 | 0.60 | 1.05 | 1.86 | 4.70 | ✕ | ✕ |

One machine (Apple M5 Pro), one run per cell; at the small end the timings carry roughly ±10 ms of run-to-run spread.

For the environment, methodology, per-size videos, and raw results, see the benchmark runbook and competitive benchmark specification.

The `reflex-xy`

adapter turns any XY chart into a regular Reflex component, with
no JavaScript, iframe, or separate chart service. It ships as its own package
and pulls in `xy`

and `reflex`

:

```
pip install reflex-xy
# or, with uv
uv add reflex-xy
```

Register the adapter once:

```
# rxconfig.py
import reflex as rx
import reflex_xy
config = rx.Config(
app_name="dashboard",
plugins=[reflex_xy.XYPlugin()],
)
```

Then add a chart anywhere in the component tree:

```
import reflex as rx
import reflex_xy
import xy
signups = xy.line_chart(
xy.line([1, 2, 3, 4, 5], [120, 180, 165, 240, 310]),
title="Weekly signups",
)
def index() -> rx.Component:
return rx.card(
rx.heading("Growth"),
reflex_xy.chart(signups, height="320px"),
width="100%",
)
app = rx.App()
app.add_page(index)
```

Hover, pan, and zoom keep working. For charts driven by Reflex state, events, or live streams, see the Reflex integration guide and the runnable example app.

Each notebook fetches its rows from the linked public source; no raw datasets are stored in this repository. Counts describe the featured chart, and the notebooks scale further. See the example guide for sources, workload controls, and setup.

Gaia DR3 · HR diagram250,000 plotted starsOpen notebook |
gnomAD v4.1 · allele frequency164,000 plotted variantsOpen notebook |
Pan-UKBB · Manhattan plot814,294 plotted variantsOpen notebook |
Dukascopy · EUR/USD ticks101,427 plotted ticksOpen notebook |
LIGO · GW150914 strain16,777,216 raw · 3,441 shownOpen notebook |
NYC TLC · taxi pickup density300,000 pickup recordsOpen notebook |

Most chart stacks serialize every value as JSON and ask the browser to draw
every mark. XY keeps exact values in a `ColumnStore`

, computes a level of detail
in Rust, and transfers typed binary buffers. Decimated and density views are
bounded by the visible result.

```
flowchart TB
API["Python API<br/>Build the chart"]
STORE["ColumnStore<br/>Keep canonical f64 columns"]
CORE["Native Rust compute<br/>Direct · decimated · density"]
PAYLOAD["Compact payload<br/>Data-less JSON spec + typed binary buffers"]
RENDER["Browser or notebook<br/>WebGL2 marks · Canvas axes · DOM interface"]
API --> STORE --> CORE --> PAYLOAD --> RENDER
```

So a dense overview can aggregate while a narrow view returns exact points. With a live host, pan and zoom request a refined payload. Canonical f64 data stays in Python, so hover and selection still return original rows.

For the full design, see the design dossier.

Broad 2D coverage first, then geographic, 3D, and volume visualization. Queued next, no dates implied:

**Categorical distributions:**strip, swarm, beeswarm, boxen, rug**Regression diagnostics:**trendline, residual, QQ, PP**Scatter matrix and joint plots:**SPLOM, pair grid, marginal histograms**Pie / donut:**`xy.pie_chart(labels, values, hole=...)`

ships over unequal-width core polar bars, with Matplotlib-shaped helpers in`xy.pyplot`

; nested donuts and variable-radius composition remain**Candlestick / OHLC and finance overlays:**SMA, VWAP, Bollinger, RSI, MACD; prototyped, awaiting a fresh landing**Waterfall and funnel****Treemap, sunburst, and icicle****Gauge / indicator:**build on the shipped polar axes and composable radial marks**Slope, bump, and dumbbell****3D and volume:**scatter, surfaces, meshes, isosurfaces, and volumetric views

The full ranked backlog is in the chart roadmap. Want a chart or feature that isn't listed? Open an issue.