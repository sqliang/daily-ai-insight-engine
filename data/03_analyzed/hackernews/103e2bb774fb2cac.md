---
title: Linux on the Atari Jaguar
source: https://cakehonolulu.github.io/linux-for-jaguar/
author:
- '[[cakehonolulu]]'
published: '2026-07-06'
created: '2026-07-07'
description: 'Article URL: https://cakehonolulu.github.io/linux-for-jaguar/ Comments
  URL: https://news.ycombinator.com/item?id=48808663 Points: 151 # Comments: 39'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 103e2bb774fb2cac
manifest_dates:
- '2026-07-07'
source_type: community_discussion
tldr: 开发者 cakehonolulu 成功将 Linux（基于 uClinux nommu 模式）移植到 1993 年发行的 Atari Jaguar 游戏主机上，解决了
  2MB 内存限制、编译器未对齐访问导致崩溃、FLAT 二进制格式及 rootfs 构建等关键技术难题，最终在 MAME 模拟器和实机上实现了内核启动。
objective_summary: cakehonolulu 在 Atari Jaguar 主机上完成了 Linux 内核的移植工作。该主机搭载 Motorola
  68000 处理器，仅有 2MB RAM 和最多 6MB ROM。移植利用 Linux 内核中已有的 m68k nommu（uClinux）支持，通过 XIP
  技术将内核只读段放在 ROM 中、动态段放在 RAM 中来解决内存不足问题。作者使用 buildroot 工具链构建了基于 uClibc 和 busybox 的
  FLAT 二进制用户空间，并编写了 Jerry DSP 串口驱动作为 early console。最终内核成功启动并打印启动日志，但因缺少 init 进程而崩溃。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Atari
  - Motorola
  technologies:
  - Linux
  - uClinux
  - m68k
  - FLAT
  - XIP
  - buildroot
  - busybox
  - uClibc
  - MAME
  key_people:
  - cakehonolulu
  - Kenneth Albanowski
  - D. Jeff Dionne
key_logic_flow:
- Atari Jaguar 主机于 1993 年发布，搭载 Motorola 68000 处理器，仅有 2MB RAM，定位为 64 位游戏主机但最终商业失败。
- Linux 内核内置了对 68000 系列处理器的支持，位于 arch/m68k/ 目录下，且 uClinux (nommu) 模式已合入主线，使得无 MMU
  的移植成为可能。
- 通过 XIP（原地执行）技术将内核只读段存储在 ROM（0x80000）中，动态段存储在 RAM（0x00000）中，解决了 2MB 内存无法容纳完整内核的问题。
- Ubuntu 仓库的 m68k-linux 交叉编译器会生成未对齐的内存访问指令，导致 68000 处理器崩溃，需使用从源码构建的 m68k-elf-gcc 替代。
- 因 ROM 映射在 0x80000 而非 0x00000，需要手动将向量表 memcpy 到 RAM 基地址，并在 Jaguar 平台代码中设置正确的 ROM
  起始地址。
- 用户空间使用 buildroot 构建基于 uClibc（malloc-simple 策略）和 busybox（FLAT 二进制格式）的 initramfs，由于内存限制仅运行
  sh shell 而非完整 applet 安装。
extract_result: success
object_mentions:
- object_type: project
  name: cakehonolulu/linux-for-jaguar
  canonical_name: linux-for-jaguar
  url: https://github.com/cakehonolulu/
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章详细记录了将 Linux 移植到 Atari Jaguar 主机的完整技术过程，包括内核配置、编译器问题、内存布局和用户空间构建。
  - 作者提供了修改后的 Linux 内核仓库地址：https://github.com/cakehonolulu/。
  - 内核在 MAME 模拟器上成功启动并输出了从 Linux 7.2.0-rc1 到内存节点初始化的完整启动日志。
  article_id: 103e2bb774fb2cac
- object_type: project
  name: buildroot
  canonical_name: buildroot
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 作者使用 buildroot 构建针对 68000 的交叉编译工具链，并在其中配置 uClibc 的内存分配策略为 malloc-simple。
  - buildroot 最近更新中已支持 m68k nommu 目标，这得益于 linuxmd 项目在 2026 年 5 月提交的补丁。
  article_id: 103e2bb774fb2cac
- object_type: project
  name: busybox
  canonical_name: busybox
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - busybox 是少数支持 nommu FLAT 二进制目标的项目之一，被用作 Jaguar Linux 的 initramfs 用户空间。
  - 由于内存限制，busybox 的 --install 模式会导致 OOM，因此仅以 sh shell 方式运行。
  article_id: 103e2bb774fb2cac
- object_type: project
  name: MAME
  canonical_name: MAME
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 作者使用 MAME 模拟器的 gdbstub 功能配合从源码编译的 m68k-gdb 进行内核调试。
  - gdb-multiarch 在协调 gdbstub 时存在地址空间跳转问题，需要改用 m68k 特定构建的 gdb。
  article_id: 103e2bb774fb2cac
- object_type: project
  name: uClinux
  canonical_name: uClinux
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - uClinux 使无 MMU 的处理器也能运行 Linux，目前已不再是下游分支而是直接集成在主线内核中。
  - Jaguar 移植依赖 uClinux 的 Flat Memory 模式和 FLAT 二进制格式来绕过没有 MMU 的限制。
  article_id: 103e2bb774fb2cac
impact_score:
  score: 1.2
  reason: 该事件是将Linux 7.2移植到1993年发布的Atari Jaguar游戏主机的复古硬件项目，与AI行业无直接关联。虽然技术上展示了XIP（片内执行）和uClinux无MMU模式在极端内存受限（2MB
    RAM）环境下的可行性，但这些技术并非新突破——uClinux已作为内核主线功能存在多年。该移植对当前AI模型训练、推理、部署或基础设施均无实质性影响，属于社区讨论源在AI行业采集中的噪音内容。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: 该移植的技术可行性和复古硬件上的工程挑战
hype_assessment:
  level: low
  reason: 文章是纯技术分享，没有任何'颠覆'、'革命性'等PR包装词汇。作者以自嘲和务实的口吻详细记录了移植过程中遇到的具体工程问题（编译器对68000生成未对齐内存访问、异常向量表地址映射修复、XIP配置等），并提供了可复现的技术细节，属于典型的社区技术帖，不存在炒作成分。
information_entropy: high
domain_disruption:
  technical_innovation: 无直接AI技术突破。文章使用的XIP技术将内核只读段存放在ROM、动态段存放在RAM的分割策略，以及uClinux的Flat内存模型，在原理上可被参考用于极端资源受限场景（如MCU级AI推理）的内存优化，但这是已成熟的既有技术，并非新贡献。
  business_model: 无
engineering_complexity: prototype
compound_value:
  score: 1.2
  reason: 该事件是面向1993年已停产游戏主机Atari Jaguar的Linux移植项目，属于复古计算领域的个人技术实践。从VC视角评估：第一，该事件不产生任何商业价值，目标硬件早已退出市场（总销量仅25万台），不存在规模化部署场景；第二，uClinux/no-MMU和XIP技术已在嵌入式Linux领域成熟应用超过20年，该移植未引入突破性创新技术；第三，与AI产业格局完全无关，不涉及算力、模型、数据或任何AI商业化要素；第四，不产生网络效应、数据飞轮或规模复利效应。属于纯粹的技术爱好者项目，长期复利价值趋近于零。
value_capture_layer: hardware_compute
moat_impact: neutral
key_beneficiaries: []
competitive_casualty: []
market_opportunities:
- 嵌入式开发者可借鉴 XIP（原地执行）与 no-MMU Linux 技术方案，为超低内存（2MB 级别）的工业物联网设备或 MCU+ 级别硬件提供 Linux
  运行能力，开辟极小成本 Linux 终端的商业化路径
- 复古游戏社区的硬件改造与内容创作者可利用该成果推出 Atari Jaguar 的 Linux 发行版教程、开发套件或定制 ROM，吸引怀旧玩家与极客群体的付费订阅与赞助
- 边缘计算场景中，该方案证明了在无 MMU、低 RAM 的设备上运行标准 Linux 内核的可行性，可启发面向传感器网关、智能仪表等轻量级边缘节点的定制 Linux
  发行版创业方向
risk_matrix:
  regulatory: 无
  technological: 该方案依赖已停止广泛更新的 uClinux 子系统和 FLAT 二进制格式，若上游 Linux 内核社区移除对 m68k no-MMU
    的支持或有更好替代方案（如 RTOS、Zephyr），该技术路线的长期可持续性存在风险
  competitive: 无
  ethical: 无
  additional: []
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: speculative_watch
object_insights:
- object_type: project
  name: cakehonolulu/linux-for-jaguar
  canonical_name: linux-for-jaguar
  url: https://github.com/cakehonolulu/
  positioning: 将 Linux 内核移植到 1993 年发布的 Atari Jaguar 游戏主机的开源项目，在仅有 2MB RAM 和 6MB ROM
    的极端限制下通过 XIP 技术实现了内核启动。
  technical_signal: 通过 XIP 技术将内核只读段存储在 ROM 中、动态段存储在 RAM 中，解决了 2MB 内存无法容纳完整内核的核心难题，验证了
    Linux nommu 模式的极限适配能力。
  adoption_signal: 移植工作在 MAME 模拟器和实机上均已验证成功，内核成功启动并输出了从 7.2.0-rc1 到内存节点初始化的完整启动日志，证明技术方案可行。
  ecosystem_relevance: 该项目验证了 Linux 主线内核中 m68k nommu 子系统的实际可用性，为其他复古硬件或嵌入式设备的 Linux
    移植提供了可复用的技术路径和参考实现。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该项目在仅有 2MB RAM 的 1990 年代游戏主机上成功运行现代 Linux 内核，对嵌入式 Linux 移植、XIP 技术实践和复古计算领域具有独特的示范价值，展示了
    Linux 在资源极端受限环境下的适应能力和 uClinux 生态的成熟度。
  risk_notes:
  - 移植依赖从源码构建的特定 m68k-elf-gcc 编译器，Ubuntu 官方工具链因生成未对齐访问指令而无法使用，工具链兼容性问题增加了复现门槛。
  - 用户空间仅为最小化的 busybox shell 环境，缺乏完整的 init 进程和应用生态，目前尚停留在技术验证阶段，离实用化有较大距离。
  - Atari Jaguar 平台在实机上的内存和存储限制极其苛刻，进一步的用户空间扩展和稳定化面临根本性的硬件瓶颈。
  score: 4.0
  article_ids:
  - 103e2bb774fb2cac
  evidence_snippets:
  - 文章详细记录了将 Linux 移植到 Atari Jaguar 主机的完整技术过程，包括内核配置、编译器问题、内存布局和用户空间构建。
  - 作者公开了针对 Atari Jaguar 的 Linux 内核仓库，其中包含平台移植补丁、Jerry DSP 串口驱动和内核配置方案。
  - 内核在 MAME 模拟器上成功启动并输出了完整启动日志，但最终因缺少 init 进程而崩溃，整体属于技术验证阶段。
- object_type: project
  name: uClinux
  canonical_name: uClinux
  url: null
  positioning: 使无 MMU 的处理器也能运行 Linux 的开源框架，早期作为独立下游分支开发，现已完全合并入 Linux 主线内核，为嵌入式系统和复古硬件提供了轻量级运行方案。
  technical_signal: uClinux 的 FLAT 二进制格式和 Flat Memory 模型为无 MMU 处理器提供了完整的内存管理和用户空间运行机制，相关代码已完全集成在
    Linux 主线内核的 arch/m68k/ 目录中。
  adoption_signal: uClinux 已从独立下游分支完全合入 Linux 主线内核，不再需要独立维护，体现了在嵌入式领域广泛采用的技术成熟度和社区认可度。
  ecosystem_relevance: uClinux 为大量嵌入式设备、物联网终端和复古硬件的 Linux 移植提供了关键技术基础，是 m68k nommu
    等架构移植工作的核心依赖。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: uClinux 作为已合入 Linux 主线的无 MMU 内核支持方案，虽然在主流服务器场景中不突出，但在资源极端受限的嵌入式设备、物联网终端和复古计算领域具有不可替代的价值，其技术演进持续影响低功耗设备的上限。
  risk_notes:
  - uClinux 合入主线后其演进节奏与社区主线绑定，针对特定极端硬件的专项优化可能缺乏独立维护动力。
  - 无 MMU 环境下缺少虚拟内存保护，单个进程的内存访问错误可能导致整个系统崩溃，不适合高安全要求的场景。
  score: 3.0
  article_ids:
  - 103e2bb774fb2cac
  evidence_snippets:
  - uClinux 早期作为独立下游分支存在，现已完全合入 Linux 主线内核，使无 MMU 的处理器也能运行完整的 Linux 系统。
  - Jaguar 移植依赖 uClinux 的 Flat Memory 模式和 FLAT 二进制格式来绕过没有 MMU 的限制，这展现了 uClinux 在极端环境中的实用价值。
---

## What in the tarnation is an Atari Jaguar?

Released in North America in November of 1993, the Atari Jaguar promised to be the new cool kid in the block thanks to it's (Highly debated) 64 bits of pure power.

*The Atari Jaguar*

The console itself ended up being a commercial disaster, even after the release of the CD addon, the Jaguar CD; which managed to sell even less units in a desperate attempt to try and compete with the Sony Playstation and the Sega Saturn.

*The Atari Jaguar CD Add-on*

## Why in the tarnation Linux of all things?

Interestingly enough, to this day, Linux has architecture code for the 68000-family of processors. 68040, 68030, 68010... and even the original base 68000 processor. All neatly structured under `arch/m68k/`

.

As a refresher, the Motorola 68000 was a CISC processor with mixed 16-32 bit capabilities (It's usually described as being 32-bit internally due to the register width length and 16-bit because the data bus was 16-bit, so 2-byte transfers at a time).

It had a 24-bit address bus, 2 to the power 24; thereabout a maximum of 16MBs of addressable memory.

It was released on 1979 to compete with the soon-to-be-released 16-bit CPUs of the era.

*The Motorola 68000*

Overall, it got lots of traction commercially; it ended up being incoporated in lots of popular commercial hardware. Biggest contenders are the original Macintosh (And Apple Lisa), the Commodore Amiga series of computers (With varying generations of the 68000), the Sega Genesis/Megadrive, the Neo-Geo AES, Plexus workstations... and the Jaguar.

Let's now address the elephant in the room, why Linux.

Well, it's easy; `because we can`

(*-ish*).

## jmp _linux?

Doing a bringup for a new 68000-based Linux port should be easy... right? Well... you're in for a good time.

As you may know (Or not), there's an extended thought that Linux requires an MMU to run (You know, being able to use virtual memory is a good thing if we want to run software w/o having day-long headaches).

Technically you are *kind of* right, but there's *uClinux*; which precisely makes this feasible. At some point it stopped being a downstream fork of Linux to become part of it; thankfully it's baked in for `m68k`

(And well, the flat memory model and the rest of requirements you can imagine that an MMU-less system has).

Okay so, we enable all the required configuration flags on the Linux `menuconfig`

(To basically tell it not to use an MMU and use the Flat Memory model), we compile and it should run right? Well yes... but no.

## What actually jmp _linux involves

The Jaguar has 2 megabytes of RAM (Mapped at 0x000000), (up to) 6 megabytes of ROM (The cartridge, basically) mapped at 0x80000 and 2 custom ICs (Tom & Jerry, a "GPU" and a DSP) which are also memory-mapped.

The main hurdle we have is the memory footprint; while it's true that it's a good amount, it's still not infinite (We're talking megabytes, not *gigabytes*).

We basically need to find a way to try and optimize our RAM usage. We can do the easy things first, removing features from the kernel, disabling debug... the usual suspects; but we'll still probably have the issue where we can't load the kernel to RAM w/o going OOM (Heck, we're not even considering an initramfs at this point, which are also bulky).

Thankfully, Linux is smart enough to let us "split" the kernel in 2 separate memory regions. One can take advantage of the fact that we can store the read-only sections of it (Think, `.rodata`

or `.text`

) on the cartidge (The ROM) and the dynamic sections (`.data`

or `.bss`

) in RAM (Think, `XIP`

; eXecute-In-Place).

Fortunately for us, it's a matter of telling it where we have the RAM, and where we have the ROM and it (Liunx) handles the relocations for us.

Cool, we can now fit Linux on the Jaguar and it should be able to execute... right? Well sure, you can try specifying the base of the kernel (Remember, `non-PIC`

code) and loading it there and just doing a `jmp $80000`

... but how do we know what is happening under the hood?

## What Linux needs to boot

In the context of booting Linux, we need 2 things (At least, when doing the bringup):

- Any type of output so we can see kernel messages
- Any way to tick the system (A timer, basically)

The first requisite usually involves `ye' good ol'`

UART. The Jaguar's DSP (Jerry) has TXD & RXD pins that (At least for booting Linux) can be repurposed to do serial output (If we ignore anything that has to do with sound). Writing a small console driver that bitbangs the pins is enough to start seeing some `earlyprintk`

messages.

The second one, is a bit tougher. But we can basically use any of the 2 timers that the Jerry IC has; commercial games & software use them for sound-related tasks but we'll use them to basically enable Linux to calibrate and setup the scheduler and the systems that also depend on a `PIT`

. It's useful that they can both trigger an interrupt on the Jerry itself but also on the 68000. We can basically override the board-specific init of the 68000 on Linux and designate that as the PIT that Linux will use from now on.

We compile, we try to run...

...

Nothing?

Okay... that's strange; we should be getting something? I mean, everything checks out... kernel config: check... addresses: check... compiler... compiler?

Turns out that the bundled `m68k-linux-`

cross compiler on Ubuntu repositories emits unaligned memory accesses (Even after passing `-68000`

) so it was, effectively, crashing (Since the base 68000 doesn't implement unaligned memory access handling).

Using a compiler built from sources specifically targetting `m68k-elf-`

for the 68000 seems to fix this, but it's definitely a strange thing.

This also points out a separate problem; since our ROM isn't mapped at 0x00000 but 0x80000, the 68000 tries jumping to VBR (0x0) and since there's no handlers there it basically eats itself and burns down. This needed a separate `memcpy`

of the so-called `vectors`

to the base of RAM to fix in the Jaguar platform-specific Linux code.

I also had to compile `gdb`

from source to get it to behave with MAME's `gdbtsub`

. For some reason `gdb-multiarch`

was incorrectly orchestrating the stub and it'd jump around all the address space; which was fun until I decided to test with an `m68k`

specific build.

Cool, we get now finally get *some* output:

```
Linux version 7.2.0-rc1+ (cakehonolulu@jaguar) (m68k-elf-gcc (GCC) 16.1.0, GNU ld (GNU Binutils) 2.46.1) #38 Sun Jul 5 11:56:37 CEST 2026
printk: legacy bootconsole [early_jerry0] enabled
uClinux with CPU MC68000
Flat model support (C) 1998,1999 Kenneth Albanowski, D. Jeff Dionne
Zone ranges:
DMA [mem 0x0000000000000000-0x00000000001fffff]
Normal empty
Movable zone start for each node
Early memory node ranges
node 0: [mem 0x0000000000000000-0x00000000001fffff]
Initmem setup node 0 [mem 0x0000000000000000-0x00000000001fffff]
On node 0, zone DMA: 512 pages in unavailable ranges
printk: log buffer data + meta data: 4096 + 12800 = 16896 bytes
Dentry cache hash table entries: 1024 (order: 0, 4096 bytes, linear)
Inode-cache hash table entries: 1024 (order: 0, 4096 bytes, linear)
Built 1 zonelists, mobility grouping off. Total pages: 512
mem auto-init: stack:all(zero), heap alloc:off, heap free:off
SLUB: HWalign=16, Order=0-1, MinObjects=0, CPUs=1, Nodes=1
NR_IRQS: 32
clocksource: jiffies: mask: 0xffffffff max_cycles: 0xffffffff, max_idle_ns: 19112604462750000 ns
Calibrating delay loop... 1.04 BogoMIPS (lpj=5248)
pid_max: default: 32768 minimum: 301
Mount-cache hash table entries: 1024 (order: 0, 4096 bytes, linear)
Mountpoint-cache hash table entries: 1024 (order: 0, 4096 bytes, linear)
```


Now it tries to find and execute an `init`

process... which we don't have... so it crashes again.

## Userspace

One of the many limitations we have is, since we can't use ELF files (We use FLAT binaries, mainly due to the `nommu`

situation); so this complicates our toolchain setup a bit.

I wasn't really able to find ways to compile `elf2flt`

(The utility that basically converts from one format to the other) in a standalone way, it basically needs you to provide some `.a`

and some `.h`

files (Related to `libiberty`

and akin) and those seem to have vanished from default configurations of `binutils`

+`gcc`

for the 68000... but strangely, `buildroot`

seems to have had a recent update where they figured it all out for us for the base 68000.

Kudos to the `linuxmd`

project, the patch to enable m68k nommu is dated May 2026 (So as of writing this blog, 2 months old!)

So I basically had it do the thing for me and also decided to go for `busybox`

which was one of the few (If not only) project of this kind that supports a `nommu`

target.

There were some issues (Mostly related to me not having dealt with flat binaries ever) but again, nothing a few minutes of debugging and logging wouldn't fix. The most glaring issue I was having was OOM issues everywhere when trying to execute init. I basically just did this:

```
❯ cat ~/jagfs/rootfs/init
#!/bin/busybox sh
/bin/busybox sh
❯
```


Usually you ask `busybox`

to populate the rest of the utilities (It's just symlinks to itself, with different names; what they call the `applet`

design where based on `argv[1]`

it knows which applet to dispatch: `echo`

, `ls`

, `cat`

...), but for some reason that was OOMing the kernel; so it's just `sh`

for now (The `shebang`

part also gets executed but seems not to eat up all the memory like `--install`

does).

We now have a rootfs, we pack it as `cpio`

and embed it into the `vmlinux`

image itself... and voila!

As an exercise left to the reader, there's a few interesting tidbits that you can basically check on Linux in regards of FLAT binaries (F.e, there's different ways of handling the execution of them: Load the entire binary to RAM, do GOT/PLT relocation... all of which can be checked with `flthdr`

).

```
❯ flthdr ~/jagfs/rootfs/bin/busybox
/home/cakehonolulu/jagfs/rootfs/bin/busybox
Magic: bFLT
Rev: 4
Build Date: Thu Jul 2 03:08:09 2026
Entry: 0x44
Data Start: 0x1e0a0
Data End: 0x25808
BSS End: 0x2a1c0
Stack Size: 0x10000
Reloc Start: 0x25808
Reloc Count: 0x100e
Flags: 0x1 ( Load-to-Ram )
```


Pretty nifty thing...

There's a separate tune we had to do on the toolchain's `libc`

(Which is `uClibc`

) that involves modiyfing the malloc strategy to `malloc-simple`

; else the metadata associated with faster and smarter allocations eat up our memory very fast and we'd panic again.

This memory allocation change was done within `buildroot`

's `uClibC`

menuconfig.

*make uclibc-menuconfig*

I also got some time to implement a simple Tom (GPU but not a GPU, more like word/object processor & blitter) console driver so this can be tested on real hardware too.

Don't forget that if you run on real hardware, the converted-to-binary `vmlinux`

(Using `objcopy`

) needs to be compiled at a fixed offset from 0x80000 so that the Jaguar cart header can be added (Which in turn, jumps to whichever location you say it to). So you need to add an offset of 8KB to `CONFIG_ROM_START`

, `CONFIG_ROM_LENGTH`

(Which will be a bit smaller due to the increase in base address), `CONFIG_ROMVEC`

and `CONFIG_ROMSTART`

.

The basic idea to replicate the setup is, compile buildroot targetting the 68000 (With the `malloc`

strategy changed). You can ask it to build busybox with a custom `.config`

file path (Provided at end of post) so it'll get you the FLAT binary. You then populate a simple initramfs file structure (There's resources online on what you need to do, usually involves creating `/{dev,bin,usr,sbin...}`

directories and adding some `mknode`

magic to `/dev/`

to prop up the `/dev/console/`

nodes plus an `init`

script; which is also provided at the end of this post).

Then you tell Linux where you have your newly-built `initramfs.cpio`

file and voilà!

That's it!

Find the modified Linux repository over at: https://github.com/cakehonolulu/linux_jag

In contrast with `linuxmd`

, we don't have special cartridge mappers (`SSF2`

, which gives an extra 4MBs of RAM on the MegaDrive) so I couldn't really justify (Memory-wise) adding any bootloader (`u-boot`

, for instance) so I basically went with the approach I documented on the blog (Of basically doing `jmp _linux`

).