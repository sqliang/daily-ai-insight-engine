---
title: 'Caddy compatibility for zeroserve: 3x throughput and 70% lower latency'
source: https://su3.io/posts/zeroserve-caddy-compat
author:
- '[[losfair]]'
published: '2026-06-14'
created: '2026-06-15'
description: 'Article URL: https://su3.io/posts/zeroserve-caddy-compat Comments URL:
  https://news.ycombinator.com/item?id=48527145 Points: 186 # Comments: 53'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b17c0458a0c4d4f6
source_type: community_discussion
tldr: zeroserve 是一款基于 eBPF 的用户态高性能 HTTPS 服务器，最新 v0.2.11 版本新增 Caddyfile 兼容模式，可将 Caddy
  配置 JIT 编译为 eBPF 再编译为原生机器码在 io_uring 中执行。基准测试显示，相比原生 Caddy，zeroserve 实现了约 3 倍吞吐量和
  70% 的延迟降低。
objective_summary: zeroserve 项目于 2026 年 7 月发布 v0.2.11 版本，新增 Caddyfile 兼容模式，可将 Caddy
  配置依次 JIT 编译为 eBPF 和原生 x86_64/ARM64 机器码，并在 io_uring 事件循环中执行。在 AMD Ryzen 7 3700X 双线程
  HTTPS 反向代理基准测试中，zeroserve（clang 编译）达到 38,948 req/s 吞吐量和 1.45ms p50 延迟，而原生 Caddy
  为 12,529 req/s 和 4.74ms p50 延迟，nginx 为 37,424 req/s 和 1.57ms p50 延迟。zeroserve 支持图灵完备的
  eBPF，用户可通过 Caddyfile 调用自定义 eBPF 中间件，例如为 S3 兼容存储桶添加 AWS SigV4 认证。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - eBPF
  - io_uring
  - JIT
  - Caddyfile
  - AWS SigV4
  - x86_64
  - ARM64
  key_people: []
key_logic_flow:
- zeroserve 是一个在用户态运行 eBPF 脚本的高性能 HTTPS 服务器，现已新增 Caddyfile 兼容模式。
- 用户提供 Caddyfile 后，zeroserve 将其 JIT 编译为 eBPF，再编译为原生 x86_64/ARM64 机器码，并在 io_uring 事件循环中执行。
- HTTPS 反向代理基准测试显示 zeroserve-clang 达到 38,948 req/s 吞吐量（原生 Caddy 为 12,529 req/s），p50
  延迟为 1.45ms（原生 Caddy 为 4.74ms），峰值内存 30.9 MiB（Caddy 为 67.4 MiB）。
- zeroserve 支持用户通过 Caddyfile 调用自定义 eBPF 代码，例如使用 io.su3.aws-sigv4 中间件为 S3 兼容存储桶添加 AWS
  SigV4 认证。
- 用户可通过 curl 下载 zeroserve v0.2.11 的 Linux 二进制文件，使用 --caddy 参数指定配置路径即可直接运行。
extract_result: success
object_mentions:
- object_type: project
  name: zeroserve
  canonical_name: losfair/zeroserve
  url: https://github.com/losfair/zeroserve
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - zeroserve 是一个在用户态运行 eBPF 脚本的高性能 HTTPS 服务器，现已新增 Caddyfile 兼容模式，可将 Caddy 配置 JIT
    编译为 eBPF 后执行。
  - 用户可通过 curl 下载 zeroserve v0.2.11 的 Linux 二进制文件，使用 --caddy 参数指定 Caddyfile 路径即可启动，支持
    --plugin 参数加载自定义 eBPF 中间件。
  - 基准测试显示 zeroserve-clang 的 HTTPS 反向代理吞吐量达到 38,948 req/s，约为原生 Caddy 12,529 req/s
    的 3 倍，同时峰值内存仅 30.9 MiB 远低于 Caddy 的 67.4 MiB。
  article_id: b17c0458a0c4d4f6
- object_type: product
  name: Caddy
  canonical_name: Caddy
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章将 zeroserve 与原生 Caddy 进行 HTTPS 反向代理基准对比，Caddy 的吞吐量为 12,529 req/s，p50 延迟为 4.74ms，p99
    延迟为 13.11ms。
  - zeroserve 新增的兼容模式可直接接受 Caddyfile 作为配置输入，并将其即时编译为 eBPF 和原生机器码后执行，无需修改现有 Caddy 配置。
  article_id: b17c0458a0c4d4f6
- object_type: product
  name: nginx
  canonical_name: nginx
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 文章在同一基准测试中将 nginx 作为对照，nginx 的 HTTPS 反向代理吞吐量为 37,424 req/s，p50 延迟为 1.57ms，峰值内存
    25.7 MiB。
  article_id: b17c0458a0c4d4f6
impact_score:
  score: 6.5
  reason: 该事件展示了基于 eBPF + io_uring 的 HTTPS 服务器的实际性能突破：Caddyfile 兼容模式下吞吐量达 Caddy 的
    3.1 倍、延迟降低 70%、内存减半。这不是概念验证，而是可执行的 v0.2.11 版本，且与 nginx 在同一测试中性能持平。虽然项目处于早期阶段（v0.2.11），社区影响力有限，但这一技术路线（配置→eBPF→原生机器码
    JIT 编译）可能对反向代理和 API 网关领域的架构设计产生深远影响，是一个重要的行业信号。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: Caddyfile 兼容性 + eBPF 原生性能的独特组合，可直接迁移现有配置获得 3 倍性能提升
hype_assessment:
  level: low
  reason: 文章提供了完整的可复现基准测试数据（2 线程 Ryzen 3700X，含吞吐量、p50/p99 延迟、峰值 RSS 内存四项指标），并附带了 CI
    原始运行结果链接。没有使用'颠覆'、'革命性'等 PR 夸大词汇，技术描述准确且克制（如实报告 nginx 性能与之接近）。提供的配置示例和 CLI 命令均可直接运行，无水分。
information_entropy: high
domain_disruption:
  technical_innovation: 提出了一种新颖的 Web 服务器架构：将 Caddyfile 配置通过 JIT 编译链路（Caddyfile → eBPF
    → x86_64/ARM64 原生机器码）转化为 io_uring 事件循环中的高效执行代码，并支持运行时加载自定义 eBPF 中间件（如 AWS SigV4
    认证）。这一方法将配置解析和执行效率推向接近手写原生代码的水平。
  business_model: 如果项目成熟，可能对云原生反向代理和 API 网关市场（如 Envoy、nginx、Caddy 的商业版本）形成冲击，提供一种'低门槛配置
    + 极致性能'的差异化竞争路径。但当前 v0.2.11 阶段距商业化还很远。
engineering_complexity: prototype
compound_value:
  score: 6.5
  reason: zeroserve 的技术路径（Caddyfile JIT → eBPF → x86_64/ARM64 原生机器码）从根本上重构了 HTTPS
    服务器的性能模型，在 2 线程基准测试中达到 Caddy 约 3.1 倍吞吐量、70% 更低延迟，且内存占用减半。这种 '配置即编译' 的思路如果成熟，有望成为高密度
    HTTPS 部署（API Gateway、CDN 边缘节点、微服务网格）的新基础设施选择，具备长期复利潜力。其 eBPF 插件机制（如 AWS SigV4
    认证）进一步拓宽了可编程性边界。但项目处于 v0.2.11 的极早期阶段，缺乏生产环境验证、社区生态和运营保障（SLA、监控、热更新），且 nginx 在同等测试中性能相近（37,424
    req/s vs 38,948 req/s），说明传统优化手段尚未被碾压。需要持续跟踪其 adoption 曲线、稳定性保障能力以及是否会被 Linux 内核
    eBPF 进化所替代。投资逻辑：值得作为技术原型深度关注，但需等到 production-grade 版本和至少一个中大型客户案例后再做配置决策。
value_capture_layer: hardware_compute
moat_impact: creates_new_moat
key_beneficiaries:
- Cloudflare
- AWS
- Fastly
competitive_casualty:
- Caddy
- 传统反向代理解决方案
market_opportunities:
- 基础设施团队可在高吞吐量反向代理场景中评估 zeroserve，其基于 eBPF + io_uring 的架构在 HTTPS 吞吐量和延迟上显著优于 Caddy，尤其适合
  API 网关和 CDN 边缘节点等延迟敏感场景
- eBPF 插件架构（如 AWS SigV4 认证示例）为云原生中间件开发者提供了新的可扩展路径，可实现自定义协议处理、认证逻辑等，无需修改核心服务器代码
- 关注该项目的技术路线可作为基础设施选型的备选方案，特别是对于当前使用 Caddy 但面临性能瓶颈的团队，迁移成本较低（兼容 Caddyfile）且性能提升显著
risk_matrix:
  regulatory: eBPF 技术本身在各主要司法管辖区暂无特殊监管限制，但若用于生产环境需关注 Linux 内核版本兼容性及 eBPF 安全审计机制
  technological: 项目处于 v0.2.11 早期阶段，生产就绪度存疑；Caddyfile 指令集兼容性可能不完整；JIT 编译 eBPF 再到原生机器码的链路增加了调试和故障排查复杂度；nginx
    已实现相近性能（37,424 vs 38,948 req/s），技术差异化优势有限
  competitive: nginx 在同等测试中性能几乎持平（37,424 req/s vs 38,948 req/s），且拥有庞大的生产部署验证和生态系统；Caddy
    拥有自动 HTTPS 等差异化功能；zeroserve 作为新生项目面临生态壁垒和信任积累挑战
  ethical: 无
  additional: []
confidence:
  impact: high
  compound: low
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: zeroserve
  canonical_name: losfair/zeroserve
  url: https://github.com/losfair/zeroserve
  positioning: 基于 eBPF 和 io_uring 的用户态高性能 HTTPS 服务器，支持将 Caddyfile 配置 JIT 编译为原生机器码并在事件循环中执行。
  technical_signal: zeroserve v0.2.11 新增 Caddyfile 兼容模式，可将 Caddy 配置依次 JIT 编译为 eBPF
    和原生 x86_64/ARM64 机器码后执行。
  adoption_signal: 项目已发布 v0.2.11 二进制版本，用户可通过 curl 一键下载并使用 --caddy 参数指定配置路径直接运行。
  ecosystem_relevance: 该项目兼容 Caddy 生态的 Caddyfile 配置体系，支持通过 Caddyfile 调用自定义 eBPF 中间件扩展功能。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: zeroserve 以 eBPF + io_uring 创新架构在 HTTPS 反向代理场景实现约 3 倍于 Caddy 的吞吐量和
    70% 的更低延迟，同时保持 Caddyfile 配置兼容性，降低了迁移门槛，值得持续跟踪其生产环境验证进展和生态扩展方向。
  risk_notes:
  - zeroserve 依赖 Linux 特定内核特性（eBPF 和 io_uring）实现核心能力，跨平台部署受限且无法在非 Linux 环境运行。
  - 项目尚处早期阶段，Caddyfile 兼容模式的完备性和生产稳定性有待充分验证。
  score: 7.0
  article_ids:
  - b17c0458a0c4d4f6
  evidence_snippets:
  - zeroserve 是一个在用户态运行 eBPF 脚本的高性能 HTTPS 服务器，现已新增 Caddyfile 兼容模式，可将 Caddy 配置 JIT
    编译为 eBPF 后执行。
  - 用户可通过 curl 下载 zeroserve v0.2.11 的 Linux 二进制文件，使用 --caddy 参数指定 Caddyfile 路径即可启动，支持
    --plugin 参数加载自定义 eBPF 中间件。
  - 基准测试显示 zeroserve-clang 的 HTTPS 反向代理吞吐量达到 38,948 req/s，约为原生 Caddy 12,529 req/s
    的 3 倍，同时峰值内存仅 30.9 MiB 远低于 Caddy 的 67.4 MiB。
- object_type: product
  name: Caddy
  canonical_name: Caddy
  url: null
  positioning: 一款以自动 HTTPS 和简洁 Caddyfile 配置为特色的现代化开源 Web 服务器，可自动管理 TLS 证书和证书续期。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - Web 运维与开发人员
  product_signal: 在本文的 HTTPS 反向代理基准测试中，Caddy 的吞吐量为 12,529 req/s，p50 延迟为 4.74ms，p99
    延迟为 13.11ms，峰值内存占用为 67.4 MiB。
  market_signal: null
  differentiation: 以自动 HTTPS 和简洁的 Caddyfile 配置为核心特色，其配置格式被新兴高性能方案 zeroserve 兼容采纳。
  watch_reason: 本文基准显示 Caddy 在纯性能上落后于新兴方案 zeroserve 和 nginx，但其 Caddyfile 作为被兼容的配置标准，使
    Caddy 在 Web 服务器生态演变中具有关键的参考锚点价值，值得持续跟踪这一趋势。
  risk_notes:
  - 在纯反向代理性能维度，Caddy 显著落后于 zeroserve 和 nginx，可能影响高吞吐场景的选型竞争力。
  score: 4.0
  article_ids:
  - b17c0458a0c4d4f6
  evidence_snippets:
  - 文章将 zeroserve 与原生 Caddy 进行 HTTPS 反向代理基准对比，Caddy 的吞吐量为 12,529 req/s，p50 延迟为 4.74ms，p99
    延迟为 13.11ms。
  - zeroserve 新增的兼容模式可直接接受 Caddyfile 作为配置输入，并将其即时编译为 eBPF 和原生机器码后执行，无需修改现有 Caddy 配置。
---

zeroserve is a high-performance HTTPS server that runs eBPF scripts in userspace (intro). Now it's got a Caddy-compat mode - when provided a Caddyfile, zeroserve JIT-compiles it to eBPF and then to native x86_64/ARM64 machine code, and runs it in an `io_uring`

event loop.

| protocol | server | throughput | p50 | p99 | peak RSS |
|---|---|---|---|---|---|
| https | zeroserve-clang | 38,948 req/s | 1.45ms | 3.91ms | 30.9 MiB |
| https | zeroserve-tcc | 36,653 req/s | 1.67ms | 4.00ms | 34.2 MiB |
| https | caddy | 12,529 req/s | 4.74ms | 13.11ms | 67.4 MiB |
| https | nginx | 37,424 req/s | 1.57ms | 4.24ms | 25.7 MiB |

*HTTPS reverse proxy, 2 threads, AMD Ryzen 7 3700X. Check CI for original run result.*

Try it with your Caddyfile:

```
curl -fL -o zeroserve https://github.com/losfair/zeroserve/releases/download/v0.2.11/zeroserve-$(uname -m)-linux
chmod +x zeroserve
./zeroserve --caddy /etc/caddy/Caddyfile
curl http://127.0.0.1:8080
```


zeroserve runs turing-complete eBPF and you can call custom code from your Caddyfile. For example, to reverse-proxy a path to an S3-compatible bucket with AWS SigV4 auth, grab io.su3.aws-sigv4.c and then:

```
# zeroserve --plugin io.su3.aws-sigv4.c --caddy Caddyfile
example.com {
route /s3/* {
uri strip_prefix /s3
rewrite * /my-bucket{uri}
# Call the `sign_request` method in the eBPF middleware `io.su3.aws-sigv4.o`
zeroserve_call io.su3.aws-sigv4 sign_request {
access_key_id "minioadmin"
secret_access_key "minioadmin"
}
reverse_proxy http://127.0.0.1:9000
}
}
```