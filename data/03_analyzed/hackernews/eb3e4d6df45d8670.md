---
title: A shell colon does nothing. Use it anyway
source: https://refp.se/articles/your-shell-and-the-magic-colon
author:
- '[[olexsmir]]'
published: '2026-07-25'
created: '2026-07-26'
manifest_dates:
- '2026-07-26'
description: 'Article URL: https://refp.se/articles/your-shell-and-the-magic-colon
  Comments URL: https://news.ycombinator.com/item?id=49047453 Points: 170 # Comments:
  60'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: eb3e4d6df45d8670
source_type: community_discussion
tldr: 文章介绍了 Shell 中的冒号（`:`）空命令和 `${var:?error}` 参数展开语法，展示了如何用一行代码替代多行的参数校验 if 语句，并列举了设置默认值、截断文件、检查文件可读性、trap
  信号处理等多种实用场景。
objective_summary: '文章以 Shell 脚本编程为主题，讲解了冒号（`:`）作为空命令（null-command）的用途，以及 `${name:?diagnostic}`
  参数展开语法在参数校验中的应用。作者通过示例说明，`:` 空命令起源于 1971 年的 Thompson shell，最初用作标签和注释标记；它不执行任何操作但会计算并丢弃参数结果。文章展示了多个实用场景：用
  `: "${1:?error}"` 替代 if 判断进行参数校验、用 `: > error.log` 截断文件、用 `( : < dataset.json )`
  检查文件可读性、用 `trap : INT` 使 sleep 可中断等。'
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - Shell
  - Bash
  - Zsh
  - Thompson shell
  - parameter expansion
  key_people: []
key_logic_flow:
- 冒号（`:`）是 Shell 的内置空命令，它不执行任何操作但会计算参数并丢弃结果，起源于 1971 年的 Thompson shell，最初用作标签和注释标记。
- '`${name:?diagnostic}` 参数展开语法检查变量是否未设置或为空，若是则打印诊断信息到 stderr 并以非零状态退出；若已设置则等价于 `$name`。'
- '用 `: "${1:?missing argument, aborting.}"` 一行代码即可替代多行的 if 参数校验逻辑，且能自动输出包含变量名的错误诊断信息。'
- '空命令可用于设置默认值（`: "${DATA_DIR:=/var/data}"`），通过单次变量引用减少拼写错误的可能性。'
- '空命令可用于截断文件（`: > error.log`）、检查文件是否可读（`: < dataset.json`）或可写（`: >> result.json`）。'
- '空命令可作为 trap 的信号处理命令（`trap : INT`）以使 sleep 等命令可被中断，也可在空的 then 分支中作为必需的占位命令。'
object_mentions: []
extract_result: success
impact_score:
  score: 2.0
  reason: 本文是一篇 Shell 编程技巧分享，介绍 `:` 空命令和 `${var:?error}` 参数展开的实用场景。这些技术自 1971 年 Thompson
    shell 起就已存在，并非新发明。对 Shell 脚本开发者有实用参考价值，但对 AI 行业短期竞争格局、技术范式无任何冲击力。属于小圈子技术经验分享。评分：2.0
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: Shell 脚本中的冒号空命令与参数校验技巧
hype_assessment:
  level: low
  reason: 文章没有任何炒作词汇或夸大表述，以技术教程形式平实地介绍 Shell 内置命令的多种实用场景，无 '颠覆'、'革命性' 等 PR 用语。属于实打实的技术干货分享。判定依据：低炒作
information_entropy: medium
domain_disruption:
  technical_innovation: 无。本文介绍的是 1971 年起就存在的 Shell 内置功能，无技术创新突破。
  business_model: 无。纯技术教程，不涉及商业模式。
engineering_complexity: production_ready
compound_value:
  score: 1.2
  reason: 该文章仅是对1971年Thompson shell时代就已存在的Shell空命令和参数展开语法的科普性介绍，并非新技术或新产品的发布。这些技巧是开发者社区长期已知的知识沉淀，既没有商业化潜力，也不构成任何公司的技术壁垒。从VC视角看，此类编程技巧分享对长期复利价值贡献可以忽略不计，不具备投资意义上的积累效应。
value_capture_layer: end_application
moat_impact: neutral
key_beneficiaries: []
competitive_casualty: []
market_opportunities:
- DevOps 团队可将冒号空命令与参数展开模式纳入内部编码规范，编写 Shell 脚本最佳实践指南，提升脚本健壮性和可维护性
- 开发者工具团队可基于此类模式开发 Shell 脚本 lint 规则（如检测未使用参数校验的脚本），集成到 CI 流水线中实现自动化代码质量门禁
- 技术培训与咨询机构可针对中高级工程师推出 Shell 脚本现代化课程，将空命令、参数展开、trap 信号处理等高效模式作为核心模块
risk_matrix:
  regulatory: 无
  technological: 无——冒号空命令（:）和参数展开语法是 POSIX 标准特性，已在所有主流 Shell（Bash、Zsh、Dash 等）中稳定实现数十年，无被替代或废弃的风险
  competitive: 无——本文不涉及市场竞争格局变化
  ethical: 无
  additional:
  - '代码可读性风险：过度使用冒号缩写语法（如 : "${1:?error}"）可能降低脚本可读性，经验不足的维护者可能难以理解其含义，应在团队编码规范中明确使用场景并配合注释'
  - 误用风险：对 set -u 模式下 ${var:?err} 的理解不足可能导致生产环境脚本意外退出，需在培训中强调其行为语义（未设置或为空时直接退出）
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: monitor
---

## In a land far-far away..

... there was once a far too cold cup of coffee next to a freshly brewed far-too-hot one. Four different terminals where three could have been closed an hour ago, and a shell script which I really (really) did not want to write.

Who would have thought a single colon would be the one to save the night?`day`



Note: Want your mind blown straight away? See more colons in the limelight.

## Checking for required arguments

This is a familiar dance, it's pretty much muscle memory by this point. You have a script, it takes a few arguments, and some of them are mandatory; alright, an if-statement like so many times before:

```
if [ -z "$1" ]; then
echo "missing argument, aborting." 1>&2
exit 1
fi
echo "Hello $1!"
```


Though.. what if I told you the above four lines could be replaced by just... one?

```
: "${1:?missing argument, aborting.}"
echo "Hello $1!"
```


```
$ bash example.sh
example.sh: line 1: 1: missing argument, aborting.
$ bash example.sh refp
Hello refp!
```


And look what happens if we refer to a variable with a proper name — it's the same behavior as previously but easier to spot; the diagnostic includes the name of our variable!

```
: "${GREET_NAME:?missing argument, aborting.}"
echo "Hello $GREET_NAME!"
```


```
$ bash greet.sh
greet.sh: line 1: GREET_NAME: missing argument, aborting.
```


###
Parameter expansion and the story of `:?`


There are two things going on in the previous snippet, and you are correct in identifying that one part is using parameter expansion:

The syntax

`${name:?diagnostic}`

checks whether`$name`

is unset or empty — if it is, the diagnostic is printed to*stderr*and the shell exits with a non-zero status, otherwise;if the variable is set, it is equivalent to

`$name`

.

## That.. other colon

So that's one colon, but what about that other one, the one who sits alone at the beginning of the line?

`:`

is the null-command — a builtin that does nothing but evaluate its arguments and discard the result.`:`

is old — it goes all the way back to the 1971 Thompson shell where it doubled as a label and Unix's very first comment marker.`:`

two eyes staring at you in the dark, with love.

## More colons in the limelight

Perhaps we have already established that there is more to `:`

than meets the
eye, but to prove the real magic of the null-command —
here are a few usages that blew my mind.

```
: "${DATA_DIR:=/var/data}" # set defaults, : swallows the result
: "${RETRIES:=3}" # instead of running it as a command
```


```
: > error.log # truncate error.log
: > error.log > access.log # truncate both error.log and access.log
```


```
( : < dataset.json ) && echo YES # is dataset.json readable?
( : >> result.json ) && echo YES # is result.json writable?
```


```
trap : INT # trap requires a command
sleep 60 # sleep is interruptible
```


```
set -u # error on unset variables
: "$DEPLOY_ENV" "$HOST" # check DEPLOY_ENV and HOST
```


```
if some-command; then
: # command required
else
echo "command failed"
fi
```


## Con-colon-sion

So, if you are like me and prefer less typing (gotta go fast) — the null-command and parameter expansion are a pair worth studying before your coffee goes cold.

And also.. isn't this — magic?

```
set : : : : : : : : : : : : : : : : : : : : :
while : colons are more than "${1:?magic}"; do
echo "$*" && shift
done
```



Note: The above example is safe to run locally, try it!

## Frequently Asked Questions

After reading a few comments online, it seems I skipped over some things worth explaining. I will keep this section updated as questions come up.

### Why do I need the null-command? Doesn't the expansion happen without the colon?

The parameter expansion will happen regardless, but without a null-command or similar usage the shell will treat the resulting string as a command to run.

`% ${HELLO:=123} zsh: command not found: 123`

If we prefix our parameter-expansion with the null-command, the result is discarded, but the expression is still evaluated (setting

`HELLO`

to`123`

).`% : ${HELLO:=123} % echo $HELLO 123`

### Why use the null-command when I could do

`VAR=${VAR:-default-value}`

?This at its core boils down to personal preference, but using our beloved colon we can shrink the number of potential typos to one (rather than two):

`: "${DATA_DIR:=/var/data}" # <- DATA_DIR mentioned once (1) DATA_DIR="${DATA_DRI:-/var/data}" # <- oops (2)`