---
title: How Do I Profile eBPF Code?
source: https://naveensrinivasan.com/posts/2026-07-22-how-do-i-profile-ebpf-code/
author:
- '[[snaveen]]'
published: '2026-07-28'
created: '2026-07-28'
manifest_dates:
- '2026-07-28'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 46039e20491ddc88
source_type: community_discussion
tldr: 这篇技术博客演示了测量 eBPF 代码性能开销的完整方法：以文件打开 LSM hook 为例，用最小 C 基准程序配合 perf 采样与火焰图，定位到
  bpf_lsm_file_open 尾调用链为性能瓶颈。
objective_summary: 作者 Naveen Srinivasan 于 2026 年 7 月发表博客，演示了如何剖析 eBPF 代码的性能影响。他针对文件打开操作的
  LSM hook，编写了基于裸 syscall(SYS_openat) 的最小 C 基准程序，通过 sysctl 开启 BPF JIT 符号解析，用 taskset
  固定 CPU 与 chrt 高优先级运行基准，并使用 perf record 采样内核态 CPU 周期生成火焰图。结果显示 bpf_lsm_file_open
  及其尾调用链（path_check_callback、bpf_probe_read_kernel 等）处于热路径，是主要性能瓶颈。文章强调重点是剖析方法而非具体数值。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - eBPF
  - LSM
  - BPF JIT
  - perf
  - bpftool
  - VFS
  key_people:
  - Naveen Srinivasan
key_logic_flow:
- 作者以文件打开操作的 LSM hook 为例，说明测量 eBPF 代码性能开销的目标是量化 hook 附加后引入的额外耗时。
- 编写了一个最小依赖的 C 基准程序，使用裸 syscall 调用 SYS_openat 重复打开同一文件，预分配并锁定内存以避免缺页干扰，并丢弃前 10% 结果作为预热。
- 通过 sysctl 开启 bpf_jit_enable 与 bpf_jit_kallsyms，让 perf 工具能够解析 JIT 编译的 BPF 符号而非显示未知地址。
- 先用 taskset 固定 CPU 3 并配合 chrt 高优先级运行基准测量基线，再在 eBPF 运行下用 perf record 以 997Hz 采样内核态
  CPU 周期并生成火焰图。
- perf report 输出显示 bpf_lsm_file_open 及其尾调用链（path_check_callback、bpf_probe_read_kernel、htab_map_hash
  等）占据了 90% 左右的开销，属于热路径瓶颈。
- 通过对比无 eBPF 时的 p50/p99 与火焰图热点，可确定 eBPF hook 的性能影响并指导优化，例如引入缓存或改进算法。
object_mentions:
- object_type: project
  name: eBPF file-open benchmark harness
  canonical_name: eBPF file-open benchmark harness
  url: null
  confidence: low
  article_role: primary_subject
  evidence_snippets:
  - 作者编写了一个最小依赖的 C 测试基准，使用裸 syscall 调用 SYS_openat 重复打开同一文件，并在循环前预分配和锁定内存以避免缺页干扰。
  - 该基准程序丢弃前 10% 的结果作为预热期，其余 90000 个样本用于计算打开文件耗时的 p50 与 p99 分位数。
  article_id: 46039e20491ddc88
- object_type: project
  name: Inferno
  canonical_name: Inferno
  url: https://github.com/facebookincubator/inferno
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章中的火焰图由 Inferno 工具生成，用于展示 eBPF 程序在文件打开路径上的调用栈热点分布。
  article_id: 46039e20491ddc88
extract_result: success
impact_score:
  score: 2.5
  reason: 评分依据：这是一篇面向 eBPF 基础设施工程师的方法论技术博客，演示了量化 eBPF LSM hook 性能开销的完整剖析流程（最小 C 基准程序
    + BPF JIT 符号解析 + perf 采样 + 火焰图）。它属于工程实践分享，而非产品发布、融资或范式转移事件，对 AI 行业竞争格局没有短期冲击力。价值在于为
    AI 基础设施的可观测性与性能调优提供可复现的剖析方法论，影响范围主要限于 eBPF 开发者这个小众圈子，故评分 2.5。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: eBPF hook 性能开销的量化剖析方法，尤其是通过 sysctl 开启 bpf_jit_kallsyms 让 perf 能解析
    JIT 编译 BPF 符号的配置技巧
hype_assessment:
  level: low
  reason: 判定依据：全文未出现'颠覆''革命性'等 PR 滥用词汇，作者明确声明'本文聚焦剖析方法而非具体数值'，并给出了完整的基准程序源码、sysctl
    配置、perf 命令和真实的火焰图/perf report 输出。结论来自实测数据而非概念包装，属于实打实的干货，故炒作指数为低。
information_entropy: high
domain_disruption:
  technical_innovation: 无本质技术突破，是对 perf、火焰图、bpftool 等既有工具的工程化组合运用；核心技巧在于开启 bpf_jit_enable/bpf_jit_kallsyms
    使 perf 能解析 JIT 编译的 BPF 符号，从而将热点定位到具体 eBPF 程序及其尾调用链，填补了 eBPF hook 性能开销'黑盒'的可观测性空白。
  business_model: 无直接商业模式影响。可外推到 AI 基础设施层：eBPF 是 AI 平台中网络、安全与可观测性的关键使能技术，此类剖析方法能降低
    AI 平台性能调优与安全策略部署的工程成本。
engineering_complexity: production_ready
compound_value:
  score: 4.0
  reason: 该事件本身是一篇方法论博客，不直接构成产品或公司，短期商业价值有限。但 eBPF 正快速成为云原生可观测、安全、网络领域的通用内核底座，性能剖析与调优能力是
    eBPF 从实验室走向生产环境的必要使能技术。这篇以 LSM hook 为样本的剖析方法论（JIT 符号解析、perf 采样、火焰图定位热路径）会随 eBPF
    采用率上升而被反复引用和复用，对 Falco、Tetragon、Cilium 等 eBPF 工具链有长期参考价值，属于'细分赛道基础设施'的前置知识积累。但单一博客的边际贡献有限，且不构成护城河，需持续观察
    eBPF 生态的生产级落地，故评分落在 4 分区间。
value_capture_layer: cloud_platform
moat_impact: democratizes_access
key_beneficiaries:
- Cilium (Isovalent)
- Datadog
- Sysdig
- Falco (CNCF)
- Red Hat
competitive_casualty:
- 传统内核模块式安全代理
- 基于 sidecar 的传统服务网格代理
- 闭源性能剖析工具
market_opportunities:
- eBPF 性能剖析工具链存在产品化机会——随着 Cilium、Tetragon 等 eBPF 安全/可观测产品在生产环境普及，本文演示的 perf+火焰图+JIT
  符号解析方法论可沉淀为标准化的 eBPF hook 开销评测模板或 SaaS 诊断服务
- LSM/eBPF 热路径优化是云原生安全代理（如零信任文件访问控制）的核心竞争力，创业者可复用该方法定位 bpf_probe_read_kernel、哈希表查找等热点，通过引入缓存或算法改进打造性能差异化优势
- 对工程师个人而言，掌握 eBPF 性能剖析技能（perf + bpftool + 火焰图）是云原生可观测性/安全方向的稀缺能力，可结合 eBPF 开源社区生态转化为培训课程或技术咨询业务
risk_matrix:
  regulatory: eBPF 内核级监控在数据合规层面存在不确定性：以 LSM hook 捕获文件打开等系统调用行为会形成敏感访问记录，在 GDPR、等保等框架下需要额外的数据最小化、留存与审计设计；在金融、政务等受管制关键基础设施中部署需谨慎评估
  technological: 剖析方法高度依赖内核版本与特性（bpf_jit_kallsyms、BTF、perf 版本匹配），内核演进可能导致具体命令与方法失效；同时内核内置安全模块或可观测性标准化的推进可能削弱特定
    eBPF 方案的不可替代性
  competitive: eBPF 工具链赛道已被 Cilium/Isovalent、Datadog、Pixie 等大厂与成熟开源项目占据，新进入者在 profiling
    与可观测性工具方向面临生态挤压、免费开源替代与价格战压力
  ethical: 基于 LSM hook 的细粒度文件访问监控存在隐私风险：可被用于追踪用户行为与文件使用模式，在多租户或员工环境部署可能构成监视，需要在透明告知与数据最小化之间取得平衡
  additional:
  - 开启 bpf_jit_kallsyms 会在 /proc/kallsyms 暴露 JIT 编译的 BPF 符号，扩大内核信息暴露面，存在辅助内核漏洞利用的信息泄露风险
confidence:
  impact: medium
  compound: medium
  hype: high
actionable_insight: monitor
object_insights:
- object_type: project
  name: Inferno
  canonical_name: Inferno
  url: https://github.com/facebookincubator/inferno
  positioning: Meta 开源的高性能火焰图生成工具，将 perf 采样数据渲染为可视化调用栈，用于定位 eBPF 等内核态代码的性能热点。
  technical_signal: 文章演示了将 perf record 采样数据交给 Inferno 生成火焰图的完整流程，证明其能清晰呈现 bpf_lsm_file_open
    尾调用链热点。
  adoption_signal: 作者在 eBPF 性能剖析工作流中默认选用 Inferno 生成火焰图，反映其已成为开发者可视化调用栈的主流工具选择。
  ecosystem_relevance: Inferno 与 perf、BPF JIT 符号解析等工具链协同，补齐了 eBPF 程序热路径可视化分析的关键环节。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 相较于 Brendan Gregg 的 FlameGraph 脚本，Inferno 主打高性能渲染与现代化工程化能力，适合大规模剖析数据的快速可视化。
  watch_reason: eBPF 性能剖析正在成为内核与云原生监控的标配方法，而 Inferno 作为火焰图生成的事实标准工具，其演进直接关系到开发者定位热路径瓶颈的效率，值得持续跟踪。
  risk_notes:
  - 文章仅在 eBPF 剖析场景提及 Inferno，缺乏对其功能边界与维护活跃度的直接证据，跟踪价值可能有限。
  - Inferno 的使用依赖 perf 等外部工具生成的采样数据，其价值受限于上游剖析工具链的完善程度。
  score: 5.0
  article_ids:
  - 46039e20491ddc88
  evidence_snippets:
  - 文章中的火焰图由 Inferno 工具生成，用于展示 eBPF 程序在文件打开路径上的调用栈热点分布。
---

6 minutes

If we are running any eBPF workload or writing eBPF code, we want to measure its performance impact, and in this post we will demonstrate an example of how to do it.

In this example, our goal is to measure the performance of file open operations, one of the most critical functions in the OS. Our code used file open hooks in eBPF, we wanted to measure the performance overhead introduced by adding this hook.

To identify likely bottlenecks, we need a simple C test harness with the fewest dependencies, designed to measure file open performance.

```
#define _GNU_SOURCE
#include <fcntl.h>
#include <stdio.h>
#include <time.h>
#include <stdint.h>
#include <stdlib.h>
#include <unistd.h>
#include <sched.h>
#include <sys/mman.h>
#include <sys/syscall.h>
static inline uint64_t now_ns(void) {
struct timespec ts;
clock_gettime(CLOCK_MONOTONIC, &ts); /* VDSO, no syscall */
return (uint64_t)ts.tv_sec * 1000000000ull + ts.tv_nsec;
}
int main(int argc, char **argv) {
const char *path = argv[1];
uint64_t n = strtoull(argv[2], NULL, 10);
uint64_t warm = n / 10;
/* preallocate + prefault + lock: no page faults in the loop */
uint32_t *d = mmap(NULL, n * sizeof(uint32_t), PROT_READ|PROT_WRITE,
MAP_PRIVATE|MAP_ANONYMOUS|MAP_POPULATE, -1, 0);
mlock(d, n * sizeof(uint32_t));
for (uint64_t i = 0; i < n; i++) d[i] = 0; /* fault everything in */
for (uint64_t i = 0; i < n; i++) {
uint64_t t0 = now_ns();
long fd = syscall(SYS_openat, AT_FDCWD, path, O_RDONLY); /* raw, no libc wrapper */
uint64_t t1 = now_ns();
if (fd >= 0) close(fd);
d[i] = (uint32_t)(t1 - t0);
}
/* dump after the loop only */
for (uint64_t i = warm; i < n; i++) printf("%u\n", d[i]);
return 0;
}
```


The goal of the code example above is to keep it simple and reopen the same file under warm cache conditions, minimizing unrelated filesystem and disk I/O variability and helping identify the p50/p99. The code invokes `syscall(SYS_openat, …)`

instead of the libc `openat()`

wrapper, and the first 10% of the results are discarded as a warmup period.

This test harness would produce results of opening a file x number of times and how long it took to open. So we could use this to measure the before and after of when the eBPF hook attached.

### Setup

When profiling the eBPF code, we want perf tool to be able to resolve symbols so we can analyze where the issue is in our code. To do that, we have to run these commands.

```
sudo sysctl -w net.core.bpf_jit_enable=1
sudo sysctl -w net.core.bpf_jit_kallsyms=1
```


The above commands enable jit and expose jit-compiled BPF symbols so the perf report can display program names instead of unknown addresses.

To check whether the symbols appear in the `perf`

tool, run your eBPF code and use a command like this.

```
sudo bpftool prog show | rg -A4 ' lsm '
sudo rg 'bpf_prog_[0-9a-f]+_ '/proc/kallsyms | rg 'security|path|file|open'
```


In the above rg command, we are checking for lsm as we are measuring LSM hooks.

Also, we are using a custom kernel version, so perf for that kernel version is not in the standard path, and we have it installed in our example: `PERF=/usr/lib/linux-tools/6.8.0-134-generic/perf`


### Measuring

Now that we have set up all the necessary tools, the first step is to measure without the eBPF code running, and this is where using the above C code can help. We measure the code by opening the file /etc/hostname and piping the results to a file so that we can calculate the p50/p99.

```
sudo taskset -c 3 chrt -f 99 ./bench /etc/hostname 100000 > /tmp/samples.txt
```


The `taskset -c 3`

pins execution to `CPU 3`

, reducing CPU migration noise, and `chrt -f 99`

gives the benchmark extremely high CPU priority. It runs before almost all normal programs and keeps running until it finishes, blocks, or is interrupted. The C code discards the first 10%; the file should contain 90,000 samples.txt.

Next, run the eBPF code and execute something like this.

```
sudo $PERF record \
-g \
--call-graph fp \
-e cycles:k \
-F 997 \
-o ~/perf.data \
-- \
taskset -c 3 \
chrt -f 99 \
./bench /etc/hostname 200000 \
> /tmp/samples.txt
```


The `-g`

records the call stacks, `--call-graph fp`

unwinds stacks using frame pointers, and `-e`

samples CPU cycles in kernel mode only, which includes syscall, VFS, LSM, and eBPF execution and not userspace benchmark work. The `-F 997`

requests 997 samples per second, and the non-round frequency helps avoid periodic alignment.

After running the above, run this command to sort the data.

```
sudo "$PERF" report -i ~/perf.data --stdio --sort comm,dso,symbol > perf.txt
```


*Flamegraph of the same perf.data, generated with Inferno. The stack of interest here is bpf_lsm_file_open and everything above it.*

Here is an example output from perf.txt, which shows that the time being spent on bpf_lsm_file_open and its tail calls is where the performance bottleneck is. This turned out to be in a hot path, which meant every allocation-to-CPU cycle shaving will make a significant impact on the performance of the system.

```
|
| | | | | |–90.52%–do_dentry_open
| | | | | | |
| | | | | | --89.78%–bpf_lsm_file_open
| | | | | | |
| | | | | | --89.30%–0xffffffffc0288c18
| | | | | | |
| | | | | | |–87.40%–bpf_prog_b06f413955402a4b_tail_call_security_check
| | | | | | | |
| | | | | | | |–77.57%–bpf_prog_934361d723613c1c_enforce_access_policy
| | | | | | | | |
| | | | | | | | |–57.87%–bpf_prog_a0f18f4b0b140d77_path_check_callback
| | | | | | | | | |
| | | | | | | | | |–29.94%–bpf_probe_read_kernel
| | | | | | | | | | |
| | | | | | | | | | |–18.88%–copy_from_kernel_nofault
| | | | | | | | | | |
| | | | | | | | | | --4.99%–copy_from_kernel_nofault_allowed
| | | | | | | | | |
| | | | | | | | | |–4.88%–htab_map_hash
| | | | | | | | | |
| | | | | | | | | --2.29%–copy_from_kernel_nofault
```


This post focuses on the profiling method rather than a specific result, and the overhead you’ll see depends heavily on what your hook actually does, so we’re leaving the numbers out and focusing on how to get them yourself.

Now, from the above, we can start analyzing where the time is being spent and perf-tune the code along with the p50/p99 of the C code with eBPF running.

By doing this, we can clearly identify the perf impact of the eBPF code and likely pinpoint where optimizations are required. It can be as simple as caching something or coming up with a better algorithm based on where the problem is.