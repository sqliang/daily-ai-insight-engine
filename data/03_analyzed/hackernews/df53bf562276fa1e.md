---
title: Maximizing the value of your Claude Code sessions
source: https://claude.com/blog/maximizing-the-value-of-your-claude-code-sessions
author:
- '[[twapi]]'
published: '2026-08-14'
created: '2026-08-15'
manifest_dates:
- '2026-08-15'
description: 'Article URL: https://claude.com/blog/maximizing-the-value-of-your-claude-code-sessions
  Comments URL: https://news.ycombinator.com/item?id=49300800 Points: 217 # Comments:
  125'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: df53bf562276fa1e
source_type: community_discussion
tldr: Claude Code 官方博客详解如何高效利用每次会话的 token：推理成本由所选模型、输入与输出 token 比例以及提示缓存命中情况共同决定，并给出
  /clear、@提及文件、/compact、/rewind、子代理等降低上下文开销的实用技巧。
objective_summary: 这篇来自 claude.com 的官方技术博客介绍了 Claude Code 会话的成本模型与优化方法。文章指出按 token
  计费实际对应 GPU 推理时间，成本由模型大小、输入与输出 token 类型以及是否命中提示缓存三者决定，其中输出 token 价格约为输入的 5 倍，缓存读取仅需
  0.1 倍输入价格。为降低开销，官方建议在任务切换时使用 /clear、用 @ 提及文件省去 Read 调用、精简 CLAUDE.md 并改用按需加载的 skills、关闭不需要的
  MCP 服务器、在长会话中适时 /compact 或 /rewind，并利用子代理把额外工作隔离在主会话上下文之外。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Anthropic
  technologies:
  - Claude Code
  - MCP
  - prompt caching
  key_people: []
key_logic_flow:
- Claude Code 按 token 计费，用户实际支付的是 GPU 推理时间，成本由所选模型、输入或输出 token 类型以及是否命中提示缓存三方面决定。
- 请求依次经过 prefill 读入阶段和逐 token 生成的 decode 阶段，输出 token 价格约为输入的 5 倍，思考 token 数量由 /effort
  级别控制。
- 提示缓存由 Claude Code 自动管理，命中缓存的读取仅需 0.1 倍输入价格，但一旦切换模型或 effort、超时或恢复旧会话，前缀失效就会导致整段对话被重新全量
  prefill。
- 减少上下文占用的技巧包括任务间执行 /clear、用 @ 提及文件省去 Read 调用、精简 CLAUDE.md、关闭不需要的 MCP 服务器，以及为常用测试命令添加安静输出参数。
- /compact 会重写整个会话因而必然产生成本，/rewind 只截断末尾回合所以更便宜；/loop 会携带整个会话反复运行，应在另一个终端的新会话中启动。
- 子代理拥有独立上下文窗口，只把最终答案带回主会话，适合隔离额外工作，但对小任务而言子代理的重复读取只会带来额外开销。
object_mentions:
- object_type: product
  name: Claude Code
  canonical_name: Claude Code
  url: https://claude.com/claude-code
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Claude Code 是一个按 token 计费的智能编程工具，每次请求都会把系统提示、CLAUDE.md 和完整对话历史发送给模型。
  - 文章围绕如何让 Claude Code 会话更高效展开，给出了 /clear、/compact、@ 提及文件、子代理等一系列降低 token 开销的操作建议。
  article_id: df53bf562276fa1e
- object_type: model
  name: Fable 5
  canonical_name: Fable 5
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 文章提到在除 Fable 5 之外的模型上可以用 MAX_THINKING_TOKENS=0 关闭思考，暗示 Fable 5 是默认保留思考能力的 Claude
    模型。
  article_id: df53bf562276fa1e
extract_result: success
impact_score:
  score: 4.5
  reason: 官方首次系统披露 Claude Code 的 token 成本模型（输出约为输入 5 倍、缓存读取 0.1 倍、缓存写入最高 2 倍），并给出
    /clear、@提及文件、/compact、/rewind 与子代理隔离等可落地的成本优化手法，对重度用户和企业 AI 编码成本治理有直接指导价值。但文章属于方法论教育而非产品能力跃迁，不改变短期竞争格局，故评分居中偏上。
sentiment: positive
developer_sentiment:
  tone: neutral
  primary_focus: 提示缓存命中率与上下文管理如何决定最终的 token 账单
hype_assessment:
  level: low
  reason: 全文围绕定价机制与操作技巧展开，给出了 5x/0.1x/2x 等具体可验证数字以及明确的缓存失效场景，通篇没有'颠覆''革命'类 PR 措辞，属于官方技术博客的务实干货输出。
information_entropy: high
domain_disruption:
  technical_innovation: 文章未提出新技术，但把 agentic coding 场景下的成本工程机制系统化了：prefill/decode 两阶段推理定价、自动提示缓存及其前缀失效规则、子代理独立上下文窗口的隔离原理。这些透明化让开发者得以把
    token 预算当作可优化的工程变量来管理，属于操作认知层面的升级。
  business_model: 以成本透明度教育用户，为按 token 计费模式的正当性背书并降低用户流失；同时把'token 经济性'塑造为 Claude Code
    相对 Cursor/Copilot 的差异化卖点，推动 AI 编码工具竞争从功能比拼延伸到成本效率比拼。
engineering_complexity: production_ready
compound_value:
  score: 8.0
  reason: 表面是一篇 token 成本优化技术博客，实则是 Anthropic 将『Agent 会话经济性』确立为 Claude Code 核心竞争壁垒的战略信号。深层看，文章揭示了
    agentic coding 的价值链正在从『按席位付费』转向『按 token 计费』——输出 token 5 倍于输入、缓存读取 0.1 倍、前缀失效引发全量
    re-prefill，这些机制决定了单位任务成本；而 Claude Code 通过自动提示缓存管理、/compact 与 /rewind 的差异化成本结构、子代理上下文隔离，正在定义编码智能体的
    token 经济学标准。资本视角下，谁能把上下文编排做到极致，谁就掌握开发者工作流的默认入口，并伴随软件工程向 AI 原生开发迁移形成强复利——3-5 年后高效
    Agent 会话编排大概率仍是 AI 编码基础设施的基石。风险在于该机制可能被 OpenAI/Google 快速复制、领先地位并非不可动摇，且博客内容本身是教育性文章而非平台级架构发布，故给
    8 分而非更高。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- Anthropic
- NVIDIA
- MCP 生态开发者工具
competitive_casualty:
- OpenAI Codex
- Cursor
- 小型 Agent 编码初创公司
market_opportunities:
- 面向企业级 Claude Code 用户开发 token 成本监控与优化分析工具，自动识别提示缓存失效点并给出 /clear、/compact、@提及等可执行建议，帮助团队把推理成本可视化
- 将 Claude Code 上下文管理与成本优化方法论沉淀为团队最佳实践与培训服务，帮助企业在 Agent 编码工具规模化落地时建立 token 预算与治理机制
- 围绕提示缓存前缀稳定性设计开发者插件或 CI 集成（如自动清理会话、管理 MCP 服务器开关、精简 CLAUDE.md 加载策略），形成轻量级的效率工具产品
risk_matrix:
  regulatory: 无
  technological: 该优化技巧高度绑定 Claude Code 当前的提示缓存机制与按 token 计费模型，若 Anthropic 调整缓存策略、定价结构或转向更长原生上下文的架构，文中建议可能迅速过时；同时缓存感知的优化思路可能被长上下文模型或状态压缩等新范式部分取代
  competitive: GitHub Copilot、Cursor、OpenAI Codex 等竞品若推出更透明或更低的计费模式、更优的上下文复用能力，将挤压
    Claude Code 的成本竞争力；价格敏感型企业可能因此迁移工具链，导致 Anthropic 生态粘性下降
  ethical: 过度追求 token 节省（频繁 /clear、/compact、关闭思考、用子代理隔离上下文）可能丢失项目关键上下文，引发隐性代码质量下降；企业需警惕以成本控制换取工程质量的伦理与工程风险
  additional:
  - 成本可预测性风险：缓存前缀失效（切换模型或 effort、超时、恢复旧会话）会触发整段对话全量 prefill 导致成本尖峰，企业预算管控困难
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: product
  name: Claude Code
  canonical_name: Claude Code
  url: https://claude.com/claude-code
  positioning: Anthropic 推出的终端原生 AI 编程代理，按 token 计费，在开发者工作流中承担代码编写、测试修复与文件操作等任务，是官方主推的
    agentic coding 工具。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 软件开发者
  - AI 编程工具用户
  - 需要控制 token 成本的技术团队
  product_signal: 按 token 计费即按 GPU 推理时间付费，成本由所选模型、输入输出 token 类型与提示缓存命中共同决定，输出 token
    约为输入的 5 倍，缓存读取仅需 0.1 倍输入价格。
  market_signal: 文章揭示 agentic coding 工具正从固定收费转向按用量计费模式，官方通过成本透明化与效率优化持续运营该产品，反映 AI
    编程工具定价范式的迁移。
  differentiation: 与编辑器固定收费不同，Claude Code 将单次任务的 token 成本显性化，并通过 /clear、/compact、/rewind
    与子代理机制让开发者自主控制上下文开销，形成按用量计费与精细优化的差异化定位。
  watch_reason: Claude Code 是 Anthropic 在 agentic coding 领域的主推产品，其成本模型与提示缓存机制直接影响开发者日常使用开销；官方持续优化
    token 效率并公开计费细节，预示该方向将成为 AI 编程工具竞争的关键差异化点，值得持续跟踪其定价与功能演进。
  risk_notes:
  - 按 token 计费模式下，单次任务成本受会话上下文长度影响显著，长会话或恢复旧会话可能造成费用激增。
  - 提示缓存依赖请求前缀完全一致，切换模型、调整 effort 或修改 CLAUDE.md 都会导致前缀失效并触发全量重算，成本不可预测。
  - 该文为官方自我宣传，未与 Cursor、Copilot 等同类产品做成本与效率横向对比，结论存在立场偏差。
  score: 7.0
  article_ids:
  - df53bf562276fa1e
  evidence_snippets:
  - Claude Code 是一个按 token 计费的智能编程工具，每次请求都会把系统提示、CLAUDE.md 和完整对话历史发送给模型。
  - 文章围绕如何让 Claude Code 会话更高效展开，给出了 /clear、/compact、@ 提及文件、子代理等一系列降低 token 开销的操作建议。
---

# Maximizing the value of your Claude Code sessions

How to run efficient sessions that get the most value from every token.

`/clear `

between tasks.`/context`

`CLAUDE.md`

, MCP tool definitions), so you can cut out anything unnecessary.`/compact`

Until pretty recently, the tools you wrote code with were a flat fee (or free). Your editor cost the same whether you fixed one test or fifty that afternoon, so an individual task didn't really have a price of its own.

With agentic coding tools like Claude Code, it does. The same completed task can also cost different amounts depending on how you use it.

In one session, Claude reads the test and the file it covers, makes the edit, and is done in a handful of turns. In another, it greps around the repo first, reads a dozen files on its way to the same two, and every one of those turns also drags along everything else that's been read into the conversation since this morning.

It's the same fix, but you spent a different number of tokens on it, and the whole time the model was also having to think about ten files it didn't need.

Being efficient with tokens doesn't mean using fewer of them overall. It means making sure the ones you do use go towards the thing you actually asked for.

So let's look at what decides the price of a token, then what decides how many of them a session sends, and along the way, what that means for how you run a session.

You're billed per token, but what you're actually paying for is inference: the time it takes a GPU (or a TPU, or whatever the model happens to be running on) to run the model over your tokens.

Three things decide how much of that time a token takes: which model you're running, whether it's an input token (going in) or an output token (coming out), and whether it was cached.

A bigger model does more work on both input and output tokens. Which model is worth it for which kind of work is a topic on its own, and we covered it in *Choosing a Claude model and effort level in Claude Code*.

For this post, all you need to know is that everything else we're about to cover gets multiplied by the model's price: use a larger model when the problem is genuinely hard or ambiguous, and a smaller one when the work is routine.

A request goes through the GPU in two phases, and they cost different amounts.

First, during prefill, the model reads your request and context: the system prompt, your `CLAUDE.md`

, your message, and everything that's been added to the conversation since (the files Claude has read and the output of the commands it ran). Those are your input tokens.

Then, during decode, it writes output tokens: its thinking, the tool calls it makes, and the text you see. This happens one token at a time; a 200-token response is 200 runs of the model, one after the other. Per token, decode keeps the GPU busy for a lot longer, which is why output is priced at roughly 5x input.

A lot of the output tokens in a session are thinking tokens, and how much thinking the model does per turn is what the effort level controls. Like the model, the level you pick with `/effort`

sticks around as your default for the next session too.

Tip:run`/model`

and`/effort`

once in a fresh session to see what you're actually on. Both remember whatever you picked last time, and you want that decision to be deliberate.

Tip:if you already know a session is going to be grunt work,`MAX_THINKING_TOKENS=0`

claude turns thinking off for that one session (except on Fable 5), which is the step below`/effort`

low.

If a request starts with exactly the same tokens as a request the server just saw, the state for that shared beginning comes out the same, so the server can keep it around from last time and only prefill whatever comes after it. This is called prompt caching.

Reading from the cache costs 0.1x the input price, because the server loads the state instead of computing it. Writing tokens into the cache costs a bit more than normal input, up to 2x, since the server also has to hold on to the state afterwards. But the write happens once per token, and the 0.1x reads happen on every turn after it.

Claude Code manages the prompt cache on every request, there's nothing to turn on. However you can break it, so it's important to know how to avoid these cost spikes.

Say we type "fix the failing test in `utils.test.ts`

". Here's what Claude Code sends for it:

`CLAUDE.md`

, and your message, and sends it off (input tokens). Nothing is in the cache yet, so all of it gets prefilled and written into the cache.`utils.test.ts`

(output tokens). Claude Code reads the file, appends it to the conversation, and sends the whole thing again (input tokens). This time everything from request 1 is read back out of the cache at a tenth of the price, and the only thing prefilled at full price is what's new: the Read call and the file.`npm test `

(output). Claude Code appends the test output and sends everything again, with the test output as the only new part (input).That's five requests for one small fix, and every one of them contained the entire conversation up to that point. A typical turn is lopsided: tens of thousands of tokens going in, a few hundred coming out. But only what's new in that turn gets prefilled at full price.

That's the whole per-turn bill: cache reads on the history, full input price on whatever's new, and the output price on the response.

This applies on a subscription too. You don't see these prices directly, but the same requests are what draw down your limits.

The cache has to match from the very start of the request forward, and requests always go out in the same order: tool definitions, then the system prompt, then the conversation (with `CLAUDE.md`

at the front of it).

If anything in that prefix changes, everything behind it gets prefilled again. A tool result appended to the end of the conversation is the ideal case, since nothing is behind it. What throws the cache away is anything that changes the request further towards the front, or changes what the cache is keyed on:

`/model`

`/effort:`

`/model `

and `/effort `

ask you to confirm when you switch in the middle of a conversation.`/compact`

`ENABLE_PROMPT_CACHING_1H=1 `

makes it an hour). Come back later than that, and the next turn prefills the whole conversation again. Resuming an old session almost always does too: the cache is usually gone by then, and the system prompt gets rebuilt at launch anyway.None of this means you should never switch models or effort. It means there are cheap moments to do it, the start of a session or right after a `/clear`

, and expensive ones, the middle of a long conversation.

Tip:if the last few turns went somewhere you don't want to keep,`/rewind`

to just before them instead of running`/compact`

. Rewinding only cuts those turns off the end, so everything before them is still cached and it costs nothing. Compacting rewrites the whole conversation, so it always costs something.

The main thing to know here is that nothing gets sent just once. Everything that ends up in the conversation, a file Claude read or the output of a command it ran, gets sent again on every turn after it, for the rest of the session.

It's cached, so each of those re-sends is cheap, but cheap isn't nothing, and it's taking up room in the context the model has to think around on every turn too.

That's really the whole cost model of a session: how many tokens end up in the context, how many turns they stay there, and how many contexts you're running at the same time.

Part of what's in the context is there before you type anything: the tool definitions, the system prompt, `CLAUDE.md`

, and whatever else gets loaded at startup.

Tip: run`/context`

in a fresh session to see what's in there before you've typed anything. Keep`CLAUDE.md`

to specific instructions and move workflow-specific ones into skills, which only get loaded when they're used. If there's an MCP server you don't need in this session, turn it off with`/mcp`

.

Nearly everything else that gets added during the session is tool results: the files Claude reads, and the output of the commands it runs.

How much Claude reads mostly comes down to how much it has to figure out on its own. If you say "the tests are failing", it first has to find out which tests: a grep or two, a few files opened to see which one is relevant, and all of those results stay in the context long after they've stopped being useful.

"Fix the failing test in `utils.test.ts`

" skips the searching and costs one Read call for the file, and "Fix the failing test in `@utils.test.ts`

" doesn't cost the Read call either.

Tip:when you're referring to a file, @-mention it instead of typing the path. Claude Code attaches the file to your message before anything gets sent, so it's in the very first request and there's no Read call for it. The file itself takes up the same room in the context either way, so you only need to mention it once per conversation: it stays there, and @-mentioning it again on a later turn generally attaches a second copy.

The other thing that fills up the context is the output of the commands Claude runs. Every time it runs your tests, a build, or a git log, whatever that prints gets appended to the conversation just like a file it read, and stays there for the same number of turns.

Really big outputs are actually fine: after 30,000 characters Claude Code writes the output to a file and only puts a short preview and the path in the conversation (`BASH_MAX_OUTPUT_LENGTH`

if you want to change it).

The problem is everything under that. A test runner that prints 400 passing tests one line at a time comes in under the limit, and those 400 lines are now part of every remaining turn.

Claude will often take care of this for you with flags and tail, and if you'd rather not leave it up to Claude, there's a small hook in the docs that rewrites noisy commands before they run so only the lines that matter come back.

Tip: put the two or three commands you run all day in`CLAUDE.md`

, quiet flags included, the way you'd type them yourself ("run a single test file with`npx vitest run <file> --reporter=dot"`

). It's a small addition, but it saves a turn and a few hundred lines of output in every session after it.

One long session costs more than the same work spread over a few short ones, and by more than you'd think, because turn 40 is also re-reading the 39 turns before it. You want the context in your session to be short and relevant, so don't carry one task's context into the next: `/clear `

when you start something new, and `/compact`

when the earlier part of the same task is done.

Tip:`/rename`

before you`/clear`

if you'll want the session back later. When you`/compact`

, tell it what to keep, or put a "Compact instructions" section in`CLAUDE.md`

if it's always the same thing. And if you're on a 1M model and would rather have the auto-compact safety net where it used to be,`/autocompact 200k`

puts it back (needs Claude Code v2.1.221+).

Keep an eye on turns that happen when you're not typing, too. A `/loop`

fires as a full turn in the session you set it up in, carrying that whole conversation with it every time, and if it's been more than an hour since the last turn, it's a cache miss on top. Start a fresh session in another terminal and run the loop from there.

The other way to keep something out of your context is to have it happen in a different one, which is what subagents are for. A subagent gets its own context window, with its own system prompt, the tools, and your `CLAUDE.md`

, but not your conversation. It runs its own turns, and the only thing that comes back to the main session is its answer. Everything else is thrown away once it's done.

The downside of not having your conversation is that a subagent sometimes has to re-read things the main session already had, and it's paying for its own turns while it does. For a small job it's just overhead.

It pays off when a job produces a lot of output you don't need to keep, like going through a log. Claude will often reach for one on its own for that kind of thing, and you can ask for one directly when it doesn't ("go through this log in a subagent"). Just keep in mind that the main session only gets back what the subagent chose to report.

Tip: if there's a noisy job you hand off over and over, give it a subagent definition of its own with model: haiku (or sonnet). Otherwise it runs on whatever your main session is running on.

Of everything above, four things are worth keeping an eye on, roughly in order of how much they cost:

Get the developer newsletter

Product updates, how-tos, community spotlights, and more. Delivered monthly to your inbox.