---
title: Fast drilldown dashboards from a single Parquet file
source: https://www.hamiltonulmer.com/customer-dashboards-r2-hyparquet/
author:
- '[[v3gas]]'
published: '2026-08-24'
created: '2026-08-24'
manifest_dates:
- '2026-08-24'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 7f90141c5d0f9071
source_type: community_discussion
tldr: 作者用 Parquet 数据立方体加浏览器端读取器 Hyparquet，在无需数据库和查询引擎的情况下实现了客户下钻仪表盘，并用纽约 311 数据集在
  R2 上验证了方案，40MB 文件中筛选仅需读取约 260KB。
objective_summary: MotherDuck 员工 Hamilton Ulmer 撰写技术博客，提出用单文件 Parquet 数据立方体加浏览器端读取器
  Hyparquet 构建客户分析仪表盘。他用纽约 311 数据集（约 3400 万行）构建 40MB 数据立方体，存放在 Cloudflare R2 上，通过范围请求按需读取匹配的
  row group 并在浏览器内聚合。演示证明点击 NYPD 仅读取约 260KB，方案不依赖数据库和查询引擎。作者还分析了 R2 的写成本经济性，并指出该方案适用于分布式与代数聚合场景。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - MotherDuck
  - Cloudflare
  - Cursor Origin
  - Amazon Web Services
  - GitHub
  technologies:
  - Parquet
  - Hyparquet
  - Iceberg
  - R2
  - S3
  - WAL
  - DuckDB-Wasm
  - PMTiles
  - SQLite
  key_people:
  - Hamilton Ulmer
  - Vicent Martí
key_logic_flow:
- 作者提出用 Parquet 数据立方体加 HTTP 范围请求的方式，在浏览器中实现无需数据库和查询引擎的下钻仪表盘。
- 作者用纽约 311 数据集（约 3400 万行）构建 40MB 数据立方体，包含机构、投诉类型、提交类型、行政区和时间列，并存储在 R2 上。
- Parquet 文件的 footer 记录各 row group 的字节范围及各列 min/max 统计值，客户端读取 footer 后只请求匹配的 row group
  并在浏览器内聚合。
- 分组集按过滤列排序使匹配行在文件中连续分布，点击 NYPD 时仅读取约 260KB 而非整个 40MB 文件。
- 该方案适用于求和、计数、最大值、平均值等分布式与代数聚合，整体聚合则需要精确或近似方案。
- 作者给出成本分析：1 万客户、10MB 立方体每月存储约 1.5 美元，五分钟级重建每月写入约 389 美元，并可用 Iceberg 快照差异只重建有变动的客户。
object_mentions:
- object_type: project
  name: Hyparquet
  canonical_name: Hyparquet
  url: https://github.com/hyparam/hyparquet
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Hyparquet 是一个运行在浏览器中的 JavaScript Parquet 读取器，体积仅 18kb，能直接执行范围扫描来替代庞大的 DuckDB-Wasm。
  - 借助 Hyparquet，作者得以在无数据库、无查询引擎的情况下直接提供真实的下钻仪表盘。
  article_id: 7f90141c5d0f9071
- object_type: project
  name: R2 + Hyparquet customer dashboard demo
  canonical_name: customer-dashboards-r2-hyparquet-demo
  url: null
  confidence: medium
  article_role: primary_subject
  evidence_snippets:
  - 作者将纽约 311 数据集（约 3400 万行）构建成 40MB 的 Parquet 数据立方体并放在 R2 上，演示仪表盘直接用 Hyparquet 读取该文件。
  - 演示中的字节通过一个 Cloudflare Worker 转发，因为免费的 r2.dev URL 存在速率限制。
  article_id: 7f90141c5d0f9071
- object_type: project
  name: PMTiles
  canonical_name: PMTiles
  url: https://github.com/protomaps/PMTiles
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - PMTiles 将瓦片集打包进单个文件，客户端通过 HTTP 范围请求读取，瓦片沿 Hilbert 曲线排布使相邻视图的瓦片聚集在文件相邻位置。
  article_id: 7f90141c5d0f9071
- object_type: project
  name: DuckDB
  canonical_name: DuckDB
  url: https://duckdb.org
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 作者提到每个客户约 10MB 的数据立方体可由一条 DuckDB 的 GROUP BY GROUPING SETS 语句生成。
  article_id: 7f90141c5d0f9071
- object_type: project
  name: DuckDB-Wasm
  canonical_name: DuckDB-Wasm
  url: https://github.com/duckdb/duckdb-wasm
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 作者通常会用 DuckDB-Wasm 处理这类 BI 负载，但最终选择体积仅 18kb 且无需专门 worker 的 Hyparquet。
  article_id: 7f90141c5d0f9071
- object_type: product
  name: MotherDuck
  canonical_name: MotherDuck
  url: https://motherduck.com
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 作者的客户朋友因不想增加供应商而排除了云托管 DuckDB 数据库 MotherDuck，作者本人则推荐用它来构建数据立方体管道。
  article_id: 7f90141c5d0f9071
- object_type: product
  name: Flights
  canonical_name: MotherDuck Flights
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Flights 是 MotherDuck 的一项功能，可调度 Python 作业来完成定期重建每个客户数据立方体的任务。
  article_id: 7f90141c5d0f9071
- object_type: product
  name: Cloudflare R2
  canonical_name: Cloudflare R2
  url: https://developers.cloudflare.com/r2/
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 数据立方体存储在 Cloudflare R2 上，R2 提供免费出口流量，因此写入成本成为管道的实际开销来源。
  article_id: 7f90141c5d0f9071
- object_type: project
  name: Apache Iceberg
  canonical_name: Apache Iceberg
  url: https://iceberg.apache.org/
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 朋友的使用数据存放在 R2 上的 Iceberg 中，Iceberg 快照差异可精确识别哪些客户有新数据，从而只重建有变动的立方体。
  article_id: 7f90141c5d0f9071
extract_result: success
impact_score:
  score: 6.0
  reason: 该方案提供了一套完整的参考实现，将查询引擎的工作前置到数据立方体构建阶段，用对象存储+HTTP范围请求+浏览器端 Parquet 读取器（Hyparquet）即可支撑交互式下钻仪表盘，在客户分析类场景下可将基础设施成本降低一到两个数量级（文中
    1 万客户每月存储成本约 1.5 美元）。但适用边界明确：图表/筛选的组合数必须受控，聚合需满足可分配性（求和/计数/均值等），且仍需自建数据管道维护立方体。短期内会显著影响数据分析基础设施的成本决策和架构选型讨论，属于重要技术模式验证，但尚未构成行业范式转移，故评分为
    6.0。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 免数据库与查询引擎、仅靠对象存储+范围请求即可提供分析服务的架构成本优势
hype_assessment:
  level: low
  reason: 作者行文克制，未使用'颠覆''革命性'等 PR 措辞，反而主动披露方案的硬性前提（组合爆炸需受控、仅支持可分配代数聚合、立方体构建管道才是真实成本所在），并给出详细的字节级文件布局表与成本核算。具备可复现的演示与完整技术论证，判定为实打实的干货，无概念炒作水分。
information_entropy: high
domain_disruption:
  technical_innovation: 核心突破在于把查询引擎的工作前移到数据立方体构建期：将多个分组集（grouping set）堆叠进单个 Parquet
    文件，并按筛选列排序使匹配行在文件中连续分布，客户端读取 footer 中的 row group 字节范围与列 min/max 统计后，仅通过范围请求拉取命中的
    row group 并在浏览器内聚合，从而在无数据库、无查询引擎的情况下实现低延迟下钻（点击 NYPD 仅读约 260KB/40MB）。
  business_model: 为 SaaS 产品内嵌客户使用分析功能提供了一种极低成本的基础设施范式：10MB 立方体、1 万客户每月存储约 1.5 美元，配合
    Iceberg 快照差异仅重建有变动的客户即可控制写入成本，可将'给客户看图表'从需要常驻数据库/分析引擎的重资产能力降为纯对象存储的轻量功能，有望改变分析类功能的定价与交付方式。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 该事件本质是一次技术范式验证：用对象存储 + Parquet 范围请求替代数据库和查询引擎来服务有界分析查询。其底层趋势（对象存储作为通用数据底座、'一切皆范围请求'）是持久且具复利效应的——与
    Cursor Origin 的 S3+WAL 思路同频，且随存储成本持续下降和带宽持续上升，该模式的性价比会不断改善，具备细分赛道基础设施的潜力。但就事件本身而言，Hyparquet
    是 18KB 的开源库、Parquet 是开放标准，技术本身无可防御性；方案仅覆盖有界的代数聚合场景（整体聚合仍需精确/近似方案），组合爆炸受限，且作者本职工作在
    MotherDuck，该思路对自家云数据库业务存在内在张力。因此更可能成为轻量嵌入式分析工具箱中的一个标准件，而非独立行业基石，需持续验证其是否被主流数据平台（DuckDB、Iceberg、对象存储厂商）吸纳为原生能力，故给予
    6 分。
value_capture_layer: cloud_platform
moat_impact: democratizes_access
key_beneficiaries:
- Cloudflare
- DuckDB
- AWS
- Hyparquet
competitive_casualty:
- DuckDB-Wasm（轻量下钻场景）
- 云数仓查询引擎（Snowflake/BigQuery 轻量分析场景）
- 嵌入式 BI 中间件
market_opportunities:
- SaaS 产品可基于 Parquet 数据立方体 + 浏览器端 HTTP 范围请求构建零数据库的客户用量分析仪表盘，大幅降低基础设施成本与供应商依赖，适合多租户用量/计费看板类场景
- 建议关注 Hyparquet 等轻量浏览器端列式读取器的生态工具机会，例如面向嵌入式 BI 的自动 cube 生成流水线、页面级微前端图表组件，以及配合 Iceberg
  快照差量的增量重建工具
- 该模式可迁移至 AI 应用可观测性领域：将 token 用量、模型调用成本、请求延迟等指标预聚合成单文件立方体，实现极低成本的自助式运营分析面板，并与现有 Agent/LLM
  应用监控体系结合
risk_matrix:
  regulatory: 将客户数据放入对象存储并通过 Worker/CDN 提供多租户访问时，需谨慎处理数据驻留与隐私合规（如 GDPR、中国个保法），建议在访问层做严格租户鉴权、行级权限与脱敏，避免数据立方体间接泄露客户明细
  technological: 方案仅适用于求和/计数/极值/均值等可分布式聚合，中位数/分位数等整体聚合需近似方案；高度依赖 row group 排序布局与 Hyparquet
    这一小众开源库的持续维护，若文件布局失当或库失维护，查询性能与可用性会急剧退化
  competitive: 该模式直接绕开数据库与查询引擎，与作者所在公司 MotherDuck 及 DuckDB-Wasm、托管 BI 厂商形成定位冲突；若主流云厂商或
    BI 巨头将类似能力内化为平台默认服务，独立工具与中间件的空间可能被快速挤压
  ethical: 客户使用数据按机构、投诉类型等维度下钻展示，若鉴权与行级权限缺失，存在跨租户数据泄露与用户行为画像被滥用的风险；应遵循数据最小化原则并限制可下钻的敏感维度
  additional:
  - 数据管线才是真实成本与技能门槛所在：预聚合成立方体需要可维护的 ETL/重建流程（文中估算分钟级重建每月约 389 美元），小团队可能严重低估这部分工程与运维成本
  - 存在对单一对象存储（R2）能力与费率结构的依赖，跨云迁移、S3 兼容性差异及成本核算需在采用前提前规划
confidence:
  impact: medium
  compound: medium
  hype: high
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: Hyparquet
  canonical_name: Hyparquet
  url: https://github.com/hyparam/hyparquet
  positioning: Hyparquet 是运行在浏览器中的轻量级 JavaScript Parquet 读取器，体积仅 18kb，可替代 DuckDB-Wasm
    实现无数据库的数据分析。
  technical_signal: Hyparquet 体积仅 18kb，能直接执行 Parquet 行组的范围扫描，利用 footer 元数据与 min/max
    统计信息实现按需读取。
  adoption_signal: 作者用它构建纽约 311 数据集演示仪表盘，点击 NYPD 时仅读取约 260KB 即可完成下钻，验证了浏览器端数据立方体方案可行性。
  ecosystem_relevance: 作为浏览器端 Parquet 生态组件，Hyparquet 与对象存储、HTTP 范围请求和 R2 结合，为无数据库分析场景提供轻量级新选择。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Hyparquet 以 18kb 体积在浏览器中完成 Parquet 范围扫描，使无数据库下钻仪表盘成为现实，其方案有望在对象存储分析领域形成轻量级技术路线，值得持续跟踪其能力边界与生态演进。
  risk_notes:
  - 浏览器端聚合受 JavaScript 单线程性能限制，复杂分析场景可能仍需查询引擎。
  - 数据立方体依赖预计算分组集，图表与筛选组合增多时文件体积和构建成本会显著上升。
  score: 8.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - Hyparquet 是一个运行在浏览器中的 JavaScript Parquet 读取器，体积仅 18kb，能直接执行范围扫描来替代庞大的 DuckDB-Wasm。
  - 借助 Hyparquet，作者得以在无数据库、无查询引擎的情况下直接提供真实的下钻仪表盘。
- object_type: project
  name: R2 + Hyparquet customer dashboard demo
  canonical_name: customer-dashboards-r2-hyparquet-demo
  url: null
  positioning: 将纽约 311 数据集约 3400 万行构建为 40MB Parquet 数据立方体并存放于 R2 的客户下钻仪表盘演示，验证无数据库方案。
  technical_signal: 演示通过分组集按过滤列排序，使匹配行在文件中连续分布，点击 NYPD 时仅读取约 260KB 而非整个 40MB 文件。
  adoption_signal: 作者实测 40MB 数据立方体可直接在浏览器中读取，数据加载速度超出预期，且嵌入文章不影响页面加载性能。
  ecosystem_relevance: 该演示展示对象存储加 HTTP 范围请求的组合模式，与 PMTiles 等单文件数据分发方案一脉相承，为无服务器分析提供参考。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该演示完整验证了单文件 Parquet 数据立方体加浏览器端读取器构建客户分析仪表盘的可行性，同时揭示写成本与重建频率是关键约束，是对象存储分析路线的重要实证。
  risk_notes:
  - 演示字节通过 Cloudflare Worker 转发，免费 r2.dev URL 的速率限制会影响规模化使用。
  - 方案依赖预计算分组集，图表与筛选组合增多时立方体构建成本快速上升。
  score: 7.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - 作者将纽约 311 数据集（约 3400 万行）构建成 40MB 的 Parquet 数据立方体并放在 R2 上，演示仪表盘直接用 Hyparquet 读取该文件。
  - 演示中的字节通过一个 Cloudflare Worker 转发，因为免费的 r2.dev URL 存在速率限制。
- object_type: project
  name: PMTiles
  canonical_name: PMTiles
  url: https://github.com/protomaps/PMTiles
  positioning: PMTiles 将地图瓦片集打包进单个文件，客户端通过 HTTP 范围请求按需读取，是基于对象存储的单文件地图数据分发方案。
  technical_signal: PMTiles 将瓦片沿 Hilbert 曲线排布，使相邻视图的瓦片聚集在文件相邻位置，配合 HTTP 范围请求降低读取成本。
  adoption_signal: 在本文中作为作者设计 Parquet 数据立方体布局的灵感参照，其单文件加范围请求模式被迁移到分析仪表盘场景。
  ecosystem_relevance: PMTiles 与 Parquet 数据立方体共同体现对象存储时代的单文件分发趋势，为地理与分析数据提供一致的基础设施思路。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: PMTiles 是单文件加 HTTP 范围请求模式的代表性实现，其瓦片沿 Hilbert 曲线排布的布局思路被作者借鉴到分析仪表盘领域，反映出对象存储分析路线对相邻数据场景的跨域影响力。
  risk_notes:
  - 本文仅将其作为参考提及，缺乏对其自身技术演进与维护状态的直接证据。
  score: 3.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - PMTiles 将瓦片集打包进单个文件，客户端通过 HTTP 范围请求读取，瓦片沿 Hilbert 曲线排布使相邻视图的瓦片聚集在文件相邻位置。
- object_type: project
  name: DuckDB-Wasm
  canonical_name: DuckDB-Wasm
  url: https://github.com/duckdb/duckdb-wasm
  positioning: DuckDB-Wasm 是运行在浏览器中的 DuckDB 移植版，为 BI 负载提供完整 SQL 查询能力，但体积达数 MB 且需专门
    worker。
  technical_signal: DuckDB-Wasm 为 BI 类负载提供完整 SQL 查询能力，但体积达数 MB 且需专门 worker，在轻量级场景中被
    Hyparquet 取代。
  adoption_signal: 作者通常用它处理 BI 负载，作为浏览器端分析的默认选择，其存在说明完整数据库引擎在浏览器场景确有真实需求。
  ecosystem_relevance: DuckDB-Wasm 与 Hyparquet 构成浏览器端数据分析的两条技术路线，前者强调完整查询能力，后者强调极简体积。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: DuckDB-Wasm 是浏览器端数据分析的基准方案，其与 Hyparquet 在体积、查询能力与部署复杂度上的取舍对比，直接关系到无数据库下钻仪表盘这类轻量级应用场景的技术路线选择。
  risk_notes:
  - 本文仅将其作为对比参照提及，未提供关于其最新能力演进的独立证据。
  score: 4.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - 作者通常会用 DuckDB-Wasm 处理这类 BI 负载，但最终选择体积仅 18kb 且无需专门 worker 的 Hyparquet。
- object_type: product
  name: MotherDuck
  canonical_name: MotherDuck
  url: https://motherduck.com
  positioning: MotherDuck 是云托管 DuckDB 数据库服务，为分析场景提供托管数据库与数据立方体管道构建能力，本文作者在该公司任职。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 需要托管 DuckDB 数据库的分析团队
  - 希望减少自建数据库运维的数据产品团队
  product_signal: MotherDuck 可被推荐用于构建数据立方体管道，其 Flights 功能可调度 Python 作业定期重建客户数据立方体。
  market_signal: 客户因不想增加供应商而排除该产品，反映在客户分析场景中新增供应商是重要的采纳障碍。
  differentiation: 与无数据库的 Hyparquet 方案相比，MotherDuck 提供完整托管数据库体验，但供应商依赖使其在轻量级场景被规避。
  watch_reason: MotherDuck 作为 DuckDB 的商业化托管服务，其功能扩展如 Flights 与客户对新增供应商依赖的顾虑，共同揭示数据分析基础设施在托管数据库与无数据库轻量级路线之间的商业化权衡。
  risk_notes:
  - 客户因不想增加供应商而明确排除该产品，说明轻量级场景获客存在结构性障碍。
  score: 5.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - 作者的客户朋友因不想增加供应商而排除了云托管 DuckDB 数据库 MotherDuck，作者本人则推荐用它来构建数据立方体管道。
- object_type: product
  name: Flights
  canonical_name: MotherDuck Flights
  url: null
  positioning: Flights 是 MotherDuck 提供的作业调度功能，可运行定时 Python 作业，用于定期重建每个客户的数据立方体。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 需要定期重建数据立方体的分析工程师
  - 使用 MotherDuck 构建数据管道的数据团队
  product_signal: Flights 可调度 Python 作业完成数据立方体周期性重建，解决无数据库仪表盘方案中最关键的写端自动化问题。
  market_signal: 该功能被作者用于数据立方体管道构建场景，反映 MotherDuck 向数据工程自动化方向扩展产品能力。
  differentiation: Flights 聚焦托管环境下的定时作业编排，与通用任务调度器相比更贴近 DuckDB 数据管道的实际需求。
  watch_reason: Flights 作为数据立方体写端的自动化调度工具，其能力直接影响无数据库仪表盘方案的生产化可行性与数据更新频率，值得持续跟踪其功能演进及与
    MotherDuck 生态的协同。
  risk_notes:
  - 本文仅简要提及该功能，缺乏对 Flights 能力边界与独立使用的详细证据。
  score: 4.0
  article_ids:
  - 7f90141c5d0f9071
  evidence_snippets:
  - Flights 是 MotherDuck 的一项功能，可调度 Python 作业来完成定期重建每个客户数据立方体的任务。
---

Every month brings a new eruption of clever uses for object storage, easily the most volcanically active corner of non-AI software infrastructure on earth. The most recent lava bomb was Vicent Martí’s writeup of Cursor Origin’s S3 + WAL approach to managing Git repositories at scale. It’s a masterpiece of technical writing, unlike this post. I’ll admit that even before reading it, I was daydreaming about a totally different kind of task where object storage probably just *works*, this time for customer-facing analytics dashboards. A friend of mine has customer usage data in Iceberg on R2, and wants to show his users some basic charts with filters. He told me he didn’t want to add any more vendors, which ruled out MotherDuck, the cloud-hosted DuckDB database company where I work.

Well, in analytics, when all you have is object storage, everything looks like a range request. We could probably just roll this kind of data up into a Parquet-backed data cube, and use range queries to pull the aggregate rows we need for the dashboard. Normally I’d turn to DuckDB-Wasm for this kind of BI-shaped workload, but then I realized that Hyparquet, a javascript Parquet reader that runs in the browser, can do the same range scans in 18kb rather than multiple mb and a dedicated worker. *With that, you can serve a real drilldown dashboard with neither a database nor a query engine.* The cube could even be tens (or hundreds) of MB, since a correctly laid-out file means you only ever read a few small slices of it at a time. You just need a data pipeline to produce the cubes ~ which is also, it turns out, where all the actual money goes when you *do* have a real analytical database.

The heresy was too good to pass up. To test it, I took the well-known NYC 311 service requests dataset I had on my computer ~ about 34 million rows at the request level, 15 or so years ~ and rolled it up into a 40MB Parquet cube with filters for city agency, complaint type, submission type, and borough, plus a single creation-time column for the time series. Then I stuck it on R2. 40MB is big enough to feel the pain of downloading the whole thing.

The demo dashboard below reads directly from that file using Hyparquet. The bytes pass through a small Cloudflare Worker on the way, because the free r2.dev URL is rate-limited. To be honest, I was surprised how fast new data loads, given that it forgoes both a real database and a powerful query engine. The UI does all of the actual reading, and it is lightweight enough to embed directly in this post without hurting the page load. The real complexity is almost entirely offloaded to the data cube layout. Try scrubbing the chart or clicking on the rows of the leaderboards.

So, how does this dashboard work?

A dashboard like this one is designed to answer a bounded set of analytical questions ~ requests per day, requests per day for one agency, all-time totals by borough. Each question can be answered by `GROUP BY`

queries, so we can precompute them all ahead of time and save each result as its own small table, called a *grouping set*. Stack all of the grouping sets in one Parquet file, one section per set, and you have a *data cube*. A grouping set is only useful if it either enables a question to be answered, or reduces the latency of pulling the data. This file has both kinds. The all-time totals feed the leaderboards, and a daily grouping set for every combination of filters provides the data for the line chart. The weekly and yearly grouping sets reduce the number of rows scanned that results from brushing the chart. The same totals *could* be summed from daily rows, but there are fewer rows to fetch if we precompute by weeks and years.

The file now holds the grouping sets that render the dashboard, but the browser still has to pull out just the rows it needs. Two features of the Parquet format make that possible. A Parquet file is divided into *row groups* of a few tens of thousands of rows, and it ends with a footer that contains metadata about the byte ranges of row groups and the min/max values of each column inside it. The client reads the footer once. Each query then uses the min/max values to pick the row groups that could match, fetches those byte ranges, and aggregates the rows in the browser. Another way to put it is that the query engine’s work gets done while creating the data cube, so serving data from it can be done with much simpler technology.

The low latency in the dashboard requests is due to how the rows in the Parquet file are sorted and scanned. If the rows of the file were randomly ordered, each row group’s min/max values would span nearly the full range of each column, and a query would have to read most of the file just to fetch a small percentage of rows. Instead, the rows of each grouping set are sorted by the columns its queries filter on. The matching rows thus usually make up a contiguous stretch of the file, and the min/max statistics enable the reader to ignore the rest of the row groups. That is why clicking NYPD in the agency leaderboard reads about 260KB out of the 40MB file rather than the whole file. Smaller row groups would reduce the scanned size at the cost of a bigger footer, and Parquet has finer tools still like page indexes and bloom filters. Probably not worth the trouble at this scale. Below is the actual layout of the file in terms of bytes and grouping sets:

| grouping set | rows | size |
|---|---|---|
831.1k | 1.7mb | |
4.8k | 103kb | |
796.6k | 1.3mb | |
29.7k | 272kb | |
5.0k | 171kb | |
770.3k | 2.4mb | |
22.7k | 171kb | |
30.0k | 330kb | |
635.6k | 1.5mb | |
82.0k | 383kb | |
4.5m | 10.6mb | |
127.7k | 401kb | |
1.1m | 2.6mb | |
2.1m | 4.5mb | |
198.0k | 640kb | |
358.5k | 997kb | |
643.0k | 1.5mb | |
7.3m | 15.8mb | |
3.3m | 6.8mb | |
804.0k | 1.9mb | |
1.1m | 2.4mb | |
2.1m | 4.6mb | |
3.3m | 6.6mb | |
— | 195kb |

This setup works under two conditions. The combinatorics of your charts and filters have to stay small, and your pipeline has to rebuild each customer’s file fast enough to meet the update cadence. Most usage and billing pages meet both. They are a fixed set of charts ~ events over time, counts or sums by hour or by day, a few filters or leaderboards ~ over data that updates on a coarse schedule rather than in realtime, for their sake as much as yours. From the perspective of latency, the cube size doesn’t matter, but you’ll want it to be somewhat small anyway since you’re regenerating one per customer on a schedule.

The time grain is clearly dominant in my example, since the daily sections account for most of the bytes of the file. Cardinality is the other multiplier ~ complaint type has 485 distinct values, and every large section in the diagram contains it. In fact, choosing a daily grain for the line chart made the file about 7x larger than the weekly equivalent (5.6mb). Still, the daily grain did not meaningfully impact the latency of the range requests, since any interaction only ever reads a few row groups. And for this case, it’s nice to see a big single-day spike, since a big uptick in service requests can happen in a single day because of major events like hurricanes or blizzards.

Dashboards such as the one above work well for *distributive* and *algebraic* aggregations, which can be computed in pieces and then combined before visualizing. Think of sums, counts, maxes, and averages. Making this setup work for *holistic* aggregations (ones that require knowledge of the distribution before achieving a final filtered aggregate) have both exact and approximate solutions. I’ll leave that as an exercise to the reader and their favorite agent.

Range requests over a carefully laid-out file have plenty of prior art. PMTiles packs a tileset into one file that clients read via range requests over http. It works because the tiles are laid out in the file along a Hilbert curve, so the tiles for a given map view sit near each other in the file and can be fetched in a few coalesced range requests. And of course, the well-known SQLite-over-HTTP writeup proved the mechanic works even for B-trees. The only real requirement is a host that serves byte ranges over http ~ R2, S3, GitHub Pages, a plain nginx box.

My favorite part is that this approach shifts the complexity “left” all the way to the data pipeline. The layout is decided beforehand, so by the time a user clicks on a leaderboard or scrubs a time series chart, the client only has to fetch the right rows and sum them. As for the pipeline, for most customer-facing dashboards, a 10mb per-customer cube falls out of a DuckDB `GROUP BY GROUPING SETS`

statement. Naturally I’m biased, but if I were building the pipeline, I would go with MotherDuck (a cloud-hosted DuckDB database & query engine) and Flights (a MotherDuck feature where you can schedule python jobs for this kind of stuff). The setup is pretty straightforward with your agent of choice. Regardless of how you do it, for my friend, who’s a data engineer, pushing the complexity to the pipeline is pitch-perfect *déformation professionnelle*.

One file per customer also makes auth refreshingly boring. Access control amounts to a signed URL for that customer’s file, or a tiny Worker that checks the session.

Given that R2 has free egress, the pipeline is also the thing that costs actual money. Writes cost $4.50 per million (12.5x the price of reads), and you pay one write per customer per rebuild, regardless of file size (a 1MB cube and a 40MB cube cost the same to upload). Take 10,000 customers. Each rebuild replaces the files, so storage is flat: 10MB cubes make 100GB, about $1.50/month; 40MB cubes make 400GB, about $6/month. Rebuilding every file once a day is 300k writes a month, or about $1.35; rebuilding hourly is 7.2M writes, about $32; rebuilding every five minutes for a month is 86M writes, about $389. Thankfully, Iceberg snapshot diffs tell you exactly which customers have new data, so it’s easy to only rebuild the cubes with new activity.

Even still, let’s say you update every 5 minutes and every customer has activity in that window (again, not very likely). For a single use-case like this one, $389/mo for 10k customers is probably cheaper across the board than standing up new infra, and it is almost certainly simpler. And both the economics and the user experience have changed very recently: egress fees wouldn’t have quite killed this idea, but they’ve probably discouraged people from experimenting this way. The same setup on S3 comes out only about 20% more expensive overall ~ roughly $20 a month of egress at a million queries, and a million queries is more traffic than most customer dashboards will ever see.

My *other* favorite part is the radically thin implementation. An 18kb javascript reader and a byte layout that does the database work for you. What a world!