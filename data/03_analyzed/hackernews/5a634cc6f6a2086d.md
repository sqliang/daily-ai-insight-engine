---
title: Jim's TrueType QR Code Font
source: https://github.com/jimparis/qr-font
author:
- '[[arantius]]'
published: '2026-07-07'
created: '2026-07-08'
description: 'https://qr.jim.sh/ Comments URL: https://news.ycombinator.com/item?id=48820119
  Points: 178 # Comments: 22'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 5a634cc6f6a2086d
manifest_dates:
- '2026-07-08'
source_type: community_discussion
tldr: 生成一种实验性 OpenType 字体，可将方括号内的文本实时渲染为 QR 码。
objective_summary: Jim Paris 发布了一个名为 QR Font 的开源项目，该项目通过 OpenType GSUB 特性生成 TrueType
  字体，使方括号分隔的文本在普通排版环境中直接显示为 QR 码。字体提供 1-L、2-L、3-L 三个版本，分别支持最多 17、32、53 个字符。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - QR Code
  - OpenType
  - TrueType
  - GSUB
  - Reed-Solomon
  key_people:
  - Jim Paris
key_logic_flow:
- 该项目生成一种实验性的 OpenType 字体，能将方括号括起来的文本在排版时实时转换为 QR 码符号，同时保持周围普通文本正常显示。
- 字体通过 Python 构建脚本自动生成，内部实现了方括号解析器、字节扩展、Reed-Solomon 纠错电路、QR 模块布局以及固定掩码模式 0 的渲染。
- 字体提供三个版本（QR Font 1-L、2-L、3-L），分别支持 21×21、25×25、29×29 的模块矩阵，最大可编码 17、32、53 个可打印 ASCII
  字符。
- 非 QR 码区域的文本字符复制自 Liberation Sans Regular 字体，经过缩放后嵌入同一字体的 em square 中，保证外观一致性。
- 该项目使用 OpenType GSUB（字形替换）特性实现 QR 码生成逻辑，因此依赖支持 OpenType 排版的渲染环境才能正常工作。
extract_result: success
impact_score:
  score: 1.5
  reason: 该项目是一个个人开源实验项目，通过OpenType GSUB特性将方括号文本实时渲染为QR码。虽然实现手法巧妙（在字体层面嵌入Reed-Solomon纠错电路和QR模块布局），但适用范围极其有限：仅支持固定掩码模式0、仅ASCII字符、依赖支持OpenType排版的渲染环境、最大仅53字符。对AI行业无任何直接或间接冲击力，属于技术兴趣层面的'小圈子自嗨'项目。
sentiment: neutral
developer_sentiment:
  tone: excited
  primary_focus: 字体引擎作为通用计算载体的创造性滥用，而非实用价值
hype_assessment:
  level: low
  reason: 项目自述为'experimental proof-of-concept font'，README中明确列出所有功能限制（最大53字符、固定掩码模式、仅ASCII），没有任何'颠覆'、'革命性'等PR夸大词汇。这是一个诚实且边界清晰的技术实验。
information_entropy: medium
domain_disruption:
  technical_innovation: 利用OpenType GSUB字形替换特性，在字体渲染管线中实时编码QR码符号，将Reed-Solomon纠错电路和QR模块布局编译为数千条上下文规则表。这是对现有字体引擎能力的创造性越界使用，挑战了'字体只做字形渲染'的传统假设，但本质是hack而非技术突破。
  business_model: 无
engineering_complexity: prototype
compound_value:
  score: 2.0
  reason: 该项目的核心是一个实验性的单次技巧型创新（OpenType GSUB 字体渲染 QR 码），本质上是'字体层面的编码器'。虽然技术上有巧思，但无网络效应、无可扩展的商业模式、无数据飞轮，且受限于
    OpenType 渲染环境（非所有平台支持 GSUB），用户迁移成本极低。市场天花板极窄——主要面向文档排版中需要内嵌 QR 码的极少数用户，而这类需求已有更成熟的解决方案（图床、Unicode
    QR 字符、SVG 嵌入）。无团队、无商业模式、无持续迭代路径，属于典型的昙花一现型个人项目，3-5 年内不具备成为行业基石的任何可能。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- 文本排版工具（LaTeX/Markdown 渲染器）
- 开源字体社区
competitive_casualty:
- 小型在线 QR 码生成器
- 文档内嵌 QR 码服务的轻量替代方案
market_opportunities:
- 文档自动化工具可集成该字体，实现发票、工单、门票等场景中文本与QR码的即时混排，省去单独生成二维码图片的步骤
- 印刷与出版行业可探索基于字体的编码方式，将校验码或短链接直接嵌入排版流程，提升防伪文档的生产效率
risk_matrix:
  regulatory: 无（基于SIL Open Font License的开源字体，无直接监管风险）
  technological: 该方案强依赖OpenType GSUB特性，不支持Web标准字体渲染环境（如部分PDF引擎、老旧浏览器），且仅支持固定掩码模式和字节编码，实用性受限于排版引擎的兼容性
  competitive: 传统QR码生成库（如qrcode.js、libqrencode）生态成熟且跨平台，该字体方案在编码容量（最多53字符）、纠错等级、掩码策略方面均有明显劣势，难以替代现有方案
  ethical: 无（该字体本身无偏见歧视、隐私侵犯或深度伪造等伦理风险）
  additional:
  - 字体基于Liberation Sans Regular修改，受SIL OFL保留字体名称条款约束，商业分发需注意合规署名和许可证要求
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: speculative_watch
---

Live Demo: https://qr.jim.sh/

Downloads:

- qrfont-1L.ttf (21x21 modules, up to 17 characters)
- qrfont-2L.ttf (25x25 modules, up to 32 characters)
- qrfont-3L.ttf (29x29 modules, up to 53 characters)

This repo generates an experimental OpenType font that turns bracket-delimited text into a QR Code symbol while leaving surrounding text readable.

```
abc[hello]ghi
```


All fonts use:

- Byte mode
- Printable ASCII input
- Fixed mask pattern 0
`[`

and`]`

as delimiters

The fonts are generated rather than hand-authored. The build script emits glyph
outlines and GSUB feature logic, then compiles them into `dist/qrfont-*.ttf`

.
The default build compiles the delimiter parser, byte expansion, Reed-Solomon
parity circuit, QR module placement, and fixed mask rendering.

Printable ASCII glyphs are copied from Liberation Sans Regular, scaled into the
QR Font em square, so text outside bracketed QR blocks renders as ordinary text
in the same font. `Liberation`

is a reserved font name under the source font
license, so the generated families are named `QR Font 1-L`

, `QR Font 2-L`

, and `QR Font 3-L`

.

`make`

The project uses `uv`

for Python dependency management. You can also run the
generator directly with:

`uv run tools/build_font.py`

By default the generator reads Liberation Sans Regular from:

```
/usr/share/fonts/truetype/liberation/LiberationSans-Regular.ttf
```


Use a different compatible TrueType source with:

`uv run tools/build_font.py --base-font /path/to/BaseFont-Regular.ttf`

To try the full generated Reed-Solomon circuit:

`make full-parity`

This is also the default `make`

path. It emits thousands of contextual lookups
and usually takes noticeably longer than a layout-only build.

For a faster layout-only build with placeholder zero parity:

`make fast-placeholder`

To inspect the shaped glyph stream:

`uv run tools/shape_debug.py '[a]' '[b]'`

Outputs:

`dist/qrfont-*.ttf`

(1-L, 2-L, and 3-L font files)`dist/index.html`

(interactive web demo)`build/qrfont-*.fea`

(generated OpenType feature files)

Open `dist/index.html`

in a browser and type bracketed text such as `[hello]`

.
Mixed text such as `abc[def]ghi`

should render as normal text, then a QR code
for `def`

, then normal text.

The generated font is a Modified Version of Liberation Sans Regular and is
licensed under the SIL Open Font License, Version 1.1. See
`LICENSE-OFL.txt`

and `NOTICE.md`

.

This is a proof-of-concept font. It relies on OpenType shaping, so it needs an environment that applies GSUB features to the font. Inputs inside a QR block are bounded to printable ASCII, up to 17, 32, or 53 characters depending on the selected font version.