---
title: Claude-real-video － any LLM can watch a video
source: https://github.com/HUANGCHIHHUNGLeo/claude-real-video
author:
- '[[cortexosmain]]'
published: '2026-07-02'
created: '2026-07-03'
description: 'Article URL: https://github.com/HUANGCHIHHUNGLeo/claude-real-video Comments
  URL: https://news.ycombinator.com/item?id=48766005 Points: 133 # Comments: 41'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: f8a707d9ac993687
manifest_dates:
- '2026-07-03'
source_type: community_discussion
tldr: claude-real-video 是一个本地运行的 Python 开源工具，通过场景变化检测和去重算法提取视频关键帧，配合 Whisper 语音转文字，让任意大模型无需上传即可理解视频内容。
objective_summary: 开发者 HUANGCHIHHUNGLeo 在 GitHub 上开源了 claude-real-video 项目。该工具使用 ffmpeg
  的场景变化检测替代固定间隔采样，从 YouTube 链接或本地视频中抓取真正有画面变化的帧，再通过像素差异和滑动窗口去重，最后配合 Whisper 转录的文本生成可供任何大模型消费的输出文件夹。所有处理在本地完成，不上传视频到云端，支持
  macOS、Windows 和 Linux。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - yt-dlp
  - Whisper
  - ffmpeg
  - LLM
  key_people:
  - HUANGCHIHHUNGLeo
key_logic_flow:
- claude-real-video 是一个本地运行的 Python CLI 工具，可从 YouTube、Instagram、TikTok 等链接或本地文件提取视频关键帧和文字转录。
- 该工具使用场景变化检测（scene-change detection）而非固定间隔采样来抓取帧，并设置密度下限确保慢速内容也有足够帧覆盖。
- 通过 RGB 像素差异和滑动窗口对近重复帧进行去重，避免 A-B-A 这类镜头切换导致已发送的帧被重复提交。
- 音频部分优先使用已有字幕，无字幕时自动调用 Whisper 进行语音转写，支持多语言检测。
- 输出结果包含帧图片、转录文本和 MANIFEST.txt 摘要文件，可直接提供给 Claude、ChatGPT 或 Gemini 等任意大模型处理。
- 所有处理在本地设备上完成，视频内容不上传至任何云端，且支持丰富的 CLI 参数自定义场景灵敏度、最大帧数、去重阈值等配置。
extract_result: success
object_mentions:
- object_type: project
  name: claude-real-video
  canonical_name: HUANGCHIHHUNGLeo/claude-real-video
  url: https://github.com/HUANGCHIHHUNGLeo/claude-real-video
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - claude-real-video 是一个本地运行的 Python 工具，可让任意大模型通过场景感知的关键帧提取和语音转文字来理解视频内容。
  - 该工具使用场景变化检测而非固定间隔采样抓取帧，通过像素差异在滑动窗口中对近重复帧进行去重。
  - 所有处理在本地完成，视频不上传云端，支持 macOS、Windows 和 Linux 平台，采用 MIT 许可证开源。
  article_id: f8a707d9ac993687
- object_type: model
  name: Whisper
  canonical_name: openai-whisper
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 当视频没有内嵌或外挂字幕时，工具自动回退到 Whisper 对音频进行语音转写，支持自动语言检测。
  - Whisper 依赖 ffmpeg 处理音频，通过 `pip install claude-real-video[whisper]` 或 `pip install
    openai-whisper` 安装。
  article_id: f8a707d9ac993687
- object_type: project
  name: yt-dlp
  canonical_name: yt-dlp/yt-dlp
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - yt-dlp 负责从 YouTube、Instagram、TikTok 等平台下载视频，支持通过 Netscape cookie 文件访问需登录的视频源。
  article_id: f8a707d9ac993687
impact_score:
  score: 4.5
  reason: 该工具本质上是将已有技术（ffmpeg场景检测、Whisper语音转文字、yt-dlp下载、像素级去重）进行了精心编排，解决了一个真实的痛点——LLM无法真正'看'视频。技术创新在于密度下限策略+滑动窗口去重的组合设计，而非突破性算法。对个人开发者和内容分析工作流有实际价值，但不足以改变行业格局。评分4.5，属于实用的开源工具发布，在局部（视频+LLM交叉领域）产生一定影响。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 本地处理+智能帧选择，无需上传视频到云端即可让任意LLM分析
hype_assessment:
  level: low
  reason: README写得非常实在，没有使用'颠覆'、'革命性'等PR滥用词汇。相反，它坦诚地与Gemini固定帧率采样方案进行对比，明确列出了自身优劣，并提供了详细的参数文档和局限说明。代码已开源（MIT协议），技术方案透明可审计。
information_entropy: high
domain_disruption:
  technical_innovation: 提出了场景变化检测+密度下限策略的混合帧选择方案（解决固定帧率对静态画面过采样、对快速剪辑漏帧的问题），并结合滑动窗口像素级RGB差异去重（对比最近N帧，而非仅对比前一帧，避免A-B-A剪辑重复发送已见过镜头），在本地实现全流程处理。
  business_model: 无
engineering_complexity: production_ready
compound_value:
  score: 3.5
  reason: 该工具在技术上做了扎实的工程优化（场景检测+密度下限+滑动窗口去重），确实解决了当前 LLM 处理视频时的两大痛点：固定帧率采样浪费 token
    和对快速剪辑漏帧。但从 VC 复利视角看，长期价值有限：(1) MIT 开源协议，无公司实体、无商业模式、无网络效应，可被任意竞品复制；(2) 核心能力是对
    ffmpeg + Whisper + yt-dlp 的编排组合，自身技术护城河极浅；(3) 随着 GPT-5、Claude 4 等下一代模型原生支持视频输入（甚至支持端到端视频理解），该工具作为"桥梁方案"的使用价值将随时间衰减。它是一款优秀的开发者工具，但难以形成长期复利资产。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Anthropic
- OpenAI
- 开发者社区
competitive_casualty:
- 云端视频分析API服务
- 付费视频转文字/摘要SaaS
market_opportunities:
- 隐私敏感行业（医疗、法律、金融）可直接基于该工具搭建本地化视频分析管道，无需将视频上传至任何第三方云平台，满足数据主权和合规要求
- 视频知识管理团队可将其嵌入自动化工作流，将培训录像、会议记录等长视频转化为结构化关键帧+文字转录的知识库，大幅提升内容检索效率
- 开发者可借鉴其场景变化检测+滑动窗口去重策略，在多模态应用中优化传递给LLM的帧数量，在降低Token成本的同时保留关键视觉信息
risk_matrix:
  regulatory: 使用 yt-dlp 下载受版权保护的视频内容可能构成侵权；--cookies 绕过登录墙的机制可能违反平台服务条款，在商业部署中需严格限定在合规授权场景下使用
  technological: 场景检测阈值和去重参数对视频类型敏感（静态直播vs快节奏剪辑），默认参数并非通用最优解；Whisper 转录在低质量音频、专业术语和混合语言场景下准确率不稳定，可能影响下游分析质量
  competitive: Gemini、GPT-4o 等商业模型已原生支持视频理解且持续迭代，本地工具在模型能力和便利性上处于追赶态势；同类开源方案（VideoRepet、LLaVA-Video）也在快速演进，生态位存在挤压风险
  ethical: 工具可被用于未经授权分析他人私密视频内容；结合LLM的批量视频处理可能被滥用于生成虚假信息或深度伪造检测规避，需注意使用边界
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: claude-real-video
  canonical_name: HUANGCHIHHUNGLeo/claude-real-video
  url: https://github.com/HUANGCHIHHUNGLeo/claude-real-video
  positioning: 一个本地运行的 Python CLI 工具，通过场景变化检测和去重算法提取视频关键帧，结合 Whisper 语音转录让任意大模型无需上传即可理解视频内容。
  technical_signal: 采用场景变化检测替代固定间隔采样，配合 RGB 像素差异滑动窗口去重算法，显著减少冗余帧并提升多模态模型理解效率。
  adoption_signal: 以 MIT 许可证开源，可通过 pip 一键安装且支持 macOS、Windows 和 Linux 三大平台，降低了 LLM
    视频理解的技术门槛。
  ecosystem_relevance: 填补了视频内容与多模态大模型之间的预处理空白，使 Claude、ChatGPT、Gemini 等不具备原生视频能力的模型也能高效处理视频。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 相比固定间隔采样方案，场景感知帧提取在慢速内容中大幅减少冗余帧，在快速剪辑中不漏关键画面，输入更少但理解效果更好。
  watch_reason: 该项目提出了一种通用且高效的本地视频预处理方案，场景感知帧提取思路可能成为多模态 LLM 视频处理的标准前置工具，值得关注其社区采用与技术演进。
  risk_notes:
  - 依赖 ffmpeg 和 Whisper 等外部工具，安装配置在跨平台环境中存在差异。
  - 开源个人项目，长期维护能力和社区响应速度存在不确定性。
  score: 7.0
  article_ids:
  - f8a707d9ac993687
  evidence_snippets:
  - claude-real-video 是一个本地运行的 Python 工具，可让任意大模型通过场景感知的关键帧提取和语音转文字来理解视频内容。
  - 该工具使用场景变化检测而非固定间隔采样抓取帧，通过像素差异在滑动窗口中对近重复帧进行去重。
  - 所有处理在本地完成，视频不上传云端，支持 macOS、Windows 和 Linux 平台，采用 MIT 许可证开源。
- object_type: project
  name: yt-dlp
  canonical_name: yt-dlp/yt-dlp
  url: null
  positioning: 一个开源视频下载工具，支持从 YouTube、Instagram、TikTok 等大量平台下载视频，提供丰富的下载参数和格式支持。
  technical_signal: 在 claude-real-video 中作为视频源下载核心依赖，支持通过 Netscape cookie 文件访问需登录的视频源，扩展了工具链的视频来源范围。
  adoption_signal: null
  ecosystem_relevance: 为本地化视频预处理工具链提供关键的视频下载能力，是将在线视频内容拉到本地处理的桥梁组件。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 claude-real-video 等本地视频预处理工具的核心依赖，其下载能力、协议兼容性和 Cookie 登录支持直接决定了上游工具的用户体验与内容来源范围。
  risk_notes:
  - 需通过 Netscape cookie 文件处理登录态视频，使用方式存在授权合规性风险。
  score: 4.0
  article_ids:
  - f8a707d9ac993687
  evidence_snippets:
  - yt-dlp 负责从 YouTube、Instagram、TikTok 等平台下载视频，支持通过 Netscape cookie 文件访问需登录的视频源。
---

**Let Claude — or any LLM — actually watch a video.**

Most AI tools don't really *see* a video. Paste a YouTube link into ChatGPT and it
reads the **transcript**, not the picture. Claude won't take a video file at all.
Even Gemini, which *can* read video natively, has to send it up to Google and
samples frames at a **fixed interval** (1 fps by default), so fast cuts slip past.

`claude-real-video`

does it differently, and **locally**: point it at a URL or a
file, and it pulls the frames that *actually matter* (every scene change, not a
fixed quota), throws away the near-duplicates, transcribes the audio, and hands
you a clean folder any LLM can read — on your own machine, nothing uploaded.

```
crv "https://www.youtube.com/watch?v=..."
# → crv-out/frames/*.jpg + crv-out/transcript.txt + crv-out/MANIFEST.txt
```

Then drop the frames + `MANIFEST.txt`

into Claude / ChatGPT / Gemini and ask away.

Most "let an LLM watch a video" scripts (and Gemini's own pipeline) grab frames
at a **fixed interval** — e.g. one per second. That over-samples a static
screencast and under-samples a fast-cut reel. `claude-real-video`

is smarter:

| fixed-interval sampling | claude-real-video |
|
|---|---|---|
| Frame selection | every N seconds | scene-change detection + density floor |
| Repeated shots (A-B-A cuts) | sent again every time | sliding-window dedup sends each shot once |
| Static slide (10 min) | ~600 near-identical frames | collapses to 1 (dedup) |
| Fast-cut reel | misses frames between samples | catches each visual change |
| Audio | often ignored | Whisper transcript w/ language detect |
| Where the video goes | often uploaded to a cloud | stays on your machine |
| Input | usually local file only | URL (yt-dlp) or local file |

You feed the model *fewer, more meaningful* frames — cheaper context, better
understanding.

```
pip install claude-real-video # core (frames + dedup)
pip install "claude-real-video[whisper]" # + audio transcription
```

`ffmpeg`

/ `ffprobe`

are used for frame extraction and audio, and aren't
pip-installable. Install them once:

| OS | command |
|---|---|
macOS |
`brew install ffmpeg` |
Linux |
`sudo apt install ffmpeg` (or your distro's package manager) |
Windows |
`winget install Gyan.FFmpeg` — or `choco install ffmpeg` — or download a build and add its `bin\` folder to your `PATH` |

Verify it's on your `PATH`

:

`ffmpeg -version`

Transcription uses the `whisper`

CLI (installed by the `[whisper]`

extra, or
`pip install openai-whisper`

). Whisper also relies on ffmpeg.

Works on **macOS, Windows, and Linux** — Python 3.10+.

```
# A YouTube / Instagram / TikTok / ... link
crv "https://www.instagram.com/reel/XXXX/"
# A local file, English transcript, output to ./out
crv lecture.mp4 -o out --lang en
# Frames only, no transcription
crv clip.mp4 --no-transcribe
# A login-gated video (your own / authorised use): pass a Netscape cookie file
crv "https://..." --cookies cookies.txt
```

`python -m claude_real_video ...`

works as an alias for `crv`

too.

| flag | default | meaning |
|---|---|---|
`-o, --out` |
`crv-out` |
output directory |
`--scene` |
`0.30` |
scene-change sensitivity (lower = more frames) |
`--fps-floor` |
`1.0` |
at least one frame every N seconds |
`--max-frames` |
`150` |
hard cap on total frames |
`--lang` |
`auto` |
Whisper language (`en` , `zh` , `auto` , ...) |
`--dedup-threshold` |
`8` |
% of pixels that must change for a frame to count as new; higher = fewer frames |
`--dedup-window` |
`4` |
compare against the last N kept frames — a shot the model already saw doesn't come back after a cutaway (`1` = consecutive-only) |
`--report` |
off | keep dropped frames in `./dropped` + write `report.html` visualising every keep/drop decision |
`--no-transcribe` |
off | skip audio |
`--keep-audio` |
off | also save the full soundtrack (`audio.m4a` ) so audio models can hear it |
`--cookies` |
– | Netscape cookie file for login-gated sources |

```
from claude_real_video import process
r = process("https://youtu.be/...", "out", lang="en")
print(r.frame_count, r.transcript_path)
```

**Fetch**—`yt-dlp`

for URLs (optional cookies), or copy a local file.**Extract**— one chronological`ffmpeg select`

pass grabs every scene change*plus*a density floor (at least one frame every`--fps-floor`

seconds), so fast cuts and slow screencasts are both covered.**Dedup**— real pixel difference (downscaled RGB, not a perceptual hash — hashes go blind on flat colours and equal-luma hue changes) against a**sliding window**of the last`--dedup-window`

kept frames, so an A-B-A cutaway doesn't re-send a shot the model has already seen.`--report`

writes`report.html`

showing every keep/drop decision with its diff %, for tuning.**Text**— if the video**already has subtitles**(a sidecar`.srt`

/`.vtt`

next to a local file, or an embedded subtitle track), those are used as the transcript — faster and more accurate than re-transcribing. Only when there are no subtitles does it fall back to**Whisper**on the audio (skipped cleanly if there's no audio).**Audio***(optional,*— save the`--keep-audio`

)**full original soundtrack**(`audio.m4a`

: music + speech + effects, copied losslessly when possible). The transcript only has the*words*; the audio file lets a model that can listen (Gemini, GPT-4o, …) actually*hear*the music and tone.**Manifest**—`MANIFEST.txt`

summarises everything for the model.

So the model can **see** (key frames), **read** (transcript) and — with `--keep-audio`

—
**hear** (full soundtrack) the video. The transcript is plain text any model can read;
the tool **doesn't burn subtitles into the video** — burning is a presentation choice,
not something needed to make a video AI-readable.

- Only download content you have the right to. The
`--cookies`

option is for your own, authorised access — don't ship credentials in a repo. - Re-running overwrites the output directory.

MIT