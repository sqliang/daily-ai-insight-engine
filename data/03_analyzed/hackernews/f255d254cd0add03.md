---
title: Running local models is good now
source: https://vickiboykis.com/2026/06/15/running-local-models-is-good-now/
author:
- '[[jfb]]'
published: '2026-06-16'
created: '2026-06-17'
description: 'Article URL: https://vickiboykis.com/2026/06/15/running-local-models-is-good-now/
  Comments URL: https://news.ycombinator.com/item?id=48555993 Points: 1313 # Comments:
  504'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: f255d254cd0add03
source_type: community_discussion
tldr: Vicki Boykis分享称，截至2026年6月，本地模型通过LM Studio和Pi代理框架在M2 Mac上实现了接近前沿模型75%效果的编码工作流，并提供了从Docker配置到模型选择的完整技术方案。
objective_summary: Vicki Boykis于2026年6月在个人博客发文，总结了她从2022年起使用本地模型的经验。她在2022款M2 Mac（64GB
  RAM）上先后测试了Mistral 7B、Gemma 3、OpenAI OSS-20B、Qwen 3 MOE等多个模型，以及llama.cpp、Ollama、LM
  Studio等多种推理引擎。GPT-OSS是首个让她在多数任务中不再需要与云端API交叉验证的本地模型。Google Gemma 4系列发布后，她通过LM Studio（推理服务端）和Pi（代理框架）实现了本地代理编码工作流，效果约达前沿模型的75%。她将所有代理会话限制在Docker容器内以确保安全，并分享了完整的配置文件、Docker
  Compose和启动脚本。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Google
  - OpenAI
  - Mistral
  - Alibaba
  - HuggingFace
  technologies:
  - llama.cpp
  - Ollama
  - LM Studio
  - llama-cpp-python
  - llamafiles
  - Docker
  - KV-cache
  - MOE
  - QAT
  - agentic coding
  key_people:
  - Vicki Boykis
key_logic_flow:
- 作者在2022款M2 Mac（64GB RAM, 1TB存储）上先后测试了Mistral 7B、Gemma 3、OpenAI OSS-20B、Qwen 3 MOE等模型，并使用过llama.cpp、Ollama、LM
  Studio等多种推理引擎。
- OpenAI发布的GPT-OSS（OSS-20B）是作者体验中首个在多数编程任务上不再需要与云端API模型反复交叉验证的本地模型。
- Google Gemma 4系列发布后，作者实现了本地代理编程工作流（从Jupyter Notebook重构为模块化Python项目、编写单元测试、引导代码库等），效果可达前沿模型的约75%。
- 作者的当前默认配置为：LM Studio作为推理服务端，Gemma-4-12b-qat作为模型，Pi作为代理框架，所有代理会话运行在Docker容器中仅暴露bash权限。
- 作者通过docker-compose配置了extra_hosts（host.docker.internal:host-gateway）使容器内的Pi能访问宿主机上的LM
  Studio推理端点，并通过volumes注入自定义models.json配置。
- 本地模型仍存在推理速度慢、上下文窗口受限于硬件、生态尚未完全成熟等问题，但LM Studio和HuggingFace的'Use This Model'按钮等工具已显著降低使用门槛。
extract_result: success
object_mentions:
- object_type: product
  name: LM Studio
  canonical_name: LM Studio
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 作者将LM Studio作为本地模型的推理服务端，并使用其运行Gemma-4-12b-qat作为默认模型。
  - 作者通过docker-compose配置extra_hosts使容器内Pi能够访问宿主机上LM Studio的1234端口推理端点。
  - 作者认为如果直接使用llama.cpp而非LM Studio可能获得更快的推理速度，但LM Studio提供了更便捷的使用体验。
  article_id: f255d254cd0add03
- object_type: project
  name: Pi
  canonical_name: Pi Agent
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 作者使用Pi作为代理框架（Agent Harness），将其部署在Docker容器中并配置了models.json指向LM Studio的本地推理端点。
  - Pi运行在Docker容器内仅有bash权限，无法直接执行Python代码或进行网页浏览，以此保证安全性。
  - 作者推荐了一篇关于如何用Pi和LM Studio设置本地代理编码的教程，并基于该教程做了若干配置调整。
  article_id: f255d254cd0add03
- object_type: model
  name: Gemma-4-12b-qat
  canonical_name: google/gemma-4-12b-qat
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Gemma-4-12b-qat是Google最新发布的小尺寸模型，作者认为其在不牺牲太多准确率的前提下更快更小。
  - 作者推荐用Gemma-4-12b-qat替代较早的Gemma 26B A4B，因为前者在速度和准确率之间取得了更好的平衡。
  - 作者在Pi的models.json中将模型ID配置为google/gemma-4-12b-qat并启用了文本和图像输入。
  article_id: f255d254cd0add03
- object_type: model
  name: GPT-OSS
  canonical_name: OpenAI OSS-20B
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - GPT-OSS（OpenAI OSS-20B）是作者体验中第一个在多数任务上不再需要与云端API模型交叉验证的本地模型。
  - 作者认为GPT-OSS的发布是本地模型能力显著提升的转折点，此后她开始大幅减少对云端API的依赖。
  article_id: f255d254cd0add03
- object_type: project
  name: llama.cpp
  canonical_name: llama.cpp
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 作者早期使用过原始llama.cpp配合Open WebUI来运行本地模型，并测试了多个开源模型。
  - 作者认为如果直接使用llama.cpp而非LM Studio作为推理后端可能获得更快的推理速度，将其列为未来实验方向。
  article_id: f255d254cd0add03
- object_type: product
  name: Ollama
  canonical_name: Ollama
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - Ollama是作者测试过的本地模型推理系统之一，用于运行Mistral 7B、Gemma 3等开源模型。
  article_id: f255d254cd0add03
- object_type: project
  name: Rijksearch
  canonical_name: Rijksearch
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 作者提到正在开发Rijksearch项目，该项目与从Arxiv论文中提取和展示热点话题相关。
  - 作者通过分析过去LM Studio的会话日志发现，大量查询与Rijksearch项目的开发工作有关。
  article_id: f255d254cd0add03
impact_score:
  score: 7.2
  reason: 本文记录了本地模型从'不可用'到'足够好用'的关键转折点。作者使用 M2 Mac（非高端服务器硬件）实现了约 75% 前沿模型水平的 agentic
    编码能力，六个月前这还不可能。这一里程碑意味着本地推理对于开发者日常编码任务已具备实用性，将冲击 API 模型的市场需求，推动更多开发者转向本地部署。虽然尚未达到
    ChatGPT 级别的范式转移，但对于 AI 基础设施的去中心化趋势而言是一个重要的临界点事件。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 本地 agentic 编码终于达到实用水平，约 75% 前沿模型效果
hype_assessment:
  level: low
  reason: 文章没有使用'颠覆'、'革命性'等 PR 词汇。作者提供了具体的硬件配置（M2 Mac/64GB）、模型版本（Gemma 4 系列）、工具链（LM
    Studio + Pi + Docker）和实测任务清单（重构 Python 脚本、编写单元测试、构建推荐系统原型），同时坦诚指出了推理速度慢、上下文窗口受限、尚未准备好用于生产级软件开发等局限性，水分较低。
information_entropy: high
domain_disruption:
  technical_innovation: Gemma 4 系列（特别是 gemma-4-12b-qat 的量化感知训练架构）在保持较小模型尺寸的同时大幅提升了本地推理质量，配合
    LM Studio 作为推理服务器和 Pi agent 框架，使得稀疏 MoE 模型在消费级硬件上实现了此前需要云端 API 才能完成的 agentic 编码工作流。
  business_model: 威胁 API 按 token 计费的商业模式：如果开发者在本地即可完成约 75% 的编码任务，将显著减少对 OpenAI/Anthropic
    API 的依赖，推动更多企业采用混合部署策略（本地日常任务 + 云端复杂任务），同时利好硬件厂商（Apple Silicon、NVIDIA 消费级 GPU）和本地推理引擎（llama.cpp、LM
    Studio、Ollama）的生态繁荣。
engineering_complexity: prototype
compound_value:
  score: 7.8
  reason: 本地模型从'玩具'到'可用'的拐点已至——Gemma 4系列配合LM Studio和Pi可实现前沿模型约75%的agentic编码能力，六个月前还不可能的任务现在可用。这一趋势具有明显复利效应：(1)
    本地推理免除API调用成本，边际成本趋近于零，对高频agentic工作流的经济账是颠覆性的；(2) 数据隐私优势不可逆，企业级部署和敏感场景的本地化需求将刚性增长；(3)
    Apple Silicon与高效模型架构（MoE、量化）的协同优化形成正反馈循环，硬件能力提升直接转化为可用性提升；(4) 工具链（LM Studio、llama.cpp、Pi）大幅降低使用门槛后，社区贡献和生态效应将持续放大。但当前上下文窗口和推理速度仍是硬约束——K-V缓存可吃掉64GB
    RAM，生产级软件开发尚未准备好。75%的覆盖率能否继续提升至90%+需要验证，且取决于模型架构创新（如QAT量化）能否持续突破。若本地+云端混合架构成为主流，中间件层的平台价值将指数级增长。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Google (Gemma)
- HuggingFace
- LM Studio
- Apple
- Mistral
- Alibaba (Qwen)
competitive_casualty:
- OpenAI
- Anthropic
- 小型闭源模型创业公司
- 纯云端AI API中间商
market_opportunities:
- 面向隐私敏感型企业和机构的本地化 AI 开发辅助方案需求激增，可基于 Gemma 4 等开源模型构建 Docker 容器化的内部编码助手，无需将代码上传至第三方
  API
- LM Studio + Pi agent 框架的组合提供了低成本、高可控的本地 agent 开发范式，创业团队可围绕该技术栈开发面向个人开发者和小团队的本地 AI
  工具箱产品
- 教育科研场景中，本地模型的可内省性（实时观察 token 推理过程）和可调参能力为 AI 教学和模型研究提供了独特的实验平台，可开发配套的教学辅助工具
risk_matrix:
  regulatory: 本地部署模式天然规避数据出境监管风险（如 GDPR、中国数据安全法），但仍需关注开源模型的许可协议约束（如 Gemma 系列的商业使用条款）
  technological: 本地模型在硬件资源上存在明显天花板，K-V 缓存占用高达 64GB RAM，推理速度远低于 API 模型，且模型迭代极快，当前最优方案可能在
    6-12 个月内被超越
  competitive: Anthropic、OpenAI 等前沿模型厂商可能通过降价或推出轻量级 API 方案挤压本地模型的使用场景，同时 Google 等开源模型发布方的策略变动（如许可证调整）也会影响生态
  ethical: 本地模型免除了数据外传的隐私风险，有利于敏感数据处理；但更低的使用门槛也可能放大偏见传播、代码安全缺陷等风险，且缺乏 API 级别的安全过滤机制
  additional:
  - 本地推理的高硬件要求（64GB RAM 起步）可能导致数字鸿沟扩大，只有中高端设备用户能享受本地模型的红利
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: strategic_invest
object_insights:
- object_type: product
  name: LM Studio
  canonical_name: LM Studio
  url: https://lmstudio.ai
  positioning: LM Studio 是一款面向开发者的本地模型推理服务端，提供图形化界面加载和运行开源模型，并通过兼容 OpenAI API 的本地端点服务于外部代理框架。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 希望在本地运行大模型并搭建代理编程工作流的开发者和技术研究者
  product_signal: 作者将 LM Studio 作为本地推理服务端，向 Docker 容器内的 Pi 代理框架提供兼容 OpenAI API 的本地推理端点，实现了完整的本地代理编码工作流。
  market_signal: 作者对比指出直接使用 llama.cpp 推理速度可能更快，但 LM Studio 以牺牲部分性能换取更便捷的使用体验，说明其市场定位为易用性优先。
  differentiation: 与 Ollama 和原始 llama.cpp 等竞品相比，LM Studio 提供图形化界面和模型一键加载能力，在用户体验和入门门槛方面具有明显优势。
  watch_reason: LM Studio 作为本地模型推理服务端，与 Gemma-4-12b-qat 和 Pi 代理框架的组合已实现约 75% 前沿模型效果的本地代理编码工作流，标志着本地模型从被动问答向主动代理任务的关键跨越，其易用性优势和生态整合能力值得持续跟踪。
  risk_notes:
  - LM Studio 相较直接使用 llama.cpp 推理速度较慢，在高频代理任务或大规模代码重构场景中可能成为明显的性能瓶颈。
  - 本地模型推理在高负载下 KV Cache 可占满 64GB 内存，长期运行对硬件资源消耗较大，限制了可处理的任务复杂度。
  score: 7.0
  article_ids:
  - f255d254cd0add03
  evidence_snippets:
  - 作者将LM Studio作为本地模型的推理服务端，并使用其运行Gemma-4-12b-qat作为默认模型，同时通过docker-compose配置了完整的代理工作流。
  - 作者通过docker-compose配置extra_hosts使容器内Pi能够访问宿主机上LM Studio的1234端口推理端点，从而建立了完整的本地推理服务链路。
  - 作者认为如果直接使用llama.cpp而非LM Studio可能获得更快的推理速度，但LM Studio提供了更便捷的使用体验，这是它在易用性与性能之间的取舍。
- object_type: project
  name: Pi
  canonical_name: Pi Agent
  url: null
  positioning: Pi 是一款面向本地模型场景的代理框架（Agent Harness），通过 Docker 容器安全隔离执行环境，连接 LM Studio
    等本地推理端点实现无需云端 API 的代理编程工作流。
  technical_signal: Pi 作为代理框架部署在 Docker 容器中，通过 models.json 配置文件连接 LM Studio 的本地推理端点，容器内仅暴露
    bash 权限实现安全隔离。
  adoption_signal: 作者基于社区教程成功搭建了 Pi + LM Studio 的本地代理编码工作流，并已用于重构 Python 项目、编写单元测试等真实编程任务，验证了其在本地开发场景中的实用价值。
  ecosystem_relevance: Pi 作为代理框架补充了本地模型生态中的代理能力缺口，使开发者无需依赖云端 API 即可在本地完成代码重构、测试编写等代理式编程任务。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Pi 作为连接本地推理引擎与代理工作流的关键中间层，与 LM Studio 和 Gemma-4-12b-qat 的组合证明了在消费级硬件上实现代理编程的可行性，其安全沙箱设计和容器化部署方案为本地模型代理工作流提供了可复用的参考架构。
  risk_notes:
  - Pi 在 Docker 容器内仅暴露 bash 权限，无法直接执行 Python 代码或进行网页浏览，这限制了其可完成的代理任务类型和范围。
  - Pi 项目名称较为通用，在搜索引擎和开发者社区中与树莓派等多义项重叠，可能影响社区讨论和资源发现的效率。
  score: 7.0
  article_ids:
  - f255d254cd0add03
  evidence_snippets:
  - 作者使用Pi作为代理框架（Agent Harness），将其部署在Docker容器中并配置了models.json指向LM Studio的本地推理端点，实现了容器化的安全代理环境。
  - Pi运行在Docker容器内仅有bash权限，无法直接执行Python代码或进行网页浏览，以此保证代理会话和宿主机的安全性。
  - 作者推荐了一篇关于如何用Pi和LM Studio设置本地代理编码的社区教程，并基于该教程针对自身环境做了配置调整。
---

# Running local models is good now

I’ve been working with local models since they came out, and finally, they’re surprisingly good now.

I have a 2022 M2 Mac with 64 GB RAM and 1TB storage and I’ve used

- Mistral 7B
- Gemma 3
- OpenAI OSS-20B
- Qwen 3 MOE, as well as a number of other Qwen variants like Qwen 2.5 Coder

across a lot of different system setups like

- raw llama.cpp with Open WebUI
- llama-cpp-python
- Ollama
- llamafiles and
- LM Studio

## Where are local models now?

Early on, models were slow, hard to use, and just not that accurate for most programming tasks. The idea that local models were severely lagging behind was largely true until, for me, the release of GPT-OSS. I have no concrete scientific evidence of this - my own personal vibe metric of “is a model good enough” is, “do I have to double-check it against an API model”, and GPT-OSS was the first one where I started doing that a lot less often.

As a result, I’ve mostly been using local models as fast, personalized Google for development questions that don’t require recency.

But with the most recent releases from Google in the Gemma 4, family, I’ve finally been able to do agentic coding locally and have loops work at about ~75% the accuracy/speed of frontier models, which is incredible.

I’ve so far been using `gemma-4-26b-a4b`

LM Studio implementation as my default local model. I’ve used the local setup so far to: Refactor a Python script that was a notebook into a repo of 5-6 modules, lint that module to use correct type hints for generics (most frontier models now do this automatically, but not always).

I’ve also used it to proofread some blog posts, write unit tests, and to bootstrap a repo that stands up a two-tower model for recommendations just to see what the agent would do with a blank slate. Here’s what it generated, which was pretty basic but still beyond the scope of anything I would have thought possible last year:

Note that the environment is restricted because I run all my agentic workflows in a Docker container with limited access to execution.

I’m also building an app that surfaces trending topics from Arxiv papers. Out of curiosity, I had Pi go through my past LM Studio session logs and figure out what I was using LM Studio for:

Unsurprisingly, since I’ve been working on Rijksearch,

None of these are groundbreaking tasks (again, a lot of personalized Google/docs lookups), and working on them does give my GPUs and RAM a workout and the K-V cache grows to 64 GB RAM.

But, the larger story for me is that these kinds of tasks, even as simple as they are, used to be impossible for local models as recently as 6 months ago.

`Gemma-4-12b-qat`

just came out but I’ve already also really been impressed with its performance relative to its size. The model architecture itself is really interesting and proposes a bunch of interesting questions like, “if we are constrained by performance and price, what architectural tradeoffs do we need to make?” a question that so far has not really been asked in the mad token gold rush.

## Running agentic models locally today

But don’t take my word for any of this, try it out for yourself! You’ll need a local model inference engine, an agentic harness, and the local model artifact if you want to try to run local agentic flows. You’ll need to set up the harness to point at your local inference endpoint, the downloaded model artifact served via the inference engine.

For my local setup, I’m currently using Pi as the agent harness and LM Studio as the inference server, although it would likely be faster if I just used llama.cpp directly - a potential direction for a future experiment.

This post was very easy to follow to set up agentic coding with Pi and LM Studio, although I did make a few tweaks to the post’s setup.

**Model:**The post recommends`Gemma 26B A4B`

, but`gemma-4-12b-qat`

is more recent and smaller and faster, without much sacrifice in accuracy.**Security:**I run every Pi session in a Docker container and give it permissions only to bash so that it can’t run Python code or do web browsing, although I do plan to allow curl in a different image for some research work I’m doing.**Agent Harness Config:**Since I run everything in Docker, I edited Pi’s`models.json`

in order to get Pi to talk to the model.

```
"lmstudio": {
"baseUrl": "http://host.docker.internal:1234/v1",
"api": "openai-completions",
"apiKey": "not-needed",
"models": [
{
"id": "google/gemma-4-12b-qat",
"input": [
"text",
"image"
]
}
]
}
```


Here’s my Docker Compose config:

```
services:
pi:
build:
context: .
dockerfile: Dockerfile
image: pi-agent:0.74.0
init: true
stdin_open: true
tty: true
extra_hosts:
- "host.docker.internal:host-gateway"
environment:
ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
OPENAI_API_KEY: ${OPENAI_API_KEY:-not-needed}
GEMINI_API_KEY: ${GEMINI_API_KEY:-}
OPENAI_API_BASE: ${OPENAI_API_BASE:-http://host.docker.internal:1234/v1} # note that you'll need to specify a base if you also use OpenAI to access OpenAI's actual completions endpoint
WHATEVER_API_KEY: ${WHATEVER_API_KEY:-}
volumes:
- ${HOME}/.pi/agent/models.json:/config/models.json
- ${WORKSPACE:-.}:/workspace
- pi-config:/config
- pi-sessions:/sessions
working_dir: /workspace
volumes:
pi-config:
pi-sessions:
```


and here’s the bash script that runs `pi`

.

```
#!/usr/bin/env bash
# Pi — Start the containerized Pi agent.
# Directory containing this script and the compose files.
SCRIPT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# Workspace to mount into the container.
WORKSPACE_DIR="${WORKSPACE:-$(pwd)}"
case "$WORKSPACE_DIR" in
/*) ;;
*) WORKSPACE_DIR="$(cd -- "$WORKSPACE_DIR" && pwd)" ;;
esac
export WORKSPACE="$WORKSPACE_DIR"
sandbox="${PI_SANDBOX:-0}"
pi_args=()
while (($#)); do
case "$1" in
--sandbox) sandbox=1 ;;
--no-sandbox) sandbox=0 ;;
*) pi_args+=("$1") ;;
esac
shift
done
compose_files=( -f "$SCRIPT_DIR/docker-compose.yml" )
if [[ "$sandbox" == "1" ]]; then
# an even more secure sandbox
compose_files+=( -f "$SCRIPT_DIR/docker-compose.sandbox.yml" )
fi
# Derive a container name from the workspace directory's basename.
# Sanitize to characters Docker accepts: [a-zA-Z0-9][a-zA-Z0-9_.-]*
repo_slug="$(basename -- "$WORKSPACE_DIR" | tr -c 'a-zA-Z0-9_.-' '-' | sed 's/^-*//')"
[[ -z "$repo_slug" ]] && repo_slug="workspace"
container_name="pi-${repo_slug}-$$"
api_key_args=(
-e OPENAI_API_KEY
-e DEEPSEEK_API_KEY
-e ANTHROPIC_API_KEY
-e GEMINI_API_KEY
)
cmd=(
docker compose
--project-directory "$SCRIPT_DIR"
"${compose_files[@]}"
run --rm
--name "$container_name"
"${api_key_args[@]}"
pi
)
if ((${#pi_args[@]})); then
cmd+=("${pi_args[@]}")
fi
exec "${cmd[@]}"
```


I build the Docker container and make changes to the files in its own repo. Then, I run Pi in the repo I’m working in, which spins up Docker so that Pi can’t wipe files or directories by acting on my physical hard drive. This also enables Pi running in the container to see my custom model `json`

config by shipping it into the container. All of this has been working fairly well for my experiments.

There are still issues with local models: inference can be slow, context windows are small and limited to your own hardware, and the ecosystem, although it’s made a ton easier by tooling like LM Studio and HuggingFace’s Use This Model button. Early releases suffer from prompt template mismatches. But, these are usually patched extremely quickly. Needless to say, I’m not sure this is ready for production software development quite yet.

The benefits, though, are numerous and the ecosystem critical to invest in, particularly now. One of the very cool parts of local models is you can introspect almost everything, like watching the token inference process live,

and watching tokens in/out.

You can do things like change the local context window and watch performance improve or degrade, and really dig into how your tokens are processed on the GPU. You can change the system prompt, the quantizations. You can pit models against each other. You can also change and introspect the harness side.

The possibilities are endless, and the tools only keep getting better.