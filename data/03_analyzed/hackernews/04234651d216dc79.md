---
title: 'Golang proposal: container/: generic collection types'
source: https://github.com/golang/go/issues/80590
author:
- '[[jabits]]'
published: '2026-07-31'
created: '2026-08-01'
manifest_dates:
- '2026-08-01'
description: 'Article URL: https://github.com/golang/go/issues/80590 Comments URL:
  https://news.ycombinator.com/item?id=49127031 Points: 154 # Comments: 125'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 04234651d216dc79
source_type: community_discussion
tldr: 'Go 官方 Collections 工作组在 GitHub issue #80590 发布伞状提案，计划为 Go 1.28 标准库新增 hash.Map、hash.Set、set.Set、ordered.Map
  等通用集合类型，并引入基于 F-bounded 多态的未导出抽象约束接口来统一各集合的 API 约定。'
objective_summary: 'Go Collections 工作组于 2025 年底成立，成员包括 Ian Lance Taylor、Robert Griesemer、Alan
  Donovan 等七位核心开发者，旨在为 Go 标准库引入常见集合数据结构。该组在 issue #80590 提出伞状方案，计划为 Go 1.28 新增 hash/maphash.Hasher、container/hash.Map、container/hash.Set、container/set.Set、container/mapset、container/ordered.Map
  和 container/heap/v2.Heap 等类型。由于各集合类型的方法存在二元方法问题，CL 761460 在 container 包中加入了基于 F-bounded
  多态的未导出抽象 Collection、Set、Map 约束接口。目前 Go 标准库仅有 heap 用于优先队列，集合通常以 map[T]bool 表达，有序映射与基于树的集合完全缺失。'
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Google
  technologies:
  - Go generics
  - iter.Seq
  - F-bounded polymorphism
  - container/hash.Map
  - container/set.Set
  - container/ordered.Map
  key_people:
  - Ian Lance Taylor
  - Robert Griesemer
  - Jonathan Amsterdam
  - Alan Donovan
  - Daniel Martí
  - Roger Peppe
  - Keith Randall
key_logic_flow:
- Go Collections 工作组于 2025 年底成立，成员包括 Ian Lance Taylor、Robert Griesemer、Alan Donovan
  等七位核心开发者，旨在把常见集合数据结构引入 Go 标准库。
- 该伞状提案计划为 Go 1.28 新增 hash/maphash.Hasher、container/hash.Map、container/hash.Set、container/set.Set、container/mapset、container/ordered.Map
  与 container/heap/v2.Heap 等通用集合类型。
- 由于各集合类型的 Union 等方法彼此不兼容，存在二元方法问题，CL 761460 在 container 包中定义了未导出的抽象 Collection、Set、Map
  约束接口，采用 F-bounded 多态来表达抽象集合类型。
- 设计上，变更方法尽量返回更多信息以避免重复查找，集合代数操作同时提供纯函数版本与就地修改的 -With 版本，Union 等基础操作保留在接口中以支持高效实现。
- Subset、Arbitrary 等次要操作被从接口中移除并改写为基于抽象集合接口的泛型函数，而 DeleteFunc 因避免 O(n log n) 的渐进退化而保留在接口中。
object_mentions:
- object_type: project
  name: 'golang/go #80590'
  canonical_name: Go generic collection types proposal
  url: https://github.com/golang/go/issues/80590
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 该提案是 Go Collections 工作组为 Go 1.28 引入通用集合数据结构的伞状议题，汇总了 hash.Map、set.Set、ordered.Map
    等多个具体子提案。
  article_id: 04234651d216dc79
- object_type: project
  name: container/hash.Map
  canonical_name: container/hash.Map
  url: https://github.com/golang/go/issues/69559
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - '提案 #69559 的 container/hash.Map[K,V] 是基于自定义哈希函数的哈希映射，配合 maphash.Hasher 使用可支持不可比较的键类型。'
  article_id: 04234651d216dc79
- object_type: project
  name: container/set.Set
  canonical_name: container/set.Set
  url: https://github.com/golang/go/issues/69230
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - '提案 #69230 的 container/set.Set[T] 是元素可比较的规范集合类型，透明地表示为 map[T]struct{}，支持 Union、Intersection
    等全部常用集合操作。'
  article_id: 04234651d216dc79
- object_type: project
  name: container/ordered.Map
  canonical_name: container/ordered.Map
  url: https://github.com/golang/go/issues/60630
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - '提案 #60630 的 container/ordered.Map[K,V] 是有序映射，当前实现基于平衡二叉树，适合需要范围查询等场景。'
  article_id: 04234651d216dc79
- object_type: project
  name: container/heap/v2.Heap
  canonical_name: container/heap/v2.Heap
  url: https://github.com/golang/go/issues/77397
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - '提案 #77397 提出 container/heap/v2.Heap 泛型二叉堆 API，用于替代现有难以使用的标准库 heap 实现。'
  article_id: 04234651d216dc79
- object_type: project
  name: hash/maphash.Hasher
  canonical_name: hash/maphash.Hasher
  url: https://github.com/golang/go/issues/70471
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - '提案 #70471 的 hash/maphash.Hasher 为任意数据类型定义自定义哈希函数与等价关系的标准接口，其代码已在 go1.27 中发布。'
  article_id: 04234651d216dc79
extract_result: success
impact_score:
  score: 6.0
  reason: 这是 Go 官方 Collections 工作组为 Go 1.28 提出的伞状提案，覆盖 hash.Map、set.Set、ordered.Map、heap/v2
    等核心通用集合类型，直接补齐标准库在集合数据结构上的长期短板，会影响所有 Go 开发者的日常编程方式和第三方集合类库的生存空间，属于生态级的基础设施改进。但它本质是渐进式演进——基于已有的
    generics（1.18）与 iter（1.23）能力做标准化收编，既不是新范式也不是颠覆性发布，且当前仍处于设计讨论阶段，实际落地要到 1.28，短期冲击被推迟。综合判定为
    6 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 集合类型的 API 设计约定，尤其是用 F-bounded 多态实现未导出抽象约束接口的复杂度与既有 map/slice 惯用法的取舍
hype_assessment:
  level: low
  reason: 提案内容高度技术化：给出每个子提案的 issue 与 CL 编号、明确渐进复杂度承诺、坦诚说明优化留待后续，并主动指出某些操作（如 Subset、Arbitrary）被降级为泛型函数的设计取舍。通篇无'颠覆''革命性'等
    PR 滥用词汇，属于实打实的工程设计讨论，判定为低水分。
information_entropy: high
domain_disruption:
  technical_innovation: 针对集合类型'二元方法问题'，引入基于 F-bounded 多态（递归约束接口）的未导出 _AbstractCollection/_AbstractSet/_AbstractMap
    约束接口，使标准库泛型函数能跨具体集合类型工作，同时通过'纯函数 + 就地修改 -With 变体'双 API、变更方法返回更多信息等约定建立未来集合类的 API
    规范。
  business_model: 标准库收编 hash.Map/set.Set/ordered.Map 后，将显著压缩 emirpasic/gods 等第三方数据结构库的市场空间，并统一
    Go 生态中集合操作的 API 惯例，使 map[T]bool 这类'民间惯用法'逐步退出新 API；对以 Go 为基础设施层的 AI 与后端 SaaS 工具链是隐性利好。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 这是 Go 官方将常见集合数据结构纳入标准库的长期工程。从复利视角看：(1) 标准库受 Go 1 兼容性承诺保护，一旦进入即永久存在，不会像第三方库那样被弃用或生态分裂，使用量随
    Go 生态同步累积，3-5 年后仍是开发者默认依赖，复利效应可靠；(2) 集合/有序映射/通用堆是数据密集型服务（含 AI 基础设施、向量存储、代理后端）的基础构件，补齐后减少对第三方库的依赖、降低供应链风险，属'地基型'改进；(3)
    但需冷静评估：这是对成熟数据结构的工程补齐而非范式创新，不创造新市场，价值高度分散到整个开发者生态，无单一公司能直接捕获收入，故给中等偏上而非高分的评分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Google
- Cloudflare
- HashiCorp
- Grafana Labs
- Go 开发者生态
competitive_casualty:
- deckarep/golang-set
- emirpasic/gods
- google/btree
- 其他第三方 Go 集合库
market_opportunities:
- AI 基础设施开发者可利用新增的 hash/maphash.Hasher 自定义哈希接口，为 embedding 向量切片等非可比较键类型构建高性能索引、去重与内存缓存组件，解决现有
  map[T]bool 方案的表达局限
- container/ordered.Map 内置平衡二叉树并支持范围查询，为 Go 编写的特征存储、时间序列索引、向量检索排序层等 AI 服务提供更优的有序数据结构，可替代'先建
  map 再手动排序'的惯用写法并降低延迟
- 围绕新集合类型存在生态工具机会：可开发存量代码迁移工具、跨实现性能基准（benchmark 对比标准库与社区第三方库）以及针对 AI 高频场景（如集合代数运算
  Union/Intersection）的适配层与最佳实践指南
risk_matrix:
  regulatory: 无
  technological: 提案仍处于设计阶段，目标为 Go 1.28，API 形态与基于 F-bounded 多态的未导出抽象约束接口方案可能在评审中被调整或推迟；泛型集合在常量因子性能上通常不及手写优化实现，在
    AI 高频关键路径上未必优于现有专用数据结构库
  competitive: 标准库集合类型将对社区第三方库（如 golang-set、emirpasic/gods、google/btree 等）形成明显的生态挤压与替代；相较
    Rust、Zig 等语言更成熟的集合生态，Go 属补齐型追赶，但方案取舍仍面临社区对 API 设计的争议压力
  ethical: 无
  additional:
  - 标准库集合 API 一旦定型将形成长期向后兼容约束，设计取舍（如 DeleteFunc 保留、二元方法问题、纯函数与 -With 就地变体并存）可能限制未来
    API 演进灵活性
  - container/heap/v2 与旧版 heap 并存可能造成社区 API 分裂，部分项目迁移过程中出现版本与用法不一致
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: 'golang/go #80590'
  canonical_name: Go generic collection types proposal
  url: https://github.com/golang/go/issues/80590
  positioning: Go 官方 Collections 工作组为 Go 1.28 标准库引入通用集合数据类型的伞状提案，统一规划 hash.Map、set.Set、ordered.Map
    等容器的 API 约定。
  technical_signal: 提案引入基于 F-bounded 多态的未导出抽象 Collection、Set、Map 约束接口，以解决不同集合类型 Union
    方法的二元方法问题。
  adoption_signal: 工作组由 Ian Lance Taylor、Robert Griesemer、Alan Donovan 等七位核心开发者组成，提案已进入社区讨论阶段。
  ecosystem_relevance: 该提案将补齐 Go 标准库在集合、有序映射与泛型堆等数据结构的空白，直接影响 Go 生态基础库的演进方向。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该提案是 Go 标准库自泛型与迭代器引入以来最大规模的集合 API 扩展，直接影响 Go 1.28 的语言演进路线与生态基础库形态，值得持续跟踪其子提案合并进度与
    API 约定落地情况。
  risk_notes:
  - 提案涵盖多个子包与抽象接口设计，API 兼容与实现复杂度较高，存在超出 Go 1.28 时间窗口的风险。
  - 未导出抽象接口仅用于约束校验而非公共 API，其长期演进策略仍需观察。
  score: 8.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - 该提案是 Go Collections 工作组为 Go 1.28 引入通用集合数据结构的伞状议题，汇总了 hash.Map、set.Set、ordered.Map
    等多个具体子提案。
- object_type: project
  name: container/hash.Map
  canonical_name: container/hash.Map
  url: https://github.com/golang/go/issues/69559
  positioning: 基于自定义哈希函数的泛型哈希映射容器，配合 hash/maphash.Hasher 可支持不可比较的键类型，是 Go 标准库集合提案的核心组件之一。
  technical_signal: 通过 maphash.Hasher 定义自定义哈希与等价关系，突破内置 map 对可比较键的限制，支持切片、映射等不可比较键类型。
  adoption_signal: '作为 #69559 子提案已附带实现 CL 612217，处于 Go 集合提案的推进序列中，尚待标准库正式合并。'
  ecosystem_relevance: 为需要深度比较或不可比较键的高性能场景提供标准库级替代，降低对第三方哈希映射库的依赖。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: hash.Map 是 Go 标准库首次支持不可比较键类型的泛型哈希容器，其 API 设计将影响大量依赖深度比较的底层库与工具链实现，值得跟踪其后续性能优化方向。
  risk_notes:
  - 基于自定义哈希的实现存在碰撞攻击与性能劣化风险，需要持续关注其哈希函数选择与安全设计。
  - 与内置 map 的性能差距尚不明确，可能影响实际采用率。
  score: 7.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - '提案 #69559 的 container/hash.Map[K,V] 是基于自定义哈希函数的哈希映射，配合 maphash.Hasher 使用可支持不可比较的键类型。'
- object_type: project
  name: container/set.Set
  canonical_name: container/set.Set
  url: https://github.com/golang/go/issues/69230
  positioning: 元素可比较的规范泛型集合类型，透明表示为 map[T]struct{}，提供 Union、Intersection 等完整集合操作，被视为未来
    Go 新 API 的标准集合。
  technical_signal: 以 map[T]struct{} 作为底层表示，避免 map[T]bool 的 false 值歧义，并完整覆盖常用集合代数操作。
  adoption_signal: 提案预期成为大多数新 Go API 的标准集合类型，具有较高生态普及潜力，已附实现 CL 745441。
  ecosystem_relevance: 补齐 Go 标准库长期缺失的集合类型，将改变社区以 map[T]bool 表达集合的既有惯例。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: set.Set 被提案明确期待成为 Go 生态的标准集合类型，其 API 约定将深刻影响未来库的设计规范，值得跟踪其落地与社区采纳情况。
  risk_notes:
  - 基于 map[T]struct{} 的实现对元素类型有可比较性限制，复杂键场景仍需依赖 hash.Set。
  score: 7.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - '提案 #69230 的 container/set.Set[T] 是元素可比较的规范集合类型，透明地表示为 map[T]struct{}，支持 Union、Intersection
    等全部常用集合操作。'
- object_type: project
  name: container/ordered.Map
  canonical_name: container/ordered.Map
  url: https://github.com/golang/go/issues/60630
  positioning: 基于平衡二叉树实现的泛型有序映射容器，面向范围查询等内置 map 表现不佳的场景，是 Go 标准库集合提案的重要组成。
  technical_signal: 当前实现采用平衡二叉树支撑有序遍历与范围查询，设计上不限定具体数据结构，保留后续演进空间。
  adoption_signal: '作为 #60630 子提案处于讨论阶段，尚未提供合并实现，落地进度较 hash.Map 等子提案滞后。'
  ecosystem_relevance: 填补 Go 标准库完全缺失的有序映射能力，为需要有序遍历的中间件与算法库提供原生选择。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: ordered.Map 是 Go 标准库中首个有序映射类型，其 API 与实现选择将决定标准库对范围查询类场景的支持力度，值得跟踪其数据结构演进。
  risk_notes:
  - 平衡二叉树实现相对复杂，性能与内存开销需与 map 加排序模式持续对比验证。
  score: 6.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - '提案 #60630 的 container/ordered.Map[K,V] 是有序映射，当前实现基于平衡二叉树，适合需要范围查询等场景。'
- object_type: project
  name: container/heap/v2.Heap
  canonical_name: container/heap/v2.Heap
  url: https://github.com/golang/go/issues/77397
  positioning: 替代现有标准库 heap 的泛型二叉堆 API，旨在解决原实现接口难用的问题，是 Go 集合提案中面向基础数据结构的改进。
  technical_signal: 以泛型方式重构二叉堆 API，简化优先队列的构造与使用流程，v2 命名标识其与旧 API 的演进关系。
  adoption_signal: '作为 #77397 子提案处于讨论阶段，尚未给出合并实现，落地优先级相对靠后。'
  ecosystem_relevance: 泛型堆 API 将降低 Go 生态中优先队列实现的门槛，影响依赖堆结构的算法库与调度器设计。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: v2.Heap 是对标准库既有 heap 接口的现代化改造，其 API 形态将影响大量优先队列使用场景的代码迁移，值得跟踪其设计定稿。
  risk_notes:
  - v2 命名与旧 heap 包并存可能造成生态分裂，迁移成本与兼容策略仍需观察。
  score: 5.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - '提案 #77397 提出 container/heap/v2.Heap 泛型二叉堆 API，用于替代现有难以使用的标准库 heap 实现。'
- object_type: project
  name: hash/maphash.Hasher
  canonical_name: hash/maphash.Hasher
  url: https://github.com/golang/go/issues/70471
  positioning: 为任意数据类型定义自定义哈希函数与等价关系的标准接口，其代码已在 go1.27 发布，是整个集合提案的底层基础能力。
  technical_signal: 定义标准化的哈希与等价关系接口，支持不可比较键类型与深度比较场景，已随 go1.27 实际发布并被 Bloom filter
    示例验证。
  adoption_signal: 代码已在 go1.27 中发布，是集合提案中首个实际落地的基础组件，后续集合类型均依赖该接口。
  ecosystem_relevance: 为 Go 生态提供统一的哈希抽象标准，可支撑 Bloom filter、自定义容器等广泛的底层库实践。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: maphash.Hasher 作为最先落地的集合提案基础组件，其接口稳定性直接影响 hash.Map、hash.Set 等后续容器的设计，值得持续跟踪其生态采用。
  risk_notes:
  - 自定义哈希接口的滥用可能引入哈希碰撞与安全风险，需关注社区在安全性上的最佳实践沉淀。
  score: 7.0
  article_ids:
  - 04234651d216dc79
  evidence_snippets:
  - '提案 #70471 的 hash/maphash.Hasher 为任意数据类型定义自定义哈希函数与等价关系的标准接口，其代码已在 go1.27 中发布。'
---

# proposal: container/...: generic collection types #80590

## Description

**Background:** The Go Collections working group was formed in late 2025 with the purpose of bringing common collection data structures to the standard library, guided by the familiar Go principles of pragmatism and simplicity. Alphabetically by last name, the group consists of Jonathan Amsterdam (@jba), Alan Donovan (@adonovan), Robert Griesemer (@griesemer), Daniel Martí (@mvdan), Roger Peppe (@rogpeppe), Keith Randall (@khr), and Ian Lance Taylor (@ianlancetaylor). We’ve now reached a point where we’re ready to share our results with the community.

This issue is an umbrella for discussing several related proposals for new collections APIs for Go 1.28. It presents a high level overview of the themes, and links to the various concrete proposals and associated implementation CLs.

Go currently provides few collection types in its library, and from the outset we have emphasized the flexibility of the language’s built-in slice and map types. Of those provided, the most important is the heap, used for priority queues. Even sets are absent; they are conventionally expressed in terms of map[T]bool or map[T]struct{}. Ordered maps and sets based on binary trees are entirely absent.

Since the addition of generics in Go 1.18 and iterators in Go 1.23, it has become possible for library-defined types to achieve comparable ergonomics to built-in types, and for many common operations on slices and maps to be expressed as calls to library functions. This work seeks to add several of the more important data types to the standard library, and to establish conventions for their APIs and those of future additions.

**Proposal:** The proposed additions include:

-
#70471, CL 657296 (released in go1.27):

**hash/maphash.Hasher**: a standard interface for expressing custom hash functions and equivalence relations for arbitrary data types. These may differ from the compiler-defined ones used by map[K]V, and are useful when the key type is not comparable (such as a slice or map), or when the default comparison yields the wrong result (such as for types.Type values, which need the deep comparison operation types.Identical). Its package docs include an example of its use in a Bloom filter. -
#69559, CL 612217

**: container/hash.Map[K,V]**: a hash-based Map that uses the custom hash functions mentioned above. -
#80584, CL 741160:

**container/hash.Set[T]**: a hash-based Set along the same lines. -
#69230

**,**CL 745441**: container/set.Set[T]**: a canonical data type for sets whose elements are comparable. It is transparently represented as map[T]struct{} and supports all the usual set operations such as Union and Intersection. It is more convenient than “legacy” sets based on map[T]bool and map[T]struct{}, and avoids ambiguity about potential false values in a map[T]bool. We expect it to become the standard set in most new Go APIs. -
#77052

**,**CL 724420**: container/mapset**: a package of helper functions (Union, Intersection, and so on) for conveniently manipulating legacy sets as sets in existing code whose API cannot be changed. These functions are exactly parallel to the methods of set.Set. -
#60630

**: container/ordered.Map[K,V]**: an ordered mapping. The current implementation uses a balanced binary tree, but nothing in the design requires that. The common Go pattern of building a map[K]V then sorting its keys performs well in most cases, but on occasion, such as when a range query is needed, other data structures perform much better. -
#77397

**: container/heap/v2.Heap**: a generic binary heap API to replace the standard library's existing heap, which can be difficult to use.

We expect to consider additional proposals in due course, such as **insertion-ordered hash maps** (#80194) and **stacks**.

The initial implementations of all the proposed data structures aim to satisfy the API and asymptotic performance expectations as simply as possible. There are doubtless many opportunities for later optimizations to reduce constant factors, but they are out of scope of the proposal process.

Though the new packages will live in the existing container tree, we prefer the term “collection” to avoid confusion with the container virtualization concept from Linux.

**Abstract collection constraint interfaces**

Most of the methods of the new Map and Set types are not particular to any concrete representation type, but are common across all Maps and Sets. However, they are not really implementions of a common interface type because of the “binary method problem”: if each set data type S has a Union method of the form `func (S) Union(S) S`

, then the Union methods of different set types are incompatible, so they have no common ordinary interface. To express this abstract Set type, we must use F-bounded polymorphism, or recursive constraint interfaces.

CL 761460 adds to the **container** package *unexported* abstract Collection, Set, and Map constraint interface types that permit package implementors to write abstract helper functions (such as ContainsAny, Subset, or Arbitrary) that work across a range of concrete collection, set or map types. We reproduce these interfaces below, with some brief commentary, to help give a high-level picture *but they are not part of any proposal*. They merely serve to guarantee conformance in tests. See the individual proposals for more detail.

```
// _AbstractCollection models a collection C of elements E,
// such as *hash.Map, *hash.Set, *ordered.Map, or set.Set.
type _AbstractCollection[E any, C _AbstractCollection[E, C]] interface {
Clear()
Clone() C
Contains(E) bool
ContainsAll(iter.Seq[E]) bool
Len() int
String() string
}
// _AbstractMap models a mapping M from keys K to values V,
// such as *hash.Map or *ordered.Map.
type _AbstractMap[K, V any, M _AbstractMap[K, V, M]] interface {
_AbstractCollection[K, M]
All() iter.Seq2[K, V]
At(K) V
Delete(K) (V, bool)
DeleteAll(iter.Seq[K]) bool
DeleteFunc(func(K, V) bool) bool
Get(K) (V, bool)
Keys() iter.Seq[K]
Set(K, V) (V, bool)
SetAll(iter.Seq2[K, V]) bool
Values() iter.Seq[V]
}
// _AbstractSet models a set S of elements E,
// such as *hash.Set, or set.Set.
type _AbstractSet[E any, S _AbstractSet[E, S]] interface {
_AbstractCollection[E, S]
All() iter.Seq[E]
Delete(E) bool
DeleteAll(iter.Seq[E]) bool
DeleteFunc(func(E) bool) bool
Difference(S) S
DifferenceWith(S)
Equal(S) bool
Insert(E) bool
InsertAll(iter.Seq[E]) bool
Intersection(S) S
IntersectionWith(S)
Intersects(S) bool
SymmetricDifference(S) S
SymmetricDifferenceWith(S)
Union(S) S
UnionWith(S)
}
```

For now these abstract types are non-exported and merely serve as documentation of Go’s conventions to help ensure consistency. We do not propose to publish them yet, but may do so a later release after gaining experience with the concrete collection types. In the meantime, users can define minimal constraint types as needed, as in this example (from CL 761460) of a generic Take function over abstract sets:

```
// _TakeSet defines an abstraction of a set sufficient for the [Take] function.
type _TakeSet[E any, S _TakeSet[E, S]] interface {
All() iter.Seq[E]
Delete(E) bool
}
// Take removes and returns an arbitrary element from a set.
// It returns zero if the set was empty.
func Take[S _TakeSet[E, S], E any](set S) (e E, found bool) {
for e = range set.All() {
found = true
set.Delete(e)
break
}
return
}
```

There is a certain arbitrariness to the set of methods included in each interface. For some data structures, a method permits a more efficient specialized implementation. But if every possible operation were added to the interface, the burden on the implementor would be unreasonable.

For instance, should the Set interface include a Subset(Set) bool method, or should Subset be written as a generic operation over abstract sets, like the Take example? Ordered sets can quickly reject a Subset test when the two operands have disjoint ranges, but even so, in the common case a Subset test is still typically O(n), so we decided to omit Subset from the interface. By contrast, we retained DeleteFunc in the Set and Map interfaces because, without it, conditionally deleting each element of a tree is asymptotically worse: O(n log n) instead of O(n).

By delaying the commitment to a particular set of methods, we can learn from practice. It may turn out that we don’t need to publish canonical constraint types at all.

**Miscellaneous rationalizations**

The remainder of this doc briefly notes a few of the many small design choices that led to the current set of proposals.

Methods return as much information as possible to avoid repeated lookups. For example:

- Most mutation methods report whether they changed the size of the collection.
- Map.Set and Map.Delete return the previous key if any, plus a boolean so that an existing key can be distinguished from the zero value.
- Get is variant of At that provides the boolean. (At is provided for convenience of use in expressions.)

Map.Set should replace any existing entry with the same key, following the built-in map.

Unlike Sets, maps have no Equal method, because map values may be non-comparable.

The fundamental set operations (Intersects, Union, etc) are part of the Set interface to enable efficient concrete implementations across a variety of representations, even though many of these could be expressed abstractly in terms of just All, Len, and Contains, as shown in this table, at some asymptotic cost in performance:

```
- Union{,With} interface{ All() iter.Seq\[E\] }
- Intersection interface{ All() iter.Seq\[E\]; Contains(E) bool; Len() int }
- IntersectionWith interface{ Contains(E) bool }
- Difference interface{ All() iter.Seq\[E\]; Contains(E) bool }
- DifferenceWith interface{ All() iter.Seq\[E\]; Contains(E) bool }
- SymmetricDifference{,With} interface{ All() iter.Seq\[E\] }
```


Secondary operations such as Set.{Take,Arbitrary,Subset,Superset} were removed from the interface and expressed as generic operations using the abstract set interface, again at some potential cost in asymptotic performance. DeleteFunc was retained.

Set algebra operations such as Union are purely functional, returning their result as a new set. Each comes with a -With variant that mutates its left operand and returns no result. The two variants are “convenient” and “allocation efficient”, respectively. We rejected the idea of merging them into a single method based on experience with the math/big.Int API, and to avoid risks of accidental mutation or forgetting to use the result.

It is possible to define a KeySetView[M, K, V] wrapper type that satisfies the Set[K] abstraction using the key set of an underlying Map[K,V] of type M. (Inserting an element to the Set is of course meaningless and must panic.)

For symmetry, let’s consider each of the AbstractMap methods and the operations provided by the existing ‘maps’ package:

- no maps.Clear: served by builtin clear(m)
- AbstractMap.Clone = maps.Clone
- no maps.Contains; served by _, ok = m[k]; but see proposal #67377
- no maps.ContainsAll: served efficiently by for k := range seq { _, ok = m[k], … }
- no maps.Len: served by builtin len(m)
- AbstractMaps.All = maps.All
- no maps.At: served by m[k]
- no maps.Delete: served by delete(m, k)
- no maps.DeleteAll: served efficiently by for k = range seq { delete(m, k) }
- AbstractMaps.DeleteFunc = maps.DeleteFunc
- no maps.Get: served by v, ok = m[k]
- AbstractMaps.Keys = maps.Keys
- no maps.Set: served by prev, ok = m[k]; m[k] = newval
- AbstractMap.SetAll = maps.Insert
- AbstractMaps.Values = maps.Values

Five of them (Clone, DeleteFunc, All, Keys, Values) are exactly parallel. One operation (AbstractMap.SetAll) has a different name (maps.Insert). All the rest are served by built-in operators.

We might want to propose adding maps.{Contains,ContainsAll,DeleteAll}. Contains is more useful than _, ok = s[k] in an expression context; ContainsAll and DeleteAll avoid the need for loops and boolean bookkeeping.