---
title: Building and shipping Mac and iOS apps without opening Xcode
source: https://scottwillsey.com/building-and-shipping-mac-and-ios-apps-without-ever-opening-xcode/
author:
- '[[speckx]]'
published: '2026-07-13'
created: '2026-07-14'
description: 'Article URL: https://scottwillsey.com/building-and-shipping-mac-and-ios-apps-without-ever-opening-xcode/
  Comments URL: https://news.ycombinator.com/item?id=48896665 Points: 464 # Comments:
  202'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 8ec874c15e3bf9b7
source_type: community_discussion
tldr: 本文介绍了一套无需打开 Xcode GUI 即可完成 macOS 和 iOS 应用开发与发布的完整工作流：通过 XcodeGen 管理项目配置、xcodebuild
  编译构建、notarytool 公证签名、devicectl 部署到真机，并借助 Claude Code 等 LLM 工具自动执行整个流程。
objective_summary: 开发者 Scott Willsey 在其博客中详细描述了如何通过命令行工具链和 AI 编码助手，在不打开 Xcode 图形界面的情况下完成
  Apple 平台应用的完整开发与发布流程。关键方法包括：安装 Xcode 但使用其内置命令行工具（xcodebuild、notarytool、stapler、devicectl）；使用
  XcodeGen 通过 YAML 配置文件自动生成 Xcode 项目；一次性初始配置包括创建 Developer ID Application 证书和存储公证凭据；编写
  release.sh 脚本实现归档、签名、公证、钉装和安装的一键部署；通过 CLAUDE.md 文件让 AI 代理自动使用该部署脚本。该工作流仅需一次性的 GUI
  初始设置，后续所有构建和发布操作均在终端中完成。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Apple
  technologies:
  - Xcode
  - XcodeGen
  - xcodebuild
  - notarytool
  - devicectl
  - stapler
  - codesign
  - CLI
  key_people:
  - Scott Willsey
key_logic_flow:
- 安装 Xcode 并确保使用其内置工具链（/Applications/Xcode.app/Contents/Developer），而非独立的 Command Line
  Tools，从而获取 iOS SDK、notarytool、devicectl 等完整开发工具。
- 安装 XcodeGen 后通过 project.yml 文件管理项目配置，每次构建时自动重新生成 .xcodeproj 文件夹，使 .xcodeproj 可被
  git 忽略，避免 Xcode 自动修改造成的版本控制冲突。
- 一次性 GUI 初始配置包括接受 Xcode 许可证、登录 Apple 开发者账号、创建 Developer ID Application 证书（私钥不可重新下载，需备份钥匙串）、以及通过命令行交互式存储
  notarytool 公证凭据。
- 创建 release.sh 脚本串联完整的发布流水线：xcodebuild 归档、-exportArchive 导出并签名、notarytool 上传 Apple
  进行恶意软件扫描、stapler 附加公证票据、spctl 验证签名，最后安装到 /Applications 目录。
- 通过 CLAUDE.md 文件告知 AI 编码助手使用 release.sh 脚本，实现从开发到部署的全自动化流程。
- 快速开发循环使用 CODE_SIGNING_ALLOWED=NO 进行无签名编译；iOS 真机部署使用 devicectl 和 -allowProvisioningUpdates
  自动获取开发配置文件。
extract_result: success
object_mentions:
- object_type: project
  name: XcodeGen
  canonical_name: XcodeGen
  url: https://github.com/Yonaba/XcodeGen
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - XcodeGen 通过 project.yml 文件管理所有项目设置，每次构建时重新生成整个 .xcodeproj 文件夹，只有 YAML 文件需提交到 git。
  - XcodeGen 可以在 homebrew 或 GitHub 上安装，是无需打开 Xcode GUI 即可管理项目配置的核心工具。
  article_id: 8ec874c15e3bf9b7
- object_type: product
  name: Claude Code
  canonical_name: Claude Code
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 作者使用 Claude Code 创建 release.sh 部署脚本，仅需描述需求即可让 LLM 生成完整的构建流水线代码。
  - 通过 CLAUDE.md 文件告知 Claude Code 自动使用 release.sh 脚本，实现从开发到部署的全自动化流程。
  - 作者让 Claude Code 访问 1Password 保管库，使其能自动创建新的 notarytool 公证凭据而无需手动操作。
  article_id: 8ec874c15e3bf9b7
- object_type: product
  name: Xcode
  canonical_name: Xcode
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Xcode.app 必须安装，因为 xcodebuild、notarytool、stapler 和 devicectl 都位于 Xcode 内部，但从不需要打开
    GUI。
  - 创建 Developer ID Application 证书需要打开 Xcode GUI 一次，后续所有构建和签名操作均通过命令行完成。
  article_id: 8ec874c15e3bf9b7
impact_score:
  score: 4.0
  reason: 该文章介绍了一套Mac/iOS应用从构建到发布的纯命令行工作流，使LLM驱动的'vibe coding'能够在Apple生态中落地。虽然不是技术突破，但对AI辅助Apple开发有实质性的工具链价值，降低了AI编码工具在Apple平台的门槛，属于局部开发者体验优化而非行业级影响。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 摆脱Xcode GUI依赖，实现完全由LLM/命令行驱动的Apple应用开发工作流
hype_assessment:
  level: low
  reason: 文章为技术操作指南，没有使用'颠覆'、'革命性'等夸张词汇，内容扎实具体，包含完整的命令行步骤和配置说明，属于实打实的干货。
information_entropy: high
domain_disruption:
  technical_innovation: 将Apple生态的构建、签名、公证、发布全流程从Xcode GUI迁移到命令行，使AI编码助手（如Claude Code）能够端到端地完成macOS/iOS应用的开发与分发，打通了'vibe
    coding'在Apple平台的最后障碍
  business_model: 无
engineering_complexity: production_ready
compound_value:
  score: 4.0
  reason: 该事件本身并非产品/公司发布，而是一种工作流模式（LLM 驱动的 Apple 原生应用 CLI 开发流水线）。从 VC 视角看，核心价值在于验证了
    AI 编程助手（如 Claude Code）已能端到端驱动 Apple 生态的构建、签名、公证、分发全链路，无需人类打开 Xcode GUI。这为'AI Agent
    作为默认 IDE'的趋势提供了实证，具有一定的范式示范效应。但局限性也明显：该工作流高度依赖 Apple 工具链的 CLI 接口稳定性，且 XcodeGen
    是开源项目无商业模式护城河。长期复利效应中等——若 AI 编程工具成为 Apple 开发者的标配入口，整个 AI Agent 中间件层将受益，但单篇博客的边际贡献会随时间递减。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Anthropic
- Apple
- XcodeGen
competitive_casualty:
- 传统 IDE 厂商（JetBrains AppCode）
- 低代码/无代码平台
- GUI 绑定的构建服务
market_opportunities:
- Apple生态内的开发者工具团队可围绕 XcodeGen + xcodebuild 推出可视化CI/CD配置面板或一键部署模板，降低中小团队采用无GUI构建流程的门槛
- AI代码辅助工具（如Claude Code）可深度集成该工作流，实现从自然语言需求到Mac/iOS应用签名、公证、分发的全自动化闭环
- 面向企业内训或开发咨询的服务商可提供"Xcode-free 构建流水线"培训课程，帮助传统iOS团队迁移至可复现的命令行构建体系
risk_matrix:
  regulatory: Apple 可能在未来更新开发者协议或公证政策（如强制要求特定Xcode版本、调整证书有效期规则），导致基于命令行的构建流程需要额外适配
  technological: xcodebuild 和 notarytool 等CLI工具依赖Xcode.app内部路径，Apple若重构工具链或迁移至新工具（如Swift
    Bundler），现有脚本可能失效；XcodeGen 作为第三方工具存在维护风险
  competitive: Apple 持续改进Xcode GUI性能和稳定性，并推出Xcode Cloud等云构建服务，可能削弱无GUI工作流对开发者的吸引力
  ethical: 无
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: XcodeGen
  canonical_name: XcodeGen
  url: https://github.com/Yonaba/XcodeGen
  positioning: XcodeGen 通过 YAML 配置驱动 Xcode 项目生成，解决项目文件的版本控制痛点，是实现 Apple 平台无 GUI 开发工作流的核心工具。
  technical_signal: XcodeGen 基于 project.yml 文件在每次构建时重新生成完整的 .xcodeproj 文件夹，使项目配置完全由文本管理且可被
    git 忽略。
  adoption_signal: null
  ecosystem_relevance: XcodeGen 填补了 Apple 命令行开发工作流中项目配置管理的空白，与 xcodebuild、notarytool
    等工具构成完整的无 GUI 构建流水线。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: XcodeGen 是实现 Apple 平台 AI 辅助编码工作流（如 Claude Code）的关键基础设施，通过 YAML 配置化的项目管理和命令行构建流水线，让开发者无需打开
    Xcode GUI 即可完成完整开发与发布流程，代表了 Apple 生态中命令行化和 AI 驱动开发的重要趋势。
  risk_notes:
  - XcodeGen 依赖 Xcode 内置工具链，Apple 系统更新可能导致兼容性问题或需要同步升级。
  score: 5.0
  article_ids:
  - 8ec874c15e3bf9b7
  evidence_snippets:
  - XcodeGen 通过 project.yml 文件管理所有项目设置，每次构建时重新生成整个 .xcodeproj 文件夹，只有 YAML 文件需提交到 git。
  - XcodeGen 可以在 homebrew 或 GitHub 上安装，是无需打开 Xcode GUI 即可管理项目配置的核心工具。
- object_type: product
  name: Xcode
  canonical_name: Xcode
  url: null
  positioning: Xcode 是 Apple 官方的集成开发环境，提供 macOS 和 iOS 应用开发所需的完整工具链，包括编译器、调试器、公证和部署工具。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - macOS 和 iOS 应用开发者
  - Apple 平台独立开发者和团队
  product_signal: Xcode 内置的 xcodebuild、notarytool、stapler 和 devicectl 等命令行工具支持完全无
    GUI 的构建、签名、公证和真机部署流程。
  market_signal: null
  differentiation: Xcode 的内置命令行工具链覆盖从开发到发布完整生命周期，无需第三方工具即可完成全流程自动化和 CI/CD 集成。
  watch_reason: 作为 Apple 官方 IDE，Xcode 的内置命令行工具链是实现无 GUI 开发工作流和 AI 辅助编码的基础设施，其 CLI
    能力的演进直接影响 Apple 平台自动化开发效率和第三方工具生态。
  risk_notes:
  - Xcode 体积庞大且更新频繁，可能对 CI/CD 环境产生存储和兼容性压力。
  - notarytool 使用的应用专用密码会在 Apple ID 密码变更后静默失效，需及时更新。
  score: 3.0
  article_ids:
  - 8ec874c15e3bf9b7
  evidence_snippets:
  - Xcode.app 必须安装，因为 xcodebuild、notarytool、stapler 和 devicectl 都位于 Xcode 内部，但从不需要打开
    GUI。
  - 创建 Developer ID Application 证书需要打开 Xcode GUI 一次，后续所有构建和签名操作均通过命令行完成。
---

Lately, I’ve heard several Apple related podcasters talk about how bad Xcode is, and how Apple needs to make vibe-coding Mac and iOS apps better by making Xcode less inscrutable. They’re not wrong, but also I don’t understand why they’re even opening Xcode in the first place. With a little bit of pre-work, you can vibe code Mac and iOS apps to your heart’s content without looking at Xcode anymore.

And if you’re ever in doubt about how to make any of the following work, point Claude Code or your LLM coding tool of choice to this blog post, and let it figure it out. That’s literally its job, figuring out things you don’t want to have to.

## TL;DR

**Xcode.app must be installed, but it never has to be open.**`xcodebuild`

,`notarytool`

,`stapler`

, and`devicectl`

all live inside Xcode and run fine from a shell.**A few one-time steps do need the GUI**(or an interactive terminal): sign into your Apple ID, create a Developer ID certificate, store a notarization password. After that, builds and deploys are fully headless.**The Mac app ships via one script**—`scripts/release.sh`

— which you write once. It runs the whole chain: archive → Developer ID sign → notarize → staple → install to`/Applications`

.**Signing is certificate-and-keychain based.**The signing key lives in the login keychain;`xcodebuild`

finds it automatically. No secrets in the repo.

The one-time setup is the only part with any friction, so let’s get it out of the way first.

## Install Xcode

You do have to have Xcode installed, there’s no getting around that, because build depends on tools that live inside Xcode.app.

Once Xcode is installed, make sure it’s the selected command line toolchain, and not `/Library/Developer/CommandLineTools`

. If the output of the check is `/Applications/Xcode.app/Contents/Developer`

, you’re in good shape:

If it DOES return the path for the standalone CommandLineTools instead, point it to Xcode.

**NOTE: The name “Command Line Tools” can be confusing.**

This is because there’s a standalone Command Line Tools package, available with `xcode-select --install`

, which is the `/Library/Developer/CommandLineTools`

version. This contains clang and git, but not the iOS SDK, notarytool, devicectl, and other items needed for full app development.

The complete toolchain is inside Xcode.app, at `/Applications/Xcode.app/Developer`

, and it has everything you need. If you have Xcode installed, you don’t need the standalone Command Line Tools.

## Install XcodeGen

Xcode and its command line tools aren’t enough to generate and manage Xcode projects automatically. For that, you’re going to need XcodeGen. You can download it from Github or install it using homebrew:

Long story short, Xcode projects are actually folders that macOS makes appear as files, and they contain everything about your project needed to create and compile your app. Xcode constantly modifies the files and file references constantly, and it creates issues for git repositories.

Xcodegen creates a project.yml (YAML) file with all your project settings, and then on every build, it recreates the entire .xcodeproj folder using that project.yml file. Only the YAML file has to be committed to git, and the whole .xcodeproj can be ignored from git’s perspective.

## Configure Xcode, Once

You do need to setup Xcode initially in order to never have to look at it again.

### Xcode License and Additional Components

First, either accept its license and install its additional components, or do it through the command line:

### Setup Your Apple Developer Account in Xcode

Next, open Xcode, click on Settings → Accounts and click on + to add your account.

**NOTE: You have to have a paid Apple Developer account in order to distribute and notarize your apps.**

And you will want them notarized in order to install them on you Mac and iOS devices and not have the OSes decided they’re malware and delete them.

### Create a Developer ID Application Certificate

Once that’s done, create a Developer ID Application certificate (Settings → Accounts → your Apple ID → **Manage Certificates…** → **+** → **Developer ID Application**), which creates a cert for signing the shipped .app bundle.

Please note that a Developer ID Application certificate and your Apple Development certificate are two separate things.

The **Apple Development** identity is for building and running on your own devices — pushing to your iPhone, local debugging. The **Developer ID Application** identity is for the notarized `.app`

that survives Gatekeeper and runs on someone else’s Mac. The release script wants that second one.

Creating the certificate in Xcode installs both the certificate *and* its private key into your login keychain. That private key is what actually does the signing, and it **cannot be re-downloaded** — so don’t delete it, and back up your keychain.

When in doubt, ask your LLM of choice about them and have it help you get set up. It’s the one that’s going to be using Xcode for you anyway.

And finally,

### Store a Notarization Credential – Once, in Terminal

Notarization uploads your signed app to Apple for a malware scan. `notarytool`

authenticates using a stored keychain profile that you create once, interactively — it prompts for an app-specific password, and there’s no way around the prompt:

A few things worth knowing here:

**Name the profile after the app.**Don’t borrow another app’s profile — it’ll work on your machine and then silently break on someone else’s.**The app-specific password is not your Apple ID password.**Generate one at appleid.apple.com →**Sign-In & Security → App-Specific Passwords**.**These passwords go stale silently**whenever you change your Apple ID password. A`401 invalid credentials`

out of notarization almost always means “go make a fresh app-specific password,” not “your setup is broken.”

Confirm it’s stored:

Side topic here, I store my app-specific passwords in a 1Password vault that Claude Code has access to. That way whenever I’m creating a new app, I can tell **IT** to create the notarization credential for me, and it knows to check its 1Password vault for the password. The whole point of using the LLM in the first place is to avoid doing things manually that you don’t want to do.

### Setup a Local.xconfig File and Add It to .gitignore

Real signing needs your team ID and bundle prefix, and I put those in a Local.xconfig file:

Again, if in doubt, ask Claude Code or your LLM of choice to create this for you.

## Set up the Agent Tools

### Create the Deploy Script

Deployment on my apps is handled via a script called `release.sh`

that lives in a `scripts`

folder inside the repo. Without it, I don’t have an automated build pipeline.

I had Claude Code create mine: I told Claude, more or less: *I want to archive, Developer ID-sign, notarize, staple, and install this app to /Applications without ever opening Xcode. Write me a script that does the whole chain and fails loudly if any step breaks.*

It didn’t need me to explain the pipeline, because the pipeline isn’t a secret — archive with `xcodebuild`

, export with `-exportArchive`

and an `ExportOptions.plist`

, submit with `notarytool --wait`

, attach the ticket with `stapler`

, check with `spctl`

. That’s the documented, conventional way to ship a Developer ID Mac app, and the model knows it. What it needed from *me* was the project-specific stuff: the scheme name, the team ID, what to call the notary profile, where to install the result.

Then it wrote a first draft, we ran it, it broke, and we fixed it. That loop is not a failure mode, it’s just the process. I always look at AI workflows as works in progress, but it doesn’t take long before you can stop tweaking things and just start working.

This is the actual script from one of my app repos:

It looks more complicated than it is, but it is a series of steps that you’d have to know need performed. Again, this is why you talk to your LLM, tell it what you want, and have it help build your workflow.

Some things to note:

`set -euo pipefail`

halts the script on any failing command immediately instead of blundering forward. There’s no half-finished state that *looks* like success.

`cd "$(dirname "$0")/.."`

means the script hops to the repo root regardless of where you invoked it from, so `./scripts/release.sh`

works whether you’re in the repo root or three directories down.

The **pre-flight block** checks that `xcodegen`

exists and that the notary profile is actually stored *before* spending five minutes on an archive that’s doomed to fail at step five.

And the last two steps re-verify the *installed* bundle, not just the exported one. Belt and suspenders, but I’ve had a copy step silently mangle a bundle before and I’d rather find out from the script than from Gatekeeper three days later when it deletes my app for me.

### Create CLAUDE.md or AGENTS.md

`release.sh`

gives you a one-command deploy. `CLAUDE.md`

(or `AGENTS.md`

for basically every other model under the sun) is what makes the agent actually *use* it without being told every single time.

I had Claude create my CLAUDE.md itself after going back and forth about the build process. Now whenever I create a new app, I tell it to reference the repo for one of my other apps and use the same methodology.

## That’s It

And that’s the whole one-time setup. From here on, nothing needs a mouse.

## How the build actually runs — no GUI in the loop

Everything below is plain command-line invocation. Xcode.app never launches; these tools live inside it but run standalone. This is exactly what Claude Code executes through its shell.

### Fast, Unsigned Checks

For “does it compile / do the tests pass,” you don’t need signing at all:

`CODE_SIGNING_ALLOWED=NO`

gives you an **ad-hoc** build: it compiles and runs in a simulator, but Gatekeeper rejects it and entitlements like iCloud KVS and App Group don’t bind. That’s the fast inner loop.

### The Mac Release Pipeline

One command does the entire shippable chain — the script from Part two:

Archive, Developer ID export, notarize, staple, verify, install. If any step fails it stops and tells you which one broke. Need a different notary profile? Override it: `TZED_NOTARY_PROFILE=<name> ./scripts/release.sh`

.

### Deploying to a Real iPhone, Headless

iOS has no notarization step — that’s a Mac-distribution concept. Getting a build onto a connected iPhone is `xcodebuild`

plus `devicectl`

, both inside Xcode’s toolchain:

`devicectl list devices`

lists connected and paired devices with their UDIDs. Device builds sign with the **Apple Development** identity (not Developer ID) plus a development provisioning profile, which `-allowProvisioningUpdates`

fetches for you.

## How code signing works when there’s no GUI

If you’ve only ever signed apps by ticking a box in Xcode, it’s worth understanding what’s actually going on under there, because none of it needs the GUI at build time.

**The private key does the signing.** When you created that Developer ID Application certificate, Apple issued a certificate and your Mac generated a matching private key, both landing in your login keychain. `codesign`

(which `xcodebuild`

calls) uses the private key to sign the binary; the certificate — which chains up to Apple’s root — gets embedded so anyone can verify it.

**Automatic signing picks the identity for you.** The release script uses `signingStyle: automatic`

, so `xcodebuild`

selects the right identity by team ID and pulls any needed provisioning profile from Apple on the fly. No profiles checked into the repo.

**Entitlements bind at sign time.** Each target has a `.entitlements`

file (sandbox, network client, iCloud KVS, App Group). These only take effect when the app is signed with a real team identity, which is the *other* reason ad-hoc builds can’t ship: the iCloud and App-Group entitlements quietly don’t bind without the team prefix, and you get to spend an hour wondering why your key-value store is empty.

**Notarization isn’t signing.** Signing proves *who* built the app. Notarization is a separate step where Apple scans the signed app for malware and issues a ticket; stapling attaches that ticket so Gatekeeper trusts the app offline. For a hidden-UI menu bar app (`LSUIElement`

), notarization is what keeps XProtect from flagging it.

**The secrets never touch git.** The signing private key lives in the login keychain. The notarization app-specific password lives in the notarytool keychain profile. Neither ever gets written into the repo.

You can verify any signed build by hand:

## What the agent actually uses

There’s no magic here. Claude Code drives all of this through a plain, non-interactive shell — there’s no special “build” MCP server or plugin doing something clever. It’s `xcodebuild`

, `xcrun notarytool`

, `xcrun stapler`

, `spctl`

, `codesign`

, `devicectl`

, `xcodegen`

, and `swift`

. Standard CLI tools, the same ones we’d use ourselves.

The glue is the `CLAUDE.md`

from Part two. It tells the agent the notary-profile convention, the two build paths, and that shipping means `release.sh`

. The result is Claude just runs the thing, without me re-explaining it every session.

The one step that stays interactive is `notarytool store-credentials`

, and that’s a choice rather than a limitation: you *could* pass `--password`

and script it, but that means putting an app-specific password in your shell history. Type it once by hand, let the keychain or 1Password hold it, and everything downstream is automated.

## Why Xcode never needs to be open

Put the GUI workflow next to the headless one and the whole thing just lines up:

| JOB | GUI WAY | HEADLESS WAY |
|---|---|---|
| Generate project | Xcode manages `.xcodeproj` | `xcodegen generate` from `project.yml` |
| Build | ⌘B / Run button | `xcodebuild … build` |
| Archive | Product → Archive | `xcodebuild … archive` |
| Export signed app | Organizer → Distribute | `xcodebuild -exportArchive` |
| Notarize | Organizer upload | `xcrun notarytool submit --wait` |
| Staple | (automatic in Organizer) | `xcrun stapler staple` |
| Install to /Applications | drag-and-drop | `cp -R` + `lsregister` |
| Deploy to iPhone | Run on device | `xcodebuild archive` + `devicectl device install` |

The GUI is only ever needed for that one-time credential setup. After that, the entire lifecycle is scriptable, which is exactly what `release.sh`

finalizes, and exactly why the agent can own it end to end while I go do something more interesting than watching a progress bar.

If you want to try it yourself, the order is: install Xcode and xcodegen, do the credential setup, then sit down with Claude and build your own `release.sh`

and `CLAUDE.md`

. That last part is the actual work, and it’s an hour or two at most. After that, “ship a new build” becomes one sentence. After the first app setup, Claude Code can copy the same setup to apply to future apps.