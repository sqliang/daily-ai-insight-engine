---
title: RISCBoy is an open-source portable games console, designed from scratch
source: https://github.com/Wren6991/RISCBoy
author:
- '[[mariuz]]'
published: '2026-07-11'
created: '2026-07-12'
description: 'Article URL: https://github.com/Wren6991/RISCBoy Comments URL: https://news.ycombinator.com/item?id=48876245
  Points: 139 # Comments: 20'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 3a32854df8c89951
source_type: community_discussion
tldr: RISCBoy 是一个完全从头设计的开源便携式游戏机项目，包含自研 RISC-V 兼容 CPU、光栅图形管线、显示控制器、KiCad PCB 布局等完整硬件设计，目标部署在
  iCE40-HX8k FPGA 上，使用开源 Icestorm 工具链进行综合。
objective_summary: 开发者 Wren6991 在 GitHub 上发布了 RISCBoy 开源项目，这是一个从头设计的便携式游戏机，包含基于 RV32IMC
  指令集的 RISC-V 处理器、光栅图形管线、显示控制器、总线架构、存储器控制器等完整芯片基础设施，以及 KiCad PCB 布局。该设计使用可综合 Verilog
  2005 编写，目标部署于 iCE40-HX8k FPGA（7680 逻辑单元），采用 Yosys、nextpnr 和 Icestorm 开源工具链。处理器通过了
  RISC-V 合规性测试套件和 riscv-formal 形式化验证。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies:
  - Lattice Semiconductor
  technologies:
  - RISC-V
  - RV32IMC
  - FPGA
  - Verilog
  - AHB-lite
  - APB
  - KiCad
  - Yosys
  - nextpnr
  - Icestorm
  - RISCBoy
  - hazard5
  key_people:
  - Wren6991
key_logic_flow:
- RISCBoy 是一个完全从头设计的开源便携式游戏机，包含自研 RISC-V CPU、光栅图形管线、显示控制器和 KiCad PCB 布局。
- 处理器支持 RV32IMC 指令集，并通过了 RISC-V 合规性测试套件和 riscv-formal 形式化验证。
- 设计使用可综合 Verilog 2005 编写，目标部署于 iCE40-HX8k FPGA（7680 逻辑单元），需要 Yosys、nextpnr 和 Icestorm
  开源工具链进行综合。
- PCB Rev A 兼容 iTead 的 4 层 5x5 cm 打样服务，Rev B 将等待门电路和引导加载程序成熟后再推进。
- 项目还提供了实验性的 ECP5 FPGA 支持，使用内部 256 kiB 同步存储器替代外部 SRAM。
- 项目仓库包含 HDL 源代码、KiCad 板级文件、LaTeX 文档、仿真脚本和软件测试用例等多个子目录。
extract_result: success
object_mentions:
- object_type: project
  name: RISCBoy
  canonical_name: Wren6991/RISCBoy
  url: https://github.com/Wren6991/RISCBoy
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - RISCBoy 是一个完全从头设计的开源便携式游戏机，包含自研 RISC-V CPU、光栅图形管线、显示控制器和 KiCad PCB 布局。
  - 该设计被视为来自 RISC-V 在 2001 年就已存在的平行宇宙中的 Gameboy Advance。
  - 处理器支持 RV32IMC 指令集，并通过了 RISC-V 合规性测试套件和 riscv-formal 形式化验证。
  article_id: 3a32854df8c89951
impact_score:
  score: 2.5
  reason: 该项目是一个开源硬件爱好者的个人工程杰作，技术质量极高（自研RISC-V CPU通过形式化验证、完整图形管线、PCB设计），但其领域属于复古游戏机硬件而非AI。对AI行业的短期影响力接近于零，属于典型的小圈子硬核自嗨项目。唯一与AI的弱关联在于其展示了开源FPGA工具链（Yosys+nextpnr）的成熟度，以及RISC-V生态在嵌入式领域的进展，但这些对AI行业没有实质影响。综合考虑技术质量高但行业冲击为零，评分为2.5。
sentiment: neutral
developer_sentiment:
  tone: excited
  primary_focus: 在仅7680逻辑单元的iCE40 FPGA上从零实现完整RISC-V CPU与图形管线的工程极限挑战
hype_assessment:
  level: low
  reason: 项目描述非常朴实，没有使用'颠覆'、'革命性'等PR词汇。作者自嘲地称其为'drunk text'，技术细节详尽（ISA支持、逻辑单元数量、工具链版本、合规性测试结果），完全符合开源硬件项目的务实风格，不存在概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 在资源极度受限的iCE40-HX8k FPGA（7680 LUT4逻辑单元）上，从零实现了完整的32位RISC-V
    CPU（支持RV32IMC指令集）和独立栅格图形渲染管线，且CPU通过了riscv-formal形式化验证和RISC-V合规性测试。使用了纯开源FPGA工具链（Yosys+nextpnr+IceStorm）完成综合、布局布线和比特流生成。这展示了开源硬件生态已具备支撑复杂SoC级别设计的能力。
  business_model: 无。该项目是纯开源社区项目（GitHub托管，Verilog/HDL和KiCad源文件全公开），没有商业模式或商业化意图。
engineering_complexity: prototype
compound_value:
  score: 2.5
  reason: RISCBoy 是一个个人开发者（Wren6991）的开源硬件爱好项目，不具备任何商业实体属性——无团队结构、无融资历史、无收入模型、无市场路径。其长期复利价值极为有限，核心在于信号层面：1)
    验证了 RISC-V RV32IMC 在仅 7680 LUT 的 iCE40 FPGA 上运行完整 SoC（含 CPU + 图形管线 + 显示控制器）的技术可行性，进一步降低
    RISC-V 嵌入式的心理门槛；2) 展示了 Yosys+nextpnr 开源 EDA 工具链处理复杂异构 SoC 设计的能力，对开源硬件工具链生态是正面信号；3)
    从零设计 CPU/GPU/PCB 的完整度在开源社区具有示范效应。但这些影响高度间接、难以量化，且项目本身不具备复利积累的商业基础，不属于可投资标的。3-5
    年后大概率被遗忘，除非其设计被衍生为商业项目。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- RISC-V International
- Lattice Semiconductor
- Yosys/nextpnr 开源 EDA 工具链社区
- 开源硬件生态（如 LibreSilicon）
competitive_casualty:
- ARM (低端嵌入式 Cortex-M 系列)
- 传统闭源 EDA 工具在低密度 FPGA 设计市场的份额
market_opportunities:
- RISCBoy 的全栈开源设计（RISC-V CPU→图形管线→PCB 布局）可作为高校计算机体系结构和数字集成电路设计课程的完整教学案例，推动 RISC-V
  开源硬件教育
- 经过 riscv-formal 形式化验证的 hazard5 RISC-V 核心可被安全关键领域（如航天电子、工业控制、车规芯片）的 FPGA 开发者复用或定制，大幅降低自研
  RISC-V CPU 核的验证成本
- 该项目验证了小逻辑量 FPGA（7680 LUT）上运行 32 位游戏机的可行性，为边缘端低功耗游戏/交互设备创业者提供了可落地的硬件参考设计起点
risk_matrix:
  regulatory: 无
  technological: 目标 FPGA（iCE40-HX8k）仅 7680 逻辑单元，性能天花板明显，难以运行复杂游戏；使用 Verilog 2005 而非更现代的
    SystemVerilog，社区贡献和 IP 复用门槛较高
  competitive: 开源 FPGA 游戏机领域存在 FPGAGBC、NeoGB 等项目竞争；商业 retro 掌机市场（Analogue Pocket、Miyoo
    Mini 等）已较为拥挤，差异化空间有限
  ethical: 无
  additional:
  - 项目依赖特定 FPGA 芯片（Lattice iCE40 系列）的供应链稳定性，芯片短缺或停产可能影响硬件复现
  - 项目为单一个体开发者（Wren6991）的业余作品，长期维护的可持续性和社区演化的确定性较低
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: speculative_watch
object_insights:
- object_type: project
  name: RISCBoy
  canonical_name: Wren6991/RISCBoy
  url: https://github.com/Wren6991/RISCBoy
  positioning: RISCBoy 是一个从头设计的开源便携式游戏机项目，包含自研 RISC-V 处理器、完整图形管线与硬件 PCB 设计，面向低成本 FPGA
    部署。
  technical_signal: 项目基于 RV32IMC 指令集自研 RISC-V 处理器，通过合规性测试套件和 riscv-formal 形式化验证，目标部署于
    iCE40-HX8k FPGA。
  adoption_signal: 项目目前由个人开发者独立维护，依赖开源 Icestorm 工具链，尚未见第三方采用或社区规模化贡献的明确信号。
  ecosystem_relevance: 作为开源 RISC-V 硬件项目，RISCBoy 丰富了 RISC-V 在嵌入式消费电子领域的应用场景验证，与开源 FPGA
    工具链生态紧密协同。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: RISCBoy 从零构建了完整的开源游戏机硬件栈，包含通过形式化验证的自研 RISC-V 处理器和完整 PCB 设计，证明了在低成本
    FPGA 上实现消费级游戏系统的可行性，对 RISC-V 在嵌入式游戏领域的生态拓展具有参考价值，值得跟踪其 PCB Rev B 和引导程序的后续迭代。
  risk_notes:
  - 项目目前为个人开发者独立维护，PCB Rev B 仍需等待门电路和引导加载程序成熟，进度存在不确定性。
  - iCE40-HX8k FPGA 仅 7680 逻辑单元，性能扩展空间极为有限，量产可行性存疑。
  - 依赖 Xilinx ISIM 14.x 仿真工具链，版本较旧且仅限 Linux 平台，可能影响协作效率。
  score: 7.0
  article_ids:
  - 3a32854df8c89951
  evidence_snippets:
  - RISCBoy 是一个完全从头设计的开源便携式游戏机，包含自研 RISC-V CPU、光栅图形管线、显示控制器和 KiCad PCB 布局。
  - 该设计被视为来自 RISC-V 在 2001 年就已存在的平行宇宙中的 Gameboy Advance。
  - 处理器支持 RV32IMC 指令集，并通过了 RISC-V 合规性测试套件和 riscv-formal 形式化验证。
---

RISCBoy is an open-source portable games console, designed from scratch. This includes:

- A RISC-V compatible CPU
- A raster graphics pipeline and display controller
- Other chip infrastructure: busfabric, memory controllers, UART, GPIO etc.
- A PCB layout in KiCad

It is a Gameboy Advance from a parallel universe where RISC-V existed in 2001. A love letter to the handheld consoles from my childhood, and a 3AM drunk text to the technology that powered them.

The design is written in synthesisable Verilog 2005, and is intended to fit onto an iCE40-HX8k FPGA. This is a LUT4-based FPGA with 7680 logic elements, so fitting a 32 bit games console requires a crowbar and some vaseline, or perhaps just careful design. The HX8k was once the largest FPGA targeted by the open-source Icestorm FPGA toolchain, but that toolchain has since moved on to greater things.

More detailed information can be found in the documentation.

The processor supports the RV32IMC instruction set, and passes the RISC-V compliance suite for these instructions, as well as the riscv-formal verification suite, and some of my own formal property checks for instruction frontend consistency and basic bus compliance. It also supports M-mode CSRs, exceptions, and a simple compliant extension for vectored external interrupts.

**This repository uses submodules for HDL as well as tests**

`git clone --recursive https://github.com/Wren6991/RISCBoy.git riscboy`

Alternatively

```
git clone https://github.com/Wren6991/RISCBoy.git riscboy
cd riscboy
git submodule update --init --recursive
```

Note a recursive submodule update is required to run the processor's standalone tests. This is not necessary for building RISCBoy gateware.

The RV32IMC toolchain is required for compilation of software-based tests. Follow the instructions on the RISC-V GNU Toolchain GitHub, except for the configure line:

```
# Prerequisites for Ubuntu 20.04
sudo apt install -y autoconf automake autotools-dev curl python3 libmpc-dev libmpfr-dev libgmp-dev gawk build-essential bison flex texinfo gperf libtool patchutils bc zlib1g-dev libexpat-dev
cd /tmp
git clone --recursive https://github.com/riscv/riscv-gnu-toolchain
cd riscv-gnu-toolchain
# The ./configure arguments are the most important difference
./configure --prefix=/opt/riscv --with-arch=rv32imc --with-abi=ilp32 --with-multilib-generator="rv32i-ilp32--;rv32ic-ilp32--;rv32im-ilp32--;rv32imc-ilp32--"
sudo mkdir /opt/riscv
sudo chown $(whoami) /opt/riscv
make -j $(nproc)
```

On smaller FPGAs, like the iCE40 UP5k, RISCBoy may be configured to use a smaller RV32I variant of the processor, rather than the higher-performance RV32IMC version. The compiler will support any of the ISA variants available on RISCBoy, but we must also instruct the toolchain build scripts to produce standard libraries for these variants, via the `--with-multilib`

arguments. Running a RV32I executable linked against an RV32IMC standard library on an RV32I-only processor will ruin your day!

The simulation flow is driven by Xilinx ISIM 14.x; makefiles are found in the scripts/ folder. This has only been tested with the Linux version of ISIM.

You will also need to checkout the RISC-V compliance suite in order to run these tests (note the `-- test`

is required to stop git from looking in the KiCad directories and complaining about the library structure there).

```
$ git submodule update --init --recursive
```


Once this is ready, you should be able to run the following:

```
. sourceme
cd test
./runtests
```

which will run all of the HDL-level tests. Software tests will require the RV32IC toolchain. You may need to adjust some of the paths in `sourceme`

if ISIM is installed in a non-default location. To graphically debug a test, run its makefile directly:

```
cd system
make TEST=helloworld gui
```

The image shows the Rev A PCB. It is compatible with iTead's 4-layer 5x5 cm prototyping service, which currently costs $65 for 10 boards.

The schematic can be viewed here (pdf)

Rev B will look quite different; I am waiting for the gateware and bootloader to mature before proceeding. My current dev hardware looks a lot like my Snowflake FPGA board.

FPGA synthesis for iCE40 uses an open-source toolchain. If you would like to build this project using the existing makefiles, you will first need to build the toolchain I used:

- Yosys for synthesis
- nextpnr for place and route
- Project Icestorm for bitstream generation

Note that I have only built these on Linux. I've heard it is possible to build these on Windows, but haven't tried it. However, they can be built on a Raspberry Pi, which is neat.

Once the toolchain is in place, run

```
. sourceme
cd synth
make -f HX8k-EVN.mk bit
```

to generate an FPGA image suitable for Lattice HX8k evaluation board.

There is also highly experimental support (i.e. not my main dev platform) for ECP5, with board files for the Lattice LEF5UM5G-85F-EVN evaluation board:

`make -f ECP5-EVN.mk BUILD=full bit`

This build replaces the external, 512 kiB, 16 bit wide SRAM of RISCBoy development hardware with an internal, 256 kiB, 32 bit wide synchronous memory, which Trellis builds out of ECP5 sysmem blocks.

`board`

: KiCad files for main RISCBoy PCB and other small boards used during development`doc`

: LaTeX source and diagrams for documentation, and the most recently built PDF`hdl`

: The Verilog source for RISCBoy gateware.`busfabric`

: AHB-lite crossbar and APB peripheral fabric`graphics`

: Source for the pixel processing unit`hazard5`

: Source for the RISC-V processor. This is completely self-contained.`mem`

: Memory controllers, and inference/injection wrappers and models for the memories themselves`peris`

: Small peripherals such as UART, SPI, PWM`riscboy_core`

: Structural module to instantiate and connect the components that comprise RISCBoy`riscboy_fpga`

: Top-level wrappers for a few different FPGAs and boards: connect up IOs, provide clock and reset

`reference`

: a few PDFs for standards used in RISCBoy, e.g. the RISC-V instruction set`scripts`

: Junk that I can't put anywhere else`software`

: Loose collection of C files that are used for system-level tests. Not really a useful software tree yet.`synth`

: Working directory for running whole-system synthesis. Top-level makefiles, pin constraint files.`test`

: Regression tests. Some are Verilog testbenches, others are software testcases that run on simulations of the processor or the full system.