---
title: 'Show HN: Kakehashi – Experimental userspace to run macOS binaries on Linux
  ARM'
source: https://github.com/wie-project/kakehashi
author:
- '[[vlad_kalinkin]]'
published: '2026-08-02'
created: '2026-08-03'
manifest_dates:
- '2026-08-03'
description: 'Article URL: https://github.com/wie-project/kakehashi Comments URL:
  https://news.ycombinator.com/item?id=49145937 Points: 217 # Comments: 55'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: e2e6b589dfbe8939
source_type: community_discussion
tldr: Kakehashi 是一个实验性开源项目，提供 macOS ARM64 到 Linux aarch64 的用户态翻译层，可在无 JIT 的情况下运行 Darwin
  二进制（如 7zz、curl），目标是让 CI 用廉价的 Linux arm64 runner 替代昂贵的 macOS 容量。
objective_summary: Kakehashi 由 wie-project 发布在 GitHub 上，是一个 macOS ARM64 到 Linux aarch64
  的实验性用户态翻译层。它通过加载 Darwin Mach-O、映射 freestanding libSystem 并翻译 BSD 系统调用，无需 JIT 即可在
  Linux aarch64 上运行 7-Zip 7zz、curl 等真实 Darwin 程序。项目在 Docker/Colima 与 UTM 上验证通过，多文件
  7zz 压缩较原生慢约 5.2 倍，但作者指出 macOS runner 按分钟单价约为 Linux arm64 的 10 至 12 倍，总成本仍可能更低。项目以
  Apache License 2.0 发布，由 kakehashi、kh-loader、kh-runtime、kh-libsystem 四个 crate 组成。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - GitHub
  - Apple
  - Docker
  - Colima
  - UTM
  technologies:
  - Mach-O
  - libSystem
  - BSD syscalls
  - aarch64
  - LZMA2
  - OpenSSL
  - NEON
  key_people: []
key_logic_flow:
- Kakehashi 是一个实验性用户态翻译层，目标是在 Linux aarch64 上运行 macOS ARM64 的 Darwin 二进制，采用 CLI-first
  且不含 JIT 的设计。
- 其实现方式是加载 Darwin Mach-O、映射独立的 freestanding libSystem 并翻译 BSD 系统调用，已验证可运行 clang、7-Zip
  7zz、curl 和多线程程序。
- 性能方面，在 UTM 的 Ubuntu aarch64 上多文件 7zz 压缩（约 240 MiB 文件树）较原生慢约 5.2 倍，而少量文件的压缩场景差距可缩小至
  1.1 至 1.2 倍。
- 项目的核心目标是让 Darwin CLI 工具运行在便宜的 Linux aarch64 CI runner 上，因为 macOS runner 按分钟单价约为
  Linux arm64 的 10 至 12 倍。
- 项目由 kakehashi（kh 命令）、kh-loader、kh-runtime、kh-libsystem 四个 crate 组成，以 Apache License
  2.0 发布，并声明并非衍生自 Darling。
object_mentions:
- object_type: project
  name: Kakehashi
  canonical_name: wie-project/kakehashi
  url: https://github.com/wie-project/kakehashi
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Kakehashi 是一个实验性的 macOS ARM64 到 Linux aarch64 用户态翻译层，采用 CLI-first 且不含 JIT，能够加载
    Darwin Mach-O 并翻译 BSD 系统调用。
  - 该项目已在 Docker/Colima 和 UTM 上验证，可通过 cargo install kakehashi 安装，并能运行真实的 Darwin 7-Zip
    7zz 和 curl 程序。
  article_id: e2e6b589dfbe8939
- object_type: product
  name: 7-Zip 7zz
  canonical_name: 7-Zip 7zz
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 项目使用真实 Darwin 版本的 7-Zip 7zz 作为验证对象，在 Ubuntu aarch64 上多文件压缩较原生 Linux 慢约 5.2 倍，并支持
    -mmt=4 多线程压缩。
  - 安装命令 kh install 7zip 会将 Darwin 版 7zz 安装到 guest 的 /usr/local/bin/7zz，并通过 docker-7zz.sh
    脚本驱动多线程压缩验证。
  article_id: e2e6b589dfbe8939
- object_type: project
  name: curl
  canonical_name: curl
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 项目通过 kh install curl 将 Darwin 版 curl 安装到 guest，并验证了 HTTP/HTTPS 请求以及自签名证书失败的负向场景。
  - curl 的完整功能集（POST 请求体、代理、HTTP/3 端到端、全部协议）尚未支持，属于后续路线图的一部分。
  article_id: e2e6b589dfbe8939
- object_type: project
  name: Darling
  canonical_name: Darling
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 项目在说明中明确声明自身并非衍生自 Darling，也不会引入任何 Apple 专有 SDK 或二进制文件。
  article_id: e2e6b589dfbe8939
extract_result: success
impact_score:
  score: 5.0
  reason: 评分依据：这是一个实验性开源项目，技术上确有硬亮点——不含 JIT 的纯用户态翻译层跑通 Darwin ARM64 二进制，且验证与基准都很扎实（7zz
    多线程压缩、curl HTTPS、Docker/UTM 可复现），直击 macOS CI runner 单价约为 Linux arm64 的 10~12 倍这一真实成本痛点。但短期冲击力受限于硬约束：仅覆盖
    CLI 工具、不支持 Apple 框架/GUI/codesign/完整 curl 特性、需现成 macOS ARM64 二进制而生态中此类产物稀少、项目处于早期实验阶段。属于'改变局部竞争格局'级别的技术探索（对标
    Darling 与 qemu 类方案、影响 CI 成本结构），远未到范式转移，综合给予 5.0 分。
sentiment: neutral
developer_sentiment:
  tone: skeptical
  primary_focus: 实际可用性边界——多文件场景 5.2 倍性能损耗、仅限 CLI 且依赖现成 Darwin 二进制，能否真正替代昂贵 macOS CI
    runner
hype_assessment:
  level: low
  reason: 判定依据：项目未使用'颠覆''革命性'等 PR 词汇，而是以克制的实验性定位发布——主动给出最坏情况性能数据（多文件约 ×5.2、少量文件 ×1.1~1.2）、明确列出不支持项（GUI、codesign、Apple
    Security.framework、HTTP/3、git/CLT）、提供可复现的验证门禁（Docker/UTM、clang probe、7zz/curl
    用例）与已知噪音日志说明，属于实打实的工程干货，几乎无概念包装。
information_entropy: high
domain_disruption:
  technical_innovation: 本质创新在于同为 ARM64 架构下绕过指令模拟：通过加载 Darwin Mach-O、映射 freestanding
    libSystem.B.dylib、翻译 BSD 系统调用并在系统调用边界做 TLS 切换/alt stack/NEON 保存恢复，把性能开销从'逐条指令'压缩到'每次
    syscall 边界'，对兼容层/二进制翻译架构具有方法论参考价值
  business_model: 商业模式冲击在于 CI 成本套利：若技术成熟，可用单价低 10~12 倍的 Linux arm64 runner 承接 macOS
    Darwin 工具链的构建与测试，重塑 CI/CD 基础设施的成本结构；但受限于需现成 Darwin ARM64 二进制且无 Apple 生态支撑，短期难以规模化落地
engineering_complexity: prototype
compound_value:
  score: 5.0
  reason: 从资本视角看，Kakehashi 卡位的是"苹果平台构建/测试的云成本套利"这一真实且长期存在的痛点——macOS runner 按分钟单价约为
    Linux arm64 的 10-12 倍，任何能把 Darwin 工具链迁移到廉价 ARM 算力的能力都有持久需求，这是其复利价值的支撑点。但天花板受三重约束：其一，技术脆性——无
    JIT、syscall 边界翻译高度依赖 Apple 二进制 ABI 与 libSystem 接口的稳定，Apple 每次系统更新都可能破坏兼容面，长期维护成本很高；其二，性能税——多文件场景约
    5.2 倍差距，使其只适合 syscall 不密集的 CLI 工具链场景，难以覆盖 GUI、codesign、完整框架等纵深需求；其三，替代路径众多（cross-compile、Docker
    Desktop for Mac、QEMU 全系统虚拟化、CI 厂商自建 macOS 容量），第三方兼容层处于竞争弱势位。它更像验证成本套利需求的早期技术组件，而非
    3-5 年后不可替代的行业基石，故给 5 分，落在'细分赛道基础设施需持续验证'区间。真正捕获长期价值的是下游云 ARM 算力与 CI 平台，而非翻译层本身。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- GitHub Actions
- AWS (Graviton)
- GitLab CI
- 跨平台开源项目维护者
competitive_casualty:
- MacStadium / MacinCloud 等 macOS 托管 CI 服务商
- AWS EC2 Mac 实例
- Darling 等 macOS 兼容层方案
- QEMU 全系统模拟方案
market_opportunities:
- CI 基础设施团队可评估用该翻译层在 Linux arm64 runner 上运行 macOS 专有 CLI 工具（如 7zz、curl），以约 5 倍性能损耗换取
  runner 单价约 10 倍下降的成本收益，尤其适合 macOS 独占工具链的构建与压缩任务
- 创业者可围绕该兼容层构建企业级 macOS 工具链迁移与 CI 成本优化服务，帮助依赖 Darwin 二进制但预算有限的中小团队实现从 macOS runner
  到 Linux arm64 的低成本过渡
- 开发者可借鉴其'无 JIT 用户态翻译 + BSD 系统调用翻译 + freestanding libSystem'的技术路线，探索其他跨操作系统二进制兼容层或系统级兼容工具的创业与产品方向
risk_matrix:
  regulatory: 在非 Apple 硬件上运行 Darwin/macOS 二进制可能触及 Apple 软件许可条款；若未来商业分发预装 Apple 组件（如
    Security.framework）的镜像，将面临知识产权与合规争议。当前项目自带 freestanding libSystem 并以 Apache 2.0
    开源，短期风险可控，但商业化需谨慎评估 Apple 生态的法律边界
  technological: 实验性项目兼容面有限（缺 GUI、codesign、Apple 框架、git/CLT、HTTP/3 等），且需随 macOS 系统调用与
    ABI 演进持续适配；若主流 CLI 工具原生支持 Linux arm64，其价值主张会被稀释。长期维护依赖类似 Darling 的社区持续性，存在技术断代与项目搁置风险
  competitive: 面临 Darling 等成熟兼容层、云厂商 macOS 托管服务（AWS EC2 Mac、MacStadium）、GitHub 官方
    macOS runner 及 osxcross/交叉编译等替代方案的竞争；若主流 CI 平台下调 macOS runner 价格或推出更便宜的等效方案，其核心成本优势将被削弱
  ethical: bottle 机制会从外部下载并执行第三方 Darwin 二进制，存在供应链投毒与恶意二进制分发的安全风险；企业 CI 若未经审查即引入翻译层运行二进制，可能扩大不可信代码的攻击面
  additional:
  - 性能损耗显著（多文件 7zz 压缩慢约 5.2 倍），部分工作负载下不可接受，需按具体 CI 任务做成本与性能的量化权衡
confidence:
  impact: medium
  compound: low
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: project
  name: Kakehashi
  canonical_name: wie-project/kakehashi
  url: https://github.com/wie-project/kakehashi
  positioning: 实验性用户态翻译层，让 macOS ARM64 的 Darwin 二进制无需 JIT 在 Linux aarch64 上运行，目标是用廉价
    Linux runner 替代昂贵 macOS CI 容量。
  technical_signal: 采用加载 Darwin Mach-O、映射 freestanding libSystem 并翻译 BSD 系统调用的方案，不依赖
    JIT，已验证可运行 7zz、curl 与多线程程序。
  adoption_signal: 已通过 cargo install kakehashi 发布，在 Docker/Colima 与 UTM 上验证通过，并提供
    kh install 7zip、curl 等开箱即用的安装命令。
  ecosystem_relevance: 以 Apache License 2.0 开源，由 kakehashi 与 kh-loader、kh-runtime、kh-libsystem
    四个 crate 组成，声明非衍生自 Darling。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 核心价值在于用约 5.2 倍的性能损耗换取 10 至 12 倍的成本优势，使 CI 能在廉价 Linux arm64 runner
    上运行 Darwin 工具链；其无 JIT 的系统调用翻译路线与对 7zz、curl 真实程序的持续验证值得长期跟踪。
  risk_notes:
  - 多文件压缩场景较原生慢约 5.2 倍，性能损耗在系统调用密集负载下仍显著，可能限制其在重负载 CI 场景的落地。
  - curl 完整功能集（POST、代理、HTTP/3 端到端）尚未支持，Apple Security.framework 与 git 等能力仍在后续路线图中。
  score: 7.0
  article_ids:
  - e2e6b589dfbe8939
  evidence_snippets:
  - Kakehashi 是一个实验性的 macOS ARM64 到 Linux aarch64 用户态翻译层，采用 CLI-first 且不含 JIT，能够加载
    Darwin Mach-O 并翻译 BSD 系统调用。
  - 该项目已在 Docker/Colima 和 UTM 上验证，可通过 cargo install kakehashi 安装，并能运行真实的 Darwin 7-Zip
    7zz 和 curl 程序。
- object_type: product
  name: 7-Zip 7zz
  canonical_name: 7-Zip 7zz
  url: null
  positioning: 7-Zip 的 Darwin 版命令行压缩程序 7zz，在 Kakehashi 项目中被用作验证翻译层正确性与性能的基准工具，支持多线程压缩。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - CI 基础设施运维人员
  - 需要在 Linux arm64 环境使用 macOS 版压缩工具的用户
  product_signal: 支持 -mmt=4 多线程压缩，可通过 kh install 7zip 命令安装到 guest 的 /usr/local/bin/7zz，并由
    docker-7zz.sh 脚本驱动完成验证。
  market_signal: 作为 Kakehashi 的验证基准，多文件压缩较原生 Linux 慢约 5.2 倍，而少量文件场景差距可缩小至 1.1 至 1.2
    倍，反映翻译层对压缩负载的可用边界。
  differentiation: 相比原生 Linux 7zz，Darwin 版在 Kakehashi 翻译层下损失约 5.2 倍性能，但换来在 Linux arm64
    基础设施上运行 macOS 工具链的能力。
  watch_reason: 7zz 是评估 Kakehashi 翻译层成熟度的关键基准，其多线程压缩与路径遍历最能暴露系统调用边界的性能损耗，跟踪其验证进展可判断该翻译层对真实负载的可用程度。
  risk_notes:
  - 多文件压缩场景较原生慢约 5.2 倍，路径遍历与逐系统调用边界的开销在重负载下仍显著，影响其作为 CI 工具的普适性。
  score: 4.0
  article_ids:
  - e2e6b589dfbe8939
  evidence_snippets:
  - 项目使用真实 Darwin 版本的 7-Zip 7zz 作为验证对象，在 Ubuntu aarch64 上多文件压缩较原生 Linux 慢约 5.2 倍，并支持
    -mmt=4 多线程压缩。
  - 安装命令 kh install 7zip 会将 Darwin 版 7zz 安装到 guest 的 /usr/local/bin/7zz，并通过 docker-7zz.sh
    脚本驱动多线程压缩验证。
- object_type: project
  name: curl
  canonical_name: curl
  url: null
  positioning: 广泛使用的开源命令行网络传输工具，其 Darwin 版本在 Kakehashi 翻译层中被用作验证 HTTP/HTTPS 网络功能正确性的测试程序。
  technical_signal: 已通过 kh install curl 安装到 guest，验证了 HTTP/HTTPS 请求与自签名证书失败的负向场景，但完整功能集尚未支持。
  adoption_signal: 在 Docker 与 UTM 验证环境中作为核心测试程序，项目提供 docker-curl.sh、docker-curl-probe.sh
    等脚本驱动 curl 的系列验证。
  ecosystem_relevance: curl 是 Linux/Unix 生态的基础网络工具，其 Darwin 版能否在翻译层下完整运行，直接关系到 Kakehashi
    对 macOS 工具链生态的兼容广度。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: curl 覆盖 TCP/TLS 与各类协议栈，是检验翻译层网络栈兼容性的理想载体；其功能集从基础 GET/HTTPS 向 POST、代理与
    HTTP/3 扩展的进度，可清晰反映 Kakehashi 的成熟度演进。
  risk_notes:
  - curl 的完整功能集（POST 请求体、代理、HTTP/3 端到端、全部协议）尚未支持，属于后续路线图，当前覆盖有限。
  score: 5.0
  article_ids:
  - e2e6b589dfbe8939
  evidence_snippets:
  - 项目通过 kh install curl 将 Darwin 版 curl 安装到 guest，并验证了 HTTP/HTTPS 请求以及自签名证书失败的负向场景。
  - curl 的完整功能集（POST 请求体、代理、HTTP/3 端到端、全部协议）尚未支持，属于后续路线图的一部分。
- object_type: project
  name: Darling
  canonical_name: Darling
  url: null
  positioning: 在 Linux 上运行 macOS 软件的既有开源兼容层项目，本文中作为 Kakehashi 的设计对照对象被提及，双方代表不同的 macOS
    兼容技术路线。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: Kakehashi 明确声明并非衍生自 Darling，也不引入 Apple 专有 SDK 或二进制，显示 Darling
    是 macOS 兼容生态中的既有参照系，两者技术路线形成对照。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Darling 作为 Linux 上运行 macOS 软件的经典兼容层方案，被 Kakehashi 专门声明区分；跟踪两者的路线差异有助于理解轻量级无
    JIT 翻译层与传统兼容层的性能与兼容性取舍。
  risk_notes:
  - 本文仅作为对照对象提及 Darling，未提供其自身的技术进展或采用数据，其当前项目状态需另行跟踪确认。
  score: 3.0
  article_ids:
  - e2e6b589dfbe8939
  evidence_snippets:
  - 项目在说明中明确声明自身并非衍生自 Darling，也不会引入任何 Apple 专有 SDK 或二进制文件。
---

Userspace **macOS ARM64 → Linux aarch64** translation layer (CLI-first, no JIT).

Load Darwin Mach-O on Linux aarch64, map a freestanding `libSystem`

, translate
BSD syscalls, and run real guests (clang probes, **7-Zip 7zz**,

**curl**, threads).

| Live execution | Linux aarch64 (bare metal, VM, Colima/Docker) |
| Dry-load / inspect | Any host (including macOS) |
| Design reference | `docs/` |

Verified on **Docker/Colima** and **UTM** (Linux aarch64). Install once:

```
cargo install kakehashi
# or from a checkout:
cargo install --path crates/kh-cli --force
kh bottle ensure
kh install 7zip # Darwin 7zz → guest /usr/local/bin/7zz
kh install curl # Darwin curl → guest /usr/local/bin/curl
```

Relative `-o`

/ archive paths resolve against the **host CWD** of the `kh`

process (create parent dirs yourself, or rely on auto-mkdir for `O_CREAT`

).
Through the bottle, `/Volumes/linux/…`

bridges to the host root (`/`

→ host `/`

).

```
# Version / help
kh run 7zz --
kh run 7zz -- --help
# Create archive (cwd-relative)
kh run 7zz -- a demo.7z README.md
kh run 7zz -- t demo.7z
kh run 7zz -- l demo.7z
kh run 7zz -- x -o./out demo.7z
# Multi-thread compress (correctness gate)
kh run 7zz -- a -t7z -m0=lzma2 -mx=5 -mmt=4 mt.7z README.md
kh run 7zz -- t mt.7z
# expect: Everything is Ok, exit 0
```

**Docker helpers** (artifacts under host `.tmp/kh-out/`

):

```
./scripts/docker-7zz.sh --help
./scripts/docker-7zz.sh a /Volumes/linux/out/demo.7z /Volumes/linux/src/README.md
ls -lh .tmp/kh-out/demo.7z
KAKEHASHI_HYPERCALL=1 ./scripts/docker-7zz.sh a -t7z -m0=lzma2 -mx=5 -mmt=4 \
/Volumes/linux/out/mt.7z /Volumes/linux/src/README.md
./scripts/docker-7zz.sh t /Volumes/linux/out/mt.7z
```

```
# Banner (G1)
kh run curl -- --version
# HTTP GET → file (G3 / G5). Parents for -o are created when missing.
kh run curl -- -sS -o .tmp/kh-out/body http://example.com/
# expect: exit 0, ~559 bytes, HTML contains "Example Domain"
wc -c .tmp/kh-out/body
head -c 80 .tmp/kh-out/body; echo
# HTTP to stdout
kh run curl -- -sS http://example.com/ | head -c 80; echo
# HTTPS GET (G4) — OpenSSL + bottle CA (from host or curl.se download)
kh run curl -- -sS -o .tmp/kh-out/https-body https://example.com/
wc -c .tmp/kh-out/https-body
# Negative: bad / self-signed cert must fail (rc ≠ 0)
kh run curl -- -sS -o /dev/null https://self-signed.badssl.com/; echo exit:$?
```

**Docker helpers:**

```
./scripts/docker-curl.sh --version
./scripts/docker-curl.sh -sS -o /Volumes/linux/out/body http://example.com/
./scripts/docker-curl.sh -sS -o /Volumes/linux/out/https-body https://example.com/
ls -lh .tmp/kh-out/body .tmp/kh-out/https-body
# Trace-first probe logs → .tmp/kh-curl-probe/
./scripts/docker-curl-probe.sh --version
# Option matrix (large tiers) → .tmp/kh-curl-options/
./scripts/docker-curl-options.sh tier1
./scripts/docker-curl-options.sh tier9-10
./scripts/docker-curl-options.sh all # tier1..10
```

Harmless noise on many runs:

`kh: open fail ENOENT(openat) path=/etc/ssl/openssl.cnf`

— OpenSSL optional config; HTTP/HTTPS still work via the seeded CA bundle.`WARN … skip dylib … Security/CoreFoundation`

— Apple frameworks not in the bottle; soft stubs cover the load path.`unresolved strong symbol; bound to named missing trampoline`

— symbols not hit on the happy path.

Details and gates: `docs/curl.md`

.

| Surface | Notes |
|---|---|
| Clang / fixture probes | `tests/clang-probe/` , `tests/fixtures/` |
Multi-thread `7zz -mmt=4` |
Docker + UTM |
Bottle + freestanding `libSystem` |
`kh bottle ensure` embeds dylib |
| Unit tests + clippy | `cargo test` / `clippy` workspace (excl. `kh-libsystem` ) |

Full curl feature set (POST bodies, proxies, HTTP/3 end-to-end, every scheme),
real Apple Security.framework, `git`

/ CLT, GUI, codesign. Next product slice:
**git** via `kh install xcode-tools`

— see `docs/git.md`

.

| Crate | Role |
|---|---|
`kakehashi` |
Binary `kh` (install this) |
`kh-loader` |
Mach-O parse, map, execute |
`kh-runtime` |
Memory, traps, BSD syscalls, bottle; embeds freestanding `libSystem.B.dylib` |
`kh-libsystem` |
Source for that dylib (`aarch64-apple-darwin` only; not a Linux host crate) |

The guest dylib is vendored at `crates/kh-runtime/resources/libSystem.B.dylib`

and compiled into the runtime with `include_bytes!`

. Publishing `kh-runtime`

ships the dylib; end users do not need a separate download.

- Rust 1.88+
**Linux aarch64**for live`kh run`

/`kh trace`

- Page sizes:
**4 KiB**(containers) and**16 KiB**(Asahi-class) - Optional:
`curl`

/`wget`

+`tar`

for`kh install 7zip`

/`kh install curl`


```
cargo install kakehashi
# or from a checkout: cargo install --path crates/kh-cli
kh bottle ensure
kh install 7zip
kh install curl
```

Default root: `~/.local/share/kakehashi/bottle/`

(override with
`KAKEHASHI_DATA_DIR`

/ `KAKEHASHI_ROOT`

).

| Host | Guest |
|---|---|
`…/bottle/` |
`/` |
`…/usr/local/bin/7zz` |
`/usr/local/bin/7zz` |
`…/usr/local/bin/curl` |
`/usr/local/bin/curl` |
`…/usr/lib/libSystem.B.dylib` |
`/usr/lib/libSystem.B.dylib` |
`…/private/etc/ssl/cert.pem` |
`/etc/ssl/cert.pem` (host CA or downloaded Mozilla) |
`…/Volumes/linux/…` |
`/Volumes/linux/…` → host FS |

Kakehashi runs guest code **natively** on the CPU. The tax is the **syscall
boundary** (TLS switch, alt stack, NEON save/restore, Rust dispatch) × how
chatty the guest is — not an instruction emulator.

On Ubuntu aarch64 bare-metal (UTM), multi-file `7zz`

archive
(`-t7z -m0=lzma2 -mx=5 -mmt=4`

, ~8k files / ~240 MiB tree):

native Linux `7zz` |
Darwin `7zz` under `kh` |
ratio | |
|---|---|---|---|
| wall | ~22.5 s | ~118 s | ~×5.2 |

On compression-heavy, few-file samples the gap is often much smaller
(~×1.1–1.2). The large multi-file gap is dominated by **path walk + per-syscall
boundary**, not “wrong LZMA”.

Hypercall is on by default for all guest threads. Opt out with
`KAKEHASHI_HYPERCALL=0`

only for debug (residual `svc`

→`brk`

/ SIGTRAP).

The product goal for CI is not “as fast as native macOS,” but **run Darwin
CLI/tools on cheap Linux aarch64 runners** instead of scarce, expensive
macOS capacity.

**GitHub Actions hosted runners** (private-repo overage rates, USD per
minute; see Actions runner pricing):

| Runner | Per-minute rate |
|---|---|
| Linux 2-core arm64 | $0.005 |
| Linux 2-core x64 | $0.006 |
| macOS 3–4 core (M1/Intel) | $0.062 |
| macOS larger (e.g. 12-core / M2 Pro) | $0.077–$0.102 |

macOS standard is roughly **×10–×12** the Linux arm64 minute rate before any
wall-time difference. Even if a job runs **×5 slower** under `kh`

on Linux
arm64 than the same work on a macOS runner, billable cost can still be lower
because the macOS minute is an order of magnitude more expensive
(illustrative: 5 × $0.005 ≈ $0.025 vs 1 × $0.062). Public-repo free minutes
and self-hosted Linux amplify that further; macOS hosted capacity also tends
to queue longer and (on GitLab SaaS) is Premium/Ultimate / beta-gated.

**When macOS runners still win:** GUI, codesign/notarization, Xcode UI tests,
or any workload that is not a pure CLI Darwin binary under freestanding
`libSystem`

.

**Gates, not benches:** CI correctness is `cargo test`

/ smoke / `7zz -mmt=4`

,
not “match native wall clock.” Perf work is tracked in
`docs/roadmap.md`

.

```
docker build -t kakehashi:dev -f Dockerfile.dev .
docker run --rm -v "$PWD":/src -w /src kakehashi:dev \
cargo test --workspace --exclude kh-libsystem
# Full smoke (build + clippy + test + micro run)
./scripts/docker-smoke.sh
```

```
cargo build -p kakehashi --release
cargo test --workspace --exclude kh-libsystem
cargo clippy --workspace --exclude kh-libsystem --all-targets -- -D warnings
# Maintainers: refresh embed after freestanding ABI changes
cargo build -p kh-libsystem --release --target aarch64-apple-darwin
./scripts/stage-libsystem.sh # → crates/kh-runtime/resources/libSystem.B.dylib
```

`libSystem`

discovery: `--libsystem`

→ `KAKEHASHI_LIBSYSTEM`

→ paths next to
`kh`

→ crate `resources/`

→ **embedded** bytes in `kh-runtime`

.

| Goal | Command | Artifacts |
|---|---|---|
| Unit tests | `cargo test --workspace --exclude kh-libsystem` |
terminal |
| Docker smoke | `./scripts/docker-smoke.sh` |
ends with `smoke ok` |
| Fixtures | `kh run --expect-code … tests/fixtures/…` |
see `tests/fixtures/README.md` |
| Clang probes | `kh run --root tests/fixtures/bottle tests/clang-probe/puts_hello` |
stdout `hello` |
Real Darwin `7zz` |
`./scripts/docker-7zz.sh …` |
host `.tmp/kh-out/` |
Real Darwin `curl` |
`./scripts/docker-curl.sh …` |
host `.tmp/kh-out/` ; probe → `.tmp/kh-curl-probe/` |
| Fair CPU bench | `./scripts/bench-fair-local.sh` |
host `.tmp/kh-bench-fair/` |

`.tmp/`

, `.kh/`

, and `target/`

are gitignored.

Bottle bridges the Linux FS as `/Volumes/linux/…`

:

| Guest path | Host |
|---|---|
`/Volumes/linux/src/README.md` |
`<repo>/README.md` |
`/Volumes/linux/out/demo.7z` |
`<repo>/.tmp/kh-out/demo.7z` (durable; default for `docker-7zz.sh` ) |
`/Volumes/linux/tmp/…` |
container `/tmp/…` — gone after `docker run --rm` |

| Script | Purpose |
|---|---|
`scripts/stage-libsystem.sh` |
Build product → `crates/kh-runtime/resources/` |
`scripts/install-linux.sh` |
Local build + install `kh` + `bottle ensure` |
`scripts/docker-smoke.sh` |
Smoke suite inside `Dockerfile` image |
`scripts/docker-7zz.sh` |
Darwin `7zz` under `kh` (outputs → `.tmp/kh-out` ) |
`scripts/docker-curl.sh` |
Darwin `curl` under `kh` (same shape as `docker-7zz` ) |
`scripts/docker-curl-probe.sh` |
`KH_CURL_PROBE=1` wrapper (logs → `.tmp/kh-curl-probe` ) |
`scripts/docker-curl-options.sh` |
Tiered curl flag smoke (`tier1` …`tier10` , `tier9-10` , `all` → `.tmp/kh-curl-options` ) |
`scripts/docker-git.sh` |
Apple `git` from CLT under `kh` (swscan + `.kh/data` cache) |
`scripts/bench-fair-local.sh` |
Native vs kh compress (artifacts → `.tmp/kh-bench-fair` ) |

Apache License 2.0. See `LICENSE.txt`

and `NOTICE`

.

This project is **not** derived from Darling. Do not vendor proprietary Apple
SDKs or blobs.