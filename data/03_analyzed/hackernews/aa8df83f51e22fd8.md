---
title: 'HTML over WebSockets: real-time SPAs with barely any JavaScript'
source: https://en.andros.dev/blog/ef4968f5/html-over-websockets-real-time-spas-with-barely-any-javascript/
author:
- '[[redbell]]'
published: '2026-08-12'
created: '2026-08-13'
manifest_dates:
- '2026-08-12'
- '2026-08-13'
description: 'Article URL: https://en.andros.dev/blog/ef4968f5/html-over-websockets-real-time-spas-with-barely-any-javascript/
  Comments URL: https://news.ycombinator.com/item?id=49275335 Points: 194 # Comments:
  136'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: aa8df83f51e22fd8
source_type: community_discussion
tldr: 本文介绍“HTML over WebSockets”架构：服务器通过持久 WebSocket 通道向浏览器推送已渲染好的 HTML，浏览器只负责插入 DOM
  与监听事件，从而用少量 JavaScript 构建实时 SPA，并对比了 WebSocket、SSE、HTTP 三种传输及多语言框架现状。
objective_summary: 文章作者从 Phoenix LiveView 的起源出发，系统阐述了 HTML over WebSockets 的工作流程：浏览器与服务器建立持久连接后，服务器直接返回渲染好的
  HTML，状态保留在服务端，并支持主动推送和广播。文中列出该模式在单一渲染引擎、无 API 契约、实时性、SEO 和安全性方面的优势，同时指出服务端资源占用、水平扩展、离线可用性和学习成本等缺点。最后通过一张多语言框架对照表，说明
  WebSocket 适合双向实时场景，SSE 适合仅服务器推送，HTTP/htmx 适合简单请求响应。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - HTML over WebSockets
  - HTML over the wire
  - hypermedia
  - WebSocket
  - SSE
  - Server-Sent Events
  - Single-page Application
  - Phoenix LiveView
  - htmx
  - Datastar
  - Hotwire
  - Turbo
  - Stimulus
  - Django LiveView
  - Reactor
  - djust
  - django-unicorn
  - Tetra
  - Blazor
  - SignalR
  - Livewire 3
  - Reverb
  - Alpine.js
  - Unicorn
  - JSON
  - GraphQL
  - HTTP/2
  - HTTP/3
  - Redis
  - ASGI
  - Morphdom
  - Rust VDOM
  - EventSource
  key_people:
  - Chris McCord
key_logic_flow:
- 文章把“HTML over the wire”按传输层分为 HTTP、SSE、WebSocket 三种变体，其中 WebSocket 变体提供了持久的双向实时通道。
- Chris McCord 于 2019 年 ElixirConf 发布 Phoenix LiveView，在 15 分钟内构建了一个无需前端渲染 JavaScript
  的实时 Twitter 克隆，并带动了多语言实现。
- 在 HTML over WebSockets 流程中，浏览器只负责建立 WebSocket 连接、将收到的 HTML 插入 DOM 并监听事件，渲染逻辑与状态全部保留在后端。
- 该架构的优势包括单一渲染引擎、无需独立 API、服务端状态、直连数据库、实时广播、更低交互延迟、SEO 友好以及更强的抗注入安全性。
- 其劣势包括更高的服务端内存与资源消耗、水平扩展需要共享状态、物理延迟影响体验、离线不可用以及学习曲线较陡。
- 文章建议按实际需求选择传输：双向实时场景选 WebSocket，仅需服务器推送选 SSE，简单请求响应场景选 HTTP/htmx。
object_mentions:
- object_type: project
  name: Phoenix LiveView
  canonical_name: Phoenix LiveView
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Chris McCord 在 2019 年 ElixirConf 上发布 Phoenix LiveView，并在 15 分钟内构建了一个无需前端渲染 JavaScript
    的实时 Twitter 克隆。
  - 文章表格显示 Phoenix LiveView 使用 WebSocket 传输、支持服务器推送，状态为 Mature，LiveView 1.0 于 2024
    年 12 月发布。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: htmx
  canonical_name: htmx
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 文章将 htmx 列为基于 HTTP 的 hypermedia 方案，并指出其可通过扩展支持 WebSocket 与 SSE，但核心设计刻意保持无状态。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Datastar
  canonical_name: Datastar
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Datastar 被描述为基于 SSE 的实现，采用与 HTML over the wire 相同的理念，只是将传输层从 WebSocket 替换为 Server-Sent
    Events。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Hotwire (Turbo + Stimulus)
  canonical_name: Hotwire
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Hotwire 在表格中以 HTTP + WebSocket/SSE（Streams）作为传输层，文章提到它是“HTML Over The Wire”名称的来源。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Django LiveView
  canonical_name: Django LiveView
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 表格列出 Python/Django 生态中的 Django LiveView，使用 WebSocket 传输并支持服务器推送，状态为 Active，且作者标注为
    mine。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Reactor
  canonical_name: Reactor (Django)
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 表格将 Reactor 列为另一个基于 WebSocket 的 Django 活跃项目，并支持服务器推送。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: djust
  canonical_name: djust
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 表格列出 djust 是一个较新的 Django 项目，使用 WebSocket 传输，并引入 Rust VDOM 以提升渲染性能。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: django-unicorn
  canonical_name: django-unicorn
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - django-unicorn 被描述为基于 HTTP/AJAX 的 Django 方案，不支持服务器推送，适用于请求响应式交互。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Tetra
  canonical_name: Tetra
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Tetra 是一个年轻的 Django 项目，使用 AJAX + WebSocket 传输，并依赖 Alpine.js 处理前端交互。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Blazor (Interactive Server)
  canonical_name: Blazor
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Blazor 在表格中以 .NET 9 的 Interactive Server 模式出现，通过 SignalR 使用 WebSocket 传输并支持服务器推送。
  article_id: aa8df83f51e22fd8
- object_type: project
  name: Livewire 3 + Reverb
  canonical_name: Laravel Livewire + Reverb
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Laravel 生态的 Livewire 3 配合 Reverb 在表格中被列为使用 WebSocket 传输并支持服务器推送的方案，Reverb 是 Laravel
    自有的 WebSocket 服务器。
  article_id: aa8df83f51e22fd8
extract_result: success
impact_score:
  score: 3.5
  reason: 该文章是一篇面向社区的技术普及博文，系统梳理了 HTML over WebSockets 的工作机制、优势与局限，并对 HTTP/SSE/WebSocket
    三种传输变体做了对比。它对已有框架（Phoenix LiveView、Django LiveView、Livewire 等）的生态推广有一定帮助，但本身并非新产品发布、融资或学术突破，也未提供新的工程数据或性能基准，因此不具备改变行业竞争格局的能力，更多是帮助开发者理解并评估这一既有架构范式。
sentiment: neutral
developer_sentiment:
  tone: excited
  primary_focus: 能否用更少的前端 JavaScript 实现实时 SPA，以及服务端状态持久化带来的扩展与运维代价
hype_assessment:
  level: medium
  reason: 标题 'real-time SPAs with barely any JavaScript' 带有明显的 PR 包装色彩，容易让人误以为可以完全抛弃前端工程；但正文在介绍优势的同时，也明确列出了服务端资源占用、水平扩展、离线可用性和学习曲线等真实缺点，并给出了按场景选择传输层的务实建议。因此整体属于'有一定包装但内容基本扎实'。
information_entropy: medium
domain_disruption:
  technical_innovation: HTML over WebSockets 并非全新的架构发明，而是对服务端渲染 + 持久双向通道模式的系统化总结；其本质突破在于将
    UI 状态、模板渲染、实时推送与广播集中到后端，浏览器仅保留连接建立、HTML 投放与事件监听的最小职责，从而用单一语言和单一渲染引擎替代前后端分离的 JSON/API
    契约。
  business_model: 该模式主要影响 SaaS 与内部工具的开发成本结构：团队可以用更少的全栈协作和前端工程投入构建实时交互应用，对传统前后端分离、依赖
    React/Vue 外包或大型前端团队的商业模式构成潜在替代；但现有前端框架生态（Next.js、Vercel 等以 Edge/Node 为中心的托管与工具链）短期内不会被颠覆，更多是补充性选择。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: HTML over WebSockets 是一种已被验证的实时 Web 架构范式，能将前后端契约、渲染状态与实时推送压缩到单一后端语言中，长期看会在实时协作面板、后台管理系统、聊天/游戏等场景持续降低开发复杂度并沉淀为细分基础设施。但它不是单一技术或公司，2019
    年 Phoenix LiveView 已经出现，未来将与 API-first SPA 长期共存而非全面取代；价值更多流向能提供持久连接托管和框架集成的云平台，模式本身的直接货币化能力有限，复利效应中等。
value_capture_layer: cloud_platform
moat_impact: democratizes_access
key_beneficiaries:
- Vercel
- Phoenix LiveView
- Hotwire
- htmx
- Django
- Datastar
competitive_casualty:
- React-centric full-stack startups
- traditional API-gateway vendors
- no-code app builders for simple CRUD apps
market_opportunities:
- 可基于 Vercel Functions 原生 WebSocket 支持，为实时协作、仪表盘、聊天等场景提供 'HTML over WebSockets' 托管与模板方案，降低全栈实时应用开发门槛。
- 可围绕 Phoenix LiveView、Django LiveView 等框架开发配套工具链（如状态广播、水平扩展、离线降级、调试可观测性），填补服务器有状态架构生态缺口。
- 企业内部系统、后台管理面板等强实时、低交互复杂度的场景，可试点该架构以减少前后端 API 契约维护成本与前端构建复杂度。
risk_matrix:
  regulatory: 无
  technological: 服务端有状态架构带来水平扩展、故障恢复与会话迁移的复杂度；WebSocket 长连接对服务器内存和连接池消耗更高；若浏览器或中间代理对
    WebSocket 支持受限，回退体验可能劣化。
  competitive: React/Vue/Angular 等主流 SPA 生态与无状态 HTTP/htmx 方案已占据开发者心智，HTML over WebSockets
    仍属小众；Phoenix LiveView 在 Elixir 社区外渗透有限。
  ethical: 无
  additional:
  - 学习曲线陡峭，依赖 Elixir/LiveView 等特定技术栈的人才供给有限
  - 调试长连接与服务器状态比传统请求响应模式更复杂
  - 离线可用性天然受限，需额外设计重连、状态同步与降级策略
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: project
  name: Phoenix LiveView
  canonical_name: Phoenix LiveView
  url: https://github.com/phoenixframework/phoenix_live_view
  positioning: Elixir/Phoenix 生态中服务端渲染实时 SPA 的成熟框架，开创了 HTML over WebSockets 范式。
  technical_signal: 通过持久 WebSocket 通道推送预渲染 HTML，浏览器仅负责 DOM 插入与事件监听。
  adoption_signal: LiveView 1.0 已于 2024 年 12 月发布，状态标记为 Mature。
  ecosystem_relevance: 作为 HTML over WebSockets 的源头实现，影响了多语言同类框架的设计。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 它是服务端驱动实时 UI 的代表性方案，适合需要低延迟双向交互、希望统一前后端语言的中后台应用；需关注其服务端状态管理对扩展性的影响。
  risk_notes:
  - 服务端持有客户端状态会提高内存占用与水平扩展复杂度。
  score: 9.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Chris McCord 在 2019 年 ElixirConf 上发布 Phoenix LiveView，并在 15 分钟内构建了一个无需前端渲染 JavaScript
    的实时 Twitter 克隆。
  - 文章表格显示 Phoenix LiveView 使用 WebSocket 传输、支持服务器推送，状态为 Mature，LiveView 1.0 于 2024
    年 12 月发布。
- object_type: project
  name: htmx
  canonical_name: htmx
  url: https://htmx.org/
  positioning: 基于 HTTP 的轻量级 hypermedia 增强库，强调无状态请求响应式交互。
  technical_signal: 通过 AJAX 在标准 HTML 属性上扩展局部更新能力，可经扩展支持 WebSocket 与 SSE。
  adoption_signal: 在 HTML over the wire 生态中处于成熟且被广泛引用的地位。
  ecosystem_relevance: 代表 HTTP 变体的核心方案，与 WebSocket/SSE 变体形成互补选型对照。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 适合希望渐进增强现有服务端渲染页面、避免引入重型前端框架的团队，是低复杂度交互场景的首选参考。
  risk_notes:
  - 核心设计刻意保持无状态，不适合需要服务端主动推送的实时场景。
  score: 8.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - 文章将 htmx 列为基于 HTTP 的 hypermedia 方案，并指出其可通过扩展支持 WebSocket 与 SSE，但核心设计刻意保持无状态。
- object_type: project
  name: Datastar
  canonical_name: Datastar
  url: https://datastar.dev/
  positioning: 基于 SSE 的 HTML over the wire 框架，使用单向持续通道实现服务器到客户端推送。
  technical_signal: 以 Server-Sent Events 替代 WebSocket，保持与 HTML over the wire 相同的服务端渲染理念。
  adoption_signal: 文章将其作为 SSE 变体的代表进行列举。
  ecosystem_relevance: 丰富了 HTML over the wire 的传输层选择，适合仅需服务器推送的场景。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 对于需要实时推送但不需要客户端高频写入的场景，SSE 方案在连接管理和基础设施复杂度上更具优势。
  risk_notes:
  - 双向交互能力弱于 WebSocket，需评估实际交互模式是否匹配。
  score: 6.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Datastar 被描述为基于 SSE 的实现，采用与 HTML over the wire 相同的理念，只是将传输层从 WebSocket 替换为 Server-Sent
    Events。
- object_type: project
  name: Hotwire (Turbo + Stimulus)
  canonical_name: Hotwire
  url: https://hotwired.dev/
  positioning: Rails 生态的 HTML over the wire 框架套件，包含 Turbo 与 Stimulus。
  technical_signal: 默认以 HTTP 传输，同时支持 WebSocket/SSE Streams 实现实时更新。
  adoption_signal: 被文章标注为“HTML Over The Wire”名称来源，生态成熟。
  ecosystem_relevance: Rails 官方推荐方案，代表了服务端驱动 UI 在 Ruby 生态的落地。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Rails 开发者向实时应用迁移的成熟路径，Turbo Streams 与 Stimulus 组合提供了渐进增强能力。
  risk_notes:
  - 实时场景依赖额外配置，且与 Rails 生态深度绑定。
  score: 7.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Hotwire 在表格中以 HTTP + WebSocket/SSE（Streams）作为传输层，文章提到它是“HTML Over The Wire”名称的来源。
- object_type: project
  name: Django LiveView
  canonical_name: Django LiveView
  url: null
  positioning: Django 生态中基于 WebSocket 的 HTML over WebSockets 实验性实现。
  technical_signal: 在 Python/Django 后端直接维护状态并通过 WebSocket 推送渲染好的 HTML。
  adoption_signal: 文章表格标注为 Active 且作者标记为 mine，表明处于积极开发中。
  ecosystem_relevance: 将 Phoenix LiveView 模式引入 Django/Python 生态。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Django 社区探索服务端实时 UI 的本土方案，值得观察其 API 稳定性与生产可用性。
  risk_notes:
  - 成熟度低于 Phoenix LiveView，生产案例与文档可能有限。
  score: 5.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - 表格列出 Python/Django 生态中的 Django LiveView，使用 WebSocket 传输并支持服务器推送，状态为 Active，且作者标注为
    mine。
- object_type: project
  name: Reactor
  canonical_name: Reactor (Django)
  url: https://github.com/edelvalle/reactor
  positioning: Django 生态中另一个基于 WebSocket 的活跃项目。
  technical_signal: 通过 WebSocket 通道推送服务器渲染的 HTML，支持服务器主动推送。
  adoption_signal: 文章表格中状态为 Active，代表 Django 社区持续投入。
  ecosystem_relevance: 与 Django LiveView、djust 等共同构成 Django 的 HTML over WebSockets
    探索矩阵。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Django 开发者选择服务端实时方案时的备选之一，需比较其与同类项目的成熟度与维护活跃度。
  risk_notes:
  - Django 生态内同类方案较多，社区注意力可能分散。
  score: 5.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - 表格将 Reactor 列为另一个基于 WebSocket 的 Django 活跃项目，并支持服务器推送。
- object_type: project
  name: djust
  canonical_name: djust
  url: null
  positioning: 较新的 Django 项目，引入 Rust VDOM 以提升 HTML over WebSockets 渲染性能。
  technical_signal: 使用 WebSocket 传输，并借助 Rust 实现的虚拟 DOM 加速差异更新。
  adoption_signal: 文章表格列其为新出现项目，代表性能优化方向。
  ecosystem_relevance: 在 Django 生态中尝试用 Rust 弥补 Python 服务端渲染性能短板。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Rust VDOM 与 Django 的结合值得关注，若性能优势被验证，可能为 Python 服务端实时框架提供新范式。
  risk_notes:
  - 项目较新，稳定性和社区规模尚未经过大规模验证。
  score: 6.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - 表格列出 djust 是一个较新的 Django 项目，使用 WebSocket 传输，并引入 Rust VDOM 以提升渲染性能。
- object_type: project
  name: django-unicorn
  canonical_name: django-unicorn
  url: https://www.django-unicorn.com/
  positioning: Django 生态中基于 HTTP/AJAX 的轻量级组件化框架。
  technical_signal: 通过 AJAX 请求实现局部更新，设计刻意保持无状态，不支持服务器主动推送。
  adoption_signal: 适用于请求响应式交互，是 Django 渐进增强的可选方案。
  ecosystem_relevance: 代表 Django 生态内 HTTP 变体的实现，与 WebSocket 方案形成对比。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 适合已有 Django 项目不想引入 WebSocket 复杂度、仅需要局部动态更新的场景。
  risk_notes:
  - 不支持服务器推送，无法覆盖实时广播类需求。
  score: 5.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - django-unicorn 被描述为基于 HTTP/AJAX 的 Django 方案，不支持服务器推送，适用于请求响应式交互。
- object_type: project
  name: Tetra
  canonical_name: Tetra
  url: https://tetraframework.com/
  positioning: 年轻的 Django 项目，结合 AJAX 与 WebSocket 并依赖 Alpine.js 处理前端交互。
  technical_signal: 使用 AJAX + WebSocket 混合传输，前端借助 Alpine.js 管理局部状态与行为。
  adoption_signal: 文章表格标注为年轻项目，代表 Django 生态的新尝试。
  ecosystem_relevance: 在 Django 中融合 WebSocket 实时能力与轻量前端响应式库。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 其混合传输策略与 Alpine.js 集成方式值得观察，可为 Django 实时应用提供新的组合思路。
  risk_notes:
  - 项目年轻，API 与长期维护存在不确定性。
  score: 5.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Tetra 是一个年轻的 Django 项目，使用 AJAX + WebSocket 传输，并依赖 Alpine.js 处理前端交互。
- object_type: project
  name: Blazor (Interactive Server)
  canonical_name: Blazor
  url: https://dotnet.microsoft.com/apps/aspnet/web-apps/blazor
  positioning: .NET 9 中通过 SignalR 使用 WebSocket 的交互式服务端渲染方案。
  technical_signal: 以 SignalR 为底层通道，服务端维护 UI 状态并推送差异更新。
  adoption_signal: Microsoft 官方支持，状态成熟，是企业 .NET 栈的可选方案。
  ecosystem_relevance: 将 HTML over WebSockets 思想引入 .NET/C# 企业生态。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: .NET 企业开发者构建实时中后台应用时可参考，SignalR 基础设施降低了 WebSocket 接入门槛。
  risk_notes:
  - 服务端状态占用资源，大规模并发下需规划扩展与连接管理。
  score: 7.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Blazor 在表格中以 .NET 9 的 Interactive Server 模式出现，通过 SignalR 使用 WebSocket 传输并支持服务器推送。
- object_type: project
  name: Livewire 3 + Reverb
  canonical_name: Laravel Livewire + Reverb
  url: https://laravel-livewire.com/
  positioning: Laravel 生态官方服务端驱动 UI 方案，配合 Reverb 自研 WebSocket 服务器。
  technical_signal: Livewire 3 通过 WebSocket 推送 HTML 更新，Reverb 提供 Laravel 原生实时通信层。
  adoption_signal: Laravel 官方主推，生态成熟且与框架深度集成。
  ecosystem_relevance: 代表 PHP/Laravel 生态中 HTML over WebSockets 的完整官方解决方案。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Laravel 开发者构建实时应用的首选官方栈，Reverb 的推出降低了自建 WebSocket 基础设施的复杂度。
  risk_notes:
  - 与 Laravel 生态深度绑定，跨语言迁移成本较高。
  score: 7.0
  article_ids:
  - aa8df83f51e22fd8
  evidence_snippets:
  - Laravel 生态的 Livewire 3 配合 Reverb 在表格中被列为使用 WebSocket 传输并支持服务器推送的方案，Reverb 是 Laravel
    自有的 WebSocket 服务器。
---

Building a SPA (Single-page Application) is a complex puzzle: a JavaScript framework that draws the view, an API serving JSON, and 2 independent codebases forced to understand each other through contracts. It is an accepted, professionalized scenario. But being a standard does not make it the only way. I want to show you another approach, one that is not new but has gained traction over the years: **HTML over WebSockets**.

The idea is this: instead of sending JSON and assembling the HTML in the browser, the server sends the HTML already built and the client just places it where it belongs. All the rendering logic stays in the Back-End, in a single language, with no need for contracts or an API. This pattern is known as **hypermedia** or **HTML over the wire**. What matters about how the HTML travels is that it determines the **latency** and the **bidirectionality** of the communication. There are three variants:

- Over
**HTTP**, request by request, like htmx or Unicorn. - Over
**SSE**, opening a one-way, continuous channel from server to client, like Datastar. - Over
**WebSockets**, a permanent, bidirectional channel, like Phoenix LiveView or Django LiveView.

The channel is so important that it determines the application's architecture and its communication pattern.

In this article I am going to talk about **HTML over WebSockets**: the **real-time and bidirectional** variant of the family. The one that lets you build a SPA with barely any JavaScript, in a single language, with no contracts and a single rendering engine. We will see what it is, how it works and when it pays off compared to its HTTP or SSE cousins.

## Origin

**Chris McCord**, creator of Phoenix (the most popular framework in the Elixir ecosystem), presented at **ElixirConf 2019** a technology called LiveView. In just 15 minutes he built a Twitter clone that worked in real time **without adding any rendering JavaScript** or a popular framework (React, Angular, Vue...) to manage the View, proving that you could stay in the Back-End and be productive with a sweet hint of good performance. Since then the solution has grown popular, **inspiring other developers** to build HTML-over-WebSockets implementations in **other languages**. You can go back to the Back-End without giving up the good parts of the Front-End.

## How does it work?

Even though it might not seem so at first, JavaScript is used on the client. Its job is not to render but to create a communication channel with WebSockets and place the received HTML in the right spot. Plus other secondary tasks like animations, event handling, etc.

McCord's solution is **not to send the Front-End a JSON, but HTML** that needs no preprocessing. That way we move the rendering load, and all its logic, to the Back-End. OK but... how do we get the server to send us new content immediately and without making a request? Easy: with WebSockets.

Let's review the **traditional** system from the introduction. From the web I make an HTTP request, the browser starts the action and gets a JSON with all the raw information in response. The next step is to interpret it and build the corresponding HTML.

sequenceDiagram participant C as Browser participant S as Server C->>S: 1. HTTP request (GET /api/article/2/) and maybe auth S->>S: 2. Query the DB S->>S: 3. Build a JSON with the article data S-->>C: 4. Return JSON C->>C: 5. Parse the JSON C->>C: 6. Build the HTML with its rendering engine

With **HTML over WebSockets**, that same request travels over a permanent channel and the response is already assembled HTML, with no JSON in between. And since the channel never closes, the server can even **get ahead** and send changes without the client asking.

The flow with WebSockets is now the following, ignoring the initial connection and authentication, which happen only once when the channel opens:

sequenceDiagram participant C as Browser participant S as Server (Back-End) C->>S: 1. Sends a text: "I want /article/2/" S->>S: 2. Query the DB S->>S: 3. Render HTML with its template engine S-->>C: 4. Return the assembled HTML/CSS/JS

"... " C->>C: 5. Place the HTML where it belongs

Simple, elegant and fast. The client takes care of placing the HTML where it belongs and listening for events. The server handles the rest. You do not have to worry about client state or rendering logic, since everything lives in the Back-End.

The full, complex cycle, including opening the connection and authentication, would look like this:

sequenceDiagram participant C as Browser participant S as Server (Back-End) C->>S: 1. Opens WebSocket connection and authenticates Note over C,S: A single persistent channel C->>S: 2. Sends a text: "I want /article/2/" S->>S: 3. Query the DB S->>S: 4. Render HTML with its template engine S-->>C: 5. Return the assembled HTML/CSS/JS

"... " C->>C: 6. Place the HTML where it belongs Note over S,C: The server can also push

changes without the client asking (broadcast)

On top of that, by its very architecture, it carries intrinsic advantages over other solutions.

## What are its advantages?

- There is only
**one rendering engine**, cutting down complexity. - You do not need to
**build an API**: the server generates HTML and sends it to the client, with no middleman. **State lives on the server.**It is not memoryless request-response: there is a process per connected client that remembers where it is. It is the opposite of htmx, which is deliberately*stateless*.**Direct connection to the database**, with no JSON or GraphQL middleman.**Real real-time**: clients receive changes as fast as possible, without polling the server.**Broadcast**: the server can push changes to every connected client at once. Building a chat, a dashboard or a multiplayer game comes for free.- Less traffic and less latency per action: a single persistent connection avoids repeating the TCP handshake and the HTTP headers on every interaction. It is not that "the WebSocket protocol is magically faster" (HTTP/2 and HTTP/3 have narrowed that gap a lot in request-response), it is that
**you skip the round trip and send assembled HTML**. - Build a
**SPA with barely any JavaScript**, without heavy frameworks like React, Angular or Vue. - Reasonable
**SEO**: since the HTML is rendered on the server, the first load is indexable. Careful though, a crawler does not see the updates that arrive later over the WebSocket, so the important content must be in that first response. **Safer against injection**: since the server renders and escapes the HTML before sending it over the channel, an attempt to sneak in a`<script>`

travels as inert text and reaches your neighbor's screen as plain letters, not as code. The same architecture that makes a chat trivial makes it immune to XSS.

## What are its drawbacks?

- The server needs
**more resources**: it keeps a WebSocket open and, usually,**each client's state in memory**. Scaling horizontally forces you to share that state (in Django, with**Channels + an ASGI server + Redis**as the channel layer). That said, the real problem only shows up with a large number of simultaneous clients, and careful design can mitigate it. My site has handled peaks of 600 simultaneous readers without trouble, running on hardware similar to a Raspberry Pi 3 with other services running alongside. **Latency**: with a lot of physical latency, the "instant" feel suffers.**It does not work offline**. If the connection drops, the site stops working. You have to design the reconnection experience and fault tolerance.**The initial learning curve is steeper**than dropping in a`<script>`

: running a WebSocket server is not trivial, and you have to learn to handle the LiveView pattern.

## The current landscape: which frameworks exist?

The hypermedia movement already has an implementation in almost every language. Look at the **transport** column: the ones running over WebSocket (the LiveView pattern, real-time and bidirectional) coexist with the HTTP and SSE cousins, for when you do not need that two-way channel. You can start here:

| Language | Framework | Transport | Server push? | Status |
|---|---|---|---|---|
| Elixir | Phoenix LiveView | WebSocket | Yes | Mature (1.x, LiveView 1.0 in Dec 2024) |
| Ruby | Hotwire (Turbo + Stimulus) | HTTP + WebSocket/SSE (Streams) | Yes | Turbo 8 with morphing |
| Ruby | Live / Lively (socketry) | WebSocket | Yes | Maintained (v0.18, 2026), niche: pure WebSocket with morphdom, outside Rails, demo-focused |
| Python / Django | Django LiveView | WebSocket | Yes | Active (mine) |
| Python / Django | Reactor | WebSocket | Yes | Active |
| Python / Django | djust | WebSocket | Yes | New, with a Rust VDOM |
| Python / Django | django-unicorn | HTTP / AJAX | No | Active |
| Python / Django | Tetra | AJAX + WebSocket | Yes | Young, on Alpine.js |
| C# / .NET | Blazor (Interactive Server) | WebSocket (SignalR) | Yes | .NET 9, with render modes |
| PHP / Laravel | Livewire 3 + Reverb | WebSocket | Yes | Reverb, Laravel's own WebSocket server (2024) |
| Agnostic (JS) | htmx | HTTP + WS/SSE extensions | Yes (extension) | 2.0 |
| Agnostic (JS) | Datastar | SSE | Yes | 1.0 |

## SSE, the cheap option

WebSockets is powerful, but keeping a bidirectional channel open per client has a cost. And often you do not need it: if the flow is mostly **server to client** (notifications, a live feed, a dashboard, the tokens of an AI response), **Server-Sent Events (SSE)** are enough. It is the same idea, sending ready-made HTML over the wire, but over a plain HTTP channel that only goes one way.

It is the cheap option: the simplest infrastructure. By not keeping a stateful process per client, it is easier to load-balance and scale.

However, it has limitations:

**It is one-way.**Only the server pushes. If the client wants to send something, there is no channel for it: it has to make a separate HTTP request.**Text only.**It carries UTF-8, no binary (WebSocket does).**Worse for heavy bidirectional work.**In a chat, collaborative editing or a game, that loose back-and-forth over HTTP weighs more than an always-open WebSocket.

htmx has an almost identical implementation in spirit with its SSE extension. You declare the channel with an attribute and the HTML that arrives in each event places itself:

```
<div hx-ext="sse" sse-connect="/updates" sse-swap="message">
Real-time content appears here
</div>
```


Under the hood it uses the browser's own `EventSource`

, with reconnection included, and the server sends HTML fragments over `text/event-stream`

. The same philosophy as this article, just changing the transport. Along the same lines is Datastar, which unifies Alpine-style reactivity over SSE.

The quick rule: if you need **bidirectional**, low-latency communication (chat, collaboration, games), WebSocket; if you only **push from the server**, SSE is simpler and cheaper to operate.

## Final notes

HTML over WebSockets is not the answer to everything, and none of these technologies is. The transport is dictated by your problem: if you need real-time back-and-forth (a chat, a live panel, something collaborative), WebSockets; if you only push from the server, SSE; if request-and-response is enough, htmx over HTTP.

Every project is its own world, with its own quirks and limits. If you take away one thing, let it be the idea underneath: send HTML instead of JSON, stay in a single language and cross the API, the contracts and half the Front-End off your list.

Trust a good architecture, not trendy frameworks or patterns.

## Sources

- Phoenix LiveView, official docs: the canonical pattern, with per-client state on the server and diffs sent over WebSocket.
- Phoenix LiveView 1.0 released, Phoenix blog: the 1.0 milestone (December 2024), six years after the first commit.
- Hotwire, official site: where the name "HTML Over The Wire" comes from and why Turbo runs mostly over HTTP.
- htmx docs: hypermedia over HTTP, deliberately
*stateless*, with WebSockets and SSE only through extensions. - Turbo Handbook: Page Refreshes, on Turbo 8's
*morphing*to update only what changed and preserve scroll and focus. - idiomorph, the DOM
*morphing*library that several of these solutions use. - Datastar, the hypermedia framework that bets on SSE instead of WebSockets.
- Using server-sent events, MDN: how SSE works (
`EventSource`

, automatic reconnection,`Last-Event-ID`

,`text/event-stream`

). - Laravel Reverb, Laravel's first-party WebSocket server (2024): proof that PHP also does real-time without third-party extensions.
- ASP.NET Core Blazor render modes, Microsoft Learn: the Interactive Server mode runs over SignalR (WebSockets).

- Origin
- How does it work?
- What are its advantages?
- What are its drawbacks?
- The current landscape: which frameworks exist?
- SSE, the cheap option
- Final notes
- Sources

This work is under a Attribution-NonCommercial-NoDerivatives 4.0 International license.

## Help me keep writing

Every coffee gives me a push toward the next article.