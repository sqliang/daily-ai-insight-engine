---
title: IP and DNS Leaks in WebKit Affecting Proxy Browsers and iCloud Private Relay
source: https://mysk.blog/2026/08/04/webkit-proxy-icloud-private-relay-ip-leak/
author:
- '[[lapcat]]'
published: '2026-08-04'
created: '2026-08-05'
manifest_dates:
- '2026-08-05'
description: 'Article URL: https://mysk.blog/2026/08/04/webkit-proxy-icloud-private-relay-ip-leak/
  Comments URL: https://news.ycombinator.com/item?id=49176697 Points: 103 # Comments:
  15'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: a10f7abdb029ec24
source_type: community_discussion
tldr: Mysk 团队发现 WebKit 的 DNS prefetching、WebAuthn Related Origin Requests 与 WebTransport
  三个特性会绕过代理配置，导致 iOS 代理浏览器和 iCloud Private Relay 泄露真实 DNS 与真实 IP，VPN 不受影响。Psylo 1.3.1
  已通过屏蔽 dns-prefetch 并默认禁用后两项功能完成修复。
objective_summary: Mysk 团队于 2026 年 8 月发布安全研究报告，指出 WebKit 中 DNS prefetching、WebAuthn
  Related Origin Requests 与 WebTransport 三个特性会绕过 WKWebsiteDataStore.proxyConfigurations
  配置的代理，分别自 iOS 26.0、18.0、26.4 起可被利用，泄露用户真实 DNS 服务器或真实 IP 地址。这三类泄露同样影响 Apple 的 iCloud
  Private Relay，但 VPN 因在系统层隧道化全部网络流量而不受影响。研究源于 Psylo 用户报告的 DNS 泄露，Psylo 1.3.1 通过屏蔽
  dns-prefetch 提示并默认禁用 WebTransport 与 WebAuthn 完成修复。
event_type: policy_and_safety
epistemic_status: verified_fact
entities:
  companies:
  - Apple
  - Tor Project
  - Mysk
  technologies:
  - WebKit
  - DNS prefetching
  - WebAuthn
  - WebTransport
  - HTTP/3
  - QUIC
  - iCloud Private Relay
  - VPN
  - WKWebsiteDataStore.proxyConfigurations
  - Lockdown Mode
  key_people: []
key_logic_flow:
- Mysk 团队发现 WebKit 的三个特性——DNS prefetching、WebAuthn Related Origin Requests 与 WebTransport——会绕过
  WKWebsiteDataStore.proxyConfigurations 设置的代理，让设备直接泄露真实 DNS 服务器和真实 IP 地址。
- DNS prefetching 自 iOS 26.0 起可用，页面通过 <link rel=dns-prefetch> 标签让 WebKit 走设备正常 DNS
  路径解析主机名，从而绕过代理与 Private Relay。
- WebAuthn Related Origin Requests 自 iOS 18.0 起可用，系统凭据服务直接从设备发起 https://<rpId>/.well-known/webauthn
  校验请求，暴露设备真实 IP。
- WebTransport 自 iOS 26.4 起可用，new WebTransport(url) 会从设备直连建立 QUIC/HTTP/3 连接，绕过代理并暴露真实
  IP。
- 这三类泄露同样影响 Apple 的 iCloud Private Relay，但 VPN 因在系统层隧道化全部网络流量而不受影响。
- Psylo 1.3.1 通过屏蔽 dns-prefetch、默认禁用 WebTransport 与 WebAuthn，并提供每 silo 级开关让用户显式选择启用，从而修复这些泄露。
object_mentions:
- object_type: product
  name: Psylo
  canonical_name: Psylo
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Psylo 1.3.1 通过屏蔽 dns-prefetch 提示并默认禁用 WebTransport 和 WebAuthn 来修复三类泄露。
  - Psylo 将每个 silo 的全部流量经由 Mysk 私有代理网络或用户自配代理转发，使 DNS 查询本应全部来自代理服务器。
  - 由于 App Store 政策要求 iOS 浏览器使用 WebKit，任何依赖该代理 API 的 iOS 浏览器（包括 Psylo）都受影响。
  article_id: a10f7abdb029ec24
- object_type: project
  name: leaks.psylo.app
  canonical_name: leaks.psylo.app
  url: https://leaks.psylo.app
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 作者在 leaks.psylo.app 部署了概念验证网站，供用户测试 DNS 与 IP 泄露是否发生。
  article_id: a10f7abdb029ec24
- object_type: product
  name: Onion Browser
  canonical_name: Onion Browser
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Onion Browser 的 Silver 安全等级通过启用锁定模式禁用了 WebTransport，该等级用户不受此泄露影响。
  - 作者已联系 Tor Project 与 iOS 版 Onion Browser 的开发者，向他们通报这三类 WebKit 泄露问题。
  article_id: a10f7abdb029ec24
- object_type: product
  name: iCloud Private Relay
  canonical_name: iCloud Private Relay
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - iCloud Private Relay 通过两跳中继代理 Safari 的网络流量与 DNS 查询，但三类泄露均发生在 WebKit 标准页面加载流程之外。
  - Private Relay 无法捕获 DNS prefetch 查询，这些查询仍会从设备真实网络到达页面指定的权威服务器。
  article_id: a10f7abdb029ec24
- object_type: product
  name: Tor Browser (iOS)
  canonical_name: Tor Browser
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 所有 iOS Tor 浏览器都依赖 WKWebsiteDataStore 代理 API，因此同样受到这三类 WebKit 泄露的影响。
  article_id: a10f7abdb029ec24
extract_result: success
impact_score:
  score: 7.0
  reason: 该发现直接击穿 iOS 代理浏览器（含所有 Tor 浏览器）与 iCloud Private Relay 的核心隐私承诺，属于可被恶意站点实际利用的真实
    IP/DNS 泄露漏洞，且影响面覆盖所有基于 WebKit 的 iOS 浏览器。虽然属于安全漏洞披露而非范式转移，但其对隐私安全细分赛道的信任冲击即时且显著，同时波及
    Apple 的主流隐私功能，故给予 7 分。
sentiment: negative
developer_sentiment:
  tone: skeptical
  primary_focus: WebKit 的 WKWebsiteDataStore.proxyConfigurations 代理保证是否可信，以及 Apple
    能否在系统层面彻底封堵这三条绕过通道而非依赖浏览器层打补丁
hype_assessment:
  level: low
  reason: 文章是扎实的实证安全研究报告：给出三个独立漏洞向量的技术细节、iOS 版本时间线、WebKit bug 编号引用（bug 285744、272190
    等）、可验证的 POC 测试站点（leaks.psylo.app），以及已落地的修复版本（Psylo 1.3.1）。通篇无'颠覆''革命性'等营销话术，内容可复现、可验证，不存在概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 并非技术创新，而是对 WebKit 网络分层架构缺陷的逆向工程发现：DNS prefetching、WebAuthn
    Related Origin 校验请求与 WebTransport/QUIC 三条路径均在系统级绕过应用层代理配置，暴露出 WebKit 将部分网络栈职责下沉至系统服务（凭据服务、QUIC
    栈）所导致的架构级隐私漏洞，揭示了'应用层代理'与'系统层隧道'在安全边界上的本质差异。
  business_model: 动摇 iOS 代理浏览器与 iCloud Private Relay 以'隐藏真实 IP/DNS'为核心卖点的商业模式根基，用户信任受损可能推动隐私产品从应用层代理转向系统级
    VPN 隧道方案，并促使 Mysk 等厂商以'每 silo 显式开关'重塑隐私取舍的产品设计。
engineering_complexity: production_ready
compound_value:
  score: 3.5
  reason: 该事件本质是一次安全漏洞披露（WebKit 三特性绕过代理配置），本身不具备直接的产品复利效应——它不会像基础设施或模型那样随使用规模增长而累积价值。但其间接影响具有持续性：第一，它系统性动摇了
    Apple iCloud Private Relay 的隐私背书，而这一背书是 Apple 高价 iCloud+ 订阅的核心卖点之一，长期将削弱 Apple
    的隐私护城河；第二，它验证了'应用层代理不可信、系统层 VPN 才可靠'这一论断，为 VPN/安全基础设施类标的提供了持续的需求侧叙事；第三，Mysk 作为独立安全研究团队借此建立声誉，属于可复利的人力资本。综合来看，作为单次事件是'昙花一现'，但作为推动平台层隐私责任倒逼与独立安全研究崛起的证据链一环，有
    3-4 分的边际积累价值，尚不足以构成细分赛道基础设施。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- Psylo
- Tor Project
- Onion Browser
- Mysk
- VPN 服务商
- 隐私安全审计服务商
competitive_casualty:
- Apple
- iCloud Private Relay
- 未及时修复的 WebKit 代理浏览器
- 依赖 WKWebsiteDataStore.proxyConfigurations 的 iOS 隐私浏览器
market_opportunities:
- 安全检测工具厂商可开发针对 WebKit 代理绕过的自动化泄露审计服务，覆盖 DNS prefetch、WebAuthn 与 WebTransport 三类通道，为代理浏览器和隐私产品提供持续的安全合规体检，作为可复用的付费检测产品。
- 代理浏览器与隐私浏览器开发者可借鉴 Psylo 的细粒度 per-silo 功能开关方案，将'默认屏蔽高风险 WebKit 特性 + 用户显式选择'作为隐私差异化卖点，在信任敏感的用户群体中建立品牌护城河。
- 面向企业数据出境与隐私合规场景，可提供基于 WebKit 内核浏览器的隐私泄露评估与加固咨询服务，帮助有 GDPR/个保法合规压力的组织规避真实 IP/DNS
  泄露引发的法律与声誉风险。
risk_matrix:
  regulatory: 苹果 App Store 强制 iOS 浏览器使用 WebKit 的政策放大了风险面；真实 IP/DNS 泄露可能违反 GDPR/个保法下的数据最小化与匿名化义务，欧盟监管机构可能据此质询
    Apple 的隐私承诺，并推动对 WebKit 隐私能力的强制性审查。
  technological: 泄露根植于 WebKit 及操作系统级凭据服务，浏览器开发商无法通过应用层完全封堵，修复依赖 Apple 上游版本；未来 WebKit
    新增特性可能持续引入新的代理绕过路径，存在单点失效与'打地鼠'式的修复压力。
  competitive: VPN 服务商因系统级隧道不受影响而获得相对竞争优势，可能挤压代理浏览器市场空间；若 Apple 在系统层面统一修复，隐私浏览器的差异化价值将被稀释；Tor
    Project 与 Onion Browser 等竞品的应对节奏将重塑市场格局。
  ethical: 真实 IP/DNS 泄露对使用代理或 Tor 的记者、活动人士等高风险用户构成严重去匿名化威胁，可能危及其人身安全；iCloud Private
    Relay 用户对隐私承诺存在认知落差，构成误导性隐私营销方面的伦理问题。
  additional:
  - 研究公开即附带可访问的 PoC 网站（leaks.psylo.app），恶意网站可能迅速利用该漏洞对目标用户实施去匿名化攻击，形成实际的主动利用窗口。
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: product
  name: Psylo
  canonical_name: Psylo
  url: null
  positioning: Psylo 是 Mysk 团队开发的 iOS 隐私代理浏览器，通过私有代理网络或用户自配代理转发全部流量，是本次 WebKit 泄露研究的核心对象。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 作为 WebKit 代理 API 的依赖方，Psylo 的修复方案揭示了 iOS 浏览器在 App Store 政策约束下应用层代理的隐私边界。
  target_users:
  - 注重网络隐私的 iOS 用户
  - 需要多隔离浏览环境（silo）的安全研究人员与专业人士
  product_signal: Psylo 1.3.1 屏蔽 dns-prefetch 提示、默认禁用 WebTransport 与 WebAuthn，并提供每
    silo 级开关让用户显式选择启用，完成三类泄露修复。
  market_signal: Psylo 由知名安全研究团队 Mysk 推出，其及时修复泄露的表现在隐私浏览器细分市场中构成重要的信任信号。
  differentiation: 与 VPN 在系统层隧道化全部流量不同，Psylo 依赖 WebKit 应用层代理 API，只能通过屏蔽特性和显式开关来平衡隐私与网页功能。
  watch_reason: Psylo 是 Mysk 安全团队自研的隐私浏览器，其修复方案直接反映 WebKit 代理生态的隐私缺陷现状；作为首个修复三类泄露的产品，其后续版本迭代与用户反馈值得持续跟踪，以评估
    iOS 代理浏览器整体安全演进。
  risk_notes:
  - Psylo 仍依赖 iOS WebKit 代理 API，未来新增的 WebKit 特性仍可能绕过代理配置引发新的泄露。
  - 默认禁用 WebTransport 与 WebAuthn 可能影响依赖 passkey 登录、网页实时通信等功能的正常使用。
  - App Store 政策强制要求 iOS 浏览器使用 WebKit，产品无法从底层绕开此类系统级泄露风险。
  score: 8.0
  article_ids:
  - a10f7abdb029ec24
  evidence_snippets:
  - Psylo 1.3.1 通过屏蔽 dns-prefetch 提示并默认禁用 WebTransport 和 WebAuthn 来修复三类泄露。
  - Psylo 将每个 silo 的全部流量经由 Mysk 私有代理网络或用户自配代理转发，使 DNS 查询本应全部来自代理服务器。
  - 由于 App Store 政策要求 iOS 浏览器使用 WebKit，任何依赖该代理 API 的 iOS 浏览器（包括 Psylo）都受影响。
- object_type: project
  name: leaks.psylo.app
  canonical_name: leaks.psylo.app
  url: https://leaks.psylo.app
  positioning: leaks.psylo.app 是 Mysk 团队部署的概念验证网站，供用户在线检测自身浏览器是否存在 DNS 与 IP 泄露，是本研究可复现的测试入口。
  technical_signal: 该 POC 站点通过嵌入 dns-prefetch 等探测手段，让用户自助验证 WebKit 代理绕过与 IP 泄露是否发生，为安全研究提供可复现测试入口。
  adoption_signal: null
  ecosystem_relevance: 该测试站点属于 Psylo 生态的配套工具，为 iOS 代理浏览器与 Private Relay 用户提供统一的泄露检测入口，补充了
    WebKit 安全研究基础设施。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 与依赖用户自行抓包分析不同，该站点提供即时在线自检，降低了验证 WebKit 泄露的技术门槛。
  watch_reason: 作为可复现的泄露验证工具，该站点后续是否持续更新以覆盖新的 WebKit 特性，将直接反映 Mysk 团队对 iOS 隐私生态漏洞的跟进节奏与修复透明度。
  risk_notes:
  - 该测试站点由第三方运营，用户访问时自身会产生网络痕迹，检测隐私需自行权衡风险。
  - 概念验证站点仅覆盖部分泄露场景，无法保证对所有 WebKit 版本与代理组合的检测完整性。
  score: 5.0
  article_ids:
  - a10f7abdb029ec24
  evidence_snippets:
  - 作者在 leaks.psylo.app 部署了概念验证网站，供用户测试 DNS 与 IP 泄露是否发生。
- object_type: product
  name: Onion Browser
  canonical_name: Onion Browser
  url: null
  positioning: Onion Browser 是 iOS 上通过 Tor 网络实现匿名浏览的代理浏览器，其安全等级设计可部分缓解 WebKit 泄露，是本次研究的受影响产品之一。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 作为 iOS 生态最知名的 Tor 浏览器之一，其安全等级设计与 WebKit 泄露的关系直接影响 Tor 匿名网络在
    iOS 上的可用性。
  target_users:
  - 希望通过 Tor 网络匿名浏览的 iOS 用户
  - 高隐私需求的安全意识人群
  product_signal: Onion Browser 的 Silver 安全等级通过启用锁定模式禁用 WebTransport，该等级用户可免受真实 IP
    泄露影响，但其他等级仍可能受影响。
  market_signal: null
  differentiation: Onion Browser 尚未发布针对性修复，仅部分安全等级能缓解泄露，与 Psylo 1.3.1 的快速修复形成鲜明对比。
  watch_reason: 作为 iOS 端最主要的 Tor 浏览器，其后续对三类 WebKit 泄露的修复进度直接关系 Tor 用户在 iOS 上的隐私保护能力，且其安全等级缓解方案值得被其他代理浏览器借鉴。
  risk_notes:
  - 除 Silver 等级外，Onion Browser 其他安全等级用户仍可能受 DNS prefetch 与 WebAuthn 相关请求等泄露影响。
  - Tor 在 iOS 上的匿名性依赖 WebKit 应用层代理，系统级漏洞难以通过浏览器自身完全规避。
  score: 6.0
  article_ids:
  - a10f7abdb029ec24
  evidence_snippets:
  - Onion Browser 的 Silver 安全等级通过启用锁定模式禁用了 WebTransport，该等级用户不受此泄露影响。
  - 作者已联系 Tor Project 与 iOS 版 Onion Browser 的开发者，向他们通报这三类 WebKit 泄露问题。
- object_type: product
  name: iCloud Private Relay
  canonical_name: iCloud Private Relay
  url: null
  positioning: iCloud Private Relay 是 Apple 面向 iCloud+ 订阅用户推出的两跳中继隐私服务，代理 Safari 的网络流量与
    DNS 查询，但同受三类 WebKit 泄露影响。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 作为 Apple 系统级隐私功能，Private Relay 的泄露问题牵涉 WebKit 内核、Safari 与 iCloud+
    服务的整体隐私架构。
  target_users:
  - iCloud+ 订阅用户
  - 依赖 Safari 内置隐私功能而非独立 VPN 的普通苹果用户
  product_signal: Private Relay 无法捕获 dns-prefetch 等发生在标准页面加载流程之外的请求，其两跳中继机制对这三类泄露均不设防，隐私保护存在边界。
  market_signal: 作为 iCloud+ 的付费内置功能，Private Relay 的泄露问题波及大量订阅用户，可能影响消费者对苹果隐私功能的信任。
  differentiation: 与 VPN 在系统层隧道全部流量不同，Private Relay 仅代理 Safari 的页面加载流程，无法覆盖 WebKit
    的非标准网络路径，这正是其泄露根源。
  watch_reason: Private Relay 是 Apple 面向大量 iCloud+ 用户的系统级隐私服务，其能否在后续 iOS 版本中修复这三类泄露，直接检验苹果对隐私承诺的兑现程度与
    WebKit 隐私架构的演进方向。
  risk_notes:
  - Private Relay 仅作用于 Safari，用户在其他 WebKit 浏览器中无法获得同等隐私保护。
  - 三类泄露均由 WebKit 底层实现导致，修复可能依赖系统级更新，短期内难以完全解决。
  score: 7.0
  article_ids:
  - a10f7abdb029ec24
  evidence_snippets:
  - iCloud Private Relay 通过两跳中继代理 Safari 的网络流量与 DNS 查询，但三类泄露均发生在 WebKit 标准页面加载流程之外。
  - Private Relay 无法捕获 DNS prefetch 查询，这些查询仍会从设备真实网络到达页面指定的权威服务器。
- object_type: product
  name: Tor Browser (iOS)
  canonical_name: Tor Browser
  url: null
  positioning: Tor Browser（iOS）是 Tor Project 面向 iOS 发布的匿名浏览器，依赖 WebKit 应用层代理 API 转发流量，因共享该技术路径而同样受三类泄露影响。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 所有 iOS Tor 浏览器均共享 WebKit 应用层代理这条技术路径，Tor 生态在 iOS 上的隐私保障整体受制于
    WebKit 实现。
  target_users:
  - 依赖 Tor 匿名网络访问的 iOS 用户
  - 记者、活动人士等高风险隐私需求人群
  product_signal: 所有 iOS Tor 浏览器均依赖 WKWebsiteDataStore 代理 API，因此同样受 DNS prefetch、WebAuthn
    与 WebTransport 三类泄露影响，无法通过自身规避。
  market_signal: null
  differentiation: 与 Psylo 已发布 1.3.1 修复不同，Tor Project 仅被作者通报、尚未见针对性修复，其匿名承诺在 iOS 上仍存在
    WebKit 层的泄露缺口。
  watch_reason: Tor 浏览器在 iOS 上承载记者、活动人士等高敏感匿名访问场景，其修复进度直接决定用户真实 IP 与 DNS 是否继续暴露，安全价值极高，需持续跟踪
    Tor Project 的响应。
  risk_notes:
  - 作者仅向 Tor Project 通报了问题，正式修复版本尚未发布，用户当前仍面临真实 IP 与 DNS 泄露风险。
  - iOS 端 Tor 无法脱离 WebKit 运行，底层泄露只能等待 WebKit 或系统更新修复，短期内无解。
  score: 7.0
  article_ids:
  - a10f7abdb029ec24
  evidence_snippets:
  - 所有 iOS Tor 浏览器都依赖 WKWebsiteDataStore 代理 API，因此同样受到这三类 WebKit 泄露的影响。
---

## Table of Contents

## Summary#

WebKit-based browsers on iOS and macOS can be built to route all web traffic through proxy servers. This is how all proxy browsers work on iOS, including iOS Tor browsers and our own browser, Psylo. Every network connection a web page makes is supposed to flow through the configured proxy, so websites only ever see the proxy’s IP address. We found three WebKit features that bypass the proxy configuration and send traffic directly from the device instead:

**DNS prefetching**resolves hostnames through the device’s normal DNS path, which reveals the user’s real DNS servers instead of the proxy’s. Available since iOS 26.0.**WebAuthn Related Origin Requests**make the operating system’s credential service fetch a validation file directly from the device. This exposes the device’s real IP address. Available since iOS 18.0.**WebTransport**opens a direct HTTP/3 connection and bypasses the proxy, which also exposes the device’s real IP address. Available since iOS 26.4.

These leaks also impact Apple’s iCloud Private Relay. It must be noted that VPNs are not affected, since they tunnel the device’s entire network traffic at the system level.

We’ve reached out to the Tor Project and the developers of Onion Browser on iOS about these issues.

To test the leaks, you can visit our proof-of-concept website at leaks.psylo.app.


Fixed in Psylo 1.3.1:Psylo now blocks`dns-prefetch`

hints and disables WebTransport and WebAuthn by default. For websites that genuinely need these features, each one can be re-enabled through per-silo toggles. This explicit opt-in keeps the privacy trade-offs in the user’s hands. See Mitigations Introduced in Psylo 1.3.1 for details.

## Background#

### Proxy Configuration on iOS and macOS#

Introduced in iOS 17 and macOS 14, `WKWebsiteDataStore.proxyConfigurations`

allows WebKit-based browsers route all of their own web traffic through proxy servers at the application level. This API is the foundation of proxy browsers on iOS: every network connection a web page makes is supposed to flow through the configured proxy, so websites only ever see the proxy’s IP address.

### DNS Leak Reported by a Psylo User#

This investigation started with a bug report from a Psylo user who noticed DNS leaks when visiting only certain websites, and we immediately started looking into it. Psylo routes all traffic from each silo through the Mysk Private Proxy Network (or the user’s own configured custom proxy), so DNS queries should all originate from the proxy server and never from the device. It also seemed odd that this only affected some websites, and not all.

As we dug deeper, we found the source of the DNS leaks, plus two more leaks that actually reveal the device’s real IP address. All three leaks live in WebKit, where they bypass the proxy settings provided by `WKWebsiteDataStore.proxyConfigurations`

. Since Apple’s App Store policy requires every iOS browser to use WebKit, any iOS browser that relies on this API for proxying is affected, including all iOS Tor browsers and Psylo. These leaks are also present in Apple’s iCloud Private Relay. VPNs, on the other hand, are not affected by these issues, since the device’s entire network traffic is tunneled through the VPN at the system level.

### iCloud Private Relay#

iCloud Private Relay is Apple’s privacy feature for iCloud+ subscribers. When enabled, it proxies Safari’s (and only Safari’s) web traffic and DNS queries through a two-hop relay, designed so that no single party, not even Apple, can see both who you are and which sites you visit. As it turns out, all three leaks described in this article occur outside WebKit’s standard page-loading process, meaning Private Relay is susceptible to the same leaks.

## 1. DNS Prefetching#

DNS prefetching lets a website ask the browser to resolve a hostname before it’s needed. So when later it needs to connect to that hostname, the lookup is already done and the connection starts faster. This is done through a `<link rel="dns-prefetch">`

HTML tag.

When a page includes that tag, WebKit resolves the hostname through the device’s normal DNS path, regardless of any proxy set by the browser through `WKWebsiteDataStore.proxyConfigurations`

. A page can embed unique per-visitor hostnames in these tags, then watch the queries arrive at its own authoritative DNS server from the visitor’s real network rather than the proxy’s.

This was the leak behind the original user report, and it explains why only some websites triggered it: without prefetch tags on the page, WebKit doesn’t perform this DNS lookup.

Private Relay doesn’t catch this one. It normally proxies Safari’s DNS queries, but these prefetch lookups skip it. The query reaches the authoritative server from the device’s real network even with Private Relay enabled.

Desktop Safari has supported

`<link rel="dns-prefetch">`

since Safari 5, but iOS ignored it until iOS 26.0 (September 2025), when WebKit enabled it in the same change that removed iOS’s older, implicit speculative DNS prefetching (bug 285744, 290327@main; browser-compat data). That resolver had been rewritten the year before, to keep hostnames out of system logs during private browsing (bug 272190, 279199@main).

## 2. WebAuthn Related Origin Requests#

WebAuthn is the web standard behind passkeys. A passkey is normally bound to a single domain, but Related Origin Requests let an organization use one passkey across a small set of domains it owns.

To make that work, when a page requests a credential whose `rpId`

differs from its own origin, the client first fetches `https://<rpId>/.well-known/webauthn`

, a JSON file listing which origins may use that `rpId`

.

That validation fetch doesn’t come from the browser’s network stack. WebKit hands WebAuthn ceremonies to the operating system’s credential service, which issues the HTTPS request itself, directly from the device and unaware of any proxy the host app configured. A page can set `rpId`

to a host of its choosing, and the fetch fires even without user interaction: with `mediation: "conditional"`

and no UI ever appears.

The same reasoning applies to iCloud Private Relay. Because the fetch is issued by the operating system’s credential service rather than by Safari, it never enters Private Relay’s proxied path. The destination server sees the device’s real IP address either way.

Apple announced the feature for iOS 18.0 / Safari 18.0 (September 2024) in WebKit Features in Safari 18.0. WebKit’s half of the plumbing landed earlier that year (bug 268426, 274592@main) and even shipped, inert, in iOS 17.4; the system component that performs the fetch only gained support in 18.0.


## 3. WebTransport#

WebTransport is a low-latency alternative to WebSocket. It runs over HTTP/3 and QUIC, offers multiple independent streams plus unreliable datagram delivery, and can fall back to HTTP/2 where QUIC is unavailable.

Calling `new WebTransport(url)`

opens a QUIC connection straight from the device. WebKit builds the connection with its own network parameters and never offers it the session’s proxy, so the server sees the device’s real IP address instead of the proxy’s.

Private Relay doesn’t help here either. WebKit builds the connection outside the web traffic that Private Relay proxies, so a WebTransport server learns the device’s real IP address even with Private Relay enabled.

There is one exception: Onion Browser’s “Silver” security level configures WebKit with Lockdown Mode, which disables WebTransport entirely, so Onion Browser users at the Silver level are not affected by this particular leak.

First traces of the API appeared in 2023 (bug 260810, 267408@main) but sat disabled until December 2025, when it was switched on for platforms with sufficient Network.framework support (bug 303453, 303860@main). It shipped publicly in iOS 26.4 (March 2026); see WebKit Features for Safari 26.4 and the Safari 26.4 Release Notes.


## Mitigations Introduced in Psylo 1.3.1#

We’ve addressed all three leaks in Psylo 1.3.1:

- Psylo blocks
`dns-prefetch`

hints, so a page can no longer make your device resolve attacker-controlled hostnames. - WebTransport is disabled by default.
- WebAuthn is disabled by default.

Passkeys and WebTransport have legitimate uses, so both can be re-enabled at any time through per-silo toggles. This keeps Psylo leak-free out of the box, while users who need one of these features on a given site can opt in explicitly, with a clear understanding of the trade-off.