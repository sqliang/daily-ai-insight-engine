---
title: qm – Multiplayer agent harness for work
source: https://github.com/yc-software/qm
author:
- '[[tosh]]'
published: '2026-07-31'
created: '2026-08-01'
manifest_dates:
- '2026-08-01'
description: 'Article URL: https://github.com/yc-software/qm Comments URL: https://news.ycombinator.com/item?id=49126604
  Points: 570 # Comments: 116'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 872178780cb4ed91
source_type: community_discussion
tldr: QM 是 yc-software 推出的开源多人智能体工作平台，可在 Slack 和网页上运行，专为初创公司设计，员工拥有独立工作区并可在频道与项目中协作，核心可切换
  Claude Code、OpenCode、Codex 等多种底层框架。
objective_summary: QM 是 yc-software 发布的开源多人智能体工作平台，面向初创公司，每位员工拥有独立隔离的工作区，也可在 Slack
  频道、群组消息和项目中与智能体协作。每个成员和聊天室拥有独立的内存、文件、密钥、权限、定时任务、Web 应用和持久化沙箱。系统基于 TypeScript/Node
  构建，核心通过 Postgres 持久化会话与状态，Slack 插件、Web UI 和管理后台均为可选插件，同一部署可在 Pi、OpenCode、Codex、Claude
  Code 等框架间切换。项目提供 Strict、Auto、Dangerous 三种安全姿态，代码以 MIT 协议开源，贡献仅接受人类撰写的文本。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - yc-software
  - Y Combinator
  - Slack
  technologies:
  - Claude Code
  - OpenCode
  - Codex
  - Pi
  - Postgres
  - Fastify
  - Bolt
  - TypeScript
  key_people: []
key_logic_flow:
- QM 是 yc-software 发布的开源多人智能体工作平台，面向初创公司，员工各自拥有独立隔离的工作区，也可在 Slack 频道和项目中与智能体协作。
- 每个成员和聊天室拥有独立的内存、文件、密钥、权限、定时任务、Web 应用和持久化沙箱，技能可按作用域授权共享并支持从 Git 仓库导入。
- 系统以 TypeScript/Node 构建，核心通过 Postgres 持久化会话与队列状态，Slack 插件、Web UI、管理后台和公共门户均为可选插件。
- 核心层是通用的，同一部署可在 Pi、OpenCode、Codex 和 Claude Code 之间切换底层智能体框架，不绑定单一厂商。
- 安全方面提供 Strict、Auto、Dangerous 三种组织级姿态，Auto 为默认值，由分类器在内容进入模型前筛查带来源标注的外部数据和工具结果。
- 部署通过 qm CLI 初始化部署目录，支持公开部署仓库与私有 fork 两种运维模式，代码以 MIT 协议开源，贡献只接受人类撰写的文本而非代码。
object_mentions:
- object_type: project
  name: yc-software/qm
  canonical_name: yc-software/qm
  url: https://github.com/yc-software/qm
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - QM 是一款面向工作的多人智能体工具平台，可在 Slack 和网页上使用，专为初创公司设计，每位员工拥有独立隔离的工作区。
  - 每个成员和聊天室拥有独立的内存、文件、密钥、权限、定时任务、Web 应用和持久化沙箱，同时支持在频道与项目中协作。
  - QM 核心基于 TypeScript 和 Node 构建，通过 Postgres 持久化会话与状态，Slack 与 Web UI 等均为可选插件，代码以 MIT
    协议开源。
  article_id: 872178780cb4ed91
- object_type: product
  name: Claude Code
  canonical_name: Claude Code
  url: https://github.com/anthropics/claude-code
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - QM 的核心是通用的，可在 Pi、OpenCode、Codex 和 Claude Code 等底层智能体框架之间切换，同一部署不绑定单一厂商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
  article_id: 872178780cb4ed91
- object_type: product
  name: OpenCode
  canonical_name: OpenCode
  url: https://github.com/opencode-ai/opencode
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - QM 的核心层可切换 OpenCode 作为底层智能体框架之一，同一部署可在多个框架间自由选择而不绑定单一厂商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
  article_id: 872178780cb4ed91
- object_type: product
  name: Codex
  canonical_name: Codex
  url: https://github.com/openai/codex
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - QM 的核心可在 Pi、OpenCode、Codex 和 Claude Code 之间切换底层框架，因此部署不依赖任何单一智能体供应商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
  article_id: 872178780cb4ed91
- object_type: product
  name: Pi
  canonical_name: Pi
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - QM 的核心可选用 Pi 作为底层智能体框架之一，与 OpenCode、Codex 和 Claude Code 一样驱动同一核心。
  article_id: 872178780cb4ed91
extract_result: success
impact_score:
  score: 6.5
  reason: 该事件是 yc-software 发布的开源多人智能体工作平台，切入的是多智能体协同编排这一竞争激烈但尚未定型的赛道。核心看点在于将 Claude
    Code、Codex、OpenCode、Pi 等本地编码智能体抽象为可替换基座，配合 Postgres 持久化与每作用域隔离沙箱，提供了组织级多租户智能体基础设施——这改变了'智能体只是个人助理'的默认假设，有望在初创公司群体中重塑智能体部署方式，属于能改变局部竞争格局的重要产品发布。但项目刚开源、生态和社区尚未建立，短期影响力主要局限于开发者圈层与
    YC 生态，尚不足以触发行业范式转移，故评 6.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 同一部署可在 Claude Code/Codex/OpenCode/Pi 之间自由切换的无厂商锁定核心抽象，以及每作用域独立沙箱与密钥隔离的工程实现
hype_assessment:
  level: low
  reason: 文章通篇是具体的技术架构描述——Postgres 持久化、Fastify/Bolt 插件栈、三档安全姿态（Strict/Auto/Dangerous）、部署目录与私有
    fork 两种运维模式，全程没有使用'颠覆''革命性'等 PR 滥用词汇。项目以 MIT 协议开源且提供了可实际运行的 CLI 初始化与 Fly/AWS 部署路径，属于实打实的工程产物。唯一的产品包装点在于'专为初创公司设计'的定位话术，但整体干货浓度高，水分低。
information_entropy: high
domain_disruption:
  technical_innovation: 将多种本地编码智能体框架抽象为可插拔基座，核心通过接口解耦 harness、会话存储、沙箱、记忆等子strate；每个成员与房间拥有独立作用域的内存、文件、密钥、权限、定时任务与持久化沙箱，实现了组织级多租户的智能体协作基础设施——这是从'个人助理'到'团队基础设施'的本质工程跃迁，而非简单放大单智能体能力。
  business_model: 以 MIT 开源 + 部署目录（Fly/AWS）模式切入，暗示后续托管服务的商业化空间；对 YC 而言是布局智能体基础设施的战略卡位。对初创生态的影响在于：企业可自托管一套覆盖全员的智能体工作台并自带安全审批策略，降低对单一厂商
    Agent 产品的依赖，可能冲击 LangChain/CrewAI 等编排层工具的生态位，并推动'智能体按席位/作用域计费'的新型 SaaS 定价模型。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: 从复利视角看，QM 押注的是'多人智能体协作层'这一新兴基础设施赛道，具备三重正向积累：其一，厂商中立架构让同一部署可在 Claude Code、Codex、OpenCode、Pi
    之间切换底层框架，显著降低采用摩擦，有机会成为跨模型之上的编排层事实标准；其二，共享技能、作用域授权与团队协作模式一旦形成生态，将积累网络效应（技能包、团队习惯、部署模板）；其三，MIT
    开源 + YC 背书显著降低了初创公司的信任与上手门槛，开源即分发。但需冷静看待两个负向变量：一是 Anthropic/OpenAI 等模型厂商正在把多代理协作能力原生植入自家
    harness（Claude Code、Codex 的团队功能），第三方中间件存在被平台原生能力'空心化'的长期风险；二是开源项目自身价值捕获能力有限，且该赛道竞争拥挤（LangChain、CrewAI
    等已有先发）。综合判断：有潜力成为细分赛道基础设施，但 3-5 年后被平台原生能力替代的概率不低，评分置于中游偏上，需持续验证其开发者采用与生态沉淀速度。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Anthropic
- OpenAI
- yc-software
- YC portfolio startups
competitive_casualty:
- 闭源单厂商 Agent 编排平台
- 传统 RPA/流程自动化厂商
- Slack 原生 AI 助手
market_opportunities:
- 创业者可借鉴 QM '框架无关核心 + 部署目录' 的架构，面向企业构建可在 Claude Code、Codex、OpenCode 等之间自由切换的多智能体协作平台，降低厂商锁定顾虑，这是当前
  agent 基础设施的差异化切入点
- 围绕'智能体即团队成员'的多人协作场景，可开发企业级智能体治理与审计 SaaS，覆盖凭证管理、操作审计、按作用域授权的技能治理等需求，填补现有单机 coding
  agent 向团队化演进留下的空白
- Auto 安全姿态中的外部数据筛查分类器与可插拔筛查代理具备产品化潜力，可作为独立的智能体安全筛查服务，销售给同时部署多种 coding agent 的企业，形成安全护城河
risk_matrix:
  regulatory: 智能体以员工身份持凭证自主执行操作（发邮件、开 PR、操作数据库）可能触发 AI Act 的透明度与人类监督要求、GDPR/个保法等数据合规风险，尤其当定时任务在无人值守时处理个人数据；企业私有化部署还需关注数据跨境与出口管制约束
  technological: 项目尚处早期（无生产部署 CI、贡献仅接受文本），依赖框架抽象层与 Slack 等外部平台；若底层模型/harness 厂商原生推出团队协作能力或收紧
    API，抽象层价值可能被架空，也存在被更成熟开源多智能体框架替代的风险
  competitive: 微软（Teams+Copilot/Codex）、Salesforce（Slack+Agentforce）、Google Jules 及众多
    agent 创业公司均押注同一赛道，巨头可借生态捆绑（通讯工具原生集成）挤压开源项目生存空间；开源社区竞争同样激烈
  ethical: Auto 模式依赖分类器筛查外部数据，但 Dangerous 模式无任何筛查，存在提示注入与数据投毒污染'公司大脑'检索的风险；智能体持员工凭证自主执行操作可能引发越权、隐私泄露，以及自动化替代初级岗位带来的就业冲击
  additional:
  - 单点维护风险：项目由 yc-software 一家主导，MIT 开源但长期演进依赖社区活跃度
  - 基础设施安全风险：每作用域持久化沙箱持有密钥视图，若沙箱隔离被攻破将造成横向移动与凭证批量泄露，需重点评估其威胁模型与已知限制
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: yc-software/qm
  canonical_name: yc-software/qm
  url: https://github.com/yc-software/qm
  positioning: QM 是 yc-software 推出的开源多人智能体工作平台，面向初创公司，员工拥有独立工作区并可在 Slack 频道与项目中协作。
  technical_signal: 基于 TypeScript/Node 构建，核心通过 Postgres 持久化会话与状态，通用核心层可在 Pi、OpenCode、Codex、Claude
    Code 等框架间自由切换。
  adoption_signal: 项目以 MIT 协议开源并支持 qm CLI 初始化部署，面向初创公司提供零厂商绑定的切入路径，但文章未披露实际采用数据。
  ecosystem_relevance: 提供 Strict、Auto、Dangerous 三种安全姿态，Slack、Web UI、管理后台均为可选插件，可与多种编码智能体框架生态协同工作。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: QM 将多人协作、持久化沙箱与可切换底层框架融为一体，代表编码智能体从单机助手走向公司级协作基础设施的方向，其安全姿态与生态采用进展值得持续跟踪。
  risk_notes:
  - 项目处于早期阶段，文章中未见实际用户规模与生产环境案例，真实采用情况仍有待验证。
  - Auto 安全姿态依赖分类器对来源标注内容的筛查，其误判与绕过风险需要在真实工作负载中检验。
  score: 8.0
  article_ids:
  - 872178780cb4ed91
  evidence_snippets:
  - QM 是一款面向工作的多人智能体工具平台，可在 Slack 和网页上使用，专为初创公司设计，每位员工拥有独立隔离的工作区。
  - 每个成员和聊天室拥有独立的内存、文件、密钥、权限、定时任务、Web 应用和持久化沙箱，同时支持在频道与项目中协作。
  - QM 核心基于 TypeScript 和 Node 构建，通过 Postgres 持久化会话与状态，Slack 与 Web UI 等均为可选插件，代码以 MIT
    协议开源。
- object_type: product
  name: Claude Code
  canonical_name: Claude Code
  url: https://github.com/anthropics/claude-code
  positioning: 作为 QM 多人智能体平台可切换的底层智能体框架之一，Claude Code 提供本地编码智能体执行能力，与 QM 通用核心解耦并存。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用本地编码智能体的工程师与研发团队
  - 部署 QM 等多人智能体平台的初创公司
  product_signal: 作为 QM 可切换的底层智能体框架，同一部署可在 Claude Code、Pi、OpenCode、Codex 之间选择，不绑定单一厂商。
  market_signal: QM 等平台将主流编码智能体框架做成可插拔引擎，反映该领域正从单厂商绑定走向多框架互操作生态。
  differentiation: 在 QM 内部 Claude Code 与 OpenCode、Codex 互为替代，其差异化将体现在模型能力、工具链与审计机制的综合表现上。
  watch_reason: Claude Code 作为 QM 可切换的主流本地编码智能体框架，其能力与生态演进将影响多人智能体平台的互操作标准和厂商格局，值得持续跟踪其被集成方式。
  risk_notes:
  - 在 QM 等平台中 Claude Code 被抽象为可替换组件，若框架切换成本过低，其生态黏性与差异化空间可能被削弱。
  score: 5.0
  article_ids:
  - 872178780cb4ed91
  evidence_snippets:
  - QM 的核心是通用的，可在 Pi、OpenCode、Codex 和 Claude Code 等底层智能体框架之间切换，同一部署不绑定单一厂商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
- object_type: product
  name: OpenCode
  canonical_name: OpenCode
  url: https://github.com/opencode-ai/opencode
  positioning: 作为 QM 多人智能体平台可切换的底层智能体框架之一，OpenCode 提供本地编码智能体执行能力，并与 QM 通用核心协同工作。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用本地编码智能体的工程师与研发团队
  - 部署 QM 等多人智能体平台的初创公司
  product_signal: QM 的核心层可切换 OpenCode 作为底层智能体框架，同一部署可在多个框架间自由选择而不绑定单一厂商。
  market_signal: OpenCode 被纳入 QM 的可切换框架集合，印证开源编码智能体正被作为可组合组件接入多人协作基础设施。
  differentiation: 在 QM 内部 OpenCode 与 Claude Code、Codex 互为替代，其差异化体现在开源生态、模型接入灵活性与工具链覆盖上。
  watch_reason: OpenCode 作为 QM 可切换的开源编码智能体框架，其被多人协作平台集成的路径反映了开源编码智能体的可组合性与生态定位变化。
  risk_notes:
  - 文章仅将 OpenCode 作为可切换框架之一提及，缺乏对其独立能力与采用情况的直接证据，需谨慎评估其实际表现。
  score: 4.0
  article_ids:
  - 872178780cb4ed91
  evidence_snippets:
  - QM 的核心层可切换 OpenCode 作为底层智能体框架之一，同一部署可在多个框架间自由选择而不绑定单一厂商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
- object_type: product
  name: Codex
  canonical_name: Codex
  url: https://github.com/openai/codex
  positioning: 作为 QM 多人智能体平台可切换的底层智能体框架之一，Codex 提供本地编码智能体执行能力，并与其他框架共用同一通用核心。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用本地编码智能体的工程师与研发团队
  - 部署 QM 等多人智能体平台的初创公司
  product_signal: QM 的核心可在 Pi、OpenCode、Codex 和 Claude Code 之间切换底层框架，因此部署不依赖任何单一智能体供应商。
  market_signal: Codex 被 QM 列为可互换的底层框架，表明编码智能体市场竞争正从单厂商方案转向可组合、可替换的平台化集成。
  differentiation: 在 QM 内部 Codex 与 Claude Code、OpenCode 互为替代，其差异化体现在模型能力、厂商策略与执行稳定性上。
  watch_reason: Codex 作为 QM 可切换的底层智能体框架，其与多家框架的互操作集成反映了编码智能体走向平台化的趋势，值得持续跟踪其在多框架生态中的定位。
  risk_notes:
  - 文章仅提及 Codex 可作为 QM 的可切换框架，未提供其独立产品信号与采用数据，需结合其他来源交叉验证。
  score: 4.0
  article_ids:
  - 872178780cb4ed91
  evidence_snippets:
  - QM 的核心可在 Pi、OpenCode、Codex 和 Claude Code 之间切换底层框架，因此部署不依赖任何单一智能体供应商。
  - QM 采用与 OpenCode、Codex 和 Claude Code 类似的本地编码智能体思路，智能体以所服务人员的身份凭据行动并全程审计。
---

A multiplayer agent harness for work. In Slack and on the web.

Most agents are designed like personal assistants. You can make one work for a whole company, but it quickly gets complex. QM is designed for startups. Employees each get their own isolated workspace and work independently without affecting each other, and they can also collaborate with the agent in channels, group messages, and projects.

Each person and each room has its own scoped memory, files, keychain view, permissions, crons, web apps, and durable sandbox.

It's built with open source in mind. Pick your own harness and model and switch between them — Pi, OpenCode, Codex, and Claude Code all drive the same core, so a deployment isn't tied to any single vendor.

**Personal and shared scopes.**People customize the agent to be*theirs*, and still work with it collaboratively in Slack channels and projects.**Slack and web.**The same identity and configuration carries between Slack and the web app.**Admin control.**Set org-level configuration, a security posture, and which harnesses and models are available.**Web apps.**Spin up custom internal apps and publish them to the right people.**Shared skills.**Skills are scope-owned and shareable by grant, with admin-gated promotion to the whole org and skill packs imported from git repositories.**Background work.**Crons and watches run work while nobody's watching.

- Search internal notes, email, documents, databases, and the web together
- Retrieve information from your company brain
- Build internal apps, publish them to the right people, and keep their data current
- Learn your writing voice from past sends, then triage your inbox on a schedule — labels and reply drafts included
- Work in an existing repository: run tests, open PRs, monitor CI, check system logs
- Track a project in a shared channel and post updates and follow-ups

```
flowchart LR
DB[("Postgres<br/>sessions · memory · queue")]
subgraph CORE["Headless core"]
API["API · identity · policy · scheduler"]
LOOP["Agent loop<br/>(Pi, OpenCode, Claude Code)"]
API <--> LOOP
end
SBX["Per-scope sandbox<br/>files · tools · logged-in services"]
DB <--> API
LOOP <--> SBX
```

Every turn runs through a central core, which can use a variety of models and harnesses
to generate the response. A Postgres persistence layer holds user data, session history,
and other durable state. The agent has a small, fixed tool surface; one of those tools is
`execute`

, which runs commands in the scope's own isolated sandbox — its durable computer,
where installed tools stay installed. The web UI, the admin panel, and the public portal
are optional plugins over the core's HTTP API;
Slack is an optional in-process plugin that core starts
and supervises through a direct service client.

The core runs TypeScript directly on Node and uses Fastify for HTTP. The Slack plugin uses Bolt; the web UI builds with Vite and renders with Lit.

The core itself is generic. Everything specific to one company — org config, custom tools
and skills, sandbox image, infrastructure — lives in a **deployment directory** that the
`qm`

CLI validates and deploys. Every substrate (harness, session
store, sandbox, memory) sits behind an interface, so production implementations swap in
via one wiring file.

QM's approach follows local coding agents like OpenCode, Codex, and Claude Code: the agent acts as the person it's working for, with their credentials and permissions, and everything it does is audited. An org picks one security posture, which narrower scopes can only tighten:

**Strict**— every harness tool call pauses for human approval, except the two no-effect turn enders.**Auto**(default) — a classifier screens provenance-labelled external data and tool results before they reach the model; a deployment can point that at its own screening proxy.**Dangerous**— no content screening, no pauses between tool calls.

The predeclared command policy — approval rules and hard denials for things like recursive deletes or destructive SQL — applies in every posture, Dangerous included.

`SECURITY.md`

has the threat model, the operator assumptions, and the
known limitations.

Create an organization-owned deployment repository that depends on `@yc-software/qm`

:

```
npm exec --yes --package=@yc-software/qm@latest -- \
qm init . --org <slug> --target <fly-or-aws>
npm install
```

Initialization materializes a deployment skill for an agent and walks through
infrastructure, web sign-in, connector credentials, optional Slack access, deployment,
and live verification — no source checkout required. Each deployment runs in the
operator's own cloud account; initialization does not generate or enable deployment CI,
and this repository has no production deployment workflow. See
`deployment.md`

for the details.

We take contributions as *human-written* text, not code — see
`CONTRIBUTING.md`

. Describe the change you'd like informally in a
`.txt`

or `.md`

file in `adrs/`

, and if we're aligned we'll handle the
implementation. Report vulnerabilities privately — see `SECURITY.md`

,
not a public issue.

The deployment repository above carries config and a sandbox layer, and never needs a
source checkout. Some organizations want the opposite trade: the whole codebase in one
place, so engineers and coding agents read core and customizations together, while the
customizations themselves stay private. For that, keep a **private fork**: a standalone
private repository whose history begins as a clone of qm and whose core stays identical
to upstream.

Populate it once, then clone it to work in:

```
gh repo create <org>/qm-private --private
git clone --bare git@github.com:yc-software/qm qm-seed.git
git -C qm-seed.git push --mirror git@github.com:<org>/qm-private
rm -rf qm-seed.git
git clone git@github.com:<org>/qm-private
git -C qm-private remote add upstream git@github.com:yc-software/qm
```

Create the private fork with a plain clone, as shown above, and never with GitHub's fork feature. The word "fork" here names the concept — a downstream copy that diverges deliberately and merges from upstream — not GitHub's Fork button. A GitHub fork inherits the visibility of the repository it came from, so a fork of a public repository cannot be made private. A GitHub fork also shares one object network with the repository it came from, so commits pushed to the fork stay fetchable by SHA from the public side. Many organizations disallow forking private repositories as well. A plain clone has none of these problems, and it costs one thing: the clone is an ordinary repository, so upstream's CI workflows run live in your own account. Expect to supply the secrets those workflows need, or disable the ones you do not want running.

Everything specific to your organization goes in `deploy/layers/<org>/`

— config, sandbox
tools and skills, plugin images, infrastructure — in the same shape `qm init`

produces. See
`deploy/layers/README.md`

. Core stays byte-identical to
upstream, which is what keeps merges small.

Two skills maintain the boundary in both directions. `update-qm`

merges upstream qm into
the private fork and opens the sync PR; `upstream-pr`

sends an organization-agnostic fix back to
qm, cutting the branch from `upstream/main`

and checking the outgoing diff, commit
messages, and screenshots for organization identifiers before it pushes. Nothing under
`deploy/layers/`

ever travels upstream.

`docs/getting-started.md`

— first run, end to end`cli/README.md`

— the`qm`

CLI and the deployment directory contract`docs/deploy-directory.md`

— the deployment directory in full`.env.example`

— every knob, documented in place`plugins/`

— the surfaces (Slack, web UI, admin, portal)

Except where otherwise noted, QM is available under the MIT License.