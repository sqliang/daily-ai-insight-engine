---
title: Upcoming breaking changes for npm v12
source: https://github.blog/changelog/2026-06-09-upcoming-breaking-changes-for-npm-v12/
author:
- '[[plasma]]'
published: '2026-06-09'
created: '2026-06-10'
description: 'Article URL: https://github.blog/changelog/2026-06-09-upcoming-breaking-changes-for-npm-v12/
  Comments URL: https://news.ycombinator.com/item?id=48467705 Points: 355 # Comments:
  134'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 1a0a2e4a0ce4b124
source_type: community_discussion
tldr: npm v12 将于2026年7月发布，默认关闭 allowScripts、Git 和远程依赖自动解析，需用户显式信任。
objective_summary: npm 官方宣布 v12 将于2026年7月发布，引入三项安全相关的默认变更：allowScripts 默认关闭、--allow-git
  默认 none、--allow-remote 默认 none。用户可在 npm 11.16.0+ 中预览警告并提前配置。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - GitHub
  - npm
  technologies:
  - npm
  key_people: []
key_logic_flow:
- npm v12 将于 2026年7月发布，在 npm 11.16.0+ 中已有行为变更警告，用户可提前准备。
- allowScripts 默认关闭：依赖包的 preinstall、install、postinstall 脚本不再自动执行，包括 node-gyp 原生构建和
  git/file/link 依赖的 prepare 脚本。
- 用户可通过 npm approve-scripts 查看待处理脚本并显式信任或拒绝，信任列表写入 package.json 并应提交到版本控制。
- --allow-git 默认 none：npm install 不再自动解析 Git 依赖，关闭了依赖中 .npmrc 覆盖 Git 可执行文件的代码执行路径。
- --allow-remote 默认 none：npm install 不再自动解析远程 URL 依赖（如 https tarball），需通过 --allow-remote
  显式允许。
impact_score:
  score: 7.0
  reason: npm 是 Node.js 生态最核心的基础设施，全球数百万开发者每天依赖它。v12 的三项默认变更——关闭脚本自动执行、禁止 Git 依赖自动解析、禁止远程
    URL 依赖自动解析——直接切断了供应链攻击中最常见的三条代码执行路径。这对整个 JavaScript 生态的安全模型是一次实质性升级，影响面极广。但并非范式转移（如
    ChatGPT 发布），而是渐进式的安全加固，故评分 7 分。
sentiment: positive
developer_sentiment:
  tone: skeptical
  primary_focus: 迁移成本与脚本兼容性：大量依赖 node-gyp 原生模块的项目需逐一审批脚本，存量项目的 CI/CD 配置可能大面积报错
hype_assessment:
  level: low
  reason: 这是一份标准的版本变更公告，语言朴实，无 PR 话术包装。每一项变更都有明确的安全动机、可预览的警告机制和具体的迁移步骤，没有使用颠覆、革命性等夸大词汇，信息可信且可验证。
information_entropy: high
domain_disruption:
  technical_innovation: 将包管理器依赖安装中的脚本执行从默认自动改为显式信任制，Git 依赖和远程 URL 依赖也从默认自动解析改为白名单制。这种安全模型从隐式信任（install
    时自动跑脚本）转向显式信任（开发者明确审批），是 npm 架构层面的根本变化。
  business_model: 无直接商业模式影响。但 npm 作为开源基础设施的安全升级，可能间接影响企业级 Node.js 项目的安全合规流程和 CI/CD
    工具链的适配。
engineering_complexity: production_ready
compound_value:
  score: 3.5
  reason: npm v12 的三项安全变更（allowScripts、--allow-git、--allow-remote 默认关闭）本质上是生态基础设施的防御性升级，而非商业模式的创新或新增值空间。它通过减少供应链攻击面来巩固
    JavaScript 生态的长期健康度，但不会直接创造新的收入流或复利增长曲线。从 VC 视角看，这类'合规安全基建'很难独立构成投资标的——它更像 Node.js
    生态的'卫生标准'提升，价值体现在降低系统性风险而非创造超额回报。最大的商业受益者是 npm 的母公司 GitHub/Microsoft（通过加固护城河间接获益），但这一事件本身不具备独立的复利效应，评分偏低。
value_capture_layer: end_application
moat_impact: strengthens_monopoly
key_beneficiaries:
- npm (GitHub/Microsoft)
- Node.js 生态
- 企业级 JavaScript 开发者
competitive_casualty:
- pnpm
- Yarn
- 依赖 install 脚本的恶意包
- 供应链攻击者
market_opportunities:
- 可为企业和开源项目开发 npm v12 迁移审计工具，自动化扫描依赖脚本并生成信任清单，帮助团队平稳过渡
- CI/CD 集成商可推出 npm v12 兼容性检测服务，在升级前识别因脚本被阻断而可能失败的构建环节
- DevOps 咨询公司可围绕供应链安全最佳实践推出培训和评估服务，协助组织从脚本自动执行转向显式信任模型
risk_matrix:
  regulatory: 无
  technological: 大量 npm 包依赖 install 脚本（如 node-gyp 原生编译、postinstall 钩子）完成构建，升级后若未配置信任清单将直接失败，可能大规模破坏现有
    CI/CD 流水线和本地开发环境
  competitive: pnpm 和 Yarn 在权限控制与安全策略方面已有更成熟的方案，npm v12 的激进安全默认值可能加速部分团队向替代包管理器迁移，但同时也强化了
    npm 的供应链安全竞争力
  ethical: 本变更是正向供应链安全提升，显著降低依赖脚本中植入恶意代码的攻击面，但可能对维护不足的老旧开源项目造成构建中断的意外后果
  additional:
  - 大规模存量项目（尤其是无人维护的遗留开源库）可能因无人配置信任清单而永久性构建失败，形成长期的软件供应链债务
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: strategic_invest
---

Our next npm major version, v12, introduces security-related default changes to `npm install`

. All these changes are available behind warnings in npm today on 11.16.0 or newer, so you can prepare before the upgrade. v12 is estimated to release in July 2026.

Each change turns an `npm install`

behavior that runs automatically today into one you explicitly opt into:

`allowScripts`

defaults to off:`npm install`

will no longer execute`preinstall`

,`install`

, or`postinstall`

scripts from dependencies unless they are explicitly allowed in your project. This includes native`node-gyp`

builds (i.e., a package with a`binding.gyp`

and no explicit install script still gets blocked, because npm runs an implicit`node-gyp rebuild`

for it).`prepare`

scripts from git, file, and link dependencies are blocked the same way. To see what would be blocked, run`npm approve-scripts --allow-scripts-pending`

. Then allow the packages you trust with`npm approve-scripts`

and block the rest with`npm deny-scripts`

. The resulting allowlist is written to`package.json`

and should be committed. If your install routine runs scripts, you can observe warnings in npm 11.16.0+.-
`--allow-git`

defaults to`none`

:`npm install`

will no longer resolve Git dependencies (direct or transitive) unless explicitly allowed via`--allow-git`

. This closes a code-execution path where a Git dependency’s`.npmrc`

could override the Git executable, even with`--ignore-scripts`

. This change was previously announced on 2026-02-18 and is available in npm 11.10.0+. -
`--allow-remote`

defaults to`none`

:`npm install`

will no longer resolve dependencies from remote URLs, such as https tarballs (direct or transitive), unless explicitly allowed via`--allow-remote`

. This flag is available in npm 11.15.0+. The related`--allow-file`

and`--allow-directory`

flags are not changing their defaults in v12.

Upgrade to npm 11.16.0 or later, run your normal install, and review the warnings. Use `npm approve-scripts --allow-scripts-pending`

to see which packages have scripts, approve the ones you trust, and commit the updated `package.json`

. After that, only the scripts you approved keep running once you upgrade. Anything you leave unapproved will stop. More details are available in our docs at `npm approve-scripts`

, `npm deny-scripts`

, and `allow-scripts`

config (for `npx`

and global installs). Please share your comments and questions in our community discussion.