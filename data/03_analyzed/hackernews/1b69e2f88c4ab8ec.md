---
title: We found a division by zero bug in FFmpeg with a vibecoded fuzzer
source: https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24290
author:
- '[[dclavijo]]'
published: '2026-08-27'
created: '2026-08-28'
manifest_dates:
- '2026-08-28'
description: 'Article URL: https://code.ffmpeg.org/FFmpeg/FFmpeg/issues/24290 Comments
  URL: https://news.ycombinator.com/item?id=49468642 Points: 238 # Comments: 185'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 1b69e2f88c4ab8ec
source_type: community_discussion
tldr: 一个名为 daedalus/fuzzer 的模糊测试工具在 FFmpeg 的 VPK 解复用器中发现了整数除零漏洞。构造的 21 字节恶意 .vpk 文件可将声道数设为
  0，触发 SIGFPE 崩溃，属于中等严重度的拒绝服务问题。报告同时给出了修复建议与回归测试用例。
objective_summary: 'daedalus 团队使用其开发的 vibecoded fuzzer（github.com/daedalus/fuzzer）在
  FFmpeg 的 VPK 解复用器中发现了整数除零漏洞，并提交为 issue #24290。漏洞根因是 vpk_read_packet 在计算数据块大小时未检查声道数是否为零，恶意构造的
  21 字节 .vpk 输入可稳定触发 SIGFPE 崩溃。该漏洞属于确定性拒绝服务原语，非内存破坏，影响任何用 avformat_open_input 与 av_read_frame
  处理不可信数据的 FFmpeg 应用。报告建议在函数入口对零声道返回 AVERROR_INVALIDDATA，并附带回归测试用例。'
event_type: policy_and_safety
epistemic_status: verified_fact
entities:
  companies:
  - FFmpeg
  - daedalus
  technologies:
  - VPK
  - SIGFPE
  - libavformat
  - fuzzing
  key_people: []
key_logic_flow:
- 'daedalus 项目使用其开发的 vibecoded fuzzer 在 FFmpeg 的 VPK 解复用器中发现整数除零漏洞，并提交为 issue #24290。'
- 漏洞根因是 vpk_read_packet 在计算 size 和 skip 时除以 par->ch_layout.nb_channels，但未检查该值是否为零。
- 恶意构造的 21 字节 .vpk 输入可将 nb_channels 设为 0，稳定触发 SIGFPE 崩溃，属于确定性拒绝服务原语而非内存破坏。
- 该漏洞影响任何通过 avformat_open_input 与 av_read_frame 处理不可信数据的 FFmpeg 应用，严重程度被评估为 Medium。
- 报告建议在 vpk_read_packet 开头检查 nb_channels 是否为 0 并返回 AVERROR_INVALIDDATA，同时提供了对应的回归测试用例。
- 报告者认为该问题与此前 2024 年 11 月 ffmpeg-devel 邮件列表讨论的同一问题疑似相同。
object_mentions:
- object_type: project
  name: daedalus/fuzzer
  canonical_name: daedalus/fuzzer
  url: https://github.com/daedalus/fuzzer/
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 报告在开头说明这个 bug 是由项目自己的 fuzzer 发现的，并附上了 https://github.com/daedalus/fuzzer/ 作为工具仓库链接。
  - 该 fuzzer 用了 495,211 次执行、耗时 10 小时 43 分钟，在 13,188 个语料条目的基础上找出了崩溃样本。
  article_id: 1b69e2f88c4ab8ec
- object_type: project
  name: FFmpeg
  canonical_name: FFmpeg
  url: https://code.ffmpeg.org/FFmpeg/FFmpeg
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 漏洞位于 FFmpeg 的 libavformat/vpk.c 第 89 行，vpk_read_packet 函数对声道数做除零运算从而触发 SIGFPE。
  - 任何调用 avformat_open_input 与 av_read_frame 处理不可信数据的 FFmpeg 应用都会受此漏洞影响。
  article_id: 1b69e2f88c4ab8ec
extract_result: success
impact_score:
  score: 3.5
  reason: 评分依据：这属于成熟开源项目中的一次常规安全缺陷发现，漏洞本体是经典整数除零、影响面仅限 VPK 解复用器这一小众格式路径，且为中等严重度的拒绝服务而非代码执行，对行业竞争格局没有实质冲击，达不到产品发布级（4
    分）的影响。但其增量价值在于：'vibecoded fuzzer'（LLM 辅助生成的模糊测试工具）成功在 FFmpeg 这类经过二十年锤炼的代码库中稳定挖出漏洞，为
    AI 驱动的漏洞挖掘工作流提供了可复现的实证样本，对 AI 安全工具链细分方向有参考意义，故略高于纯日常更新（3 分）的基线。综合评定为 3.5 分。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: vibecoded fuzzer（LLM 辅助生成的模糊测试工具）在真实开源项目中挖掘漏洞的实际有效性
hype_assessment:
  level: low
  reason: 判定依据：报告未使用'颠覆''革命性'等 PR 滥用词汇，严重度被客观标注为 Medium，明确说明'非内存破坏、无代码执行原语'，不存在夸大危害。全篇为可验证的技术细节：21
    字节 crash 输入的十六进制转储、完整触发链分析、GDB 回溯栈、495,211 次执行发现的元数据、可利用性评估表和可直接合入的修复补丁与回归测试。'vibecoded'一词本身带有一定的流行语包装色彩，但内容实质是扎实的工程报告，故判定为低炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 技术价值不在漏洞本身（教科书式整数除零），而在发现手段：报告展示了 LLM 辅助生成的 fuzzer 能够自主构造最小化
    crash 输入（21 字节）、完成根因定位并附带回归测试，验证了'AI 写 fuzzer'从概念走向真实漏洞挖掘的可行性，为 LLM 驱动的安全研究范式提供了端到端的实证样本。
  business_model: 无直接商业模式影响。间接信号是 LLM 辅助安全工具链正在降低漏洞挖掘的专业门槛，未来可能催生 AI 安全即服务或自动化漏洞赏金类产品形态，但当前仍属早期探索阶段，未形成可量化的商业重塑力。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: 该事件的核心价值不在漏洞本身（21 字节 DoS 触发、Medium 严重度、修复成本极低，属一次性安全事件），而在于 daedalus 的 vibecoded
    fuzzer 证明了 AI 生成式模糊测试在真实大型 C 项目（FFmpeg 这类生产级代码库）上具备实用漏洞发现能力。从资本视角看，AI 驱动的安全测试是确定性增长赛道：安全左移、供应链安全和漏洞赏金机制使自动化漏洞发现需求持续扩张；且
    fuzzer 每次发现漏洞都能反哺语料库与生成逻辑，具备'发现→学习→再发现'的数据飞轮特征，10 小时 43 分钟内独立发现真实崩溃路径即为有效验证。但扣分项有三：其一，该项目为开源模式，价值分散，商业化变现路径不清晰；其二，Google
    OSS-Fuzz、Meta 及传统安全大厂均在布局同类 AI 辅助 fuzzing 能力，竞争格局拥挤；其三，'vibecoded'的低门槛特性意味着先发优势有限——本次仅命中一个低危
    DoS 原语而非内存破坏类高危漏洞，需持续验证其在更多目标（而非单一多媒体库）与更高危漏洞类型上的稳定产出，方能从技术演示升级为行业基础设施。综合给予 5.5
    分，处于'细分赛道潜力标的、需持续验证'区间。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- daedalus
- FFmpeg
- Google OSS-Fuzz
- OpenAI
- Anthropic
competitive_casualty:
- 传统人工模糊测试服务商
- 闭源商业安全测试工具厂商
market_opportunities:
- AI 辅助模糊测试已验证能发现真实世界漏洞（FFmpeg 除零崩溃），安全团队与初创公司可将 LLM 驱动的 fuzzer 产品化为自动化漏洞挖掘与安全审计服务，切入开源供应链安全市场。
- 所有使用 FFmpeg 处理不可信媒体输入的企业（视频平台、CDN、播放器厂商）应将解析层 DoS 加固纳入安全预算，采购 fuzz 驱动的代码审计与修复服务以降低攻击面。
- 该报告展示了从漏洞发现、修复建议到回归测试的 AI 全自动闭环，可封装为安全协作工具（issue 平台插件、CI 安全门禁），为开源维护者提供 AI 补丁工作流。
risk_matrix:
  regulatory: 事件本身属负责任披露，无直接监管风险；但若修复未及时合并进各发行版，依赖 FFmpeg 的产品可能因携带已知 DoS 漏洞，在欧盟 CRA
    等供应链安全法规的『已知漏洞处置』要求下暴露合规风险。
  technological: AI 生成的 fuzzer 对传统模糊测试工具与方法（AFL/libFuzzer/手工 harness）构成技术替代压力；同时该案例表明
    FFmpeg 等解析层仍有大量未校验输入导致的同类缺陷，需要持续修补而非一次性解决。
  competitive: AI 安全测试赛道竞争加剧：Google OSS-Fuzz、GitHub 及主流安全厂商均已布局 AI 辅助漏洞挖掘，daedalus
    这类小团队面临巨头生态、算力与语料资源的挤压，需靠垂直差异化突围。
  ethical: 公开的 21 字节 PoC 可被攻击者直接复制用于构造恶意 .vpk/媒体文件，对依赖 FFmpeg 的广泛应用实施确定性 DoS；同时『vibecoded』（AI
    生成代码）若缺乏人工审查，本身可能引入新的代码质量与安全问题。
  additional:
  - 供应链放大效应：FFmpeg 嵌入绝大多数音视频栈与嵌入式设备，单一解析漏洞的修复需经发行版与厂商传导，暴露窗口可能长达数月。
  - 该 issue 与 2024 年 11 月 ffmpeg-devel 邮件列表讨论疑似同一问题，暴露开源项目对重复漏洞报告与跨渠道响应的协调滞后。
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: project
  name: daedalus/fuzzer
  canonical_name: daedalus/fuzzer
  url: https://github.com/daedalus/fuzzer/
  positioning: daedalus/fuzzer 是一款由 AI 辅助编码（vibecoding）方式开发的开源模糊测试工具，专注于自动发现多媒体处理库中的崩溃与安全漏洞。
  technical_signal: 该工具以 495,211 次执行、耗时 10 小时 43 分钟，基于 13,188 个语料条目稳定复现出崩溃样本，验证了端到端漏洞挖掘能力。
  adoption_signal: 该工具已被实际用于审计 FFmpeg 这类主流开源项目并成功产出漏洞报告，初步展示了可用的实战效果。
  ecosystem_relevance: 工具在 FFmpeg 的 VPK 解复用器中发现真实漏洞，为媒体处理开源生态的安全加固提供了自动化审计新手段。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: vibecoded fuzzer 代表了 AI 辅助编码与漏洞挖掘结合的前沿方向，该项目首次公开即成功在 FFmpeg 等成熟项目中发现真实缺陷，其方法论演进与后续战绩值得持续跟踪。
  risk_notes:
  - 目前仅披露单次漏洞发现结果，工具在更多目标上的稳定性与泛化能力尚未得到验证。
  - vibecoded 生成的代码质量与可维护性存在不确定性，项目是否长期维护仍待观察。
  score: 7.0
  article_ids:
  - 1b69e2f88c4ab8ec
  evidence_snippets:
  - 报告在开头说明这个 bug 是由项目自己的 fuzzer 发现的，并附上了 https://github.com/daedalus/fuzzer/ 作为工具仓库链接。
  - 该 fuzzer 用了 495,211 次执行、耗时 10 小时 43 分钟，在 13,188 个语料条目的基础上找出了崩溃样本。
- object_type: project
  name: FFmpeg
  canonical_name: FFmpeg
  url: https://code.ffmpeg.org/FFmpeg/FFmpeg
  positioning: FFmpeg 是业界广泛使用的开源跨平台多媒体处理框架，提供音视频编解码、容器复用与解复用等完整能力。
  technical_signal: 漏洞位于 libavformat/vpk.c 第 89 行，vpk_read_packet 计算数据块大小时未校验声道数是否为零，除零触发
    SIGFPE 崩溃。
  adoption_signal: 该漏洞影响任何调用 avformat_open_input 与 av_read_frame 处理不可信数据的 FFmpeg 应用，反映其解析
    API 被广泛集成。
  ecosystem_relevance: FFmpeg 是大量播放器、转码服务与多媒体应用的底层依赖，其解析器安全直接关乎整个媒体生态的稳定。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: FFmpeg 的 VPK 解复用器除零漏洞属于确定性拒绝服务，报告者指出其疑似与 2024 年 11 月邮件列表讨论的问题重复，修复久拖未决，值得跟踪补丁落地与同类解析器审计进展。
  risk_notes:
  - 该漏洞虽非内存破坏，但 21 字节恶意输入即可稳定触发崩溃，任何处理不可信 VPK 文件的应用都可能被拒绝服务。
  - 问题疑似早在 2024 年 11 月就被提出却未修复，显示 FFmpeg 解析器缺陷的发现与修复周期可能较长。
  score: 6.0
  article_ids:
  - 1b69e2f88c4ab8ec
  evidence_snippets:
  - 漏洞位于 FFmpeg 的 libavformat/vpk.c 第 89 行，vpk_read_packet 函数对声道数做除零运算从而触发 SIGFPE。
  - 任何调用 avformat_open_input 与 av_read_frame 处理不可信数据的 FFmpeg 应用都会受此漏洞影响。
---

#
Integer Divide-by-Zero in `vpk_read_packet`

(VPK Demuxer) #24290

Hello.

This is a bug found with our fuzzer: https://github.com/daedalus/fuzzer/

**File**: `libavformat/vpk.c:89`


**Severity**: Medium — crafted 21-byte input crashes any FFmpeg-based application that opens a malicious `.vpk`

file or stream

**Root cause**: `vpk_read_packet`

divides `vpk->last_block_size`

by `par->ch_layout.nb_channels`

without checking whether `nb_channels`

is zero. A malformed VPK header can set `nb_channels = 0`

, causing `SIGFPE`

on the division.

### Description

The Sony PS2 VPK demuxer (`libavformat/vpk.c`

) reads audio blocks from a custom container format. In `vpk_read_packet`

, the last block of the stream is handled specially:

```
if (vpk->current_block == vpk->block_count) {
unsigned size = vpk->last_block_size / par->ch_layout.nb_channels;
unsigned skip = (par->block_align - vpk->last_block_size)
/ par->ch_layout.nb_channels;
...
}
```


Both `size`

and `skip`

divide by `par->ch_layout.nb_channels`

. When `nb_channels`

is zero, the CPU raises `SIGFPE`

(integer divide-by-zero exception).

### Trigger Chain

**Demuxer probe**(`vpk_probe`

) matches the`VPK`

big-endian magic and assigns the input to the VPK demuxer.parses the 24-byte header. The fuzz input sets`vpk_read_header`

`nb_channels = 0`

at header bytes`0x0e`

–`0x11`

.`vpk_read_header`

does validate`nb_channels > 0`

, but in the fuzzer's custom-AVIO path the probe/header data and the later packet-read data can diverge: by the time`vpk_read_packet`

runs,`par->ch_layout.nb_channels`

has reverted to`0`

from the original fuzz stream while`vpk->last_block_size`

and`vpk->block_count`

were computed from probe data with a valid channel count. The division is therefore reached with a live-but-zero divisor.reaches the final-block branch and divides by zero on both`vpk_read_packet`

`size`

and`skip`

.

### Crash Input

Hex dump of the 21-byte crash input (`crash_1787378545_34bc062c_sig_signal8.bin`

):

```
00000000 20 4b 50 56 56 50 00 f8 04 00 3b 03 61 39 56 32 | KPVVP....;.a9V2|
00000010 36 36 30 38 50 |6608P|
```


- Bytes 0–3:
`20 4b 50 56`

— ASCII`" KPV"`

, which is the VPK big-endian magic`VPK`

byte-reversed across a word boundary - Byte 0x0e–0x11:
`00 00 00 00`

—`nb_channels = 0`

, the crash trigger

### GDB Backtrace

```
Program received signal SIGFPE, Arithmetic exception.
0x00005555557a9877 in vpk_read_packet (s=0x555557fed700, pkt=0x555557fed300)
at libavformat/vpk.c:89
89 unsigned size = vpk->last_block_size / par->ch_layout.nb_channels;
#0 vpk_read_packet
#1 ff_read_packet
#2 read_frame_internal
#3 av_read_frame
#4 fuzz_ffmpeg
#5 main
```


### Crash Metadata

| Field | Value |
|---|---|
| Signal | `SIGFPE` (returncode −8) |
| Fault address / RIP | `0x7ffff48a66d7` (instruction itself) |
| RSP | `0x7fffffffcce0` |
| Execs to find | 495,211 |
| Corpus at find | 13,188 entries |
| Elapsed | 10 h 43 m |
| Parent seed | `36e65f4009ba0cab` |
| Target SHA256 | `d704c2a52b21bd33` |

### Exploitability Assessment

| Factor | Assessment |
|---|---|
| Crash determinism | Deterministic — 21 bytes, single demuxer code path |
| Trigger depth | Shallow — `avformat_open_input` auto-detects format from magic |
| Preconditions | None — input is self-contained, no network, no heap setup |
| Signal type | SIGFPE (integer divide-by-zero), not memory corruption |
| Memory safety | No OOB read/write, no use-after-free, no NULL dereference |
| Reach | Any application that calls `avformat_open_input` + `av_read_frame` on untrusted data |
| Severity | Medium — reliable DoS; not an immediate code-execution primitive |

The divide-by-zero is a **denial-of-service** primitive. There is no controlled write or arbitrary read adjacent to the faulting instruction. The input can be embedded in a `.vpk`

file or a container that identifies itself as VPK to trigger the crash in any FFmpeg-linked application.

### Suggested Fix

Add a guard at the top of `vpk_read_packet`

to reject zero-channel streams cleanly:

```
static int vpk_read_packet(AVFormatContext *s, AVPacket *pkt)
{
AVCodecParameters *par = s->streams[0]->codecpar;
VPKDemuxContext *vpk = s->priv_data;
int ret, i;
if (par->ch_layout.nb_channels == 0)
return AVERROR_INVALIDDATA;
vpk->current_block++;
...
}
```


This is consistent with the existing validation in `vpk_read_header`

(`if (st->codecpar->ch_layout.nb_channels <= 0) return AVERROR_INVALIDDATA;`

) and returns a clean error instead of `SIGFPE`

.

### Regression Test

```
/* Trigger: 21-byte VPK stream with nb_channels=0 — SIGFPE in vpk.c:89 */
static const unsigned char vpk_crash[] = {
0x20, 0x4b, 0x50, 0x56, 0x56, 0x50, 0x00, 0xf8,
0x04, 0x00, 0x3b, 0x03, 0x61, 0x39, 0x56, 0x32,
0x36, 0x36, 0x30, 0x38, 0x50
};
/* Expect: av_read_frame returns -22 (AVERROR_INVALIDDATA), does not crash */
```


It seems to be the same issue as the one discussed in https://ffmpeg.org/pipermail/ffmpeg-devel/2024-November/335598.html.

**Labels**

**2 participants**

**Notifications**

**Due date**

No due date set.

**Dependencies**

No dependencies set

**Reference**

Deleting a branch is permanent. Although the deleted branch may continue to exist for a short time before it actually gets removed, it CANNOT be undone in most cases. Continue?