---
title: 'Show HN: HelixDB – A graph database built on object storage'
source: https://github.com/HelixDB/helix-db/tree/main
author:
- '[[GeorgeCurtis]]'
published: '2026-06-10'
created: '2026-06-11'
description: 'Hey HN, it’s been just over a year since we launched HelixDB (https://news.ycombinator.com/item?id=43975423),
  a project a friend and I started in college. It’s an OLTP graph database built on
  object-storage, with native vector search and full-text search (FTS).Why graph,
  vector and FTS? Graph databases provide a natural cognitive model for data, vectors
  allow for a semantic understanding of the entities and relationships in the graph,
  and FTS provides more specific filtering. Many AI-driven applications attempt to
  combine all of these functionalities by stitching together multiple disconnected
  systems, but even then there’s no native way to perform joins or queries that span
  all systems. You still need to handle this logic at the application level.Helix
  started as a graph DB, but we moved to a hybrid graph/vector approach after attempting
  to build an AI memory system, which led us down the GraphRAG and HybridRAG rabbit
  hole, where we would need separate graph and vector databases.We knew scalability
  would be a challenge at each stage of our product''s development, however our initial
  focus this past year was to prove out the product through local deployments and
  was only meant to be run on a single node. Scaling graph DBs remained a difficult
  and expensive problem we’d have to solve later. Some common ways other graph DBs
  solve scaling is by duplicating entire datasets across distributed machines (extremely
  expensive per node), or by sharding the data.Sharding databases is effective and
  affordable, however, graph data doesn’t have explicit partitions like relational
  databases do. For example, sharding a relational DB involves splitting up tables.
  When it comes to graph DBs, the edges can span across any of the partitions, and
  hopping across multiple machines when traversing nodes is ineffective and computationally
  expensive.Replicating graph DBs for high availability and better throughput drastically
  increases the operational cost of the db and still has a limit of how big you can
  vertically scale. The workload that we’re used for requires storing a huge amount
  of data for agents, where only a subset of that data is ever needed at any one time.
  So rather than having the whole thing in memory, we can store it all in object-storage
  and get the bits we need when they’re needed.Agents benefit from better context,
  which is achieved from more and better data (more relationships etc). By using S3
  as the persistence/data layer there is no limit to how big the graph can be or how
  many relationships you can have, and we can scale to serve throughput and requests
  by horizontally spinning up nodes and caching relevant subsets of the graph on each
  node. This way, you get extremely low latency for “hot” data and a p99 of ~100ms
  for writes and ~50ms for reads from cold storage (S3). Plus you get the benefit
  of dirt cheap storage.Workloads that HelixDB is currently supporting: - Huge amounts
  of data (TBs) from which the agents need to search and traverse over - Offering
  affordable graph storage for companies where cost of graph data is a bottleneck
  - Consolidating multiple databases, enabling AI agents to have autonomy over companies,
  helping them become more autonomous. - AI memory - Company brainsWe’re currently
  working on our own generalised AI memory layer which will use HelixDB under the
  hood and be completely open-source. Also, we’re finishing up on pre-filtering for
  vector search which will allow you to pre-filter based on relationships in the graph,
  metadata, and sub-graphs. And lastly, GA cloud will be available in the coming weeks.If
  you want to run Helix locally (either on-disk or in-memory), you can find more info
  on our github (https://github.com/HelixDB/helix-db) or via our docs (https://docs.helix-db.com/database/local-development).
  If you’re interested in getting started with our distributed cloud, please email
  us founders@helix-db.com.Many thanks! Comments and feedback welcome! Comments URL:
  https://news.ycombinator.com/item?id=48478148 Points: 122 # Comments: 36'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 6b280ad3df67c570
source_type: community_discussion
tldr: HelixDB 是一款基于对象存储的图数据库，融合图、向量、KV、文档和关系多种数据模型，面向 AI 应用提供统一平台。
objective_summary: HelixDB 团队发布了一款以图+向量为核心的多模型数据库，支持通过 CLI、Rust DSL 和 TypeScript SDK
  进行查询和操作，本地开发模式使用容器化实例，云端版本基于对象存储并提供 ACID 事务与高可用能力。
event_type: application_landing
epistemic_status: pr_statement
entities:
  companies:
  - HelixDB
  technologies:
  - graph database
  - vector database
  - MCP
  - ACID
  key_people: []
key_logic_flow:
- HelixDB 是一款以图+向量为核心数据模型的数据库，同时支持 KV、文档和关系型数据，旨在为 AI 应用提供统一的数据平台。
- 用户可通过 `helix chef` 一键引导工具完成安装、项目初始化、本地实例启动和数据导入，并可选交由 Claude Code、Codex 或 OpenCode
  等编码 Agent 构建完整应用。
- 本地开发实例默认运行在内存模式，可通过 `--disk` 参数启用持久化存储。
- 查询通过 Rust 或 TypeScript DSL 编写，以动态 JSON AST 格式发送至运行中的实例，无需编译或部署步骤。
- HelixDB Cloud 是基于对象存储的托管服务，提供向量搜索、全文搜索、ACID 事务、单写入器+自动扩缩读取节点和多网关高可用架构。
- HelixDB CLI 的 `helix chef` 命令会自动安装 HelixDB 查询技能和文档相关的 MCP（Model Context Protocol）集成。
impact_score:
  score: 4.5
  reason: HelixDB 在技术架构上有实质性创新——以图+向量为核心数据模型、原生构建在对象存储之上、通过 JSON AST 线格式支持动态 DSL 查询、内置
    MCP 协议集成。这些设计使其在 AI 应用数据层有一定差异化优势。但作为 Show HN 的早期项目，缺乏性能基准测试、生产案例和规模化验证，短期内对 Neo4j、ArangoDB、Pinecone
    等既有格局的冲击有限。评分依据：有技术含量但未经验证，影响力主要局限在数据库和 AI 工程开发者圈子。
sentiment: positive
developer_sentiment:
  tone: skeptical
  primary_focus: 多模型数据库的性能和成熟度能否匹敌专用数据库，以及对象存储架构下的查询延迟表现
hype_assessment:
  level: medium
  reason: 文章使用了'一个平台取代多种专用数据库'的宏大叙事，存在明显 PR 包装成分。虽然项目本身有实质性代码、文档和 SDK 支撑，但缺少与竞品的独立性能对比数据和基准测试。'只需一个平台'的暗示属于典型的营销话术，多模型数据库在历史上（如
    OrientDB、ArangoDB）常因'样样通样样松'受到质疑，HelixDB 尚未正面回应此问题。
information_entropy: high
domain_disruption:
  technical_innovation: 以图+向量为核心数据模型原生构建在对象存储之上，而非传统本地 SSD 架构，为 AI 工作负载提供更具成本弹性的存储底座；Rust/TypeScript
    DSL 查询以 JSON AST 为线格式，无需编译部署即可动态执行，架构设计具有前瞻性；内置 MCP 协议集成使 AI 编码 Agent 可直接与数据库交互，降低了
    AI 应用的数据层开发门槛。
  business_model: 开源核心+托管云服务的经典模式，但通过 MCP 集成和 'helix chef' 一键引导工具瞄准 AI 应用开发者生态，意图以开发者体验和
    AI Agent 兼容性作为差异化竞争点，可能推动数据库产品的 'AI 原生' 设计范式。
engineering_complexity: prototype
compound_value:
  score: 5.5
  reason: HelixDB 的差异化在于两点：一是图+向量多模型融合（减少 AI 应用拼接多套存储的摩擦），二是基于对象存储的架构（理论上 TCO 显著低于传统云数据库）。其
    MCP 原生集成和 'helix chef' 一键引导展示了清晰的 AI Agent 生态卡位意识，如果能在 Agent 工作流的数据持久化场景形成事实标准，将具备很强的迁移成本护城河。但数据库是典型的赢家通吃赛道，Neo4j
    在图数据库有深厚的开发者心智，Pinecone/Qdrant 在向量搜索已有规模验证，PostgreSQL（pgvector）更是以零额外成本占据长尾。HelixDB
    当前处于 Show HN 阶段，尚未证明 PMF（产品-市场匹配），且多模型数据库在工程上极易出现 '样样通样样松' 的问题。长期复利取决于能否在 AI Agent
    数据层场景率先建立开发者生态和网络效应，但验证周期长、竞争激烈，现阶段给予 5.5 分——有细分赛道基础设施的潜力，但需持续跟踪 adoption 和性能基准数据。
value_capture_layer: cloud_platform
moat_impact: democratizes_access
key_beneficiaries:
- HelixDB
- AWS S3
- Claude Code / Codex / OpenCode
competitive_casualty:
- Neo4j
- Pinecone
- Weaviate
- Qdrant
market_opportunities:
- 开发者可基于 HelixDB 的图+向量多模型能力，构建面向 AI Agent 的统一记忆与上下文管理平台，减少在多个数据库之间的数据搬运成本
- HelixDB 的 MCP（Model Context Protocol）原生集成使得编码 Agent（Claude Code、Codex 等）可直接操作数据库，创业者可围绕此能力开发
  AI 原生应用脚手架和垂直行业模板
- 基于对象存储的云架构可能大幅降低图数据库的存储成本，适合需要大规模图分析但预算有限的业务场景，如社交网络分析、供应链溯源等
risk_matrix:
  regulatory: 托管服务涉及数据跨境存储与合规（GDPR、数据本地化法案），若用户将敏感数据存入 HelixDB Cloud，需自行评估云服务商的合规资质
  technological: 图数据库赛道已有 Neo4j、Amazon Neptune、ArangoDB 等成熟竞品；基于对象存储的架构在大规模、低延迟在线查询场景下尚未得到充分验证，存在性能瓶颈风险；项目为早期
    Show HN 阶段，API 和存储引擎可能快速迭代导致向后兼容问题
  competitive: 竞争极为激烈：传统图数据库（Neo4j、TigerGraph）、云厂商托管服务（Neptune、Cosmos DB）、以及 AI 专用向量数据库（Pinecone、Weaviate、Milvus）均构成直接或间接竞争；多模型数据库本身并非新概念（如
    ArangoDB、SurrealDB），HelixDB 差异化尚需时间验证
  ethical: 统一数据平台可能集中存储更多维度的用户数据，一旦发生数据泄露，影响范围和敏感度远超单一用途数据库；AI Agent 直接操作数据库可能引入未预期数据修改风险
  additional:
  - 项目仍处于非常早期的开发阶段（GitHub 公开仓库 + Show HN），社区活跃度、文档完备度和生产环境稳定性均未建立，存在项目停摆或方向变更的风险
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: speculative_watch
---

HelixDB is a database that makes it easy to build all the components needed for AI applications in a single platform.

You don't need a separate application DB, relational DB, vector DB, graph DB, or application layers to manage the multiple storage locations. HelixDB gives your agents federated access to company data, for memory, company brains, and applications.

Helix primarily operates with a graph + vector data model, but it also supports KV, documents, and relational data.

The Helix CLI runs and manages local instances and talks to Helix Cloud.

`curl -sSL "https://install.helix-db.com" | bash`

Already installed? Update to the latest version with `helix update`

.

`helix chef`

is an interactive, one-shot bootstrapper. It installs the HelixDB query skills and docs MCP, scaffolds a project, starts a local instance, seeds some example data, and writes a `HELIX_CHEF_PROMPT.md`

. If a coding agent is available (Claude Code, Codex, or OpenCode), it can hand off and build a working app — frontend and all — from a one-line description of what you want.

`helix chef`

That's it — no flags. Answer "what do you want to build?" and follow the prompts.

If you'd rather wire things up yourself:

**Initialize a project.**This scaffolds`helix.toml`

, a`.helix/`

workspace dir, and a ready-to-run`examples/request.json`

.

```
mkdir my-helix-app && cd my-helix-app
helix init
```

**Start a local instance.**Runs a background container on port`6969`

and waits until it accepts queries.

` helix start dev`


⚠️ The default storage mode isin-memory— stopping the instance wipes its data. Use`helix start dev --disk`

to persist data across restarts, or`--foreground`

to stream logs.

**Send a query.**

` helix query dev --file examples/request.json`

**Stop the instance when you're done.**

` helix stop dev`

Queries are authored with the Rust or TypeScript DSL and sent straight to a running instance as dynamic requests against `POST /v1/query`

— no build or deploy step. Both SDKs produce the same JSON AST. The examples below talk to a local instance on `http://localhost:6969`

(the default `helix start dev`

port). See the Querying Guide for the full builder catalog and the dynamic-query wire format.

Install the crate (published as `helix-db`

, imported as `helix_db`

):

`cargo init && cargo add helix-db tokio sonic-rs`

Define your queries as `#[register]`

functions, then run them directly through the client:

```
use helix_db::Client;
use helix_db::dsl::prelude::*;
#[register]
pub fn add_user(name: String) {
write_batch()
.var_as(
"user",
g().add_n("User", vec![("name", name)])
.value_map(None::<Vec<String>>),
)
.returning(["user"])
}
#[register]
pub fn get_user(name: String) {
read_batch()
.var_as(
"user",
g().n_with_label("User")
.where_(Predicate::eq("name", name))
.value_map(None::<Vec<String>>),
)
.returning(["user"])
}
#[tokio::main]
async fn main() {
let client = Client::new(None).unwrap(); // defaults to http://localhost:6969
// add user
let new_user = client
.query::<sonic_rs::Value>()
.dynamic(add_user("John Doe".to_string()))
.send()
.await
.unwrap();
println!("new user: {:#}", sonic_rs::to_string_pretty(&new_user).unwrap());
// get user
let user = client
.query::<sonic_rs::Value>()
.dynamic(get_user("John Doe".to_string()))
.send()
.await
.unwrap();
println!("user: {:#}", sonic_rs::to_string_pretty(&user).unwrap());
}
```

Install the package (Node.js 20+):

`npm init -y && npm install @helix-db/helix-db`

Define your queries as functions, then `POST`

them to the running instance:

```
import {
Predicate, PropertyInput, PropertyProjection,
defineParams, g, param, readBatch, writeBatch,
} from "@helix-db/helix-db";
const addUserParams = defineParams({ name: param.string() });
function addUser(p = addUserParams) {
return writeBatch()
.varAs("user",
g().addN("User", { name: PropertyInput.param("name") })
.project([PropertyProjection.new("name")]),
)
.returning(["user"]);
}
const getUserParams = defineParams({ name: param.string() });
function getUser(p = getUserParams) {
return readBatch()
.varAs("user",
g().nWithLabel("User")
.where(Predicate.eqParam("name", "name"))
.project([PropertyProjection.new("name")]),
)
.returning(["user"]);
}
const HELIX_URL = "http://localhost:6969/v1/query";
// add user
const newUser = await fetch(HELIX_URL, {
method: "POST",
headers: { "content-type": "application/json" },
body: addUser().toDynamicJson(addUserParams, { name: "John Doe" }),
}).then((r) => r.json());
console.log("new user:", newUser);
// get user
const user = await fetch(HELIX_URL, {
method: "POST",
headers: { "content-type": "application/json" },
body: getUser().toDynamicJson(getUserParams, { name: "John Doe" }),
}).then((r) => r.json());
console.log("user:", user);
```

HelixDB Cloud is an object-storage-backed deployment with integrated vector and full-text search, full ACID transactions, a single writer with auto-scaling reader nodes, and high availability (3+ gateways and DB nodes). Cloud clusters use a separate deploy path from local instances:

```
helix auth login # authenticate
helix workspace switch <workspace> # select workspace + project
helix project switch <project>
helix init cloud --cluster-id <cluster-id> # or: helix add cloud --name production --cluster-id <id>
helix sync production # pull gateway URL + auth contract into helix.toml
helix query production --file examples/request.json
```

HelixDB is available as a distributed, high-availability, managed service. If you're interested in using Helix's managed service, go to our website to get started or contact us to talk with a founder.

Just Use Helix.