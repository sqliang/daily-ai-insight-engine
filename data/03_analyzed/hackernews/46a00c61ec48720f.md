---
title: This is my attempt to get Vulkan going on NetBSD
source: https://github.com/segaboy/vulkan-netbsd
author:
- '[[segaboy81]]'
published: '2026-07-02'
created: '2026-07-03'
description: 'Article URL: https://github.com/segaboy/vulkan-netbsd Comments URL:
  https://news.ycombinator.com/item?id=48765607 Points: 106 # Comments: 30'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 46a00c61ec48720f
manifest_dates:
- '2026-07-03'
source_type: community_discussion
tldr: 将 Vulkan 软件栈（Mesa/Lavapipe）移植到 NetBSD 10.1 并实现自动化构建。
objective_summary: 开发者 segaboy 在 GitHub 发布项目，成功在 NetBSD 10.1 amd64 上配置、编译、链接并安装 Lavapipe
  软件 Vulkan 驱动（libvulkan_lvp.so，约 17 MB），注册为 Vulkan API 1.
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - Vulkan
  - Mesa
  - Lavapipe
  - LLVM
  - glslang
  - NetBSD
  - ICD
  key_people:
  - segaboy
key_logic_flow:
- 该项目目标是将 Vulkan 软件栈（Mesa/Lavapipe）移植到 NetBSD 10.1 amd64 平台，使 NetBSD 成为支持 Vulkan 的
  BSD 系统。
- 项目已实现 Mesa 在 NetBSD 上的配置、编译、链接、安装，以及 Lavapipe 软件 Vulkan 驱动（libvulkan_lvp.so）的 ICD
  注册，驱动约 17 MB，支持 Vulkan API 1.4。
- 整个构建过程已通过多个 Shell 脚本实现端到端自动化，可从全新最小化安装的 NetBSD 系统开始完整重现，且支持断点续建。
- Vulkan 运行时所需的加载器（libvulkan.so.1）尚未构建完成，因此目前尚无法实际运行任何 Vulkan 应用程序。
- GCC 对 Mesa 中 %m 格式说明符的兼容性问题仍通过 -Wno-error=format 绕过，计划改用 strerror(errno) 作为上游可接受的修复方案。
- 项目计划提供预编译二进制发布包，使用户无需数小时从源码编译即可直接下载安装驱动。
extract_result: success
impact_score:
  score: 2.0
  reason: 该事件对AI行业无直接关联。将一个CPU驱动的Vulkan软件渲染器（Lavapipe）移植到极小众操作系统NetBSD上，属于操作系统生态层面的基础工作。NetBSD市场份额极小，且Lavapipe是CPU软件光栅化方案，性能远低于GPU原生Vulkan实现。该事件对AI计算、大模型训练/推理、AI应用开发等核心领域没有可感知的短期影响。给予2分，属于小圈子社区维护范畴。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: NetBSD获得Vulkan软件驱动的可行性验证
hype_assessment:
  level: low
  reason: 文章无任何PR包装或夸张宣传。这是一个开发者自述的项目文档，语言平实，明确标注了尚未完成的部分（loader未就绪、format绕过方案）、范围限定（仅CPU软件渲染、仅在VirtualBox中编译验证），并且使用了'work-in-progress'、'Expect
    churn'等谨慎措辞。无任何'颠覆'、'革命性'等滥用词汇。
information_entropy: high
domain_disruption:
  technical_innovation: 将Mesa/Lavapipe Vulkan软件栈移植到NetBSD 10.1 amd64平台，实现了从环境配置到驱动编译、安装、ICD注册的完整自动化构建流水线（libvulkan_lvp.so约17
    MB，支持Vulkan API 1.4），并设计了断点续建和预编译二进制发布机制。属于工程集成而非架构创新。
  business_model: 无
engineering_complexity: prototype
compound_value:
  score: 2.0
  reason: 该项目是个人开发者将 Vulkan 软件栈(Mesa/Lavapipe)移植到 NetBSD 10.1 的社区工程。从 VC 视角看：(1) NetBSD
    市场份额极小，属于操作系统长尾中的长尾，不具备规模化商业基础；(2) 项目无公司实体、无商业模式、无融资路径，纯个人兴趣驱动；(3) 软件 Vulkan 驱动(Lavapipe)本身性能远不如硬件
    GPU 加速，实用场景狭窄；(4) 即便移植完成，NetBSD 上能运行的 Vulkan 应用生态也极为有限。虽然工程规范性好(自动化脚本、断点续建、计划提供预编译包)，但商业层面的复利效应几乎为零，不具备任何
    VC 级投资价值。
value_capture_layer: hardware_compute
moat_impact: neutral
key_beneficiaries:
- NetBSD 社区
- Mesa 项目
competitive_casualty: []
market_opportunities:
- 对于嵌入式系统和工业自动化领域，NetBSD 的 Vulkan 支持可带来统一的现代图形计算接口，适合对图形需求较低但需要跨平台一致性的场景
- 该项目的自动化构建脚本和断点续建机制可作为其他开源操作系统移植工作的参考模板，吸引希望将 Vulkan 带到小众系统的技术团队
risk_matrix:
  regulatory: 无
  technological: Vulkan 加载器（libvulkan.so.1）尚未构建完成，目前无法实际运行任何 Vulkan 应用程序，距离可用还有较大差距；Lavapipe
    为 CPU 软件渲染，性能远低于硬件加速，在桌面场景吸引力有限
  competitive: NetBSD 是唯一缺乏 Vulkan 支持的主流 BSD 系统，但 BSD 生态在桌面/图形领域本身市场份额极小，该项目的完成不会对主流
    Vulkan 生态（Linux/Windows）产生竞争影响
  ethical: 无
  additional:
  - 个人兴趣项目，开发进度和长期维护存在不确定性，上游合并（Mesa、pkgsrc）尚未完成
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: speculative_watch
---

An effort to bring the **Vulkan** software stack (Mesa / Lavapipe) to
**NetBSD**, and to document and automate the process so it can be reproduced
and maintained.


Milestone reached:Mesa configures, compiles, links,installs, andregistersthe Lavapipe software Vulkan driver on NetBSD 10.1 amd64, against LLVM 19.1.7. The driver (`libvulkan_lvp.so`

, ~17 MB) installs into`/usr/pkg/lib`

, and its ICD manifest (advertising Vulkan API 1.4) installs into`/usr/pkg/share/vulkan/icd.d/`

, so a Vulkan loader on the system can discover it.`ldd`

resolves every dependency cleanly. The entire process — environment setup, dependency builds, the Mesa build, and installation — is automated end to end and reproducible on a fresh install.

Prebuilt binaries are coming.The tooling to build, fingerprint, and publish prebuilt artifacts is in place, and the build scripts already know how to fetch and install a matching prebuilt instead of building from source. Once a build machine is publishing releases, installing the driver will be a download rather than a multi-hour build. That's the near-term direction.

What this is NOT (yet):

Running Vulkan programs needs the loader.This project builds and installs the Vulkandriver(the Lavapipe ICD). Actually executing a Vulkan application also requires the Vulkanloader(`libvulkan.so.1`

), which is the next component to bring up. Runtime execution is therefore not yet verified.One workaround is still in place.The build applies`-Wno-error=format`

to sidestep GCC rejecting Mesa's`%m`

format specifier on NetBSD. A proper upstreamable fix (using`strerror(errno)`

) is pending.Steps, scripts, and documents will continue to change as the loader and the release pipeline come together.


NetBSD is currently the only major BSD without Vulkan support. The aim of this project is to:

- Build the Vulkan software stack (Mesa's Lavapipe driver, which runs on the CPU via LLVM and needs no GPU) on NetBSD.
- Document every step, including the dead ends, so the process is reproducible.
- Automate the setup so a fresh machine can be brought to a build-ready state with a couple of scripts.
- Eventually feed the necessary fixes upstream (to Mesa and to pkgsrc) and provide prebuilt binaries or a pkgsrc package, so that Vulkan on NetBSD becomes something you can simply install rather than build from source.

**Target OS:**NetBSD 10.1 (amd64), minimal ISO install.**Host:**VirtualBox VM.**Build goal only:**This targets*compilation and linkage*of the Vulkan stack. Runtime GPU acceleration is not available under VirtualBox; the software driver (Lavapipe) is the target.

```
vulkan-netbsd/
├── docs/
│ ├── 01-environment-setup.md Base system + pkgsrc + build deps
│ ├── 02-source-dependencies.md Dependencies not in pkgsrc (built from source)
│ ├── 03-mesa-build.md Configure + compile Mesa (Lavapipe); port notes
│ └── 04-prebuilt-artifacts.md Build-once / reuse prebuilt binaries
└── scripts/
├── setup-env.sh Automates the environment setup
├── build-glslang.sh Builds glslang (required by Mesa; not in pkgsrc)
├── build-mesa.sh Clones + configures + compiles Mesa (Lavapipe)
├── install-mesa.sh Installs the built driver + ICD manifest
├── lib-artifacts.sh Shared: fingerprint + prebuilt fetch helpers
└── package-artifacts.sh Packages built binaries into release tarballs
```


On a fresh, minimal NetBSD 10.1 (amd64) install, as root:

```
cd /root
ftp https://raw.githubusercontent.com/segaboy/vulkan-netbsd/main/scripts/setup-env.sh
sh setup-env.sh
. /root/.profile
ftp https://raw.githubusercontent.com/segaboy/vulkan-netbsd/main/scripts/build-glslang.sh
sh build-glslang.sh
ftp https://raw.githubusercontent.com/segaboy/vulkan-netbsd/main/scripts/build-mesa.sh
sh build-mesa.sh --build
ftp https://raw.githubusercontent.com/segaboy/vulkan-netbsd/main/scripts/install-mesa.sh
sh install-mesa.sh
```

`build-mesa.sh --build`

clones, configures, and compiles Mesa, producing the
Lavapipe Vulkan driver (`libvulkan_lvp.so`

). `install-mesa.sh`

then installs
that driver and its ICD manifest into `/usr/pkg`

and verifies the registration.

If a build is interrupted or the machine crashes, re-run the same command — the build scripts detect the existing build and resume automatically.

Together, these scripts take a fresh minimal install all the way to an installed, registered Vulkan software driver.

See `docs/01-environment-setup.md`

for the full, commented walkthrough and
notes on running over SSH.

This is a personal work-in-progress. Expect churn. The documentation is written as a running record of what actually worked (and what didn't), so it doubles as field notes for anyone attempting the same port.

The original work in this repository — the scripts, documentation, and any
patches authored here — is released under the MIT License (see `LICENSE`

).

This project builds, installs, and (optionally) redistributes third-party
software that is **not** covered by that license and retains its own:

**Mesa**— primarily MIT-licensed (some components under other permissive licenses).**glslang**— permissive licenses (BSD-style, Apache 2.0, and Khronos terms).**LLVM**— Apache 2.0 with LLVM Exceptions. LLVM is installed separately via pkgsrc and is not redistributed by this project.

Prebuilt artifacts produced by `scripts/package-artifacts.sh`

bundle the
relevant upstream license texts (under `share/licenses/`

) so that binary
redistribution preserves each project's attribution. If you publish artifacts,
keep those files intact.