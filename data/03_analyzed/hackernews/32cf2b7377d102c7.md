---
title: C extensions, portability, and alternative compilers
source: https://lemon.rip/w/6-c-extensions-compilers/
author:
- '[[xngbuilds]]'
published: '2026-05-25'
created: '2026-05-26'
description: 'Article URL: https://lemon.rip/w/6-c-extensions-compilers/ Comments
  URL: https://news.ycombinator.com/item?id=48267126 Points: 143 # Comments: 50'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 32cf2b7377d102c7
source_type: community_discussion
tldr: 本文通过分析glibc、OpenBSD、bionic等C标准库头文件和Gnulib的兼容性代码，揭示了现实C代码广泛依赖GCC/Clang非标准扩展的现状，以及这对替代编译器开发者造成的兼容性挑战。
objective_summary: 一位正在开发自有C编译器的开发者，以glibc的sys/cdefs.h和limits.h、OpenBSD标准头文件中的__only_inline宏、SDL_endian.h的特性检测逻辑、Android的bionic
  libc以及Gnulib的extern inline兼容代码为例，系统梳理了C语言生态因深度依赖GCC特有扩展（如__attribute__、#include_next、内联汇编和inline语义差异）导致非GCC编译器难以兼容的普遍问题。文章指出，解决这一问题的可行路径是向GCC靠拢、实现其扩展并伪装成GCC版本。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - GNU
  - LLVM
  - Google
  - OpenBSD Foundation
  - SDL
  technologies:
  - glibc
  - GCC
  - clang
  - bionic
  - Gnulib
  - POSIX
  - __attribute__
  - '#include_next'
  key_people:
  - Youtao Guo
key_logic_flow:
- glibc的sys/cdefs.h通过检查__GNUC__、__clang__、__TINYC__等预定义宏来决定是否启用__attribute__扩展，其他编译器即使实现了该扩展也无法通过检查。
- glibc的<limits.h>深度依赖GCC特有的#include_next扩展和GCC内置limits.h来正确定义宏，其他编译器必须绕开这些GCC特定逻辑才能正确工作。
- SDL_endian.h的特性检测逻辑先检查__has_builtin再检查x86_64等ISA宏，若编译器定义了ISA宏但不支持GCC风格扩展内联汇编则会触发编译错误。
- OpenBSD标准头文件中的__only_inline宏采用GCC内联语义，在非GNU编译器上退化为static链接，导致函数定义出现冲突链接错误。
- Android的bionic libc头文件大量依赖clang特有扩展（如_Nonnull），但可以通过命令行宏定义将其消除以保持兼容。
- 替代编译器开发者面对这些问题的可行方案包括向上游打补丁、等待足够流行后被纳入ifdef分支、向下游分发兼容补丁、或伪装成GCC版本并实现其扩展。
extract_result: success
object_mentions:
- object_type: project
  name: glibc
  canonical_name: glibc
  url: https://www.gnu.org/software/libc/
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - glibc的sys/cdefs.h通过检查__GNUC__、__clang__、__TINYC__等宏来决定是否启用__attribute__扩展，非此三者的编译器即使实现了该特性也无法被识别。
  - glibc的<limits.h>深度依赖GCC内置的limits.h和#include_next扩展来正确定义宏常量，clang也不得不处理这种对GCC特定机制的依赖。
  - glibc中的struct epoll_event使用GNU __attribute__((packed))改变了结构体内存布局，若忽略该属性会导致ABI不兼容。
  article_id: 32cf2b7377d102c7
- object_type: project
  name: Gnulib
  canonical_name: Gnulib
  url: https://www.gnu.org/software/gnulib/
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Gnulib的extern-inline.m4包含极其复杂的预处理器条件判断，用于应对不同编译器下inline语义的混乱差异。
  - Gnulib的兼容代码需要同时处理GCC 4.2前后inline语义变化、C99与C++的inline差异以及各BSD系统的特有问题。
  article_id: 32cf2b7377d102c7
- object_type: project
  name: bionic
  canonical_name: Android bionic
  url: https://android.googlesource.com/platform/bionic/
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Android的bionic libc头文件大量使用clang特有的_Nonnull和_Null_unspecified等空值检查扩展，但可以通过命令行宏定义将其消除。
  - bionic是Android系统的C标准库实现，作者在Termux环境下使用aarch64开发时遇到了其头文件的兼容性问题。
  article_id: 32cf2b7377d102c7
- object_type: project
  name: tcc
  canonical_name: Tiny C Compiler
  url: https://bellard.org/tcc/
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - glibc的sys/cdefs.h中通过defined __TINYC__将tcc纳入白名单，使其能够正常使用__attribute__扩展。
  article_id: 32cf2b7377d102c7
- object_type: project
  name: SDL
  canonical_name: SDL
  url: https://www.libsdl.org/
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - SDL_endian.h的字节交换函数特性检测先检查__has_builtin再检查x86_64宏，若编译器定义了ISA宏但不支持GCC风格内联汇编则可能出错。
  article_id: 32cf2b7377d102c7
- object_type: project
  name: OpenBSD
  canonical_name: OpenBSD
  url: https://www.openbsd.org/
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - OpenBSD标准头文件中使用__only_inline宏定义内联函数，该宏依赖GCC内联语义并在非GNU编译器上退化为static链接导致冲突。
  - OpenBSD的__only_inline宏在sys/cdefs.h中使用__attribute__指定gnu89内联语义，但定义了_ANSI_LIBRARY宏后可完全避免使用这些定义。
  article_id: 32cf2b7377d102c7
impact_score:
  score: 3.5
  reason: 该文章深刻揭示了C语言生态对GCC/Clang的深度绑定机制，具有较高的技术洞察价值。然而，其直接涉及的是C编译器基础设施领域，与AI行业的关联性较弱——尽管AI框架的底层依赖C/C++编译链，但该讨论本身不会在短期内改变AI行业的竞争格局或技术路线。评分依据：属于技术社区的高质量讨论，但对AI行业冲击力有限，属于'重要但不紧急'的基础设施话题。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: GCC/Clang双头垄断下替代编译器的生存策略——伪装兼容与标准化的两难
hype_assessment:
  level: low
  reason: 文章为技术博客形式，以具体代码实例（glibc sys/cdefs.h、limits.h、SDL_endian.h等）论证观点，无PR话术、无'颠覆''革命性'等夸大宣传词汇，属于实打实的技术经验总结。作者以第一人称记录编译器开发中的真实踩坑经历，信息可信度高。
information_entropy: high
domain_disruption:
  technical_innovation: 无直接技术创新。文章的价值在于系统性地记录了C语言生态中编译器扩展依赖的具体机制：glibc通过__GNUC__/__clang__/__TINYC__宏白名单控制__attribute__可用性、#include_next扩展在limits.h中的深度绑定、gnu89
    inline语义与C99标准的冲突、以及Android bionic对Clang专有扩展的对称依赖。这些发现构成了理解编译器生态锁定现象的技术地图。
  business_model: 该现象间接影响AI基础设施的商业格局——编译器双头垄断增加了新进入者的合规成本，使得NVIDIA等AI芯片厂商不得不基于LLVM/Clang构建自有编译器工具链（如NVCC依赖LLVM），而非从零开发。对AI
    SaaS生态无直接影响。
engineering_complexity: infrastructure
compound_value:
  score: 3.5
  reason: 该文章揭示的是C编译器生态长期存在的结构性锁定问题，而非新商业机会。GCC/Clang双头垄断通过glibc、OpenBSD、Android bionic等核心库的头文件深度绑定专有扩展（__attribute__、#include_next、gnu89
    inline语义），已形成自我强化的网络效应——更多代码依赖GCC扩展→替代编译器更难进入→更多代码继续依赖。从VC视角看，编译器赛道本身商业化难度极高（LLVM/Clang靠Apple和Google战略性供养才存活），且该洞察并非新发现，而是社区长期共识。长期复利价值有限：3-5年后GCC/Clang双头垄断格局几乎不会因此文改变，替代编译器（tcc、cproc、Zig的C后端）的生存空间不会因此扩大。唯一微弱的正向价值在于推动__has_builtin/__has_attribute等功能检测宏的采用，但这是渐进式改良而非范式转移。
value_capture_layer: foundation_model
moat_impact: strengthens_monopoly
key_beneficiaries:
- GCC/GNU Project
- LLVM/Clang
- Apple
- Google (Android/bionic)
competitive_casualty:
- 独立C编译器项目 (cproc/lacc等)
- TCC (TinyCC)
- Zig (自研C编译目标)
- COSMIC/Green Hills等商业编译器厂商
- 新编译器创业者
market_opportunities:
- 编译器兼容性测试与认证服务：可基于本文揭示的GCC/Clang扩展依赖图谱，构建一套自动化检测工具，扫描代码库中对__attribute__((packed))、#include_next、gnu89
  inline语义等非标准扩展的依赖点，帮助企业评估代码的可移植性风险并出具合规报告
- 替代编译器差异化切入机会：开发者可参考Clang通过定义__GNUC__=4实现渐进兼容的策略，在特定垂直领域（如嵌入式、WASM目标）推出轻量级C编译器，通过实现__has_builtin/__has_attribute功能检测宏来建立差异化优势，而非与GCC/Clang在通用领域正面竞争
- 开源基础设施韧性咨询服务：针对glibc、bionic、OpenBSD等核心库对特定编译器生态的绑定风险，可向大型机构提供供应链多元化评估服务，帮助其识别编译器单点故障风险并制定迁移预案
risk_matrix:
  regulatory: 无
  technological: C生态对GCC/Clang专有扩展的深度绑定已形成事实上的编译器双头垄断，替代编译器即使实现了与GCC等价的功能，仍因预定义宏检测（__GNUC__、__clang__）被系统头文件排斥。glibc、bionic、OpenBSD等关键基础设施的头文件均存在编译器锁定逻辑，导致整个开源C生态的可移植性持续退化。若未来GCC或Clang因许可证变更、地缘政治等因素不可用，替代成本极高
  competitive: Clang通过伪装GCC（定义__GNUC__=4）入场后，已与GCC形成准双头垄断格局，替代编译器的进入壁垒从技术实现升级为生态兼容——需要无限期追赶GCC/Clang的扩展版本迭代，且上游库（glibc、Gnulib、SDL）对新兴编译器缺乏原生支持的激励，形成自我强化的锁定效应
  ethical: C语言标准与工程实践的持续背离导致'标准合规代码'在现实中几乎不存在，这削弱了ISO标准化进程的公信力，并对依赖标准合规性进行安全审计的领域（如高可靠性系统、安全关键软件）构成隐蔽的质量风险
  additional:
  - 供应链单点故障风险：glibc的sys/cdefs.h仅通过预定义宏白名单决定是否启用__attribute__，若GCC因不可抗力停更，整个Linux生态的ABI兼容性将面临系统性崩塌
  - Android生态的编译器分裂风险：bionic深度依赖Clang专有扩展（_Nonnull、_Null_unspecified），而GNU/Linux生态绑定GCC扩展，导致两大移动/桌面生态在编译器层面形成对称锁定，跨平台代码复用成本上升
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: glibc
  canonical_name: glibc
  url: https://www.gnu.org/software/libc/
  positioning: GNU C 标准库，Linux 发行版的事实底层系统接口层，定义了系统调用、POSIX 兼容和运行时 ABI 规范。
  technical_signal: glibc 的 sys/cdefs.h 通过硬编码宏白名单检测编译器，非 GCC/Clang/TCC 即使实现了 __attribute__
    扩展也无法被识别。
  adoption_signal: 作为 GNU/Linux 系统的事实标准 C 库，几乎覆盖所有 Linux 发行版和服务器、桌面、嵌入式环境。
  ecosystem_relevance: 'glibc 深度依赖 GCC 特有的 #include_next、内置 limits.h 和 __attribute__((packed))
    等扩展，形成替代编译器难以绕开的兼容壁垒。'
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: glibc 对非 GCC 编译器的兼容性策略直接影响替代编译器（如自研编译器）的落地可行性，是 C 语言生态碎片化与平台标准化博弈的关键节点，其头文件中的宏检测模式决定了整个
    Linux 生态对新型编译器的接纳程度。
  risk_notes:
  - glibc 中 struct epoll_event 使用 __attribute__((packed)) 改变 ABI 布局，忽略该属性即导致二进制不兼容。
  - glibc 的兼容检测仅对 GCC/Clang/TCC 三家编译器开放白名单，新型编译器面临先有鸡还是先有蛋的生态困境。
  score: 7.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - glibc的sys/cdefs.h通过检查__GNUC__、__clang__、__TINYC__等宏来决定是否启用__attribute__扩展，非此三者的编译器即使实现了该特性也无法被识别。
  - glibc的<limits.h>深度依赖GCC内置的limits.h和#include_next扩展来正确定义宏常量，clang也不得不处理这种对GCC特定机制的依赖。
  - glibc中的struct epoll_event使用GNU __attribute__((packed))改变了结构体内存布局，若忽略该属性会导致ABI不兼容。
- object_type: project
  name: Gnulib
  canonical_name: Gnulib
  url: https://www.gnu.org/software/gnulib/
  positioning: GNU 可移植性工具库，提供跨平台兼容层帮助 C 程序在不同操作系统和编译器间保持行为一致。
  technical_signal: Gnulib 的 extern-inline.m4 包含极其复杂的预处理器条件判断，用于应对不同编译器下 inline 语义的混乱差异。
  adoption_signal: 被 GNU 生态中大量需要跨平台兼容性的开源项目使用，是 C 语言移植性基础设施的重要组成部分。
  ecosystem_relevance: Gnulib 的兼容代码需同时处理 GCC 4.2 前后 inline 语义变化、C99 与 C++ 的 inline
    差异及各 BSD 系统的特有问题，集中反映了编译器扩展兼容性问题的复杂全貌。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Gnulib 集中呈现了 C 语言生态中编译器扩展兼容性问题的系统性能，其兼容解决方案的演进可作为观察 C 标准化进程的缩影，对编译器开发者和开源项目维护者都有参考价值。
  risk_notes:
  - Gnulib 的兼容层本质是打补丁而非根治问题，新增编译器仍需逐一适配。
  - Gnulib 兼容代码日益复杂，维护成本和引入新 bug 的风险持续上升。
  score: 5.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - Gnulib的extern-inline.m4包含极其复杂的预处理器条件判断，用于应对不同编译器下inline语义的混乱差异。
  - Gnulib的兼容代码需要同时处理GCC 4.2前后inline语义变化、C99与C++的inline差异以及各BSD系统的特有问题。
- object_type: project
  name: Android bionic
  canonical_name: Android bionic
  url: https://android.googlesource.com/platform/bionic/
  positioning: Android 系统定制化 C 标准库，为移动和嵌入式环境优化，替代 glibc 成为 Android 生态的底层运行时核心。
  technical_signal: bionic libc 头文件大量使用 clang 特有的 _Nonnull 和 _Null_unspecified 等空值检查扩展，但可通过命令行宏定义将其消除以保持兼容。
  adoption_signal: 作为 Android 系统的默认 C 库，搭载在全球数十亿 Android 设备上，是部署最广泛的 C 标准库实现之一。
  ecosystem_relevance: bionic 对 clang 特有扩展的深度依赖反映了 Android 生态与 clang 编译器的技术绑定，以及 Google
    通过编译工具链锁定生态的战略。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: bionic 作为移动端主导 C 库，其编译器依赖策略体现了大型平台通过编译工具链锁定生态的典型路径，对替代编译器在移动平台的落地构成关键参考。
  risk_notes:
  - bionic 头文件中的 clang 特有扩展需通过宏定义手动消除，对非 clang 编译器构成额外适配负担。
  - Termux 等 Linux-on-Android 项目因 bionic 的 clang 依赖面临持续的兼容性适配挑战。
  score: 4.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - Android的bionic libc头文件大量使用clang特有的_Nonnull和_Null_unspecified等空值检查扩展，但可以通过命令行宏定义将其消除。
  - bionic是Android系统的C标准库实现，作者在Termux环境下使用aarch64开发时遇到了其头文件的兼容性问题。
- object_type: project
  name: Tiny C Compiler
  canonical_name: Tiny C Compiler
  url: https://bellard.org/tcc/
  positioning: 极轻量级 C 编译器，由 Fabrice Bellard 创建，专注于快速编译和最小化代码体积，适用于自举和嵌入式场景。
  technical_signal: glibc 的 sys/cdefs.h 通过 defined __TINYC__ 将 TCC 纳入 __attribute__
    白名单，使其成为唯一被显式认可的非主流编译器。
  adoption_signal: TCC 是极少数能在不修改 glibc 头文件的情况下正常工作的非 GCC 非 Clang 编译器，被用于自举和教学等场景。
  ecosystem_relevance: TCC 被 glibc 白名单采纳表明，替代编译器只要达到一定流行度即可被主流 C 生态纳入兼容范围。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: TCC 是替代编译器争取自举兼容性的成功样本，证明非主流编译器可以逐步被 C 生态接纳，其经验对新型编译器开发者有直接借鉴意义。
  risk_notes:
  - TCC 的功能完整度远不及 GCC/Clang，缺乏现代优化能力和完整的扩展支持。
  - TCC 被纳入白名单更多靠历史积累而非技术优势，新型编译器难以简单复制其路径。
  score: 3.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - glibc的sys/cdefs.h中通过defined __TINYC__将tcc纳入白名单，使其能够正常使用__attribute__扩展。
- object_type: project
  name: SDL
  canonical_name: SDL
  url: https://www.libsdl.org/
  positioning: 跨平台多媒体开发库，为游戏和多媒体应用提供统一的底层硬件访问抽象层。
  technical_signal: SDL_endian.h 的字节交换函数特性检测存在逻辑缺陷：先检查 ISA 宏再检测编译器能力，可能导致不支持 GCC 内联汇编的编译器触发编译错误。
  adoption_signal: 被大量游戏、模拟器和跨平台多媒体应用广泛采用，是 C/C++ 跨平台开发的基础依赖库之一。
  ecosystem_relevance: SDL 的特性检测逻辑典型体现了现实 C 代码在性能优化（使用内联汇编）与编译器可移植性之间难以兼得的普遍困境。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: SDL 的兼容性问题展示了性能优化（内联汇编）与编译器可移植性之间的典型冲突，是 C 生态中此类权衡困境的缩影。
  risk_notes:
  - SDL_endian.h 的检测优先检查 ISA 宏而非编译器能力，导致部分兼容编译器被错误排除。
  - 大量依赖 SDL 的项目可能因底层兼容隐患而影响跨平台构建。
  score: 3.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - SDL_endian.h的字节交换函数特性检测先检查__has_builtin再检查x86_64宏，若编译器定义了ISA宏但不支持GCC风格内联汇编则可能出错。
- object_type: project
  name: OpenBSD
  canonical_name: OpenBSD
  url: https://www.openbsd.org/
  positioning: 以安全性和代码质量为核心理念的类 Unix 操作系统，采用严格的代码审查和默认安全策略。
  technical_signal: OpenBSD 头文件的 __only_inline 宏依赖 GCC 内联语义，在非 GNU 编译器上退化为 static
    链接导致符号冲突错误。
  adoption_signal: 在安全敏感领域（防火墙、VPN、密码学）和系统编程社区拥有忠实用户群，其代码质量深受敬重。
  ecosystem_relevance: OpenBSD 的头文件设计反映了 BSD 生态对 GCC 扩展的隐性依赖，即使是安全优先的系统也难以摆脱对特定编译器的路径依赖。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: OpenBSD 的 inline 宏兼容问题展示了即使是代码质量标杆的操作系统也无法避免对 GCC 扩展的路径依赖，体现了 C 生态兼容性问题的系统性和普遍性。
  risk_notes:
  - OpenBSD 头文件可通过定义 _ANSI_LIBRARY 宏绕开 __only_inline 依赖，但该解决方案缺乏文档说明。
  - OpenBSD 对 GCC 内联语义的依赖使得移植到其他编译器需要额外适配工作量。
  score: 4.0
  article_ids:
  - 32cf2b7377d102c7
  evidence_snippets:
  - OpenBSD标准头文件中使用__only_inline宏定义内联函数，该宏依赖GCC内联语义并在非GNU编译器上退化为static链接导致冲突。
  - OpenBSD的__only_inline宏在sys/cdefs.h中使用__attribute__指定gnu89内联语义，但定义了_ANSI_LIBRARY宏后可完全避免使用这些定义。
---

Anyone who's written C knows that full ISO C standard-adhering code is an impractical rarity. Most real world C code out there relies on non-standard behaviors and language extensions to varying extents, and a lot of this isn't for extra features, but just to work around bugs and gaps in different compilers and libraries. A lot of codebases will try somewhat to support various environments, mostly through the use of preprocessor checks and guards, but these attempts are finicky at best and straight up broken at worst.

I have ran into many of these situations while working on my C compiler, so here's a small list of some of them.

The system's C library headers is the first 'obstacle' for a C compiler
aspiring to be useful. If you can't preprocess and parse `<stdio.h>`

, you
won't get past hello world. Because I use GNU/Linux, that means glibc. Now, to
their credit, glibc *does* try to retain compatibility of its headers on non-GCC
compilers. In the monstrosity that is sys/cdefs.h, which is indirectly
included by every libc header, they use all kinds of preprocessor checks for
compiler-predefined macros to determine what kinds of compiler extensions are
supported, and #define things away when they aren't.

Unfortunately this is just broken sometimes. For example, on Linux `struct epoll_event`

from `sys/epoll.h`

is a packed
struct,
which uses the GNU `__attribute__((packed))`

. Because this changes the struct
layout (on 64 bits), you can't ignore it without breaking ABI. So okay, say you implement
support for `__attribute__((packed))`

in your compiler. But this isn't enough,
because the aforementioned `sys/cdefs.h`

contains this code:

```
/* GCC, clang, and compatible compilers have various useful declarations
that can be made with the '__attribute__' syntax. All of the ways we use
this do fine if they are omitted for compilers that don't understand it. */
#if !(defined __GNUC__ || defined __clang__ || defined __TINYC__)
# define __attribute__(xyz) /* Ignore */
#endif
```


If you aren't gcc, clang, or tcc, tough luck.

Although, the `epoll`

header is Linux-specific, so you could argue
that applying C standards portability criteria is not fair.

Some C headers are supposed to be provided by the compiler because they should
be present even on freestanding implementations, and rely on
compiler-internal definitions. In my computer for example, these live in
`/usr/lib/gcc/x86_64-pc-linux-gnu/16.1.1/include/`

for GCC and
`/usr/lib/clang/22/include/`

for clang. These builtin headers include
`stddef.h`

, `stdint.h`

, `limits.h`

, `float.h`

and more. However, POSIX requires
`limits.h`

to define some POSIX-specific constants in addition to the standard
C constants. So you still need a platform-specific `limits.h`

on top of the compiler's.

glibc's `<limits.h>`

looks like this (abridged):

```
...
/* If we are not using GNU CC we have to define all the symbols ourself.
Otherwise use gcc's definitions (see below). */
#if !defined __GNUC__ || __GNUC__ < 2
/* We only protect from multiple inclusion here, because all the other
#include's protect themselves, and in GCC 2 we may #include_next through
multiple copies of this file before we get to GCC's. */
# ifndef _LIMITS_H
# define _LIMITS_H 1
/* We don't have #include_next. Define ANSI <limits.h> for standard 32-bit words. */
/* These assume 8-bit `char's, 16-bit `short int's, and 32-bit `int's and `long int's. */
# define CHAR_BIT 8
...
# endif /* limits.h */
#endif /* GCC 2. */
#endif /* !_LIBC_LIMITS_H_ */
/* Get the compiler's limits.h, which defines almost all the ISO constants.
We put this #include_next outside the double inclusion check because
it should be possible to include this file more than once and still get
the definitions from gcc's header. */
#if defined __GNUC__ && !defined _GCC_LIMITS_H_
/* `_GCC_LIMITS_H_' is what GCC's file defines. */
# include_next <limits.h>
#endif
/* The <limits.h> files in some gcc versions don't define LLONG_MIN, LLONG_MAX,
and ULLONG_MAX. */
#if defined __USE_ISOC99 && defined __GNUC__
# ifndef LLONG_MIN
# define LLONG_MIN (-LLONG_MAX-1)
# endif
...
#endif
#ifdef __USE_POSIX
/* POSIX adds things to <limits.h>. */
# include <bits/posix1_lim.h>
#endif
...
```


It depends on the gcc-specific builtin `limits.h`

to define some macros to work correctly,
on top of the use of the `#include_next`

extension. Even clang has to work around this silliness.

SDL_endian.h has a goofy bit of feature detection for its byteswapping functions. The purpose is to use compiler builtins or inline assembly whenever possible, and only fall back to portable generic bitwise operations as a last resort. But the way it goes about this is with the following logic:

`__has_builtin(__builtin_bswapX)`

→ use builtins`__x86_64__`

) -> use inline assemblyThis means that if you aren't GCC or clang, but you define the ISA-specific
predefined macro (for good reasons), it will try to use (extended) inline
assembly, even if you have the bswap builtins and provide the `__has_builtin`

special operator. Seems a little odd to expect an unknown compiler to support
GCC-style extended inline assembly.

Some OpenBSD headers include inline function definitions intended
to be used optionally by the compiler when optimizing. These are defined with the
macro `__only_inline`

, e.g.:

```
__only_inline int sigemptyset(sigset_t *__set)
{
*__set = 0;
return (0);
}
```


and are supposed to fallback to the 'real' external symbol when/if the compiler
doesn't actually inline it. In other words, an inline function with extern
linkage. These are, generally, a mess: though they are specified in C99, the
standard behavior conflicts with the pre-C99 non-standard GCC behavior (default
prior to 4.2). In short, the inline definition in the header should use `extern inline`

with the function body, and this will not emit the actual exported
function, and in the translation unit, one should declare the function with
just `inline`

to export its definition there. To further add to the confusion,
the meaning of `inline`

differs between C++ and C. Refer to this good article by
Youtao Guo.

So, OpenBSD relies on GCC's inline semantics and to paper over the GCC version
discrepancy, the `__only_inline`

macro in sys/cdefs.h uses an
explicit `__attribute__`

on newer GCC versions to specify the old gnu89 inline
semantics. But on non-GNU compilers, it gets defined to `static`

linkage,
which breaks because it ends up declaring/defining functions with conflicting linkage.

Luckily, they respect a macro, `_ANSI_LIBRARY`

that when defined entirely omits
the use of these broken `__only_inline`

definitions in standard headers like
`signal.h`

. So you don't get the "optimized versions" (not that it probably
makes a big difference), but at least it works.

I also ran across Gnulib's compatibility code
for `extern inline`

when building Guile and nano, which highlights all the broken
and weird implementations of this corner case of C. See extern-inline.m4 for comments
explaining it but here's an excerpt:

```
#if (((defined __APPLE__ && defined __MACH__) \
|| defined __DragonFly__ || defined __FreeBSD__) \
&& (defined HAVE___HEADER_INLINE \
? (defined __cplusplus && defined __GNUC_STDC_INLINE__ \
&& ! defined __clang__) \
: ((! defined _DONT_USE_CTYPE_INLINE_ \
&& (defined __GNUC__ || defined __cplusplus)) \
|| (defined _FORTIFY_SOURCE && 0 < _FORTIFY_SOURCE \
&& defined __GNUC__ && ! defined __cplusplus))))
# define _GL_EXTERN_INLINE_STDHEADER_BUG
#endif
#if ((__GNUC__ \
? (defined __GNUC_STDC_INLINE__ && __GNUC_STDC_INLINE__ \
&& !defined __PCC__) \
: (199901L <= __STDC_VERSION__ \
&& !defined __HP_cc \
&& !defined __PGI \
&& !(defined __SUNPRO_C && __STDC__))) \
&& !defined _GL_EXTERN_INLINE_STDHEADER_BUG)
# define _GL_INLINE inline
# define _GL_EXTERN_INLINE extern inline
# define _GL_EXTERN_INLINE_IN_USE
#elif (2 < __GNUC__ + (7 <= __GNUC_MINOR__) && !defined __STRICT_ANSI__ \
&& !defined __PCC__ \
&& !defined _GL_EXTERN_INLINE_STDHEADER_BUG)
# if defined __GNUC_GNU_INLINE__ && __GNUC_GNU_INLINE__
/* __gnu_inline__ suppresses a GCC 4.2 diagnostic. */
# define _GL_INLINE extern inline __attribute__ ((__gnu_inline__))
# else
# define _GL_INLINE extern inline
# endif
# define _GL_EXTERN_INLINE extern
# define _GL_EXTERN_INLINE_IN_USE
#else
# define _GL_INLINE _GL_UNUSED static
# define _GL_EXTERN_INLINE _GL_UNUSED static
#endif
```


...okay. How lovely.

bionic is Android's
libc. In an original twist, its headers heavily assume clang instead of gcc.
It's full of clang-specific extensions like `_Nonnull`

,
`_Null_unspecified`

1, for nullability checks, and other things. Luckily
not difficult to `#define`

those away with command-line flags.

Also the only reason I've ran into this is because I've been using my Android phone with Termux as a native aarch64 development environment (lol) and that has bionic headers.

I realize I've mostly ended up writing about libc headers, however I could go on with countless examples but I have exams soon and I have been procrastinating enough.

While it is very annoying to deal with the way many open source projects rely on compiler-specific non-standard extensions and behaviors for non-essential things, it's not fair either to ask of every developer to test their C code with different compilers, including obscure or small ones. C portability is hard enough as is. From the point of view of someone writing a compiler, the possible solutions are:

Try to patch these incompatibilities upstream.

Amass enough popularity to warrant developers adding dedicated #ifdef checks and testing on your compiler by default.

Deal with these "downstream", perhaps distributing your patches.

Pretend to be (some version of) GCC and implement its extensions.

(1) seems like a losing battle, (3) is the easiest, (4) is the realistic
(though arduous) way to support a large number of codebases with minimal
disruption to users of your compiler and developers of said codebases. For
example, clang defines `__GNUC__=4`

(and `__GNUC_MINOR__=2`

,
`__GNUC_PATCHLEVEL__=1`

) to claim compatibility with GCC 4.2.1. Although by
this point clang is at (2), it took significant effort to e.g. get clang to
compile the Linux kernel (needing patches in both projects).

Of course, a problem with (4) is that there are also many codebases that will
check for `#ifdef __GNUC__`

and freely use all kinds of newer GCC extensions if that
macro is defined, without version checks. So you end up playing catch-up, which is
one reason why clang doesn't bump its `__GNUC__`

macros even though it supports
GNU extensions newer than those of 4.2.1 (see this discussion).

Ideally, feature test macros like `__has_builtin`

, `__has_feature`

,
`__has_attribute`

, and even the standard ones like `__STDC_NO_VLA__`

would be more widely used instead of compiler-specific guards and version checks.

For now, the GCC/clang quasi-duopoly is the status quo in *NIX land, for better or worse. Kudos to the developers of smaller independent C compilers: tcc, cproc, scc, vbcc, nwcc, kefir, and more.

*EDIT:* see discussion on lobsters, and more cases by slimcc's developer