---
title: We scaled PgBouncer to 4x throughput
source: https://clickhouse.com/blog/pgbouncer-clickhouse-managed-postgres
author:
- '[[saisrirampur]]'
published: '2026-07-11'
created: '2026-07-12'
description: 'Article URL: https://clickhouse.com/blog/pgbouncer-clickhouse-managed-postgres
  Comments URL: https://news.ycombinator.com/item?id=48872874 Points: 208 # Comments:
  45'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b6751349536eec88
source_type: community_discussion
tldr: ClickHouse 通过多进程 + so_reuseport + peering 将 PgBouncer 吞吐量提升至 4 倍
objective_summary: ClickHouse 在托管 Postgres 服务中，通过部署多个 PgBouncer 进程绑定同一端口（so_reuseport），并实现进程间
  Peering 以解决查询取消问题，将吞吐量从单进程的约 7.7 万 TPS 提升至约 33.6 万 TPS，提升约 4 倍。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - ClickHouse
  - AWS
  technologies:
  - PgBouncer
  - PostgreSQL
  - so_reuseport
  - pgbench
  key_people: []
key_logic_flow:
- PgBouncer 是单线程架构，在 16 vCPU 机器上只能使用一个 CPU 核心，其余核心空闲，导致连接池先于 Postgres 成为瓶颈。
- ClickHouse Managed Postgres 部署了与 CPU 核心数相匹配的 PgBouncer 进程集群，所有进程通过 so_reuseport
  绑定同一端口，由内核将新连接均匀分配。
- 多进程方案面临查询取消问题：取消请求通过新连接到达，可能被分发到不持有该会话的进程。
- 进程间 Peering 机制解决了取消问题：取消请求到达错误进程后会被转发到实际持有该会话的进程，使取消在全集群生效。
- 基准测试显示，256 连接下单进程吞吐量约 7.7 万 TPS（CPU 利用率 ~8%），16 进程集群吞吐量约 33.6 万 TPS（CPU 利用率 ~49%），吞吐量提升约
  4 倍。
- 连接预算（max_client_conn 和 max_db_connections）在集群中均分，确保整体不超过 Postgres 限制。
extract_result: success
impact_score:
  score: 5.5
  reason: PgBouncer 是 PostgreSQL 生态中广泛使用的连接池组件，其单线程架构长期制约高并发场景下的吞吐上限。ClickHouse 通过
    so_reuseport + 多进程 + Peering 机制将吞吐量从约 7.7 万 TPS 提升至约 33.6 万 TPS（4x），同时解决了多进程带来的查询取消一致性难题。该方案虽非理论突破（so_reuseport
    和进程分片为已知技术），但 Peering 机制对查询取消的修复是一项扎实的工程创新，对大规模部署 Postgres 的团队具有直接参考价值。考虑到该方案已成为
    ClickHouse Managed Postgres 的默认配置，对托管 Postgres 市场的竞争格局有一定影响。综合评定为 5.5 分——重要的基础设施优化，但非行业范式转移。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 多进程 PgBouncer 架构配合 Peering 解决连接池单核瓶颈的工程方案
hype_assessment:
  level: low
  reason: 文章提供了完整的基准测试数据（包含低并发下单进程略优的坦诚对比）、CPU 利用率指标（pidstat + CloudWatch 双重验证）、以及清晰的问题-方案-验证逻辑链。没有使用'颠覆式''革命性'等
    PR 词汇，对多进程在低并发下的性能回退也有透明披露。属于实打实的技术分享。
information_entropy: high
domain_disruption:
  technical_innovation: 通过 so_reuseport 实现多 PgBouncer 进程共享同一端口，由内核做连接负载均衡，并首创进程间 Peering
    机制解决多进程下 Postgres 查询取消请求被错误路由的问题，使多进程方案在全功能层面（包括取消操作）与单进程等价。
  business_model: 该方案作为 ClickHouse Managed Postgres 的默认配置，直接提升了其在大规模 Postgres 托管服务中的连接池性能和可靠性竞争力，对
    AWS RDS/Aurora 等竞品形成差异化优势，可能推动托管数据库市场在连接层架构上的跟进与升级。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: PgBouncer 单线程瓶颈是 Postgres 生态中经典痛点，几乎每个生产级 Postgres 部署都受其影响。ClickHouse 的方案（多进程
    + so_reuseport + peering）工程上极为优雅，基准测试数据扎实（16 进程集群达 33.6 万 TPS，提升约 4 倍）。但该方案本质上是配置层面的改进
    + 进程间通信机制的补丁，非新产品、新协议或新商业模式。竞争者可快速跟进：AWS RDS、Google Cloud SQL 等托管 Postgres 厂商只需部署多进程
    + 实现 peering 即可复现。长期复利取决于两个变量：(1) 该模式能否被 PgBouncer 社区吸收为官方推荐的标准部署架构（如 upstream
    合并 peering 机制）；(2) ClickHouse 能否借此在托管 Postgres 市场实现显著的份额增长，将工程优势转化为商业壁垒。目前看来属于「优秀的功能差异化」而非「平台级护城河」，复利潜力中等。
value_capture_layer: cloud_platform
moat_impact: creates_new_moat
key_beneficiaries:
- ClickHouse
- PgBouncer 社区
- PostgreSQL 生态用户
competitive_casualty:
- AWS RDS for PostgreSQL
- Google Cloud SQL for PostgreSQL
- Azure Database for PostgreSQL
- 其他托管 Postgres 服务商
market_opportunities:
- 托管数据库服务商可将多进程 PgBouncer + so_reuseport + Peering 模式作为标准化部署方案，在连接池吞吐量方面形成差异化竞争优势
- AI 应用后端团队可在高并发 Postgres 场景下采用此模式，消除单线程 PgBouncer 导致的连接瓶颈，提升整体服务水位
- 该 Peering 机制的设计思路可推广至其他单线程网络代理服务（如 Redis 侧车代理、MySQL 连接池）的多进程扩展方案，形成通用的基础设施优化模式
risk_matrix:
  regulatory: 无
  technological: PgBouncer 未来版本若原生支持多线程架构，则该定制的多进程 + Peering 方案的技术价值将被稀释，维护成本可能高于收益
  competitive: AWS RDS Proxy、Azure Database 等云厂商可能推出更成熟或零运维的连接池扩展方案，削弱 ClickHouse
    Managed Postgres 在该维度的差异化优势
  ethical: 无
  additional:
  - 多进程集群部署引入了进程间通信和状态同步的复杂性，Peering 机制的故障（如消息丢失、转发延迟）可能导致查询取消行为异常，增加排障难度
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
---

PgBouncer is single-threaded. A single process uses one CPU core, no matter how many the machine has. On a 16-vCPU box that means one core does all the connection pooling while the other fifteen sit idle, and the pooler starts capping throughput long before Postgres runs out of room.

In ClickHouse Managed Postgres we run a fleet of PgBouncer processes, sized proportional to the available cores.

Every process in the fleet binds the same port with `so_reuseport`

enabled. The kernel load-balances incoming connections across the processes, so clients still connect to a single endpoint and never know there is more than one PgBouncer behind it. This is the mechanism PgBouncer's own docs point to for using more than one core: it is single-threaded per process, and `so_reuseport`

is how you put every core to work.


## The catch: query cancellation

A Postgres cancel request arrives on a brand-new connection carrying a cancel key, separate from the connection running the query. With `so_reuseport`

, the kernel is free to hand that new connection to a different process than the one holding the session. The cancel lands on a process that has never heard of the query, and nothing happens.

Peering fixes this. The processes are aware of one another, so a cancel that lands on the wrong process is forwarded to the one that actually owns the session. Cancellation works across the whole fleet, even though any given request can arrive anywhere.

Pooling runs in transaction mode, so a server connection is returned to the pool the moment a transaction commits. And the connection budget is split across the fleet: `max_client_conn`

and `max_db_connections`

are divided by the number of processes, so the fleet as a whole never oversubscribes Postgres.

## Seeing it on real hardware

We ran both configurations on identical AWS EC2 instances: a 16-vCPU `c7i.4xlarge`

for the pooler, a separate box for Postgres, and a third driving load with `pgbench`

in select-only, transaction-pooled mode. One pooler box ran a single PgBouncer process; the other ran a fleet of 16. Same instance type, same Postgres, same workload. The only variable is one process versus sixteen.

We ramped client connections from 8 to 256 and measured throughput and how much of the 16-core box each pooler actually used.


The single process peaks around 87k transactions/sec and then gets *worse* under more load, sliding to 77k at 256 clients as everything contends for one core. The fleet keeps climbing to roughly 336k transactions/sec, about 4x, because it has more cores to climb into.


The single process never gets past about one core of work: under load, `pidstat`

shows the PgBouncer process pinned at ~97% CPU, a full core, while the 16-vCPU box as a whole stays under 10% utilized. The fleet spreads across the machine, reaching roughly 8 cores busy, and it still had headroom when Postgres and the load generator became the limit.


Hold 256 clients steady against each box: the single-process box runs near 9% CPU for the entire run while the fleet holds around 52%. Same instance type, same Postgres, same workload. One configuration leaves the machine idle, the other puts it to work.


EC2's own CloudWatch metric says the same thing from outside the guest: during the load the single-process instance averages about 16% CPUUtilization, the fleet about 60%. CloudWatch reads a little higher than the in-guest number, but the same gap holds: on a box you're paying 16 vCPUs for, a single PgBouncer leaves almost all of it on the floor.

The connection ceiling behaves the same way. A single process enforces `max_client_conn`

on its own, and once you cross it, new clients are turned away:

` FATAL: no more connections allowed (max_client_conn)`


Splitting the budget across the fleet is what lets you raise the aggregate ceiling while keeping each process, and Postgres, within safe limits.

| Clients | Single TPS | Single box CPU | Fleet TPS | Fleet box CPU |
|---|---|---|---|---|
| 8 | 8,910 | 0.8% | 6,450 | 2.9% |
| 32 | 54,203 | 5.2% | 64,244 | 12.3% |
| 64 | 86,570 | 8.3% | 219,439 | 31.9% |
| 128 | 83,463 | 8.1% | 320,547 | 45.9% |
| 256 | 76,893 | 7.7% | 336,469 | 48.9% |

At a handful of connections the single process is actually fine, even a hair faster, since there's nothing to parallelize and the fleet's connections are spread thin. The gap opens exactly where it matters: under real concurrency, where one core becomes the wall.

## The takeaway

A single PgBouncer is a fine default until the pooler, not Postgres, is what caps your throughput. Sizing a fleet to the cores, sharing one port with `so_reuseport`

, and wiring the processes together with peering turns the pooler back into plumbing instead of a bottleneck.

Every ClickHouse Managed Postgres server ships with this setup by default. Provision a Postgres and see it in action.

### Try Postgres managed by ClickHouse

ClickHouse + Postgres has become the unified data stack for applications that scale. With Managed Postgres now available in ClickHouse Cloud, this stack is a day-1 decision.

Sign up