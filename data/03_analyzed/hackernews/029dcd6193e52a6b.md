---
title: Saving 100 terabytes of memory by optimizing 1.1.1.1's DNS cache
source: https://blog.cloudflare.com/dns-cache-memory-optimization-1111/
author:
- '[[TangerineDream]]'
published: '2026-08-27'
created: '2026-08-28'
manifest_dates:
- '2026-08-28'
description: 'Article URL: https://blog.cloudflare.com/dns-cache-memory-optimization-1111/
  Comments URL: https://news.ycombinator.com/item?id=49468083 Points: 740 # Comments:
  221'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 029dcd6193e52a6b
source_type: community_discussion
tldr: Cloudflare 优化了 Big Pineapple 平台（承载 1.1.1.1 等 DNS 服务）的缓存内存布局，五轮改动使每条 DNS 缓存记录的内存占用下降超
  50%，全舰队释放约 100 TB 内存，同时插入吞吐提升 43%、查找延迟下降 19%。
objective_summary: Cloudflare 工程团队在承载 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等服务的
  Big Pineapple 平台上优化 DNS 缓存条目的内存表示，具体措施包括用 Box<[T]> 和 Box<str> 替代 Vec 与 String、用
  u16 偏移合并记录列表、按需丢弃记录 owner、对枚举大尺寸变体装箱并以带长度前缀的线格式存储记录。在任意时刻缓存超过 2500 亿条记录的场景下，这些改动使每条目占用下降超过
  50%，全舰队释放约 100 TB 内存，相当于 130 台 Gen 13 服务器的内存量。性能同步改善，插入吞吐提升 43%，查找延迟下降 19%，并未以速度换取空间。对使用
  EDNS Client Subnet 的数据中心，因条目数量与单条占用都更高，收益尤其明显。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - Cloudflare
  technologies:
  - DNS
  - EDNS Client Subnet (ECS)
  - DNSSEC
  - RFC 1035
  - jemalloc
  - Rust
  key_people: []
key_logic_flow:
- Big Pineapple 平台承载 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等 DNS 服务，任意时刻缓存超过 2500
  亿条记录，每条目多占 1 字节即全舰队浪费 250 GB 内存。
- 团队用 Box<[T]> 和 Box<str> 替代 Vec<T> 和 String，去掉无用的容量字段与预留堆空间，每条目节省 64 字节，合计释放超过 15
  TB 内存。
- 将 answer、authority、additional 三个独立记录列表合并为单一列表并用 u16 偏移定位各段起始，每条目再节省 28 字节。
- 当记录 owner 与查询域名相同时丢弃 owner 字段并在读取时从缓存键推断，仅 CNAME 等 owner 不同的记录保留完整名称。
- 对 NAPTR 等大尺寸枚举变体装箱，使占流量 80% 以上的 A 记录每条节省 120 字节，并最终以带 2 字节长度前缀的原始字节线格式存储记录以改善内存局部性。
- 五轮改动后每条目内存占用下降超 50%，全舰队释放约 100 TB 内存，插入吞吐提升 43%、查找延迟下降 19%。
object_mentions:
- object_type: project
  name: Big Pineapple
  canonical_name: Big Pineapple
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Big Pineapple 是承载 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等 Cloudflare DNS 服务的底层平台，任意时刻缓存超过
    2500 亿条 DNS 记录。
  - 五轮对缓存条目内存存储方式的改动使每条目占用减少超过 50%，全舰队释放约 100 TB 内存，插入吞吐提升 43%。
  article_id: 029dcd6193e52a6b
- object_type: product
  name: 1.1.1.1
  canonical_name: 1.1.1.1
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章标题即围绕优化 1.1.1.1 的 DNS 缓存展开，1.1.1.1 是运行在 Big Pineapple 平台上的 Cloudflare 公共 DNS
    解析服务。
  - 缓存优化使 1.1.1.1 依赖的缓存层插入吞吐提升 43%、查找延迟下降 19%，且未以牺牲速度换取空间。
  article_id: 029dcd6193e52a6b
- object_type: product
  name: Gateway DNS
  canonical_name: Gateway DNS
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章首段说明 Gateway DNS 运行在 Big Pineapple 平台上，与 1.1.1.1、DNS Firewall、AS112 同为其承载的 Cloudflare
    DNS 服务。
  article_id: 029dcd6193e52a6b
- object_type: product
  name: DNS Firewall
  canonical_name: DNS Firewall
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - DNS Firewall 是 Big Pineapple 平台承载的 Cloudflare DNS 服务之一，与 Gateway DNS、AS112 等并列，缓存内存优化惠及全部承载服务。
  article_id: 029dcd6193e52a6b
- object_type: product
  name: AS112
  canonical_name: AS112
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - AS112 是运行在 Big Pineapple 平台上的 Cloudflare DNS 服务，与 1.1.1.1、Gateway DNS、DNS Firewall
    一同列于文章首段。
  article_id: 029dcd6193e52a6b
extract_result: success
impact_score:
  score: 4.5
  reason: 评分依据：这是一篇基础设施工程优化实战复盘，属于 Cloudflare 对自有 DNS 平台（Big Pineapple）的内部改造，技术上扎实（五轮改动、逐字节拆解、基准测试与生产环境双重验证），对大规模缓存/推理系统的工程师有很强启发价值；但它并非
    AI 行业范式级事件，不改变任何局部竞争格局，也未发布新工具或新产品，短期行业冲击力有限。综合判定：4.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: Rust 内存布局优化手法（Box<[T]> 去容量字段、u16 偏移合并列表、大枚举变体装箱、线格式存储）在超大规模缓存中的实战收益
hype_assessment:
  level: low
  reason: 判定依据：全文是工程实操复盘，没有使用'颠覆'、'革命性'等 PR 滥用词汇；每个改动都给出明确的字节节省测算（64B/28B/120B）和性能数据（插入吞吐
    +43%、查找延迟 -19%），100 TB 释放量有生产环境驻留内存验证作为支撑，属于实打实的干货。
information_entropy: high
domain_disruption:
  technical_innovation: 并非全新的算法或架构突破，而是将 Rust 内存布局优化的既有手法系统性应用于超过 2500 亿条 DNS 缓存记录的行星级场景：用
    Box<[T]>/Box<str> 去除无用的容量字段与预留堆空间、三段记录合并为单列表 + u16 偏移、按需丢弃 owner 字段、对大尺寸枚举变体装箱并以带长度前缀的原始字节线格式存储以改善内存局部性。这些手法对
    AI 推理 KV 缓存、嵌入向量缓存、向量数据库的内存压缩有直接迁移价值。
  business_model: 对 Cloudflare 而言，释放约 100 TB 内存相当于省下 130 台 Gen 13 服务器的硬件成本，直接改善其免费
    DNS 服务（1.1.1.1）等边际成本敏感业务的毛利结构，是超大规模运营商成本工程的典范。对 AI 行业而言，为缓存密集型推理服务的单位成本优化提供了可复制的工程范式，但不重塑
    SaaS 生态或商业模式。
engineering_complexity: production_ready
compound_value:
  score: 5.0
  reason: 这是一次对存量基础设施的效率优化，而非打开新市场或新品类。收益边界清晰且可量化：全舰队释放约 100TB 内存（约 130 台服务器），属于一次性资本开支节约与单位经济性改善，对
    Cloudflare 的 DNS 业务是确定性利好。但复利效应有限——没有沉淀出新的平台层、网络效应或数据飞轮，边际价值不会随规模自我放大。真正具备复利属性的，是这家公司在
    2500 亿条记录量级上锤炼出的超大规模缓存工程方法论（Box<[T]> 替代 Vec、u16 偏移合并列表、按需丢弃 owner 等模式可复用于其他内存敏感服务），这部分组织能力有长期价值。综合判断：短期收益确定、长期无法独立构成行业基石，落在
    4-7 分区间中值。
value_capture_layer: cloud_platform
moat_impact: strengthens_monopoly
key_beneficiaries:
- Cloudflare
- Cloudflare 企业客户（Gateway DNS / DNS Firewall）
- Rust 基础设施生态
competitive_casualty:
- 中小型公共 DNS 服务商
- 自建 DNS 基础设施的互联网企业
- 规模效应不足的同类边缘云厂商
market_opportunities:
- 工程团队可将本文的 Rust 内存布局优化经验（Box<[T]>/Box<str> 替代 Vec/String、u16 偏移合并列表、位标志打包、带长度前缀的线格式存储）沉淀为可复用的缓存数据结构组件库，为高并发
  DNS、CDN、边缘缓存类基础设施提供开箱即用的降本方案。
- 云服务商和边缘计算厂商可借鉴文中'每条目字节数 × 条目规模'的内存成本量化方法，建立覆盖全舰队的内存优化路线图，在硬件采购与扩容预算上实现可论证的 ROI（本文即释放相当于
  130 台服务器的内存）。
- 可围绕 Rust 高并发系统的内存剖析与优化咨询（含自定义分配器基准测试、内存局部性调优）提供专业服务，在基础设施成本敏感期，此类'少买硬件即省钱'的工程能力有明确商业变现空间。
risk_matrix:
  regulatory: 无。本次改动仅涉及 DNS 缓存的内存表示与字段压缩，不改变数据保留策略、日志记录或查询处理逻辑，未引入新的合规义务或数据跨境问题。
  technological: 手写优化的内存布局（位标志、u16 偏移、线格式存储）降低代码可读性与可维护性，未来新增记录类型或字段变更时的调整成本上升；u16
    偏移隐含各段记录数不超过 65535 的假设，若 DNS 协议演进产生超大响应需重新设计；jemalloc 或 Rust 标准库分配行为变化也可能部分抵消既有收益。
  competitive: Cloudflare 借此获得显著的硬件成本优势（约 100 TB 内存）并在性能上同步领先（插入吞吐 +43%、查找延迟 -19%），可能加剧公共
    DNS 解析器（Google Public DNS、Quad9 等）及 DNS 安全/过滤服务市场的竞争与价格压力，倒逼竞争对手跟进同等程度的优化。
  ethical: 无。去除记录 owner 并在读取时从缓存键推断仅属内部内存布局变化，不涉及查询数据收集、用户隐私处理或数据投毒风险，也未引入偏见或就业冲击。
  additional:
  - 运维与代码复杂度风险：在 2500 亿条记录规模的分布式系统上，涉及对齐、位域与 unsafe 相关的深度内存优化若引入未定义行为或数据损坏，故障影响面极大，需依赖强化的
    fuzz 测试、canary 发布与灰度回滚机制兜底。
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: Big Pineapple
  canonical_name: Big Pineapple
  url: null
  positioning: Cloudflare 承载 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等 DNS 服务的底层平台，任意时刻缓存超
    2500 亿条记录，是本次内存优化的核心对象。
  technical_signal: 基于 Rust 实现，通过 Box<[T]>、u16 偏移合并列表、丢弃冗余 owner、枚举装箱与线格式存储五轮改动重构缓存布局，单条内存占用下降超
    50%。
  adoption_signal: 优化已全舰队部署落地，释放约 100 TB 内存，相当于 130 台 Gen 13 服务器内存量，生产环境实测验证收益显著。
  ecosystem_relevance: 作为 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等多项 DNS 服务的共同底层平台，其缓存优化直接惠及整个
    Cloudflare DNS 服务生态。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Big Pineapple 以超 2500 亿条缓存的极端规模验证了 Rust 内存布局优化的系统方法论，其 Box 切片、u16
    偏移与线格式存储等手法对高吞吐基础设施项目具有普适借鉴价值，值得持续跟踪其后续演进。
  risk_notes:
  - 基准测试采用模拟流量分布，与真实生产流量存在偏差，实际收益需结合各数据中心场景验证。
  - 内存优化深度依赖 Rust 语言特性，其手法难以直接迁移到其他技术栈，适用面存在局限。
  score: 8.0
  article_ids:
  - 029dcd6193e52a6b
  evidence_snippets:
  - Big Pineapple 是承载 1.1.1.1、Gateway DNS、DNS Firewall、AS112 等 Cloudflare DNS 服务的底层平台，任意时刻缓存超过
    2500 亿条 DNS 记录。
  - 五轮对缓存条目内存存储方式的改动使每条目占用减少超过 50%，全舰队释放约 100 TB 内存，插入吞吐提升 43%。
- object_type: product
  name: 1.1.1.1
  canonical_name: 1.1.1.1
  url: null
  positioning: Cloudflare 推出的公共 DNS 解析服务，运行在 Big Pineapple 平台上，面向全球用户提供免费高速的域名解析能力。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 全球个人互联网用户
  - 依赖公共 DNS 的企业与开发者
  product_signal: 依托缓存内存优化，其底层插入吞吐提升 43%、查找延迟下降 19%，在节省内存的同时改善了端到端解析性能。
  market_signal: 作为全球流量领先的公共 DNS 服务，内存成本的大幅下降有效压缩了 Cloudflare 的基础设施运营开支，增强服务竞争力。
  differentiation: 在同级别公共 DNS 服务中展现出深度的基础设施工程能力，实现更快且更省内存的双重优化，而非简单堆叠资源。
  watch_reason: 1.1.1.1 是衡量 Cloudflare DNS 基础设施优化成效的窗口服务，其缓存性能与成本改善反映了公共 DNS 领域的大规模运维水平，持续跟踪可观察公共解析服务体验与成本的演进趋势。
  risk_notes:
  - 文章未披露优化对长尾或超大响应记录类型的具体影响，特定流量场景下收益可能不及基准。
  - 100 TB 释放量基于超大规模缓存场景，普通规模部署难以复现同等量级收益。
  score: 7.0
  article_ids:
  - 029dcd6193e52a6b
  evidence_snippets:
  - 文章标题即围绕优化 1.1.1.1 的 DNS 缓存展开，1.1.1.1 是运行在 Big Pineapple 平台上的 Cloudflare 公共 DNS
    解析服务。
  - 缓存优化使 1.1.1.1 依赖的缓存层插入吞吐提升 43%、查找延迟下降 19%，且未以牺牲速度换取空间。
- object_type: product
  name: Gateway DNS
  canonical_name: Gateway DNS
  url: null
  positioning: Cloudflare 面向企业提供的安全 DNS 网关服务，运行在 Big Pineapple 平台上，与 1.1.1.1 等共享底层缓存基础设施。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 企业客户
  product_signal: 作为 Big Pineapple 承载的 DNS 服务之一，本文的缓存优化同样惠及其缓存层，获得内存与性能的同步改善。
  market_signal: null
  differentiation: null
  watch_reason: Gateway DNS 与 1.1.1.1 共享 Big Pineapple 缓存基础设施，其缓存层性能可直接受益于本次优化，作为企业级
    DNS 网关产品值得持续观察其安全能力与解析体验的演进。
  risk_notes:
  - 文章对 Gateway DNS 的提及仅为背景介绍，未提供独立性能指标或用户数据，信息维度有限。
  score: 4.0
  article_ids:
  - 029dcd6193e52a6b
  evidence_snippets:
  - 文章首段说明 Gateway DNS 运行在 Big Pineapple 平台上，与 1.1.1.1、DNS Firewall、AS112 同为其承载的 Cloudflare
    DNS 服务。
- object_type: product
  name: DNS Firewall
  canonical_name: DNS Firewall
  url: null
  positioning: Cloudflare 提供的企业级 DNS 防火墙服务，运行在 Big Pineapple 平台上，用于保护企业域名免受 DNS 层攻击。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 企业客户
  product_signal: 作为 Big Pineapple 承载的 DNS 服务之一，缓存内存优化惠及 DNS Firewall，其底层缓存层同样获得内存与性能的改善。
  market_signal: null
  differentiation: null
  watch_reason: DNS Firewall 与 1.1.1.1 共享 Big Pineapple 底层平台，可低成本获得缓存性能提升，作为 Cloudflare
    安全产品组合的一员，值得跟踪其防护能力与解析性能的持续演进。
  risk_notes:
  - 文章仅将其列为承载服务之一，未涉及 DNS Firewall 的独立功能与安全特性，分析依据有限。
  score: 4.0
  article_ids:
  - 029dcd6193e52a6b
  evidence_snippets:
  - DNS Firewall 是 Big Pineapple 平台承载的 Cloudflare DNS 服务之一，与 Gateway DNS、AS112 等并列，缓存内存优化惠及全部承载服务。
- object_type: product
  name: AS112
  canonical_name: AS112
  url: null
  positioning: Cloudflare 参与运营的 AS112 分布式任播服务，承载特殊用途反向 DNS 查询，运行在 Big Pineapple 平台上。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 运营商与网络运维人员
  - 依赖反向 DNS 解析的机构
  product_signal: 作为 Big Pineapple 承载的服务之一，AS112 的缓存层同步受益于内存优化带来的性能改善。
  market_signal: null
  differentiation: null
  watch_reason: AS112 作为承载于 Big Pineapple 的公共服务，其解析性能随缓存优化间接提升，值得关注 Cloudflare 在基础设施层面对特殊
    DNS 场景的持续投入与运营表现。
  risk_notes:
  - 文章对 AS112 的描述仅限背景提及，未提供独立服务数据，无法量化其实际收益。
  score: 3.0
  article_ids:
  - 029dcd6193e52a6b
  evidence_snippets:
  - AS112 是运行在 Big Pineapple 平台上的 Cloudflare DNS 服务，与 1.1.1.1、Gateway DNS、DNS Firewall
    一同列于文章首段。
---

__Big Pineapple__, the platform behind __1.1.1.1__, __Gateway DNS__, __DNS Firewall__, __AS112__, and several other Cloudflare DNS services, stores over 250 billion DNS cache entries at any given time. At that scale, wasting a single byte per entry costs more than 250 gigabytes of memory across our fleet.

Five successive changes to how cache entries are stored in memory cut the per-entry footprint by over 50%. Across our fleet, these changes freed up roughly 100 terabytes of memory, equivalent to the amount of RAM in 130 of our __Gen 13 servers__. The cache also got faster. Insert throughput rose 43% and lookup latency dropped 19%, as fewer allocations and better memory locality meant we did not trade speed for space.

## What we cache

On cold start, Big Pineapple starts out with an empty cache. As DNS queries arrive, the cache fills until it hits its maximum entry count, at which point we evict older or less popular items to make room.

The exact cache size varies by data center. When __EDNS Client Subnet (ECS)__ is in use, authoritative servers return different answers depending on the client's network, so we cache multiple versions of the same query. This increases both the number of entries and the memory each one consumes, making the optimizations in this post especially impactful for ECS-heavy locations.

Each item in the cache is a key-value pair. The key identifies what was queried:

```
pub struct CacheKey {
qname: Name,
qtype: Rtype,
authenticated: bool,
tag: Vec<u8>,
}
```


The value stores the DNS response itself: the answer, authority, and additional record sections, along with metadata like the creation time, a hit counter, and the Time-to-Live (TTL).

```
pub struct CacheEntry {
timestamp: UnixTimeStamp,
pub inception: Instant,
pub ttl: Ttl,
pub hits: u32,
pub answers: Vec<Record>,
pub authority: Vec<Record>,
pub additional: Vec<Record>,
pub errors: Vec<ExtendedError>,
...
}
```


Both structs have room for improvement. Several fields use types that carry overhead we don't need once the entry is stored.

## Benchmarking memory usage

To measure the impact of each change, we benchmark by filling the cache with randomly generated entries that roughly match the traffic distribution we see in production: 56% `A`

records, 25% `AAAA`

, and 19% `TXT`

. Each entry contains between one and four records.

`TXT`

records serve as a stand-in for all non-`A`

/`AAAA`

record types in the benchmark. Their size is randomized between 64 and 224 bytes, close to the average response size we see for variable-length record types.

We track memory usage using a __custom allocator__ that wraps Rust’s `System`

__ allocator__ and records the number and size of allocations per cache entry. Alongside memory, we measure insert throughput and lookup latency across the full cache flow to make sure memory savings don’t come at the cost of performance.

These inputs approximate production rather than reproduce it exactly. Process memory also depends on traffic mix, cache occupancy, allocator state, and memory used outside the cache. We therefore measured resident memory across production instances during the rollout.

## The cost of capacity

`Vec<T>`

stores three fields: a pointer to heap-allocated data, the current length, and the total capacity. When you push an item, `Vec`

checks whether the length exceeds the capacity and reallocates if needed. If there’s room, it just appends the item and increments the length.

Once we store a DNS response in the cache, however, we never modify it again. The capacity field serves no purpose, but still costs 8 bytes per `Vec`

. The over-allocated heap space is wasted as well, as a `Vec`

with capacity for eight items but only five stored leaves three slots unused on the heap.

Using `Box<[T]>`

solves both problems. It can’t grow after creation, so it doesn’t need a capacity field or reserve space for future elements. The same applies to `String`

, which also carries a capacity field. `Box<str>`

drops it.

Each cache entry stores 8 `Vec`

and `String`

fields. Replacing them with `Box<[T]>`

and `Box<str>`

saves 8 bytes per field, 64 bytes per entry. It also eliminates the excess heap memory that `Vec`

reserves for future growth. The combined savings add up to over 15 terabytes with over 250 billion cache entries.

## Fewer lists, fewer pointers

Rather than storing the answer, authority, and additional sections in separate lists, we can store a single list with offsets to the start of each section. Since DNS record counts per section fit in a `u16`

, we can use a `u16`

(2 bytes) for each offset, compared to the 8-byte pointer and 8-byte length that each separate `Box<[T]>`

requires.

This removes two lists, each with an 8-byte pointer and 8-byte length, and replaces them with two 2-byte offsets, saving 28 bytes per entry.

These savings do not always map directly to the number of bytes removed from individual fields. Rust inserts __padding to satisfy alignment requirements__ and rounds a struct’s size up to a multiple of its alignment. Removing a small field can therefore eliminate additional padding. For example, we also packed several boolean fields into a single __bitflag__. This reduced the surrounding padding, causing the struct to shrink by more than the size of the individual booleans.

## Dropping the owner

Each DNS record has an owner, the domain the record belongs to. In many cases, this owner is identical to the domain being queried. For example, a query for `example.com A`

returns two records with the same owner:

```
$ dig example.com A
;; ANSWER SECTION:
example.com. 300 IN A 198.51.100.1
example.com. 300 IN A 198.51.100.2
```


But when a `CNAME`

is involved, for example, the record owner can differ from the queried domain:

```
$ dig example.com A
;; ANSWER SECTION:
example.com. 300 IN CNAME cdn.example.com.
cdn.example.com. 300 IN A 198.51.100.1
cdn.example.com. 300 IN A 198.51.100.2
```


The DNS wire format handles repeated owners using name compression, as defined in __RFC 1035__. Rather than encoding the same domain twice, subsequent occurrences store a 2-byte pointer to the first occurrence. A domain like `www.example.com`

can encode just `www`

followed by a pointer to where `example.com`

already appeared in the message.

This works well on the wire, but in our cache we store the full owner name alongside each record. Following compression pointers during cache lookups is expensive on the hot path, so we trade memory for speed.

Most records, however, have an owner identical to the queried domain. For those, we can drop the owner entirely and infer it at read time. When the owner differs, such as the `A`

records behind a `CNAME`

, we store the full name.

```
pub struct Record {
owner: Option<Box<Name>>,
class: Class,
ttl: Ttl,
rtype: Rtype,
data: RecordData,
}
```


When `owner`

is `None`

, response construction restores the queried domain from the cache key, avoiding a heap allocation. This means the record is no longer self-contained, but the cache key is already available during every lookup. When the owner differs, `Some`

stores a pointer to the full name on the heap.

In practice, most cached records have an owner identical to the queried domain, so the majority require no heap allocation for the owner field.

## Enum sizing

Rust enums are __sum types__: each variant can carry different data, but the enum is always the size of its largest variant.

```
pub enum Option<T> {
Some(T),
None,
}
```


`Option`

is either `Some`

and holds a value, or `None`

and holds nothing. Both variants take the same amount of memory. The enum stores a tag indicating the active variant, followed by space large enough for the largest variant’s data. When the variant is `None`

, that space is unused.

For record data, it seems natural to store each DNS record type as an enum variant:

```
pub enum RecordData {
A(Ipv4Addr),
Aaaa(Ipv6Addr),
Txt(Txt),
Naptr(Naptr),
Svcb(Svcb),
// ...
}
```


But the enum is always as large as its largest variant. In our case, that’s `NAPTR`

at 136 bytes. It stores three variable-length text fields, a domain name, and two integers. As a result, the full enum, including the variant tag and padding, becomes 144 bytes.

An `A`

record only needs 4 bytes, and an `AAAA`

record needs 16 bytes. `A`

and `AAAA`

make up over 80% of our traffic, so most records waste over 120 bytes on padding. Since a single cache entry can store many records this quickly adds up.

### Boxing the variants

To solve this problem, we can box the larger variants of the enum, moving them to a separate heap allocation. The enum then stores an 8-byte pointer to the heap, where the data takes up only the size it actually requires.

```
pub enum RecordData {
// Small and common variants are stored inline
A(Ipv4Addr),
Aaaa(Ipv6Addr),
// Large variants are stored on the heap
Txt(Box<Txt>),
Naptr(Box<Naptr>),
Svcb(Box<Svcb>),
// ...
}
```


For `A`

and `AAAA`

records, this saves 120 bytes per record. Smaller variant types like `TXT`

and `CNAME`

also benefit. They still occupy the 24-byte enum, but their heap allocation is sized to their actual data rather than padded to 144 bytes. `NAPTR`

, the largest variant, actually pays slightly more. It now adds the cost of a heap pointer and allocation overhead. But `NAPTR`

records are rare in practice, so the tradeoff is worth it.

But boxing the larger record variants introduces costs of its own.

### The costs of boxing

Boxing has two costs. The first is allocator overhead. Each boxed variant becomes a separate heap allocation, and allocators round up to the nearest size class. Big Pineapple uses __jemalloc__, an allocator designed for multithreaded, allocation-heavy workloads. jemalloc groups allocations of similar sizes into fixed-size bins. A `TXT`

record requests 32 bytes and fits exactly into a 32-byte bin, wasting nothing, but an `MX`

record requests 40 bytes and rounds up to 48, wasting 8 bytes.

The second cost is poor memory locality. Without boxing, the record enum values for a cache entry sit in a single contiguous allocation. With boxing, data for each boxed variant lives in a separate heap region. Reading it requires following a pointer, and when that pointer lands far from the rest of the entry, the CPU has to fetch a new cache line. With millions of cache entries, boxed data ends up scattered across the heap rather than packed together.

Neither cost is catastrophic on its own, but eliminating both, as the next section shows, yields a measurable improvement in both memory usage and lookup latency.

## Storing records in wire format

An obvious next step would be to store the full DNS response in wire format, patching only per-client fields like the message ID on each lookup. But this has drawbacks. DNSSEC records are only included when the client sets the __DO (DNSSEC OK)__ flag. Storing a complete wire format message means either caching two variants, one with DNSSEC and one without, or filtering them out of an already-built message. There is also a cost to parsing the full message on every lookup, which the enum approach we just described avoids by storing already-parsed records.

As a middle ground, we store just the record data as raw bytes, while keeping the rest of the cache entry as structured fields. Instead of a list of parsed enum variants, we store the records as a single `Box<[u8]>`

containing each record encoded as a 2-byte length prefix followed by its raw bytes.

This eliminates the per-variant enum overhead and the boxed heap allocations from the previous optimization. The data also becomes packed contiguously, which improves CPU cache locality. The tradeoff is that records can no longer be randomly indexed. We have to iterate through the buffer sequentially. This adds some complexity for features like __round-robin__ rotation of `A`

/`AAAA`

records, but since record counts per entry are small, the cost is negligible.

When building a DNS response from cached records, most record types can be copied directly from the buffer into the outgoing message. Previously, each parsed record had to be serialized field by field back into DNS wire format. The new layout skips that work for `A`

, `AAAA`

, `TXT`

, and all DNSSEC record types by copying their encoded bytes directly. Only records containing domain names, such as `CNAME`

, `NS`

, `MX`

, and `SOA`

, still require parsing so we can apply DNS name compression. Since records that support direct copying make up the vast majority of our traffic, this change reduces work on the lookup path. Combined with improved memory locality, this reduced cache lookup latency by 5% in our benchmarks.

To build the record data buffer, we write into a reusable scratchspace buffer that persists across cache insertions. Since previous writes have already grown it, the buffer rarely needs to be reallocated. Records vary in size, so we do not know the exact buffer size until they have been serialized. Once the records are in the scratchspace buffer, we allocate a `Box<[u8]>`

and `memcpy`

the data into it. This replaces the separate allocation for each boxed record with one allocation for all record data. It also avoids the waste from shrinking a `Vec<u8>`

, where the allocator may not be able to reclaim the unused tail of the original allocation. In our benchmark, this change alone increased cache insert throughput by 13%.

## The results

The production measurements show how the benchmarked per-entry savings translated to whole-process resident memory. The graph below shows p90, p98, and p99 memory usage across Big Pineapple instances. The first dashed line marks the start of the rollout on May 18, 2026, and the second marks its completion across all services on July 6, 2026. Each release introduced one or more of the optimizations described above, so memory usage dropped in steps rather than all at once.

As each release rolled out, restarted instances began with empty caches and consumed more memory as those caches filled. The stable plateaus therefore represent steady-state memory usage better than the initial dips.

Per-instance memory usage dropped across all percentiles. At p99, memory dropped from 9.3 GB to 5.3 GB, a 43% reduction in resident memory. At p90, memory dropped from 6.5 GB to 3.8 GB, a 42% reduction. Instances with fuller caches saw the largest absolute savings.

In our benchmarks, these five optimizations reduced the per-entry memory footprint from 953 bytes to 420 bytes, a 56% reduction. Per-entry allocations dropped from 1.1 KB to 461 bytes. The reductions measured in production are smaller because resident memory includes the cache alongside all other process data. After the rollouts settled, aggregate working-set memory across the fleet was roughly 100 terabytes lower.

Performance also improved. Cache insert throughput increased by 43%, while lookup latency dropped by 19%.

Metric | Before | After | Change |
Per-entry net footprint | 953 bytes | 420 bytes | -56% |
Per-entry allocations | 1.1 KB | 461 bytes | -58% |
Cache insert throughput | 625,000 entries/s | 893,000 entries/s | +43% |
Cache lookup latency | 828 ns | 670 ns | -19% |

We plan to reinvest the freed memory into increasing cache capacity without increasing our memory usage, which improves cache hit rates and reduces upstream query volume. We're also exploring further optimizations to the cache itself.

To learn more about Big Pineapple, see __How Rust and Wasm power Cloudflare's 1.1.1.1__. If you work on DNS or other large systems, share the optimizations that have worked for you in the __Cloudflare Community__ or on the __Cloudflare Developers Discord__.