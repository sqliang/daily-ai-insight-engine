---
title: In praise of memcached
source: https://jchri.st/blog/in-praise-of-memcached/
author:
- '[[j03b]]'
published: '2026-06-23'
created: '2026-06-24'
description: 'Article URL: https://jchri.st/blog/in-praise-of-memcached/ Comments
  URL: https://news.ycombinator.com/item?id=48638886 Points: 147 # Comments: 56'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 3b7bf5bec283784a
source_type: community_discussion
tldr: 作者认为memcached比Redis更适合纯缓存场景，因其简单、无持久化、客户端容错性好。
objective_summary: 一名系统管理员在其博客中对比了Redis和memcached作为缓存的方案，指出Redis常被开发者误用作持久化数据库导致运维困境，而memcached因其纯粹性、无持久化、客户端级容错和简单集群机制，更适合作为缓存基础设施。
event_type: infrastructure_update
epistemic_status: theoretical_claim
entities:
  companies:
  - Redis
  technologies:
  - Redis
  - memcached
  - Django
  key_people: []
key_logic_flow:
- Redis作为缓存被引入后，开发者常将其当作持久化数据库使用，导致运维监控盲区和数据丢失风险。
- Redis官网首页已转向推广AI相关功能，而非缓存核心用途。
- memcached官网首页第一句即清晰说明其作为分布式内存对象缓存系统的定位，使用意图明确。
- memcached客户端库在服务器宕机时自动忽略连接异常，返回默认值，处理停机非常容易。
- memcached通过客户端哈希实现集群，无内置集群功能，节点故障时自动从哈希环移除并后续重连。
- memcached不提供持久化功能，天然适合作为无状态工作负载调度，运维复杂度远低于Redis。
extract_result: success
impact_score:
  score: 2.5
  reason: 这篇文章是一个运维工程师的个人经验分享，对比 memcached 和 Redis 的缓存场景适用性。虽然观点有道理（Redis 被当作持久化数据库滥用确实常见），但这并非新发现，此类讨论在基础设施社区已持续近十年。Redis
    的 AI 转型方向（主页转向 AI 功能推广）是本文的一个新观察点，但整体仍属于小圈子内的最佳实践讨论，不会改变行业格局。
sentiment: neutral
developer_sentiment:
  tone: skeptical
  primary_focus: 是否应该从 Redis 切换回 memcached 作为缓存层
hype_assessment:
  level: low
  reason: 文章完全是个人博客观点分享，没有使用任何'颠覆'、'革命性'等夸张措辞。作者清晰地将其定位为经验分享（'In praise of'），并承认 Redis
    的功能丰富性。无任何 PR 包装或商业推广意图。
information_entropy: medium
domain_disruption:
  technical_innovation: 无
  business_model: 无
engineering_complexity: production_ready
compound_value:
  score: 2.0
  reason: memcached 是存在超过 15 年的成熟开源技术，该文章本质是技术选型层面的'理性回归'呼吁，并未引入任何新技术突破、商业模式创新或资本市场事件。文章揭示的
    Redis 公司战略漂移（官网转向 AI 营销）虽然是一个值得关注的信号，但 memcached 本身无商业实体、无融资叙事、无网络效应可积累，不具备 VC
    视角下的复利投资价值。该事件对资本流动的影响接近于零。评分理由：1-3 分区间，属于无长期积累效应的观点输出。
value_capture_layer: cloud_platform
moat_impact: neutral
key_beneficiaries:
- AWS (ElastiCache Memcached)
- Google Cloud (Memorystore for Memcached)
competitive_casualty:
- Redis (Redis Ltd.)
market_opportunities:
- 基础设施团队可利用 memcached 的纯粹缓存特性，将现有 Redis 缓存集群中确认为无状态缓存的工作负载迁移至 memcached，显著降低运维复杂度和资源开销
- 可开发 Redis 使用审计工具，自动扫描项目中 Redis 的读写模式并识别哪些 Key 实际被当作缓存而非持久化数据库使用，为技术债清理提供数据支撑
risk_matrix:
  regulatory: 无
  technological: memcached 功能集远小于 Redis，缺乏内置持久化、数据结构和发布订阅等高级能力，若未来缓存场景需要更丰富的数据操作，迁移回
    Redis 将产生返工成本
  competitive: Redis 在缓存市场拥有极高的开发者心智占有率和生态成熟度，推动团队从 Redis 切换到 memcached 面临组织惯性和开发者抵触，且
    Redis 作为公司持续迭代产品，其 AI 相关新功能可能间接削弱 memcached 的差异化优势
  ethical: 无
  additional: []
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: monitor
---

If you happen to find yourself in a sysadmin position, or a position where you just so happen to maintain someone’s infrastructure, chances are that at some point in time the topic “we need a cache” comes up.

You think for a moment and reach out for Redis, because you’re used to it, it’s fully featured, and it works! You remember it being a good, solid cache, and you wonder which new features recent releases have brought, and head to its homepage:

Your agents aren’t failing. Their context is.


Inquiring agents want to know:How can I use Redis Iris as a real-time context engine for AI apps?

1

Right, so probably something with AI2. This is sort of understandable, because
Redis is a company that wants to make money.

Anyways, Redis homepage aside, you deploy it, and off you go - your trusty cache. You hand the connection string to the people who asked for it, and off you go.

## Few months later

After a while, it turns out that `cache.set("key", "value")`

is a really simple
abstraction, and definitely easier than `INSERT INTO table VALUES ('key', 'value')`

. People start treating the *REmote DIctionary Server* as something
that’s *always* there, something that *persists* data, something that is *a
database*.

You don’t know this. Your ops colleagues don’t know this. Therefore, your
alerting doesn’t know this either, because you assume that people treat the
*cache* as something volatile.

You find out that this has been going on when you happen to do something to Redis. Maybe you upgrade it, maybe you move it to another node, maybe your cat hits the eject button on your RAID0 server’s HDD tray.

The issue isn’t that Redis doesn’t have persistence, the issue is that usually,
Redis is brought into a stack as a *cache*, and it is run with the assumption
that people treat it that way.

Usually, by the time you realize this, it is already too late, and Redis is too intertwined in the app to really leave its place. Instead, you have the eternal pleasure of maintaining it and monitoring it like a pet.

## Enter memcached

First off - what’s a memcached? Easy, ask its website:


What is Memcached?Free & open source, high-performance, distributed memory object caching system, generic in nature, but intended for use in speeding up dynamic web applications by alleviating database load.

3

Wow, first sentence on the page, even with code examples. And look at those cute little mascots at the top!

Memcached is also a cache, similar to Redis. Chances are that you’re using a framework like Django, which supports pluggable caching and allows you to switch between different caching backends.

But **why should you use memcached** when it has much less features than Redis?
Here are my reasons for why these days, I will always prefer memcached over
Redis:

-
Dealing with

**memcached downtime**is incredibly easy, because client libraries generally ignore connection exceptions. For instance, a simple`get`

will just return the default value (or none) if the server is down. -
**Clustering**memcached is wonderful, because memcached actually has no clustering built-in. To “cluster” it, you configure the client library with multiple URLs4, and the client will select the target instance based on hashing the key. If a client-side call detects an instance as down, it removes the node from the hasher. After a certain time, the client will automatically attempt to reconnect and use the dead node. -
memcached “solves” the whole persistence issue, because it does not persist to disk. It is therefore a perfect fit to just being scheduled as a stateless workload wherever you desire it.


None of these things are impossible with Redis, it’s just that memcached’s architecture in general more leans towards these directions, which makes it much, much more straightforward from an operations point of view.

But because of memcached being such a relatively simple application (plus the fact that you can run dozens instances of it with ~64 MB cache size and close to no overhead), if I need a cache these days, I usually reach out to memcached.

That said, a lot of “database too slow” problems actually begin their life as “query too slow” or “missing indices”, so be a kind person and help your developers with optimizing their queries.

Also, if you’re curious about some of the decisions behind memcached, the blog contains interesting posts, with one published just in May: “How Long Does That Response Take… For Real?”.

-
https://redis.io/, accessed 2026-06-02 ↩︎

-
That said, https://redis.io is probably not the best entrypoint for engineers, that website seems geared to people making purchasing decisions. ↩︎

-
https://memcached.org, accessed 2026-06-02 ↩︎

-
Bonus points for using a service discovery mechanism to automatically generate this setting. ↩︎