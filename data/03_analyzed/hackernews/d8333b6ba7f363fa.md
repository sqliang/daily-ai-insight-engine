---
title: Visualizing Binary Files
source: https://movq.de/blog/postings/2026-08-05/0/POSTING-en.html
author:
- '[[zdw]]'
published: '2026-08-24'
created: '2026-08-24'
manifest_dates:
- '2026-08-24'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: d8333b6ba7f363fa
source_type: community_discussion
tldr: 作者在自研十六进制编辑器 bine 中，为避免 Python 调用 ncurses 的高开销，改用 ASCII 列中的特殊字符标记 NUL 与非常规字节；对超大文件则直接把二进制字节映射为
  PGM 图像像素，用极简命令即可生成可视化。
objective_summary: 2026年8月5日，开发者在其博客发布《Visualizing Binary Files》，介绍自研十六进制编辑器 bine
  的可视化方案。bine 基于作者的 TUI 框架 movwin 编写，底层为 ncurses 且用 Python 实现，因逐字节着色需要大量 curses 调用而成本过高，作者改用
  ASCII 列中的特殊字符标记字节类型。对于数 MB 至 GB 级大文件，作者用 printf 输出 P5 格式 PGM 文件头再 cat 原始文件，将字节直接映射为像素，全程无预处理；该方案处理
  690 MB ISO 生成 PGM 仅需 0.2 秒，但 4 GB 文件因 ImageMagick 内存占用过大而失败。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - ncurses
  - PGM
  - Portable Graymap
  key_people: []
key_logic_flow:
- 作者的十六进制编辑器 bine 希望在 ASCII 列实现类似 Vim xxd 的彩色可视化，但逐字节着色需要大量 curses 调用，Python 调用 ncurses
  的开销过高。
- 由于 bine 依赖的 TUI 框架 movwin 基于 ncurses 且采用分层调色板系统，作者最终放弃着色方案，改用 ASCII 列中正常不会出现的特殊字符来标记字节类型。
- NUL 字节使用 U+2593（▓）显示，超出 0x20 至 0x7E 范围的字节使用浅阴影符（░）显示，可打印 ASCII 字符原样显示，该方案实现简单且运行时开销低。
- 对于大文件可视化，作者将二进制文件直接映射为 PGM（P5 格式）图像：用 printf 输出文件头、cat 追加原始字节，无需任何预处理逻辑。
- 作者用该方案可视化了 GORILLAS.BAS 的 EXE、含文本与图片的 tar 包、25 MB 的 ruff 二进制、690 MB 的 ISO 与 8 GB
  磁盘镜像，其中 690 MB ISO 生成 PGM 仅需 0.2 秒。
- 该方案受内存限制：用 ImageMagick 缩放 4 GB 文件时因内存占用过大失败，作者设想可先迭代缩放再压缩来扩展，但尚未实现相应工具。
object_mentions:
- object_type: project
  name: bine
  canonical_name: bine
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - bine 是作者自研的十六进制编辑器，文中详细描述了其 ASCII 列可视化功能的实现与性能取舍。
  - bine 基于作者的 TUI 框架 movwin 编写，底层调用 ncurses，作者在文中分析了 Python 调用 ncurses 的高昂开销问题。
  article_id: d8333b6ba7f363fa
- object_type: project
  name: movwin
  canonical_name: movwin
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - movwin 是作者用 Python 编写的 TUI 框架，底层有意使用 ncurses 以保持特性稳定。
  - movwin 采用分层调色板系统并默认支持深色与浅色两套主题，导致快速逐字节着色在运行时开销上不可行。
  article_id: d8333b6ba7f363fa
- object_type: project
  name: xxd
  canonical_name: xxd
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Vim 的 xxd 工具近期新增了输出着色能力，作者希望在自己的 bine 中实现类似效果，但该功能未包含在 Vim Classic 中。
  article_id: d8333b6ba7f363fa
- object_type: product
  name: ImageMagick
  canonical_name: ImageMagick
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者用 ImageMagick 缩放 690 MB ISO 文件生成的 PGM 图像耗时 6 秒，但处理 4 GB 文件时因内存占用过大而失败。
  article_id: d8333b6ba7f363fa
- object_type: product
  name: GIMP
  canonical_name: GIMP
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 作者提到 GIMP 可以轻松处理 25 MB 的 ruff 二进制文件生成的 PGM 图像，没有性能或可用性问题。
  article_id: d8333b6ba7f363fa
- object_type: product
  name: nsxiv
  canonical_name: nsxiv
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 作者提到 nsxiv 同样可以流畅显示 25 MB 的 ruff 二进制文件生成的 PGM 图像，性能与可用性均无问题。
  article_id: d8333b6ba7f363fa
extract_result: success
impact_score:
  score: 1.5
  reason: 该事件是个人开发者博客的工程实践分享，非产品发布、融资或学术突破。受众仅限 TUI 开发与二进制分析的小众技术圈，对 AI 行业竞争格局、技术路线均无实质影响。唯一可圈可点之处是
    PGM 零预处理可视化思路简洁优雅，可能被个别开发者借鉴，但整体属于'小圈子自嗨'范畴，故评分 1.5。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: Python 调用 ncurses 的逐字节着色性能开销，以及字节直映 PGM 的零预处理大文件可视化技巧
hype_assessment:
  level: low
  reason: 全文未出现'颠覆''革命性'等营销词汇，作者以谦逊口吻记录工程取舍，主动披露方案局限（4GB 文件因 ImageMagick 内存不足而失败、无法完全覆盖
    xxd 的着色场景），并明言'尚未编写此类工具'，无任何夸大包装，属于实打实的干货分享。
information_entropy: medium
domain_disruption:
  technical_innovation: 无本质技术突破，属工程巧思：一是以 ASCII 列的 U+2593（▓）与 U+2592（░）特殊字符标记 NUL
    与非打印字节，绕开 Python 调用 ncurses 逐字节着色的高昂开销，牺牲表达力换取极低运行时成本；二是利用 PGM P5 格式'字节即像素'的天然同构性，用
    printf 拼文件头 + cat 追加原始字节实现零预处理的大文件可视化（690MB 仅 0.2 秒）。该思路在二进制可视化领域已有先例（如 binvis），创新性主要体现在极简与零依赖的实现方式，而非方法论本身。
  business_model: 无。该文为个人自用工具的技术笔记，不涉及商业模式、定价或生态影响，也无商业化路径可推演。
engineering_complexity: production_ready
compound_value:
  score: 1.5
  reason: 从资本视角看，该事件是一个独立开发者对自己自研十六进制编辑器 bine 的可视化技巧分享，属于纯技术博客内容，无任何商业主体、融资动作或产品生态参与。技术层面虽巧妙（用特殊字符替代逐字节着色规避
    Python+ncurses 性能瓶颈；用 printf+cat 将字节直映射为 PGM 像素），但本质是低门槛、高可复制性的 shell 技巧，不存在专利、网络效应或数据飞轮。作者本人也明确表态'keep
    it just as it is now'，无扩展商业化意图。该事件不涉及算力、模型或平台任何环节，无法形成长期复利积累，3-5 年后不会有任何结构性影响，仅可能被同类工具开发者收藏为一条工具技巧。故评分落在'昙花一现'区间。
value_capture_layer: end_application
moat_impact: neutral
key_beneficiaries:
- bine
- movwin
competitive_casualty: []
market_opportunities:
- 开发者可将二进制字节直映射为 PGM 图像（printf+cat 零预处理）的可视化方案，低成本复用于恶意软件分析、固件逆向与磁盘取证工具链
- 用 ASCII 列特殊字符替代逐字节着色的低开销终端渲染思路，可借鉴到 TUI/CLI 工具的性能优化设计中，尤其适合资源受限环境
- 面向 4GB 级以上超大文件的流式缩放可视化（先迭代缩放再压缩）存在工具空白，可作为二进制分析工具链的差异化功能方向
risk_matrix:
  regulatory: 无
  technological: 字节到像素的直映射高度依赖内存加载，4GB 级文件缩放即因 ImageMagick 内存上限失败，扩展到超大规模文件存在明显技术瓶颈；且该可视化思路并非新技术，可替代实现众多
  competitive: 个人项目与 ImHex、010 Editor、Vim xxd 等成熟十六进制编辑器存在明显代际差距，功能与生态竞争力有限，难以形成独立商业价值
  ethical: 可视化磁盘镜像或复合文件时可能无意暴露其中嵌入的敏感数据（密钥、凭据、私人内容），共享可视化结果前需进行脱敏处理
  additional:
  - 图像模式识别具有主观性，可视化呈现出的'规律'容易被过度解读，需结合结构化分析交叉验证，避免在安全取证场景中形成误判
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: ignore
object_insights:
- object_type: project
  name: bine
  canonical_name: bine
  url: null
  positioning: 作者自研的十六进制编辑器，基于 Python 编写的 TUI 框架 movwin，底层调用 ncurses，主打轻量高效的可视化十六进制查看能力。
  technical_signal: 通过 ASCII 列特殊字符标记字节类型，NUL 用 U+2593（▓）、非常规字节用浅阴影符，实现简单且运行时开销低。
  adoption_signal: null
  ecosystem_relevance: 基于作者自研的 movwin TUI 框架并依赖 ncurses，功能上与 Vim 的 xxd 着色能力形成对照，属开发者工具生态中的小众自研项目。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: bine 展示了 Python 与 ncurses 组合下终端可视化的实用取舍，并以字节直接映射 PGM 像素的极简思路实现大文件可视化，对二进制分析工具设计有借鉴价值，值得跟踪其后续演进。
  risk_notes:
  - 项目依赖作者个人的 movwin 框架，功能演进受个人维护节奏制约，社区参与度有限。
  - 特殊字符标记方案覆盖的字节类型场景少于 xxd，可视化信息粒度有限。
  - 大文件可视化受内存限制，作者尚未实现迭代缩放工具，4 GB 级文件处理不可行。
  score: 6.0
  article_ids:
  - d8333b6ba7f363fa
  evidence_snippets:
  - bine 是作者自研的十六进制编辑器，文中详细描述了其 ASCII 列可视化功能的实现与性能取舍。
  - bine 基于作者的 TUI 框架 movwin 编写，底层调用 ncurses，作者在文中分析了 Python 调用 ncurses 的高昂开销问题。
- object_type: project
  name: xxd
  canonical_name: xxd
  url: null
  positioning: Vim 自带的十六进制转储工具，近期新增输出着色能力，帮助用户快速辨识文件中的文本与二进制区域，但新功能未包含在 Vim Classic
    中。
  technical_signal: xxd 近期新增输出着色能力，能通过颜色区分 ASCII 文本区域与二进制区域，作者在 bine 中尝试复现类似效果。
  adoption_signal: 作为 Vim 生态内置工具，xxd 拥有广泛用户基础，着色功能表明其仍在持续演进。
  ecosystem_relevance: xxd 属于 Vim 工具链生态，其着色功能为其他十六进制编辑器（如 bine）提供了功能参照基准。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: xxd 是 Vim 生态中被广泛使用的十六进制查看工具，其新增着色能力反映了终端二进制可视化功能的演进方向，可作为同类工具功能对照与追踪的基准。
  risk_notes:
  - 着色功能未包含在 Vim Classic 中，经典版用户无法使用该新特性。
  - 文中仅将其作为参照对象提及，缺乏对实现细节与性能表现的更多信息。
  score: 4.0
  article_ids:
  - d8333b6ba7f363fa
  evidence_snippets:
  - Vim 的 xxd 工具近期新增了输出着色能力，作者希望在自己的 bine 中实现类似效果，但该功能未包含在 Vim Classic 中。
- object_type: product
  name: ImageMagick
  canonical_name: ImageMagick
  url: null
  positioning: 跨平台的开源图像处理工具套件，支持 PGM 等多种图像格式的缩放、转换与编辑，被开发者广泛用于命令行图像批处理场景。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 开发者
  - 系统管理员
  - 命令行图像处理用户
  product_signal: 文中用它缩放 690 MB 的 PGM 图像耗时 6 秒，但处理 4 GB 文件时因内存占用过大而失败，说明其大文件处理存在内存瓶颈。
  market_signal: 作为成熟的开源命令行图像处理工具，ImageMagick 在开发者批处理场景中占据主流地位，但超大文件处理能力受限。
  differentiation: 与 GIMP、nsxiv 等工具相比，ImageMagick 聚焦命令行批处理与格式转换，但其一次性载入大图的内存策略使其难以处理数
    GB 级图像。
  watch_reason: ImageMagick 是开发者常用的命令行图像处理工具，本文揭示了其在大规模图像（数 GB 级 PGM）处理上的内存限制，这一边界条件对图像处理工具选型有参考意义。
  risk_notes:
  - 处理数 GB 级超大图像时内存占用过高，导致任务失败，缺少迭代式缩放方案。
  - 文章仅将其作为辅助工具提及，未涉及其自身产品路线与维护状态。
  score: 3.0
  article_ids:
  - d8333b6ba7f363fa
  evidence_snippets:
  - 作者用 ImageMagick 缩放 690 MB ISO 文件生成的 PGM 图像耗时 6 秒，但处理 4 GB 文件时因内存占用过大而失败。
---

blog - git - desktop - contact

2026-08-05

This is my hex editor
`bine`

:

It's all black and white, basically. A while ago, Vim's `xxd`

gained the
ability to color the output (this is not included in
Vim Classic, which I use):

I wanted to have this feature in `bine`

as well. So I started
implementing it. An early draft can be seen here:

I think this is already an improvement, because it's easier to spot ranges with mostly ASCII text. But it's not that great. More colors would be needed and it would probably be better if the hex columns were right next to each other, instead of being separated by a space.

My TUI framework `movwin`

uses ncurses under the hood (this is entirely
intentional, because I value ncurses' feature stability). The framework
itself is written in Python, ncurses is a C library. I had already
noticed that making calls from Python to ncurses can be very expensive
(whether this is a problem in general or just with Python + ncurses or
simply with ncurses itself, I don't know, and it doesn't matter).

Without colors, `bine`

writes *an entire hex line* using one curses
call. With colors, there would have to be *many calls per line*. That
would be too expensive.

And the framework itself is also not particularly well suited to using
lots of different colors in a *speedy* manner. It has a hierarchical
palette system and all that is costly. And I would have to maintain at
least two versions of the color palette, because `movwin`

supports a
dark and a light theme by default.

Eventually, I came up with this:

Instead of colorizing the hex column, `bine`

uses special characters in
the ASCII column -- ones, that normally cannot appear there, so there are
no conflicts. NUL bytes use U+2593 (`▓`

), bytes outside of the range
`0x20 <= byte <= 0x7E`

use U+2593 (`░`

), and the rest are printable
ASCII chars that are shown as is.

This doesn't cover all the same cases as `xxd`

does, but it's good
enough. You get a quick overview over what's "plain text" and what
isn't, NUL bytes stand out.

It was super easy to implement and it's cheap at runtime.

I also wanted to have a similar visualization feature for entire big
files. The tiny ASCII pane in `bine`

can only show so much. I wanted
this for something that's a couple of megabytes in size, maybe
gigabytes.

My visualization essentially is an image. So ... why not coerce the
computer into interpreting an existing binary file as an image? Don't do
any processing, just take all the bytes in the range of `0x00`

thru
`0xFF`

and map them to pixels.

A Portable Graymap is pretty much exactly that. The Wikipedia example shows the ASCII version of this format, but there's also a binary version.

So what I had to do, was basically just this:

```
printf 'P5\n%s %s 255\n' "$width" "$height"
cat "$infile"
```


Done.

The only "logic" I implemented was determining a good width and height.

The result can be shown in any image viewer (on Linux and BSD).

Here's a visualization of an EXE file of GORILLAS.BAS, unscaled:

(Yes, that's the entire game. You could even take this image and convert it back to an EXE file.)

What can we see here? Let me annotate a few interesting sections:

I think it's nice that there is *no pre-processing at all*. All this
simply exploits the properties of the data.

As a more "explicit" example, here's a tarball with two files in it, a text file and an image:

Also, notice the "pattern" in the text area? Looks like a tiled wallpaper? That's because the text is repeating.

Just some more examples. Here's a scaled-down visualization of ruff:

An unscaled excerpt of the black stripe of mostly NUL bytes towards the top:

Lots of neatly aligned data and tables.

The `ruff`

binary is 25 MB in size and so is the resulting PGM file
(4096x6225 pixels), so I scaled it down, because I don't want to have
such a large file lingering in my blog forever. But
GIMP
and
nsxiv
can easily handle the PGM file, there are no performance or usability
issues at all.

Here's a visualization of a 690 MB `.iso`

file (heavily scaled down, of
course):

Creating the PGM file takes 0.2 seconds, resizing the image using ImageMagick takes 6 seconds. This shows some limits of this approach: I tried doing this with a 4 GB file, but ImageMagick required too much RAM. I think that you could make this work, in theory, by iteratively resizing the image first (put only a couple of rows at a time into memory), then compressing the smaller version. I have not written such a tool, yet, and ImageMagick doesn't appear to implement this (or I don't know how -- haven't searched, though).

Still, depending on which kind of data I'm looking for, this can already be a useful tool. Inspecting large files in the range of gigabytes is something that I very rarely need to do -- if need be, I can easily split it into smaller parts. Just as a demo, here's an old 8 GB disk image:

Mildly interesting to look at, but as I said, depending on what I look for, it can be helpful.

I think I'll keep it just as it is now.