---
title: Tooltips need a delay, and then they need to skip it
source: https://blog.master.dev/tooltips-need-a-delay-and-then-they-need-to-skip-it/
author:
- '[[ibobev]]'
published: '2026-08-25'
created: '2026-08-26'
manifest_dates:
- '2026-08-26'
description: 'Article URL: https://blog.master.dev/tooltips-need-a-delay-and-then-they-need-to-skip-it/
  Comments URL: https://news.ycombinator.com/item?id=49436786 Points: 173 # Comments:
  47'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 49a4eac9ae7460f3
source_type: community_discussion
tldr: 作者发现产品 FrontPrep 中悬停 Logo 即弹出工具提示的体验不佳，于是引入 200ms 打开延迟，并设计“温暖窗口”机制：工具提示关闭后 300ms
  内再次悬停可跳过延迟立即显示。作者将这一模式封装成 Claude skill，供开发者审计自己的代码库。
objective_summary: 博客作者在面试题产品 FrontPrep 的 Logo 悬停交互中发现，将工具提示延迟设为 0 会导致光标扫过页面时沿途误弹出提示。作者采用
  200ms 打开延迟修复误触，但连续悬停相邻 Logo 时每次仍需等待 200ms，体验迟钝。他据此实现一套基于 TooltipProvider 共享状态的模式：悬停后等待
  200ms 打开，关闭后进入 300ms 冷却期，期间再次悬停任何 Logo 会跳过延迟立即显示。该实现基于 Radix 与 Motion，并封装为一个 Claude
  skill 用于审计代码库中的工具提示时序。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - FrontPrep
  - Radix
  - Coinbase
  technologies:
  - Radix
  - Motion
  - React
  - Claude Skills
  key_people: []
key_logic_flow:
- 作者发现将工具提示延迟设为 0 会导致光标扫过页面时沿途误弹出提示，因此改为 200ms 打开延迟。
- 连续悬停相邻 Logo 时每次都要等待 200ms，体验迟钝，于是作者设计了跳过延迟的“温暖窗口”机制。
- 核心模式是：悬停触发后若页面处于温暖状态则立即显示工具提示，否则启动 200ms 定时器等待。
- 工具提示打开时通过 data-instant 属性让 CSS 跳过入场动画，关闭后 provider 启动 300ms 冷却计时。
- 300ms 冷却期内再次悬停任意 Logo 会取消冷却并保持页面温暖以跳过延迟，冷却结束后页面变冷并恢复 200ms 等待。
- 作者将这一模式封装为 Claude skill，定义了打开延迟 200ms、温暖窗口 300ms、关闭延迟 0ms 三个关键数值。
object_mentions:
- object_type: product
  name: FrontPrep
  canonical_name: FrontPrep
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 作者在 FrontPrep 的题目列表页中为公司 Logo 添加了悬停提示，当光标悬停在 Logo 上时，工具提示会展示对应的公司名称。
  - 文章指出 FrontPrep 的工具提示基于 Radix 和 Motion 构建，并应用了延迟与温暖窗口的交互模式来改善用户体验。
  article_id: 49a4eac9ae7460f3
- object_type: project
  name: tooltip
  canonical_name: tooltip (Claude skill)
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 作者创建了一个名为 tooltip 的 Claude skill，用于审计代码库中工具提示的延迟与跳过延迟的时序设计。
  - 该 skill 定义了三个关键数值：打开延迟 200ms、温暖窗口 300ms、关闭延迟 0ms，并附带了完整的行为说明。
  article_id: 49a4eac9ae7460f3
- object_type: project
  name: Radix UI
  canonical_name: Radix UI
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章提到 FrontPrep 中的工具提示使用 Radix 构建，作者在此基础上实现了打开延迟与温暖窗口的交互模式。
  article_id: 49a4eac9ae7460f3
- object_type: project
  name: Motion
  canonical_name: Motion
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章提到 FrontPrep 中的工具提示使用 Motion 构建，作者在此基础上实现了打开延迟与温暖窗口的交互模式。
  article_id: 49a4eac9ae7460f3
extract_result: success
impact_score:
  score: 2.5
  reason: 这是一篇前端交互细节的工程博客，属于社区讨论性质。其技术含量在于一个巧妙的'温暖窗口'时序模式（200ms 打开延迟 + 300ms 冷却跳过窗口），并对
    Radix/Motion 实现与 Claude skill 封装给出了完整代码。但它既不涉及模型能力突破，也不影响资本流向或竞争格局，受众局限于正在做工具提示/悬停交互的开发者群体，属于小圈子高质量分享而非行业事件，短期冲击力有限。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: 工具提示 200ms 延迟与 300ms 温暖窗口的参数取舍，以及如何将交互模式封装为 Claude skill 供代码库审计
hype_assessment:
  level: low
  reason: 全文为实战问题驱动的工程复盘，从 0ms 延迟的误触痛点出发逐步推导解决方案，配有 before/after 视频和可运行的代码示例，未使用'颠覆'、'革命'等夸大词汇，也没有任何商业推广话术，属于实打实的干货分享。
information_entropy: medium
domain_disruption:
  technical_innovation: 核心贡献不在 AI 模型本身，而是一套可复用的工具提示时序模式：通过共享 TooltipProvider 状态维护页面'温暖/寒冷'双态，以
    data-instant 属性跳过 CSS 入场动画，在误触抑制与快速连续悬停之间取得平衡。更值得注意的是作者将该模式封装为 Claude skill，使
    LLM 能依据时序规范自动审计代码库中的工具提示实现，体现了'前端交互模式 → 可执行技能'的 AI 辅助开发新形态。
  business_model: 无（纯前端交互模式分享，不涉及商业模式变革；虽然封装为 Claude skill 的实践折射出 AI 编码工具生态的演进方向，但尚不构成商业模式重塑）
engineering_complexity: production_ready
compound_value:
  score: 3.0
  reason: 强制 CoT 推理路径：(1) 资产属性判断——这是一篇前端微交互模式博客 + 一个 Claude skill，既非公司也非产品更非基础设施，属于单点
    UX 创新；(2) 复利机制判断——200ms 延迟 + 300ms 温暖窗口本身无网络效应、无数据飞轮，唯一可能累积的是'设计模式被封装为 Claude
    skill 并进入代理工作流'这一分发形式，但单篇 skill 的边际价值极低；(3) 时间维度判断——3-5 年内该模式几乎必然被 Radix、shadcn/ui
    等主流组件库默认吸收，或直接成为 Claude 编码代理的默认行为，届时独立价值归零，属于'平台吸收型'创新而非'平台基座型'创新；(4) 结论——给 3.0
    分，它代表的是 UX 知识商品化、注入 AI 代理的趋势信号，但事件本身不构成长期复利资产。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- Anthropic
- FrontPrep
- Radix
- Motion
competitive_casualty:
- UX 设计咨询与审计服务商
- 传统设计系统顾问团队
market_opportunities:
- 产品团队可借鉴'温暖窗口'机制优化 AI 界面中的悬停信息卡、来源引用与内联解释提示，在连续浏览场景下显著提升感知流畅度，降低'每次悬停都要等待'的迟滞感
- 作者将交互模式封装为 Claude skill 的做法值得跟进，可构建'UX 微模式审计'类 AI 辅助开发工具，帮助团队扫描代码库中工具提示等交互的时序缺陷，形成可复用的工程化资产
- 可将该模式沉淀为开源组件或设计系统规范（依托 Radix/shadcn 生态），以极低门槛向社区扩散，为复杂数据密集型界面（仪表盘、编辑器、AI Agent 控制台）提供即插即用的悬停交互优化方案
risk_matrix:
  regulatory: 无
  technological: 模式依赖 Radix/Motion/React 的特定实现，跨框架迁移或依赖升级存在维护成本；若未同步处理键盘导航与屏幕阅读器语义，跳延迟逻辑可能与可访问性（ARIA）要求产生状态不一致
  competitive: 该模式实现门槛低，主流设计系统或 AI 编码助手（如 shadcn/ui、Radix 官方）可能直接将其内化为默认行为，使该差异化迅速商品化，先发优势窗口有限
  ethical: 悬停触发式工具提示对行动障碍与低视力用户本就不够友好，'跳过延迟'机制可能进一步放大误触风险；若缺少键盘替代方案，存在数字包容性隐患
  additional: []
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: monitor
object_insights:
- object_type: product
  name: FrontPrep
  canonical_name: FrontPrep
  url: null
  positioning: 面向技术面试准备的在线产品，在题目列表页展示公司 Logo 与对应的面试题目，帮助求职者了解各公司的面试考察内容。
  technical_signal: 工具提示基于 Radix 与 Motion 构建，通过 TooltipProvider 共享 isWarm 状态，实现延迟打开与温暖窗口跳过的交互逻辑。
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 准备技术面试的求职者
  product_signal: 产品为题目列表页的 Logo 悬停交互引入了 200ms 打开延迟与 300ms 温暖窗口，解决误触与连续悬停迟钝的双重体验问题。
  market_signal: null
  differentiation: 通过温暖窗口机制让连续悬停不同 Logo 时立即响应，在工具提示交互细节上区别于默认零延迟或固定延迟的同类产品。
  watch_reason: FrontPrep 将工具提示的延迟与跳过延迟模式落地到真实产品中，其温暖窗口设计是通用可复用的交互模式，可作为面试产品 UX 细节打磨的参考案例。
  risk_notes:
  - 温暖窗口机制依赖跨组件共享状态，实现复杂度与维护成本随组件数量上升。
  - 300ms 温暖窗口可能误判用户缓慢移动的意图，导致非预期地立即弹出提示。
  score: 5.0
  article_ids:
  - 49a4eac9ae7460f3
  evidence_snippets:
  - 作者在 FrontPrep 的题目列表页中为公司 Logo 添加了悬停提示，当光标悬停在 Logo 上时，工具提示会展示对应的公司名称。
  - 文章指出 FrontPrep 的工具提示基于 Radix 和 Motion 构建，并应用了延迟与温暖窗口的交互模式来改善用户体验。
- object_type: project
  name: tooltip
  canonical_name: tooltip (Claude skill)
  url: null
  positioning: 一个用于审计代码库中工具提示时序设计的 Claude skill，封装了打开延迟 200ms、温暖窗口 300ms、关闭延迟 0ms
    三个关键数值。
  technical_signal: 该 skill 定义了打开延迟 200ms、温暖窗口 300ms、关闭延迟 0ms 三个关键数值，并配套完整的时序行为说明。
  adoption_signal: 该 skill 由作者创建用于审计个人代码库的工具提示时序，目前处于作者自用阶段，尚未见到公开采用案例。
  ecosystem_relevance: 属于 Claude skill 生态的可复用技能，帮助开发者在各自代码库中落地统一的工具提示延迟与温暖窗口模式。
  target_users:
  - 前端开发者
  - UI 组件库维护者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 工具提示的延迟与跳过延迟是各类界面普遍存在的交互细节，该 skill 将这一模式系统化为可复用审计工具，值得持续跟踪其后续迭代与实际采用情况。
  risk_notes:
  - 200ms 与 300ms 关键数值来自作者特定场景，直接套用到其他产品可能需要重新调优，普适性有待验证。
  - 该 skill 目前仅由作者个人维护，缺乏社区反馈与长期演进保障。
  score: 6.0
  article_ids:
  - 49a4eac9ae7460f3
  evidence_snippets:
  - 作者创建了一个名为 tooltip 的 Claude skill，用于审计代码库中工具提示的延迟与跳过延迟的时序设计。
  - 该 skill 定义了三个关键数值：打开延迟 200ms、温暖窗口 300ms、关闭延迟 0ms，并附带了完整的行为说明。
---

On the question listing pages for my product, FrontPrep, I display company logos next to the interview questions. When you hover over a logo, a tooltip shows the company name.

One thing had been annoying me for a couple of days. If I just moved the cursor across the page, tooltips kept showing up instantly along the way. The cause was simple: I had set the tooltip delay to `0`

, so it appeared the moment the cursor hovered over the logo.

To fix this issue, I added a 200ms transition delay, which worked but created a new problem. If you look at the user interface, there are a couple of rows where multiple logos sit next to each other because the same interview questions are asked at multiple companies. Now, moving from one logo to another meant waiting the same 200ms delay every time you hovered over a different company logo, which felt sluggish and led to a poor user experience.

This post is not about building a tooltip. It’s about one small interaction pattern, the same one you will find in browser toolbars and various websites, and people are unaware of it.

Here is a short video of the before-and-after experience on my website. It helps you understand the problem better.

## A Solution

I will divide my solution into three parts.

- You hover over a logo. The tooltip waits 200ms before opening.
- A tooltip closes, and a 300ms timer starts. I call this the warm window or warm page. If you hover over another logo while this 300ms timer is running, or we could say when the page is warm, the tooltip opens instantly without any waiting or animation.
- When the 300ms timer expires, everything returns to normal, and the page becomes cold. If you hover over the logo again, the next tooltip has to wait 200ms again. Without this step, the first tooltip you opened would turn off the delay for the whole page permanently.

hover → wait 200ms → tooltip opens (page is now warm)

leave → tooltip closes → 300ms cooldown

├─ hover another tooltip before cooldown → opens instantly, page stays warm

└─ cooldown ends → page is cold, the 200ms wait is back

In FrontPrep, my tooltips are built with Radix and Motion. I will demonstrate the pattern using a simpler React version below, and later in the post, I will put this idea into a Claude skill which you can use to audit your own codebase.

Here’s a follow-up article that replicates the basic UX here without any JavaScript.

## How the Code Works

I will try to explain the code in the same order in which things happen when you move the cursor.

### Step 1: You Hover Over a Logo

When `onMouseEnter`

is triggered on a logo, this function is called:

```
function handleEnter({
// `tooltips` is a useContext variable where the isWarm state is tracked between components
If (tooltips.isWarm) {
show();
return;
}
// `openTimer` is a useRef variable, so .current is how you set/access the value.
openTimer.current = setTimeout(show, tooltips.openDelay);
}
```

Code language: JavaScript (javascript)

On hover, this component asks one question: is the page warm? If it is, the tooltip opens instantly. If not, it starts a 200ms timer and waits. You might be wondering where this tooltips object came from. It comes from the `TooltipProvider`

which I will discuss in the last step.

### Step 2: The Tooltip Opens

Here’s that show function that handles the opening of the tooltip:

```
function show({
setInstant(tooltips.isWarm);
setOpen(true);
tooltips.markOpened();
}
```

Code language: JavaScript (javascript)

This does three things:

- It copied the value of
`tooltips.isWarm`

into a state called`instant`

. - It opens the tooltip.
- It tells the provider that the tooltip is open, which makes the page warm.

Coming back to point 1, it copied what `tooltips.isWarm`

returns into a flag called instant because this instant flag is added to the tooltip as a data attribute, which CSS uses to skip the entrance animation.

```
.tooltip[data-instant="true"] {
transition-duration: 0ms;
}
```

Code language: CSS (css)

This is the reason why, when the page is warm, a tooltip opens without any animation or delay.

### Step 3: You Leave the Logo

When `onMouseLeave`

is triggered on a logo, this function is called:

```
function handleLeave({
clearTimeout(openTimer.current);
If (!open) return;
setOpen(false);
setInstant(false);
tooltips.markClosed();
}
```

Code language: JavaScript (javascript)

First, we have to know whether the tooltip is currently open.

Let’s say your cursor crosses a logo in 50ms, which is far less than 200ms, so the timer started by `handleEnter`

is still running and the tooltip is not opened yet. `clearTimeout`

in `handleLeave`

cancels that timer so the tooltip never opens at all, and `if (!open) return;`

stops the function right there, because the tooltip, which never opened has nothing to close and no cooldowns to start. In the first video, a sweep opened every tooltip in its path; now, the same sweep opens no tooltips.

If the tooltip is open, this happens when you rest your cursor on the logo for more than 200ms, or when the page is warm, and the tooltip opens instantly on enter. We will close the tooltip and tell our provider, the provider will then start a 300ms cooldown timer, if you hover over the next logo before this timer ends, the tooltip opens instantly.

### Step 4: The Provider

```
const warm = useRef(false);
const cooldownTimer = useRef(null);
useEffect(() => () => clearTimeout(cooldownTimer.current), []);
const tooltips = useMemo(
() => ({
openDelay,
isWarm: () => skipWhenWarm && warm.current,
markOpened() {
warm.current = true;
clearTimeout(cooldownTimer.current);
},
markClosed() {
clearTimeout(cooldownTimer.current);
cooldownTimer.current = setTimeout(() => {
warm.current = false;
}, warmFor);
},
}),
[openDelay, warmFor, skipWhenWarm]
);
```

Code language: JavaScript (javascript)

This is the shared state we need for this whole pattern to work. The page could either be warm or cold. If you notice, markOpened cancels the pending cooldowns; this is what keeps the page warm when you move from one logo to another. Basically, every new tooltip cancels the cooldown started by the previous one.

You don’t need to get confused about `skipWhenWarm`

flag, it is just for the before and after toggle in the demo; turning it off always makes the provider cold to help you see the before behavior.

Another important detail here is that `isWarm`

is a ref and not a React state because changing it does not re-render all the tooltips on the page. This ref is only read inside the event handlers.

## Why a Timer of 200ms?

If we choose a timer of less than 150ms, when a cursor passes over a tooltip trigger, it will most likely open the tooltip. If we choose a timer of more than 250ms, the hover will feel sluggish and broken. So, 200ms is a good, balanced number in this case.

## Judgement & Taste

If you ask AI to build a tooltip, it will build a fully functional tooltip, but in the end, it is you, the human, who will decide if the tooltip built is worthy because there are details that separate a working tooltip from a polished tooltip, the same way they separate a working product and a polished product. AI can build a polished product only if you guide it to do so, and you can guide it when you have developed taste and judgment, which come from years of experience, mistakes, and practice.

However, you can use skills of other engineers/designers to have their taste of a polished product and eventually build yours as you start developing your own judgement, you can create/use skills around colors, accessibility, forms, animations, typography etc, you can generate solid outputs but it again does not mean it is production ready, you are still there at the end to judge and ship it only if it meets your standards and taste.

## Skill

I have created a Claude skill for solving the above problem which I faced, you can build a similar skill or copy the one written below and run it to audit your codebase. Every codebase is different, and every codebase uses different libraries for tooltips or has written custom tooltips differently; this skill will work for all.

.claude

/skills

/tooltip

/SKILL.md

```
---
name: tooltip
description: Tooltips need a delay so they don't open up on unintentional mouse travel
---
# Tooltip Timing
A click is always intentional whereas a hover is not intentional. The cursor travels across the page to get wherever it has to, and it passes over elements on the way, so a tooltip cannot tell from the hover whether the user wants to open it. The 200ms is how it finds out.
```css
.tooltip {
transition-delay: 200ms;
}
```
## The three numbers and states
| Value | Number | Why |
| ----------- | ------ | ------------------------------------------------------------------- |
| Open delay | 200ms | Below 150ms a cursor that is only passing over a trigger still opens it. Above 250ms an intentional hover feels broken. |
| Warm window | 300ms | Long enough to cover the move from one trigger to the next one. Short enough that a hover a second later waits again. |
| Close delay | 0ms | Leaving a trigger should be clear. So there is nothing to wait for.|
## How it should behave
```
hover -> wait 200ms -> tooltip opens (page is now warm)
leave -> tooltip closes -> 300ms cooldown
```
```

Code language: Markdown (markdown)

The views expressed in this article are those of the author only and not Coinbase.