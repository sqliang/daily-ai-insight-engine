---
title: Wake up! 16b
source: https://hellmood.111mb.de/wake_up_16b_writeup.html
author:
- '[[MaximilianEmel]]'
published: '2026-05-24'
created: '2026-05-24'
description: 'Article URL: https://hellmood.111mb.de/wake_up_16b_writeup.html Comments
  URL: https://news.ycombinator.com/item?id=48253060 Points: 153 # Comments: 10'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: bc2197128103ef22
source_type: community_discussion
tldr: 开发者 HellMood 在 2026 年 5 月的 Outline Demoparty 上发布了一款仅 16 字节的 x86 实模式 DOS 演示程序
  "wake up!"，该程序利用视频内存作为计算空间生成无限谢尔宾斯基分形，同时通过 PC 扬声器输出同一几何结构所产生的音频，实现视听联觉效果。
objective_summary: HellMood 于 2026 年 5 月在荷兰 Ommen 举行的 Outline Demoparty 上发布了 16 字节
  x86 汇编演示程序 "wake up!"。该程序通过 BIOS 中断设置 40×25 文本模式，将数据段指向 0xB800（VGA/CGA 文本缓冲区），使用
  XOR 替代 ADD 实现无进位加法，从而生成元胞自动机规则 60 对应的谢尔宾斯基三角形。程序以 `out 61h, al` 指令将计算结果直送 PC 扬声器端口，使位
  1 控制扬声器振膜伸缩，产生自相似方波节拍。通过逆向步进 56 字节而非正向 16 字节，程序将循环周期延长至 8192 步，使音高降低一个八度，并在屏幕上形成
  10 列对角线字符柱。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - x86 assembly
  - Sierpinski fractal
  - cellular automaton
  - BIOS
  - DOS
  - VGA
  key_people:
  - HellMood
  - miragept
  - Plex
key_logic_flow:
- 程序开头使用 BIOS 中断 int 10h 设置视频模式 0（40×25 文本模式），然后将数据段寄存器指向 0xB800（VGA/CGA 文本缓冲区的内存地址）。
- 程序使用 XOR 替代 ADD 来实现无进位加法，配合起始累加器值为 2（二进制 00000010）的条件，仅对位 1 进行翻转，从而精确映射到初等元胞自动机规则
  60，生成谢尔宾斯基三角形。
- '`out 61h, al` 指令将计算所得的字节直接发送到 PC 扬声器端口 61h，其中位 1 控制扬声器振膜的推出与拉回，将分形的 0/1 序列转换为具有自相似性的方波节拍。'
- 程序使用 `sub si, byte 57` 加 `lodsb` 实现每次逆向步进 56 字节，由于 56 不能整除 65536，程序需要 8192 步并回绕
  7 次才重置，使声音降低一个八度。
- 在 80 字节宽的屏幕上逆向移动 56 字节等效于正向移动 24 字节（12 列），最终仅访问 10 个不同列，使谢尔宾斯基分形以 10 列对角线字符柱的形式沿屏幕上移。
- 不同 BIOS 版本和模拟器在内存中留下的初始数据不同，程序通过 XOR 与这些遗留数据进行运算，因此输出高度依赖于运行环境，这被视作 sizecoding 文化的一部分。
extract_result: success
object_mentions:
- object_type: project
  name: wake up!
  canonical_name: wake up!
  url: https://hellmood.111mb.de/wake_up_16b_writeup.html
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 该程序仅用 16 字节的 x86 实模式 DOS 汇编代码，利用视频内存作为计算空间绘制无限谢尔宾斯基分形。
  - 程序通过 `out 61h, al` 指令将计算得到的字节直接发送到 PC 扬声器端口，实现视听联觉效果。
  - 该程序在 2026 年 5 月荷兰 Ommen 举行的 Outline Demoparty 上正式发布。
  article_id: bc2197128103ef22
- object_type: project
  name: M8trix
  canonical_name: M8trix
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者 HellMood 的原创作品 "M8trix" 于 2014 年发布，仅用 8 字节（后压缩至 7 字节）即可在屏幕上涂抹伪随机字母。
  - 作者在开发 "wake up!" 时始终思考如何让 "M8trix" 的效果听起来更好，但本次声音部分优先于画面创作。
  article_id: bc2197128103ef22
- object_type: project
  name: Rainbow Surf
  canonical_name: Rainbow Surf
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Plex 的作品 "Rainbow Surf" 仅用 16 字节即实现了出色的演示效果，这促使作者重新翻出旧草图开始创作。
  article_id: bc2197128103ef22
impact_score:
  score: 1.5
  reason: 这是一篇关于极简汇编代码艺术（sizecoding/demoscene）的技术文章，与 AI 行业完全无关。虽然作者对 16 字节程序背后的数学原理（细胞自动机
    Rule 60、Sierpinski 分形、XOR 等价性）进行了深度剖析且技术质量极高，但对于 AI 行业的短期冲击力几乎为零——它不涉及任何模型、框架、产品、融资或行业政策。
sentiment: neutral
developer_sentiment:
  tone: neutral
  primary_focus: demoscene 圈层对极致代码压缩与数学之美的技术赞叹，AI 开发者对此事件几乎无感知
hype_assessment:
  level: low
  reason: 文章完全没有任何 PR 包装或概念炒作。作者以极严谨的数学推导解释了 XOR 运算、二项式系数、Rule 60 细胞自动机与 Sierpinski
    分形之间的内在联系，甚至主动纠正了此前小 intro 作品容易造成的误解。'颠覆'、'革命性'等词汇完全未出现，属于纯粹的硬核技术博客。
information_entropy: high
domain_disruption:
  technical_innovation: 无AI相关技术突破。文章的技术价值体现在 sizecoding 领域的极端代码压缩技巧：利用 VGA 显存作为计算空间、XOR
    的模2加法等价性生成分形图案、端口61h bit1 直接驱动 PC 喇叭实现音画同步。这些是 x86 底层编程与数学交叉领域的精巧工程，但与 AI 技术栈（深度学习、推理引擎、训练框架等）无任何交集。
  business_model: 无——此为 demoscene 社区的纯技术艺术创作，不涉及任何商业产品或商业模式。
engineering_complexity: production_ready
compound_value:
  score: 1.5
  reason: 这是一件精湛的 demoscene 技术艺术作品，在 16 字节内实现了分形生成与音频合成的数学耦合，展示了极致的算法压缩能力。但从 VC 资本视角审视：①无产品化路径——该程序运行于
    40 年前的 DOS 实模式环境，不存在可寻址市场；②无复利积累——demoscene 创作以单次炫技为核心，不产生可迭代的技术资产、用户网络或数据飞轮；③技术迁移价值极弱——XOR
    细胞自动机与 PC 喇叭驱动的技巧虽精巧，但不构成任何当代技术栈的组件。唯一可辨认的"价值"是创作者 HellMood 在 demoscene 社区中的声誉积累，但这属于文化资本而非商业资本。评分
    1.5 分，属于'昙花一现'的最低档位。
value_capture_layer: end_application
moat_impact: neutral
key_beneficiaries:
- HellMood
- Outline Demoparty
- Plex
competitive_casualty: []
market_opportunities:
- 创意编程与STEAM教育从业者可借鉴该程序以16字节极简代码同步生成分形图案与声音的算法设计思路，开发面向青少年的计算思维、细胞自动机与算法可视化教学工具
- AI音乐与生成艺术领域可参考其'视觉即听觉'的跨模态同步生成范式——同一段XOR运算数据同时驱动视觉图案与音频输出，为实时音画合成模型提供了一种极端高效的算法原型
- 边缘AI与TinyML研究者可从该程序'将显存作为计算空间、以硬件端口直驱输出'的极端资源利用策略中获得启发，探索在微控制器级设备上实现智能信号生成与感知融合的新路径
risk_matrix:
  regulatory: 无
  technological: 无
  competitive: 无
  ethical: 无
  additional: []
confidence:
  impact: low
  compound: low
  hype: low
actionable_insight: speculative_watch
object_insights:
- object_type: project
  name: wake up!
  canonical_name: wake up!
  url: https://hellmood.111mb.de/wake_up_16b_writeup.html
  positioning: 一款仅16字节的x86实模式DOS演示程序，利用视频内存作为计算空间生成无限谢尔宾斯基分形，并通过PC扬声器同步输出音频，实现视听联觉效果。
  technical_signal: 使用XOR替代ADD实现无进位加法，精确映射到初等元胞自动机规则60，仅16字节即可生成谢尔宾斯基三角形分形。
  adoption_signal: 于2026年5月在荷兰Ommen举行的Outline Demoparty上正式发布，获得业内人士在真实硬件上运行验证。
  ecosystem_relevance: 代表了sizecoding和demoscene文化的前沿，展示了极端算法密度与数学优雅性的完美结合，是16字节演示领域的重要突破。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 将视觉分形生成与同一计算产生的音频输出同步结合，实现真正的视听联觉效果，作者声称其深度远超其他微型演示程序。
  watch_reason: 这款16字节程序展示了极致的算法密度与数学优雅性；其将视频内存作为计算空间并实现视听联觉的创新方法，使其成为demoscene极简主义领域具有里程碑意义的作品，值得持续关注其技术影响与文化传承。
  risk_notes:
  - 不同BIOS版本和模拟器在内存中留下的初始数据存在差异，程序通过XOR与这些遗留数据进行运算，输出高度依赖运行环境。
  - 程序仅适用于x86实模式DOS环境，在现代操作系统上运行需要模拟器或虚拟机支持，使用场景受限。
  score: 7.0
  article_ids:
  - bc2197128103ef22
  evidence_snippets:
  - 该程序仅用 16 字节的 x86 实模式 DOS 汇编代码，利用视频内存作为计算空间绘制无限谢尔宾斯基分形。
  - 程序通过 `out 61h, al` 指令将计算得到的字节直接发送到 PC 扬声器端口，实现视听联觉效果。
  - 该程序在 2026 年 5 月荷兰 Ommen 举行的 Outline Demoparty 上正式发布。
- object_type: project
  name: M8trix
  canonical_name: M8trix
  url: null
  positioning: HellMood于2014年发布的仅8字节（后压缩至7字节）的x86实模式DOS演示程序，在屏幕上涂抹伪随机字母，是sizecoding领域的早期里程碑作品。
  technical_signal: 仅用8字节x86汇编代码即实现在DOS文本屏幕上涂抹伪随机字母的效果，展现了极致的代码压缩能力。
  adoption_signal: null
  ecosystem_relevance: 作为sizecoding历史的早期里程碑，M8trix为作者后续16字节作品"wake up!"的创作奠定了技术基础与创意方向。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 在2014年即实现仅8字节的伪随机字母涂抹效果，将sizecoding推向新的极限，体现了极简代码表现力的突破性。
  watch_reason: 作为HellMood在sizecoding领域的早期里程碑，M8trix以仅8字节展示了极简代码的无限可能，并与"wake up!"共同呈现了从2014到2026年间的技术演进脉络，对于研究demoscene文化传承具有参考价值。
  risk_notes:
  - 作为仅8字节的极简演示程序，其功能较为单一，仅限于屏幕涂抹伪随机字母，技术复杂度远低于后续同类作品。
  score: 4.0
  article_ids:
  - bc2197128103ef22
  evidence_snippets:
  - 作者 HellMood 的原创作品 "M8trix" 于 2014 年发布，仅用 8 字节（后压缩至 7 字节）即可在屏幕上涂抹伪随机字母。
  - 作者在开发 "wake up!" 时始终思考如何让 "M8trix" 的效果听起来更好，但本次声音部分优先于画面创作。
- object_type: project
  name: Rainbow Surf
  canonical_name: Rainbow Surf
  url: null
  positioning: Plex创作的16字节x86实模式DOS演示程序，以其出色的视觉效果重新激发HellMood的创作热情，是"wake up!"项目诞生的重要灵感来源。
  technical_signal: 在16字节代码限制内实现了令人印象深刻的视觉效果，展示了sizecoding中创意与技术的精妙平衡。
  adoption_signal: null
  ecosystem_relevance: 作为demoscene中16字节演示的标杆，Rainbow Surf直接激发了HellMood创作"wake up!"，体现了sizecoding创作者之间的技术传承。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为直接激发"wake up!"创作的灵感来源，Rainbow Surf代表了16字节sizecoding领域的重要参考标准，对于理解demoscene社区的创意传承和技术演进脉络具有独特价值。
  risk_notes:
  - 文章仅将其作为创作灵感来源提及，未说明具体技术实现方式和视觉效果细节，可独立分析的信息较为有限。
  score: 3.0
  article_ids:
  - bc2197128103ef22
  evidence_snippets:
  - Plex 的作品 "Rainbow Surf" 仅用 16 字节即实现了出色的演示效果，这促使作者重新翻出旧草图开始创作。
---

Released at the Outline Demoparty in May 2026, Ommen, NL

An exploration of algorithmic density in 16 bytes of x86 assembly.

Hey everyone. I learned programming as a kid on an old IBM PC with a monochrome green monitor over 30 years ago and always wanted to create a program for this system. I created well over 100 tiny intros in the last 15 years. Recently I was not too active but the fantastic "Rainbow Surf" from Plex in just 16 bytes motivated me to dig up some old dusty sketches again and get to work.

The creation of this program happened with the usual tinkering around. I was messing with cellular automaton for graphics and sounds and discovering sizecoding tricks. Actually: a) polymorphic asm instructions, like `add [bx+si],al`

which is `0x0000`

b) jumping into the middle of instructions to save bytes and reuse opcodes. In hundreds of tiny experiments, this one stuck out, just by the sound of it.

When I unfolded what's left and removed "the rest", I had a hard time to grasp what's really going on. I was scratching my head looking at the simple formula that remained after golfing many bytes away. I myself didn't expect that the explanation would go this deep for just these few bytes xD.

My original "M8trix" from 2014 already did smear pseudorandom letters across the screen (in 8 bytes, then in 7) and I always wondered how I could make it "sound good". But chronologically in the development of "wakeup", the sound was first. Since you "see what you hear" it doesn't really matter, but "16 bytes that turn Sierpinski sound into Matrix rain" would be a good subtitle =)

So, here are the **16 bytes** of x86 real-mode DOS assembly. When you run it, it uses the video memory as a calculation space to draw an infinite Sierpinski fractal, and at the same time bangs the speaker with that geometry.

```
int 10h ; 2 bytes
mov bh, 0xb8 ; 2 bytes
mov ds, bx ; 2 bytes
L:
lodsb ; 1 byte
sub si, byte 57 ; 3 bytes
xor [si], al ; 2 bytes
out 61h, al ; 2 bytes
jmp short L ; 2 bytes
```


The code starts with a standard BIOS interrupt: `int 10h`

. This sets up video mode 0, giving a 40x25 text mode grid. Then the data segment (`ds`

) is pointed to `0xb800`

, the memory address of the VGA/CGA text buffer.

When the BIOS clears the screen, it doesn't fill memory with absolute zeroes. Every character space is two bytes: the ASCII character and the color attribute. All 2,000 slots are set to ** 0x20** (space) and

`0x07`

The intertwine, the synesthesia goes far beyond what I found so far in other tiny intros. I would even go so far as to say it's revealing more mathematical secrets and relations than using iterated function systems for the "chaos game" without an RNG. Anyway, this time I want you to fundamentally understand the mathematics of what you hear. Not just "you do some operation here and then it sounds interesting".

To strip it down to pure math: assume a zeroed state instead of `0x20`

, use `add`

instead of `xor`

, and step forward **16 bytes** at a time. Assume the accumulator `al`

starts at **2**.

A DOS segment is exactly 65,536 bytes. Moving 16 bytes per step means exactly **4,096 steps** to traverse the segment (\( 65536 / 16 = 4096 \)). Then `si`

wraps cleanly back to `0x0000`

.

Adding up values between cells creates partial sums. Because 4,096 is a multiple of 256 (the 8-bit register size), the carryover aligns perfectly when the segment wraps, cleanly resetting `al`

to 2 at the start of each sweep.

The value follows a binomial sequence, scaled by 2:

Here is how the first 16 steps accumulate row by row:

| Pass \ Cell |
|---|

Now, back to combinatorics. By special laws, when doing modulo two, the Sierpinski triangle emerges. This specific bit is what gets banged into the speaker, while the other bits are ignored.

To separate the bitplanes, the fact that carry-free addition of bits is **XOR** is why it is there instead of `add`

.

Since the code starts with 2 (binary `00000010`

), **only bit 1 is toggled** between `0x00`

and `0x02`

. This perfectly maps to **rule 60** in elementary cellular automata:

Lucas's theorem guarantees this matches bit 1 from the addition table. See for yourself ('2' means bit 1 is set):

| Pass \ Cell |
|---|

Here is the trick: `out 61h, al`


Port `61h`

interfaces with the PC speaker. **Bit 1** pushes the speaker cone out (1) and pulls it in (0). The code computes the fractal via XOR, writes it to memory, and shoves that byte straight into the speaker port.

The 1s and 0s from the fractal create square waves that shift naturally in pulse width and frequency:

When played linewise, this creates self-similar, almost tempo-invariant bytebeats.

But it gets better: not only the text is output to the speaker but also the remaining bytes of the 64 kilobyte segment, which in this case also contains shadowed video ROM BIOS code, which is the secret ingredient to the punky and gritty sound that differs quite a bit from the expected Sierpinski line based overlayed rectangle wave bytebeat.

To recreate the M8trix effect, the cells themselves have to be splat across the screen in a way that the sound buffer is not too large and the screen is nicely sparsely filled with chars.

So the code doesn't step by 16. `sub si, byte 57`

plus `lodsb`

means it moves **-56 bytes** per iteration - going backwards.

56 doesn't divide 65,536 evenly. The code only hits offsets that are multiples of 8, taking **8,192 steps** and wrapping 7 times before resetting. This doubles the cycle length, halving the fundamental frequency. The sound drops one octave.

Moving back 56 bytes on an 80-byte wide screen is like moving forward 24 bytes (12 columns). Only 10 distinct columns are visited. The fractal isn't drawn as a solid image; it shears diagonally into 10 pillars of characters moving up the screen.

The scener *miragept* did a capture with this motivation:

"This is so awesome that I had to try running it in real hardware. The green text is a natural fit for MDA/Hercules, so I patched the address from 0xB800 to 0xB000 which is what MDA uses.

I don't have the exact IBM computer, but used a 286 with EGA card capable of emulating MDA/Hercules, and a real MDA monitor, so it's close enough.

Sorry for the low quality audio (the constant noise is from the machine itself). Please note that this monitor (IBM 5151) has a HUGE phosphor persistence, which I think hurts the presentation in this case because it's very fast."

My reply to him:

"HellMood @miragept: Thank you so much for this ♥ I'm happy to see it works like intended, even with a slightly different sound due to the byte change. Maybe it would indeed be a bit better to have it run slower, but what I found remarkable is, that the Sierpinski structure becomes actually more visible (towards the end) than in my version =)"

Emulators and different BIOS versions leave slightly different artifacts in RAM. Since the code XORs against whatever is there, the output is highly sensitive to the environment. Clearing the memory first would give a perfectly uniform output, but that costs precious bytes. Embracing the hardware's natural state is just part of the charm of sizecoding. Thanks for reading.

This text is handwritten.