---
title: Running a 28.9M parameter LLM on an $8 microcontroller
source: https://github.com/slvDev/esp32-ai
author:
- '[[boveyking]]'
published: '2026-07-25'
created: '2026-07-26'
manifest_dates:
- '2026-07-26'
description: 'Article URL: https://github.com/slvDev/esp32-ai Comments URL: https://news.ycombinator.com/item?id=49050512
  Points: 183 # Comments: 39'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 0c8bd8e44146054e
source_type: community_discussion
tldr: 开发者 slvDev 在仅售 8 美元的 ESP32-S3 微控制器上成功运行了一个 2890 万参数的 LLM，采用 Google Gemma 模型的
  Per-Layer Embeddings 技术将 2500 万参数存储在闪存中，推理速度约 9.5 token/秒，所有计算均在设备本地完成。
objective_summary: 开发者 slvDev 将 2890 万参数的语言模型部署在 ESP32-S3 微控制器（8 美元、512KB SRAM、8MB
  PSRAM、16MB 闪存）上，模型以 4-bit 量化占用 14.9MB。通过 Google Gemma 的 Per-Layer Embeddings 技术，将
  2500 万参数的嵌入表存储在慢速闪存中，每次推理仅加载约 450 字节，解决了微控制器 SRAM 容量限制。模型基于 TinyStories 数据集训练，仅能生成简短故事，速度约
  9.5 token/秒，代码和训练方法已在 GitHub 开源。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies:
  - Google
  - Microsoft Research
  technologies:
  - Per-Layer Embeddings
  - Gemma 3n
  - Gemma 4
  - llama2.c
  - TinyStories
  - 4-bit quantization
  key_people:
  - slvDev
  - Andrej Karpathy
  - Ronen Eldan
  - Yuanzhi Li
key_logic_flow:
- ESP32-S3 微控制器仅有 512KB SRAM，传统方案只能运行 26 万参数级别的模型，此前同类芯片上最大的语言模型为 26 万参数。
- slvDev 利用 Google Gemma 模型的 Per-Layer Embeddings 设计，将 2500 万参数的嵌入表存储在闪存中，每次推理仅从闪存读取约
  450 字节（约 6 行）到 SRAM 中。
- 模型参数总量为 2890 万，4-bit 量化后体积为 14.9MB，运行时纯计算速度约 9.7 token/s，端到端速度约 9.5 token/s，不依赖任何网络连接。
- 该模型在 TinyStories 数据集上训练，仅能生成简短连贯的故事，无法回答问题、遵循指令、编写代码或提供事实知识。
- 项目的固件、训练代码、消融实验和芯片实测数据均在 GitHub 仓库 slvDev/esp32-ai 中完整开源，包括作者自行发现并修复的参数统计 bug 的完整提交历史。
object_mentions:
- object_type: project
  name: esp32-ai
  canonical_name: slvDev/esp32-ai
  url: https://github.com/slvDev/esp32-ai
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 该项目在 ESP32-S3 微控制器上运行 2890 万参数的语言模型，推理速度约 9.5 token/秒，所有计算均在本机完成。
  - Per-Layer Embeddings 技术将 2500 万参数的嵌入表存储在闪存中，每次推理仅读取约 450 字节数据。
  - 模型基于 TinyStories 数据集训练，固件、训练代码和芯片实测数据在 GitHub 仓库中完整开源。
  article_id: 0c8bd8e44146054e
- object_type: model
  name: Gemma 3n / Gemma 4
  canonical_name: Google Gemma
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Google 的 Gemma 3n 和 Gemma 4 模型提出了 Per-Layer Embeddings 设计，使大模型能够适配小型设备的存储布局。
  - Per-Layer Embeddings 是该项目的核心技术基础，首次被应用在微控制器级别的芯片上。
  article_id: 0c8bd8e44146054e
- object_type: dataset
  name: TinyStories
  canonical_name: TinyStories
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - TinyStories 是由 Ronen Eldan 和 Yuanzhi Li（Microsoft Research）创建的合成短故事数据集，旨在让小模型也能学习生成连贯文本。
  - 该模型在 TinyStories 上训练，因此只能编写简短故事，无法回答问题或编写代码。
  article_id: 0c8bd8e44146054e
- object_type: project
  name: llama2.c
  canonical_name: karpathy/llama2.c
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Andrej Karpathy 的 llama2.c 项目让许多开发者相信可以用纯 C 语言训练和运行小型语言模型，该项目也受其启发。
  article_id: 0c8bd8e44146054e
extract_result: success
impact_score:
  score: 7.2
  reason: 该项目的技术含量很高：将 Per-Layer Embeddings 架构移植到微控制器，使可承载模型规模从 26 万参数跃升至 2890 万参数（提升约
    110 倍），在 8 美元芯片上实现 9.5 token/s 的本地推理。这是嵌入式 AI 领域的实质性突破，证明了极低成本硬件运行 LLM 的可行性。但扣分项在于：模型仅基于
    TinyStories 训练，能力局限于生成简短故事，无法问答/指令遵循/代码生成，实际产品化路径尚不清晰。短期内难以改变行业格局，但对于物联网、边缘计算方向的局部竞争格局有显著启发意义。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 在 512KB SRAM 的微控制器上通过闪存嵌入表按需加载实现百倍参数规模跃升
hype_assessment:
  level: low
  reason: 作者没有使用任何 PR 套话或夸张宣传，全文完全以技术细节和实测数据说明问题。特别值得肯定的是：公开了自己发现的参数统计 bug 及其修复过程，并在
    RESULTS.md 中记录了完整的消融实验和芯片实测数据（9.5 vs 9.7 tok/s 的端到端与纯计算速度差异）。同时对模型的能力边界（仅 TinyStories
    短故事、无法问答/编程/推理）做了诚实声明。不存在概念炒作。
information_entropy: high
domain_disruption:
  technical_innovation: 将 Google Gemma 的 Per-Layer Embeddings 设计移植到微控制器的三层存储层级架构（SRAM/PSRAM/Flash）上，将
    2500 万参数的嵌入表存储在慢速闪存中，每次推理仅按需读取约 450 字节（6 行）到 SRAM，从而突破微控制器 512KB SRAM 的容量瓶颈。这一架构创新的本质在于改变了边缘推理的内存
    Wall 约束——模型规模不再被 SRAM 大小硬限制，而是被闪存容量和读取延迟软约束替代。
  business_model: 打开了超低成本（8 美元硬件 + 零网络成本）离线 AI 推理的产品品类可能性。虽然当前模型能力仅限故事生成，但该架构可推广至其他对延迟不敏感、参数需求低的应用场景（如智能家居语音触发词识别、工业传感器异常描述、儿童教育玩具等），可能催生一批完全无云端的
    AI 硬件产品，颠覆现有端侧 AI 必须依赖网络或高端芯片的商业模式。
engineering_complexity: prototype
compound_value:
  score: 7.0
  reason: 从投资视角看，这是一项'架构突破'而非'产品突破'—它用 Per-Layer Embeddings 技术将 MCU 上可运行的 LLM 参数规模从
    26 万推到 2890 万（百倍提升），核心价值在于开辟了'超低成本边缘推理'这条新部署范式。复利逻辑在于：小模型蒸馏/压缩技术正在快速进步（Microsoft
    Phi、Google Gemma 2/3n、Llama 3.2 1B 等方向），每一轮小模型能力的提升都会直接放大这套部署架构的价值——28.9M 参数今天只能写
    TinyStories，但若未来 100M 参数模型可做基础问答，同样的 MCU 部署方案价值将指数级上升。此外，代码和训练方法完全开源，这意味着该方案可被芯片厂商（Espressif、ST、NXP
    等）吸收为标准参考设计，形成行业复利。缺点是当前模型能力极其有限（不能问答/编程/事实知识），且 MCU 闪存读取带宽和 SRAM 大小构成硬天花板，商业模式尚未形成。综合评分
    7.0：有潜力成为边缘 AI 部署的基础架构范式，但需等待小模型能力拐点到来。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Espressif Systems
- Google
- 开源边缘 AI 工具链 (llama2.c 等)
competitive_casualty:
- 高价边缘 AI 推理硬件（如 NVIDIA Jetson Nano/Google Coral 的轻量推理市场）
- 云端小模型推理 API 服务
market_opportunities:
- IoT设备厂商可借鉴 Per-Layer Embeddings 架构，在$8 ESP32微控制器上实现离线本地文本生成，用于智能家居、儿童故事机等场景，以极致低成本（<10美元物料）与云端依赖型产品形成差异化竞争
- 隐私合规高要求行业（医疗病历摘要、政务文档辅助、军工通信）可利用该完全离线推理方案规避数据出域风险，构建不依赖网络连接的AI终端设备
- 开源硬件与AI教育创业者可基于此项目开发嵌入式AI教学套件（ESP32 + 屏幕 + 开源代码），将AI模型推理的学习门槛从云端服务器降低到几美元的开发板，培育边缘AI开发者生态
risk_matrix:
  regulatory: 无显著监管风险。模型完全离线运行、不涉及数据传输与隐私合规问题；且基于开放数据集 TinyStories 训练，不涉及版权争议。
  technological: 模型能力严重受限——仅基于 TinyStories 数据集训练，只能生成简短故事，无法回答问题、遵循指令或提供事实知识，实际应用场景极其有限。推理速度约
    9.5 tok/s，远低于用户可接受的交互体验阈值。Per-Layer Embeddings 本质是对闪存读取速度的妥协，未来更强大的低成本边缘 SoC（如集成NPU的MCU）出现后，该方案的架构优势可能被快速替代。
  competitive: 边缘AI芯片领域竞争激烈：Google Coral、Qualcomm Snapdragon、NVIDIA Jetson 等方案虽成本更高但模型能力远超此方案。一旦主流厂商推出$10-20价位且支持更大模型推理的芯片，本项目的技术窗口期可能快速关闭。此外，llama2.c
    等开源项目已在持续降低边缘推理门槛，差异化空间有限。
  ethical: 正面社会影响为主——完全本地处理增强用户隐私保护，无数据外泄风险。模型能力弱、输出内容可控，几乎不存在偏见歧视或深度伪造风险。
  additional:
  - 闪存写入寿命有限（ESP32-S3 闪存典型擦写次数约1万-10万次），若需OTA更新模型可能导致硬件使用寿命问题
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: esp32-ai
  canonical_name: slvDev/esp32-ai
  url: https://github.com/slvDev/esp32-ai
  positioning: 在 8 美元 ESP32-S3 微控制器上运行 2890 万参数 LLM 的开源项目，利用 Per-Layer Embeddings
    将嵌入表存于闪存，突破 SRAM 容量瓶颈。
  technical_signal: 利用 Google Gemma 的 Per-Layer Embeddings 技术将 2500 万参数嵌入表存储在闪存中，每次推理仅读取约
    450 字节数据到 SRAM。
  adoption_signal: 固件、训练代码、消融实验和芯片实测数据在 GitHub 完整开源，包含作者自行发现并修复参数统计 bug 的完整提交历史。
  ecosystem_relevance: 将 Google Gemma 系列的 Per-Layer Embeddings 设计应用于嵌入式领域，拓展了 TinyStories
    数据集在极端边缘设备上的部署边界。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 这是目前已知在极低成本微控制器上运行的最大参数语言模型，将嵌入式 LLM 参数规模从 26 万提升至 2890 万，Per-Layer
    Embeddings 在边缘设备上的架构创新值得关注。
  risk_notes:
  - 模型仅能生成简短故事，无法回答问题或编写代码，实际应用场景非常有限。
  - 推理速度约 9.5 token/秒，在实时交互场景中存在性能瓶颈，不适合对延迟敏感的应用。
  score: 7.0
  article_ids:
  - 0c8bd8e44146054e
  evidence_snippets:
  - 该项目在 ESP32-S3 微控制器上运行 2890 万参数的语言模型，推理速度约 9.5 token/秒，所有计算均在本机完成。
  - Per-Layer Embeddings 技术将 2500 万参数的嵌入表存储在闪存中，每次推理仅读取约 450 字节数据。
  - 模型基于 TinyStories 数据集训练，固件、训练代码和芯片实测数据在 GitHub 仓库中完整开源。
- object_type: project
  name: llama2.c
  canonical_name: karpathy/llama2.c
  url: null
  positioning: 用纯 C 语言实现 LLM 推理的开源项目，证明了在资源受限环境下运行语言模型的技术可行性。
  technical_signal: 证明了用纯 C 语言进行 LLM 推理与训练的可行性，为嵌入式环境部署小型语言模型奠定了基础范式。
  adoption_signal: null
  ecosystem_relevance: null
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 esp32-ai 等嵌入式 LLM 项目的基础启发来源，代表了最小化 LLM 推理栈的技术方向，在边缘 AI 部署需求增长背景下具有持续参考价值。
  risk_notes:
  - 项目属于早期概念验证性质，主要作为教学和启发工具，实际产品化路径尚不明确。
  score: 3.0
  article_ids:
  - 0c8bd8e44146054e
  evidence_snippets:
  - Andrej Karpathy 的 llama2.c 项目让许多开发者相信可以用纯 C 语言训练和运行小型语言模型，该项目也受其启发。
---

Open to Work · 𝕏 slvDev · LinkedIn

This is a 28.9 million parameter language model that generates text on an ESP32-S3, a microcontroller that costs about $8. It runs on the chip itself, with nothing sent to a server, and it writes each word to a small screen wired to the chip at roughly 9 tokens per second. The last language model people ran on a chip like this had 260 thousand parameters, so this one holds about a hundred times more. It fits because most of the model lives in flash instead of RAM, using an idea from Google's Gemma models called Per-Layer Embeddings.

| Parameters | 28.9M stored (25M of them in a flash lookup table) |
| Chip | ESP32-S3, about $8, with 512KB SRAM, 8MB PSRAM and 16MB flash |
| Speed | about 9.5 tok/s end to end (9.7 tok/s of pure compute) |
| Connectivity | none, everything runs on the device |
| Model size | 14.9MB at 4-bit |

A microcontroller has very little fast memory. The ESP32-S3 gives you 512KB of SRAM. Normally the whole model has to be reachable from there, which keeps you stuck with tiny models, and that is why the previous model on a chip like this had only 260 thousand parameters.

The way around it is to stop putting the model in fast memory at all. Most of a language model's parameters sit in an embedding table, which the model reads from rather than computes on. So you can leave that 25 million row table in slow flash and pull only the few rows each token needs, about 450 bytes, while the small part that does the actual work stays in fast memory. The large model then costs almost nothing to run, because you never load most of it. It just sits in flash and gets sampled a little at a time.

That idea is Google's Per-Layer Embeddings, from Gemma 3n and Gemma 4. Here it runs on the memory layout of a microcontroller instead of a phone or a GPU. As far as I can tell, nobody had tried it on a chip this small.

```
SRAM (fast, tiny) the "thinking" core, used on every token
PSRAM (medium) the output head and working memory
FLASH (huge, slow) the 25M-param table, about 6 rows read per token (~450 B)
```


The model was trained on TinyStories, so it writes short, simple stories and mostly keeps them coherent. It will not answer questions, follow instructions, write code, or know facts. That limit comes from the small part of the model that does the reasoning, and the memory trick does not change it. What is interesting here is the architecture, fitting a large model onto a tiny chip, rather than what a 28.9 million parameter model can say.

The firmware, the wiring, and the flashing steps live in
`firmware/esp32_llm/README.md`

. The training,
ablation, and quantization code is in `src/`

and `experiments/`

. The full method,
the ablations, and the on-chip measurements are written up in
`RESULTS.md`

.

TinyStories is the dataset this trains on: short synthetic stories simple enough that a small model can still learn to write coherently (Ronen Eldan and Yuanzhi Li, Microsoft Research, arXiv:2305.07759). The other half is Per-Layer Embeddings, Google's design from the Gemma models, which is what lets a big model fit on a small chip.

Andrej Karpathy's llama2.c is why a lot of people, me included, believe you can train a tiny language model and run it in plain C at all. This grew out of that.

I left the messy history in the repo on purpose. That includes a bug I found in my
own parameter accounting, which had inflated an early number, and the corrected
result that followed once I fixed it. The commit history and `RESULTS.md`

show where
the numbers moved and why.