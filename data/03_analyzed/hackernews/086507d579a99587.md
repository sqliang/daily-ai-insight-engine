---
title: Solo – a .so loader for static Linux binaries
source: https://github.com/pg83/solo
author:
- '[[zX41ZdbW]]'
published: '2026-08-18'
created: '2026-08-19'
manifest_dates:
- '2026-08-19'
description: 'Article URL: https://github.com/pg83/solo Comments URL: https://news.ycombinator.com/item?id=49354613
  Points: 143 # Comments: 148'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 086507d579a99587
source_type: community_discussion
tldr: SoLo 是一个针对 Linux 静态二进制文件的 .so 加载器：让完全静态链接的 musl 可执行文件在运行时加载宿主机器上已安装的 glibc 版
  Vulkan/OpenGL GPU 驱动，无需容器、AppImage 或第二份 libc。项目附带端到端 Vulkan 演示程序，已在多款 GPU 上验证。
objective_summary: pg83 发布的 SoLo 是一个开源 .so 加载器，为静态 Linux 二进制提供 dlfcn 风格 API，内含自研 ELF
  加载器（x86-64 与 aarch64）以及在 musl 之上实现的 glibc ABI 桥。它让静态可执行文件在运行时加载宿主机上未修改的 glibc 版
  Vulkan/OpenGL 驱动，仓库附带端到端 Vulkan 计算着色器演示，能将结果写入 PNG。CI 在每次提交时用 x86-64 和 aarch64 两个架构加载
  Debian 一千个最常安装软件包的两千多个共享库进行回归验证。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - pg83
  technologies:
  - musl
  - glibc
  - Vulkan
  - OpenGL
  - SPIR-V
  - ELF
  - dlfcn
  - TLS
  key_people: []
key_logic_flow:
- SoLo 让完全静态链接的 musl 可执行文件在运行时通过自带 ELF 加载器加载宿主机上基于 glibc 的 GPU 驱动，无需容器或第二份 libc。
- 项目提供 lib/dlfcn.cpp、lib/elf_loader.cpp、lib/glibc_shim.cpp、lib/glibc_stubs.cpp 四个核心模块，分别负责
  dlopen/dlsym API、ELF 段映射与重定位、glibc ABI 适配器以及未支持函数的显式兜底。
- 仓库附带的 Vulkan 演示程序会加载宿主机未修改的 Vulkan ICD、创建设备与计算管线、调度内嵌 SPIR-V 着色器，并通过静态链接的 libpng
  输出 PNG 图片。
- 演示已在 AMD radv、radeonsi、Intel、NVIDIA 以及 Asahi Linux 上的 Apple M1 上测试通过，CI 会加载 Debian
  最常安装的一千个软件包的二千多个共享对象进行验证。
- 与 gcompat、Detour、Cosmopolitan Libc、ClickHouse 用户态加载器等方案相比，SoLo 选择在 musl 上实现 glibc
  ABI 桥，保证进程中只有一套 libc 和一套 TLS 世界。
- 该项目还用于构建 Shitty 终端模拟器，支持 x86-64 与 aarch64 两种架构，并同时提供预编译二进制与源码构建两种使用方式。
object_mentions:
- object_type: project
  name: SoLo
  canonical_name: SoLo
  url: https://github.com/pg83/solo
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - SoLo 是一个针对 Linux 静态二进制文件的 .so 加载器，为完全静态的 musl 可执行文件提供 dlopen/dlsym 能力，以加载宿主机上已有的
    glibc 版 GPU 驱动。
  - 仓库附带端到端 Vulkan 演示，静态可执行文件加载宿主机未修改的 Vulkan 驱动并运行计算着色器，将结果写入 PNG 文件。
  article_id: 086507d579a99587
- object_type: project
  name: IX
  canonical_name: IX
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章称其使用 IX 这一 source-first 构建系统来产出完全静态的 Linux 二进制文件，但文中未给出 IX 的官网或仓库地址。
  article_id: 086507d579a99587
- object_type: product
  name: Shitty
  canonical_name: Shitty
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章提到 SoLo 正是用来构建 Shitty 发布二进制的方案，并将其描述为一款速度极快的终端模拟器。
  article_id: 086507d579a99587
- object_type: project
  name: gcompat
  canonical_name: gcompat
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章将 gcompat 描述为发行版级的 glibc API 兼容层，用于在 musl 上运行预编译的 glibc 程序，但它无法给完全静态的 musl 进程提供动态加载器。
  article_id: 086507d579a99587
- object_type: project
  name: Cosmopolitan Libc
  canonical_name: Cosmopolitan Libc
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章指出 Cosmopolitan Libc 的 cosmo_dlopen() 采用与 Detour 相同的双运行时方案，引导宿主的 ELF 解释器与 libc
    后委托宿主的 dlopen() 加载目标 DSO。
  article_id: 086507d579a99587
- object_type: project
  name: Detour
  canonical_name: Detour
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章介绍 Detour 会引导系统的 ld-linux 并允许多个 C 运行时共存，而 SoLo 走相反路线，自行映射所需 DSO 并把其 glibc 导入翻译到
    musl 之上。
  article_id: 086507d579a99587
- object_type: project
  name: ClickHouse experimental userspace dynamic loader
  canonical_name: ClickHouse userspace dynamic loader
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 文章称 ClickHouse 的实验性用户态动态加载器目前自行映射 ELF 对象，但止步于加载 glibc，其通往 CUDA 等真实系统库的路径是引导系统
    ld.so 并保留第二份 libc 运行时。
  article_id: 086507d579a99587
extract_result: success
impact_score:
  score: 5.5
  reason: 该工具精准解决了静态二进制分发生态中一个长期存在的痛点——完全静态链接的 musl 程序无法在运行时加载宿主机 glibc 版 GPU 驱动。它的核心价值是让'单文件部署'从无
    GPU 场景扩展到需要 Vulkan/OpenGL 的场景，可能影响游戏、终端模拟器、CI 工具等静态分发生态的构建方式。但受众仍然局限于静态二进制开发者这一细分群体，且已有
    AppImage/容器等替代路径，并非范式转移级事件。综合评分依据：工程完成度高（CI 双架构回归 2100+ 共享库、多 GPU 厂商验证），但生态影响面中等，故给予
    5.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 在不引入第二份 libc 的前提下让静态二进制加载 glibc 驱动的实现方式是否真的可靠、以及 glibc ABI 桥的覆盖面是否够广
hype_assessment:
  level: low
  reason: 仓库提供端到端 Vulkan 计算着色器演示、readelf 可验证的无动态链接证明、CI 在每次提交时对 Debian 前一千个软件包的 2100+
    共享对象做双架构回归测试，并在 AMD/Intel/NVIDIA/Apple M1 多平台验证。文中的 'blazingly fast' 属于对自家终端模拟器的调侃而非对项目的宣传。无
    '颠覆'、'革命性' 等 PR 滥用词汇，属于实打实的技术干货，故判定为低水分。
information_entropy: high
domain_disruption:
  technical_innovation: 在 musl 之上实现 glibc ABI 桥是其本质创新——通过 ABI 正确的适配器（如 malloc@GLIBC_2.2.5
    → musl 运行时）将 glibc 构建的 DSO 导入 musl 静态进程，且刻意不加载 glibc，保证进程内只有一套 libc 和一套 TLS 世界；配合自研
    ELF 加载器（x86-64/aarch64）对 TLS/TLSDESC、IFUNC、RELRO、版本化符号解析的完整处理，以及静态 provider 注册表（如用进程内已链接的
    Wayland 函数满足依赖），系统性解决了静态二进制与宿主动态库之间的 ABI 鸿沟。
  business_model: 对商业 Linux 软件分发模式有潜在重塑力：ISV 可将游戏、图形应用、GPU 计算工具打包为单一静态可执行文件直接分发，摆脱对容器镜像、AppImage/Flatpak
    运行时和发行版依赖管理的依赖，降低分发与支持成本；对 IX 这类 source-first 静态构建系统也是生态补强。目前项目本身为开源 MIT 风格工具，尚无直接商业模式，但为'单文件
    GPU 应用'这一分发范式提供了基础设施。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: SoLo 解决的是 Linux 生态中一个持久且真实存在的硬问题：完全静态的 musl 二进制无法 dlopen 宿主机上 glibc 链接的
    GPU 驱动，这直接卡住了静态部署模型在 GPU/边缘场景的落地。工程含金量高（自研 x86-64/aarch64 ELF 加载器 + glibc ABI
    桥 + TLS/TLSDESC/IFUNC 处理），CI 用 Debian 2000+ 共享库做回归验证，技术壁垒真实，且随边缘 AI 推理、静态分发（IX/单文件交付）趋势具备成长性。但作为单维护者开源项目，市场盘子小众，无网络效应与商业化闭环；且存在
    gcompat、Cosmopolitan Libc、ClickHouse 用户态加载器等竞争路线，尚未确立标准地位。属于『有潜力成为细分赛道基础设施、但需持续验证』区间，因此给出
    5.5 分而非更高。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- NVIDIA
- AMD
- Intel
- musl/Alpine 生态
- IX 静态构建工具链
competitive_casualty:
- gcompat 等 glibc 兼容层
- AppImage/Snap 类 GPU 打包方案
- ClickHouse 用户态加载器（路线竞争）
- 部分边缘场景的容器化 GPU 部署栈
market_opportunities:
- 创业者可基于 SoLo 的"静态二进制 + 运行时加载宿主 glibc 共享库"模式，构建跨发行版的单文件 GPU 应用分发工具链，替代 AppImage/Flatpak
  的驱动打包痛点，覆盖终端、图像处理、3D 渲染等桌面场景
- 面向 AI 边缘部署，可将推理/图形应用编译为静态单文件并复用宿主已有的 Vulkan/ROCm/CUDA 驱动栈，大幅降低终端用户安装 SDK 与 GPU 驱动的门槛，适合边端一体机与瘦客户端产品
- 将 SoLo 的 ELF 加载器与 glibc ABI 桥抽象为可嵌入 Zig/Rust/musl 静态链接工具链的通用库，让"静态可执行文件加载任意 glibc
  插件生态"成为发行版打包与容器减负的通用方案
risk_matrix:
  regulatory: 开源工具本身合规风险低，但"静态二进制运行时加载宿主任意 .so"的模式可能绕过发行版/企业的依赖来源审计与软件包审查，在金融、军工、医疗等受监管行业部署时或触发供应链安全合规评估
  technological: glibc ABI 桥建立在 musl 之上，依赖对版本化符号、TLS/TLSDESC、IFUNC、RELRO 等机制的精确模拟，glibc
    内部实现演进可能导致脆弱性；目前仅支持 x86-64/aarch64，且未覆盖的 glibc 函数只能显式报错而非完整兼容
  competitive: 同赛道存在 gcompat、Detour、Cosmopolitan Libc、ClickHouse 用户态加载器等替代方案；项目由个人维护（pg83），长期投入与生态支持不确定，且该能力可能被
    Zig/Rust/musl 等主流工具链原生吸收而边缘化
  ethical: 安全与供应链维度存在风险：该模式会削弱对运行时加载依赖的来源可控性与完整性校验，宿主驱动被劫持或替换时静态二进制难以自证安全；同时"隐蔽加载宿主库"的技术路径可能被用于规避软件包审查或植入不易追踪的依赖
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: SoLo
  canonical_name: SoLo
  url: https://github.com/pg83/solo
  positioning: 面向 Linux 静态二进制文件的 .so 加载器，让完全静态的 musl 可执行文件在运行时加载宿主机上基于 glibc 的 GPU
    驱动，无需容器、AppImage 或第二份 libc。
  technical_signal: 自带 ELF 加载器（x86-64 与 aarch64）并在 musl 之上实现 glibc ABI 桥，支持 ELF TLS、TLSDESC、IFUNC、RELRO
    与递归加载依赖，进程内只保留一套 libc 与 TLS 世界。
  adoption_signal: CI 在每次提交时通过 SoLo 加载 Debian 最常安装的一千个软件包的两千多个共享库做回归验证，并已适配 Alpine、Fedora、Ubuntu
    三种发行版构建与测试。
  ecosystem_relevance: 直接解决静态 musl 二进制无法 dlopen 宿主 glibc GPU 驱动的痛点，与 gcompat、Detour、Cosmopolitan
    Libc 等方案形成差异化，为 Linux 图形生态的静态分发打开新路径。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: SoLo 用独特方案打通静态分发与宿主 GPU 驱动之间的鸿沟，并以 Vulkan 端到端演示和大规模 CI 回归证明可行性；若其稳定成熟，可能重塑
    Linux 上静态二进制分发图形应用的生态格局，值得持续跟踪其架构演进与社区采纳。
  risk_notes:
  - glibc ABI 覆盖面取决于手工 shim 与兜底 stub，未支持的函数一旦被驱动调用会直接失败，长期维护成本较高。
  - 项目以个人作者维护为主，社区与治理结构尚不成熟，生态采纳速度存在不确定性。
  score: 8.0
  article_ids:
  - 086507d579a99587
  evidence_snippets:
  - SoLo 是一个针对 Linux 静态二进制文件的 .so 加载器，为完全静态的 musl 可执行文件提供 dlopen/dlsym 能力，以加载宿主机上已有的
    glibc 版 GPU 驱动。
  - 仓库附带端到端 Vulkan 演示，静态可执行文件加载宿主机未修改的 Vulkan 驱动并运行计算着色器，将结果写入 PNG 文件。
- object_type: project
  name: IX
  canonical_name: IX
  url: null
  positioning: source-first 构建系统，用于产出完全静态的 Linux 二进制文件，是 SoLo 项目构建静态可执行文件的底层工具链。
  technical_signal: 以源码优先方式组织构建输入，规避对系统动态库的隐式依赖，从而稳定地产出完全静态的 Linux 可执行文件。
  adoption_signal: 被 SoLo 项目实际用作静态发布二进制的构建工具链，佐证其具备产出真实可分发静态可执行文件的能力。
  ecosystem_relevance: null
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: IX 作为 SoLo 背后的静态构建工具链被实际用于产出可分发二进制，但其公开信息有限且缺乏独立官网或仓库地址，需进一步观察其能力边界与社区基础。
  risk_notes:
  - 文章未提供 IX 的官网或仓库地址，公开资料不足，难以独立验证其功能特性与成熟度。
  score: 5.0
  article_ids:
  - 086507d579a99587
  evidence_snippets:
  - 文章称其使用 IX 这一 source-first 构建系统来产出完全静态的 Linux 二进制文件，但文中未给出 IX 的官网或仓库地址。
- object_type: product
  name: Shitty
  canonical_name: Shitty
  url: null
  positioning: 一款强调极致速度的 Linux 终端模拟器，采用 SoLo 构建静态发布二进制，可在不依赖容器或 AppImage 的情况下分发给任意带
    Vulkan 驱动的 Linux 环境。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 追求极速体验的 Linux 终端用户
  - 偏好单文件静态分发软件的开发者
  product_signal: 作为 SoLo 的第一个实际生产用例，其发布二进制由 SoLo 静态构建并借助宿主 Vulkan 驱动运行，直接验证了该加载器在真实产品中的可用性。
  market_signal: null
  differentiation: 以完全静态的单文件形态提供终端体验，省去容器或 AppImage 的分发包袱，是多数传统终端模拟器少有的分发优势。
  watch_reason: Shitty 是 SoLo 技术路线的首个真实落地产品，其分发方式能否被社区接受将直接影响 SoLo 的推广价值，值得跟踪其发布形态与用户反馈。
  risk_notes:
  - 文章未给出 Shitty 的官网或仓库地址，产品完成度与可用性尚无法独立核实。
  score: 5.0
  article_ids:
  - 086507d579a99587
  evidence_snippets:
  - 文章提到 SoLo 正是用来构建 Shitty 发布二进制的方案，并将其描述为一款速度极快的终端模拟器。
---

**Ship one musl-linked executable. At runtime, load the user's existing
glibc-linked GPU driver. No container, no AppImage, and no second libc in the
process.**

Static binaries are a wonderfully boring way to deploy software on Linux: one
file, no dependencies, nothing to break. We build ours with
IX, a source-first build system for producing
fully static Linux binaries. The boredom ends the moment the application needs
the GPU: Vulkan and OpenGL drivers are supplied by the host as shared objects,
usually built against glibc, and a fully static musl binary cannot normally
`dlopen()`

them.

SoLo crosses that boundary. It provides a `dlfcn`

-style source API backed by
its own ELF loader (x86-64 and aarch64) and a glibc ABI bridge implemented on
top of musl.
The result is still one ordinary static executable, but it can use the graphics
driver already installed on the machine.

The repository includes an end-to-end Vulkan proof: a fully static executable loads the host's unmodified Vulkan driver, runs a compute shader, and writes the result to a PNG. Tested on AMD radv, radeonsi, Intel, and NVIDIA GPUs under Linux, and on Apple M1 under Asahi Linux.

**The host keeps the hardware-specific code. You ship everything else.**

And not on a demo's word alone: on every commit, CI loads the shared libraries of the 1,000 most-installed Debian packages — over 2,100 host objects — through SoLo, on both x86-64 and aarch64.

Grab the prebuilt binary — no clone, no toolchain, any Linux with a Vulkan
driver installed (`mesa-vulkan-drivers`

is enough):

```
curl -LO https://github.com/pg83/solo/releases/latest/download/vulkan-x86_64
chmod +x vulkan-x86_64
./vulkan-x86_64 hello.png
```

`vulkan-aarch64`

is the same demo for arm64 machines. The command discovers
the distro-installed Vulkan ICD in the usual way and produces a 512×512 RGBA
image. This is how we build the
Shitty release binaries—a blazingly
fast terminal emulator, BTW! To force a particular driver:

```
./vulkan-x86_64 --driver /usr/share/vulkan/icd.d/radeon_icd.x86_64.json radeon.png
./vulkan-x86_64 --driver /usr/share/vulkan/icd.d/lvp_icd.json lavapipe.png
```

ICD manifest names vary slightly between distributions. Passing no `--driver`

lets the embedded Khronos loader perform its normal discovery.

You can verify that the executable itself is not dynamically linked:

```
readelf -lW ./vulkan-x86_64 | grep INTERP # no output
readelf -dW ./vulkan-x86_64 # "There is no dynamic section"
```

Or build the same demo from source, with Python 3 and a C/C++ compiler in
`PATH`

:

```
git clone https://github.com/pg83/solo.git
cd solo
./build vulkan
./vulkan hello.png
```

This is not a toy call to `vkCreateInstance`

. The demo:

- enters the statically linked Khronos Vulkan loader;
- loads the host's Vulkan ICD and its non-glibc dependencies through SoLo;
- creates a device, storage buffer, descriptor set, and compute pipeline;
- dispatches a checked-in SPIR-V shader;
- maps the result and writes it through statically linked libpng.

The complete example is in `bin/vulkan`

, and the Vulkan program
itself is in `main.cpp`

.

```
┌──────────────────── fully static executable ────────────────────┐
│ │
│ application → embedded Vulkan loader → SoLo dlopen/dlsym │
│ ├─ x86-64 ELF mapper │
│ └─ glibc ABI → musl │
│ │ │
└───────────────────────────────────────────┬─────────────────────┘
│ maps at runtime
▼
system Mesa/Vulkan ICD.so + DSOs
```


`elf_loader.cpp`

maps ELF segments, walks `DT_NEEDED`

,
resolves versioned symbols, applies x86-64 relocations, supports ELF TLS and
TLSDESC, materializes IFUNCs, applies RELRO, and runs initializers. Dependencies
that are themselves ELF DSOs are loaded recursively.

glibc is deliberately *not* loaded. Imports such as `malloc@GLIBC_2.2.5`

are
resolved by `glibc_shim.cpp`

to ABI-correct adapters over
the process's existing musl runtime. Unsupported glibc functions have unique
generated stubs that fail loudly with the exact symbol and version if they are
ever called, instead of silently corrupting the process.

Because musl sizes its synchronization objects to the glibc ABI of each
architecture, the bridge does not shadow them: a `pthread_mutex_t`

a driver
creates is used in place. A lock is therefore one lock for both the loaded DSO
and the static executable that may share it, and glibc's static recursive and
error-check initializers are adopted on first use.

Before loading a DSO from disk, SoLo checks its static provider registry. This
lets an application satisfy a dependency—Wayland, for example—with functions
already linked into the executable. `LD_LIBRARY_PATH`

and
`DL_ELF_LIBRARY_PATH`

are honored for libraries outside the standard system
directories.

The interesting pieces are small enough to read:

`lib/dlfcn.cpp`

—`dlopen`

,`dlsym`

, errors, and static providers`lib/elf_loader.cpp`

— ELF mapping, symbols, relocations, and TLS`lib/glibc_shim.cpp`

— implemented glibc ABI adapters`lib/glibc_stubs.cpp`

— explicit fallbacks for the rest of the ABI

The default target builds the standalone archive:

`./build`

The published `./dlfcn`

symlink points to the resulting `libdlfcn.a`

. Include
`lib/dlfcn.h`

, link the archive into a musl-static application,
and ordinary `dlopen()`

/`dlsym()`

calls are redirected to SoLo. The source tree
is intentionally self-contained and suitable for copying into another static
build graph.

```
./build test # load an Arch glibc DSO closure in the smoke test
./build vulkan_test # build the static demo and verify a native Lavapipe PNG
```

CI performs the native build and test on Alpine/musl with GCC, Fedora with GCC, and Ubuntu with Clang. The Vulkan test installs each distribution's own Lavapipe package; it does not run the driver from an Arch sysroot.

Every build input for the standalone Vulkan executable is vendored under
`bin/vulkan`

. `build.py`

compiles those sources directly: upstream
CMake, Meson, configure, and Make build systems are not invoked.

## Vendored versions

- musl 1.2.5 (
`0784374d561435f7c787a555aeab8ede699ed298`

) - LLVM runtimes 15.0.7: libc++, libc++abi, libunwind, and compiler-rt builtins
(
`8dfdcc7b7bf66834a761bd8de445840ef68e4d1a`

) - Vulkan Headers 1.4.357 (
`e3b1eec08173d6b825cd3ac88c885a63b621504a`

) - Vulkan Loader 1.4.357 (
`5f157b62e333c63260d05d81bf66faa216ab0fb8`

) - zlib 1.3.2 (
`da607da739fa6047df13e66a2af6b8bec7c2a498`

) - libpng 1.6.50 (
`2b978915d82377df13fcbb1fb56660195ded868a`

)

License files are retained beside the corresponding sources. `shader.inc`

is
the checked-in SPIR-V form of `shader.comp`

, so no shader compiler is required.

In the general case, only SoLo lets a static application tell the dynamic
loader: "for this system DSO's `libwayland`

dependency, use the symbols already
linked into my executable." This lets the application embed the newest
`libwayland`

instead of targeting the oldest version available on every
supported system.

And the boundary between the two worlds is not a thin dlsym shim — it carries the parts that make foreign code actually behave:

**C++ exceptions cross it in both directions.**A throw in the static world unwinds through glibc-compiled frames into a glibc`catch`

, and the other way around, destructors running on both sides: the guests'`_Unwind_*`

imports are bound to the one unwinder in the executable, so there is a single exception machinery in the process instead of two fighting ones.**All four TLS models, without wrappers or code patching.**General- and local-dynamic through`__tls_get_addr`

, TLSDESC through its custom-ABI resolver, and initial-exec — whose GOT slots are plain thread-pointer-relative offsets no loader can intercept — served from a surplus arena that rides in the executable's own static TLS, so one process-wide offset is valid in every thread and unmodified musl does the per-thread layout.Global-scope interposition,`ld.so`

's binding semantics, not an approximation.`RTLD_DEEPBIND`

,`DT_SYMBOLIC`

, symbol versioning with the unversioned-provider compatibility rule, lazy PLT binding with the argument registers preserved through the resolver, GNU and SysV hash lookups, ifunc resolvers handed their hwcaps,`/etc/ld.so.cache`

.**Cross-world introspection.**`backtrace()`

walks static and glibc frames alike and names both through one`dladdr`

;`dl_iterate_phdr`

,`dladdr1`

, and the`link_map`

facade let unwinders and profilers see every image; the file-backed mappings keep real paths in`/proc/self/maps`

for debuggers.**The stateful corners of glibc, for real.**`getcontext`

/`makecontext`

/`swapcontext`

in assembly against glibc's`mcontext`

layouts on both architectures, the pre-2.34 pthread ABIs, GNU obstacks, the fortified`_chk`

family, and the inline-stdio ABI — musl's`FILE`

is deliberately laid out so glibc's inlined`putc_unlocked`

compiles against it — down to`_IO_2_1_stdout_`

resolving to musl's own stream.

Every one of these is exercised by a conformance battery compiled against
real glibc headers at `-O2`

, and by loading every shared object of the
thousand most-installed Debian library packages in CI, on x86-64 and
aarch64.

- gcompat is a distribution-level
glibc API shim for running prebuilt glibc binaries on musl. Its loader stub
re-executes the program through musl's dynamic linker with
`libgcompat.so`

preloaded; using it from a musl program requires linking that shared library or adding it to the loaded DSO's`DT_NEEDED`

. It does not give a fully static musl process a dynamic loader. SoLo's self-contained model is stronger: the executable embeds both the ELF loader and ABI bridge, loads unchanged host DSOs without a system compatibility package, preserves the versions of their glibc imports, and lets unused unsupported functions remain behind symbol-specific, fail-loud stubs instead of blocking the entire DSO. - Detour bootstraps the system's
`ld-linux`

and allows multiple C runtimes to coexist. SoLo takes the opposite route: it maps the required DSOs itself and translates their glibc imports onto musl, so a second libc and its TLS state never enter the process. - Cosmopolitan Libc's
`cosmo_dlopen()`

follows the same split-runtime scheme as Detour, with all of its advantages and drawbacks: it bootstraps the host's ELF interpreter and libc, then delegates loading the target DSO to the host's`dlopen()`

. - ClickHouse's experimental userspace dynamic loader
currently maps ELF objects itself, but stops short of loading glibc. Its
proposed path to real-world system libraries such as CUDA is Detour-like:
bootstrap the system's
`ld.so`

, keep a second libc runtime, and swap the musl/glibc thread pointer at every boundary. SoLo instead implements the glibc ABI over the host's musl runtime and can satisfy DSO dependencies from providers already linked into the static executable. - graphics.gd's
`musl`

+`dlopen`

experiment follows the same split-runtime model as Detour: an embedded helper brings in the host's glibc loader, and assembly trampolines switch between musl and glibc TLS around foreign calls. This leaves two independent TLS worlds: every boundary crossing needs a trampoline, and a callback implemented in musl cannot be passed safely to glibc code because glibc invokes it while its own TLS is active. SoLo keeps a single musl TLS world instead. - Flatpak, AppImage, and containers solve the problem by hiding a small Linux
distribution inside or around your program. This works in roughly the same
way that moving house solves a missing power adapter. The result is a huge
blob full of duplicated libraries, mounts, namespaces, extraction tricks,
and runtime indirection—all of which make profiling, debugging, and basic
introspection worse. Shipping a distro because you need one system
`.so`

is not portability. SoLo ships one normal, inspectable executable and borrows the only component that genuinely belongs to the host: its hardware driver.

- Linux only, on x86-64 and aarch64. The loader, the TLSDESC and lazy-PLT
resolvers, and the initial-exec arena cover both; the glibc symbol
inventories are generated per architecture, so
`printf@GLIBC_2.2.5`

on one is`printf@GLIBC_2.17`

on the other without a single translation rule in the code; - focused on real Mesa/Vulkan ICD dependency closures, and driven by the top 1000 Debian library packages by popcon votes: the 885 of them that ship glibc-linked shared objects — about 2100 objects — all load through SoLo in CI on both architectures. Loading is the floor, not the claim: calls into the symbols the bridge still stubs abort loudly, and dev/abi-demand.txt is the remaining work, ranked by how many installations demand each symbol;
- a load-once runtime (
`dlclose`

succeeds but does not unload an image); - supporting all four TLS models. Initial-exec variables are placed in a
16 KiB surplus arena that rides in the executable's own static TLS, so one
process-wide offset is valid in every thread without patching musl. The one
restriction: threads created
*before*a`dlopen`

see zero-initialized TLS for the modules it loaded, so load initial-exec libraries before spawning the threads that use them. An initial-exec module that does not fit the arena fails to load with an error naming the image and the byte counts; - explicit about missing ABI coverage: an unimplemented glibc call aborts and names itself.

The goal is to turn the hard wall between “fully static” and “uses the system GPU” into a finite, testable compatibility layer. The Vulkan PNG is the first proof that the wall has a door.