---
title: The git history command
source: https://lalitm.com/post/git-history/
author:
- '[[turbocon]]'
published: '2026-07-14'
created: '2026-07-14'
description: 'Article URL: https://lalitm.com/post/git-history/ Comments URL: https://news.ycombinator.com/item?id=48901010
  Points: 285 # Comments: 165'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 894f8896e67e52f5
source_type: community_discussion
tldr: Git 2.54 和 2.55 版本分两次发布了实验性命令 `git history`，包含 fixup、reword、split 三个子命令，可在不破坏工作树的前提下原子性地修改历史提交、拆分提交内容并自动更新所有相关分支，无需切换版本控制系统即可获得类似
  jj 的部分核心能力。
objective_summary: Git 核心团队在 2.54（2025 年 4 月）和 2.55（2025 年 6 月）版本中分两次发布了实验性命令 `git
  history`，包含 reword、split 和 fixup 三个子命令。该命令允许用户直接修改历史提交的消息（reword）、将暂存修改折叠到旧提交中（fixup）以及将单个提交拆分为两个（split），所有操作均以原子方式执行——拒绝任何可能产生冲突的操作以避免工作树进入半损坏状态。与
  jj（Jujutsu）等替代品相比，git history 功能范围更小（不支持冲突传递、操作日志撤销等功能），但作为核心 Git 发行版的一部分，用户无需安装额外工具即可使用。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - git
  - jj
  key_people: []
key_logic_flow:
- Git 2.54（2025 年 4 月）引入了 git history 的 reword 和 split 子命令，2.55（2025 年 6 月）引入了 fixup
  子命令。
- '`git history fixup` 可将暂存的修改折叠到目标历史提交中，并自动变基所有包含该提交的本地分支，覆盖范围超过 `git rebase --update-refs`。'
- '`git history reword` 允许直接编辑历史提交的提交信息，仅操作提交图而不影响索引或工作树。'
- '`git history split` 通过逐块交互式选择将单个提交拆分为两个，避免使用复杂的 git rebase 操作。'
- 所有 git history 子命令均以原子方式执行，拒绝任何可能产生冲突的操作，确保工作树不会进入半损坏状态。
- git history 不支持冲突传递和操作日志撤销——这些是 jj 的关键差异化功能——但作为 Git 内置命令无需用户切换工作流程。
extract_result: success
object_mentions:
- object_type: project
  name: git history
  canonical_name: git history
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Git 在 2.54（2025 年 4 月）和 2.55（2025 年 6 月）两个版本中分两次发布了实验性命令 git history，包含 reword、split
    和 fixup 三个子命令。
  - git history 的三个子命令均以原子方式执行，拒绝任何可能产生冲突的操作，确保工作树永远不会进入半损坏状态。
  - git history 会自动更新所有包含被修改提交的本地分支，比 git rebase --update-refs 覆盖范围更广，后者仅移动正在变基范围内的引用。
  article_id: 894f8896e67e52f5
- object_type: project
  name: jj
  canonical_name: jj (Jujutsu)
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - jj 作为 Git 的替代方案，在业界获得广泛讨论，但作者多次尝试将其融入工作流程后仍选择回归 Git。
  - jj 拥有操作日志支持轻松撤销、将工作副本建模为一次提交、以及在变基过程中携带冲突等 git history 尚未实现的能力。
  - git history 在功能上不及 jj 强大，但作为 Git 内置命令无需用户切换整个工作流程即可获得部分类似收益。
  article_id: 894f8896e67e52f5
impact_score:
  score: 5.5
  reason: Git 作为全球开发者基础设施，任何改进都有广泛影响。但本特性是实验性的（有明确限制：不支持 merge commit、拒绝冲突操作、功能弱于
    jj），并非范式转移。它解决了 rebase -i 工作中的核心痛点（半损坏状态、多分支自动变基），属于重要的渐进式改进，改变局部工作流体验，但尚未到改变行业竞争格局的程度。综合评分为
    5.5 分（中等偏上）。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 原子性历史修改与自动变基所有下游分支，无需切换 jj 工作流
hype_assessment:
  level: low
  reason: 文章非常克制且诚实。明确标注 experimental、列出所有已知限制（不支持 merge commit、不支持冲突传递、功能弱于 jj）、附带了清晰的前后对比图示和文档来源。没有任何'颠覆''革命性'等
    PR 式夸大用语，属于实打实的工程改进介绍。判定为低炒作。
information_entropy: high
domain_disruption:
  technical_innovation: Git 核心发行版内建原子性历史提交修改能力。三个子命令（fixup/reword/split）采用'拒绝冲突操作+原子执行'的设计哲学，确保不会让仓库处于半损坏状态，且自动变基所有包含目标提交的本地分支，解决了传统
    rebase -i 工作流中需要手动跟踪多个分支的核心痛点。无需切换 jj 工作流即可获得部分历史编辑能力。
  business_model: 无
engineering_complexity: prototype
compound_value:
  score: 7.0
  reason: Git 作为全球软件开发的基础设施，此次改进直接回击了 jj 等替代方案的核心卖点——安全的历史修改能力。长期复利效应体现在三个层面：1) 原子性设计（拒绝冲突操作）意味着可以在不破坏工作流的前提下逐步扩展能力边界，文档已暗示未来可能支持
    first-class conflicts，为更强大的功能预留了空间；2) fixup 自动变基所有相关下游分支，解决了多分支协作的核心痛点，显著降低 Git
    的认知门槛；3) 命令内置在核心 Git 发行版中，零摩擦采纳——升级 Git 的开发者自动获得这些能力，网络效应极强。虽然目前是实验性功能且不支持 merge
    commits（限制了部分重度用户的采用），但方向正确。这不是革命性创新，而是对 Git 护城河的持续加固——让数百万开发者留在 Git 生态的理由又多了一条，同时削弱了
    jj 等替代品通过解决历史修改痛点来获取用户的核心叙事。从 VC 视角看，这是在 developer tooling 基础设施层的防守性创新，价值在于保护而非创造新市场。
value_capture_layer: end_application
moat_impact: strengthens_monopoly
key_beneficiaries:
- GitHub
- GitLab
- Microsoft
competitive_casualty:
- jj (Jujutsu)
- Sapling (Meta)
market_opportunities:
- 技术教育者或技术写作者可围绕 `git history` 的 fixup/reword/split 三个子命令制作从 rebase -i 迁移到原子性历史修改的实战教程，填补社区对新功能系统化学习内容的空白
- IDE 和 Git GUI 工具（如 VS Code、JetBrains、GitKraken）可为 `git history` 设计可视化交互界面（如拖拽式 commit
  拆分、点选式 fixup），降低实验性功能的使用门槛并提升产品竞争力
- 企业 DevOps 团队可将 `git history` 纳入内部 Git 工作流标准化规范，替代容易造成仓库半损坏状态的传统 rebase 操作，降低新成员上手成本和操作事故率
risk_matrix:
  regulatory: 无
  technological: 当前为实验性功能，不处理合并提交且无法解决冲突状态（能力弱于 jj），功能可能在后续版本中变更、废弃或行为不兼容
  competitive: jj 等新一代版本控制系统在操作日志回滚、工作副本即提交、冲突状态传递等核心能力上仍领先于 `git history`；IDE 内置
    Git 工具可独立实现类似 UX 而无需依赖内核命令
  ethical: 无
  additional: []
confidence:
  impact: high
  compound: medium
  hype: high
actionable_insight: monitor
object_insights:
- object_type: project
  name: git history
  canonical_name: git history
  url: https://git-scm.com/docs/git-history
  positioning: 实验性的 Git 内置命令，通过 fixup、reword、split 三个子命令提供原子性的历史提交修改能力，无需安装额外工具即可使用。
  technical_signal: fixup 子命令支持将暂存修改折叠到目标历史提交中，并自动变基所有包含该提交的本地分支，覆盖范围超越 git rebase
    --update-refs。
  adoption_signal: 发布时曾引发短暂关注，但此后社区讨论有限，作者认为该命令带来的价值被严重低估。
  ecosystem_relevance: 作为 Git 核心发行版的一部分，用户无需切换版本控制系统即可获得类似 jj 的部分历史编辑能力，降低了工作流改进的门槛。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Git history 在不切换工作流程的前提下提供了原子性的历史修改能力，fixup 自动更新所有分支的设计是 Git 生态的重要进步，且官方文档暗示未来可能支持冲突传递，值得持续跟踪。
  risk_notes:
  - 当前不支持冲突传递和操作日志撤销，功能范围远小于 jj。
  - fixup 子命令在存在合并提交时无法工作，对部分 Git 使用场景构成限制。
  - 该命令仍为实验性状态，API 和行为可能在后续版本中发生变化。
  score: 8.0
  article_ids:
  - 894f8896e67e52f5
  evidence_snippets:
  - Git 在 2.54（2025 年 4 月）和 2.55（2025 年 6 月）两个版本中分两次发布了实验性命令 git history，包含 reword、split
    和 fixup 三个子命令。
  - git history 的三个子命令均以原子方式执行，拒绝任何可能产生冲突的操作，确保工作树永远不会进入半损坏状态。
  - git history 会自动更新所有包含被修改提交的本地分支，比 git rebase --update-refs 覆盖范围更广，后者仅移动正在变基范围内的引用。
- object_type: project
  name: jj
  canonical_name: jj (Jujutsu)
  url: https://github.com/jj-vcs/jj
  positioning: 作为 Git 替代方案被广泛讨论的版本控制工具，提供操作日志支持轻松撤销、将工作副本建模为一次提交以及在变基中携带冲突等高级能力。
  technical_signal: jj 将冲突视为一等公民，可在变基过程中携带冲突状态并允许稍后处理，这是 git history 尚未实现的关键差异化能力。
  adoption_signal: 尽管在业界获得大量讨论，本文作者多次尝试融入工作流程后仍选择回归 Git，反映出从 Git 迁移的实际阻力。
  ecosystem_relevance: jj 的创新理念正在反向影响 Git 生态——git history 的设计目标即是缩小与 jj 的功能差距，推动 Git
    本身向前演进。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: jj 作为 Git 替代方案的核心理念正在影响 Git 本身的演进方向，其冲突传递、操作日志撤销等设计是否会被 Git 逐步采纳值得持续跟踪。
  risk_notes:
  - 从 Git 迁移到 jj 的工作流程切换成本较高，部分用户多次尝试后仍选择回归 Git。
  score: 5.0
  article_ids:
  - 894f8896e67e52f5
  evidence_snippets:
  - jj 作为 Git 的替代方案，在业界获得广泛讨论，但作者多次尝试将其融入工作流程后仍选择回归 Git。
  - jj 拥有操作日志支持轻松撤销、将工作副本建模为一次提交、以及在变基过程中携带冲突等 git history 尚未实现的能力。
  - git history 在功能上不及 jj 强大，但作为 Git 内置命令无需用户切换整个工作流程即可获得部分类似收益。
---

Working with lots of changes in parallel on git can be painful. You end up
juggling branches and commits, and running scary `rebase -i`

commands that can
leave your tree in a half-broken state if you so much as sneeze.

`jj`

, an alternative to `git`

, gets discussed a
lot these days (1,
2,
3,
4) and is
often pitched as a solution. While I’m very sold on the *problems* `jj`

is
trying to solve, the way it solves them hasn’t quite hit home with me. Every 3
months, for the last 1.5 years, I try it out for a few days, really trying to
make it part of my workflow but eventually I give up and go back to git.1

That’s where `git history`

comes in. It’s an **experimental**
command that arrived across two
releases,
2.54
(April, `reword`

and `split`

subcommands) and
2.55
(June, `fixup`

subcommand). It got a flurry of attention on each release day,
and then, as far as I can tell, not much community discussion since. Which is a
shame, because IMO it already delivers several of the benefits people tout for
`jj`

without needing to switch your whole workflow. And the cool thing is that
it’s part of the core git distribution, so you can try it without installing
anything.

There are three subcommands: `fixup`

, `reword`

and `split`

.

## fixup

`git history fixup`

fixes an old commit that has something wrong in it, then autorebases *all* your
branches to match.

You stage the fix as usual with `git add`

, then run `git history fixup <commit>`

to fold those staged changes into the target commit. It’s like a
`git commit --fixup`

plus an autosquash rebase but with the extra magic that it
*also* updates any other branch which contained that commit.

That last part goes further than `git rebase --update-refs`

, which only moves
refs sitting inside the range you’re actively rebasing. `git history`

instead
finds and rewrites *every* local branch descended from the commit (while also
having an option to limit it to only the current branch). On the other hand it
does *not* work in the presence of merge commits which, for some usages of git,
is going to be a dealbreaker.

Here’s how it works in practice:

Before, with a fix staged for `B`

:

After `git history fixup B`

:

`B*`

is `B`

with the fix folded in. Rewriting a commit gives it a new hash, so
`C`

and `D`

are automatically re-created on top as `C*`

and `D*`

, and the
`feat-1`

and `feat-2`

branch tips move with them.

The most important property, common to all three commands, is that it’s atomic: it never leaves your tree in a half-broken state. It manages this by refusing any operation that could produce a conflict.

To be clear, this is strictly *less* powerful than `jj`

. `jj`

treats conflicts as first class so
it can carry a conflicted state through a rebase and let you sort it out later.
`git history`

doesn’t do this yet *but* the
docs leave the door open:

“This limitation is by design as history rewrites are not intended to be stateful operations. The limitation can be lifted once (if) Git learns about first-class conflicts.”


So basically, this limitation may change in the future; excited to see if it does!

## reword

`git history reword`

updates the commit message on an old commit and automatically rebases everything
on top. This is very useful for going back and
fixing commit messages when the design shifts as you iterate.

`git history reword <commit>`

opens your editor with that commit’s existing
message. You edit it, save, and the rest of the stack is rebuilt on top with the
branches following along. It’s exactly like fixup but for commit messages
instead of the tree contents.

Because it only changes a message, `reword`

(like `split`

later) never touches
your index or working tree at all; it works purely on the commit graph. So both
let you rewrite a commit on a branch you don’t have checked out without
disturbing whatever you’re in the middle of.

Before:

After `git history reword B`

:

*Only B’s message changes, but that still gives it a new hash, so C is
rebuilt on top as C* and feat-1 follows along.*




## split

`git history split`

takes one commit and splits it into two, interactively picking what you care
about from each. It’s the equivalent of `git add -p`

, but
without needing gymnastics with `git rebase`

. I’ve found this to be the most
specialized of the three, but invaluable when I need it.

Specifically, `git history split <commit>`

drops you into a hunk-by-hunk prompt
over that commit’s diff. The hunks you keep make up the first commit, the rest
fall into the second.

Before, with `B`

bundling two unrelated changes:

After `git history split B`

:

`B`

becomes `B1`

and `B2`

, and `C`

is rebuilt on top of the pair as `C*`

.

## Conclusion

Judging by how many people are using `jj`

, I do think there’s still some key
mental shift which I’m not yet making. And to be clear, `git history`

doesn’t
close the full gap: `jj`

still gives you an
operation log with easy undo,
models your working copy as a commit,
and can
carry conflicts through a rebase,
none of which this is trying to do.

But for now, `git history`

is a *big* step forward in adopting many of the
pieces that attract people to `jj`

, and it’s already in the tool I use every
day. And the way the documentation is written makes me hopeful that more
improvements will be coming in upcoming releases!