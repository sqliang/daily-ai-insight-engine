---
title: Every Byte Matters
source: https://fzakaria.com/2026/06/01/every-byte-matters
author:
- '[[ingve]]'
published: '2026-06-03'
created: '2026-06-04'
description: 'Article URL: https://fzakaria.com/2026/06/01/every-byte-matters Comments
  URL: https://news.ycombinator.com/item?id=48382382 Points: 231 # Comments: 113'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 29df3f8f559b339f
source_type: community_discussion
tldr: 数据结构布局（AoS vs SoA）通过影响CPU缓存行利用率，最高可带来30倍性能差异。
objective_summary: Farid Zakaria 于2026年6月1日发表技术文章，通过实测数据说明数据结构的内存布局（Array of Structs
  vs Struct of Arrays）如何影响CPU缓存行利用率和各级缓存命中率，在随机访问模式下工作集大小决定了性能所在缓存层级，Struct of Arrays
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - CPU cache
  - cache line
  - Struct of Arrays (SoA)
  - Array of Structs (AoS)
  - prefetcher
  - pointer-chasing
  key_people:
  - Farid Zakaria
  - Jeff Dean
key_logic_flow:
- CPU缓存行大小为64字节，读取单字节时硬件会填满整个缓存行；L1d缓存约35 KiB/核心，可容纳约560条缓存行。
- Array of Structs（AoS）布局下，每个64字节缓存行仅包含一个Monster结构体，遍历时每次只用到其中的1字节is_alive字段，缓存行利用率低。
- Struct of Arrays（SoA）布局将相同字段连续存储，一条缓存行可容纳64个Monster的is_alive布尔值，利用率提升64倍。
- 在顺序访问模式下，CPU预取器可提前加载后续缓存行，SoA与AoS差异较小；在随机访问模式下，工作集总大小直接决定数据落在哪个缓存层级。
- Monster结构体从64字节翻倍到128字节时，512个实例的工作集即从L1d（~3ns）溢出到L2缓存（~11ns），延迟增加约3.7倍。
- 指针追逐基准测试显示，不同结构体大小均呈现随工作集增长而逐级跳升的阶梯式延迟曲线，较大结构体曲线向左偏移，更早触及更高延迟层级。
impact_score:
  score: 5.5
  reason: 该文章通过实测基准数据，生动展示了数据结构内存布局（AoS vs SoA）对CPU缓存利用率的影响，最高可达30倍的性能差异。虽然AoS/SoA对比是计算机体系结构中已有数十年积累的经典知识，并非新发现，但文章在教育意义和工程实践参考价值上贡献显著——尤其是对AI推理场景（KV缓存布局、注意力机制的内存访问模式）有直接启示。对于不熟悉数据导向设计（Data-Oriented
    Design）的开发者群体，这篇文章可能带来认知转变；但对于系统编程和AI框架领域专家而言，这是已知原理的现代硬件验证。综合来看，属于高质量的教育性工程文章，但并非行业范式转移级别的冲击。
sentiment: neutral
developer_sentiment:
  tone: excited
  primary_focus: SoA布局在随机访问模式下可带来数十倍性能提升，直接启示LLM推理的内存优化方向
hype_assessment:
  level: low
  reason: 文章没有使用'颠覆'、'革命性'等PR词汇来包装内容。作者提供了完整的实验环境（lscpu输出、缓存行大小64B、逐级延迟数据）、具体的基准测试方法和实测对比数据（12组延迟数字），逻辑链条完整。30x的改进数字有明确前提限定（struct为1KiB时），未夸大适用范围。全文基调是工程教育而非商业宣传。
information_entropy: high
domain_disruption:
  technical_innovation: 该文章本身未提出新的技术突破，而是系统性地量化验证了数据导向设计（Data-Oriented Design）原则在现代CPU上的实际效果。其核心价值在于：通过精确的阶梯式延迟曲线，揭示了工作集大小如何决定性能所在缓存层级这一规律，为LLM推理引擎的KV缓存布局优化、Attention计算的访存模式设计提供了工程层面的量化参考基准。
  business_model: 无直接商业模式影响，但间接地：此类访存优化直接关系AI推理的每token成本。如果开发者将SoA等布局原则应用于推理引擎优化，可在同硬件上实现更高吞吐，降低AI服务的单位推理成本，从而影响AI基础设施的定价竞争力。
engineering_complexity: production_ready
compound_value:
  score: 3.5
  reason: 本文是对已有数十年历史的CPU缓存与数据结构布局优化知识的系统性实测验证，并非新技术或新产品的发布。SoA vs AoS模式在游戏引擎(ECS架构)、列式数据库、科学计算、SIMD向量化等领域早已大规模应用，文章本身不创造新的复利价值或竞争壁垒。但其提供的精确基准数据(30倍性能差异、缓存层级跳变曲线)可作为工程团队的优化参考，间接推动更高效的底层系统实现。从VC视角看，这是一篇优质技术科普，但不足以构成改变投资格局的'事件'。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- Apache Arrow
- DuckDB
- Unity
competitive_casualty:
- 忽视底层性能优化的传统SaaS厂商
market_opportunities:
- 数据库引擎、游戏引擎和高频交易系统等数据密集型场景的开发者可基于 SoA 布局原则对核心数据结构进行重构，实现数倍至数十倍的随机访问性能提升
- 可开发自动化性能分析工具，在编译或 CI 阶段检测数据结构的缓存行利用效率，识别 AoS 布局中因字段稀疏访问导致的缓存浪费，并建议 SoA 重构方案
- 面向系统程序员和数据工程师推出'缓存感知编程'专题培训或技术咨询服务，帮助团队系统性地将硬件缓存层次纳入架构设计考量
risk_matrix:
  regulatory: 无
  technological: 盲目使用 SoA 布局可能导致代码可读性和可维护性下降；未来 CPU 微架构变更（如缓存行大小增大到 128 字节或引入新型预取机制）可能改变当前优化假设，需持续跟踪硬件演进
  competitive: 无。AoS 与 SoA 布局优化属于成熟的计算机体系结构知识，不属于特定公司的竞争壁垒
  ethical: 无。该事件不涉及偏见歧视、隐私侵犯或就业冲击等伦理问题
  additional: []
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
---

# Every byte matters

Published 2026-06-01 on Farid Zakaria's Blog

I have spent a large portion of my career working in Java. In that time, you get used to huge classes. New functionality? Just add a new method and field to the class.
The cost of each new field is rarely considered. Performance is often considered from a *classic computer science* perspective by considering asymptotic analysis of the algorithms and data structures in-use.

Turns out that even within a growth scale for your algorithm, such as a simple for-loop `O(N)`

, time can vary dramatically if we have a little deeper understanding of the underlying hardware.

First, let’s understand our current machine. Let’s take a peek at our *cache line* and *page* sizes.

```
$ lscpu | grep -i cache
L1d cache: 352 KiB (10 instances)
L1i cache: 640 KiB (10 instances)
L2 cache: 10 MiB (5 instances)
L3 cache: 12 MiB (1 instance)
$ getconf LEVEL1_DCACHE_LINESIZE
64
```


The *instances* number is a reflection of how the caches are shared amongst CPUs. If I had 10 CPUs, each one has their own `L1d`

cache, whereas two of them would share an `L2`

cache.

Our cache line size is **64 bytes**.

```
┌─────────────────────────────────────────────┐
│ 64 bytes │
│ byte 0 byte 1 byte 2 ... byte 63 │
└─────────────────────────────────────────────┘
```


When you read **a single** byte from memory, the hardware will fill the surrounding 64 bytes into the cache line.
The idea being that data is often temporal and spatially located, meaning data is often accessed near each other and close in time to each other.

We can reference Jeff Dean’s famous “Latency numbers every programmer should know”, however a quick recap with the values from our particular machine is the following:

```
┌──────────────────────────────────────────────────────────────┐
│ CPU Core │
│ ┌───────────┐ │
│ │ Registers │ < 1 ns │
│ └─────┬─────┘ │
│ ▼ │
│ ┌───────────┐ │
│ │ L1d Cache │ ~35 KiB/core ~4-5 cycles ~1-2 ns │
│ │ │ ~560 cache lines │
│ └─────┬─────┘ │
│ ▼ │
│ ┌───────────┐ │
│ │ L2 Cache │ ~2 MiB/core-pair ~12-15 cycles ~4-5 ns │
│ │ │ ~32,000 cache lines │
│ └─────┬─────┘ │
│ ▼ │
│ ┌───────────┐ │
│ │ L3 Cache │ 12 MiB shared ~30-40 cycles ~10-15 ns │
│ │ │ ~196,000 cache lines │
│ └─────┬─────┘ │
│ ▼ │
│ ┌───────────┐ │
│ │ DRAM │ ~100-200 cycles ~60-100 ns │
│ │ │ │
│ └───────────┘ │
└──────────────────────────────────────────────────────────────┘
```


The sizes for each cache, is the number returned by `lscpu`

divided by the number of cores or instances; i.e. 352 KiB ÷ 10 instances = ~35 KiB.
We then determine the number of cache lines by dividing this number by 64; i.e. 35 KiB ÷ 64 bytes = 560 cache lines.

How does this all matter ? 🤔

Let’s consider an example where we want to iterate over a single struct `Monster`

and pull out the `boolean is_alive`

to filter them.
We create our struct, and in this particular example we need 64 bytes to represent a single Monster.

```
struct Monster {
uint32_t id; // 4 bytes
float x, y, z; // 12 bytes
float vx, vy, vz; // 12 bytes
int32_t hp; // 4 bytes
int32_t attack; // 4 bytes
int32_t defense; // 4 bytes
uint8_t is_alive; // 1 byte
uint8_t team; // 1 byte
char name[22]; // 22 bytes
}; // total: 64 bytes
```


If we had an array of Monsters and we iterate over them, the cache line would fill up like so.
Each cache line would fill with a single monster, and we would fetch only the `is_alive`

byte.

This is often referred to as “Array of Structs”.

```
cache line 0 cache line 1
┌──────────────────────────────┐ ┌──────────────────────────────┐
│ id0 x0 y0 z0 vx0 vy0 vz0 hp0 │ │ id1 x1 y1 z1 vx1 vy1 vz1 hp1 │
│ atk0 def0 alive0 team0 name0 │ │ atk1 def1 alive1 team1 name1 │
│ ▲ │ │ ▲ │
└─────────────┼────────────────┘ └────────────┼─────────────────┘
│ │
need this need this
```


If we instead normalize the data such that each field is in it’s own list, we can pack the cache lines much tighter.

```
cache line 0
┌───────────────────────────────────────────────────────────────┐
│alive0 alive1 alive2 alive3 alive4 alive5 ... alive62 alive63 │
│ ▲ ▲ ▲ ▲ ▲ ▲ ▲ ▲ │
└──┼──────┼──────┼──────┼──────┼──────┼──────────┼───────┼──────┘
│ │ │ │ │ │ │ │
└──────┴──────┴──────┴──────┴──────┴──────────┴───────┘
all 64 in one fetch
```


```
// SoA layout
struct Monsters {
uint32_t *ids;
float *xs, *ys, *zs;
float *vxs, *vys, *vzs;
int32_t *hps;
int32_t *attacks;
int32_t *defenses;
uint8_t *is_alives; // packed contiguously
uint8_t *teams;
char (*names)[22];
};
```


This type of layout is referred to as “Struct of Arrays”.

How much of an impact can this have?

We can observe up to **30x** improvements when the Monster struct is 1KiB 🤯

The delta is less observable when the struct is small because multiple Monster structs can still be fetched within a single cache-line.

This data access is incredibly hot though. Your CPU pre-fetcher knows it’s going sequentially and fetches the next cache line before you need it. You never actually have to wait for the memory to be fetched.

What about random access patterns?

Not all access patterns are sequential. Hash maps, trees, graph traversal, and pointer-heavy data structures jump to unpredictable locations. The CPU can’t prefetch what it can’t predict. With random access, the CPU needs the entire array to be present in the cache in order to avoid stalls due to memory lookup.

This means the **total size of your collection** determines your performance tier.

| Monsters | Working Set (64B) | Latency (64B) | Working Set (128B) | Latency (128B) |
|---|---|---|---|---|
| 512 | 32 KiB | ~3 ns | 64 KiB | ~11 ns |
| 4,096 | 256 KiB | ~11 ns | 512 KiB | ~13 ns |
| 32,768 | 2 MiB | ~29 ns | 4 MiB | ~43 ns |
| 65,536 | 4 MiB | ~49 ns | 8 MiB | ~65 ns |
| 131,072 | 8 MiB | ~163 ns | 16 MiB | ~162 ns |

Doubling the struct from 64B to 128B doubles the working set for the same number of monsters, pushing the data into slower cache levels. At just 512 monsters, a 64B struct fits in L1d at ~3 ns — but a 128B struct has already spilled to L2 at ~11 ns.

We can observe this with a pointer-chasing benchmark. We allocate N monster-sized nodes, wire them into a random order, and chase pointers. Each hop lands at an unpredictable address, defeating the CPU’s prefetcher entirely.

Rather than graph it logarithmically, which I find sometimes is easy to miss, I have included a zoomed in graph. We can see that all struct sizes hit the same *staircase* like pattern as they go through the various cache levels however the larger struct sizes are *shifted left*, meaning they hit the increase earlier.

This means for random access patterns, if you can keep tight control on your total working set size, you can drastically affect the time.

Knowing your struct and working set size can make a substantial difference.

Improve this page @ f42923f

The content for this site is
CC-BY-SA.