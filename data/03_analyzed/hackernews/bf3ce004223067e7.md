---
title: 'Autolith: A programming agent with a live runtime'
source: https://www.lambda-symbolics.com/autolith
author:
- '[[vismit2000]]'
published: '2026-08-20'
created: '2026-08-20'
manifest_dates:
- '2026-08-20'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: bf3ce004223067e7
source_type: community_discussion
tldr: Autolith 是一款运行在终端中的编程代理，可直接在仓库内读写文件、搜索工作区并运行命令与测试，内置可实时检查、修改和扩展的 Common Lisp（SBCL）运行时。v0.40.1
  支持 Linux、macOS 及多个 BSD 平台，默认模型为 gpt-5.6-sol，兼容多种模型提供商。
objective_summary: Autolith 由 lambda-symbolics 发布，是一款运行在终端的编程代理，直接工作在用户仓库中，提供文件系统、shell
  与搜索工具，并能通过 rlm.complete 处理超出上下文窗口的语料。其核心特性是活运行时：代理与其工具运行在同一 Common Lisp 镜像内，可在运行中替换函数并通过私有镜像提交持久化，配合追加式变更日志、工作区议程与崩溃恢复机制。v0.40.1
  覆盖 Linux x86-64、Linux aarch64、macOS arm64 等平台，二进制发行自带 SBCL 2.6.6，默认模型为 gpt-5.6-sol，支持
  ChatGPT Codex、Grok、Anthropic、OpenRouter、Mistral AI 等提供商。
event_type: framework_tools
epistemic_status: pr_statement
entities:
  companies:
  - lambda-symbolics
  - OpenAI
  - xAI
  - Nous Research
  - Fireworks AI
  - Anthropic
  - OpenRouter
  - Mistral AI
  technologies:
  - Common Lisp
  - SBCL
  - rlm.complete
  - fff
  key_people: []
key_logic_flow:
- Autolith 是一个终端编程代理，直接工作在用户仓库中，可读写文件、搜索工作区、运行命令与测试并保持项目上下文。
- 代理与其所有工具运行在同一个 Common Lisp 镜像中，内置 SBCL 2.6.6 运行时，可在不重启的情况下检查、替换和扩展运行中的函数。
- 超出上下文窗口的语料通过 rlm.complete 被实例化为内容寻址对象，模型只接收其标签、大小与摘要，在显式调用和 token 预算内驱动受限的 Lisp
  环境。
- 每次变更都记录在追加式的 mutation journal 中，可被演练、丢弃或固化为私有镜像提交，提交含清单与可执行 Lisp 重放脚本并保存在独立 Git
  历史。
- 对话、记忆、议程、私有镜像提交、代际快照、worker 镜像与恢复镜像等不同状态按各自生命周期分离存放，而非统一存入单一数据库。
- 安装支持 Nix 与二进制两种路径，v0.40.1 覆盖 Linux、macOS、FreeBSD、NetBSD 与 OpenBSD，默认模型为 gpt-5.6-sol，并可通过
  --immutable 进入只读检查模式。
object_mentions:
- object_type: product
  name: Autolith
  canonical_name: Autolith
  url: https://github.com/lambda-symbolics/autolith
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Autolith 是一款运行在终端中的编程代理，直接工作在用户仓库中，可读写文件、搜索工作区并运行命令与测试。
  - Autolith 的 Common Lisp 镜像内含提供商客户端、终端界面、工具注册表、会话状态与持久记忆，代理与工具运行在同一进程。
  - Autolith v0.40.1 支持 Linux x86-64、macOS arm64、FreeBSD 等平台，二进制发行自带 SBCL 2.6.6，Nix
    为推荐安装路径。
  article_id: bf3ce004223067e7
- object_type: project
  name: rlm.complete
  canonical_name: rlm.complete
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - rlm.complete 将超出上下文窗口的语料实例化为内容寻址对象，模型只接收其标签、大小与摘要，并在显式调用和 token 预算内驱动受限的 Lisp
    环境。
  - 使用 rlm.complete 处理约 3.1 MB 的源码语料时，模型在 3 分 15 秒内列出全部 83 个条件类并按其所属的 14 个子系统分组，语料本身从未进入提示词。
  article_id: bf3ce004223067e7
- object_type: project
  name: fff
  canonical_name: fff
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - Autolith 通过 fff 这一快速 Rust 搜索库在进程内执行工作区搜索，并保持索引常驻而非每次查询都启动新的搜索进程。
  article_id: bf3ce004223067e7
- object_type: project
  name: SBCL
  canonical_name: SBCL
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - Autolith 的二进制发行自带 SBCL 2.6.6、Lisp 依赖与原生命令行工具，运行在同一个可被代理实时检查、测试和扩展的 Common Lisp
    运行时中。
  article_id: bf3ce004223067e7
- object_type: model
  name: gpt-5.6-sol
  canonical_name: gpt-5.6-sol
  url: null
  confidence: low
  article_role: ecosystem_context
  evidence_snippets:
  - Autolith 的默认模型是 gpt-5.6-sol，同时可通过浏览器设备流或 API 密钥接入 ChatGPT Codex、Grok、Nous Research、Fireworks
    AI、Anthropic 等提供商。
  article_id: bf3ce004223067e7
extract_result: success
impact_score:
  score: 4.0
  reason: 评分依据：该工具提出的"活运行时"架构（代理与工具共享同一 SBCL 镜像、可在运行中热替换函数、以私有镜像提交持久化状态并支持崩溃恢复）在编程代理领域具有真实的架构新颖性，rlm.complete
    对超长上下文做内容寻址实例化、模型仅消费标签/大小/摘要的思路也有启发价值；但它来自小厂 lambda-symbolics，属于 PR 声明，版本号仅 v0.40.1，且基于小众
    Lisp 生态，短期难以撼动由 Cursor、Claude Code、Codex 等主导的编程代理竞争格局，影响更多停留在技术理念层而非市场层。综合判定为圈层内的重要技术演示而非行业级事件，故给
    4.0 分。
sentiment: neutral
developer_sentiment:
  tone: skeptical
  primary_focus: 活运行时与镜像提交机制能否真正提升 agent 的可检查性、可恢复性与调试体验，还是以牺牲主流工程生态为代价的小众 Lisp 概念演示
hype_assessment:
  level: medium
  reason: 文章本质是厂商 PR 声明，三个示例被刻意编排以逐项展示活重定义、崩溃恢复、跨进程持久化等能力，token 数、trace ID 与时间线的堆砌带有明显包装成分；但文中给出了可复现命令、具体版本号（v0.40.1、SBCL
    2.6.6）与机制级细节，且未滥用"颠覆/革命性"等词汇，属于有实质内容但经过营销编排的中等包装。
information_entropy: high
domain_disruption:
  technical_innovation: 把"活运行时"理念引入编程代理：代理与其所有工具同驻一个 SBCL 镜像，函数可在运行中热替换、测试并通过含清单与可执行
    Lisp 重放脚本的私有镜像提交实现持久化；rlm.complete 将超出上下文窗口的语料实例化为内容寻址对象，模型仅接收标签、大小与摘要，在显式调用与
    token 预算内驱动受限 Lisp 环境，从而绕开长上下文瓶颈，这是相对主流 agent 框架（提示词 + 工具调用循环）的架构级差异化。
  business_model: 无直接商业模式冲击；作为开源 CLI 工具，其"可检查、可恢复"的 agent 基础设施理念若被主流平台吸收，可能推动按可靠性、可审计性、状态持久化能力进行差异化的商业化方向。
engineering_complexity: production_ready
compound_value:
  score: 4.5
  reason: 从资本视角审视，Autolith 的长期价值不在产品本身，而在其验证的三个架构理念：一是『活运行时』——代理可在运行中检查、替换并持久化自身代码（私有镜像提交），切中当前编程
    Agent 普遍『不可检视、不可恢复』的痛点；二是 rlm.complete 的内容寻址外置化，将超出上下文窗口的语料实体化，代表一条与『无限上下文窗口』路线并行且
    token 成本更优的路径；三是 mutation journal + crash capsule 的崩溃恢复机制，天然适配长任务可靠性。但落地层面，Common
    Lisp/SBCL 技术栈使其主流开发者采纳门槛极高，lambda-symbolics 作为独立小团队缺乏生态运营与分发资源，且编程 Agent 赛道正被
    OpenAI/Anthropic/Cursor 等重仓，这些理念极易被吸收进闭源产品。属『有复利潜力但需持续验证』的细分创新——若理念升维为可复用协议/标准则价值上修，否则
    3-5 年后大概率只是理念先行者，故评 4.5 分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- OpenAI
- OpenRouter
- Anthropic
- lambda-symbolics
competitive_casualty:
- 闭源编程代理平台（Devin、Cursor 等）
- 依赖单一大上下文窗口的 Agent 框架
market_opportunities:
- 可借鉴 Autolith 的'活运行时'架构理念，在主流编程代理中引入运行时可替换函数与私有镜像提交能力，满足高级开发者对可审计、可深度定制代理的需求
- rlm.complete 内容寻址的上下文管理思路可产品化为通用工具，服务于需要在远超模型窗口的代码库或文档库上执行推理的企业级知识问答与代码审计场景
- 崩溃胶囊与恢复镜像的状态持久化方案，为高自主度 AI Agent 的可靠性工程（故障恢复、状态审计、镜像回滚）提供了可参考的落地范式
risk_matrix:
  regulatory: 以用户完整权限执行模型生成代码，若未来进入受监管行业（金融、医疗等）可能引发事故责任归属与审计合规问题；目前无直接适用的监管限制
  technological: Common Lisp/SBCL 属小众技术栈，活运行时与自修改代理概念可能被主流代理产品吸收后同质化；rlm.complete
    内容寻址方案也可能被更简单的检索增强或长上下文模型替代
  competitive: 编程代理赛道高度拥挤（Claude Code、Codex、Cursor、OpenCode 等），大厂一旦跟进活运行时与状态恢复能力，Autolith
    的差异化优势将被压缩；且其依赖特定模型订阅生态，社区与生态位较为狭窄
  ethical: 项目明示'进程边界只保护可靠性而非对抗恶意代码'，模型生成代码以用户权限执行，存在供应链投毒或恶意输出导致本机受损的安全风险；自修改代理的行为可解释性与问责性亦存疑
  additional:
  - 该事件属自述式产品声明（pr_statement），缺乏第三方独立验证与真实生产环境的长期观测数据
  - Common Lisp 社区规模有限，工具的长线维护动力与生态拓展存在不确定性
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: product
  name: Autolith
  canonical_name: Autolith
  url: https://github.com/lambda-symbolics/autolith
  positioning: Autolith 是一款运行在终端中的编程代理，直接工作在用户仓库中，可读写文件、搜索工作区、运行命令与测试，并内置可实时检查、修改和扩展的
    Common Lisp 活运行时。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 希望深入检查、定制与扩展代理行为的开发者
  - 熟悉 Common Lisp 并追求活系统能力的程序员
  - 重视会话持久化、崩溃恢复与跨进程延续的编程代理重度用户
  product_signal: 核心特色是活运行时，代理与所有工具运行在同一 Common Lisp 镜像内，可在不重启的情况下检查、替换和扩展运行中的函数，并通过私有镜像提交持久化每次变更。
  market_signal: v0.40.1 覆盖 Linux、macOS、FreeBSD、NetBSD 与 OpenBSD 等平台，默认模型为 gpt-5.6-sol，并兼容
    ChatGPT Codex、Grok、Anthropic、OpenRouter 与 Mistral AI 等多家模型提供商。
  differentiation: 与传统编程代理不同，Autolith 把代理自身构建在 Common Lisp 活运行时之上，支持运行中函数热替换、崩溃自动恢复与跨进程会话延续，强调可检查、可扩展、可恢复的代理形态。
  watch_reason: Autolith 将活运行时概念引入编程代理，支持运行中函数热替换、崩溃胶囊自动恢复与跨进程私有镜像提交，代表了代理向可检查、可扩展、可恢复方向演进的前沿探索，其工程实现与社区采用情况值得持续跟踪。
  risk_notes:
  - Autolith 以用户权限执行模型生成的代码，其进程边界仅保障可靠性而非安全性，不能当作安全沙箱使用。
  - 活运行时与镜像提交机制高度依赖 Common Lisp 生态，可能限制非 Lisp 开发者的上手意愿与通用性。
  - 当前为 v0.40.1 较新版本，社区规模、生产环境验证与长期维护稳定性仍有待观察。
  score: 7.0
  article_ids:
  - bf3ce004223067e7
  evidence_snippets:
  - Autolith 是一款运行在终端中的编程代理，直接工作在用户仓库中，可读写文件、搜索工作区并运行命令与测试。
  - Autolith 的 Common Lisp 镜像内含提供商客户端、终端界面、工具注册表、会话状态与持久记忆，代理与工具运行在同一进程。
  - Autolith v0.40.1 支持 Linux x86-64、macOS arm64、FreeBSD 等平台，二进制发行自带 SBCL 2.6.6，Nix
    为推荐安装路径。
---

# Autolith: a programming agent with a live runtime

Autolith runs in a terminal and works directly in your repository. It can read and edit files, search the workspace, run commands and tests, keep project context, and use Common Lisp without leaving the conversation.

**Repository work.**Filesystem, shell, and search tools with visible results.**Oversized context.**Recursive inference over corpora larger than the model window.**Continuity.**Portable conversations, memories, agendas, checkpoints, and recovery.**Live Lisp.**An SBCL runtime it can inspect, test, and extend.

*For developers who want an agent they can inspect, extend, and recover.*

## Install Autolith

Autolith v0.40.1 runs on Linux x86-64, Linux aarch64, macOS x86-64, macOS arm64, FreeBSD x86-64, NetBSD x86-64, and OpenBSD x86-64. The binary release carries its own SBCL 2.6.6, Lisp dependencies, and native helpers; Linux is served in glibc and musl builds. Installed releases check for newer tags and ask before updating. Nix is the recommended route when you want the complete build pinned from the start.

Providers sign in with a browser device flow or an API key: ChatGPT Codex subscription, Grok subscription, Nous Research subscription, Fireworks AI, Anthropic API (pay-per-token), OpenRouter (pay-per-token), OpenCode (zen/go), and Mistral AI. Any other OpenAI-compatible endpoint can be registered from the REPL. The default model is gpt-5.6-sol.

*
Yes, piping a URL into a shell is evil. Read the installer before
running it. Nix is the better installation path.
*

*
Autolith executes model-generated code with your user privileges.
Its process boundaries protect reliability, not against hostile
code. Use it as a development agent, not as a security sandbox.
*

```
After the binary installer
$
````autolith auth`
$ `autolith`
Recommended · Nix
$ `nix run github:lambda-symbolics/autolith -- auth`
$ `nix run github:lambda-symbolics/autolith`
Build from the repository
$ `git clone https://github.com/lambda-symbolics/autolith`
$ `cd autolith`
$ `./script/bootstrap`
$ `./script/check`
$ `./bin/autolith auth`
$ `./bin/autolith`


## Examples

- Prompt
- Use rlm.complete over autolith-source.txt in the workspace root. It is the concatenation of this repository's 121 src/*.lisp files with ';;;; FILE:' headers, about 3.1 MB, larger than your context window. Task: list every condition class defined in the source, grouped by subsystem, with the defining file for each. Use a budget of 32 calls, 400000 tokens, depth 2. Do not read or search the file directly; if the budget exhausts, stop and report.
- Provider
- ChatGPT Codex subscription · gpt-5.6-terra (effort high)
- Wall time
- 3 min 15 s for the answer; 4 min 36 s for the whole session
- Tokens
- 59.6K in the root conversation (58.7K input + 915 output); the corpus itself never enters a prompt
- Artifact
- All 83 condition classes, grouped by 14 subsystems, each with its defining file
- Trace
`inference:s2Wb1o2`

, the complete frame conversation, readable in-session with resource.read- Reproduce
`find src -name '*.lisp' | sort | while read f; do printf ';;;; FILE: %s\n' "$f"; cat "$f"; done > autolith-source.txt && autolith`

, then the prompt above

- Prompt
- Show the source of terminal-ui--duration-text in your running image, then redefine it live so durations of 24 hours and more render as Dd H:MM:SS, for example 1d 2:00:00. Exercise the change with assertions for 59 seconds, 61 minutes, and 26 hours, then commit the mutation with a short message. Then deliberately break the function with a redefinition that signals an error, demonstrate the failure with self.eval, and recover by discarding the broken mutation. Verify with self.eval that the committed version is active again.
- Provider
- ChatGPT Codex subscription · gpt-5.6-terra (effort high)
- Wall time
- About 6 minutes, including the crash and the recovery boot
- Tokens
- 289.0K total (286.1K input + 3.0K output), most of it the bounded crash context of the diagnosis turn
- Artifact
- Private image commit "Format long activity durations". The broken
formatter is used by the live status row, so the deliberate break
killed the active process outright: Autolith wrote crash capsule
`efa6aa16`

, booted the pristine recovery image, restarted from clean committed source with the private commit selected, restored the conversation, and opened a read-only diagnosis turn that asked before repairing anything. The committed mutation survived its own author. - Trace
- Append-only mutation journal, the crash capsule, and the private commit's replay script
- Reproduce
`autolith`

in any workspace, then the prompt above

- Prompt
- Define autolith-demo-parse-fraction in the autolith package: parse a string like "3/4" into a rational. Exercise it, then persist it as a private image commit. Then add an agenda entry: handle zero denominators in autolith-demo-parse-fraction. After (quit) and autolith resume: Where were we? Check the agenda, confirm autolith-demo-parse-fraction is still defined in this fresh image by evaluating (autolith-demo-parse-fraction "3/4"), then finish the agenda item: signal a clear error for a zero denominator, exercise both paths, and mark the agenda entry done.
- Provider
- ChatGPT Codex subscription · gpt-5.6-terra (effort high)
- Wall time
- 10 min 11 s across two processes; each persist runs the full validation suite before anything is retained
- Tokens
- 601.1K total for the resumed conversation (598.5K input + 2.6K output)
- Artifact
- Private image commits 4048480f and 9eee3ae4. The function defined before (quit) answers (autolith-demo-parse-fraction "3/4") = 3/4 in the next process without any reloading; the agenda entry written in the first session is found, finished, and marked done in the second.
- Trace
- Append-only conversation store, the two commits' replay scripts, and the workspace agenda
- Reproduce
`autolith`

, the first prompt,`(quit)`

, then`autolith resume`

and the second prompt

## The agent and its tools run in one process

Autolith is a terminal programming agent, not a wrapper around another agent process. Its Common Lisp image contains the provider client, terminal interface, tool registry, conversation state, persistent memories, workspace agenda, and the code that decides what happens next.

Context larger than the window is an environment, not a prompt.
** rlm.complete** interns the corpus as a
content-addressed object; the model receives only its label, size,
and digest, and drives a heap-isolated Lisp environment through
bounded slices, searches, and sub-inferences under an explicit
call and token budget. Every run leaves a readable inference
trace.

## Fast search, optional immutable mode

Workspace search runs in-process through
**fff**, a fast Rust
search library. Autolith keeps the index warm instead of starting a
new search process for every query.

If you want inspection without active-image changes, start it with
** --immutable**. The mode retains read-only inspection and
recovery information while withholding evaluation, mutation,
persistence, checkpoint, and rollback tools.

## Update the running agent without restarting it

Autolith can inspect and replace complete functions, methods, classes, macros, conditions, and global settings in its running image. An exploratory change takes effect immediately and is recorded in an append-only mutation journal. It can be exercised, discarded, or retained as a private image commit.

A useful change can then become a private image commit. The commit contains a manifest and a complete executable Lisp replay script, retained in a separate private Git history. It changes the active agent without quietly patching the tracked source repository.

```
→ lisp.source TERMINAL-UI--DURATION-TEXT target self
← complete tracked DEFUN
→ self.redefine
(defun terminal-ui--duration-text ...)
← compiled and installed in the active image
→ self.exercise
(assert (string= "1d 2:00:00" ...))
← journaled assertion passed
→ self.diff
← one reconstructible live mutation
→ self.commit "Show days in long durations"
← private image commit
````commit-id`
complete reconstruct.lisp retained in private Git


## Different kinds of state stay separate

Source, conversations, useful facts, live mutations, exact heaps, and disposable experiments have different lifetimes. Autolith keeps them separate instead of pretending that one database or one saved core is everything.

| Conversations | Append-only portable S-expressions with exact resume commands and crash-tail repair. |
|---|---|
| Memories | Workspace or global facts, preferences, and decisions with bounded prompt recall. |
| Agendas | Short workspace tasks and notes, available in full on every request. |
| Private image commits | Complete replay scripts for durable user-specific definitions and settings, retained in private Git. |
| Generations | A saved active core, exact tracked source commit, reconstruction script, manifest, and journal position. |
| Worker images | Immutable experimental SBCL cores with parentage and durable notes, never selected as the active agent. |
| Recovery | A separately built pristine image that can inspect a crash and select a known-working generation without loading the damaged core. |