---
title: Linux 0.11 rewritten in idiomatic Rust, boots in QEMU
source: https://github.com/Poseidon-fan/linux-0.11-rs
author:
- '[[arto]]'
published: '2026-07-13'
created: '2026-07-14'
description: 'Article URL: https://github.com/Poseidon-fan/linux-0.11-rs Comments
  URL: https://news.ycombinator.com/item?id=48898134 Points: 102 # Comments: 86'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 8a3c99014d12362b
source_type: community_discussion
tldr: Linux 0.11 内核用 Rust 重写，可在 QEMU 上启动运行
objective_summary: 开发者 Poseidon-fan 将 1991 年的 Linux 0.11 内核用现代 Rust 语言从零重写，保留原版语义但使用更强类型和清晰模块划分。项目支持进程管理、虚拟内存、Minix
  v1 文件系统等核心功能，包含 80 多个 coreutils 和 POSIX 子集 shell，可在
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - Rust
  - QEMU
  - i386
  - Minix v1
  - POSIX
  key_people:
  - Poseidon-fan
  - yuan-xy
key_logic_flow:
- 该项目用现代 Rust 语言从零重写了 1991 年的 Linux 0.11 内核，保留了原版的系统调用语义和功能行为。
- 重写的内核支持进程管理、虚拟内存（含请求分页和写时复制）、Minix v1 文件系统、ATA 磁盘驱动、VGA 和串口控制台、TTY 层、信号处理及完整系统调用表。
- 项目提供了名为 user_lib 的 Rust 用户空间标准库，其接口镜像了 Rust 标准库中的 fs、io、path、env、process、time 等模块。
- 包含 80 多个 coreutils 工具和一个支持管道、控制流、函数、glob 匹配、Tab 补全和命令历史的 POSIX 子集 shell。
- 通过一条命令即可编译所有用户程序、构建 Unix 风格文件系统并打包为可启动磁盘镜像，在 QEMU 上运行。
- 项目还包含配套的 MBR 工具 mbrkit 和 Minix 文件系统工具 miniximg，以及基于 QEMU 串口的端到端测试框架。
extract_result: success
impact_score:
  score: 4.0
  reason: 该项目是开源社区中 Linux 0.11 内核的 Rust 完整重写，技术上令人印象深刻——支持进程管理、虚拟内存（请求分页+写时复制）、Minix
    v1 文件系统、ATA 驱动、80+ coreutils 和一个功能丰富的 POSIX 子集 shell，且端到端可启动运行。但从 AI 行业视角看，这是一个教育性和展示性的个人项目，未改变当前
    AI 基础设施格局。Rust 在系统编程领域的渗透确实对 AI 工具链（推理引擎、运行时）有长期正面意义，但重写对象是 1991 年的古董内核，运行在 QEMU
    i386 模拟器上，距离现代生产级内核差距甚远。社区反响会很好，但短期行业冲击有限。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: Rust 重写经典内核的完整性与工程优雅性，以及 user_lib 用户空间标准库的设计
hype_assessment:
  level: low
  reason: 项目 README 以事实性语言描述功能和局限，明确标注 'Floppy support is intentionally out of scope'、'Tutorial
    is early draft'，未使用 '颠覆'、'革命性' 等 PR 话术。功能列表具体可验证，且诚实说明运行在 QEMU 模拟器上。属于扎实的工程展示，无概念炒作成分。
information_entropy: high
domain_disruption:
  technical_innovation: 用现代 Rust 语言完整重写 Linux 0.11 内核，在保留原始系统调用语义和功能行为的同时，借助 Rust
    的所有权模型和强类型系统实现更高的代码安全性与可维护性。项目自建了 Rust 用户空间标准库 user_lib，镜像 Rust 标准库的 fs、io、path、env
    等公共接口，使用户程序的编写体验接近普通 Rust 开发。配套的 mbrkit 和 miniximg 作为独立 crate 发布，为 MBR 和 Minix
    v1 文件系统操作提供了可复用的工具链。该项目为 '用 Rust 重写系统级基础设施' 提供了完整的参考实现。
  business_model: 无。这是一个开源教育/演示项目，无直接商业模式影响。
engineering_complexity: prototype
compound_value:
  score: 4.0
  reason: 该项目将 1991 年的 Linux 0.11 内核用现代 Rust 重写，定位为教育性和示范性开源项目，无直接商业模式或营收来源。正面因素：完整实现了进程管理、虚拟内存（请求分页+CoW）、Minix
    v1 文件系统、ATA 驱动等核心功能，配套了 Rust 用户空间标准库 user_lib、80+ coreutils 和 POSIX 子集 shell，并提供了
    devcontainer 和端到端测试框架，工程质量扎实。这证明了 Rust 在 OS 内核级别的成熟度，对 Rust 系统编程教育和人才培育有一定长期价值。制约因素：(1)
    基于 Linux 0.11 而非现代内核，与现代操作系统及 AI 基础设施的关联度有限；(2) 同类教育项目（rCore、Theseus OS）已存在，差异化不明显；(3)
    目标受众极窄（Rust + OS 开发交叉领域），社区天花板低。综合来看，在'Rust OS 开发教学'这一极细分赛道有轻度基础设施潜力，但复利效应受限于历史内核定位和受众规模，难以成为行业级基石。
value_capture_layer: hardware_compute
moat_impact: democratizes_access
key_beneficiaries:
- Rust Foundation
- AWS
- Google
- Microsoft
competitive_casualty:
- 传统 C 语言 OS 教学项目
- x86 汇编 OS 开发教程
market_opportunities:
- 教育培训机构可基于该项目开发 Rust 系统级编程与操作系统内核开发的实战课程，填补传统 OS 教学与现代内存安全语言之间的空白
- 嵌入式与 IoT 安全咨询公司可借鉴该项目的架构思路，为客户的遗留 C 语言底层系统提供 Rust 重写的可行性评估与迁移方案
- Rust 工具链开发者可围绕该项目衍生出内核测试框架、构建工具链等配套生态工具，形成开源商业化服务
risk_matrix:
  regulatory: 无
  technological: 该项目基于 1991 年的 Linux 0.11 内核，功能和架构已严重落后于现代内核；若有人将其误用于生产环境，将面临巨大的安全漏洞和兼容性风险
  competitive: 无，该项目为个人开源教育项目，未构成商业竞争威胁
  ethical: 无
  additional:
  - 项目维护风险——作为个人开发者的业余项目，长期维护和社区治理的不确定性较高，依赖者可能面临断更风险
confidence:
  impact: low
  compound: medium
  hype: low
actionable_insight: monitor
---

A modern Rust rewrite of the Linux 0.11 kernel — boots on

`i386`

in QEMU, runs a self-hosted Unix-style userland.

```
$ cd kernel && make run
…
Welcome to linux-0.11-rs.
https://github.com/Poseidon-fan/linux-0.11-rs
(login: 0 @ linux-rs :: Wed May 27 09:35:43 UTC 2026)
[root@linux-rs /root]# ls /bin | wc -l
69
```

`linux-0.11-rs`

rebuilds the 1991 Linux 0.11 kernel from scratch in modern
Rust. It keeps the original system's semantics — what it does — while
rethinking how it's expressed: stronger types, clearer module boundaries,
idiomatic abstractions everywhere. The kernel boots on emulated `i386`

hardware, runs a full `init`

→ shell → coreutils stack, and ships with the
tooling to build your own bootable image in one command.

**A kernel with most of what Linux 0.11 had**— processes, virtual memory with demand paging and CoW fork, the Minix v1 filesystem, ATA disk driver, VGA + PS/2 console, 8250 serial console, TTY layer, signals, and the complete syscall table.**A Rust user-space "std"**—`user_lib`

mirrors the public shape of`std::{fs, io, path, env, process, time}`

so user programs read like ordinary Rust, not like syscall plumbing.**A real userland**— 80+ coreutils plus a hand-written POSIX-subset shell (`sh`

) with pipelines, control flow, functions, glob, command and arithmetic substitution, and an interactive line editor with Tab completion and history.**One-command images**—`tools/build-disk.sh`

compiles every user program, lays them out into a Unix-style filesystem, and packs the result into a bootable disk image.**Companion image tools**—`mbrkit`

and`miniximg`

are standalone crates, useful on their own for any project that touches MBR or Minix v1 images.**Devcontainer included**— clone, open in VS Code, hit "Reopen in Container", run`make run`

.

```
# Build every user program, lay out /etc /dev /bin, pack into disk.img.
tools/build-disk.sh
# Build the kernel and boot it in QEMU.
cd kernel && make run # VGA console
cd kernel && make run-console # serial console (-nographic)
```

You'll land at a shell prompt in `/root`

. Try:

```
ls /bin # browse what's installed
echo $((1 + 2 * 3)) # arithmetic expansion
for f in /etc/*; do echo $f; done # for loop + glob
fact() { if [ $1 -le 1 ]; then echo 1; else echo $(($1 * $(fact $(($1-1))))); fi; }
fact 7 # → 5040
ec<TAB> # completes to `echo `
↑ # walks command history
```

Outside a devcontainer you'll also need a recent Rust nightly (pinned in
`rust-toolchain.toml`

), `qemu-system-i386`

, the `x86_64-linux-gnu-*`

cross-binutils, and the local image/test tools:

`tools/install-tools.sh # installs mbrkit, miniximg, ktest onto PATH`

End-to-end tests boot the kernel under QEMU and drive the serial console
from short `.ktest`

scripts under `ktest/suites/`

.

```
tools/run-tests.sh # run everything
tools/run-tests.sh --suite=shell # one suite
tools/run-tests.sh --test-set=shell.basic # one test
tools/run-tests.sh --disable-reboot # share one QEMU across tests
```

```
kernel/ The kernel itself
user_lib/ std-style user-space library
user_lib_macros/ proc-macro: #[user_lib::main]
user_program/ ~80 coreutils + the `sh` shell
ktest/ end-to-end test runner driving QEMU over serial
mbrkit/ MBR disk-image CLI (also on crates.io)
miniximg/ Minix-fs image CLI and library
rootfs/ disk-image content template (/etc, /root, …)
tools/ developer scripts (build-disk.sh, run-tests.sh, …)
tutorial/ mdbook walkthrough (work in progress)
.devcontainer/ ready-to-use dev environment
```


**Kernel**— substantially feature-complete relative to Linux 0.11. Floppy support is intentionally out of scope; ongoing work is on polish and tooling.**User library**— covers what the shell and coreutils need today.**Userland**— usable for real interactive work.**Tutorial**— early draft; the long-term plan is a complete build-from-scratch walkthrough.

```
cargo install mdbook
cd tutorial && mdbook serve --open
```

- Thanks to yuan-xy/Linux-0.11 for providing the original Linux 0.11 kernel source used as an important reference during development.
- Many parts of this project were also inspired by or implemented with reference to rcore-os/rCore-Tutorial-v3.

See LICENSE.