---
title: cloudflare/computer
source: https://github.com/cloudflare/computer
author: []
published: ''
created: '2026-08-06'
manifest_dates:
- '2026-08-06'
description: 'Give your agent a computer 👾Cloudflare Computer Cloudflare Computer
  is a virtual filesystem that lives inside a Durable Object. The Durable Object holds
  the authoritative state in SQLite and exposes one pluggable execution surface through
  workspace.runtime. Three backends ship today: Container projects the SQLite state
  into a sandbox container as a real FUSE mount. A sandbox-side daemon (computerd)
  mounts the state as a filesystem and syncs changes back over a capnweb RPC channel.
  Full Linux userland, real binaries, real network. Isolate shell runs just-bash in
  a Dynamic Worker. It reaches the authoritative Workspace over Workers RPC, so there
  is no second store or sync round trip. Isolate JavaScript runs an ECMAScript module
  in a fresh Dynamic Worker with structured input/results, durable relative imports,
  configured libraries, Workspace-backed node:fs/promises, and trusted ws:git and
  ws:artifacts modules. A Workspace may register multiple backends under stable IDs.
  workspace.runtime.exec(source, { backend }) is the single execution entry point;
  the selected backend defines whether source is a shell command or an ECMAScript
  module. Backends connect lazily on first use. Workspace can also be constructed
  without a backend at all, giving callers the filesystem on its own. Important PREVIEW
  ONLY This package is provided as a preview for feedback only. APIs are unstable
  and the design is subject to change. Suitable for experiments, exploration and prototypes.
  It is NOT suitable for production use at this time. The specification under docs/
  is forward-looking — read it for intent, not as description of the code today. Using
  it If you want to build on Cloudflare Computer, install @cloudflare/computer and
  follow that package''s README — it has the installation steps, the entrypoint map,
  and worked examples of the fs and runtime surfaces. To contribute feedback, see
  CONTRIBUTING.md. Approved collaborators should follow COLLABORATORS.md for setup,
  build, and test instructions. Examples The examples/ directory holds runnable consumers
  of the public surface. Each is a Worker workspace with its own README. examples/container
  — runs computerd inside a container, mounts a workspace, and talks to a Durable
  Object over capnweb. A write / read / exec HTTP surface. examples/worker-shell —
  same HTTP surface as the container example, but the shell runs just-bash in a Dynamic
  Worker loaded through env.LOADER. No container. examples/worker-javascript — mirrors
  worker-shell, but exec evaluates an ECMAScript module in a Dynamic Worker instead
  of running a shell command. examples/think — a @cloudflare/think chat agent that
  uses the workspace as its working directory, reachable from a terminal. examples/think-compare-runtimes
  — a web UI that runs the same agent task against the container and worker runtimes
  side by side. examples/tutorial — a step-by-step build: one endpoint, one agent
  that writes a markdown recipe card on the host and runs pandoc on it in the container
  to produce a PDF. examples/artifacts — generates a Worker project in a workspace
  and publishes it to Cloudflare Artifacts as a clone-ready repo. examples/assets
  — turns a prompt into an image with Workers AI, writes it to the workspace, and
  returns a shareable link through @cloudflare/computer/assets. Repository layout
  The repo is a small monorepo. Each package has its own README with package-specific
  status and usage notes. packages/dofs (@cloudflare/dofs) — Durable Object SQLite-backed
  virtual filesystem, sync protocol building blocks, and a @platformatic/vfs provider
  for Node. packages/rpc (@cloudflare/computer-rpc) — capnweb wire types and server/client
  helpers shared between the Durable Object and computerd. packages/computerd (@cloudflare/computerd)
  — the computerd daemon: a FUSE mount plus HTTP/WebSocket RPC server that runs inside
  the sandbox container. packages/computer (@cloudflare/computer) — the top-level
  Computer package consumed by Durable Objects. Work in progress. packages/computer-computerd-linux-x64
  — private Docker image context for the prebuilt computerd linux-x64 binary. The
  image, not an npm package, is the release artifact. Performance computerd''s FUSE
  mount beats real disk on metadata-heavy work and trails it on large sequential I/O.
  See docs/19_performance.md for the full fs-bench numbers, a cloudflare/sandbox-sdk
  npm install comparison, and how to reproduce them. Documentation docs/ — design
  specification. Forward-looking; treat as intent. docs/19_performance.md — filesystem
  benchmarks. Contributing We accept bug reports, fix proposals, feature requests,
  and design proposals through issues and discussions. We do not accept unsolicited
  pull requests. See CONTRIBUTING.md for the public contribution paths. Approved collaborators
  should follow COLLABORATORS.md for setup, formatting, testing, commit message, and
  pull request conventions. If you''re working in this repo as an agent, start with
  AGENTS.md and the skills under .agents/skills/. License MIT. See LICENSE.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 36da1b921f121c1d
source_type: community_discussion
tldr: Cloudflare 发布了 Cloudflare Computer 的预览版，这是一个运行在 Durable Object 内的虚拟文件系统，权威状态存于
  SQLite，通过 workspace.runtime 提供容器、隔离 shell 与隔离 JavaScript 三种可插拔执行后端。目前仅限实验与原型，不适合生产使用。
objective_summary: Cloudflare 在 GitHub 上以预览形式发布了 Cloudflare Computer，它是一个位于 Durable
  Object 内的虚拟文件系统，权威状态保存在 SQLite 中。该包通过 workspace.runtime.exec(source, { backend })
  提供统一执行入口，并附带三种后端：Container 以 FUSE 挂载并经 capnweb RPC 同步、Isolate shell 在 Dynamic Worker
  中运行 bash、Isolate JavaScript 在 Dynamic Worker 中执行 ECMAScript 模块。仓库为小型 monorepo，包含
  dofs、computer-rpc、computerd、computer 等包以及 container、worker-shell、tutorial 等可运行示例，并附有
  fs-bench 基准测试。Cloudflare 明确标注该包为预览版，API 不稳定、设计可能变更，适合实验探索但暂不适合生产使用。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Cloudflare
  technologies:
  - Durable Object
  - SQLite
  - FUSE
  - capnweb
  - Dynamic Worker
  - Workers RPC
  - Workers AI
  - Cloudflare Artifacts
  - node:fs/promises
  key_people: []
key_logic_flow:
- Cloudflare Computer 是一个运行在 Durable Object 内的虚拟文件系统，权威状态保存在 SQLite 中，并通过 workspace.runtime
  暴露统一的可插拔执行面。
- 该包目前提供三种后端：Container 由 computerd 守护进程以 FUSE 挂载文件系统并经 capnweb RPC 同步变更，Isolate shell
  在 Dynamic Worker 中运行 bash，Isolate JavaScript 在 Dynamic Worker 中执行 ECMAScript 模块。
- workspace.runtime.exec(source, { backend }) 是唯一执行入口，后端按需惰性连接，一个 Workspace 可在稳定 ID
  下注册多个后端。
- 该包目前仅作预览发布，API 不稳定且设计可能变更，适合实验、探索和原型，暂不适合生产使用。
- 仓库是小型 monorepo，包含 dofs、computer-rpc、computerd、computer 等包，以及 container、worker-shell、worker-javascript、tutorial、artifacts
  等可运行示例。
- 基准测试显示 computerd 的 FUSE 挂载在元数据密集型任务上优于真实磁盘，但在大顺序 I/O 上落后于真实磁盘。
object_mentions:
- object_type: project
  name: cloudflare/computer
  canonical_name: Cloudflare Computer
  url: https://github.com/cloudflare/computer
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Cloudflare Computer 是一个运行在 Durable Object 内的虚拟文件系统，权威状态保存在 SQLite 中，并通过 workspace.runtime
    暴露可插拔的执行面。
  - 该仓库是小型 monorepo，每个包都有独立 README，并附带 container、worker-shell、tutorial 等可运行示例。
  article_id: 36da1b921f121c1d
- object_type: product
  name: '@cloudflare/computer'
  canonical_name: '@cloudflare/computer'
  url: https://github.com/cloudflare/computer/tree/main/packages/computer
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 安装 @cloudflare/computer 并遵循该包的 README 即可在其上构建，README 包含安装步骤、入口点映射以及 fs 和 runtime
    表面的可运行示例。
  article_id: 36da1b921f121c1d
- object_type: project
  name: '@cloudflare/computerd'
  canonical_name: '@cloudflare/computerd'
  url: https://github.com/cloudflare/computer/tree/main/packages/computerd
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - computerd 是运行在沙箱容器内的守护进程，它将 SQLite 状态以 FUSE 挂载为真实文件系统，并通过 capnweb RPC 通道将变更同步回去。
  article_id: 36da1b921f121c1d
- object_type: project
  name: '@cloudflare/dofs'
  canonical_name: '@cloudflare/dofs'
  url: https://github.com/cloudflare/computer/tree/main/packages/dofs
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - '@cloudflare/dofs 提供基于 Durable Object SQLite 的虚拟文件系统、同步协议构建块，并为 Node 提供 @platformatic/vfs
    提供方。'
  article_id: 36da1b921f121c1d
- object_type: project
  name: '@cloudflare/computer-rpc'
  canonical_name: '@cloudflare/computer-rpc'
  url: https://github.com/cloudflare/computer/tree/main/packages/rpc
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - '@cloudflare/computer-rpc 提供 capnweb 线上类型以及 Durable Object 与 computerd 之间共享的服务器和客户端辅助工具。'
  article_id: 36da1b921f121c1d
- object_type: product
  name: '@cloudflare/think'
  canonical_name: '@cloudflare/think'
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - examples/think 是一个 @cloudflare/think 聊天代理，它把工作区作为自己的工作目录，用户可以从终端直接访问。
  article_id: 36da1b921f121c1d
- object_type: project
  name: cloudflare/sandbox-sdk
  canonical_name: cloudflare/sandbox-sdk
  url: https://github.com/cloudflare/sandbox-sdk
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - docs/19_performance.md 中给出了 fs-bench 数据，以及与 cloudflare/sandbox-sdk npm install
    方式的性能对比和复现方法。
  article_id: 36da1b921f121c1d
extract_result: success
compound_value:
  score: 7.0
  reason: 投资逻辑分三层推演：(1) 定位判断——这不是一个普通工具，而是 Cloudflare 在争夺'云级计算机'这一基础原语：把文件系统（SQLite
    权威状态）+ 执行（Container / Isolate shell / Isolate JS 三后端可插拔）+ 边缘分发（Durable Object
    + Artifacts）打包成 AI Agent 时代的持久化底座，直接对标'状态+计算+网络的下一代云操作系统'；(2) 复利来源——一旦 Agent 的工作目录、状态与产物都沉淀在
    Cloudflare Computer 上，SQLite 权威状态带来强数据引力与迁移锁定，配合 Workers 全球边缘网络形成'状态+执行+网络'飞轮，且文件系统是
    Agent 的通用心智模型（目录/文件/命令），具备成为基础设施标准的潜力，属于典型的高复利资产；(3) 扣分项与验证点——当前明确为 preview、API
    不稳定，FUSE 在顺序大 I/O 上落后真实磁盘，且 AWS/Azure/OpenAI 等巨头完全有能力复制该抽象，标准之争存在失败风险，生态落地尚未验证。综合判断：若'Agent
    持久化状态层'成为刚需，Cloudflare Computer 有潜力成为细分赛道基础设施，但需生产可用性与开发者生态的持续验证，故给 7 分而非更高。
value_capture_layer: cloud_platform
moat_impact: creates_new_moat
key_beneficiaries:
- Cloudflare
- Cloudflare Workers 生态开发者
- '@cloudflare/think 等边缘 Agent 框架'
- 依赖持久化状态的 AI Agent 初创公司
competitive_casualty:
- E2B
- Daytona
- Vercel（Eve）
- 传统无状态边缘计算平台
market_opportunities:
- 创业者可利用 Cloudflare Computer 预览版快速构建带持久化文件系统的 AI Agent 工作区原型，抢占边缘计算从无状态向有状态演进的时间窗口，但应以验证性
  PoC 为主而非生产依赖
- 建议关注'边缘 AI Agent 沙箱执行'赛道，围绕 workspace.runtime.exec 统一执行面与可插拔后端（容器/隔离 shell/隔离 JS）衍生出的工具链、可观测性与编排服务存在商业化空间
- Durable Object + SQLite 虚拟文件系统作为新原语，可启发云端 IDE、CI 缓存、数据处理中间层等元数据密集型 I/O 场景的产品迭代方向
risk_matrix:
  regulatory: 该产品本质是第三方代码执行平台，若未来对外开放服务，可能面临内容合规、恶意代码责任划分与沙箱滥用等监管审查；当前预览版风险较低，但生产化后需重点评估代码执行服务的合规边界
  technological: 官方明确标注 API 不稳定、设计可能变更，仅适合实验与原型；且基准测试显示 FUSE 挂载在大型顺序 I/O 上落后真实磁盘，存在被更成熟的本地文件系统、既有
    serverless 存储或官方后续迭代方案替代的风险
  competitive: Cloudflare 进入有状态边缘计算后，将直接与 AWS Lambda+EFS、Fly.io、Vercel Fluid Compute、Deno
    等平台竞争，面临生态挤压、价格战及巨头跟进复制该架构的压力
  ethical: 在沙箱中执行任意代码存在被用于挖矿、垃圾流量或恶意软件分发等滥用风险；用户数据落盘于 Durable Object 的 SQLite 中，涉及数据隐私、越权访问与多租户隔离的伦理与安全考量
  additional:
  - 对 Cloudflare 专有生态（Durable Object、Workers RPC、Artifacts）存在强锁定风险，迁移成本高；预览期 API 变动可能造成早期采用者返工成本
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: project
  name: cloudflare/computer
  canonical_name: Cloudflare Computer
  url: https://github.com/cloudflare/computer
  positioning: Cloudflare 官方开源的虚拟文件系统项目，运行在 Durable Object 内，权威状态存于 SQLite，并通过 workspace.runtime
    暴露统一的可插拔执行面。
  technical_signal: 以 Durable Object 承载虚拟文件系统，用 SQLite 存储权威状态，并以 workspace.runtime.exec
    作为唯一执行入口，后端按需惰性连接。
  adoption_signal: 仓库为小型 monorepo，包含 dofs、computer-rpc、computerd 等包及 container、worker-shell、tutorial
    等可运行示例，尚处于预览阶段。
  ecosystem_relevance: 与 Cloudflare 的 Durable Object、Workers、capnweb RPC 及 sandbox-sdk
    等基础设施深度绑定，是 Cloudflare 计算平台向持久化执行空间扩展的重要一步。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Cloudflare Computer 把虚拟文件系统、SQLite 权威状态与可插拔执行后端统一在一个 Durable Object
    内，代表了 Cloudflare 对 Agent 工作区与持久计算原语的新探索，其设计方向可能影响未来 Workers 生态的执行模型，值得持续跟踪。
  risk_notes:
  - 官方明确标注为预览版，API 不稳定且设计可能变更，目前仅适合实验与原型，不适合生产使用。
  - 基准测试显示 computerd 的 FUSE 挂载在元数据密集任务上优于真实磁盘，但在大顺序 I/O 上明显落后。
  score: 8.0
  article_ids:
  - 36da1b921f121c1d
  evidence_snippets:
  - Cloudflare Computer 是一个运行在 Durable Object 内的虚拟文件系统，权威状态保存在 SQLite 中，并通过 workspace.runtime
    暴露可插拔的执行面。
  - 该仓库是小型 monorepo，每个包都有独立 README，并附带 container、worker-shell、tutorial 等可运行示例。
- object_type: product
  name: '@cloudflare/computer'
  canonical_name: '@cloudflare/computer'
  url: https://github.com/cloudflare/computer/tree/main/packages/computer
  positioning: 面向 Durable Object 开发者的顶层包，提供 fs 与 runtime 统一入口，让工作区既可直接当文件系统使用，也可挂载容器或隔离执行后端。
  technical_signal: 包内封装 workspace.runtime 统一执行入口，可将 source 按所选后端解析为 shell 命令或 ECMAScript
    模块执行，并支持无后端纯文件系统模式。
  adoption_signal: 仓库标注为 work in progress，API 不稳定，当前主要面向实验与原型开发者，未披露生产环境采用数据。
  ecosystem_relevance: 作为 Cloudflare Computer 生态的消费入口，与 Durable Object、Dynamic Worker、capnweb
    RPC 及 Workers AI 等组件协同工作。
  target_users:
  - Durable Object 开发者
  - 构建 Agent 工作区的开发者
  - 实验与原型阶段的开发者
  product_signal: 提供统一 fs 与 runtime 表面，支持注册容器、隔离 shell 与隔离 JavaScript 多种执行后端，并可独立使用纯文件系统能力。
  market_signal: Cloudflare 官方以预览形式发布，尚处早期阶段，暂无明确商业化与生产就绪时间表。
  differentiation: 将文件系统与执行运行时统一封装在一个 Durable Object 包内，与 Cloudflare 现有沙箱和 Workers
    运行时形成差异化组合。
  watch_reason: 该包是 Cloudflare 把虚拟文件系统、SQLite 持久化与多后端执行揉合进单一工作区的产品化载体，若演进成熟可能催生新的云端开发与
    Agent 基础设施模式，值得跟踪其 API 稳定化与生产化进程。
  risk_notes:
  - 官方明确标注为预览版，API 不稳定且设计可能变更，暂不适合生产使用。
  - 顶层包本身标注 work in progress，功能完整性与稳定性仍有待完善。
  score: 7.0
  article_ids:
  - 36da1b921f121c1d
  evidence_snippets:
  - 安装 @cloudflare/computer 并遵循该包的 README 即可在其上构建，README 包含安装步骤、入口点映射以及 fs 和 runtime
    表面的可运行示例。
- object_type: product
  name: '@cloudflare/think'
  canonical_name: '@cloudflare/think'
  url: null
  positioning: Cloudflare 生态中的聊天代理，将 Computer 工作区作为自身工作目录，用户可从终端直接访问并与之交互。
  technical_signal: 作为聊天代理把工作区当作工作目录，用户可通过终端直接访问，展示了 Computer 工作区承载 Agent 的典型用法。
  adoption_signal: 以 examples/think 示例形式随仓库分发，用于演示而非独立发布，未见独立采用数据。
  ecosystem_relevance: 作为 Cloudflare Computer 的示例消费方，体现工作区在聊天代理与终端交互场景中的落地形态。
  target_users:
  - 在 Cloudflare 平台上构建聊天代理或 Agent 的开发者
  - 探索终端交互式 Agent 工作区的开发者
  product_signal: 具备聊天代理能力，将工作区作为工作目录，用户可从终端直接访问并交互。
  market_signal: null
  differentiation: 以工作区为核心组织代理运行环境，区别于传统独立部署的聊天代理形态。
  watch_reason: '@cloudflare/think 是 Cloudflare Computer 工作区能力在 Agent 场景的落地示例，其演进能反映
    Cloudflare 对 Agent 工作目录与终端交互模式的产品思路，值得持续观察。'
  risk_notes:
  - 该对象在文章中以示例形式提及，公开信息有限，产品形态与发布节奏尚不明确。
  score: 4.0
  article_ids:
  - 36da1b921f121c1d
  evidence_snippets:
  - examples/think 是一个 @cloudflare/think 聊天代理，它把工作区作为自己的工作目录，用户可以从终端直接访问。
- object_type: project
  name: cloudflare/sandbox-sdk
  canonical_name: cloudflare/sandbox-sdk
  url: https://github.com/cloudflare/sandbox-sdk
  positioning: Cloudflare 的沙箱 SDK 项目，作为 Computer 文件系统基准测试的对比对象，用于衡量 FUSE 挂载与 npm install
    等场景的性能表现。
  technical_signal: 在 fs-bench 基准测试中作为 npm install 方式对照组，与 computerd 的 FUSE 挂载方案进行性能对比并提供复现方法。
  adoption_signal: 文章仅在性能基准语境中提及，未披露其独立的采用规模或用户数据。
  ecosystem_relevance: 作为 Cloudflare 沙箱能力的 SDK 组件，与 Computer 同属计算平台，二者在文件系统性能上形成直接对照。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: sandbox-sdk 作为 Cloudflare 既有沙箱方案的 npm install 基准，是评估 Computer FUSE
    虚拟文件系统性能优劣的关键参照系，其表现直接影响 Computer 的推广价值判断。
  risk_notes:
  - 文章仅以其作为基准对比对象，未披露 sandbox-sdk 自身的功能细节与更新动态，信息量有限。
  score: 3.0
  article_ids:
  - 36da1b921f121c1d
  evidence_snippets:
  - docs/19_performance.md 中给出了 fs-bench 数据，以及与 cloudflare/sandbox-sdk npm install
    方式的性能对比和复现方法。
impact_score:
  score: 7.0
  reason: 评分依据：这是 Cloudflare 这类头部基础设施厂商发布的底层创新，把『虚拟文件系统』作为 Durable Object 的一等状态（SQLite
    权威存储），并统一出三种可插拔执行后端（FUSE 容器 / Dynamic Worker shell / Dynamic Worker JS）。对 AI agent
    生态意义重大——边缘持久化工作区正是当前 agent 落地的关键短板，可能改变边缘无服务器的竞争格局。但明确标注为 PREVIEW ONLY、API 不稳定、不适合生产，尚未形成生态级范式转移，因此落在
    6.5-7.5 区间而非更高。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 预览版 API 不稳定且 FUSE 后端在大顺序 I/O 上落后于真实磁盘，生产就绪度与性能水分是开发者最关心的争议点
hype_assessment:
  level: low
  reason: 判定依据：仓库 README 通篇使用 PREVIEW ONLY、APIs are unstable、NOT suitable for production
    use 等保守措辞，刻意压制预期；同时给出可复现的 fs-bench 基准并如实披露短板（FUSE 在元数据密集型占优、大顺序 I/O 落后于真实磁盘），这种『自我降级』式沟通没有『颠覆』『革命』等
    PR 滥用词汇，属于实打实的技术披露。
information_entropy: high
domain_disruption:
  technical_innovation: 核心突破在于把文件系统视为边缘计算的一等状态对象：权威状态存于 Durable Object 内的 SQLite，通过
    workspace.runtime.exec(source, {backend}) 统一暴露可插拔执行面，将『状态持久化』与『代码执行』彻底解耦——容器后端用
    FUSE 挂载 + capnweb RPC 同步，隔离后端则直接经 Workers RPC 复用权威状态，消除二次存储与同步往返。
  business_model: 为边缘 AI agent 提供了『持久化工作区即服务』的底层能力：agent 可拥有云端可迁移的工作目录（写代码、跑 pandoc、产出
    artifact 并发布到 Cloudflare Artifacts），按 Durable Objects 存储与后端执行时长计费，可能重塑 agent 平台与边缘无服务器的商业模式。
engineering_complexity: prototype
---

Cloudflare Computer is a virtual filesystem that lives inside a
Durable Object. The Durable Object holds the authoritative state in
SQLite and exposes one pluggable execution surface through
`workspace.runtime`

. Three backends ship today:

**Container**projects the SQLite state into a sandbox container as a real FUSE mount. A sandbox-side daemon (`computerd`

) mounts the state as a filesystem and syncs changes back over a capnweb RPC channel. Full Linux userland, real binaries, real network.**Isolate shell**runs just-bash in a Dynamic Worker. It reaches the authoritative Workspace over Workers RPC, so there is no second store or sync round trip.**Isolate JavaScript**runs an ECMAScript module in a fresh Dynamic Worker with structured input/results, durable relative imports, configured libraries, Workspace-backed`node:fs/promises`

, and trusted`ws:git`

and`ws:artifacts`

modules.

A Workspace may register multiple backends under stable IDs.
`workspace.runtime.exec(source, { backend })`

is the single execution
entry point; the selected backend defines whether `source`

is a shell
command or an ECMAScript module. Backends connect lazily on first use.

Workspace can also be constructed without a backend at all, giving callers the filesystem on its own.

Important

**PREVIEW ONLY** This package is provided as a preview for feedback only.
APIs are unstable and the design is subject to change.

Suitable for experiments, exploration and prototypes. It is NOT suitable for production use at this time.

The specification under `docs/`

is forward-looking — read it for
intent, not as description of the code today.

If you want to build on Cloudflare Computer, install
`@cloudflare/computer`

and follow that
package's README — it has the installation steps, the entrypoint map,
and worked examples of the `fs`

and `runtime`

surfaces.

To contribute feedback, see `CONTRIBUTING.md`

.
Approved collaborators should follow `COLLABORATORS.md`

for setup, build, and test instructions.

The `examples/`

directory holds runnable consumers of the
public surface. Each is a Worker workspace with its own README.

`examples/container`

— runs`computerd`

inside a container, mounts a workspace, and talks to a Durable Object over capnweb. A`write`

/`read`

/`exec`

HTTP surface.`examples/worker-shell`

— same HTTP surface as the container example, but the shell runs just-bash in a Dynamic Worker loaded through`env.LOADER`

. No container.`examples/worker-javascript`

— mirrors`worker-shell`

, but`exec`

evaluates an ECMAScript module in a Dynamic Worker instead of running a shell command.`examples/think`

— a`@cloudflare/think`

chat agent that uses the workspace as its working directory, reachable from a terminal.`examples/think-compare-runtimes`

— a web UI that runs the same agent task against the container and worker runtimes side by side.`examples/tutorial`

— a step-by-step build: one endpoint, one agent that writes a markdown recipe card on the host and runs`pandoc`

on it in the container to produce a PDF.`examples/artifacts`

— generates a Worker project in a workspace and publishes it to Cloudflare Artifacts as a clone-ready repo.`examples/assets`

— turns a prompt into an image with Workers AI, writes it to the workspace, and returns a shareable link through`@cloudflare/computer/assets`

.

The repo is a small monorepo. Each package has its own README with package-specific status and usage notes.

`packages/dofs`

(`@cloudflare/dofs`

) — Durable Object SQLite-backed virtual filesystem, sync protocol building blocks, and a`@platformatic/vfs`

provider for Node.`packages/rpc`

(`@cloudflare/computer-rpc`

) — capnweb wire types and server/client helpers shared between the Durable Object and`computerd`

.`packages/computerd`

(`@cloudflare/computerd`

) — the`computerd`

daemon: a FUSE mount plus HTTP/WebSocket RPC server that runs inside the sandbox container.`packages/computer`

(`@cloudflare/computer`

) — the top-level Computer package consumed by Durable Objects. Work in progress.`packages/computer-computerd-linux-x64`

— private Docker image context for the prebuilt`computerd`

linux-x64 binary. The image, not an npm package, is the release artifact.

computerd's FUSE mount beats real disk on metadata-heavy work and
trails it on large sequential I/O. See
`docs/19_performance.md`

for the full `fs-bench`

numbers, a `cloudflare/sandbox-sdk`

`npm install`

comparison, and how
to reproduce them.

`docs/`

— design specification. Forward-looking; treat as intent.`docs/19_performance.md`

— filesystem benchmarks.

We accept bug reports, fix proposals, feature requests, and design
proposals through issues and discussions. We do not accept unsolicited
pull requests. See `CONTRIBUTING.md`

for the public
contribution paths.

Approved collaborators should follow
`COLLABORATORS.md`

for setup, formatting, testing,
commit message, and pull request conventions.

If you're working in this repo as an agent, start with
`AGENTS.md`

and the skills under
`.agents/skills/`

.

MIT. See `LICENSE`

.