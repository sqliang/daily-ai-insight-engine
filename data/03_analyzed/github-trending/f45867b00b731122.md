---
title: livekit/agents
source: https://github.com/livekit/agents
author: []
published: ''
created: '2026-08-04'
manifest_dates:
- '2026-08-04'
- '2026-08-05'
description: 'A framework for building realtime voice AI agents 🤖🎙️📹 Looking for the
  JS/TS library? Check out AgentsJS What is Agents? The Agent Framework is designed
  for building realtime, programmable participants that run on servers. Use it to
  create conversational, multi-modal voice agents that can see, hear, and understand.
  Features Flexible integrations: A comprehensive ecosystem to mix and match the right
  STT, LLM, TTS, and Realtime API to suit your use case. Integrated job scheduling:
  Built-in task scheduling and distribution with dispatch APIs to connect end users
  to agents. Extensive WebRTC clients: Build client applications using LiveKit''s
  open-source SDK ecosystem, supporting all major platforms. Telephony integration:
  Works seamlessly with LiveKit''s telephony stack, allowing your agent to make calls
  to or receive calls from phones. Exchange data with clients: Use RPCs and other
  Data APIs to seamlessly exchange data with clients. Semantic turn detection: Uses
  a transformer model to detect when a user is done with their turn, helps to reduce
  interruptions. MCP support: Native support for MCP. Integrate tools provided by
  MCP servers with one line of code. Builtin test framework: Write tests and use judges
  to ensure your agent is performing as expected. Open-source: Fully open-source,
  allowing you to run the entire stack on your own servers, including LiveKit server,
  one of the most widely used WebRTC media servers. Installation To install the core
  Agents library, along with plugins for popular model providers: pip install "livekit-agents[openai,deepgram,cartesia]"
  Docs and guides Documentation on the framework and how to use it can be found here
  Building with AI coding agents If you''re using an AI coding assistant to build
  with LiveKit Agents, we recommend the following setup for the best results: Install
  the LiveKit Docs MCP server — Gives your coding agent access to up-to-date LiveKit
  documentation, code search across LiveKit repositories, and working examples. Install
  the LiveKit Agent Skill — Provides your coding agent with architectural guidance
  and best practices for building voice AI applications, including workflow design,
  handoffs, tasks, and testing patterns. npx skills add livekit/agent-skills --skill
  livekit-agents The Agent Skill works best alongside the MCP server: the skill teaches
  your agent how to approach building with LiveKit, while the MCP server provides
  the current API details to implement it correctly. Core concepts Agent: An LLM-based
  application with defined instructions. AgentSession: A container for agents that
  manages interactions with end users. entrypoint: The starting point for an interactive
  session, similar to a request handler in a web server. AgentServer: The main process
  that coordinates job scheduling and launches agents for user sessions. Usage Simple
  voice agent from livekit.agents import ( Agent, AgentServer, AgentSession, JobContext,
  RunContext, cli, function_tool, inference, ) @function_tool async def lookup_weather(
  context: RunContext, location: str, ): """Used to look up weather information."""
  return {"weather": "sunny", "temperature": 70} server = AgentServer() @server.rtc_session()
  async def entrypoint(ctx: JobContext): session = AgentSession( vad=inference.VAD(),
  # any combination of STT, LLM, TTS, or realtime API can be used # this example shows
  LiveKit Inference, a unified API to access different models via LiveKit Cloud #
  to use model provider keys directly, replace with the following: # from livekit.plugins
  import deepgram, openai, cartesia # stt=deepgram.STT(model="nova-3"), # llm=openai.LLM(model="gpt-4.1-mini"),
  # tts=cartesia.TTS(model="sonic-3", voice="9626c31c-bec5-4cca-baa8-f8ba9e84c8bc"),
  stt=inference.STT("deepgram/nova-3", language="multi"), llm=inference.LLM("google/gemma-4-31b-it"),
  # low-latency gemma, hosted on LiveKit tts=inference.TTS("cartesia/sonic-3", voice="9626c31c-bec5-4cca-baa8-f8ba9e84c8bc"),
  ) agent = Agent( instructions="You are a friendly voice assistant built by LiveKit.",
  tools=[lookup_weather], ) await session.start(agent=agent, room=ctx.room) await
  session.generate_reply(instructions="greet the user and ask about their day") if
  __name__ == "__main__": cli.run_app(server) You''ll need the following environment
  variables for this example: LIVEKIT_URL LIVEKIT_API_KEY LIVEKIT_API_SECRET Multi-agent
  handoff This code snippet is abbreviated. For the full example, see multi_agent.py
  ... class IntroAgent(Agent): def __init__(self) -> None: super().__init__( instructions=f"You
  are a story teller. Your goal is to gather a few pieces of information from the
  user to make the story personalized and engaging." "Ask the user for their name
  and where they are from" ) async def on_enter(self): self.session.generate_reply(instructions="greet
  the user and gather information") @function_tool async def information_gathered(
  self, context: RunContext, name: str, location: str, ): """Called when the user
  has provided the information needed to make the story personalized and engaging.
  Args: name: The name of the user location: The location of the user """ context.userdata.name
  = name context.userdata.location = location story_agent = StoryAgent(name, location)
  return story_agent, "Let''s start the story!" class StoryAgent(Agent): def __init__(self,
  name: str, location: str) -> None: super().__init__( instructions=f"You are a storyteller.
  Use the user''s information in order to make the story personalized." f"The user''s
  name is {name}, from {location}", # override the default model, switching to Realtime
  API from standard LLMs llm=openai.realtime.RealtimeModel(voice="echo"), chat_ctx=chat_ctx,
  ) async def on_enter(self): self.session.generate_reply() @server.rtc_session()
  async def entrypoint(ctx: JobContext): userdata = StoryData() session = AgentSession[StoryData](
  vad=inference.VAD(), stt="deepgram/nova-3", llm="google/gemma-4-31b-it", # low-latency
  gemma, hosted on LiveKit tts="cartesia/sonic-3:9626c31c-bec5-4cca-baa8-f8ba9e84c8bc",
  userdata=userdata, ) await session.start( agent=IntroAgent(), room=ctx.room, ) ...
  Testing Automated tests are essential for building reliable agents, especially with
  the non-deterministic behavior of LLMs. LiveKit Agents include native test integration
  to help you create dependable agents. @pytest.mark.asyncio async def test_no_availability()
  -> None: llm = google.LLM() async with AgentSession(llm=llm) as sess: await sess.start(MyAgent())
  result = await sess.run( user_input="Hello, I need to place an order." ) result.expect.skip_next_event_if(type="message",
  role="assistant") result.expect.next_event().is_function_call(name="start_order")
  result.expect.next_event().is_function_call_output() await ( result.expect.next_event()
  .is_message(role="assistant") .judge(llm, intent="assistant should be asking the
  user what they would like") ) Examples For more examples and detailed setup instructions,
  see the examples directory. For even more examples, see the python-agents-examples
  repository. 🎙️ Starter Agent A starter agent optimized for voice conversations.
  Code 🔄 Multi-user push to talk Responds to multiple users in the room via push-to-talk.
  Code 🎵 Background audio Background ambient and thinking audio to improve realism.
  Code 🛠️ Dynamic tool creation Creating function tools dynamically. Code ☎️ Outbound
  caller Agent that makes outbound phone calls Code 📋 Structured output Using structured
  output from LLM to guide TTS tone. Code 🔌 MCP support Use tools from MCP servers
  Code 💬 Text-only agent Skip voice altogether and use the same code for text-only
  integrations Code 📝 Multi-user transcriber Produce transcriptions from all users
  in the room Code 🎥 Video avatars Add an AI avatar with Tavus, Bithuman, LemonSlice,
  and more Code 🍽️ Restaurant ordering and reservations Full example of an agent that
  handles calls for a restaurant. Code 👁️ Gemini Live vision Full example (including
  iOS app) of Gemini Live agent that can see. Code Running your agent Testing in terminal
  python myagent.py console Runs your agent in terminal mode, enabling local audio
  input and output for testing. This mode doesn''t require external servers or dependencies
  and is useful for quickly validating behavior. Developing with LiveKit clients python
  myagent.py dev Starts the agent server and enables hot reloading when files change.
  This mode allows each process to host multiple concurrent agents efficiently. The
  agent connects to LiveKit Cloud or your self-hosted server. Set the following environment
  variables: LIVEKIT_URL LIVEKIT_API_KEY LIVEKIT_API_SECRET You can connect using
  any LiveKit client SDK or telephony integration. To get started quickly, try the
  Agents Playground. Running for production python myagent.py start Runs the agent
  with production-ready optimizations. License The Agents framework is licensed under
  Apache-2.0. The LiveKit turn detection models are licensed under the LiveKit Model
  License. Contributing The Agents framework is under active development in a rapidly
  evolving field. We welcome and appreciate contributions of any kind, be it feedback,
  bugfixes, features, new plugins and tools, or better documentation. You can file
  issues under this repo, open a PR, or chat with us in the LiveKit community. Development
  setup This project uses uv for package management. To install dependencies for development:
  uv sync --all-extras --dev Examples This project includes many examples in the examples
  directory. To run them, create the file examples/.env with credentials for LiveKit
  Server and any necessary model providers (see examples/.env.example), then run:
  uv run examples/voice_agents/basic_agent.py dev For more information, see the examples
  README. Tests Unit tests are in the tests directory and can be run with: uv run
  pytest --unit Integration tests for each plugin require various API credentials
  and run automatically in GitHub CI for PRs submitted by project maintainers. See
  the tests workflow for details. Formatting This project uses ruff for formatting
  and linting: uv run ruff format uv run ruff check --fix Documentation To generate
  docs locally with pdoc: uv sync --all-extras --group docs uv run --active pdoc --skip-errors
  --html --output-dir=docs livekit LiveKit Ecosystem Agents SDKs Python · Node.js
  LiveKit SDKs Browser · Swift · Android · Flutter · React Native · Rust · Node.js
  · Python · Unity · Unity (WebGL) · ESP32 · C++ Starter Apps Python Agent · TypeScript
  Agent · React App · SwiftUI App · Android App · Flutter App · React Native App ·
  Web Embed UI Components React · Android Compose · SwiftUI · Flutter Server APIs
  Node.js · Golang · Ruby · Java/Kotlin · Python · Rust · PHP (community) · .NET (community)
  Resources Docs · Docs MCP Server · CLI · LiveKit Cloud LiveKit Server OSS LiveKit
  server · Egress · Ingress · SIP Community Developer Community · Slack · X · YouTube'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: f45867b00b731122
source_type: community_discussion
tldr: LiveKit 发布开源 Python 框架 Agents，用于构建实时语音对话智能体。框架支持 STT、LLM、TTS 与 Realtime API
  灵活组合，原生集成 MCP 工具、语义断句检测与电话系统，并提供内置测试框架。
objective_summary: LiveKit 公司开源并持续维护 livekit/agents 框架，这是一个面向服务器端实时语音智能体的 Python 开发框架。该框架提供
  Agent、AgentSession、entrypoint 与 AgentServer 四个核心组件，支持将 Deepgram、OpenAI、Cartesia、Google
  等不同厂商的语音与语言模型自由组合，并原生支持 MCP 工具集成与 WebRTC 客户端。框架可通过 pip 安装，连接 LiveKit Cloud 或自托管服务器运行，采用
  Apache-2.0 许可证，同时提供 console、dev、start 三种运行模式与内置测试框架。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - LiveKit
  - Deepgram
  - OpenAI
  - Cartesia
  - Google
  technologies:
  - WebRTC
  - STT
  - LLM
  - TTS
  - MCP
  - VAD
  - RPC
  - Realtime API
  key_people: []
key_logic_flow:
- LiveKit Agents 是一个开源 Python 框架，用于在服务器上构建可实时运行、具备听觉与视觉理解能力的多模态语音智能体。
- 框架核心由 Agent、AgentSession、entrypoint 与 AgentServer 四个组件构成，负责管理智能体与终端用户的交互及任务调度。
- 框架支持任意组合各家 STT、LLM、TTS 与 Realtime API，并内置语义断句检测、原生 MCP 工具集成与 RPC 数据交换能力。
- 框架提供 console、dev、start 三种运行模式，分别用于本地终端测试、文件热重载开发与生产优化运行。
- LiveKit 还提供 Docs MCP server 与 Agent Skill，帮助 AI 编码助手获取最新文档并遵循语音应用的最佳实践。
object_mentions:
- object_type: project
  name: livekit/agents
  canonical_name: livekit/agents
  url: https://github.com/livekit/agents
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - LiveKit Agents 是一个开源 Python 框架，用于在服务器上构建可实时运行、具备多模态感知能力的语音智能体。
  - 框架采用 Apache-2.0 许可证，允许开发者将整个技术栈包括 LiveKit 服务器部署在自己的服务器上。
  article_id: f45867b00b731122
- object_type: project
  name: AgentsJS
  canonical_name: livekit/agents-js
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章开头提示寻找 JS/TS 库的开发者改用 AgentsJS，说明 LiveKit 同时提供 JavaScript 版本的智能体框架。
  article_id: f45867b00b731122
- object_type: project
  name: LiveKit Docs MCP server
  canonical_name: LiveKit Docs MCP server
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - LiveKit 推荐开发者安装 Docs MCP server，让 AI 编码助手访问最新的 LiveKit 文档、代码检索与可运行示例。
  article_id: f45867b00b731122
- object_type: project
  name: livekit/agent-skills
  canonical_name: livekit/agent-skills
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - LiveKit 提供 Agent Skill，可通过 npx skills add livekit/agent-skills 命令安装，用于指导语音 AI
    应用的工作流设计。
  article_id: f45867b00b731122
- object_type: product
  name: Agents Playground
  canonical_name: Agents Playground
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 文章建议通过 Agents Playground 快速开始体验，它支持任何 LiveKit 客户端 SDK 或电话集成进行连接。
  article_id: f45867b00b731122
- object_type: project
  name: LiveKit Server OSS
  canonical_name: LiveKit Server
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 框架可连接 LiveKit Cloud 或自托管服务器，LiveKit server 是其中被广泛使用的 WebRTC 媒体服务器。
  article_id: f45867b00b731122
extract_result: success
impact_score:
  score: 7.0
  reason: 评分依据：语音智能体开发框架赛道已有多家竞争者（Pipecat、Vapi、Retell 等），LiveKit 并非第一个入局者，但它的差异化优势明显——自研并开源了全球使用最广的
    WebRTC 媒体服务器之一，提供完整自托管能力，同时以四组件抽象（Agent/AgentSession/entrypoint/AgentServer）+
    原生 MCP 集成 + 语义断句检测 + 内置测试框架，显著降低了实时语音 Agent 的构建门槛。这属于改变局部竞争格局的重要产品发布，但尚未达到范式转移级别（其核心依赖的
    LLM/STT/TTS 能力仍来自 OpenAI、Deepgram 等第三方）。综合判定为 7 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 多厂商模型自由组合与全栈自托管能力，能否真正避免生态锁定
hype_assessment:
  level: low
  reason: 文章为 GitHub README 的客观技术说明，提供了完整可运行的代码示例、组件架构说明和插件安装方式，无'颠覆'、'革命'等 PR 滥用词汇。唯一的营销性表述是'one
    of the most widely used WebRTC media servers'，但该说法在行业内基本站得住脚。整体属于实打实的技术干货，判定为
    low。
information_entropy: high
domain_disruption:
  technical_innovation: 核心创新在于三层：其一，用 transformer 模型做语义断句检测（VAD 之外判断用户是否说完，减少打断），解决实时语音交互的关键体验痛点；其二，提供
    LiveKit Inference 统一推理 API，将 Deepgram/OpenAI/Cartesia/Google 等多厂商 STT/LLM/TTS
    抽象为同一接口，并支持 BYO 密钥的插件模式；其三，AgentServer 的作业调度与多智能体交接（handoff）机制，把'语音应用'抽象为可编程的服务器端参与者模型，配合原生
    MCP 工具集成形成完整开发范式。
  business_model: 采用 Apache-2.0 开源 + 云服务的经典 open-core 变现路径：框架本身全开源可自托管，但通过 LiveKit
    Cloud 提供托管媒体服务器、Inference 统一推理 API（如 low-latency Gemma 托管）和链路优化来商业化。其策略是以开源框架抢占语音智能体开发入口，形成'框架-云'双轮驱动的生态锁定，同时用
    Docs MCP server 和 Agent Skill 渗透 AI 编码助手工作流，扩大开发者触达面。
engineering_complexity: production_ready
compound_value:
  score: 8.0
  reason: 从资本视角看，实时语音正成为 AI 的主流交互界面，语音智能体是下一轮规模化的细分市场，而本事件恰好卡在'模型能力→语音应用'之间的管道层。LiveKit
    本身已是开源 WebRTC 媒体服务器的事实标准，拥有大量生产部署，livekit/agents 等于在这条既有的分发渠道上向上延伸出 Agent 编排层，形成'传输+会话+工具+电话'的纵深栈，网络效应直接继承。框架
    Apache-2.0 全开源，支持 STT/LLM/TTS/Realtime API 任意混搭，并原生绑定 MCP 工具与电话系统——开发者一旦在它上面沉淀会话逻辑、测试用例与工具链，迁移成本与生态锁定会随时间复利加深，3-5
    年后大概率仍是语音 Agent 的基础设施选项。扣分项在于：模型层（OpenAI/Google 原生 Realtime API）与云平台层可能向上收编中间件价值，且
    Pipecat 等竞品与云厂商原生方案会分流，故给 8.0 而非更高。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- LiveKit
- Deepgram
- OpenAI
- Cartesia
- 语音 AI 初创团队
competitive_casualty:
- Pipecat
- Twilio
- Vapi
- Retell AI
- 闭源语音 Agent 平台
market_opportunities:
- 创业者可基于 LiveKit Agents 开源框架快速搭建垂直行业（如医疗问诊、客服外呼、教育陪练）的实时语音智能体产品，大幅降低从零构建 WebRTC 与语音链路的门槛
- 框架原生集成 MCP 工具调用，可围绕『语音+工具』场景（如语音下单、语音查账、语音调度）开发定制化企业解决方案，形成差异化壁垒
- 框架内置测试与评测（judges）机制，语音智能体规模化落地后对测试工具、质量评估与可观测性服务的需求将随之增长，存在配套工具链的商业机会
risk_matrix:
  regulatory: 语音智能体涉及通话录音、用户知情同意与数据留存，需关注 GDPR、AI Act 对透明度与人工监督的要求；电话外呼场景还可能触及电信合规与营销呼叫的红线
  technological: 语音智能体框架迭代极快，开源替代与厂商自研方案（如 OpenAI Realtime API 原生栈、Pipecat 等）可能侵蚀其生态，依赖单一框架存在架构过时风险
  competitive: 赛道竞争激烈，Vapi、Retell AI、Pipecat 等托管或开源平台林立，且 LiveKit 自家 Cloud 与自托管模式之间存在商业张力；上游模型厂商向应用层延伸可能挤压中间层空间
  ethical: 语音合成与克隆存在深度伪造滥用风险；STT/TTS 对不同口音与语种的偏见可能造成服务歧视；语音数据高度敏感，且电话客服自动化可能冲击就业
  additional:
  - 对 LiveKit Cloud 生态（如 LiveKit Inference）的依赖可能形成供应商锁定，自托管体验与云托管存在差距
  - 开源虽为 Apache-2.0，但核心商业价值集中于云端服务，完全自建堆栈的迁移成本不可忽视
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: livekit/agents
  canonical_name: livekit/agents
  url: https://github.com/livekit/agents
  positioning: 面向服务器端实时语音智能体构建的开源 Python 框架，支持多模态感知并自由组合各家语音模型，可自托管部署。
  technical_signal: 框架支持任意组合各家 STT、LLM、TTS 与 Realtime API，内置语义断句检测、原生 MCP 工具集成与 RPC
    数据交换能力。
  adoption_signal: 框架可通过 pip 安装，支持连接 LiveKit Cloud 或自托管服务器运行，采用 Apache-2.0 开源许可证分发。
  ecosystem_relevance: 作为 LiveKit 实时通信生态的核心组件，与 WebRTC SDK、电话系统及 MCP 工具链深度集成，构成完整语音智能体开发栈。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 实时语音智能体是当前 AI 应用落地热点，LiveKit 作为广泛使用的 WebRTC 媒体服务器厂商推出该开源框架，且原生集成 MCP
    与语义断句等前沿能力，其演进方向对语音交互开发者具有重要参考价值。
  risk_notes:
  - 框架与 LiveKit Cloud 服务绑定较深，完全自托管需要自行部署整条技术栈，存在一定运维复杂度。
  - 实时语音智能体框架赛道竞争激烈，面临 OpenAI Realtime、通用智能体框架等替代方案的持续冲击。
  score: 8.0
  article_ids:
  - f45867b00b731122
  evidence_snippets:
  - LiveKit Agents 是一个开源 Python 框架，用于在服务器上构建可实时运行、具备多模态感知能力的语音智能体。
  - 框架采用 Apache-2.0 许可证，允许开发者将整个技术栈包括 LiveKit 服务器部署在自己的服务器上。
  - 框架支持任意组合各家 STT、LLM、TTS 与 Realtime API，并内置语义断句检测、原生 MCP 工具集成与 RPC 数据交换能力。
- object_type: project
  name: AgentsJS
  canonical_name: livekit/agents-js
  url: null
  positioning: LiveKit 提供的 JavaScript/TypeScript 版本智能体框架，与 Python 版 livekit/agents
    功能对应，面向 JS/TS 技术栈开发者。
  technical_signal: 提供与 Python 版框架对等的实时语音智能体构建能力，让 JS/TS 开发者接入 LiveKit 实时通信生态。
  adoption_signal: 作为 livekit/agents 的 JavaScript 姊妹版本面向 JS/TS 开发者分发，文章将其作为该生态的推荐入口。
  ecosystem_relevance: 与 Python 版框架互补，共同覆盖主流语言技术栈，是 LiveKit 多语言智能体框架矩阵的组成部分。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该框架反映 LiveKit 覆盖多语言技术栈的生态布局，其与 Python 版功能对齐的进度及在 JS 开发者中的采用情况，可作为观察实时语音智能体框架跨语言发展的重要窗口。
  risk_notes:
  - 文章仅作提示性提及，缺乏功能细节、版本状态与使用规模等直接佐证。
  - JS 版本功能可能与 Python 版存在差异，实际能力边界有待进一步验证。
  score: 4.0
  article_ids:
  - f45867b00b731122
  evidence_snippets:
  - 文章开头提示寻找 JS/TS 库的开发者改用 AgentsJS，说明 LiveKit 同时提供 JavaScript 版本的智能体框架。
- object_type: project
  name: LiveKit Docs MCP server
  canonical_name: LiveKit Docs MCP server
  url: null
  positioning: 面向 AI 编码助手的 LiveKit 官方文档检索 MCP 服务器，提供最新文档访问、代码检索与可运行示例服务。
  technical_signal: 基于 MCP 协议为 AI 编码助手提供实时文档查询与代码检索能力，帮助开发者按最新 API 规范实现语音应用。
  adoption_signal: 由 LiveKit 官方推荐作为使用 AI 编码助手开发时的标准配置，与 Agent Skill 配合构成完整的开发辅助方案。
  ecosystem_relevance: 属于 LiveKit 开发者工具链与 MCP 生态的一部分，反映主流平台加速拥抱 MCP 协议辅助开发工具的趋势。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该 MCP 服务器体现 LiveKit 在 AI 辅助开发工具链上的布局，其与 Agent Skill 的组合代表技能包加文档服务器的新型开发范式，值得跟踪其采纳效果与生态扩散。
  risk_notes:
  - 作为辅助开发工具，其独立价值有限，热度主要依赖主框架生态的持续活跃。
  score: 4.0
  article_ids:
  - f45867b00b731122
  evidence_snippets:
  - LiveKit 推荐开发者安装 Docs MCP server，让 AI 编码助手访问最新的 LiveKit 文档、代码检索与可运行示例。
- object_type: project
  name: livekit/agent-skills
  canonical_name: livekit/agent-skills
  url: null
  positioning: 由 LiveKit 官方发布的 AI 编码助手技能包，提供语音 AI 应用的架构指导、工作流设计、任务交接与测试模式最佳实践。
  technical_signal: 可通过 npx skills add livekit/agent-skills 命令安装，为 AI 编码助手注入语音应用开发的架构与测试指引。
  adoption_signal: 由 LiveKit 官方发布并与 Docs MCP server 配合使用，其中技能包负责方法论、文档服务器负责 API 细节。
  ecosystem_relevance: 属于 LiveKit 开发者生态的新兴组件，体现技能包与 MCP 服务器组合辅助 AI 编码助手的新趋势。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该技能包展示 AI 辅助开发中技能包加文档服务器的新型组合范式，其与 MCP 生态的结合方式及被 AI 编码助手采用的程度，值得作为开发者工具链趋势持续观察。
  risk_notes:
  - 属于工具链辅助组件，独立影响力有限，其有效性依赖 LiveKit 文档与主框架的持续维护更新。
  score: 4.0
  article_ids:
  - f45867b00b731122
  evidence_snippets:
  - LiveKit 提供 Agent Skill，可通过 npx skills add livekit/agent-skills 命令安装，用于指导语音 AI
    应用的工作流设计。
---

Looking for the JS/TS library? Check out AgentsJS

The Agent Framework is designed for building realtime, programmable participants that run on servers. Use it to create conversational, multi-modal voice agents that can see, hear, and understand.

**Flexible integrations**: A comprehensive ecosystem to mix and match the right STT, LLM, TTS, and Realtime API to suit your use case.**Integrated job scheduling**: Built-in task scheduling and distribution with dispatch APIs to connect end users to agents.**Extensive WebRTC clients**: Build client applications using LiveKit's open-source SDK ecosystem, supporting all major platforms.**Telephony integration**: Works seamlessly with LiveKit's telephony stack, allowing your agent to make calls to or receive calls from phones.**Exchange data with clients**: Use RPCs and other Data APIs to seamlessly exchange data with clients.**Semantic turn detection**: Uses a transformer model to detect when a user is done with their turn, helps to reduce interruptions.**MCP support**: Native support for MCP. Integrate tools provided by MCP servers with one line of code.**Builtin test framework**: Write tests and use judges to ensure your agent is performing as expected.**Open-source**: Fully open-source, allowing you to run the entire stack on your own servers, including LiveKit server, one of the most widely used WebRTC media servers.

To install the core Agents library, along with plugins for popular model providers:

`pip install "livekit-agents[openai,deepgram,cartesia]"`

Documentation on the framework and how to use it can be found here

If you're using an AI coding assistant to build with LiveKit Agents, we recommend the following setup for the best results:

-
**Install the LiveKit Docs MCP server**— Gives your coding agent access to up-to-date LiveKit documentation, code search across LiveKit repositories, and working examples. -
**Install the LiveKit Agent Skill**— Provides your coding agent with architectural guidance and best practices for building voice AI applications, including workflow design, handoffs, tasks, and testing patterns.npx skills add livekit/agent-skills --skill livekit-agents


The Agent Skill works best alongside the MCP server: the skill teaches your agent *how to approach* building with LiveKit, while the MCP server provides the *current API details* to implement it correctly.

- Agent: An LLM-based application with defined instructions.
- AgentSession: A container for agents that manages interactions with end users.
- entrypoint: The starting point for an interactive session, similar to a request handler in a web server.
- AgentServer: The main process that coordinates job scheduling and launches agents for user sessions.

```
from livekit.agents import (
Agent,
AgentServer,
AgentSession,
JobContext,
RunContext,
cli,
function_tool,
inference,
)
@function_tool
async def lookup_weather(
context: RunContext,
location: str,
):
"""Used to look up weather information."""
return {"weather": "sunny", "temperature": 70}
server = AgentServer()
@server.rtc_session()
async def entrypoint(ctx: JobContext):
session = AgentSession(
vad=inference.VAD(),
# any combination of STT, LLM, TTS, or realtime API can be used
# this example shows LiveKit Inference, a unified API to access different models via LiveKit Cloud
# to use model provider keys directly, replace with the following:
# from livekit.plugins import deepgram, openai, cartesia
# stt=deepgram.STT(model="nova-3"),
# llm=openai.LLM(model="gpt-4.1-mini"),
# tts=cartesia.TTS(model="sonic-3", voice="9626c31c-bec5-4cca-baa8-f8ba9e84c8bc"),
stt=inference.STT("deepgram/nova-3", language="multi"),
llm=inference.LLM("google/gemma-4-31b-it"), # low-latency gemma, hosted on LiveKit
tts=inference.TTS("cartesia/sonic-3", voice="9626c31c-bec5-4cca-baa8-f8ba9e84c8bc"),
)
agent = Agent(
instructions="You are a friendly voice assistant built by LiveKit.",
tools=[lookup_weather],
)
await session.start(agent=agent, room=ctx.room)
await session.generate_reply(instructions="greet the user and ask about their day")
if __name__ == "__main__":
cli.run_app(server)
```

You'll need the following environment variables for this example:

- LIVEKIT_URL
- LIVEKIT_API_KEY
- LIVEKIT_API_SECRET

This code snippet is abbreviated. For the full example, see multi_agent.py

```
...
class IntroAgent(Agent):
def __init__(self) -> None:
super().__init__(
instructions=f"You are a story teller. Your goal is to gather a few pieces of information from the user to make the story personalized and engaging."
"Ask the user for their name and where they are from"
)
async def on_enter(self):
self.session.generate_reply(instructions="greet the user and gather information")
@function_tool
async def information_gathered(
self,
context: RunContext,
name: str,
location: str,
):
"""Called when the user has provided the information needed to make the story personalized and engaging.
Args:
name: The name of the user
location: The location of the user
"""
context.userdata.name = name
context.userdata.location = location
story_agent = StoryAgent(name, location)
return story_agent, "Let's start the story!"
class StoryAgent(Agent):
def __init__(self, name: str, location: str) -> None:
super().__init__(
instructions=f"You are a storyteller. Use the user's information in order to make the story personalized."
f"The user's name is {name}, from {location}",
# override the default model, switching to Realtime API from standard LLMs
llm=openai.realtime.RealtimeModel(voice="echo"),
chat_ctx=chat_ctx,
)
async def on_enter(self):
self.session.generate_reply()
@server.rtc_session()
async def entrypoint(ctx: JobContext):
userdata = StoryData()
session = AgentSession[StoryData](
vad=inference.VAD(),
stt="deepgram/nova-3",
llm="google/gemma-4-31b-it", # low-latency gemma, hosted on LiveKit
tts="cartesia/sonic-3:9626c31c-bec5-4cca-baa8-f8ba9e84c8bc",
userdata=userdata,
)
await session.start(
agent=IntroAgent(),
room=ctx.room,
)
...
```

Automated tests are essential for building reliable agents, especially with the non-deterministic behavior of LLMs. LiveKit Agents include native test integration to help you create dependable agents.

```
@pytest.mark.asyncio
async def test_no_availability() -> None:
llm = google.LLM()
async with AgentSession(llm=llm) as sess:
await sess.start(MyAgent())
result = await sess.run(
user_input="Hello, I need to place an order."
)
result.expect.skip_next_event_if(type="message", role="assistant")
result.expect.next_event().is_function_call(name="start_order")
result.expect.next_event().is_function_call_output()
await (
result.expect.next_event()
.is_message(role="assistant")
.judge(llm, intent="assistant should be asking the user what they would like")
)
```

For more examples and detailed setup instructions, see the examples directory. For even more examples, see the python-agents-examples repository.

|
A starter agent optimized for voice conversations. |
Responds to multiple users in the room via push-to-talk. |
|
Background ambient and thinking audio to improve realism. |
Creating function tools dynamically. |
|
Agent that makes outbound phone calls |
Using structured output from LLM to guide TTS tone. |
|
Use tools from MCP servers |
Skip voice altogether and use the same code for text-only integrations |
|
Produce transcriptions from all users in the room |
Add an AI avatar with Tavus, Bithuman, LemonSlice, and more |
|
Full example of an agent that handles calls for a restaurant. |
Full example (including iOS app) of Gemini Live agent that can see. |

`python myagent.py console`

Runs your agent in terminal mode, enabling local audio input and output for testing. This mode doesn't require external servers or dependencies and is useful for quickly validating behavior.

`python myagent.py dev`

Starts the agent server and enables hot reloading when files change. This mode allows each process to host multiple concurrent agents efficiently.

The agent connects to LiveKit Cloud or your self-hosted server. Set the following environment variables:

- LIVEKIT_URL
- LIVEKIT_API_KEY
- LIVEKIT_API_SECRET

You can connect using any LiveKit client SDK or telephony integration. To get started quickly, try the Agents Playground.

`python myagent.py start`

Runs the agent with production-ready optimizations.

The Agents framework is licensed under Apache-2.0. The LiveKit turn detection models are licensed under the LiveKit Model License.

The Agents framework is under active development in a rapidly evolving field. We welcome and appreciate contributions of any kind, be it feedback, bugfixes, features, new plugins and tools, or better documentation. You can file issues under this repo, open a PR, or chat with us in the LiveKit community.

This project uses uv for package management. To install dependencies for development:

`uv sync --all-extras --dev`

This project includes many examples in the `examples`

directory. To run them, create the file `examples/.env`

with credentials for LiveKit Server and any necessary model providers (see `examples/.env.example`

), then run:

`uv run examples/voice_agents/basic_agent.py dev`

For more information, see the examples README.

Unit tests are in the `tests`

directory and can be run with:

`uv run pytest --unit`

Integration tests for each plugin require various API credentials and run automatically in GitHub CI for PRs submitted by project maintainers. See the tests workflow for details.

This project uses ruff for formatting and linting:

```
uv run ruff format
uv run ruff check --fix
```

To generate docs locally with pdoc:

```
uv sync --all-extras --group docs
uv run --active pdoc --skip-errors --html --output-dir=docs livekit
```

| LiveKit Ecosystem | |
|---|---|
| Agents SDKs | Python · Node.js |
| LiveKit SDKs | Browser · Swift · Android · Flutter · React Native · Rust · Node.js · Python · Unity · Unity (WebGL) · ESP32 · C++ |
| Starter Apps | Python Agent · TypeScript Agent · React App · SwiftUI App · Android App · Flutter App · React Native App · Web Embed |
| UI Components | React · Android Compose · SwiftUI · Flutter |
| Server APIs | Node.js · Golang · Ruby · Java/Kotlin · Python · Rust · PHP (community) · .NET (community) |
| Resources | Docs · Docs MCP Server · CLI · LiveKit Cloud |
| LiveKit Server OSS | LiveKit server · Egress · Ingress · SIP |
| Community | Developer Community · Slack · X · YouTube |