---
title: abi/screenshot-to-code
source: https://github.com/abi/screenshot-to-code
author: []
published: ''
created: '2026-08-29'
manifest_dates:
- '2026-08-29'
description: 'Drop in a screenshot and convert it to clean code (HTML/Tailwind/React/Vue)https://screenshottocode.comscreenshot-to-code
  Convert screenshots, mockups, Figma designs, and screen recordings into clean, functional
  code using AI. The easiest way to try this is using the official, hosted product
  at screenshottocode.com → https://github.com/user-attachments/assets/ec08a5e6-9606-41c5-b03a-1bf47dfeba75
  Supported stacks: HTML + Tailwind HTML + CSS React + Tailwind Vue + Tailwind Bootstrap
  Ionic + Tailwind Default AI models: Gemini 3 Flash Preview and Gemini 3.1 Pro Preview
  - the best models GPT-5.5 and GPT-5.4 Mini Claude Opus 4.6, Claude Opus 4.8 z-image-turbo
  (using Replicate) for image generation See the Examples section below for more demos.
  Screenshot to Code also supports taking a screen recording of a website in action
  and turning that into a functional prototype. 🛠 Getting Started Choose the path
  that fits what you want to do: Run locally: best if you want to customize, self-host,
  or contribute. Use the hosted app: the fastest way to try Screenshot to Code with
  no local setup. Open the hosted app → Running locally requires API keys and a backend/frontend
  setup. The app has a React/Vite frontend and a FastAPI backend. API keys You need
  at least one model provider key (OpenAI, Anthropic, or Gemini). Gemini and Replicate
  are strongly recommended for the best quality of screenshot-to-code accuracy — Gemini
  powers asset extraction (reusing the real logos/images from your screenshot) and
  Replicate powers image generation, background removal, and image editing. Adding
  all four keys gives the best results and lets you compare multiple models per generation.
  Key Required? What it unlocks OPENAI_API_KEY One of these three GPT code-gen variants
  (GPT-5.5, GPT-5.4 Mini) ANTHROPIC_API_KEY One of these three Claude code-gen variants
  (Opus 5, Opus 4.8, Fable 5, Sonnet 4.6) GEMINI_API_KEY One of these three — strongly
  recommended Gemini code-gen variants (3 Flash, 3.1 Pro); extracts real assets from
  the screenshot; required for video mode REPLICATE_API_KEY Strongly recommended Image
  editing, background removal, and Replicate-backed image generation — without it,
  edit_images and remove_backgrounds are unavailable With more keys, the app automatically
  picks a stronger mix of models per variant; with a single key it uses that provider''s
  models only. If you''d like to run the app with Ollama open-source models (not recommended
  due to poor-quality results), follow this comment. Run the backend (I use Poetry
  for package management; run pip install --upgrade poetry if you don''t have it):
  cd backend echo "OPENAI_API_KEY=sk-your-key" > .env echo "ANTHROPIC_API_KEY=your-key"
  >> .env echo "GEMINI_API_KEY=your-key" >> .env echo "REPLICATE_API_KEY=r8_your-key"
  >> .env poetry install # Install the Chromium browser used by the screenshot preview
  tool. # On Linux, use `poetry run playwright install --with-deps chromium` to also
  # install the required system libraries (needs sudo/apt). poetry run playwright
  install chromium poetry env activate # run the printed command, e.g. source /path/to/venv/bin/activate
  poetry run uvicorn main:app --reload --port 7001 You can also set up OpenAI, Anthropic,
  and Gemini keys using the settings dialog in the frontend (click the gear icon after
  loading the app). Replicate must be configured in backend/.env as REPLICATE_API_KEY.
  The Settings dialog also shows whether screenshot preview is available on your backend.
  Screenshot preview (optional) lets the agent render its own generated page in a
  headless browser and visually check its work. It''s enabled automatically once Chromium
  is installed (the playwright install chromium step above, or automatically in the
  Docker image). If Chromium is missing, the app just skips the tool — the Settings
  dialog shows whether it''s available. Run the frontend: cd frontend pnpm install
  pnpm dev Open http://localhost:5173 to use the app. If you prefer to run the backend
  on a different port, update VITE_WS_BACKEND_URL in frontend/.env.local. Docker If
  you have Docker installed, run this from the root directory: echo "OPENAI_API_KEY=sk-your-key"
  > .env docker-compose up -d --build The app will be up and running at http://localhost:5173.
  Note that you can''t develop the application with this setup, as file changes won''t
  trigger a rebuild. 🙋‍♂️ FAQs I''m running into an error when setting up the backend.
  How can I fix it? Try this. If that still doesn''t work, open an issue. How do I
  get an OpenAI API key? See https://github.com/abi/screenshot-to-code/blob/main/Troubleshooting.md
  How can I configure an OpenAI proxy? If you''re not able to access the OpenAI API
  directly, for example because of country restrictions, you can try a VPN or configure
  the OpenAI base URL to use a proxy. Set OPENAI_BASE_URL in backend/.env or directly
  in the UI in the settings dialog. Make sure the URL has v1 in the path, for example:
  https://xxx.xxxxx.xxx/v1. How can I update the backend host that my frontend connects
  to? Configure VITE_HTTP_BACKEND_URL and VITE_WS_BACKEND_URL in frontend/.env.local.
  For example, set VITE_HTTP_BACKEND_URL=http://124.10.20.1:7001. Seeing UTF-8 errors
  when running the backend? On Windows, open the .env file with Notepad++, then go
  to Encoding and select UTF-8. How can I provide feedback? For feedback, feature
  requests, and bug reports, open an issue or ping me on Twitter. 📚 Examples NYTimes
  Original Replica Instagram https://github.com/user-attachments/assets/a335a105-f9cc-40e6-ac6b-64e5390bfc21
  Hacker News https://github.com/user-attachments/assets/205cb5c7-9c3c-438d-acd4-26dfe6e077e5'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: ef17663e884139a8
source_type: community_discussion
tldr: abi/screenshot-to-code 是使用 AI 将截图、mockup、Figma 设计稿和屏幕录制视频转换为可运行代码的开源项目，支持 HTML/Tailwind、React、Vue
  等技术栈，可本地运行或直接使用托管版 screenshottocode.com。
objective_summary: 开源项目 abi/screenshot-to-code 使用 AI 将截图、mockup、Figma 设计和屏幕录制视频转换为干净可运行的代码，支持
  HTML+Tailwind、React+Tailwind、Vue+Tailwind、Bootstrap、Ionic+Tailwind 等技术栈。该项目由 React/Vite
  前端与 FastAPI 后端组成，本地运行至少需要 OpenAI、Anthropic 或 Gemini 其中一家的 API 密钥，官方推荐搭配 Gemini 提取截图中的真实资产、Replicate
  负责图像生成与编辑。官方还提供托管产品 screenshottocode.com，无需本地配置即可直接在线试用。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - OpenAI
  - Anthropic
  - Google
  - Replicate
  - Ollama
  technologies:
  - Gemini
  - GPT-5.5
  - Claude Opus
  - z-image-turbo
  - FastAPI
  - React
  - Tailwind CSS
  - Vue
  - Bootstrap
  - Ionic
  - Playwright
  - Docker
  key_people: []
key_logic_flow:
- 该项目使用 AI 将截图、mockup、Figma 设计和屏幕录制视频转换为干净且可运行的代码，并支持将网站操作录屏转成功能原型。
- 项目支持 HTML+Tailwind、HTML+CSS、React+Tailwind、Vue+Tailwind、Bootstrap、Ionic+Tailwind
  等多种输出技术栈。
- 本地运行由 React/Vite 前端和 FastAPI 后端构成，至少需要 OpenAI、Anthropic 或 Gemini 三家中一家的模型密钥。
- 官方强烈推荐配置 Gemini 密钥用于从截图中提取真实 logo 与图片资产，配置 Replicate 密钥用于图像生成、背景移除和图片编辑。
- 可选的截图预览功能依赖 Playwright 安装的 Chromium 无头浏览器，让代理在浏览器中渲染并自检其生成的页面。
- 官方托管产品 screenshottocode.com 提供免本地配置的在线试用路径，项目同时支持 Docker Compose 一键启动。
object_mentions:
- object_type: project
  name: abi/screenshot-to-code
  canonical_name: abi/screenshot-to-code
  url: https://github.com/abi/screenshot-to-code
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 该开源项目使用 AI 将截图、mockup、Figma 设计和屏幕录制视频转换为干净且可运行的代码，支持多种主流前端技术栈。
  - 本地运行需要配置 React/Vite 前端与 FastAPI 后端，并至少提供 OpenAI、Anthropic 或 Gemini 其中一家的模型密钥。
  article_id: ef17663e884139a8
- object_type: product
  name: screenshottocode.com
  canonical_name: Screenshot to Code Hosted App
  url: https://screenshottocode.com
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 官方托管产品位于 screenshottocode.com，无需本地配置即可试用，是体验该项目功能的最快路径。
  article_id: ef17663e884139a8
extract_result: success
impact_score:
  score: 5.5
  reason: 该开源项目是 UI-to-Code 工具品类的标杆，GitHub 星标超六万，开创并验证了'截图→代码'这一细分赛道，且持续迭代（新增录屏转原型、多模型对比、Playwright
    渲染自检、Docker 一键部署等能力），对开发者工具链的局部竞争格局有实质性影响。但本次信息来源仅为项目主页 README，属成熟工具的常规更新而非重大版本发布或范式级突破，未改变行业整体技术路线。综合评分
    5.5。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 多模型自由组合（OpenAI/Anthropic/Gemini）与生成质量的可控性，以及本地自托管与托管版双路径的零门槛可及性
hype_assessment:
  level: low
  reason: 项目核心主张（截图/录屏转可运行代码）可通过实际使用复现验证，README 虽出现 'best models'、'clean code' 等营销性措辞，但功能真实存在且提供了详尽的本地部署步骤、密钥配置矩阵和
    Docker Compose 方案，无概念泡沫或伪创新。
information_entropy: medium
domain_disruption:
  technical_innovation: 构建了一条'视觉输入→多模型分工→浏览器自检'的完整 agentic 流水线：Gemini 负责从截图提取真实 logo/图片资产，Replicate
    负责图像生成、背景移除与图片编辑，代码生成 LLM 输出 HTML/React/Vue 等目标代码，最后由 Playwright 无头浏览器渲染生成页面并进行视觉自检。这种'生成-渲染-校验'闭环已成为整个
    UI 生成品类争相复刻的技术模板。
  business_model: 采用开源核心（AGPL）+ 托管 SaaS（screenshottocode.com）双轨模式，用户自带 API Key（BYOK）即可零配置使用，为
    AI 开发者工具提供了'开源获客、托管变现'的典型参考路径，同时验证了按垂直场景聚合分发多家模型 API 的商业可行性。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: 从复利视角评估：screenshot-to-code 在'设计转代码'细分赛道验证了真实且高频的需求（高 GitHub 星标、社区活跃、托管版
    screenshottocode.com 已有商业化路径），其多模型编排（OpenAI/Anthropic/Gemini 可切换）与本地自托管能力使其短期内有开发者口碑与网络效应积累空间，具备成为前端工作流标配的潜力。但长期看，核心价值高度依赖底层多模态模型的能力提升，而'视觉转代码'正在被
    v0、Lovable、Cursor、Figma 等更大平台内化为标准功能，独立工具的单点护城河偏薄，被平台级产品吸收或挤压的风险显著，属于'细分赛道基础设施但需持续验证'的
    4-7 分区间，给予 5.5 分。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- Google
- Replicate
- OpenAI
- Anthropic
- screenshottocode.com
competitive_casualty:
- Vercel v0
- Lovable
- Bolt.new
- 传统设计交付工具（Zeplin/Avocode/Anima）
- 付费低代码 UI 构建平台
market_opportunities:
- 开发者或团队可基于该开源项目的本地化架构，针对电商、企业内部系统、政务表单等垂直场景构建私有化'截图/设计稿→业务代码'微调方案，借助本地部署规避数据出境与供应商锁定问题
- 屏幕录制转功能原型的差异化能力值得产品设计工具赛道跟进，可将真实网站交互录屏快速转化为可交互原型，用于需求评审、低代码交付与售前演示等场景
- Gemini 真实资产提取（从截图还原真实 logo 与图片）方向可作为差异化切入点，创业者可开发面向品牌方的一键还原设计资产并输出规范代码的专项工具
risk_matrix:
  regulatory: 生成代码的版权归属不明确（可能复现受版权保护的 UI 设计）；部分国家/地区对第三方模型 API 的访问存在限制（README 中已提及需通过代理规避）；欧盟
    AI Act 等生成式 AI 可追溯性要求可能波及此类 UI→代码工具
  technological: 核心能力高度依赖 OpenAI/Anthropic/Gemini 等闭源商业模型，README 自认 Ollama 开源模型质量差，存在结构性供应商依赖；若底层模型或主流
    IDE 原生内置截图转代码能力，独立工具易被同质化淘汰；Playwright/Chromium 与 FastAPI 技术栈存在迭代漂移风险
  competitive: 赛道拥挤且巨头环伺，v0、Bolt.new、Lovable、Replit、Cursor 及 Figma AI 均提供类似 UI→代码能力并常以免费/订阅捆绑获客，开源项目与托管版
    screenshottocode.com 双重承压
  ethical: 该工具可被快速用于克隆竞品 App/网站 UI，加剧设计抄袭与知识产权争议；也可能被滥用将真实网站截图转化为可运行的仿冒前端，用于钓鱼页面或仿冒站，放大深度伪造类社会风险
  additional:
  - 多模型（OpenAI+Anthropic+Gemini+Replicate）组合的 token 与 API 调用成本不菲，托管版商业模式在模型降价与免费替代品冲击下盈利承压
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: monitor
object_insights:
- object_type: project
  name: abi/screenshot-to-code
  canonical_name: abi/screenshot-to-code
  url: https://github.com/abi/screenshot-to-code
  positioning: 使用 AI 将截图、mockup、Figma 设计稿和屏幕录制视频一键转换为可运行前端代码的开源项目，支持多种主流技术栈并配套托管版在线产品。
  technical_signal: 项目由 React/Vite 前端与 FastAPI 后端组成，支持 HTML/Tailwind、React、Vue、Bootstrap
    等多种输出技术栈，并可通过 Playwright 无头浏览器预览生成页面。
  adoption_signal: 项目为 GitHub 热门开源仓库，官方提供 screenshottocode.com 托管服务降低使用门槛，并支持 Docker
    Compose 一键部署。
  ecosystem_relevance: 深度依赖 OpenAI、Anthropic、Gemini 与 Replicate 等多家模型服务，是生成式前端编码工具链的代表性开源实现，推动设计到代码工作流的落地。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该项目是设计到代码（Design-to-Code）赛道的标杆开源项目，同时覆盖截图、Figma 和录屏多种输入形态，并持续接入 GPT-5.5、Claude
    Opus、Gemini 3 等最新模型，能直观反映多模态编码能力的前沿进展与商业化路径，值得持续跟踪。
  risk_notes:
  - 本地运行需要同时配置多家模型密钥，依赖成本较高，配置门槛影响普通用户的试用体验。
  - 输出代码质量受所接模型能力制约，官方提示 Ollama 开源模型效果不佳，生成结果存在不确定性。
  score: 7.0
  article_ids:
  - ef17663e884139a8
  evidence_snippets:
  - 该开源项目使用 AI 将截图、mockup、Figma 设计和屏幕录制视频转换为干净且可运行的代码，支持多种主流前端技术栈。
  - 本地运行需要配置 React/Vite 前端与 FastAPI 后端，并至少提供 OpenAI、Anthropic 或 Gemini 其中一家的模型密钥。
---

Convert screenshots, mockups, Figma designs, and screen recordings into clean, functional code using AI. The easiest way to try this is using the official, hosted product at screenshottocode.com →

## youtube.mp4

Supported stacks:

- HTML + Tailwind
- HTML + CSS
- React + Tailwind
- Vue + Tailwind
- Bootstrap
- Ionic + Tailwind

Default AI models:

- Gemini 3 Flash Preview and Gemini 3.1 Pro Preview - the best models
- GPT-5.5 and GPT-5.4 Mini
- Claude Opus 4.6, Claude Opus 4.8
- z-image-turbo (using Replicate) for image generation

See the Examples section below for more demos.

Screenshot to Code also supports taking a screen recording of a website in action and turning that into a functional prototype.

Choose the path that fits what you want to do:

**Run locally:**best if you want to customize, self-host, or contribute.**Use the hosted app:**the fastest way to try Screenshot to Code with no local setup. Open the hosted app →

Running locally requires API keys and a backend/frontend setup. The app has a React/Vite frontend and a FastAPI backend.

You need **at least one** model provider key (OpenAI, Anthropic, or Gemini).
**Gemini and Replicate are strongly recommended for the best quality of
screenshot-to-code accuracy** — Gemini powers asset extraction (reusing the
real logos/images from your screenshot) and Replicate powers image
generation, background removal, and image editing. Adding all four keys gives
the best results and lets you compare multiple models per generation.

| Key | Required? | What it unlocks |
|---|---|---|
`OPENAI_API_KEY` |
One of these three | GPT code-gen variants (GPT-5.5, GPT-5.4 Mini) |
`ANTHROPIC_API_KEY` |
One of these three | Claude code-gen variants (Opus 5, Opus 4.8, Fable 5, Sonnet 4.6) |
`GEMINI_API_KEY` |
One of these three — strongly recommended |
Gemini code-gen variants (3 Flash, 3.1 Pro); extracts real assets from the screenshot; required for video mode |
`REPLICATE_API_KEY` |
Strongly recommended |
Image editing, background removal, and Replicate-backed image generation — without it, `edit_images` and `remove_backgrounds` are unavailable |

With more keys, the app automatically picks a stronger mix of models per variant; with a single key it uses that provider's models only.

If you'd like to run the app with Ollama open-source models (not recommended due to poor-quality results), follow this comment.

Run the backend (I use Poetry for package management; run `pip install --upgrade poetry`

if you don't have it):

```
cd backend
echo "OPENAI_API_KEY=sk-your-key" > .env
echo "ANTHROPIC_API_KEY=your-key" >> .env
echo "GEMINI_API_KEY=your-key" >> .env
echo "REPLICATE_API_KEY=r8_your-key" >> .env
poetry install
# Install the Chromium browser used by the screenshot preview tool.
# On Linux, use `poetry run playwright install --with-deps chromium` to also
# install the required system libraries (needs sudo/apt).
poetry run playwright install chromium
poetry env activate
# run the printed command, e.g. source /path/to/venv/bin/activate
poetry run uvicorn main:app --reload --port 7001
```

You can also set up OpenAI, Anthropic, and Gemini keys using the settings dialog in the frontend (click the gear icon after loading the app). Replicate must be configured in `backend/.env`

as `REPLICATE_API_KEY`

. The Settings dialog also shows whether **screenshot preview** is available on your backend.


Screenshot preview(optional) lets the agent render its own generated page in a headless browser and visually check its work. It's enabled automatically once Chromium is installed (the`playwright install chromium`

step above, or automatically in the Docker image). If Chromium is missing, the app just skips the tool — the Settings dialog shows whether it's available.

Run the frontend:

```
cd frontend
pnpm install
pnpm dev
```

Open http://localhost:5173 to use the app.

If you prefer to run the backend on a different port, update `VITE_WS_BACKEND_URL`

in `frontend/.env.local`

.

If you have Docker installed, run this from the root directory:

```
echo "OPENAI_API_KEY=sk-your-key" > .env
docker-compose up -d --build
```

The app will be up and running at http://localhost:5173. Note that you can't develop the application with this setup, as file changes won't trigger a rebuild.

**I'm running into an error when setting up the backend. How can I fix it?**Try this. If that still doesn't work, open an issue.**How do I get an OpenAI API key?**See https://github.com/abi/screenshot-to-code/blob/main/Troubleshooting.md**How can I configure an OpenAI proxy?**If you're not able to access the OpenAI API directly, for example because of country restrictions, you can try a VPN or configure the OpenAI base URL to use a proxy. Set`OPENAI_BASE_URL`

in`backend/.env`

or directly in the UI in the settings dialog. Make sure the URL has`v1`

in the path, for example:`https://xxx.xxxxx.xxx/v1`

.**How can I update the backend host that my frontend connects to?**Configure`VITE_HTTP_BACKEND_URL`

and`VITE_WS_BACKEND_URL`

in`frontend/.env.local`

. For example, set`VITE_HTTP_BACKEND_URL=http://124.10.20.1:7001`

.**Seeing UTF-8 errors when running the backend?**On Windows, open the`.env`

file with Notepad++, then go to Encoding and select UTF-8.**How can I provide feedback?**For feedback, feature requests, and bug reports, open an issue or ping me on Twitter.

**NYTimes**

| Original | Replica |
|---|---|

**Instagram**

## instagram.mp4

**Hacker News**