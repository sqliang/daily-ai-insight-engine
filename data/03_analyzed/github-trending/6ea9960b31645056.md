---
title: shiyu-coder/Kronos
source: https://github.com/shiyu-coder/Kronos
author: []
published: ''
created: '2026-05-26'
description: 'Kronos: A Foundation Model for the Language of Financial Markets Kronos:
  A Foundation Model for the Language of Financial Markets Deutsch | Español | Français
  | 日本語 | 한국어 | Português | Русский | 中文 Kronos is the first open-source foundation
  model for financial candlesticks (K-lines), trained on data from over 45 global
  exchanges. 📰 News 🚩 [2025.11.10] Kronos has been accpeted by AAAI 2026. 🚩 [2025.08.17]
  We have released the scripts for fine-tuning! Check them out to adapt Kronos to
  your own tasks. 🚩 [2025.08.02] Our paper is now available on arXiv! 📜 Introduction
  Kronos is a family of decoder-only foundation models, pre-trained specifically for
  the "language" of financial markets—K-line sequences. Unlike general-purpose TSFMs,
  Kronos is designed to handle the unique, high-noise characteristics of financial
  data. It leverages a novel two-stage framework: A specialized tokenizer first quantizes
  continuous, multi-dimensional K-line data (OHLCV) into hierarchical discrete tokens.
  A large, autoregressive Transformer is then pre-trained on these tokens, enabling
  it to serve as a unified model for diverse quantitative tasks. ✨ Live Demo We have
  set up a live demo to visualize Kronos''s forecasting results. The webpage showcases
  a forecast for the BTC/USDT trading pair over the next 24 hours. 👉 Access the Live
  Demo Here 📦 Model Zoo We release a family of pre-trained models with varying capacities
  to suit different computational and application needs. All models are readily accessible
  from the Hugging Face Hub. Model Tokenizer Context length Params Open-source Kronos-mini
  Kronos-Tokenizer-2k 2048 4.1M ✅ NeoQuasar/Kronos-mini Kronos-small Kronos-Tokenizer-base
  512 24.7M ✅ NeoQuasar/Kronos-small Kronos-base Kronos-Tokenizer-base 512 102.3M
  ✅ NeoQuasar/Kronos-base Kronos-large Kronos-Tokenizer-base 512 499.2M ❌ 🚀 Getting
  Started Installation Install Python 3.10+, and then install the dependencies: pip
  install -r requirements.txt 📈 Making Forecasts Forecasting with Kronos is straightforward
  using the KronosPredictor class. It handles data preprocessing, normalization, prediction,
  and inverse normalization, allowing you to get from raw data to forecasts in just
  a few lines of code. Important Note: The max_context for Kronos-small and Kronos-base
  is 512. This is the maximum sequence length the model can process. For optimal performance,
  it is recommended that your input data length (i.e., lookback) does not exceed this
  limit. The KronosPredictor will automatically handle truncation for longer contexts.
  Here is a step-by-step guide to making your first forecast. 1. Load the Tokenizer
  and Model First, load a pre-trained Kronos model and its corresponding tokenizer
  from the Hugging Face Hub. from model import Kronos, KronosTokenizer, KronosPredictor
  # Load from Hugging Face Hub tokenizer = KronosTokenizer.from_pretrained("NeoQuasar/Kronos-Tokenizer-base")
  model = Kronos.from_pretrained("NeoQuasar/Kronos-small") 2. Instantiate the Predictor
  Create an instance of KronosPredictor, passing the model, tokenizer, and desired
  device. # Initialize the predictor predictor = KronosPredictor(model, tokenizer,
  max_context=512) 3. Prepare Input Data The predict method requires three main inputs:
  df: A pandas DataFrame containing the historical K-line data. It must include columns
  [''open'', ''high'', ''low'', ''close'']. volume and amount are optional. x_timestamp:
  A pandas Series of timestamps corresponding to the historical data in df. y_timestamp:
  A pandas Series of timestamps for the future periods you want to predict. import
  pandas as pd # Load your data df = pd.read_csv("./data/XSHG_5min_600977.csv") df[''timestamps'']
  = pd.to_datetime(df[''timestamps'']) # Define context window and prediction length
  lookback = 400 pred_len = 120 # Prepare inputs for the predictor x_df = df.loc[:lookback-1,
  [''open'', ''high'', ''low'', ''close'', ''volume'', ''amount'']] x_timestamp =
  df.loc[:lookback-1, ''timestamps''] y_timestamp = df.loc[lookback:lookback+pred_len-1,
  ''timestamps''] 4. Generate Forecasts Call the predict method to generate forecasts.
  You can control the sampling process with parameters like T, top_p, and sample_count
  for probabilistic forecasting. # Generate predictions pred_df = predictor.predict(
  df=x_df, x_timestamp=x_timestamp, y_timestamp=y_timestamp, pred_len=pred_len, T=1.0,
  # Temperature for sampling top_p=0.9, # Nucleus sampling probability sample_count=1
  # Number of forecast paths to generate and average ) print("Forecasted Data Head:")
  print(pred_df.head()) The predict method returns a pandas DataFrame containing the
  forecasted values for open, high, low, close, volume, and amount, indexed by the
  y_timestamp you provided. For efficient processing of multiple time series, Kronos
  provides a predict_batch method that enables parallel prediction on multiple datasets
  simultaneously. This is particularly useful when you need to forecast multiple assets
  or time periods at once. # Prepare multiple datasets for batch prediction df_list
  = [df1, df2, df3] # List of DataFrames x_timestamp_list = [x_ts1, x_ts2, x_ts3]
  # List of historical timestamps y_timestamp_list = [y_ts1, y_ts2, y_ts3] # List
  of future timestamps # Generate batch predictions pred_df_list = predictor.predict_batch(
  df_list=df_list, x_timestamp_list=x_timestamp_list, y_timestamp_list=y_timestamp_list,
  pred_len=pred_len, T=1.0, top_p=0.9, sample_count=1, verbose=True ) # pred_df_list
  contains prediction results in the same order as input for i, pred_df in enumerate(pred_df_list):
  print(f"Predictions for series {i}:") print(pred_df.head()) Important Requirements
  for Batch Prediction: All series must have the same historical length (lookback
  window) All series must have the same prediction length (pred_len) Each DataFrame
  must contain the required columns: [''open'', ''high'', ''low'', ''close''] volume
  and amount columns are optional and will be filled with zeros if missing The predict_batch
  method leverages GPU parallelism for efficient processing and automatically handles
  normalization and denormalization for each series independently. 5. Example and
  Visualization For a complete, runnable script that includes data loading, prediction,
  and plotting, please see examples/prediction_example.py. Running this script will
  generate a plot comparing the ground truth data against the model''s forecast, similar
  to the one shown below: Additionally, we provide a script that makes predictions
  without Volume and Amount data, which can be found in examples/prediction_wo_vol_example.py.
  🔧 Finetuning on Your Own Data (A-Share Market Example) We provide a complete pipeline
  for finetuning Kronos on your own datasets. As an example, we demonstrate how to
  use Qlib to prepare data from the Chinese A-share market and conduct a simple backtest.
  Disclaimer: This pipeline is intended as a demonstration to illustrate the finetuning
  process. It is a simplified example and not a production-ready quantitative trading
  system. A robust quantitative strategy requires more sophisticated techniques, such
  as portfolio optimization and risk factor neutralization, to achieve stable alpha.
  The finetuning process is divided into four main steps: Configuration: Set up paths
  and hyperparameters. Data Preparation: Process and split your data using Qlib. Model
  Finetuning: Finetune the Tokenizer and the Predictor models. Backtesting: Evaluate
  the finetuned model''s performance. Prerequisites First, ensure you have all dependencies
  from requirements.txt installed. This pipeline relies on qlib. Please install it:
  pip install pyqlib You will need to prepare your Qlib data. Follow the official
  Qlib guide to download and set up your data locally. The example scripts assume
  you are using daily frequency data. Step 1: Configure Your Experiment All settings
  for data, training, and model paths are centralized in finetune/config.py. Before
  running any scripts, please modify the following paths according to your environment:
  qlib_data_path: Path to your local Qlib data directory. dataset_path: Directory
  where the processed train/validation/test pickle files will be saved. save_path:
  Base directory for saving model checkpoints. backtest_result_path: Directory for
  saving backtesting results. pretrained_tokenizer_path and pretrained_predictor_path:
  Paths to the pre-trained models you want to start from (can be local paths or Hugging
  Face model names). You can also adjust other parameters like instrument, train_time_range,
  epochs, and batch_size to fit your specific task. If you don''t use Comet.ml, set
  use_comet = False. Step 2: Prepare the Dataset Run the data preprocessing script.
  This script will load raw market data from your Qlib directory, process it, split
  it into training, validation, and test sets, and save them as pickle files. python
  finetune/qlib_data_preprocess.py After running, you will find train_data.pkl, val_data.pkl,
  and test_data.pkl in the directory specified by dataset_path in your config. Step
  3: Run the Finetuning The finetuning process consists of two stages: finetuning
  the tokenizer and then the predictor. Both training scripts are designed for multi-GPU
  training using torchrun. 3.1 Finetune the Tokenizer This step adjusts the tokenizer
  to the data distribution of your specific domain. # Replace NUM_GPUS with the number
  of GPUs you want to use (e.g., 2) torchrun --standalone --nproc_per_node=NUM_GPUS
  finetune/train_tokenizer.py The best tokenizer checkpoint will be saved to the path
  configured in config.py (derived from save_path and tokenizer_save_folder_name).
  3.2 Finetune the Predictor This step finetunes the main Kronos model for the forecasting
  task. # Replace NUM_GPUS with the number of GPUs you want to use (e.g., 2) torchrun
  --standalone --nproc_per_node=NUM_GPUS finetune/train_predictor.py The best predictor
  checkpoint will be saved to the path configured in config.py. Step 4: Evaluate with
  Backtesting Finally, run the backtesting script to evaluate your finetuned model.
  This script loads the models, performs inference on the test set, generates prediction
  signals (e.g., forecasted price change), and runs a simple top-K strategy backtest.
  # Specify the GPU for inference python finetune/qlib_test.py --device cuda:0 The
  script will output a detailed performance analysis in your console and generate
  a plot showing the cumulative return curves of your strategy against the benchmark,
  similar to the one below: 💡 From Demo to Production: Important Considerations Raw
  Signals vs. Pure Alpha: The signals generated by the model in this demo are raw
  predictions. In a real-world quantitative workflow, these signals would typically
  be fed into a portfolio optimization model. This model would apply constraints to
  neutralize exposure to common risk factors (e.g., market beta, style factors like
  size and value), thereby isolating the "pure alpha" and improving the strategy''s
  robustness. Data Handling: The provided QlibDataset is an example. For different
  data sources or formats, you will need to adapt the data loading and preprocessing
  logic. Strategy and Backtesting Complexity: The simple top-K strategy used here
  is a basic starting point. Production-level strategies often incorporate more complex
  logic for portfolio construction, dynamic position sizing, and risk management (e.g.,
  stop-loss/take-profit rules). Furthermore, a high-fidelity backtest should meticulously
  model transaction costs, slippage, and market impact to provide a more accurate
  estimate of real-world performance. 📝 AI-Generated Comments: Please note that many
  of the code comments within the finetune/ directory were generated by an AI assistant
  (Gemini 2.5 Pro) for explanatory purposes. While they aim to be helpful, they may
  contain inaccuracies. We recommend treating the code itself as the definitive source
  of logic. 📖 Citation If you use Kronos in your research, we would appreciate a citation
  to our paper: @misc{shi2025kronos, title={Kronos: A Foundation Model for the Language
  of Financial Markets}, author={Yu Shi and Zongliang Fu and Shuo Chen and Bohan Zhao
  and Wei Xu and Changshui Zhang and Jian Li}, year={2025}, eprint={2508.02739}, archivePrefix={arXiv},
  primaryClass={q-fin.ST}, url={https://arxiv.org/abs/2508.02739}, } 📜 License This
  project is licensed under the MIT License.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 6ea9960b31645056
source_type: community_discussion
tldr: Kronos 是首个面向金融K线数据的开源基础模型，在45个以上全球交易所数据上训练，采用两阶段架构（专用分词器将OHLCV数据量化为层次离散Token
  + 自回归Transformer预训练），模型系列已开源至Hugging Face，论文已被AAAI 2026接收。
objective_summary: 研究团队（Yu Shi等人）于2025年发布了Kronos，一个专为金融K线序列设计的解码器-only基础模型家族。该模型通过两阶段框架——先用专用分词器将多维OHLCV数据量化为层次离散Token，再在大规模自回归Transformer上进行预训练——来捕捉金融市场的语言模式。模型提供mini（4.1M参数）、small（24.7M参数）、base（102.3M参数）三个开源版本，均已上传至Hugging
  Face Hub的NeoQuasar组织下。论文于2025年8月发布在arXiv上，并于2025年11月被AAAI 2026接收。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - NeoQuasar
  technologies:
  - Kronos
  - K-line
  - OHLCV
  - Transformer
  key_people:
  - Yu Shi
  - Zongliang Fu
  - Shuo Chen
  - Bohan Zhao
  - Wei Xu
  - Changshui Zhang
  - Jian Li
key_logic_flow:
- Kronos 是首个开源的金融K线基础模型，在超过45个全球交易所的数据上训练。
- 模型采用两阶段框架：专用分词器将连续多维OHLCV数据量化为层次离散Token，再通过自回归Transformer进行预训练。
- Kronos 家族包含多个参数规模的模型，其中mini（4.1M）、small（24.7M）、base（102.3M）三个版本已开源至Hugging Face
  Hub。
- 论文于2025年8月发布在arXiv上，并于2025年11月被AAAI 2026会议接收。
- 项目提供完整的微调流程示例，包括使用Qlib进行中国A股市场的数据准备和回测脚本。
- Kronos 提供了KronosPredictor和predict_batch等接口，支持单序列预测和多序列批量并行预测。
extract_result: success
object_mentions:
- object_type: project
  name: shiyu-coder/Kronos
  canonical_name: shiyu-coder/Kronos
  url: https://github.com/shiyu-coder/Kronos
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Kronos 是首个开源的金融K线基础模型，在超过45个全球交易所的数据上训练。
  - 项目提供了完整的微调流程，包括使用Qlib进行中国A股市场的数据准备和回测脚本。
  - 该项目已发布论文至arXiv，并被AAAI 2026会议接收。
  article_id: 6ea9960b31645056
- object_type: model
  name: Kronos-mini
  canonical_name: NeoQuasar/Kronos-mini
  url: https://huggingface.co/NeoQuasar/Kronos-mini
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Kronos-mini 使用 Kronos-Tokenizer-2k 分词器，上下文长度2048，参数量4.1M，已开源至 Hugging Face Hub。
  - 该模型是 Kronos 家族中最小规模的版本，适合计算资源有限的场景。
  article_id: 6ea9960b31645056
- object_type: model
  name: Kronos-small
  canonical_name: NeoQuasar/Kronos-small
  url: https://huggingface.co/NeoQuasar/Kronos-small
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Kronos-small 使用 Kronos-Tokenizer-base 分词器，上下文长度512，参数量24.7M，已开源至 Hugging Face
    Hub。
  - 该模型通过 KronosPredictor 接口可直接用于金融K线数据的预测任务。
  article_id: 6ea9960b31645056
- object_type: model
  name: Kronos-base
  canonical_name: NeoQuasar/Kronos-base
  url: https://huggingface.co/NeoQuasar/Kronos-base
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Kronos-base 使用 Kronos-Tokenizer-base 分词器，上下文长度512，参数量102.3M，已开源至 Hugging Face
    Hub。
  - 该模型是 Kronos 家族中参数量最大的开源版本，适合更复杂的金融预测任务。
  article_id: 6ea9960b31645056
- object_type: paper
  name: 'Kronos: A Foundation Model for the Language of Financial Markets'
  canonical_name: 'Kronos: A Foundation Model for the Language of Financial Markets'
  url: https://arxiv.org/abs/2508.02739
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - '论文标题为《Kronos: A Foundation Model for the Language of Financial Markets》，作者包括Yu
    Shi、Zongliang Fu等人。'
  - 论文于2025年8月发布于arXiv，并于2025年11月被AAAI 2026会议接收。
  article_id: 6ea9960b31645056
impact_score:
  score: 5.5
  reason: Kronos 是首个面向金融K线数据的开源基础模型，填补了该垂直领域的空白，论文被 AAAI 2026 接收增强了学术可信度。然而，模型参数量最大仅
    499M、上下文窗口仅 512，属于 Transformer 架构在特定数据模态上的应用而非基础架构突破。对量化金融圈有实际影响，但不足以引发全行业范式转移。综合评估为中等偏上的产品级发布，短期冲击力
    5.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 首个金融K线基础模型的实际预测能力与 Kronos-large 未开源引发的公平性争议
hype_assessment:
  level: medium
  reason: 项目 README 整体技术导向、代码示例丰富，但'首个''foundation model'等措辞存在一定拔高。最大规模的 Kronos-large（499M）明确标注不开源，使得'开源基础模型'的定位打了折扣，存在一定的宣传包装成分。好在有
    AAAI 2026 接收作为学术背书，整体水分可控。
information_entropy: high
domain_disruption:
  technical_innovation: 核心创新在于分层离散化 tokenizer——将连续多维 OHLCV 数据量化为层次化离散 token，使金融K线数据能够被标准
    decoder-only Transformer 以自回归方式建模。这本质上将金融时序预测问题转化为'金融语言建模'问题，是 Transformer 范式在非自然语言领域的创造性迁移。tokenizer
    设计（Kronos-Tokenizer-2k vs Kronos-Tokenizer-base）直接决定模型的上下文表征粒度。
  business_model: MIT 许可证开源（除 Kronos-large 外）+ Hugging Face 分发 + Qlib 生态集成的组合，形成'开源引流、闭源变现'的典型商业模式雏形。与微软
    Qlib 平台的深度整合使其能触达中国 A 股量化开发者社区，具备从学术项目转向商业化量化工具平台的潜力。
engineering_complexity: production_ready
compound_value:
  score: 6.0
  reason: Kronos作为首个面向金融K线数据的开源基础模型，具备明确的首发优势和学术背书（AAAI 2026）。长期复利效应的支撑逻辑：（1）MIT许可证降低了金融量化的AI准入门槛，有望形成社区生态——更多用户微调→更多下游任务适配→生态飞轮启动；（2）金融K线语言化（tokenization+自回归预测）的范式创新具有可迁移性，可能成为金融时序建模的新标准路径。限制因素：（1）金融市场的高度非平稳性意味着历史模式的复利积累可能被市场结构变迁打断，模型需要持续重训练而非自然累积；（2）最大模型（499M参数）未开源，破坏了开源基础模型的价值闭环——社区无法基于最强版本迭代，商业化意图与开源承诺之间存在张力；（3）团队商业化路径不明，单篇论文+小团队能否支撑持续研发与模型更新存疑。综合判断：有潜力成为金融时序建模的细分赛道基础设施，但需2-3年持续验证其在真实交易场景中的有效性。
value_capture_layer: foundation_model
moat_impact: democratizes_access
key_beneficiaries:
- NeoQuasar
- Hugging Face
- Microsoft (Qlib)
- Quantitative hedge funds
- Fintech startups
competitive_casualty:
- Bloomberg
- Refinitiv
- Proprietary quant platforms
- High-cost financial ML SaaS vendors
market_opportunities:
- 量化交易团队可基于Kronos开源模型（MIT许可）开发面向A股、加密货币等特定市场的微调量化策略，并结合Qlib回测流水线快速验证策略有效性，降低从零训练的成本
- 金融科技SaaS创业者可将Kronos封装为K线预测API服务，面向中小型基金和独立交易者提供多资产并行预测能力，利用predict_batch接口实现规模化推理
- AI+金融交叉领域从业者可深入研究Kronos的分层离散化tokenizer设计思路，将'金融数据 token化 + 自回归Transformer'范式迁移至订单流、高频tick数据等其他金融时序场景
risk_matrix:
  regulatory: 金融预测模型在多个司法管辖区面临合规风险：中国证监会对AI辅助投资决策有严格备案要求，欧盟AI Act将金融信用评估类AI列为高风险，美国SEC对算法交易策略有注册和审查义务。若Kronos被用于实际投资决策，使用者需评估当地监管框架，避免因未备案AI交易系统而受罚。
  technological: 金融市场具有非平稳性（regime change），历史数据训练的模型在市场结构突变时可能系统性失效；decoder-only架构在长序列金融预测中尚未被充分验证优于传统时序模型（如PatchTST、TimesFM等）；论文虽被AAAI
    2026接收，但学术接受不等于实盘有效，存在实盘表现不及预期的替代风险。
  competitive: 摩根大通、Two Sigma、Citadel等顶级量化机构拥有数亿美元投入的私有金融大模型且数据壁垒极高；Google DeepMind的TimesFM、IBM的TinyTimeMixers等通用时序基础模型也在向金融领域渗透；若OpenAI/Grok等巨头发布金融专用模型，开源Kronos可能面临生态挤压。
  ethical: 金融预测模型若被广泛使用，可能导致自证预言效应——大量资金基于同一模型的预测同向操作，加剧市场波动和羊群效应；模型的不透明性（黑箱预测）可能使散户在不知情下面临被机构'收割'的信息不对称风险；A股微调数据若包含未来信息泄露或数据窥探偏差，可能导致回测优秀、实盘亏损的误导性结果。
  additional:
  - 数据质量风险：45个全球交易所的数据覆盖可能不均衡，新兴市场K线数据质量和规范性差异大，模型在特定市场的泛化能力未经充分验证
  - 操作风险：KronosPredictor自动处理归一化和截断，使用者可能在不知情下输入非平稳数据或超出max_context的序列，导致静默预测偏差而无从察觉
confidence:
  impact: medium
  compound: medium
  hype: high
actionable_insight: monitor
object_insights:
- object_type: project
  name: shiyu-coder/Kronos
  canonical_name: shiyu-coder/Kronos
  url: https://github.com/shiyu-coder/Kronos
  positioning: Kronos 是首个面向金融K线数据的开源基础模型家族，在45个以上全球交易所数据上训练，提供多参数规模版本，已被AAAI 2026接收。
  technical_signal: 采用两阶段架构：专用分词器将OHLCV数据量化为层次离散Token，再通过自回归Transformer进行大规模预训练，已被AAAI
    2026接收。
  adoption_signal: 三个参数规模版本（mini 4.1M、small 24.7M、base 102.3M）已开源至Hugging Face Hub，提供完整微调流程示例及基于Qlib的中国A股回测脚本。
  ecosystem_relevance: 模型系列托管于Hugging Face Hub的NeoQuasar组织下，与Qlib量化平台集成，提供中国A股市场全流程数据准备和回测支持。
  target_users:
  - 量化金融研究员
  - 金融科技开发者
  - 金融数据分析师
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Kronos 填补了金融K线领域开源基础模型的空白，AAAI 2026接收验证了其学术价值；多参数规模满足不同场景需求，完整工具链降低了量化研究门槛，其两阶段分词架构对金融时序建模有启发性。
  risk_notes:
  - 金融时序预测模型在极端市场条件下存在过拟合风险，历史K线模式在不同市场阶段和品种间的泛化能力需要谨慎验证。
  - 模型最大上下文长度偏短（512-2048），对长周期预测任务可能受限；最宽参数版本Kronos-large尚未开源，限制了更大参数规模的探索。
  score: 7.0
  article_ids:
  - 6ea9960b31645056
  evidence_snippets:
  - Kronos 是首个开源的金融K线基础模型，在超过45个全球交易所的数据上训练，提供从4.1M到499.2M的多参数规模版本。
  - 项目提供了完整的微调流程，包括使用Qlib进行中国A股市场的数据准备和回测脚本，支持KronosPredictor等高层接口。
  - 该项目已发布论文至arXiv，并被AAAI 2026会议接收，两阶段离散Token化架构为金融时序建模提供了新方案。
---

Kronos is the

first open-source foundation modelfor financial candlesticks (K-lines), trained on data from over45 global exchanges.

- 🚩
**[2025.11.10]**Kronos has been accpeted by AAAI 2026. - 🚩
**[2025.08.17]**We have released the scripts for fine-tuning! Check them out to adapt Kronos to your own tasks. - 🚩
**[2025.08.02]**Our paper is now available on arXiv!


**Kronos** is a family of decoder-only foundation models, pre-trained specifically for the "language" of financial markets—K-line sequences. Unlike general-purpose TSFMs, Kronos is designed to handle the unique, high-noise characteristics of financial data. It leverages a novel two-stage framework:

- A specialized tokenizer first quantizes continuous, multi-dimensional K-line data (OHLCV) into
**hierarchical discrete tokens**. - A large, autoregressive Transformer is then pre-trained on these tokens, enabling it to serve as a unified model for diverse quantitative tasks.

We have set up a live demo to visualize Kronos's forecasting results. The webpage showcases a forecast for the **BTC/USDT** trading pair over the next 24 hours.

We release a family of pre-trained models with varying capacities to suit different computational and application needs. All models are readily accessible from the Hugging Face Hub.

| Model | Tokenizer | Context length | Params | Open-source |
|---|---|---|---|---|
| Kronos-mini | Kronos-Tokenizer-2k | 2048 | 4.1M | ✅ NeoQuasar/Kronos-mini |
| Kronos-small | Kronos-Tokenizer-base | 512 | 24.7M | ✅ NeoQuasar/Kronos-small |
| Kronos-base | Kronos-Tokenizer-base | 512 | 102.3M | ✅ NeoQuasar/Kronos-base |
| Kronos-large | Kronos-Tokenizer-base | 512 | 499.2M | ❌ |

- Install Python 3.10+, and then install the dependencies:

`pip install -r requirements.txt`

Forecasting with Kronos is straightforward using the `KronosPredictor`

class. It handles data preprocessing, normalization, prediction, and inverse normalization, allowing you to get from raw data to forecasts in just a few lines of code.

**Important Note**: The `max_context`

for `Kronos-small`

and `Kronos-base`

is **512**. This is the maximum sequence length the model can process. For optimal performance, it is recommended that your input data length (i.e., `lookback`

) does not exceed this limit. The `KronosPredictor`

will automatically handle truncation for longer contexts.

Here is a step-by-step guide to making your first forecast.

First, load a pre-trained Kronos model and its corresponding tokenizer from the Hugging Face Hub.

```
from model import Kronos, KronosTokenizer, KronosPredictor
# Load from Hugging Face Hub
tokenizer = KronosTokenizer.from_pretrained("NeoQuasar/Kronos-Tokenizer-base")
model = Kronos.from_pretrained("NeoQuasar/Kronos-small")
```

Create an instance of `KronosPredictor`

, passing the model, tokenizer, and desired device.

```
# Initialize the predictor
predictor = KronosPredictor(model, tokenizer, max_context=512)
```

The `predict`

method requires three main inputs:

`df`

: A pandas DataFrame containing the historical K-line data. It must include columns`['open', 'high', 'low', 'close']`

.`volume`

and`amount`

are optional.`x_timestamp`

: A pandas Series of timestamps corresponding to the historical data in`df`

.`y_timestamp`

: A pandas Series of timestamps for the future periods you want to predict.

```
import pandas as pd
# Load your data
df = pd.read_csv("./data/XSHG_5min_600977.csv")
df['timestamps'] = pd.to_datetime(df['timestamps'])
# Define context window and prediction length
lookback = 400
pred_len = 120
# Prepare inputs for the predictor
x_df = df.loc[:lookback-1, ['open', 'high', 'low', 'close', 'volume', 'amount']]
x_timestamp = df.loc[:lookback-1, 'timestamps']
y_timestamp = df.loc[lookback:lookback+pred_len-1, 'timestamps']
```

Call the `predict`

method to generate forecasts. You can control the sampling process with parameters like `T`

, `top_p`

, and `sample_count`

for probabilistic forecasting.

```
# Generate predictions
pred_df = predictor.predict(
df=x_df,
x_timestamp=x_timestamp,
y_timestamp=y_timestamp,
pred_len=pred_len,
T=1.0, # Temperature for sampling
top_p=0.9, # Nucleus sampling probability
sample_count=1 # Number of forecast paths to generate and average
)
print("Forecasted Data Head:")
print(pred_df.head())
```

The `predict`

method returns a pandas DataFrame containing the forecasted values for `open`

, `high`

, `low`

, `close`

, `volume`

, and `amount`

, indexed by the `y_timestamp`

you provided.

For efficient processing of multiple time series, Kronos provides a `predict_batch`

method that enables parallel prediction on multiple datasets simultaneously. This is particularly useful when you need to forecast multiple assets or time periods at once.

```
# Prepare multiple datasets for batch prediction
df_list = [df1, df2, df3] # List of DataFrames
x_timestamp_list = [x_ts1, x_ts2, x_ts3] # List of historical timestamps
y_timestamp_list = [y_ts1, y_ts2, y_ts3] # List of future timestamps
# Generate batch predictions
pred_df_list = predictor.predict_batch(
df_list=df_list,
x_timestamp_list=x_timestamp_list,
y_timestamp_list=y_timestamp_list,
pred_len=pred_len,
T=1.0,
top_p=0.9,
sample_count=1,
verbose=True
)
# pred_df_list contains prediction results in the same order as input
for i, pred_df in enumerate(pred_df_list):
print(f"Predictions for series {i}:")
print(pred_df.head())
```

**Important Requirements for Batch Prediction:**

- All series must have the same historical length (lookback window)
- All series must have the same prediction length (
`pred_len`

) - Each DataFrame must contain the required columns:
`['open', 'high', 'low', 'close']`

`volume`

and`amount`

columns are optional and will be filled with zeros if missing

The `predict_batch`

method leverages GPU parallelism for efficient processing and automatically handles normalization and denormalization for each series independently.

For a complete, runnable script that includes data loading, prediction, and plotting, please see `examples/prediction_example.py`

.

Running this script will generate a plot comparing the ground truth data against the model's forecast, similar to the one shown below:

Additionally, we provide a script that makes predictions without Volume and Amount data, which can be found in `examples/prediction_wo_vol_example.py`

.

We provide a complete pipeline for finetuning Kronos on your own datasets. As an example, we demonstrate how to use Qlib to prepare data from the Chinese A-share market and conduct a simple backtest.


Disclaimer:This pipeline is intended as a demonstration to illustrate the finetuning process. It is a simplified example and not a production-ready quantitative trading system. A robust quantitative strategy requires more sophisticated techniques, such as portfolio optimization and risk factor neutralization, to achieve stable alpha.

The finetuning process is divided into four main steps:

**Configuration**: Set up paths and hyperparameters.**Data Preparation**: Process and split your data using Qlib.**Model Finetuning**: Finetune the Tokenizer and the Predictor models.**Backtesting**: Evaluate the finetuned model's performance.

- First, ensure you have all dependencies from
`requirements.txt`

installed. - This pipeline relies on
`qlib`

. Please install it:pip install pyqlib

- You will need to prepare your Qlib data. Follow the official Qlib guide to download and set up your data locally. The example scripts assume you are using daily frequency data.

All settings for data, training, and model paths are centralized in `finetune/config.py`

. Before running any scripts, please **modify the following paths** according to your environment:

`qlib_data_path`

: Path to your local Qlib data directory.`dataset_path`

: Directory where the processed train/validation/test pickle files will be saved.`save_path`

: Base directory for saving model checkpoints.`backtest_result_path`

: Directory for saving backtesting results.`pretrained_tokenizer_path`

and`pretrained_predictor_path`

: Paths to the pre-trained models you want to start from (can be local paths or Hugging Face model names).

You can also adjust other parameters like `instrument`

, `train_time_range`

, `epochs`

, and `batch_size`

to fit your specific task. If you don't use Comet.ml, set `use_comet = False`

.

Run the data preprocessing script. This script will load raw market data from your Qlib directory, process it, split it into training, validation, and test sets, and save them as pickle files.

`python finetune/qlib_data_preprocess.py`

After running, you will find `train_data.pkl`

, `val_data.pkl`

, and `test_data.pkl`

in the directory specified by `dataset_path`

in your config.

The finetuning process consists of two stages: finetuning the tokenizer and then the predictor. Both training scripts are designed for multi-GPU training using `torchrun`

.

This step adjusts the tokenizer to the data distribution of your specific domain.

```
# Replace NUM_GPUS with the number of GPUs you want to use (e.g., 2)
torchrun --standalone --nproc_per_node=NUM_GPUS finetune/train_tokenizer.py
```

The best tokenizer checkpoint will be saved to the path configured in `config.py`

(derived from `save_path`

and `tokenizer_save_folder_name`

).

This step finetunes the main Kronos model for the forecasting task.

```
# Replace NUM_GPUS with the number of GPUs you want to use (e.g., 2)
torchrun --standalone --nproc_per_node=NUM_GPUS finetune/train_predictor.py
```

The best predictor checkpoint will be saved to the path configured in `config.py`

.

Finally, run the backtesting script to evaluate your finetuned model. This script loads the models, performs inference on the test set, generates prediction signals (e.g., forecasted price change), and runs a simple top-K strategy backtest.

```
# Specify the GPU for inference
python finetune/qlib_test.py --device cuda:0
```

The script will output a detailed performance analysis in your console and generate a plot showing the cumulative return curves of your strategy against the benchmark, similar to the one below:

**Raw Signals vs. Pure Alpha**: The signals generated by the model in this demo are raw predictions. In a real-world quantitative workflow, these signals would typically be fed into a portfolio optimization model. This model would apply constraints to neutralize exposure to common risk factors (e.g., market beta, style factors like size and value), thereby isolating the**"pure alpha"**and improving the strategy's robustness.**Data Handling**: The provided`QlibDataset`

is an example. For different data sources or formats, you will need to adapt the data loading and preprocessing logic.**Strategy and Backtesting Complexity**: The simple top-K strategy used here is a basic starting point. Production-level strategies often incorporate more complex logic for portfolio construction, dynamic position sizing, and risk management (e.g., stop-loss/take-profit rules). Furthermore, a high-fidelity backtest should meticulously model transaction costs, slippage, and market impact to provide a more accurate estimate of real-world performance.


📝 AI-Generated Comments: Please note that many of the code comments within the`finetune/`

directory were generated by an AI assistant (Gemini 2.5 Pro) for explanatory purposes. While they aim to be helpful, they may contain inaccuracies. We recommend treating the code itself as the definitive source of logic.

If you use Kronos in your research, we would appreciate a citation to our paper:

```
@misc{shi2025kronos,
title={Kronos: A Foundation Model for the Language of Financial Markets},
author={Yu Shi and Zongliang Fu and Shuo Chen and Bohan Zhao and Wei Xu and Changshui Zhang and Jian Li},
year={2025},
eprint={2508.02739},
archivePrefix={arXiv},
primaryClass={q-fin.ST},
url={https://arxiv.org/abs/2508.02739},
}
```


This project is licensed under the MIT License.