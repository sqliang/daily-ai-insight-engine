---
title: workweave/router
source: https://github.com/workweave/router
author: []
published: ''
created: '2026-08-30'
manifest_dates:
- '2026-08-30'
description: 'Model router for agentic systems. Routes every prompt to the right model
  in https://weaveos.com/products/router One endpoint. Every model. Always the right
  one. A drop-in proxy for Anthropic, OpenAI, and Gemini that picks the best model
  for every request: using a tiny on-box embedder, not a vibes-based prompt. Built
  by Weave: The #1 engineering intelligence platform, loved by Robinhood, PostHog,
  Reducto, and hundreds of others. What it does Point Claude Code, Codex, Cursor,
  or your own app at localhost:8080. The router: 🎯 Routes per action. A cluster scorer
  derived from Avengers-Pro [1] picks the right model from your enabled providers,
  for every upstream API request. (See docs/SEMANTICS.md for the canonical terminology:
  the router routes per action, not per turn.) 🔌 Speaks everyone''s API. Anthropic
  Messages, OpenAI Chat Completions, Gemini native. Streaming, tools, vision, the
  works. 🧠 Knows OSS too. DeepSeek, Kimi, GLM, Qwen, Llama, Mistral via OpenRouter
  (or any OpenAI-compatible endpoint). 🔒 BYOK by default. Provider keys stay on your
  box, encrypted at rest. 📊 Observable. OTLP traces out of the box. See them in the
  Weave dashboard (http://localhost:8080/ui/dashboard) or drop in Honeycomb, Datadog,
  Grafana, whatever. 30-second quickstart The fastest way: point Claude Code, Codex,
  opencode, or pi at the hosted Weave Router with one command. No clone, no Docker,
  no Postgres. npx @workweave/router That''s it. The installer asks which tool (Claude
  Code, Codex, opencode, or pi), walks you through scope (user vs. project), grabs
  a router key, and wires the right config file. Other flavors: npx @workweave/router
  --claude # skip the picker, Claude Code npx @workweave/router --codex # skip the
  picker, OpenAI Codex CLI npx @workweave/router --opencode # skip the picker, opencode
  npx @workweave/router --pi # skip the picker, pi + Loom UI npx @workweave/router
  --scope project # per-repo, commits settings.json (or .codex/ / opencode.json) npx
  @workweave/router --local # self-hosted localhost:8080 npx @workweave/router --base-url
  https://router.acme.internal npx @workweave/router@0.1.0 # pin a version Requires
  Node ≥ 18 (Claude Code, opencode, and pi paths also need jq). Full flag reference:
  install/npm/README.md. Or: self-host the whole stack If you want the router (and
  dashboard) running on your own box: # 1. Drop a provider key in. OpenRouter is the
  recommended baseline. echo "OPENROUTER_API_KEY=sk-or-v1-..." >> .env.local # 2.
  Boot Postgres + router on :8080 and seed an rk_ key. make full-setup The router
  is up at http://localhost:8080, the dashboard at http://localhost:8080/ui/ (password:
  admin), and your rk_... key prints in the logs. # Call it like Anthropic curl -sS
  http://localhost:8080/v1/messages \ -H "Authorization: Bearer rk_..." \ -d ''{"model":"claude-sonnet-4-5","max_tokens":256,
  "messages":[{"role":"user","content":"hi"}]}'' # ...or like OpenAI curl -sS http://localhost:8080/v1/chat/completions
  \ -H "Authorization: Bearer rk_..." \ -d ''{"model":"gpt-4o-mini", "messages":[{"role":"user","content":"hi"}]}''
  # Peek at the routing decision without proxying curl -sS http://localhost:8080/v1/route
  -H "Authorization: Bearer rk_..." -d ''...'' What that stack looks like Only the
  grey boxes are off your machine. The router, the scorer, Postgres, and your provider
  keys all stay local; prompts go from the router straight to the provider you configured,
  never to Weave. flowchart LR client["Claude Code, Codex, opencode,<br/>pi, Cursor,
  your own app"] router["Router :8080<br/>/v1/messages · /v1/chat/completions<br/>/v1beta/models
  · /v1/route"] scorer["Cluster scorer<br/>in-process ONNX embedder"] hmm["HMM policy
  sidecar :8093<br/>optional, make up-hmm"] pg[("Postgres<br/>installations, rk_ keys,<br/>encrypted
  BYOK keys, usage")] ui["Dashboard /ui<br/>selfhosted mode only"] providers["Anthropic
  · OpenAI · Gemini<br/>OpenRouter and any<br/>OpenAI-compatible endpoint"] otel["Your
  OTLP collector<br/>Honeycomb, Datadog, Grafana"] client -->|"rk_… bearer token,<br/>streamed
  response back"| router router -->|"embed and score the action"| scorer router -.->|"ROUTER_DEFAULT_STRATEGY=hmm"|
  hmm router -->|"auth, config, usage"| pg pg --> ui router -->|"provider key from
  env or BYOK"| providers router -.->|"spans and usage logs"| otel classDef external
  fill:#f4f4f5,stroke:#a1a1aa,color:#3f3f46 class providers,otel external Multi-replica
  deployments also need Pub/Sub (PUBSUB_*) for cache invalidation; docker compose
  runs the emulator for you. Optional: self-host the frozen HMM policy The default
  stack uses the in-process cluster scorer. To run the frozen HMM policy as a companion
  container, add a Google API key and use the opt-in target: echo ''GOOGLE_API_KEY=...''
  >> .env.local make up-hmm This does not change the default strategy. See sidecars/hmm/README.md
  for artifact verification, embedding compatibility, and explicit HMM selection.
  Wire it into your tools Claude Code. Run make install-cc to wire Claude Code at
  the local self-hosted router (it''s also invoked automatically at the end of make
  full-setup). For the hosted router, use npx @workweave/router above. Codex (OpenAI
  CLI). npx @workweave/router --codex patches ~/.codex/config.toml (or <repo>/.codex/config.toml
  with --scope project) with a managed [model_providers.weave] block and sets model_provider
  = "weave". The provider preserves Codex''s existing ChatGPT OAuth login while the
  router key rides in an X-Weave-Router-Key HTTP header and the installer selects
  the HMM strategy for the public hosted endpoint. --codex --local and custom self-hosted
  URLs keep their router''s configured default because the HMM sidecar is optional.
  HMM and forced selections in the native Codex family (gpt-5.6-sol, gpt-5.6-terra,
  gpt-5.6-luna) use that OAuth credential; every other selected model uses its WorkWeave
  deployment or BYOK credential, matching the Claude Code plugin''s model-to-credential
  dispatch. Codex does not load third-party slash-command files, so the installer
  ships the router directives as native Codex skills: $force-model <model-id> (alias
  $fm <model-id>), $unforce-model (alias $ufm), and $router-feedback <text> (alias
  $rf <text>). Each skill runs a local scripts/emit.sh that prints the leading-space
  directive (for example, /force-model gpt-5.6-terra); the router intercepts that
  exec output. You can type that form directly instead. Re-install and --uninstall
  --codex rewrite/remove only the managed block, leaving the rest of your Codex config
  untouched. Codex also gets $router-status, $router-off, $router-on, and $router-models
  as skills that call this installer''s own verbs. Invoke $disable-routing (or $router-off)
  to switch the next Codex session back to its normal provider, or run npx @workweave/router
  disable-routing in a shell; a literal /disable-routing is not a third-party extension
  point in Codex. opencode. npx @workweave/router --opencode merges a provider.weave
  entry into ~/.config/opencode/opencode.json (or <repo>/opencode.json with --scope
  project). It uses opencode''s bundled @ai-sdk/anthropic provider pointed at the
  router''s /v1 endpoint — the router speaks the Anthropic Messages API natively,
  so opencode works unmodified. The router key and identity headers ride alongside
  the provider config; re-install rewrites only the managed block and --uninstall
  --opencode strips it. pi. npx @workweave/router --pi keeps stock pi as the runtime
  and installs the router''s pi extension. It adds the Loom header, Wooly''s animated
  terminal mascot, a persistent WEAVE ROUTER route/savings line, /fm + /ufm model-pin
  commands with a [forced] status, and context-isolated subagents without shipping
  or maintaining a forked pi binary. Cursor (early beta, performance may not be the
  best). Settings → Models → Override OpenAI Base URL → http://localhost:8080/v1,
  paste rk_... as the API key. Switching on/off. After installing, npx @workweave/router
  off --claude (or --codex / --opencode) routes that client straight to its provider
  again without discarding the router config; on flips it back, and status reports
  which way it''s pointing. Claude Code also gets /router-off, /router-on, and /router-status
  slash commands. Cursor toggles via the same Settings → Models override above. See
  install/README.md. Choosing which models the router may pick. npx @workweave/router
  models --claude lists every deployed model with its on/off state, and models enable
  / models disable change it — the same setting as the dashboard''s settings page,
  edited from the terminal. Claude Code gets this as /router-models (alias /models).
  Requires a router that serves the model-selection API; against the Weave-hosted
  router the list still prints and points you at the dashboard, where selection is
  an organization-wide setting. See install/README.md. Two keys, don''t mix them up:
  sk-or-... / sk-ant-... / sk-... = your upstream provider key. Lives in .env.local.
  rk_... = your router key. Clients send this as a Bearer token. Endpoints Endpoint
  Format POST /v1/messages Anthropic Messages, routed POST /v1/chat/completions OpenAI
  Chat Completions, routed POST /v1beta/models/:action Gemini generateContent, routed
  POST /v1/route Returns the decision, no upstream call GET /v1/models &nbsp;·&nbsp;
  POST /v1/messages/count_tokens Anthropic passthrough GET /health &nbsp;·&nbsp; GET
  /readyz &nbsp;·&nbsp; GET /validate liveness + dependency readiness + key check
  GET /v1/analytics/routing-decisions Raw routing decisions as cursor-paginated NDJSON
  (docs) GET /v1/analytics/schema &nbsp;·&nbsp; GET /v1/analytics/models Export field
  dictionary + price book Keep liveness probes on /health. Point startup or readiness
  probes at /readyz when configured policy sidecars must be ready before traffic arrives.
  Deeper docs 📐 Configuration reference: every env var, BYOK encryption, OTel knobs,
  cluster routing. 🧭 Semantics and terminology: canonical definitions for session,
  round, turn, action, and step. 📊 Analytics export: pulling raw routing decisions
  into your own warehouse with a read-only key. Policy router harness: contract and
  rollout checklist for adding an out-of-process policy model. 🛠️ Contributing: layering
  rules, hot-reload dev, migrations, tests, the whole engineering loop. 🏗️ Architecture:
  package layout, import contracts, recipes for adding endpoints / providers / strategies.
  Zhang, Y. et al. Beyond GPT-5: Making LLMs Cheaper and Better via Performance–Efficiency
  Optimized Routing (Avengers-Pro). arXiv:2508.12631, 2025. https://arxiv.org/abs/2508.12631
  ↩︎'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 0af70dc4df451a55
source_type: community_discussion
tldr: WorkWeave 发布开源路由代理 workweave/router，统一 Anthropic、OpenAI 与 Gemini 的 API 协议，基于
  Avengers-Pro 聚类打分器按动作选择最佳模型，支持 Claude Code、Codex、Cursor 等工具接入，密钥默认以 BYOK 方式留存本机。
objective_summary: WorkWeave（Weave）发布了开源项目 workweave/router，一个可本地部署的模型路由代理，同时兼容 Anthropic
  Messages、OpenAI Chat Completions 与 Gemini 原生 API。它通过进程内 ONNX 嵌入器运行派生自论文 Avengers-Pro
  的聚类打分器，按动作而非轮次为每个上游请求选择模型。产品提供托管版与自托管版两种接入方式，自托管模式在 localhost:8080 启动路由器与仪表盘，供应商密钥以
  BYOK 方式加密保存在用户本机，并可选接入 HMM 策略边车。
event_type: framework_tools
epistemic_status: pr_statement
entities:
  companies:
  - Weave
  - Anthropic
  - OpenAI
  - Google
  - OpenRouter
  - Robinhood
  - PostHog
  - Reducto
  technologies:
  - ONNX
  - OTLP
  - HMM
  - BYOK
  - Anthropic Messages API
  - OpenAI Chat Completions
  - Gemini API
  key_people:
  - Zhang, Y.
key_logic_flow:
- workweave/router 是 Weave 发布的可本地部署模型路由代理，为 Anthropic、OpenAI 与 Gemini 提供统一 API 入口，并宣称能为每个请求选择最优模型。
- 路由决策基于论文 Avengers-Pro（arXiv:2508.12631）派生的聚类打分器，在进程内通过 ONNX 嵌入器运行，按动作而非对话轮次进行路由。
- 产品提供托管版与自托管版两种接入方式，托管版通过 npx @workweave/router 一条命令配置 Claude Code、Codex、opencode
  与 pi 等工具。
- 自托管模式在 localhost:8080 启动路由器与仪表盘，依赖 Postgres 存储安装信息、路由器密钥与加密的 BYOK 供应商密钥。
- 路由器可选启动 HMM 策略边车（:8093）以运行冻结策略，该功能需要 Google API 密钥并通过 make up-hmm 开启。
- 路由器提供 /v1/route 决策预览接口与 OTLP 追踪导出，路由决策可接入 Honeycomb、Datadog、Grafana 等观测系统。
object_mentions:
- object_type: project
  name: workweave/router
  canonical_name: workweave/router
  url: https://github.com/workweave/router
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - workweave/router 是一个面向 Anthropic、OpenAI 与 Gemini 的可本地部署 API 路由代理，宣称通过小型本机嵌入器为每个请求挑选最优模型。
  - 路由器同时兼容 Anthropic Messages、OpenAI Chat Completions 与 Gemini 原生 API，可被 Claude Code、Codex、Cursor
    或自有应用通过 localhost:8080 接入。
  article_id: 0af70dc4df451a55
- object_type: project
  name: '@workweave/router'
  canonical_name: '@workweave/router'
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 通过 npx @workweave/router 单条命令即可将 Claude Code、Codex、opencode 或 pi 指向托管版 Weave Router，安装器会自动写入对应的配置文件。
  - 该安装器支持 --claude、--codex、--opencode、--pi、--scope project、--local 等参数，并可在重装时仅改写受管配置块。
  article_id: 0af70dc4df451a55
- object_type: product
  name: Weave Router
  canonical_name: Weave Router
  url: null
  confidence: medium
  article_role: primary_subject
  evidence_snippets:
  - 托管版 Weave Router 提供组织级的模型选择设置，用户可在仪表盘中查看路由决策与各模型的启用状态。
  - 自托管版在 localhost:8080 提供路由服务与仪表盘页面，并以 rk_ 开头的路由器密钥作为客户端的 Bearer 凭证。
  article_id: 0af70dc4df451a55
- object_type: paper
  name: Avengers-Pro
  canonical_name: Avengers-Pro
  url: https://arxiv.org/abs/2508.12631
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 路由器采用的聚类打分器派生于论文 Avengers-Pro（arXiv:2508.12631），该研究题为《Beyond GPT-5》，旨在通过性能-效率优化路由降低调用成本并提升效果。
  article_id: 0af70dc4df451a55
- object_type: product
  name: Weave
  canonical_name: Weave
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 该路由器由 Weave 工程智能平台团队构建，README 宣称该平台被 Robinhood、PostHog、Reducto 等数百家公司采用。
  article_id: 0af70dc4df451a55
extract_result: success
impact_score:
  score: 6.0
  reason: 评分依据：模型路由代理赛道已有 OpenRouter、LiteLLM、Portkey 等成熟玩家，并非全新品类，因此达不到范式转移级别；但该项目在工程上提供了三点真实差异化——按'动作'而非对话轮次路由（派生自
    Avengers-Pro 论文的聚类打分器）、进程内 ONNX 嵌入器实现本机评分（低延迟 + 隐私友好）、BYOK 默认将供应商密钥留存在用户本机（不介入
    API 计费中介）——这正好切中当下多模型碎片化与数据合规的双重痛点，且对 Claude Code/Codex/Cursor 的零配置接入能快速撬动核心开发者人群。短期会加剧
    AI 编码工具链上路由层的局部竞争格局，属于重要产品发布但非行业范式转移，综合评分为 6.0。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 按动作粒度智能路由的实际效果与本机部署（BYOK）带来的隐私和成本优势
hype_assessment:
  level: medium
  reason: 识别到标题宣称 'Always the right one'、'picks the best model for every request'
    等典型 PR 话术，路由质量实际依赖 Avengers-Pro 派生评分器的真实效果，尚无大规模生产验证数据支撑'每次都是最优'的绝对化表述；但项目提供了完整源码、架构文档、部署方案与诚实的边界说明（HMM
    策略为可选边车、密钥确留本机、仅灰色区域出网），技术干货含量较高，属一定包装而非严重概念炒作，故判定为 medium。
information_entropy: high
domain_disruption:
  technical_innovation: 以 Avengers-Pro 论文派生的聚类打分器在进程内通过 ONNX 嵌入器运行，实现按'动作'（语义任务类型）而非对话轮次的模型路由：相比传统静态模型配置或按轮次启发式路由，粒度更细、可解释性更强，且本机推理避免了把
    prompt 发往第三方路由服务，兼顾低延迟与数据隐私。
  business_model: BYOK 模式让供应商密钥留存在用户本机、prompt 直达供应商，路由器不介入 API 计费中介，与 OpenRouter 的中介计费模式形成差异化；其商业化路径更接近基础设施订阅
    + 可观测性增值（OTLP 追踪、仪表盘、HMM 策略边车），同时可作为 Weave 工程智能平台的获客引流入口，有机会沉淀为 AI 编码工具链上本地路由层的标准组件。
engineering_complexity: production_ready
compound_value:
  score: 6.2
  reason: 投资逻辑推演：一、需求侧——前沿模型数量持续膨胀（Anthropic/OpenAI/Gemini/DeepSeek/Qwen/Mistral 多线并行），'按动作而非按轮次选最优模型'的智能路由正从可选项变为
    Agent 工具链的刚需，路由层具备跨协议、跨模型的长期复利空间。二、供给侧差异化——workweave/router 基于 Avengers-Pro 派生的聚类打分器做按动作路由，配合进程内
    ONNX 嵌入器与可选 HMM 策略边车，技术深度明显优于纯透传网关，且 BYOK+本地部署（自托管 localhost:8080）降低了企业数据合规门槛，这是其区别于
    OpenRouter/LiteLLM 的关键卡位。三、风险点——该赛道已拥挤（OpenRouter、LiteLLM、Portkey 等），核心算法随开源发布可被复制；路由层本质是'过路税'，若大模型厂商或客户端工具内嵌同类路由能力，中间层价值可能被挤压；当前仅
    PR/开源发布阶段，真实路由质量（成本节省与质量增益）缺乏第三方验证数据。四、结论——有潜力成为细分基础设施，但需持续验证真实采纳率与路由决策数据的沉淀壁垒，故给予
    6.2 分（4-7 区间偏上）。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Weave
- DeepSeek
- Qwen
- Mistral
competitive_casualty:
- LiteLLM
- Portkey
- OpenRouter
- 传统闭源 LLM 网关/代理厂商
market_opportunities:
- 创业者可借鉴 workweave/router 的'按动作而非轮次'路由范式，开发面向多模型 Agent 的智能路由网关，按任务类型动态选择成本与质量最优模型，切入
  LLMOps 中间件市场
- 建议关注'模型路由 × AI 可观测性'交叉赛道——结合 OTLP 追踪与路由决策审计能力，打造企业级模型成本归因、质量追踪与治理一体化平台
- 面向金融、医疗等强合规行业，可基于 BYOK 本地部署模式（密钥自持、数据不出域）打造私有化模型网关与路由方案，满足数据出境与供应商密钥管控诉求
risk_matrix:
  regulatory: 本地部署与 BYOK 模式降低了部分数据合规风险，但作为统一代理转发多家供应商 API 可能触及各家服务条款；跨区域路由（如境内接入境外模型）可能面临出口管制与数据跨境传输合规约束
  technological: 路由依赖的 Avengers-Pro 聚类打分器与本地 ONNX 嵌入器精度有限，可能被大模型厂商原生路由能力或更先进的在线路由算法取代；基于历史聚类的策略存在选型劣化风险
  competitive: 赛道已拥挤：OpenRouter、LiteLLM、Portkey、Vercel AI Gateway 等已形成生态，Weave 作为新进入者面临价格战与生态挤压，且
    Anthropic/OpenAI/Google 原生模型选择能力可能边缘化第三方路由层
  ethical: 自动路由使模型选择成为黑盒，不同模型对同一请求的偏见与输出差异难以追溯解释；本地加密存储的 BYOK 密钥若实现不当存在泄露风险，路由决策可解释性不足
  additional:
  - 供应链风险：npx 安装器会改写 Claude Code、Codex 等工具的本地配置文件，npm 包供应链若被投毒将波及大量开发者
  - 架构引入新故障点：路由器作为中间层一旦宕机将导致所有接入工具不可用，依赖 Postgres 的部署也增加运维复杂度
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: workweave/router
  canonical_name: workweave/router
  url: https://github.com/workweave/router
  positioning: 可本地部署的模型路由代理，统一 Anthropic、OpenAI 与 Gemini 的 API 协议，通过本机嵌入器按动作挑选最优模型，支持主流
    AI 编程工具接入。
  technical_signal: 基于 Avengers-Pro 论文派生的聚类打分器，在进程内通过 ONNX 嵌入器运行，按动作而非对话轮次进行路由，并可选启动
    HMM 策略边车。
  adoption_signal: 可被 Claude Code、Codex、Cursor 或自有应用通过 localhost:8080 接入，同时兼容 Anthropic、OpenAI、Gemini
    及 OpenRouter 等模型端点。
  ecosystem_relevance: 与 Claude Code、Codex、opencode、pi、Cursor 等主流 AI 编码工具深度集成，并通过
    OpenRouter 兼容 DeepSeek、Qwen、Llama 等开源模型。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 该路由代理将论文 Avengers-Pro 的聚类打分器落地为生产级路由方案，宣称按动作粒度挑选最优模型，且默认 BYOK 让密钥留存本机，代表模型路由层正从中心化服务向本地化、可观测化演进，值得持续跟踪其路由准确性与生态接入进展。
  risk_notes:
  - 路由决策质量依赖聚类打分器与嵌入器，实际准确性尚缺第三方基准验证，存在选型劣化风险。
  - 项目处于早期阶段，自托管依赖 Postgres 与 Docker 编排，多副本场景还需 Pub/Sub，部署运维复杂度较高。
  score: 7.0
  article_ids:
  - 0af70dc4df451a55
  evidence_snippets:
  - workweave/router 是一个面向 Anthropic、OpenAI 与 Gemini 的可本地部署 API 路由代理，宣称通过小型本机嵌入器为每个请求挑选最优模型。
  - 路由器同时兼容 Anthropic Messages、OpenAI Chat Completions 与 Gemini 原生 API，可被 Claude Code、Codex、Cursor
    或自有应用通过 localhost:8080 接入。
- object_type: project
  name: '@workweave/router'
  canonical_name: '@workweave/router'
  url: null
  positioning: 作为 Weave Router 的官方安装器，通过 npx 单条命令即可将 Claude Code、Codex、opencode 或
    pi 指向托管版路由，并自动改写对应工具配置。
  technical_signal: 安装器支持 --claude、--codex、--opencode、--pi、--scope project、--local
    等参数，重装时仅改写受管配置块以保留非受管设置。
  adoption_signal: 托管版接入无需克隆仓库、Docker 或 Postgres，仅需 Node ≥18 环境即可完成配置，显著降低模型路由的采用门槛。
  ecosystem_relevance: 与 Claude Code、Codex、opencode、pi 等工具深度绑定，可一键写入 settings.json、.codex/config.toml
    等受管配置块。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 安装器以零部署方式将路由代理接入主流 AI 编码工具，其 --scope project 与受管配置块设计体现对开发者工作流侵入的最小化，值得跟踪其安装量增长与对路由产品采用率的带动。
  risk_notes:
  - npm 安装器自动改写本地工具配置存在供应链与误配置风险，需关注版本固定与受管配置块的边界控制。
  - 依赖 jq 等辅助工具及 Node ≥18 环境，部分旧环境或安全策略下安装可能受阻。
  score: 6.0
  article_ids:
  - 0af70dc4df451a55
  evidence_snippets:
  - 通过 npx @workweave/router 单条命令即可将 Claude Code、Codex、opencode 或 pi 指向托管版 Weave Router，安装器会自动写入对应的配置文件。
  - 该安装器支持 --claude、--codex、--opencode、--pi、--scope project、--local 等参数，并可在重装时仅改写受管配置块。
- object_type: product
  name: Weave Router
  canonical_name: Weave Router
  url: null
  positioning: Weave 推出的模型路由服务，提供托管版与自托管版两种形态，统一多供应商 API 入口并宣称按请求挑选最优模型，附带仪表盘与 OTLP
    观测能力。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 使用 Claude Code、Codex 等 AI 编码工具的开发者
  - 需要统一多供应商模型 API 入口并优化成本与质量的工程团队
  product_signal: 提供组织级模型选择设置、/v1/route 决策预览接口与 OTLP 追踪导出，可在仪表盘中查看路由决策与各模型的启用状态。
  market_signal: 由工程智能平台 Weave 推出，宣称被 Robinhood、PostHog 等公司采用，反映多模型路由与成本优化需求正在升温。
  differentiation: 区别于基于提示词软路由的方案，采用论文派生的聚类打分器按动作粒度决策，并以默认 BYOK 强调供应商密钥不出本机。
  watch_reason: Weave Router 同时提供托管与自托管双形态，以 BYOK、OTLP 可观测性和按动作路由为核心卖点，切入多模型成本与质量优化痛点，值得跟踪其组织级采用与路由决策的实际效果。
  risk_notes:
  - 路由决策的准确性依赖 Avengers-Pro 打分器落地质量，缺少第三方独立评估，效果宣称需谨慎对待。
  - 托管版与自托管版并存可能造成版本与行为差异，密钥与 HMM 边车等配置链路较长，落地复杂度较高。
  score: 7.0
  article_ids:
  - 0af70dc4df451a55
  evidence_snippets:
  - 托管版 Weave Router 提供组织级的模型选择设置，用户可在仪表盘中查看路由决策与各模型的启用状态。
  - 自托管版在 localhost:8080 提供路由服务与仪表盘页面，并以 rk_ 开头的路由器密钥作为客户端的 Bearer 凭证。
---

**One endpoint. Every model. Always the right one.**

A drop-in proxy for Anthropic, OpenAI, and Gemini that picks the best model
for *every* request: using a tiny on-box embedder, not a vibes-based prompt.

*Built by Weave: The #1 engineering intelligence platform,
loved by Robinhood, PostHog, Reducto, and hundreds of others.*

Point Claude Code, Codex, Cursor, or your own app at `localhost:8080`

. The router:

- 🎯
**Routes per action.**A cluster scorer derived from Avengers-Pro1picks the right model from your enabled providers, for every upstream API request. (See docs/SEMANTICS.md for the canonical terminology: the router routes per**action**, not per**turn**.) - 🔌
**Speaks everyone's API.**Anthropic Messages, OpenAI Chat Completions, Gemini native. Streaming, tools, vision, the works. - 🧠
**Knows OSS too.**DeepSeek, Kimi, GLM, Qwen, Llama, Mistral via OpenRouter (or any OpenAI-compatible endpoint). - 🔒
**BYOK by default.**Provider keys stay on your box, encrypted at rest. - 📊
**Observable.**OTLP traces out of the box. See them in the Weave dashboard (http://localhost:8080/ui/dashboard) or drop in Honeycomb, Datadog, Grafana, whatever.

The fastest way: point Claude Code, Codex, opencode, or pi at the **hosted**
Weave Router with one command. No clone, no Docker, no Postgres.

`npx @workweave/router`

That's it. The installer asks which tool (Claude Code, Codex, opencode, or pi), walks you through scope (user vs. project), grabs a router key, and wires the right config file. Other flavors:

```
npx @workweave/router --claude # skip the picker, Claude Code
npx @workweave/router --codex # skip the picker, OpenAI Codex CLI
npx @workweave/router --opencode # skip the picker, opencode
npx @workweave/router --pi # skip the picker, pi + Loom UI
npx @workweave/router --scope project # per-repo, commits settings.json (or .codex/ / opencode.json)
npx @workweave/router --local # self-hosted localhost:8080
npx @workweave/router --base-url https://router.acme.internal
npx @workweave/router@0.1.0 # pin a version
```

Requires Node ≥ 18 (Claude Code, opencode, and pi paths also need `jq`

). Full
flag reference: install/npm/README.md.

If you want the router (and dashboard) running on your own box:

```
# 1. Drop a provider key in. OpenRouter is the recommended baseline.
echo "OPENROUTER_API_KEY=sk-or-v1-..." >> .env.local
# 2. Boot Postgres + router on :8080 and seed an rk_ key.
make full-setup
```

The router is up at http://localhost:8080, the dashboard at
http://localhost:8080/ui/ (password: `admin`

), and your `rk_...`

key
prints in the logs.

```
# Call it like Anthropic
curl -sS http://localhost:8080/v1/messages \
-H "Authorization: Bearer rk_..." \
-d '{"model":"claude-sonnet-4-5","max_tokens":256,
"messages":[{"role":"user","content":"hi"}]}'
# ...or like OpenAI
curl -sS http://localhost:8080/v1/chat/completions \
-H "Authorization: Bearer rk_..." \
-d '{"model":"gpt-4o-mini",
"messages":[{"role":"user","content":"hi"}]}'
# Peek at the routing decision without proxying
curl -sS http://localhost:8080/v1/route -H "Authorization: Bearer rk_..." -d '...'
```

Only the grey boxes are off your machine. The router, the scorer, Postgres, and your provider keys all stay local; prompts go from the router straight to the provider you configured, never to Weave.

```
flowchart LR
client["Claude Code, Codex, opencode,<br/>pi, Cursor, your own app"]
router["Router :8080<br/>/v1/messages · /v1/chat/completions<br/>/v1beta/models · /v1/route"]
scorer["Cluster scorer<br/>in-process ONNX embedder"]
hmm["HMM policy sidecar :8093<br/>optional, make up-hmm"]
pg[("Postgres<br/>installations, rk_ keys,<br/>encrypted BYOK keys, usage")]
ui["Dashboard /ui<br/>selfhosted mode only"]
providers["Anthropic · OpenAI · Gemini<br/>OpenRouter and any<br/>OpenAI-compatible endpoint"]
otel["Your OTLP collector<br/>Honeycomb, Datadog, Grafana"]
client -->|"rk_… bearer token,<br/>streamed response back"| router
router -->|"embed and score the action"| scorer
router -.->|"ROUTER_DEFAULT_STRATEGY=hmm"| hmm
router -->|"auth, config, usage"| pg
pg --> ui
router -->|"provider key from env or BYOK"| providers
router -.->|"spans and usage logs"| otel
classDef external fill:#f4f4f5,stroke:#a1a1aa,color:#3f3f46
class providers,otel external
```

Multi-replica deployments also need Pub/Sub (`PUBSUB_*`

) for cache
invalidation; `docker compose`

runs the emulator for you.

The default stack uses the in-process cluster scorer. To run the frozen HMM policy as a companion container, add a Google API key and use the opt-in target:

```
echo 'GOOGLE_API_KEY=...' >> .env.local
make up-hmm
```

This does not change the default strategy. See
`sidecars/hmm/README.md`

for artifact verification,
embedding compatibility, and explicit HMM selection.

**Claude Code.** Run `make install-cc`

to wire Claude Code at the local
self-hosted router (it's also invoked automatically at the end of
`make full-setup`

). For the hosted router, use `npx @workweave/router`

above.

**Codex** (OpenAI CLI). `npx @workweave/router --codex`

patches
`~/.codex/config.toml`

(or `<repo>/.codex/config.toml`

with `--scope project`

)
with a managed `[model_providers.weave]`

block and sets `model_provider = "weave"`

.
The provider preserves Codex's existing ChatGPT OAuth login while the router
key rides in an `X-Weave-Router-Key`

HTTP header and the installer selects the
HMM strategy for the public hosted endpoint. `--codex --local`

and custom
self-hosted URLs keep their router's configured default because the HMM
sidecar is optional. HMM and forced selections in the native Codex family
(`gpt-5.6-sol`

, `gpt-5.6-terra`

, `gpt-5.6-luna`

) use that OAuth credential;
every other selected model uses its WorkWeave deployment or BYOK credential,
matching the Claude Code plugin's model-to-credential dispatch.
Codex does not load third-party slash-command files, so the installer ships the
router directives as native Codex skills: `$force-model <model-id>`

(alias
`$fm <model-id>`

), `$unforce-model`

(alias `$ufm`

), and
`$router-feedback <text>`

(alias `$rf <text>`

). Each skill runs a local
`scripts/emit.sh`

that prints the leading-space directive (for example,
` /force-model gpt-5.6-terra`

); the router intercepts that exec output.
You can type that form directly instead. Re-install
and `--uninstall --codex`

rewrite/remove only the managed block, leaving the
rest of your Codex config untouched. Codex also gets `$router-status`

,
`$router-off`

, `$router-on`

, and `$router-models`

as skills that call this
installer's own verbs. Invoke `$disable-routing`

(or `$router-off`

) to switch the
next Codex session back to its normal provider, or run
`npx @workweave/router disable-routing`

in a shell; a literal
`/disable-routing`

is not a third-party extension point in Codex.

**opencode.** `npx @workweave/router --opencode`

merges a `provider.weave`

entry into `~/.config/opencode/opencode.json`

(or `<repo>/opencode.json`

with `--scope project`

). It uses opencode's bundled `@ai-sdk/anthropic`

provider pointed at the router's `/v1`

endpoint — the router speaks the
Anthropic Messages API natively, so opencode works unmodified. The router
key and identity headers ride alongside the provider config; re-install
rewrites only the managed block and `--uninstall --opencode`

strips it.

**pi.** `npx @workweave/router --pi`

keeps stock pi as the runtime and installs
the router's pi extension. It adds the Loom header, Wooly's animated terminal
mascot, a persistent `WEAVE ROUTER`

route/savings line, `/fm`

+ `/ufm`

model-pin commands with a `[forced]`

status, and context-isolated subagents
without shipping or maintaining a forked pi binary.

**Cursor** *(early beta, performance may not be the best).* Settings →
Models → *Override OpenAI Base URL* → `http://localhost:8080/v1`

, paste
`rk_...`

as the API key.

**Switching on/off.** After installing, `npx @workweave/router off --claude`

(or `--codex`

/ `--opencode`

) routes that client straight to its provider
again without discarding the router config; `on`

flips it back, and `status`

reports which way it's pointing. Claude Code also gets `/router-off`

,
`/router-on`

, and `/router-status`

slash commands. Cursor toggles via the same
Settings → Models override above. See install/README.md.

**Choosing which models the router may pick.** `npx @workweave/router models --claude`

lists every deployed model with its on/off state, and `models enable`

/ `models disable`

change it — the same setting as the dashboard's settings
page, edited from the terminal. Claude Code gets this as `/router-models`

(alias `/models`

). Requires a router that serves the model-selection API;
against the Weave-hosted router the list still prints and points you at the
dashboard, where selection is an organization-wide setting. See
install/README.md.

Two keys, don't mix them up:


`sk-or-...`

/`sk-ant-...`

/`sk-...`

= yourupstreamprovider key. Lives in`.env.local`

.`rk_...`

= yourrouterkey. Clients send this as a Bearer token.

| Endpoint | Format |
|---|---|
`POST /v1/messages` |
Anthropic Messages, routed |
`POST /v1/chat/completions` |
OpenAI Chat Completions, routed |
`POST /v1beta/models/:action` |
Gemini `generateContent` , routed |
`POST /v1/route` |
Returns the decision, no upstream call |
`GET /v1/models` · `POST /v1/messages/count_tokens` |
Anthropic passthrough |
`GET /health` · `GET /readyz` · `GET /validate` |
liveness + dependency readiness + key check |
`GET /v1/analytics/routing-decisions` |
Raw routing decisions as cursor-paginated NDJSON (docs) |
`GET /v1/analytics/schema` · `GET /v1/analytics/models` |
Export field dictionary + price book |

Keep liveness probes on `/health`

. Point startup or readiness probes at
`/readyz`

when configured policy sidecars must be ready before traffic arrives.

- 📐
**Configuration reference**: every env var, BYOK encryption, OTel knobs, cluster routing. - 🧭
**Semantics and terminology**: canonical definitions for session, round, turn, action, and step. - 📊
**Analytics export**: pulling raw routing decisions into your own warehouse with a read-only key. **Policy router harness**: contract and rollout checklist for adding an out-of-process policy model.- 🛠️
**Contributing**: layering rules, hot-reload dev, migrations, tests, the whole engineering loop. - 🏗️
**Architecture**: package layout, import contracts, recipes for adding endpoints / providers / strategies.

## Footnotes

-
Zhang, Y. et al.

*Beyond GPT-5: Making LLMs Cheaper and Better via Performance–Efficiency Optimized Routing*(Avengers-Pro). arXiv:2508.12631, 2025. https://arxiv.org/abs/2508.12631 ↩