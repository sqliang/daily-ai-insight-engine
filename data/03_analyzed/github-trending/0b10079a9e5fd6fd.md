---
title: microsoft/mxc
source: https://github.com/microsoft/mxc
author: []
published: ''
created: '2026-06-07'
description: 'Policy-driven, layered isolation and containmentMicrosoft eXecution
  Container (MXC) MXC is a sandboxed code execution system for running untrusted code
  (model output, plugins, tools) on Windows, Linux, and macOS. It provides multiple
  containment backends — from OS-native process sandboxes to full VMs — behind a unified
  JSON configuration schema and TypeScript SDK. Warning This repository contains an
  early preview of code published to enable early integration and feedback from developers
  on Microsoft Execution Containers. The underlying sandboxes in this early preview
  are expected to change as they are under ongoing development however we will aim
  to minimize compatibility impact as functionality evolves. There are known cases
  where the current policies generated by the MXC SDK in this repository are overly
  permissive and will be addressed before this is made more generally available. Security
  researcher partnership while MXC matures is welcome, however no MXC profiles should
  be treated as security boundaries currently. Features Cross-platform: Windows, Linux,
  and macOS support with platform-appropriate containment backends JSON-based Configuration:
  Define execution parameters and security policies via a versioned JSON schema Multiple
  Containment Backends: ProcessContainer, Windows Sandbox, LXC, Bubblewrap, Seatbelt
  (macOS), MicroVM (NanVix), Hyperlight, IsolationSession, and WSLC Policy-driven
  Sandboxing: Filesystem Policy: Read-only and read-write path lists (denied paths
  not yet supported on Windows) Network Policy: Proxy support (not supported on macOS);
  allow/block outbound and host filtering (not yet supported on Windows) UI Policy:
  Clipboard, display, and GUI access controls State-aware Lifecycle: Multi-step sandbox
  lifecycle (provision → start → exec → stop → deprovision) for session sandboxes
  TypeScript SDK: @microsoft/mxc-sdk npm package with one-shot and state-aware APIs
  Diagnostics: Debug logging and Event Tracing for Windows (ETW) for troubleshooting
  Building MXC ships a native container wrapper plus a TypeScript SDK — see the SDK
  README for full API documentation. Platforms Platform Default backend Other backends
  Minimum build Windows 11 24H2+ (verified on 25H2) processcontainer windows_sandbox,
  wslc, microvm, hyperlight, isolation_session processcontainer: 26100 (24H2)isolation_session:
  26300.8553 (Insider Preview) Linux x64 / ARM64 bubblewrap lxc, microvm, hyperlight
  — macOS ARM64 / x64 (schema 0.6.0-alpha+) seatbelt — — The stable one-shot backends
  (processcontainer, bubblewrap, and lxc) do not require experimental mode; Linux
  hosts also need the matching runtime installed: bwrap (Bubblewrap) for the default
  backend, or the lxc toolset for the lxc backend. Experimental backends (windows_sandbox,
  wslc, microvm, seatbelt, isolation_session, hyperlight) require { experimental:
  true } in SandboxSpawnOptions or the --experimental CLI flag. Requirements Rust
  toolchain — version pinned to 1.93 via src/rust-toolchain.toml (auto-selected by
  rustup) Node.js ≥ 18 npm (for SDK and CLI builds) Project Structure src/ Rust workspace
  (native binaries + shared library crates) sdk/ TypeScript SDK (@microsoft/mxc-sdk
  npm package) schemas/ JSON configuration schemas (stable + dev) docs/ Documentation
  (schema reference, backend guides, design docs) tests/ Test collateral (configs,
  examples, scripts) scripts/ Build and utility scripts Full Build Windows build.bat
  # Release build for current architecture build.bat --debug # Debug build build.bat
  --all # Release build for both x64 and ARM64 build.bat --with-microvm # Include
  NanVix micro-VM binaries Linux ./build.sh # Release build ./build.sh --debug # Debug
  build ./build.sh --rust-only # Only Rust binaries, skip SDK/CLI macOS ./build-mac.sh
  # Release build for native architecture ./build-mac.sh --all # Both Apple Silicon
  and Intel ./build-mac.sh --debug # Debug build ./build-mac.sh --rust-only # Only
  Rust binary, skip SDK All build scripts: Build the platform-appropriate Rust binary
  Copy the binary into sdk/bin/<arch>/ (for example, x64 or arm64) for SDK bundling
  Build the TypeScript SDK Building Components Individually # Rust workspace (from
  src/) cargo build --release --target x86_64-pc-windows-msvc # Windows x64 cargo
  build --release --target aarch64-pc-windows-msvc # Windows ARM64 cargo build --release
  -p lxc # Linux — lxc-exec (serves both LXC and Bubblewrap) cargo build --release
  -p mxc_darwin --target aarch64-apple-darwin # macOS # SDK (from sdk/) npm install
  && npm run build Lint and Format # Windows Rust (from src/) cargo clippy --workspace
  --all-targets -- -D warnings # Linux Rust (from src/; matches build.sh''s platform-compatible
  crate set) cargo clippy -p lxc -p lxc_common -p wxc_common -p bwrap_common -p linux_test_proxy
  --all-targets -- -D warnings # macOS Rust (from src/) cargo clippy -p mxc_darwin
  -p seatbelt_common --all-targets -- -D warnings Tests # Rust unit tests (from src/)
  cargo test --workspace cargo test -p wxc_common # Single crate cargo test -p wxc_common
  -- config_parser # Filter by test name # SDK (from sdk/) npm test # Unit tests npm
  run test:integration # Integration tests # E2E (from src/) cargo test -p wxc_e2e_tests
  Usage MXC uses a JSON configuration to define execution parameters. See the schema
  documentation for full reference. Native Binary # File path wxc-exec.exe config.json
  # Base64-encoded config wxc-exec.exe --config-base64 <base64-encoded-json> # Debug
  output wxc-exec.exe --debug config.json On Linux: ./lxc-exec config.json On macOS:
  ./mxc-exec-mac --experimental config.json TypeScript SDK npm install @microsoft/mxc-sdk
  import { spawnSandboxFromConfig, createConfigFromPolicy, getAvailableToolsPolicy,
  getTemporaryFilesPolicy, getPlatformSupport, } from ''@microsoft/mxc-sdk''; if (!getPlatformSupport().isSupported)
  { throw new Error(''MXC not available on this host''); } const tools = getAvailableToolsPolicy(process.env);
  const temp = getTemporaryFilesPolicy(); const config = createConfigFromPolicy({
  version: ''0.6.0-alpha'', filesystem: { readonlyPaths: tools.readonlyPaths, readwritePaths:
  temp.readwritePaths, }, network: { allowOutbound: false }, timeoutMs: 30_000, });
  config.process!.commandLine = ''python -c "print(\''hello from sandbox\'')"''; const
  child = spawnSandboxFromConfig(config, { usePty: false }); child.stdout!.on(''data'',
  (d) => process.stdout.write(d)); child.on(''close'', (code) => console.log(''exit:'',
  code)); The SDK also provides a state-aware lifecycle API for long-lived sandboxes:
  import { provisionSandbox, startSandbox, execInSandboxAsync, stopSandbox, deprovisionSandbox,
  } from ''@microsoft/mxc-sdk''; See the SDK README for full API documentation. Schema
  Versions Version Status Schema File 0.5.0-alpha Stable schemas/stable/mxc-config.schema.0.5.0-alpha.json
  0.6.0-alpha Stable (current) schemas/stable/mxc-config.schema.0.6.0-alpha.json 0.7.0-dev
  Dev (experimental backends, state-aware lifecycle) schemas/dev/mxc-config.schema.0.7.0-dev.json
  Pick 0.6.0-alpha for new code on any supported platform. See docs/versioning.md
  for the full versioning design. Debugging Debug Console Mode By default, native
  binaries run in silent mode — stdin/stdout/stderr is coupled directly to the container.
  Use --debug for verbose output: wxc-exec.exe --debug config.json See docs/diagnostics.md
  for full diagnostics reference. Documentation Document Description docs/schema.md
  Full JSON configuration schema reference docs/versioning.md Schema versioning and
  experimental feature lifecycle docs/examples.md Annotated configuration examples
  docs/host-prep.md Windows host preparation (wxc-host-prep.exe) docs/diagnostics.md
  Diagnostic logging and ETW docs/sandbox-policy/v1/policy.md Sandbox policy v1 specification
  docs/base-process-container/guide.md Windows AppContainer / BaseContainer guide
  docs/lxc-support/lxc-backend.md LXC backend (Linux) docs/bwrap-support/bubblewrap-backend.md
  Bubblewrap backend (Linux) docs/macos-support/seatbelt-backend.md Seatbelt backend
  (macOS) docs/windows-sandbox/windows-sandbox.md Windows Sandbox backend docs/state-aware-lifecycle/mxc-state-aware-sandbox-api.md
  State-aware sandbox lifecycle API Contributing See CONTRIBUTING.md for contribution
  guidelines. License See LICENSE.md for details.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 0b10079a9e5fd6fd
source_type: community_discussion
tldr: 微软开源 MXC，一个跨平台沙盒代码执行系统，支持多重隔离后端。
objective_summary: 微软在 GitHub 上发布 MXC（Microsoft Execution Containers）早期预览版，一个跨平台沙盒代码执行系统，通过统一
  JSON 配置和 TypeScript SDK 管理进程、虚拟机等多重隔离后端，用于安全运行模型输出、插件等不可信代码。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Microsoft
  technologies:
  - MXC
  - TypeScript SDK
  key_people: []
key_logic_flow:
- MXC 是一个跨平台（Windows、Linux、macOS）的沙盒代码执行系统，专门用于安全运行不可信代码（模型输出、插件、工具等）。
- 该系统提供多种隔离后端，包括 ProcessContainer、Windows Sandbox、LXC、Bubblewrap、Seatbelt、MicroVM（NanVix）、Hyperlight、IsolationSession
  和 WSLC，覆盖操作系统级沙盒到完整虚拟机。
- MXC 通过版本化 JSON Schema 统一定义执行参数和安全策略，支持文件系统策略（只读/读写路径）、网络策略（代理、出站过滤）和 UI 策略（剪贴板、显示、GUI
  访问控制）。
- MXC 提供 TypeScript SDK（@microsoft/mxc-sdk），支持一次性执行和有状态生命周期管理两种 API。
- 该项目当前为早期预览阶段，安全策略存在过度宽松的情况，官方明确表示当前版本不应被视为安全边界。
- 构建依赖 Rust 1.93 工具链和 Node.js ≥ 18，支持 Windows、Linux 和 macOS 原生构建。
impact_score:
  score: 6.5
  reason: 微软开源 MXC 解决的是 AI 行业当前最痛的痛点之一——安全运行 AI Agent 生成/调用的不可信代码。目前业界缺乏标准化方案，各家自行造轮子（如
    sandboxed-python、deno、e2b），MXC 以统一 JSON Schema + TypeScript SDK 覆盖从 OS 级沙盒到完整虚拟机的多重隔离后端，方向正确且具备平台级影响力。但项目明确标注为早期预览版，安全策略尚存过度宽松问题，官方声明
    '不应被视为安全边界'，短期内无法直接用于生产，短期内对竞争格局的实际冲击有限，属于基础设施布局期的信号释放。综合评估 6.5 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 跨平台统一沙盒 API 能否真正成为 AI Agent 代码执行的安全标准
hype_assessment:
  level: low
  reason: 项目 README 非常诚实，没有使用 '颠覆'、'革命性' 等 PR 修辞。明确标注是 'early preview'，主动承认已知存在过度宽松的安全策略，并声明当前版本不应被视为安全边界。这种程度的自我披露在微软开源项目中较为少见，炒作成分极低。
information_entropy: high
domain_disruption:
  technical_innovation: 在统一抽象层下聚合 ProcessContainer、Bubblewrap、Seatbelt、LXC、Windows
    Sandbox、MicroVM、Hyperlight 等十余种隔离后端，通过版本化 JSON Schema 统一定义文件系统策略、网络策略、UI 策略，并提供
    TypeScript SDK 封装有状态生命周期管理。这套抽象架构本身并非全新理论，但将操作系统级沙盒到完整虚拟机纳入同一配置体系并面向前端生态输出 SDK，在工程整合度上是行业首次。
  business_model: 对微软而言，MXC 是 Azure AI Agent 生态的关键基础设施拼图——一旦成熟，可将安全代码执行作为 Azure 的托管服务（Sandbox-as-a-Service）变现，并绑定
    AI SDK 和云资源消费。对 SaaS 生态而言，标准化沙盒接口有望降低 Agent 平台的构建门槛，催生新的 'AI 沙盒即服务' 商业模式。
engineering_complexity: prototype
compound_value:
  score: 7.5
  reason: MXC 定位在 AI Agent 执行基础设施层，解决的是模型生成代码安全执行这一关键瓶颈。其核心价值在于提供了一个统一的多后端隔离抽象层（进程容器
    → 完整 VM），让开发者无需关心底层沙盒实现。这种抽象具有平台效应：后端越多、策略越丰富 → 采用率越高 → 生态越强，形成正向循环。随着 AI Agent
    自主性持续提升（代码生成、工具调用、插件执行），安全沙盒执行需求将指数级增长。微软通过开源 MXC 有望在该层建立事实标准，类似于 Docker 对容器编排的贡献。复利效应显著：每多一个后端支持，就多一个使用场景；每多一个使用场景，就多一批开发者绑入
    MXC 生态。风险在于：1）当前为早期预览，安全边界尚未成熟；2）微软历史上存在开源项目维护不稳定的先例；3）竞品（如 Modal、E2B、Firecracker
    生态）可能形成替代标准。综合判断，这是 3-5 年后大概率仍存在的行业基础组件，但验证期仍需 12-18 个月。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Microsoft
- LangChain
- AutoGPT
- OpenAI
- Anthropic
competitive_casualty:
- Modal
- E2B
- RunCode
- 专有沙盒安全厂商
- 云平台锁定型隔离方案
market_opportunities:
- AI Agent 开发者可基于 MXC 的统一沙盒 API 快速集成安全代码执行能力，避免从零构建多平台隔离环境的高昂技术成本，尤其适合需要运行 LLM 模型输出和第三方插件的工作流
- 企业级 AI 平台可参考 MXC 的多层隔离后端架构（进程级到虚拟机级）设计插件系统安全执行环境，文件系统/网络/UI 三级策略控制模式可作为行业参考范式
- 围绕 MXC 的安全审计与策略硬化工具链存在创业机会——官方明确承认当前策略过度宽松，意味着第三方安全加固、合规扫描和策略推荐工具将有市场需求
risk_matrix:
  regulatory: MXC 当前为早期预览版且官方明确声明安全策略存在过度宽松问题，企业若提前用于处理受监管数据（如金融、医疗、政务）可能面临合规审查风险，需等待正式版安全边界确认后再投入敏感场景
  technological: 沙盒执行领域已有 gVisor、Firecracker、nsjail、Docker 等成熟方案，MXC 需证明其跨平台统一 API
    和多重后端架构的差异化优势；当前 Rust 1.93 特定版本和 Node.js ≥ 18 的构建依赖可能带来兼容性障碍
  competitive: 微软开源 MXC 可能挤压独立沙盒创业公司的市场空间；AWS（Firecracker）、Google（gVisor）等云巨头已有类似产品，生态竞争激烈且切换成本较低
  ethical: 若开发者忽视官方安全警告而过早将 MXC 投入生产环境，宽松策略可能导致沙盒逃逸和数据泄露；被用于执行不可信代码的特性在未充分硬化时存在被恶意利用的风险
  additional:
  - 多后端架构增加了运维复杂度，不同平台默认后端和行为差异（如 Windows 上 denied paths 尚不支持）可能导致开发与生产环境行为不一致
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: deep_dive
---

MXC is a **sandboxed code execution system** for running untrusted code (model output, plugins, tools) on Windows, Linux, and macOS. It provides multiple containment backends — from OS-native process sandboxes to full VMs — behind a unified JSON configuration schema and TypeScript SDK.

Warning

This repository contains an early preview of code published to enable early integration and feedback from developers on Microsoft Execution Containers. The underlying sandboxes in this early preview are expected to change as they are under ongoing development however we will aim to minimize compatibility impact as functionality evolves. There are known cases where the current policies generated by the MXC SDK in this repository are overly permissive and will be addressed before this is made more generally available. Security researcher partnership while MXC matures is welcome, however no MXC profiles should be treated as security boundaries currently.

**Cross-platform**: Windows, Linux, and macOS support with platform-appropriate containment backends**JSON-based Configuration**: Define execution parameters and security policies via a versioned JSON schema**Multiple Containment Backends**: ProcessContainer, Windows Sandbox, LXC, Bubblewrap, Seatbelt (macOS), MicroVM (NanVix), Hyperlight, IsolationSession, and WSLC**Policy-driven Sandboxing**:**Filesystem Policy**: Read-only and read-write path lists (denied paths not yet supported on Windows)**Network Policy**: Proxy support (not supported on macOS); allow/block outbound and host filtering (not yet supported on Windows)**UI Policy**: Clipboard, display, and GUI access controls

**State-aware Lifecycle**: Multi-step sandbox lifecycle (provision → start → exec → stop → deprovision) for session sandboxes**TypeScript SDK**:`@microsoft/mxc-sdk`

npm package with one-shot and state-aware APIs**Diagnostics**: Debug logging and Event Tracing for Windows (ETW) for troubleshooting

MXC ships a native container wrapper plus a TypeScript SDK — see the SDK README for full API documentation.

| Platform | Default backend | Other backends | Minimum build |
|---|---|---|---|
| Windows 11 24H2+ (verified on 25H2) | `processcontainer` |
`windows_sandbox` , `wslc` , `microvm` , `hyperlight` , `isolation_session` |
`processcontainer` : 26100 (24H2)`isolation_session` : 26300.8553 (Insider Preview) |
| Linux x64 / ARM64 | `bubblewrap` |
`lxc` , `microvm` , `hyperlight` |
— |
macOS ARM64 / x64 (schema `0.6.0-alpha` +) |
`seatbelt` |
— | — |

The stable one-shot backends (`processcontainer`

, `bubblewrap`

, and `lxc`

) do not require experimental mode; Linux hosts also need the matching runtime installed: bwrap (Bubblewrap) for the default backend, or the lxc toolset for the lxc backend. **Experimental backends** (`windows_sandbox`

, `wslc`

, `microvm`

, `seatbelt`

, `isolation_session`

, `hyperlight`

) require `{ experimental: true }`

in `SandboxSpawnOptions`

or the `--experimental`

CLI flag.

- Rust toolchain — version pinned to
**1.93**via`src/rust-toolchain.toml`

(auto-selected by`rustup`

) - Node.js
**≥ 18** - npm (for SDK and CLI builds)

```
src/ Rust workspace (native binaries + shared library crates)
sdk/ TypeScript SDK (@microsoft/mxc-sdk npm package)
schemas/ JSON configuration schemas (stable + dev)
docs/ Documentation (schema reference, backend guides, design docs)
tests/ Test collateral (configs, examples, scripts)
scripts/ Build and utility scripts
```


```
build.bat # Release build for current architecture
build.bat --debug # Debug build
build.bat --all # Release build for both x64 and ARM64
build.bat --with-microvm # Include NanVix micro-VM binaries
```

```
./build.sh # Release build
./build.sh --debug # Debug build
./build.sh --rust-only # Only Rust binaries, skip SDK/CLI
```

```
./build-mac.sh # Release build for native architecture
./build-mac.sh --all # Both Apple Silicon and Intel
./build-mac.sh --debug # Debug build
./build-mac.sh --rust-only # Only Rust binary, skip SDK
```

All build scripts:

-
Build the platform-appropriate Rust binary

-
Copy the binary into

`sdk/bin/<arch>/`

(for example,`x64`

or`arm64`

) for SDK bundling -
Build the TypeScript SDK


```
# Rust workspace (from src/)
cargo build --release --target x86_64-pc-windows-msvc # Windows x64
cargo build --release --target aarch64-pc-windows-msvc # Windows ARM64
cargo build --release -p lxc # Linux — lxc-exec (serves both LXC and Bubblewrap)
cargo build --release -p mxc_darwin --target aarch64-apple-darwin # macOS
# SDK (from sdk/)
npm install && npm run build
```

```
# Windows Rust (from src/)
cargo clippy --workspace --all-targets -- -D warnings
# Linux Rust (from src/; matches build.sh's platform-compatible crate set)
cargo clippy -p lxc -p lxc_common -p wxc_common -p bwrap_common -p linux_test_proxy --all-targets -- -D warnings
# macOS Rust (from src/)
cargo clippy -p mxc_darwin -p seatbelt_common --all-targets -- -D warnings
```

```
# Rust unit tests (from src/)
cargo test --workspace
cargo test -p wxc_common # Single crate
cargo test -p wxc_common -- config_parser # Filter by test name
# SDK (from sdk/)
npm test # Unit tests
npm run test:integration # Integration tests
# E2E (from src/)
cargo test -p wxc_e2e_tests
```

MXC uses a JSON configuration to define execution parameters. See the schema documentation for full reference.

```
# File path
wxc-exec.exe config.json
# Base64-encoded config
wxc-exec.exe --config-base64 <base64-encoded-json>
# Debug output
wxc-exec.exe --debug config.json
```

On Linux: `./lxc-exec config.json`

On macOS: `./mxc-exec-mac --experimental config.json`


`npm install @microsoft/mxc-sdk`

```
import {
spawnSandboxFromConfig, createConfigFromPolicy,
getAvailableToolsPolicy, getTemporaryFilesPolicy,
getPlatformSupport,
} from '@microsoft/mxc-sdk';
if (!getPlatformSupport().isSupported) {
throw new Error('MXC not available on this host');
}
const tools = getAvailableToolsPolicy(process.env);
const temp = getTemporaryFilesPolicy();
const config = createConfigFromPolicy({
version: '0.6.0-alpha',
filesystem: {
readonlyPaths: tools.readonlyPaths,
readwritePaths: temp.readwritePaths,
},
network: { allowOutbound: false },
timeoutMs: 30_000,
});
config.process!.commandLine = 'python -c "print(\'hello from sandbox\')"';
const child = spawnSandboxFromConfig(config, { usePty: false });
child.stdout!.on('data', (d) => process.stdout.write(d));
child.on('close', (code) => console.log('exit:', code));
```

The SDK also provides a **state-aware lifecycle** API for long-lived sandboxes:

```
import {
provisionSandbox, startSandbox, execInSandboxAsync,
stopSandbox, deprovisionSandbox,
} from '@microsoft/mxc-sdk';
```

See the SDK README for full API documentation.

| Version | Status | Schema File |
|---|---|---|
`0.5.0-alpha` |
Stable | `schemas/stable/mxc-config.schema.0.5.0-alpha.json` |
`0.6.0-alpha` |
Stable (current) | `schemas/stable/mxc-config.schema.0.6.0-alpha.json` |
`0.7.0-dev` |
Dev (experimental backends, state-aware lifecycle) | `schemas/dev/mxc-config.schema.0.7.0-dev.json` |

Pick `0.6.0-alpha`

for new code on any supported platform. See docs/versioning.md for the full versioning design.

By default, native binaries run in **silent mode** — stdin/stdout/stderr is coupled directly to the container. Use `--debug`

for verbose output:

`wxc-exec.exe --debug config.json`

See docs/diagnostics.md for full diagnostics reference.

| Document | Description |
|---|---|
| docs/schema.md | Full JSON configuration schema reference |
| docs/versioning.md | Schema versioning and experimental feature lifecycle |
| docs/examples.md | Annotated configuration examples |
| docs/host-prep.md | Windows host preparation (`wxc-host-prep.exe` ) |
| docs/diagnostics.md | Diagnostic logging and ETW |
| docs/sandbox-policy/v1/policy.md | Sandbox policy v1 specification |
| docs/base-process-container/guide.md | Windows AppContainer / BaseContainer guide |
| docs/lxc-support/lxc-backend.md | LXC backend (Linux) |
| docs/bwrap-support/bubblewrap-backend.md | Bubblewrap backend (Linux) |
| docs/macos-support/seatbelt-backend.md | Seatbelt backend (macOS) |
| docs/windows-sandbox/windows-sandbox.md | Windows Sandbox backend |
| docs/state-aware-lifecycle/mxc-state-aware-sandbox-api.md | State-aware sandbox lifecycle API |

See CONTRIBUTING.md for contribution guidelines.

See LICENSE.md for details.