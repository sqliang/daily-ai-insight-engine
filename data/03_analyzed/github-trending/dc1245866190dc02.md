---
title: MemPalace/mempalace
source: https://github.com/MemPalace/mempalace
author: []
published: ''
created: '2026-06-07'
description: 'The best-benchmarked open-source AI memory system. And it''s free. MemPalace
  Local-first AI memory. Verbatim storage, pluggable backend, 96.6% R@5 raw on LongMemEval
  — zero API calls. Caution Beware of impostor sites. MemPalace has no other official
  websites. The only official sources are this GitHub repository, the PyPI package,
  and the docs at mempalaceofficial.com. Any other domain (including .tech, .net,
  or other .com variants) is an impostor and may distribute malware. Details and timeline:
  docs/HISTORY.md. Important Claude Code sessions expire in 30 days without auto-save
  hooks wired. Read this → Need the shortest recovery/setup path? Use the Claude Code
  retention setup checklist. What it is MemPalace stores your conversation history
  as verbatim text and retrieves it with semantic search. It does not summarize, extract,
  or paraphrase. The index is structured — people and projects become wings, topics
  become rooms, and original content lives in drawers — so searches can be scoped
  rather than run against a flat corpus. The retrieval layer is pluggable. The current
  default is ChromaDB; the interface is defined in mempalace/backends/base.py and
  alternative backends can be dropped in without touching the rest of the system.
  Nothing leaves your machine unless you opt in. Architecture, concepts, and mining
  flows: mempalaceofficial.com/concepts/the-palace. Install MemPalace ships a CLI,
  so install it in an isolated environment to avoid PEP 668 errors on Debian/Ubuntu/Homebrew
  Pythons and to keep mempalace''s deps (chromadb, numpy, grpcio, …) from conflicting
  with anything else in your global site-packages. We recommend uv — uv tool install
  puts the mempalace CLI in an isolated environment on your PATH: uv tool install
  mempalace mempalace init ~/projects/myapp pipx works the same way if you prefer
  it: pipx install mempalace. Prefer plain pip only inside an activated virtualenv
  where you explicitly want import mempalace available: python -m venv .venv && source
  .venv/bin/activate pip install mempalace Docker A container image is also available
  for running the MCP server or the CLI without a local Python toolchain. Everything
  persists under /data (palace, config, and the cached embedding model), so mount
  a volume there. # Build the image (CPU; bundles the `extract` + `spellcheck` extras)
  docker build -t mempalace . # MCP server over stdio — note the `-i` flag (JSON-RPC
  needs stdin) docker run -i --rm -v mempalace-data:/data mempalace # Run any CLI
  command instead (mount the host directory you want to mine) docker run --rm -v mempalace-data:/data
  -v /path/to/project:/work mempalace mine /work docker run --rm -v mempalace-data:/data
  mempalace search "why GraphQL" Wire it into an MCP client (e.g. Claude Code) as
  a stdio server: { "mcpServers": { "mempalace": { "command": "docker", "args": ["run",
  "-i", "--rm", "-v", "mempalace-data:/data", "mempalace"] } } } docker compose run
  --rm mcp works too (see docker-compose.yml). For CUDA-accelerated embeddings, build
  the GPU variant with docker build -f Dockerfile.gpu -t mempalace:gpu . and run it
  with --gpus all. Customise the bundled extras at build time, e.g. docker build --build-arg
  EXTRAS="extract,spellcheck" -t mempalace .. Storage backends ChromaDB is the default.
  For the pluggable-backend preview, MemPalace also ships sqlite_exact for local exact-vector
  correctness checks, and two opt-in external service backends — qdrant (REST) and
  pgvector (Postgres). The two external backends exercise the storage contract on
  different substrates (a REST/dict store and a SQL/JSONB store), so it is not accidentally
  shaped around one vendor. # local no-service backend mempalace mine ~/projects/myapp
  --backend sqlite_exact # Qdrant backend, defaulting to http://localhost:6333 MEMPALACE_QDRANT_URL=http://localhost:6333
  \ mempalace mine ~/projects/myapp --backend qdrant # Postgres + pgvector backend,
  defaulting to postgresql://localhost:5432/mempalace # needs the optional driver:
  pip install mempalace[pgvector] # and the `vector` extension available on the server
  MEMPALACE_PGVECTOR_DSN=postgresql://localhost:5432/mempalace \ mempalace mine ~/projects/myapp
  --backend pgvector Qdrant can also be configured with MEMPALACE_QDRANT_API_KEY,
  MEMPALACE_QDRANT_NAMESPACE, and MEMPALACE_QDRANT_TIMEOUT; pgvector with MEMPALACE_PGVECTOR_NAMESPACE.
  Both external backends isolate tenants by namespace (advertised via the supports_namespace_isolation
  capability) and write a local marker (qdrant_backend.json / pgvector_backend.json)
  to guard against silently opening a palace against the wrong server. When MEMPALACE_QDRANT_URL
  or MEMPALACE_PGVECTOR_DSN points anywhere other than your own local or trusted self-hosted
  service, MemPalace will send and store verbatim drawer text and metadata there.
  That is an explicit opt-in backend choice, never the default. Quickstart # Mine
  content into the palace mempalace mine ~/projects/myapp # project files mempalace
  mine ~/.claude/projects/ --mode convos # Claude Code sessions (scope with --wing
  per project) # Search mempalace search "why did we switch to GraphQL" # Load context
  for a new session mempalace wake-up For Claude Code, Gemini CLI, MCP-compatible
  tools, and local models, see mempalaceofficial.com/guide/getting-started. Benchmarks
  All numbers below are reproducible from this repository with the commands in benchmarks/BENCHMARKS.md.
  Full per-question result files are committed under benchmarks/results_*. LongMemEval
  — retrieval recall (R@5, 500 questions): Mode R@5 LLM required Raw (semantic search,
  no heuristics, no LLM) 96.6% None Hybrid v4, held-out 450q (tuned on 50 dev, not
  seen during training) 98.4% None Hybrid v4 + LLM rerank (full 500) ≥99% Any capable
  model The raw 96.6% requires no API key, no cloud, and no LLM at any stage. The
  hybrid pipeline adds keyword boosting, temporal-proximity boosting, and preference-pattern
  extraction; the held-out 98.4% is the honest generalisable figure. The rerank pipeline
  promotes the best candidate out of the top-20 retrieved sessions using an LLM reader.
  It works with any reasonably capable model — we have reproduced it with Claude Haiku,
  Claude Sonnet, and minimax-m2.7 via Ollama Cloud (no Anthropic dependency). The
  gap between raw and reranked is model-agnostic; we do not headline a "100%" number
  because the last 0.6% was reached by inspecting specific wrong answers, which benchmarks/BENCHMARKS.md
  flags as teaching to the test. Other benchmarks (full results in benchmarks/BENCHMARKS.md):
  Benchmark Metric Score Notes LoCoMo (session, top-10, no rerank) R@10 60.3% 1,986
  questions LoCoMo (hybrid v5, top-10, no rerank) R@10 88.9% Same set ConvoMem (all
  categories, 250 items) Avg recall 92.9% 50 per category MemBench (ACL 2025, 8,500
  items) R@5 80.3% All categories We deliberately do not include a side-by-side comparison
  against Mem0, Mastra, Hindsight, Supermemory, or Zep. Those projects publish different
  metrics on different splits, and placing retrieval recall next to end-to-end QA
  accuracy is not an honest comparison. See each project''s own research page for
  their published numbers. Reproducing every result: git clone https://github.com/MemPalace/mempalace.git
  cd mempalace uv sync --extra dev # or: pip install -e ".[dev]" # see benchmarks/README.md
  for dataset download commands uv run python benchmarks/longmemeval_bench.py /path/to/longmemeval_s_cleaned.json
  Knowledge graph MemPalace includes a temporal entity-relationship graph with validity
  windows — add, query, invalidate, timeline — backed by local SQLite. Usage and tool
  reference: mempalaceofficial.com/concepts/knowledge-graph. MCP server 29 MCP tools
  cover palace reads/writes, knowledge-graph operations, cross-wing navigation, drawer
  management, and agent diaries. Installation and the full tool list: mempalaceofficial.com/reference/mcp-tools.
  Agents Each specialist agent gets its own wing and diary in the palace. Discoverable
  at runtime via mempalace_list_agents — no bloat in your system prompt: mempalaceofficial.com/concepts/agents.
  Auto-save hooks Two Claude Code hooks save periodically and before context compression:
  mempalaceofficial.com/guide/hooks. If you are installing under time pressure, start
  with the Claude Code retention setup checklist: wire the hooks, back up existing
  JSONL transcripts, and backfill them with mempalace mine ~/.claude/projects/ --mode
  convos. For per-message recall on top of the file-level chunks the hooks produce,
  run mempalace sweep <transcript-dir> periodically — it stores one verbatim drawer
  per user/assistant message, idempotent and resume-safe. Requirements Python 3.9+
  A vector-store backend (ChromaDB by default) ~300 MB disk for the embedding model.
  Onboarding (python -m mempalace.onboarding) offers embeddinggemma-300m (multilingual,
  100+ languages, recommended) or all-MiniLM-L6-v2 (English-only, ~30 MB). See the
  docstring at mempalace/embedding.py for details and migration notes. No API key
  is required for the core benchmark path. Docs Getting started → mempalaceofficial.com/guide/getting-started
  CLI reference → mempalaceofficial.com/reference/cli Python API → mempalaceofficial.com/reference/python-api
  Full benchmark methodology → benchmarks/BENCHMARKS.md Release notes → CHANGELOG.md
  Corrections and public notices → docs/HISTORY.md Contributing PRs welcome. See CONTRIBUTING.md.
  License MIT — see LICENSE.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: dc1245866190dc02
source_type: community_discussion
tldr: MemPalace 是一个本地优先的 AI 记忆开源项目，以原文形式存储对话历史并通过语义搜索检索，在 LongMemEval 基准测试中纯语义搜索达到
  96.6% R@5 召回率，无需任何 API 调用或 LLM 参与。
objective_summary: MemPalace 是一个本地优先的开源 AI 记忆项目，以原文形式存储对话历史并通过语义搜索实现检索，不进行摘要、提取或改写操作。其默认向量存储后端为
  ChromaDB，同时支持 Qdrant 和 pgvector 等可插拔后端方案。在 LongMemEval 500 道题的检索召回测试中，纯语义搜索模式达到
  96.6% R@5 召回率，混合 v4 模式达到 98.4%，均无需 API 密钥或外部 LLM 参与。项目提供 29 个 MCP 工具和一个基于本地 SQLite
  的时序知识图谱，数据默认不离开用户机器。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - MemPalace
  technologies:
  - ChromaDB
  - Qdrant
  - pgvector
  - MCP
  - SQLite
  - LongMemEval
  - LoCoMo
  - ConvoMem
  - MemBench
  - Semantic Search
  key_people: []
key_logic_flow:
- MemPalace 以全文形式存储对话历史并通过语义搜索检索，不做摘要、提取或改写，确保信息完整无损。
- 检索层采用可插拔架构，默认使用 ChromaDB 后端，也可切换为 Qdrant、pgvector 或 SQLite exact 后端。
- 在 LongMemEval 500 道题的测试中，纯语义搜索模式达到 96.6% R@5 召回率，混合 v4 模式达到 98.4%，均无需 LLM 或 API
  密钥。
- 项目提供 29 个 MCP 工具，覆盖宫殿读写、知识图谱操作、跨 wing 导航和代理日记管理等功能。
- 内置基于本地 SQLite 的时序实体关系图谱，支持实体添加、关系查询、失效标记和 timeline 回溯。
- 该项目强调本地优先设计，数据默认不离开用户机器，使用 Qdrant 或 pgvector 等外部后端需用户明确选择启用。
extract_result: success
object_mentions:
- object_type: project
  name: MemPalace/mempalace
  canonical_name: MemPalace/mempalace
  url: https://github.com/MemPalace/mempalace
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - MemPalace 以全文形式存储对话历史并通过语义搜索检索，不进行摘要、提取或改写，确保信息完整无损。
  - 在 LongMemEval 500 道题的检索召回测试中，纯语义搜索模式达到 96.6% R@5 召回率，无需任何 API 密钥或 LLM 参与。
  - 该项目采用可插拔检索层架构，默认使用 ChromaDB 后端，同时支持 Qdrant 和 pgvector 等外部存储方案。
  article_id: dc1245866190dc02
- object_type: project
  name: ChromaDB
  canonical_name: ChromaDB
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - MemPalace 默认使用 ChromaDB 作为向量存储后端，其接口在 mempalace/backends/base.py 中定义，可被替换为其他后端实现。
  article_id: dc1245866190dc02
- object_type: project
  name: Qdrant
  canonical_name: Qdrant
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - MemPalace 支持通过 MEMPALACE_QDRANT_URL 环境变量配置 Qdrant REST 后端，并可设置 API 密钥和命名空间进行租户隔离。
  article_id: dc1245866190dc02
- object_type: project
  name: pgvector
  canonical_name: pgvector
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - MemPalace 支持 pgvector 作为 Postgres 后端，需安装 mempalace[pgvector] 可选驱动且服务器提供 vector
    扩展。
  article_id: dc1245866190dc02
impact_score:
  score: 5.5
  reason: MemPalace 发布了一个设计扎实的本地优先 AI 记忆工具。其纯语义搜索无需任何 API 调用即可在 LongMemEval 上达到 96.6%
    R@5，对 AI 代理的记忆层构建有参考价值。但它仍是一个早期开源项目（500 题基准规模有限，LongMemEval 并非行业标准基准），尚未形成广泛的生态效应。该发布改善了
    AI 记忆/上下文管理工具类的局部竞争格局，但远未达到行业范式转移的级别。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 本地优先、零 API 费用即可达高召回率的 AI 记忆方案，以及 29 个 MCP 工具的集成实用性
hype_assessment:
  level: low
  reason: 项目提供了可复现的基准测试命令（50 题 dev + 450 题 held-out 集）、完整的性能数据、清晰的架构文档，并明确标注了外部后端场景的隐私风险。README
    采用技术文档风格，没有使用'颠覆''革命性'等 PR 话术，而是如实呈现能力和局限。
information_entropy: high
domain_disruption:
  technical_innovation: 逐字原文存储（不做摘要/提取/重述）+ 可插拔检索后端（ChromaDB/Qdrant/pgvector/SQLite）+
    结构化索引（wing/room/drawer 分层）+ 含有效性窗口的时序知识图谱，在 AI 记忆工具中构成了一套务实且模块化的架构组合。96.6% R@5
    纯语义搜索证明高召回检索无需 LLM 参与，挑战了'记忆必须靠 AI 摘要'的隐含前提。
  business_model: 无——该项目为开源社区项目，目前没有明确的商业模式。其潜在影响是可能成为 AI 代理框架中记忆层的参考实现，类似 LangChain
    之于 Agent 编排的生态位。
engineering_complexity: production_ready
compound_value:
  score: 6.5
  reason: MemPalace 解决了 AI Agent 生态中一个真实且日益尖锐的痛点——对话历史与上下文的持久化记忆。其技术亮点在于：本地优先架构无需
    API 调用即达到 96.6% R@5（LongMemEval），纯语义搜索性能令人印象深刻；29 个 MCP 工具使其天然融入快速扩张的 MCP 生态，成为
    Claude Code、Gemini CLI 等客户端的记忆层基础设施候选；可插拔后端（ChromaDB/Qdrant/pgvector）提供了架构灵活性。从长期复利角度看，如果
    MCP 协议成为 AI Agent 的事实标准通信层，MemPalace 作为 MCP 原生的记忆中间件将占据先发优势，具备成为细分赛道基础设施的潜力。但扣分点在于：这是一个开源工具项目，没有明确的公司实体或商业模式支撑，长期维护和迭代依赖社区活力，而
    AI 记忆赛道已出现 Mem0、MemGPT/Letta 等竞品；'本地优先'虽然带来隐私优势，但也限制了云原生场景下的网络效应和数据飞轮。综合判断，它在技术架构和时机上占据有利位置，但需观察是否能从工具进化为可持续的生态基础设施，评分留出下行空间以反映开源项目的不确定性。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Claude Code 用户
- Gemini CLI 用户
- MCP 生态系统
- ChromaDB
- Qdrant
competitive_casualty:
- 闭源 AI 记忆 API 服务
- 依赖云端 LLM 的记忆工具
- 缺乏开放协议的记忆平台
market_opportunities:
- AI Agent 开发者可直接集成 MemPalace 的 29 个 MCP 工具为编程助手、知识管理工具等提供持久化记忆能力，解决当前 LLM 对话上下文的碎片化痛点
- 企业合规部门可基于其本地优先 + 逐字存储（verbatim）设计，在金融、医疗等强监管行业部署可审计的 AI 对话记忆系统，所有数据不出本地且无摘要丢失风险
- 基础设施类创业团队可借鉴其可插拔检索后端架构（ChromaDB/Qdrant/pgvector），为不同规模团队提供分层部署方案——从个人开发者的 SQLite
  到企业级 Postgres 集群
risk_matrix:
  regulatory: 本地优先设计默认规避了 GDPR/个人信息保护法的跨境数据传输风险，但若配置外部后端（Qdrant 云端或远程 pgvector），verbatim
    文本中混入的 PII 将直接落入第三方存储，需额外签订数据处理协议
  technological: 96.6% R@5 的纯语义检索表现依赖默认嵌入模型的质量，若模型更新导致向量分布偏移则召回率可能下降；此外，大模型原生超长上下文（如
    1M+ token）若成为标配，外部记忆层的刚需程度将被稀释
  competitive: 面临 mem0、Mem.ai、Rewind.ai 等同赛道竞品的直接竞争，且 OpenAI/Anthropic 随时可能将记忆功能内置到平台层；MCP
    协议本身尚处生态培育期，若未能成为行业标准则项目价值将大打折扣
  ethical: 逐字存储对话历史意味着用户的每一句话都被永久记录，存在无意识捕获他人敏感信息的风险；知识图谱在编码人际关系时可能固化并放大对话中的偏见或错误关联
  additional:
  - 项目文档已警告出现假冒域名和恶意软件分发事件，开源项目品牌保护能力弱，社区用户可能遭遇供应链攻击
  - 项目仍处于早期阶段，长期维护活力和社区治理机制尚待验证，存在 abandonware 风险
confidence:
  impact: medium
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: MemPalace/mempalace
  canonical_name: MemPalace/mempalace
  url: https://github.com/MemPalace/mempalace
  positioning: 本地优先的 AI 记忆开源项目，以原文形式完整存储对话历史并通过语义搜索检索，不进行摘要、提取或改写操作。
  technical_signal: 在 LongMemEval 500 道题测试中纯语义搜索达到 96.6% R@5 召回率，混合 v4 模式达 98.4%，无需
    LLM 或 API 密钥参与。
  adoption_signal: 项目提供 29 个 MCP 工具和本地时序知识图谱，支持 uv/pipx/Docker 安装，可集成到 Claude Code
    等 MCP 客户端使用。
  ecosystem_relevance: 采用可插拔检索层架构，默认 ChromaDB 后端，支持切换为 Qdrant、pgvector 或 SQLite exact
    后端，适应不同部署场景。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: MemPalace 以"不摘要、不改写"的全文存储策略在 AI 记忆赛道走出差异化路线，纯语义搜索即可达 96.6% 召回率且无需外部
    LLM，结合 MCP 协议融入现有 AI 工具生态，是本地优先 AI 记忆基础设施的有力竞争者。
  risk_notes:
  - 全文存储策略在对话量极大时可能面临存储膨胀问题，长周期运行后的存储开销与检索性能尚需验证。
  - 项目仍处于早期阶段，社区规模和生态系统成熟度有限，长期维护可持续性有待观察。
  score: 8.0
  article_ids:
  - dc1245866190dc02
  evidence_snippets:
  - MemPalace 以全文形式存储对话历史并通过语义搜索检索，不进行摘要、提取或改写，确保信息完整无损。
  - 在 LongMemEval 500 道题的检索召回测试中，纯语义搜索模式达到 96.6% R@5 召回率，无需任何 API 密钥或 LLM 参与。
  - 该项目采用可插拔检索层架构，默认使用 ChromaDB 后端，同时支持 Qdrant 和 pgvector 等外部存储方案。
- object_type: project
  name: ChromaDB
  canonical_name: ChromaDB
  url: null
  positioning: 开源嵌入式向量数据库，为 MemPalace 提供默认的本地向量存储检索能力，无需额外配置即可运行。
  technical_signal: 在 MemPalace 中作为默认向量存储后端，其接口被定义为可替换的后端抽象，支持交换为其他向量数据库。
  adoption_signal: 作为 MemPalace 默认后端用户无需额外配置即可获得本地向量检索能力，降低了 AI 记忆系统的部署门槛。
  ecosystem_relevance: 通过 MemPalace 的可插拔后端架构与之深度集成，成为本地优先 AI 记忆方案的核心存储组件之一。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 MemPalace 默认向量后端，随该项目在 AI 记忆场景的成长可能获得更多采用，但其角色为可替换组件而非唯一选型。
  risk_notes:
  - 作为项目依赖项，其在 MemPalace 生态中的角色是可替换的，不一定能持续绑定在默认后端位置。
  score: 5.0
  article_ids:
  - dc1245866190dc02
  evidence_snippets:
  - MemPalace 默认使用 ChromaDB 作为向量存储后端，其接口在 mempalace/backends/base.py 中定义，可被替换为其他后端实现。
- object_type: project
  name: Qdrant
  canonical_name: Qdrant
  url: null
  positioning: 开源向量数据库，通过 REST API 为 MemPalace 提供可选的云原生向量存储后端，支持租户隔离与命名空间管理。
  technical_signal: MemPalace 支持通过 MEMPALACE_QDRANT_URL 等环境变量配置 Qdrant REST 后端，具备
    API 密钥和命名空间租户隔离能力。
  adoption_signal: 作为 MemPalace 可选的外部存储后端，用户需明确选择启用，数据将发送到配置的 Qdrant 服务器而非本地默认存储。
  ecosystem_relevance: 与 MemPalace 的可插拔后端架构兼容，为需要云原生分布式向量存储的用户提供从本地到远端的弹性部署选项。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 MemPalace 支持的外部向量存储后端之一，为需要分布式存储或云部署场景的用户提供弹性扩展方案，验证了 REST 后端在
    AI 记忆系统中的可行性。
  risk_notes:
  - 使用外部 Qdrant 后端时数据将离开用户本地机器，需用户明确 opt-in，存在数据隐私与传输安全考量。
  score: 4.0
  article_ids:
  - dc1245866190dc02
  evidence_snippets:
  - MemPalace 支持通过 MEMPALACE_QDRANT_URL 环境变量配置 Qdrant REST 后端，并可设置 API 密钥和命名空间进行租户隔离。
- object_type: project
  name: pgvector
  canonical_name: pgvector
  url: null
  positioning: PostgreSQL 的向量检索扩展，为 MemPalace 提供基于 SQL/JSONB 的向量存储后端，面向已有 Postgres
    基础设施的团队。
  technical_signal: MemPalace 支持 pgvector 作为 Postgres 后端，需安装 mempalace[pgvector] 可选驱动且服务器提供
    vector 扩展。
  adoption_signal: 作为 MemPalace 可选后端之一，面向已有 Postgres 基础设施的用户群体，通过命名空间实现多租户隔离。
  ecosystem_relevance: 通过 MemPalace 可插拔架构集成，为 Postgres 生态用户提供 AI 记忆存储方案，降低引入新存储系统的迁移成本。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 成为 MemPalace 三种可选后端之一，验证了 pgvector 作为通用向量存储方案在 AI 记忆场景的适用性，为 Postgres
    用户提供无缝整合路径。
  risk_notes:
  - 依赖 Postgres 服务器提供 vector 扩展，部署复杂度相对高于本地嵌入式方案如 ChromaDB。
  - pgvector 在向量检索性能与精度方面与专用向量数据库存在差距，大规模场景下需评估。
  score: 4.0
  article_ids:
  - dc1245866190dc02
  evidence_snippets:
  - MemPalace 支持 pgvector 作为 Postgres 后端，需安装 mempalace[pgvector] 可选驱动且服务器提供 vector
    扩展。
---

Local-first AI memory. Verbatim storage, pluggable backend, 96.6% R@5 raw on LongMemEval — zero API calls.

Caution

**Beware of impostor sites.** MemPalace has no other official websites. The **only** official sources are this **GitHub repository**, the **PyPI package**, and the docs at **mempalaceofficial.com**. Any other domain (including `.tech`

, `.net`

, or other `.com`

variants) is an impostor and may distribute malware. Details and timeline: docs/HISTORY.md.

Important

**Claude Code sessions expire in 30 days without auto-save hooks wired.** Read this →

Need the shortest recovery/setup path? Use the Claude Code retention setup checklist.

MemPalace stores your conversation history as verbatim text and retrieves
it with semantic search. It does not summarize, extract, or paraphrase.
The index is structured — people and projects become *wings*, topics
become *rooms*, and original content lives in *drawers* — so searches
can be scoped rather than run against a flat corpus.

The retrieval layer is pluggable. The current default is ChromaDB; the
interface is defined in `mempalace/backends/base.py`

and alternative backends can be dropped in without touching the rest of
the system.

Nothing leaves your machine unless you opt in.

Architecture, concepts, and mining flows: mempalaceofficial.com/concepts/the-palace.

MemPalace ships a CLI, so install it in an isolated environment to avoid
PEP 668 errors on Debian/Ubuntu/Homebrew Pythons and to keep mempalace's
deps (`chromadb`

, `numpy`

, `grpcio`

, …) from conflicting with anything
else in your global site-packages.

We recommend `uv`

— `uv tool install`

puts
the `mempalace`

CLI in an isolated environment on your PATH:

```
uv tool install mempalace
mempalace init ~/projects/myapp
```

`pipx`

works the same way if you prefer it:
`pipx install mempalace`

.

Prefer plain `pip`

only inside an activated virtualenv where you
explicitly want `import mempalace`

available:

```
python -m venv .venv && source .venv/bin/activate
pip install mempalace
```

A container image is also available for running the MCP server or the CLI
without a local Python toolchain. Everything persists under `/data`

(palace,
config, and the cached embedding model), so mount a volume there.

```
# Build the image (CPU; bundles the `extract` + `spellcheck` extras)
docker build -t mempalace .
# MCP server over stdio — note the `-i` flag (JSON-RPC needs stdin)
docker run -i --rm -v mempalace-data:/data mempalace
# Run any CLI command instead (mount the host directory you want to mine)
docker run --rm -v mempalace-data:/data -v /path/to/project:/work mempalace mine /work
docker run --rm -v mempalace-data:/data mempalace search "why GraphQL"
```

Wire it into an MCP client (e.g. Claude Code) as a stdio server:

```
{
"mcpServers": {
"mempalace": {
"command": "docker",
"args": ["run", "-i", "--rm", "-v", "mempalace-data:/data", "mempalace"]
}
}
}
```

`docker compose run --rm mcp`

works too (see `docker-compose.yml`

). For
CUDA-accelerated embeddings, build the GPU variant with
`docker build -f Dockerfile.gpu -t mempalace:gpu .`

and run it with
`--gpus all`

. Customise the bundled extras at build time, e.g.
`docker build --build-arg EXTRAS="extract,spellcheck" -t mempalace .`

.

ChromaDB is the default. For the pluggable-backend preview, MemPalace also
ships `sqlite_exact`

for local exact-vector correctness checks, and two opt-in
external service backends — `qdrant`

(REST) and `pgvector`

(Postgres). The two
external backends exercise the storage contract on different substrates (a
REST/dict store and a SQL/JSONB store), so it is not accidentally shaped around
one vendor.

```
# local no-service backend
mempalace mine ~/projects/myapp --backend sqlite_exact
# Qdrant backend, defaulting to http://localhost:6333
MEMPALACE_QDRANT_URL=http://localhost:6333 \
mempalace mine ~/projects/myapp --backend qdrant
# Postgres + pgvector backend, defaulting to postgresql://localhost:5432/mempalace
# needs the optional driver: pip install mempalace[pgvector]
# and the `vector` extension available on the server
MEMPALACE_PGVECTOR_DSN=postgresql://localhost:5432/mempalace \
mempalace mine ~/projects/myapp --backend pgvector
```

Qdrant can also be configured with `MEMPALACE_QDRANT_API_KEY`

,
`MEMPALACE_QDRANT_NAMESPACE`

, and `MEMPALACE_QDRANT_TIMEOUT`

; pgvector with
`MEMPALACE_PGVECTOR_NAMESPACE`

. Both external backends isolate tenants by
namespace (advertised via the `supports_namespace_isolation`

capability) and
write a local marker (`qdrant_backend.json`

/ `pgvector_backend.json`

) to guard
against silently opening a palace against the wrong server.

When `MEMPALACE_QDRANT_URL`

or `MEMPALACE_PGVECTOR_DSN`

points anywhere other
than your own local or trusted self-hosted service, MemPalace will send and
store verbatim drawer text and metadata there. That is an explicit opt-in
backend choice, never the default.

```
# Mine content into the palace
mempalace mine ~/projects/myapp # project files
mempalace mine ~/.claude/projects/ --mode convos # Claude Code sessions (scope with --wing per project)
# Search
mempalace search "why did we switch to GraphQL"
# Load context for a new session
mempalace wake-up
```

For Claude Code, Gemini CLI, MCP-compatible tools, and local models, see mempalaceofficial.com/guide/getting-started.

All numbers below are reproducible from this repository with the commands
in `benchmarks/BENCHMARKS.md`

. Full
per-question result files are committed under `benchmarks/results_*`

.

**LongMemEval — retrieval recall (R@5, 500 questions):**

| Mode | R@5 | LLM required |
|---|---|---|
| Raw (semantic search, no heuristics, no LLM) | 96.6% |
None |
| Hybrid v4, held-out 450q (tuned on 50 dev, not seen during training) | 98.4% |
None |
| Hybrid v4 + LLM rerank (full 500) | ≥99% | Any capable model |

The raw 96.6% requires no API key, no cloud, and no LLM at any stage. The hybrid pipeline adds keyword boosting, temporal-proximity boosting, and preference-pattern extraction; the held-out 98.4% is the honest generalisable figure.

The rerank pipeline promotes the best candidate out of the top-20
retrieved sessions using an LLM reader. It works with any reasonably
capable model — we have reproduced it with Claude Haiku, Claude Sonnet,
and minimax-m2.7 via Ollama Cloud (no Anthropic dependency). The gap
between raw and reranked is model-agnostic; we do not headline a "100%"
number because the last 0.6% was reached by inspecting specific wrong
answers, which `benchmarks/BENCHMARKS.md`

flags as teaching to the test.

**Other benchmarks (full results in benchmarks/BENCHMARKS.md):**

| Benchmark | Metric | Score | Notes |
|---|---|---|---|
| LoCoMo (session, top-10, no rerank) | R@10 | 60.3% | 1,986 questions |
| LoCoMo (hybrid v5, top-10, no rerank) | R@10 | 88.9% | Same set |
| ConvoMem (all categories, 250 items) | Avg recall | 92.9% | 50 per category |
| MemBench (ACL 2025, 8,500 items) | R@5 | 80.3% | All categories |

We deliberately do not include a side-by-side comparison against Mem0, Mastra, Hindsight, Supermemory, or Zep. Those projects publish different metrics on different splits, and placing retrieval recall next to end-to-end QA accuracy is not an honest comparison. See each project's own research page for their published numbers.

**Reproducing every result:**

```
git clone https://github.com/MemPalace/mempalace.git
cd mempalace
uv sync --extra dev # or: pip install -e ".[dev]"
# see benchmarks/README.md for dataset download commands
uv run python benchmarks/longmemeval_bench.py /path/to/longmemeval_s_cleaned.json
```

MemPalace includes a temporal entity-relationship graph with validity windows — add, query, invalidate, timeline — backed by local SQLite. Usage and tool reference: mempalaceofficial.com/concepts/knowledge-graph.

29 MCP tools cover palace reads/writes, knowledge-graph operations, cross-wing navigation, drawer management, and agent diaries. Installation and the full tool list: mempalaceofficial.com/reference/mcp-tools.

Each specialist agent gets its own wing and diary in the palace.
Discoverable at runtime via `mempalace_list_agents`

— no bloat in your
system prompt:
mempalaceofficial.com/concepts/agents.

Two Claude Code hooks save periodically and before context compression: mempalaceofficial.com/guide/hooks.

If you are installing under time pressure, start with the
Claude Code retention setup checklist:
wire the hooks, back up existing JSONL transcripts, and backfill them with
`mempalace mine ~/.claude/projects/ --mode convos`

.

For per-message recall on top of the file-level chunks the hooks produce,
run `mempalace sweep <transcript-dir>`

periodically — it stores one
verbatim drawer per user/assistant message, idempotent and resume-safe.

- Python 3.9+
- A vector-store backend (ChromaDB by default)
- ~300 MB disk for the embedding model. Onboarding (
`python -m mempalace.onboarding`

) offers`embeddinggemma-300m`

(multilingual, 100+ languages, recommended) or`all-MiniLM-L6-v2`

(English-only, ~30 MB). See the docstring at`mempalace/embedding.py`

for details and migration notes.

No API key is required for the core benchmark path.

- Getting started → mempalaceofficial.com/guide/getting-started
- CLI reference → mempalaceofficial.com/reference/cli
- Python API → mempalaceofficial.com/reference/python-api
- Full benchmark methodology → benchmarks/BENCHMARKS.md
- Release notes → CHANGELOG.md
- Corrections and public notices → docs/HISTORY.md

PRs welcome. See CONTRIBUTING.md.

MIT — see LICENSE.