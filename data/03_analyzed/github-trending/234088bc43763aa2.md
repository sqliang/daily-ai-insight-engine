---
title: AlexsJones/llmfit
source: https://github.com/AlexsJones/llmfit
author: []
published: ''
created: '2026-07-22'
manifest_dates:
- '2026-07-22'
description: 'Hundreds of models & providers. One command to find what runs on your
  hardware.llmfit English · 中文 · 日本語 📊 New: benchmark & share — real numbers from
  your machine, better estimates for everyone. Download a model, serve it, and measure
  real tok/s on your hardware — then contribute the results back to the project as
  a PR, straight from the TUI. No gh CLI, no third-party account. Every run is saved
  locally first, your own measurements replace estimates in the fit table, and each
  merged submission ships in the next release: anyone on identical hardware gets measured
  ✓ numbers before they ever run a benchmark. Follow the step-by-step benchmarking
  guide → Previously: llmfit 1.0 — the release where the numbers became verifiable
  → Hundreds of models & providers. One command to find what runs on your hardware.
  A terminal tool that right-sizes LLM models to your system''s RAM, CPU, and GPU.
  Detects your hardware, scores each model across quality, speed, fit, and context
  dimensions, and tells you which ones will actually run well on your machine. Ships
  with an interactive TUI (default) and a classic CLI mode. Supports multi-GPU setups,
  MoE architectures, dynamic quantization selection, speed estimation, and local runtime
  providers (Ollama, llama.cpp, MLX, Docker Model Runner, LM Studio). Sister projects:
  sympozium — managing agents in Kubernetes. llmserve — a simple TUI for serving local
  LLM models. Pick a model, pick a backend, serve it. llama-panel — a native macOS
  app for managing local llama-server instances. Documentation Get started Install
  · Usage · How it works Guides TUI guide · Benchmarking step-by-step · CLI & automation
  · Runtime providers · OpenClaw integration Reference How it works (full) · Platform
  & GPU support · Custom models · Development Project Contributing · Alternatives
  · Code signing · License Install Windows scoop install llmfit If Scoop is not installed,
  follow the Scoop installation guide. macOS / Linux Homebrew Prebuilt binary (recommended,
  works on all macOS/Linux versions): brew install AlexsJones/llmfit/llmfit Or from
  the homebrew-core formula, which builds from source on macOS versions without a
  bottle: brew install llmfit MacPorts port install llmfit Quick install curl -fsSL
  https://llmfit.axjns.dev/install.sh | sh Downloads the latest release binary from
  GitHub and installs it to /usr/local/bin (or ~/.local/bin if no sudo). Install to
  ~/.local/bin without sudo: curl -fsSL https://llmfit.axjns.dev/install.sh | sh -s
  -- --local uv / pip To install or update llmfit: uv tool install -U llmfit To run
  without installing: uvx llmfit You can also install llmfit as a Python package in
  the normal way with tools such as pip or uv. Docker / Podman docker run ghcr.io/alexsjones/llmfit
  This prints JSON from llmfit recommend command. The JSON could be further queried
  with jq. podman run ghcr.io/alexsjones/llmfit recommend --use-case coding | jq ''.models[].name''
  To launch the interactive TUI instead, pass the global --tui flag: docker run --rm
  -it ghcr.io/alexsjones/llmfit --tui From source git clone https://github.com/AlexsJones/llmfit.git
  cd llmfit cargo build --release # binary is at target/release/llmfit Usage llmfit
  # interactive TUI: your hardware, every model, ranked The TUI shows your detected
  specs at the top and every model scored for fit, speed, quality, and context. See
  the TUI guide for navigation, planning, simulation, downloads, the community leaderboard,
  and benchmarking. For scripts, agents, and classic terminal output: llmfit fit #
  table of all models ranked by fit llmfit recommend --json # top picks as JSON (agent/script
  consumption) llmfit info "<model>" # one model: fit analysis, estimate basis, verify
  commands llmfit bench # measure real tok/s/TTFT against your running provider llmfit
  doctor # hardware detection report for bug reports Full reference: CLI & automation.
  How it works llmfit detects your hardware (RAM, CPU, GPU/VRAM, backend), then scores
  every model in its catalog across four dimensions: memory fit, estimated speed,
  quality, and context. Speed estimates come from a memory-bandwidth model grounded
  in runtime sampling and real community measurements — and every estimate ships its
  inputs, so llmfit info shows exactly what a number assumes and how to verify it
  on your machine. Full detail, including the estimation formulas and the model database:
  How llmfit works. Contributing Contributions are welcome, especially new models.
  Before submitting a PR Please run cargo fmt before pushing your changes. Most CI
  check failures are caused by unformatted code: cargo fmt Guides for adding models
  — locally (no rebuild) or to the built-in catalog: Custom models. Alternatives If
  you''re looking for a different approach, check out llm-checker -- a Node.js CLI
  tool with Ollama integration that can pull and benchmark models directly. It takes
  a more hands-on approach by actually running models on your hardware via Ollama,
  rather than estimating from specs. Good if you already have Ollama installed and
  want to test real-world performance. Note that it doesn''t support MoE (Mixture-of-Experts)
  architectures -- all models are treated as dense, so memory estimates for models
  like Mixtral or DeepSeek-V3 will reflect total parameter count rather than the smaller
  active subset. Code signing llmfit''s Windows release binaries are digitally signed
  (Authenticode) via SignPath.io, with a free code signing certificate provided by
  the SignPath Foundation. Signing happens automatically in the release pipeline:
  only artifacts built by GitHub Actions from this repository are submitted for signing,
  and signing requests are approved by the project maintainer (@AlexsJones). Code
  signing policy: see the SignPath Foundation code signing policy and terms. Privacy:
  this program will not transfer any information to other networked systems unless
  specifically requested by the user or the person installing or operating it. llmfit
  only contacts external services when you explicitly use the corresponding feature
  (e.g. model downloads, runtime provider queries, or the community leaderboard).
  License MIT'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 234088bc43763aa2
source_type: community_discussion
tldr: llmfit 是一个终端工具，能根据用户硬件的 RAM、CPU 和 GPU 自动检测配置并评分推荐最适配的 LLM 模型，支持交互式 TUI 和 CLI
  模式，兼容多种本地运行框架，已发布 1.0 版本。
objective_summary: AlexsJones 发布了 llmfit 的基准测试与分享新功能，用户可在本地硬件上测量真实 tok/s 并通过 PR 将结果贡献回社区。该工具可检测用户系统的硬件配置，从适配度、速度、质量和上下文四个维度对数百个
  LLM 模型进行评分和推荐。llmfit 支持多 GPU、MoE 架构和动态量化选择，兼容 Ollama、llama.cpp、MLX、Docker Model
  Runner 和 LM Studio 等多种运行时框架，提供 Scoop、Homebrew、uv、Docker 等多种安装方式。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - LLM
  - MoE
  - Ollama
  - llama.cpp
  - MLX
  - LM Studio
  - Docker Model Runner
  key_people:
  - AlexsJones
key_logic_flow:
- llmfit 是一个终端工具，可根据用户硬件的 RAM、CPU 和 GPU 自动检测配置并评分推荐最适配的 LLM 模型。
- 该工具提供交互式 TUI 和命令行 CLI 两种模式，支持多 GPU 设置、MoE 架构和动态量化选择。
- llmfit 新增了基准测试与分享功能，用户可在本地硬件上测量真实 tok/s，然后将结果通过 PR 贡献回项目。
- 工具兼容 Ollama、llama.cpp、MLX、Docker Model Runner 和 LM Studio 等多种本地运行时框架。
- 该项目提供了 Scoop、Homebrew、MacPorts、一键安装脚本、uv 工具和 Docker 等多种安装方式。
- llmfit 的 Windows 版本通过 SignPath.io 进行了 Authenticode 数字签名认证，遵循 MIT 开源协议。
object_mentions:
- object_type: project
  name: llmfit
  canonical_name: AlexsJones/llmfit
  url: https://github.com/AlexsJones/llmfit
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - llmfit 可检测用户系统的 RAM、CPU 和 GPU 配置，从适配度、速度、质量和上下文四个维度对数百个 LLM 模型进行评分和推荐。
  - 该工具支持多 GPU 设置、MoE 架构和动态量化选择，兼容 Ollama、llama.cpp、MLX、Docker Model Runner 和 LM Studio
    等运行时框架。
  - llmfit 新增了基准测试与分享功能，用户可在本地硬件上测量真实 tok/s 并通过 PR 将结果贡献到社区。
  article_id: 234088bc43763aa2
- object_type: project
  name: sympozium
  canonical_name: sympozium
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - sympozium 是 llmfit 的姊妹项目之一，主要功能是在 Kubernetes 集群中管理 AI 智能体的部署和运行。
  article_id: 234088bc43763aa2
- object_type: project
  name: llmserve
  canonical_name: llmserve
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - llmserve 是一个为本地 LLM 模型提供服务的简单 TUI 工具，用户可选择模型和后端进行本地部署。
  article_id: 234088bc43763aa2
- object_type: product
  name: llama-panel
  canonical_name: llama-panel
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - llama-panel 是一个原生 macOS 桌面应用，用于管理本地 llama-server 运行实例。
  article_id: 234088bc43763aa2
- object_type: project
  name: llm-checker
  canonical_name: llm-checker
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - llm-checker 是一个带有 Ollama 集成的 Node.js CLI 工具，可拉取和基准测试模型以测试真实性能，但不支持 MoE 架构。
  article_id: 234088bc43763aa2
extract_result: success
impact_score:
  score: 5.0
  reason: llmfit 是一个高质量的本地 LLM 工具链项目，通过硬件自动检测和四维评分系统有效降低了开发者选择本地模型的试错成本。其社区基准测试共享机制能逐步积累真实性能数据，对本地模型部署生态有正向价值。但该项目属于开发者工具优化层面，并未改变
    LLM 底层技术范式、推理效率或行业竞争格局，影响力局限于本地模型选型效率提升的细分领域。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 一键检测硬件并自动推荐最适配的本地 LLM 模型，大幅降低模型选型的试错成本
hype_assessment:
  level: low
  reason: 项目文档务实，没有使用'颠覆'、'革命性'等 PR 话术。技术方案有明确的估算公式说明和可验证的基准测试机制，支持多运行时提供兼容性矩阵，Windows
    版本有 Authenticode 签名认证，所有功能均可实测验证。
information_entropy: high
domain_disruption:
  technical_innovation: 基于内存带宽模型的多维度 LLM 适配评分算法，结合社区众包的 tok/s 基准测试数据实现持续校准，是本地模型选择领域少见的工程化解决方案
  business_model: 无
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: llmfit 解决了本地 LLM 部署中的关键信息不对称——用户不清楚自己硬件能跑什么模型、跑多快。其社区基准测试共享功能（实测 tok/s →
    PR 贡献 → 下个版本内置）创造了轻度网络效应：贡献者越多，推荐精度越高。4 维评分体系（适配度/速度/质量/上下文）和跨运行时兼容（Ollama/llama.cpp/MLX/LM
    Studio）使其具备成为本地 LLM 选型基础设施的潜力。但风险也很明确：(1) MIT 开源协议下无商业变现路径，独立生存能力存疑；(2) 功能边界较窄，本质是一个终端工具，而非平台；(3)
    可被 Ollama 等运行时提供商快速内化吸收成为内置功能；(4) 硬件检测与模型评分逻辑的复现门槛低。长期复利取决于社区基准数据库能否突破临界规模形成数据壁垒，但作为独立商业项目天花板明显，更适合作为大生态中的功能模块存在。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- Ollama
- LM Studio
- llama.cpp
- Apple Silicon Mac 用户
competitive_casualty:
- 商业模型选型咨询/推荐平台
- 面向普通用户的高价 GPU 云推理服务（边际替代效应）
market_opportunities:
- 企业可基于 llmfit 的硬件检测与模型评分机制，构建内部 LLM 选型决策系统，降低 GPU 采购和模型部署的试错成本
- 社区众包的硬件-模型性能数据库（tok/s 实测数据）可衍生为面向 AI 基础设施规划的数据服务，帮助企业在采购前预测模型在特定硬件上的表现
- llmfit 的多框架兼容性（Ollama、llama.cpp、MLX、LM Studio）使其可作为本地 LLM 工具链的底层引擎，可集成到 AI 开发平台或
  MLOps 工作流中提供模型推荐能力
risk_matrix:
  regulatory: 无（MIT 开源协议，Windows 版本已通过 Authenticode 数字签名，合规性良好）
  technological: 工具依赖多个第三方本地运行时框架（Ollama、llama.cpp、MLX 等），若其中某个框架被淘汰或发生重大架构变更，可能影响推荐覆盖面和兼容性；模型评分算法的准确性依赖于社区贡献数据的质量和时效性
  competitive: 已存在 llm-checker 等竞品，且 Ollama 等运行时厂商可能将类似的硬件适配推荐功能内置于自身生态中，存在被平台原生功能替代的风险
  ethical: 用户贡献基准测试数据时硬件配置信息会被公开（通过 PR 合并到项目数据库），虽项目声明仅在使用者主动操作时才联网，但社区排行榜上的硬件信息聚合仍存在一定隐私暴露隐忧
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: llmfit
  canonical_name: AlexsJones/llmfit
  url: https://github.com/AlexsJones/llmfit
  positioning: llmfit 是一款终端工具，可根据用户硬件配置自动检测并评分，从数百个 LLM 模型中推荐最适配的本地运行方案。
  technical_signal: 使用 Rust 开发，通过内存带宽模型估算推理速度，支持多 GPU、MoE 架构和动态量化选择，并已新增基于真实 tok/s
    的基准测试功能。
  adoption_signal: 已发布 1.0 版本，通过 Homebrew、Scoop、Docker、uv 等多种渠道分发，Windows 版本获得 SignPath.io
    Authenticode 数字签名认证。
  ecosystem_relevance: 兼容 Ollama、llama.cpp、MLX、Docker Model Runner 和 LM Studio 等主流本地推理框架，填补了硬件配置与模型匹配之间的工具空白。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: llmfit 解决了本地 LLM 部署中硬件与模型匹配的核心痛点，其内置的社区基准测试共享机制有望积累大规模真实性能数据库，对个人开发者和企业本地部署均有重要参考价值。
  risk_notes:
  - 模型数据库和速度估算依赖社区贡献，初期模型覆盖度有限可能影响推荐准确度。
  - 本地推理框架生态变化较快，保持与所有运行时框架的兼容需要持续维护投入。
  score: 7.0
  article_ids:
  - 234088bc43763aa2
  evidence_snippets:
  - llmfit 可检测用户系统的 RAM、CPU 和 GPU 配置，从适配度、速度、质量和上下文四个维度对数百个 LLM 模型进行评分和推荐。
  - 该工具支持多 GPU 设置、MoE 架构和动态量化选择，兼容 Ollama、llama.cpp、MLX、Docker Model Runner 和 LM Studio
    等运行时框架。
  - llmfit 新增了基准测试与分享功能，用户可在本地硬件上测量真实 tok/s 并通过 PR 将结果贡献到社区。
- object_type: project
  name: sympozium
  canonical_name: sympozium
  url: null
  positioning: sympozium 专注于在 Kubernetes 集群中管理 AI 智能体的部署和运行，是 llmfit 开发者生态中的姊妹项目。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 作为 llmfit 的姊妹项目聚焦 K8s AI 智能体管理，与本地模型推荐和基准测试工具 llmfit 形成全链路互补。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 随着企业级 AI 部署向 Kubernetes 迁移，sympozium 在 AI 智能体编排管理方向的定位具有前瞻性，值得持续关注其进展。
  risk_notes:
  - 项目信息公开有限，仅知为 K8s AI 智能体管理工具，需更多资料评估其成熟度和社区活跃度。
  score: 3.0
  article_ids:
  - 234088bc43763aa2
  evidence_snippets:
  - sympozium 是 llmfit 的姊妹项目之一，主要功能是在 Kubernetes 集群中管理 AI 智能体的部署和运行。
- object_type: project
  name: llmserve
  canonical_name: llmserve
  url: null
  positioning: llmserve 是一个为本地 LLM 模型提供服务的简易 TUI 工具，用户可选择模型和后端进行本地部署和服务。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 作为 llmfit 生态中的姊妹项目，llmserve 聚焦本地模型的服务化部署环节，与模型推荐功能形成上下游衔接。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 本地 LLM 服务化是边缘部署的关键能力，llmserve 以简易 TUI 模式降低模型服务化门槛，适合快速原型验证场景。
  risk_notes:
  - 项目信息极为有限，仅知为一个简易 TUI 模型服务工具，缺乏功能深度和社区规模的公开数据。
  score: 2.0
  article_ids:
  - 234088bc43763aa2
  evidence_snippets:
  - llmserve 是一个为本地 LLM 模型提供服务的简单 TUI 工具，用户可选择模型和后端进行本地部署。
- object_type: product
  name: llama-panel
  canonical_name: llama-panel
  url: null
  positioning: llama-panel 是一款原生 macOS 桌面应用，用于管理本地 llama-server 运行实例，提供图形化操作界面。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - macOS 用户
  - 本地 LLM 部署开发者
  - llama-server 运维人员
  product_signal: 提供原生 macOS 桌面体验，专注于 llama-server 实例管理这一细分场景，通过 GUI 降低命令行操作复杂度。
  market_signal: null
  differentiation: 区别于纯命令行管理方式，llama-panel 以 macOS 原生桌面应用形态填补了本地 llama-server 可视化管理工具的空白。
  watch_reason: 随着本地 LLM 部署需求持续增长，llama-panel 在 macOS 平台提供了 llama-server 图形化管理入口，对
    Mac 开发者生态具有补充价值。
  risk_notes:
  - 仅限于 macOS 平台，受众面较窄且与 Apple Silicon 生态绑定。
  - 作为 llmfit 生态中的辅助工具，独立迭代和维护节奏存在不确定性。
  score: 2.0
  article_ids:
  - 234088bc43763aa2
  evidence_snippets:
  - llama-panel 是一个原生 macOS 桌面应用，用于管理本地 llama-server 运行实例。
- object_type: project
  name: llm-checker
  canonical_name: llm-checker
  url: null
  positioning: llm-checker 是一个集成 Ollama 的 Node.js CLI 工具，通过实际运行模型来测试真实性能以提供基准数据。
  technical_signal: 基于 Node.js 开发，深度集成 Ollama 运行时，通过实际运行模型而非理论估算的方式测量本地推理性能。
  adoption_signal: null
  ecosystem_relevance: 作为 llmfit 的替代方案，llm-checker 以实测方式评估模型性能，适合已部署 Ollama 的用户场景，但缺少
    MoE 架构支持。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: llm-checker 的实测方法对已有 Ollama 环境的用户更为直接，但与 llmfit 相比缺少 MoE 内存优化估算和社区基准共享机制。
  risk_notes:
  - 不支持 MoE 架构的内存优化估算，对 Mixtral、DeepSeek-V3 等 MoE 模型的推荐结果不准确。
  - 依赖用户已安装 Ollama 环境，使用门槛高于一站式硬件检测与推荐工具 llmfit。
  score: 2.0
  article_ids:
  - 234088bc43763aa2
  evidence_snippets:
  - llm-checker 是一个带有 Ollama 集成的 Node.js CLI 工具，可拉取和基准测试模型以测试真实性能，但不支持 MoE 架构。
---

📊 New: benchmark & share — real numbers from your machine, better estimates for everyone.Download a model, serve it, and measure real tok/s on your hardware — then contribute the results back to the project as a PR, straight from the TUI. No`gh`

CLI, no third-party account. Every run is saved locally first, your own measurements replace estimates in the fit table, and each merged submission ships in the next release: anyone on identical hardware gets measured`✓`

numbers before they ever run a benchmark. Follow the step-by-step benchmarking guide →

Previously: llmfit 1.0 — the release where the numbers became verifiable →

**Hundreds of models & providers. One command to find what runs on your hardware.**

A terminal tool that right-sizes LLM models to your system's RAM, CPU, and GPU. Detects your hardware, scores each model across quality, speed, fit, and context dimensions, and tells you which ones will actually run well on your machine.

Ships with an interactive TUI (default) and a classic CLI mode. Supports multi-GPU setups, MoE architectures, dynamic quantization selection, speed estimation, and local runtime providers (Ollama, llama.cpp, MLX, Docker Model Runner, LM Studio).


Sister projects:

- sympozium — managing agents in Kubernetes.
- llmserve — a simple TUI for serving local LLM models. Pick a model, pick a backend, serve it.
- llama-panel — a native macOS app for managing local llama-server instances.

Get started |
Install · Usage · How it works |
Guides |
TUI guide · Benchmarking step-by-step · CLI & automation · Runtime providers · OpenClaw integration |
Reference |
How it works (full) · Platform & GPU support · Custom models · Development |
Project |
Contributing · Alternatives · Code signing · License |

`scoop install llmfit`

If Scoop is not installed, follow the Scoop installation guide.

Prebuilt binary (recommended, works on all macOS/Linux versions):

`brew install AlexsJones/llmfit/llmfit`

Or from the homebrew-core formula, which builds from source on macOS versions without a bottle:

`brew install llmfit`

`port install llmfit`

`curl -fsSL https://llmfit.axjns.dev/install.sh | sh`

Downloads the latest release binary from GitHub and installs it to `/usr/local/bin`

(or `~/.local/bin`

if no sudo).

**Install to ~/.local/bin without sudo:**

`curl -fsSL https://llmfit.axjns.dev/install.sh | sh -s -- --local`

To install or update llmfit:

`uv tool install -U llmfit`

To run without installing:

`uvx llmfit`

You can also install llmfit as a Python package in the normal way with tools such as pip or uv.

`docker run ghcr.io/alexsjones/llmfit`

This prints JSON from `llmfit recommend`

command. The JSON could be further queried with `jq`

.

```
podman run ghcr.io/alexsjones/llmfit recommend --use-case coding | jq '.models[].name'
```


To launch the interactive TUI instead, pass the global `--tui`

flag:

`docker run --rm -it ghcr.io/alexsjones/llmfit --tui`

```
git clone https://github.com/AlexsJones/llmfit.git
cd llmfit
cargo build --release
# binary is at target/release/llmfit
```

`llmfit # interactive TUI: your hardware, every model, ranked`

The TUI shows your detected specs at the top and every model scored for fit, speed, quality, and context. See the TUI guide for navigation, planning, simulation, downloads, the community leaderboard, and benchmarking.

For scripts, agents, and classic terminal output:

```
llmfit fit # table of all models ranked by fit
llmfit recommend --json # top picks as JSON (agent/script consumption)
llmfit info "<model>" # one model: fit analysis, estimate basis, verify commands
llmfit bench # measure real tok/s/TTFT against your running provider
llmfit doctor # hardware detection report for bug reports
```

Full reference: CLI & automation.

llmfit detects your hardware (RAM, CPU, GPU/VRAM, backend), then scores every model in its catalog across four dimensions: memory fit, estimated speed, quality, and context. Speed estimates come from a memory-bandwidth model grounded in runtime sampling and real community measurements — and every estimate ships its inputs, so `llmfit info`

shows exactly what a number assumes and how to verify it on your machine.

Full detail, including the estimation formulas and the model database: How llmfit works.

Contributions are welcome, especially new models.

Please run `cargo fmt`

before pushing your changes. Most CI check failures are caused by unformatted code:

`cargo fmt`

Guides for adding models — locally (no rebuild) or to the built-in catalog: Custom models.

If you're looking for a different approach, check out llm-checker -- a Node.js CLI tool with Ollama integration that can pull and benchmark models directly. It takes a more hands-on approach by actually running models on your hardware via Ollama, rather than estimating from specs. Good if you already have Ollama installed and want to test real-world performance. Note that it doesn't support MoE (Mixture-of-Experts) architectures -- all models are treated as dense, so memory estimates for models like Mixtral or DeepSeek-V3 will reflect total parameter count rather than the smaller active subset.

llmfit's Windows release binaries are digitally signed (Authenticode) via SignPath.io, with a free code signing certificate provided by the SignPath Foundation.

Signing happens automatically in the release pipeline: only artifacts built by GitHub Actions from this repository are submitted for signing, and signing requests are approved by the project maintainer (@AlexsJones).

**Code signing policy:** see the SignPath Foundation code signing policy and terms.

**Privacy:** this program will not transfer any information to other networked systems unless specifically requested by the user or the person installing or operating it. llmfit only contacts external services when you explicitly use the corresponding feature (e.g. model downloads, runtime provider queries, or the community leaderboard).

MIT