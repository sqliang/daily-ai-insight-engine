---
title: github/copilot-sdk
source: https://github.com/github/copilot-sdk
author: []
published: ''
created: '2026-07-17'
manifest_dates:
- '2026-07-17'
- '2026-07-18'
description: 'Multi-platform SDK for integrating GitHub Copilot Agent into apps and
  servicesGitHub Copilot CLI SDKs Agents for every app. Embed Copilot''s agentic workflows
  in your application with the GitHub Copilot SDK for Python, TypeScript, Go, .NET,
  Java, and Rust. The GitHub Copilot SDK exposes the same engine behind Copilot CLI:
  a production-tested agent runtime you can invoke programmatically. No need to build
  your own orchestration—you define agent behavior, Copilot handles planning, tool
  invocation, file edits, and more. Available SDKs SDK Location Cookbook Installation
  API docs Node.js / TypeScript nodejs/ Cookbook npm install @github/copilot-sdk Python
  python/ Cookbook pip install github-copilot-sdk Go go/ Cookbook go get github.com/github/copilot-sdk/go
  API docs .NET dotnet/ Cookbook dotnet add package GitHub.Copilot.SDK Rust rust/
  — cargo add github-copilot-sdk API docs Java java/ Cookbook Maven coordinatescom.github:copilot-sdk-javaSee
  instructions for Maven and Gradle API docs See the individual SDK READMEs for installation,
  usage examples, and API reference. Getting Started For a complete walkthrough, see
  the Getting Started Guide. Quick steps: (Optional) Install the Copilot CLI For Node.js,
  Python, and .NET SDKs, the Copilot CLI is bundled automatically and no separate
  installation is required. For Go, Java, and Rust, install the CLI manually or ensure
  copilot is available in your PATH. Go and Rust also expose application-level CLI
  bundling features. Install your preferred SDK using the commands above. See the
  SDK README for usage examples and API documentation. Architecture All SDKs communicate
  with the Copilot CLI server via JSON-RPC: Your Application ↓ SDK Client ↓ JSON-RPC
  Copilot CLI (server mode) The SDK manages the CLI process lifecycle automatically.
  You can also connect to an external CLI server—see the Getting Started Guide for
  details on running the CLI in server mode. FAQ Do I need a GitHub Copilot subscription
  to use the SDK? Yes, a GitHub Copilot subscription is required to use the GitHub
  Copilot SDK, unless you are using BYOK (Bring Your Own Key). With BYOK, you can
  use the SDK without GitHub authentication by configuring your own API keys from
  supported LLM providers. For standard usage (non-BYOK), refer to the GitHub Copilot
  pricing page, which includes a free tier with limited usage. How does billing work
  for SDK usage? Billing for the GitHub Copilot SDK is based on the same model as
  the Copilot CLI, with each prompt being counted towards your usage allowance. For
  more information on Copilot usage billing, see Usage in GitHub Copilot. Does it
  support BYOK (Bring Your Own Key)? Yes, the GitHub Copilot SDK supports BYOK (Bring
  Your Own Key). You can configure the SDK to use your own API keys from supported
  LLM providers (e.g. OpenAI, Azure AI Foundry, Anthropic) to access models through
  those providers. See the BYOK documentation for setup instructions and examples.
  Note: BYOK uses key-based authentication only. Microsoft Entra ID (Azure AD), managed
  identities, and third-party identity providers are not supported. What authentication
  methods are supported? The SDK supports multiple authentication methods: GitHub
  signed-in user - Uses stored OAuth credentials from copilot CLI login OAuth GitHub
  App - Pass user tokens from your GitHub OAuth app Environment variables - COPILOT_GITHUB_TOKEN,
  GH_TOKEN, GITHUB_TOKEN BYOK - Use your own API keys (no GitHub auth required) See
  the Authentication documentation for details on each method. Do I need to install
  the Copilot CLI separately? No — for Node.js, Python, and .NET SDKs, the Copilot
  CLI is bundled automatically as a dependency. You do not need to install it separately.
  For Go, Java, and Rust SDKs, the CLI is not bundled by default. Install the CLI
  manually or ensure copilot is available in your PATH. Go and Rust also expose application-level
  CLI bundling features. Advanced: You can override the CLI binary or connect to an
  external server. See the individual SDK README for language-specific options. What
  tools are enabled by default? By default, the SDK exposes the Copilot CLI''s first-party
  tools, similar to running the CLI with --allow-all. Tool execution is still governed
  by each SDK''s permission handler, so applications can approve, deny, or customize
  tool calls. You can customize tool availability by configuring the SDK client options
  to enable and disable specific tools. Refer to the individual SDK documentation
  for details on tool configuration and to the Copilot CLI documentation for the list
  of available tools. Can I use custom agents, skills or tools? Yes, the GitHub Copilot
  SDK allows you to define custom agents, skills, and tools. You can extend the functionality
  of the agents by implementing your own logic and integrating additional tools as
  needed. Refer to the SDK documentation of your preferred language for more details.
  Are there instructions or SDK guidance for Copilot to speed up development? Yes,
  check out the custom instructions and SDK-specific guidance: Node.js / TypeScript
  Python .NET Go Rust (SDK guidance; custom instructions not yet published) Java What
  models are supported? All models available via Copilot CLI are supported in the
  SDK. The SDK also exposes a method which will return the models available so they
  can be accessed at runtime. Is the SDK production-ready? The GitHub Copilot SDK
  is generally available and follows semantic versioning. See CHANGELOG.md for release
  notes. How do I report issues or request features? Please use the GitHub Issues
  page to report bugs or request new features. We welcome your feedback to help improve
  the SDK. Quick Links Documentation – Full documentation index Getting Started –
  Tutorial to get up and running Setup Guides – Architecture, deployment, and scaling
  Authentication – GitHub OAuth, BYOK, and more Features – Hooks, custom agents, MCP,
  skills, and more Troubleshooting – Common issues and solutions Cookbook – Practical
  recipes for common tasks across all languages More Resources – Additional examples,
  tutorials, and community resources Unofficial, Community-maintained SDKs ⚠️ Disclaimer:
  These are unofficial, community-driven SDKs and they are not supported by GitHub.
  Use at your own risk. SDK Location Clojure copilot-community-sdk/copilot-sdk-clojure
  C++ 0xeb/copilot-sdk-cpp Contributing See CONTRIBUTING.md for contribution guidelines.
  License MIT'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 30c775fdbb4f52bd
source_type: community_discussion
tldr: GitHub 发布了多语言 Copilot SDK，覆盖 Python、TypeScript、Go、.NET、Java 和 Rust，允许开发者将 Copilot
  的智能代理工作流嵌入自己的应用，并支持 BYOK 自定义模型和多种认证方式。
objective_summary: GitHub 于 2026 年正式发布了 Copilot SDK（通用可用版本），提供 Python、TypeScript、Go、.NET、Java
  和 Rust 六种语言的客户端库。该 SDK 暴露了与 Copilot CLI 相同的生产级代理运行时引擎，通过 JSON-RPC 协议通信，自动管理 CLI
  进程生命周期，开发者只需定义代理行为，由 Copilot 处理规划、工具调用和文件编辑。SDK 支持 BYOK（自带密钥）模式，可使用 OpenAI、Azure
  AI Foundry 和 Anthropic 等提供商的 API 密钥，无需 GitHub 认证。此外还支持 GitHub OAuth、环境变量令牌等多种认证方式，并允许自定义代理、技能和工具。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - GitHub
  technologies:
  - JSON-RPC
  - BYOK
  - MCP
  - Copilot CLI
  key_people: []
key_logic_flow:
- GitHub 发布了多语言 Copilot SDK，覆盖 Python、TypeScript、Go、.NET、Java 和 Rust 六种语言，均遵循语义化版本控制。
- 该 SDK 暴露了与 Copilot CLI 相同的代理运行时引擎，开发者无需自行构建编排逻辑即可嵌入应用。
- SDK 通过 JSON-RPC 协议在应用与 Copilot CLI 服务器之间通信，并自动管理 CLI 进程生命周期。
- SDK 支持 BYOK（自带密钥）模式，允许开发者使用 OpenAI、Azure AI Foundry 和 Anthropic 等提供商的 API 密钥而无需
  GitHub 认证。
- SDK 支持多种认证方式，包括 GitHub 登录用户、OAuth GitHub App、环境变量和 BYOK 模式。
- 对于 Node.js、Python 和 .NET SDK，Copilot CLI 作为依赖自动捆绑，无需单独安装。
object_mentions:
- object_type: project
  name: github/copilot-sdk
  canonical_name: github/copilot-sdk
  url: https://github.com/github/copilot-sdk
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - GitHub 发布了多语言 Copilot SDK，覆盖 Python、TypeScript、Go、.NET、Java 和 Rust，允许开发者将 Copilot
    的智能代理工作流嵌入自己的应用。
  - 该 SDK 暴露了与 Copilot CLI 相同的生产级代理运行时引擎，通过 JSON-RPC 协议通信并自动管理 CLI 进程生命周期。
  - SDK 支持 BYOK 模式，可使用 OpenAI、Azure AI Foundry 和 Anthropic 等提供商的 API 密钥，无需 GitHub
    认证。
  article_id: 30c775fdbb4f52bd
- object_type: project
  name: copilot-community-sdk/copilot-sdk-clojure
  canonical_name: copilot-community-sdk/copilot-sdk-clojure
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 社区提供了 Clojure 语言的 Copilot SDK，对应仓库为 copilot-community-sdk/copilot-sdk-clojure。
  article_id: 30c775fdbb4f52bd
- object_type: project
  name: 0xeb/copilot-sdk-cpp
  canonical_name: 0xeb/copilot-sdk-cpp
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 社区提供了 C++ 语言的 Copilot SDK，对应仓库为 0xeb/copilot-sdk-cpp。
  article_id: 30c775fdbb4f52bd
extract_result: success
impact_score:
  score: 7.0
  reason: GitHub 正式发布多语言 Copilot SDK（GA），将 Copilot CLI 的生产级代理运行时引擎以 SDK 形式开放给开发者，覆盖
    Python、TypeScript、Go、.NET、Java、Rust 六种语言。这不是范式转移级别的事件（因为 Copilot CLI 已存在，SDK 是程序化封装而非全新能力），但也不是小更新。核心冲击力在于三点：一是
    BYOK（自带密钥）模式允许使用 OpenAI、Anthropic、Azure 等 API 密钥而无需 GitHub 订阅，极大地降低了准入门槛并减少厂商锁定顾虑；二是多语言全栈覆盖让后端和非
    JS 生态也能原生集成 Copilot 代理工作流；三是支持自定义代理、技能和工具，使 Copilot SDK 从封闭工具演变为可嵌入的应用平台。这将改变
    AI 编码代理领域的竞争格局，对 Cursor、Windsurf 等竞品构成直接挑战，也为 GitHub 构建更大的 AI 开发者生态奠定基础。综合量级约
    7.0 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: BYOK 模式允许零 GitHub 订阅使用 Copilot 代理运行时，以及多语言 SDK 全栈覆盖
hype_assessment:
  level: low
  reason: 这是 GitHub 官方仓库的 README 文档，行文客观务实，逐条说明语言支持、安装方式、协议细节、认证选项、BYOK 限制条件（如不支持
    Entra ID）和 CLI 依赖关系。没有使用'颠覆性'、'革命性'等 PR 宣传词汇，甚至主动列出了当前不支持的功能边界（如 BYOK 仅支持密钥认证，不支持
    Azure AD/托管身份）。信息信噪比高，属于实打实的产品发布说明。
information_entropy: high
domain_disruption:
  technical_innovation: 将生产级代理运行时引擎（Copilot CLI 的内核）标准化为 JSON-RPC 协议的多语言 SDK 抽象层，开发者只需定义代理行为，SDK
    自动管理进程生命周期、规划、工具调用和文件编辑。BYOK 模式从根本上解耦了代理运行时与模型提供商的选择，允许在同一个 SDK 框架下自由切换 OpenAI、Anthropic
    或 Azure 的模型，这对代理框架的工程架构设计具有示范意义。
  business_model: BYOK 模式打破了 GitHub 订阅绑定，使 Copilot SDK 可以作为独立代理平台被任何应用嵌入——即使应用使用非
    GitHub 模型提供商。这实质上是将 Copilot 从'IDE 增值功能'升级为'代理即服务平台'，通过生态扩张（自定义技能/工具/MCP）来构建开发者网络效应，而非传统
    SaaS 订阅锁定。对 Cursor、Windsurf 等竞品构成平台级挤压。
engineering_complexity: production_ready
compound_value:
  score: 7.8
  reason: GitHub Copilot SDK 具备显著的长期复利效应。核心逻辑有三层：第一，它是生产级代理运行时引擎的开放，将 Copilot CLI
    背后经过大规模验证的规划-工具调用-文件编辑能力标准化为 SDK，开发者无需自建编排，这降低了高质量 Agent 应用的构建门槛，同时将 Copilot 从终端产品扩展为底层基础设施。第二，BYOK
    模式是关键差异点——允许使用 OpenAI、Azure、Anthropic 的 API 密钥而无须 GitHub 认证，等于用一个开放接口吸引全生态的开发者流量，形成
    '代理运行时即标准' 的网络效应。第三，覆盖 6 种主流语言（Python/TS/Go/.NET/Java/Rust），一旦开发者基于此 SDK 构建应用，切换到竞品的成本较高，形成锁定效应。但需注意：Agent
    中间件赛道竞争激烈（LangChain、Vercel AI SDK、Semantic Kernel 等），且 Copilot SDK 本质依赖底层模型能力，若模型层出现范式级变化（如从
    tool-calling 转向全新交互范式），当前架构的复利可能被削弱。扣分项：生态尚在早期，社区 SDK 只有 Clojure 和 C++ 两个第三方贡献，网络效应尚未完全启动。综合评分
    7.8，具备成为 Agent 基础设施核心层的潜力，但需持续观察生态发展和竞争格局。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- GitHub
- Microsoft (Azure AI Foundry)
- OpenAI
- Anthropic
- Copilot 生态开发者
competitive_casualty:
- LangChain
- Vercel AI SDK
- Semantic Kernel
- CrewAI
- AutoGen
- 中小型 Agent 编排框架初创公司
market_opportunities:
- 企业可基于 Copilot SDK 快速搭建垂直行业 AI 代理，如代码审查助手、自动化运维代理、文档生成工具，大幅降低从零构建编排层的成本
- BYOK 模式允许创业公司利用现有 OpenAI/Anthropic API 额度接入 Copilot 级代理引擎，无需 GitHub 订阅，降低了 AI 代理开发的门槛
- 多语言 SDK（Python/TS/Go/.NET/Java/Rust）覆盖主流企业技术栈，系统集成商可为不同语言客户提供一致的 AI 代理嵌入方案
risk_matrix:
  regulatory: BYOK 模式下企业自行管理 API 密钥和模型调用，需自行确保符合 GDPR、中国生成式 AI 管理办法等数据保护法规；GitHub
    的日志和遥测数据跨境传输可能引发合规审查
  technological: SDK 深度依赖 Copilot CLI 运行时和 JSON-RPC 协议，若 GitHub 变更协议或弃用旧版本，已构建的应用可能面临兼容性断裂风险
  competitive: 面临 LangChain、AutoGen、CrewAI 等开源代理框架的竞争生态挤压；微软/OpenAI 与 Anthropic 之间的模型供应商博弈可能影响
    BYOK 模式下的模型可用性
  ethical: 开发者可构建自主决策的 AI 代理，若缺乏权限约束和安全审计，可能导致代码仓库越权操作、数据泄露或供应链投毒等伦理风险
  additional: []
confidence:
  impact: high
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: github/copilot-sdk
  canonical_name: github/copilot-sdk
  url: https://github.com/github/copilot-sdk
  positioning: GitHub 官方发布的多语言 Copilot SDK，将 Copilot CLI 的生产级代理运行时引擎封装为可编程接口，使开发者能将智能代理工作流嵌入自有应用。
  technical_signal: 提供 Python、TypeScript、Go、.NET、Java 和 Rust 六种语言 SDK，均遵循语义化版本控制，通过
    JSON-RPC 协议与 Copilot CLI 服务器通信。
  adoption_signal: Node.js、Python 和 .NET 版本自动捆绑 Copilot CLI 为依赖，无需单独安装，显著降低了开发者的集成与部署门槛。
  ecosystem_relevance: 深度绑定 GitHub Copilot 生态，同时 BYOK 模式兼容 OpenAI、Azure AI Foundry
    和 Anthropic 等主流模型提供商，具备跨平台连接能力。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: GitHub 将 Copilot CLI 的生产级代理引擎以 SDK 形式开放，标志着 Copilot 从命令行工具向可嵌入平台能力演进。开发者可基于此构建自定义代理应用，这是
    AI 编程助手平台化战略的关键一步，对开发工具链生态格局有深远影响。
  risk_notes:
  - 标准使用模式需要 GitHub Copilot 订阅，对未订阅 Copilot 的独立开发者和中小团队构成一定的采用门槛。
  - SDK 的自定义代理、技能和工具等扩展生态尚处早期阶段，其成熟度和稳定性有待持续验证。
  score: 7.0
  article_ids:
  - 30c775fdbb4f52bd
  evidence_snippets:
  - GitHub 发布了多语言 Copilot SDK，覆盖 Python、TypeScript、Go、.NET、Java 和 Rust，允许开发者将 Copilot
    的智能代理工作流嵌入自己的应用。
  - 该 SDK 暴露了与 Copilot CLI 相同的生产级代理运行时引擎，通过 JSON-RPC 协议通信并自动管理 CLI 进程生命周期。
  - SDK 支持 BYOK 模式，可使用 OpenAI、Azure AI Foundry 和 Anthropic 等提供商的 API 密钥，无需 GitHub
    认证。
---

Agents for every app.

Embed Copilot's agentic workflows in your application with the GitHub Copilot SDK for Python, TypeScript, Go, .NET, Java, and Rust.

The GitHub Copilot SDK exposes the same engine behind Copilot CLI: a production-tested agent runtime you can invoke programmatically. No need to build your own orchestration—you define agent behavior, Copilot handles planning, tool invocation, file edits, and more.

| SDK | Location | Cookbook | Installation | API docs |
|---|---|---|---|---|
Node.js / TypeScript |
`nodejs/` |
Cookbook | `npm install @github/copilot-sdk` |
|
Python |
`python/` |
Cookbook | `pip install github-copilot-sdk` |
|
Go |
`go/` |
Cookbook | `go get github.com/github/copilot-sdk/go` |
API docs |
.NET |
`dotnet/` |
Cookbook | `dotnet add package GitHub.Copilot.SDK` |
|
Rust |
`rust/` |
— | `cargo add github-copilot-sdk` |
API docs |
Java |
`java/` |
Cookbook | Maven coordinates`com.github:copilot-sdk-java` See instructions for Maven and Gradle |
API docs |

See the individual SDK READMEs for installation, usage examples, and API reference.

For a complete walkthrough, see the **Getting Started Guide**.

Quick steps:

**(Optional) Install the Copilot CLI**

For Node.js, Python, and .NET SDKs, the Copilot CLI is bundled automatically and no separate installation is required.
For Go, Java, and Rust, install the CLI manually or ensure `copilot`

is available in your PATH. Go and Rust also expose application-level CLI bundling features.

-
**Install your preferred SDK**using the commands above. -
**See the SDK README**for usage examples and API documentation.

All SDKs communicate with the Copilot CLI server via JSON-RPC:

```
Your Application
↓
SDK Client
↓ JSON-RPC
Copilot CLI (server mode)
```


The SDK manages the CLI process lifecycle automatically. You can also connect to an external CLI server—see the Getting Started Guide for details on running the CLI in server mode.

Yes, a GitHub Copilot subscription is required to use the GitHub Copilot SDK, **unless you are using BYOK (Bring Your Own Key)**. With BYOK, you can use the SDK without GitHub authentication by configuring your own API keys from supported LLM providers. For standard usage (non-BYOK), refer to the GitHub Copilot pricing page, which includes a free tier with limited usage.

Billing for the GitHub Copilot SDK is based on the same model as the Copilot CLI, with each prompt being counted towards your usage allowance. For more information on Copilot usage billing, see Usage in GitHub Copilot.

Yes, the GitHub Copilot SDK supports BYOK (Bring Your Own Key). You can configure the SDK to use your own API keys from supported LLM providers (e.g. OpenAI, Azure AI Foundry, Anthropic) to access models through those providers. See the **BYOK documentation** for setup instructions and examples.

**Note:** BYOK uses key-based authentication only. Microsoft Entra ID (Azure AD), managed identities, and third-party identity providers are not supported.

The SDK supports multiple authentication methods:

**GitHub signed-in user**- Uses stored OAuth credentials from`copilot`

CLI login**OAuth GitHub App**- Pass user tokens from your GitHub OAuth app**Environment variables**-`COPILOT_GITHUB_TOKEN`

,`GH_TOKEN`

,`GITHUB_TOKEN`

**BYOK**- Use your own API keys (no GitHub auth required)

See the **Authentication documentation** for details on each method.

No — for Node.js, Python, and .NET SDKs, the Copilot CLI is bundled automatically as a dependency. You do not need to install it separately.

For Go, Java, and Rust SDKs, the CLI is **not** bundled by default. Install the CLI manually or ensure `copilot`

is available in your PATH. Go and Rust also expose application-level CLI bundling features.

Advanced: You can override the CLI binary or connect to an external server. See the individual SDK README for language-specific options.

By default, the SDK exposes the Copilot CLI's first-party tools, similar to running the CLI with `--allow-all`

. Tool execution is still governed by each SDK's permission handler, so applications can approve, deny, or customize tool calls. You can customize tool availability by configuring the SDK client options to enable and disable specific tools. Refer to the individual SDK documentation for details on tool configuration and to the Copilot CLI documentation for the list of available tools.

Yes, the GitHub Copilot SDK allows you to define custom agents, skills, and tools. You can extend the functionality of the agents by implementing your own logic and integrating additional tools as needed. Refer to the SDK documentation of your preferred language for more details.

Yes, check out the custom instructions and SDK-specific guidance:

All models available via Copilot CLI are supported in the SDK. The SDK also exposes a method which will return the models available so they can be accessed at runtime.

The GitHub Copilot SDK is generally available and follows semantic versioning. See CHANGELOG.md for release notes.

Please use the GitHub Issues page to report bugs or request new features. We welcome your feedback to help improve the SDK.

**Documentation**– Full documentation index**Getting Started**– Tutorial to get up and running**Setup Guides**– Architecture, deployment, and scaling**Authentication**– GitHub OAuth, BYOK, and more**Features**– Hooks, custom agents, MCP, skills, and more**Troubleshooting**– Common issues and solutions**Cookbook**– Practical recipes for common tasks across all languages**More Resources**– Additional examples, tutorials, and community resources

| SDK | Location |
|---|---|
Clojure |
copilot-community-sdk/copilot-sdk-clojure |
C++ |
0xeb/copilot-sdk-cpp |

See CONTRIBUTING.md for contribution guidelines.

MIT