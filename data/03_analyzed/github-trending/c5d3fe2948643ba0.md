---
title: google-labs-code/design.md
source: https://github.com/google-labs-code/design.md
author: []
published: ''
created: '2026-06-26'
description: 'A format specification for describing a visual identity to coding agents.
  DESIGN.md gives agents a persistent, structured understanding of a design system.DESIGN.md
  A format specification for describing a visual identity to coding agents. DESIGN.md
  gives agents a persistent, structured understanding of a design system. The Format
  A DESIGN.md file combines machine-readable design tokens (YAML front matter) with
  human-readable design rationale (markdown prose). Tokens give agents exact values.
  Prose tells them why those values exist and how to apply them. --- name: Heritage
  colors: primary: "#1A1C1E" secondary: "#6C7278" tertiary: "#B8422E" neutral: "#F7F5F2"
  typography: h1: fontFamily: Public Sans fontSize: 3rem body-md: fontFamily: Public
  Sans fontSize: 1rem label-caps: fontFamily: Space Grotesk fontSize: 0.75rem rounded:
  sm: 4px md: 8px spacing: sm: 8px md: 16px --- ## Overview Architectural Minimalism
  meets Journalistic Gravitas. The UI evokes a premium matte finish — a high-end broadsheet
  or contemporary gallery. ## Colors The palette is rooted in high-contrast neutrals
  and a single accent color. - **Primary (#1A1C1E):** Deep ink for headlines and core
  text. - **Secondary (#6C7278):** Sophisticated slate for borders, captions, metadata.
  - **Tertiary (#B8422E):** "Boston Clay" — the sole driver for interaction. - **Neutral
  (#F7F5F2):** Warm limestone foundation, softer than pure white. An agent that reads
  this file will produce a UI with deep ink headlines in Public Sans, a warm limestone
  background, and Boston Clay call-to-action buttons. Getting Started Validate a DESIGN.md
  against the spec, catch broken token references, check WCAG contrast ratios, and
  surface structural findings — all as structured JSON that agents can act on. npx
  @google/design.md lint DESIGN.md { "findings": [ { "severity": "warning", "path":
  "components.button-primary", "message": "textColor (#ffffff) on backgroundColor
  (#1A1C1E) has contrast ratio 15.42:1 — passes WCAG AA." } ], "summary": { "errors":
  0, "warnings": 1, "info": 1 } } Compare two versions of a design system to detect
  token-level and prose regressions: npx @google/design.md diff DESIGN.md DESIGN-v2.md
  { "tokens": { "colors": { "added": ["accent"], "removed": [], "modified": ["tertiary"]
  }, "typography": { "added": [], "removed": [], "modified": [] } }, "regression":
  false } The Specification The full DESIGN.md spec lives at docs/spec.md. What follows
  is a condensed reference. File Structure A DESIGN.md file has two layers: YAML front
  matter — Machine-readable design tokens, delimited by --- fences at the top of the
  file. Markdown body — Human-readable design rationale organized into ## sections.
  The tokens are the normative values. The prose provides context for how to apply
  them. Token Schema version: <string> # optional, current: "alpha" name: <string>
  description: <string> # optional colors: <token-name>: <Color> typography: <token-name>:
  <Typography> rounded: <scale-level>: <Dimension> spacing: <scale-level>: <Dimension
  | number> components: <component-name>: <token-name>: <string | token reference>
  Token Types Type Format Example Color Any CSS color (hex, rgb(), oklch(), named,
  etc.) "#1A1C1E", "oklch(62% 0.18 250)" Dimension number + unit (px, em, rem) 48px,
  -0.02em Token Reference {path.to.token} {colors.primary} Typography object with
  fontFamily, fontSize, fontWeight, lineHeight, letterSpacing, fontFeature, fontVariation
  See example above Section Order Sections use ## headings. They can be omitted, but
  those present must appear in this order: # Section Aliases 1 Overview Brand & Style
  2 Colors 3 Typography 4 Layout Layout & Spacing 5 Elevation & Depth Elevation 6
  Shapes 7 Components 8 Do''s and Don''ts Component Tokens Components map a name to
  a group of sub-token properties: components: button-primary: backgroundColor: "{colors.tertiary}"
  textColor: "{colors.on-tertiary}" rounded: "{rounded.sm}" padding: 12px button-primary-hover:
  backgroundColor: "{colors.tertiary-container}" Valid component properties: backgroundColor,
  textColor, typography, rounded, padding, size, height, width. Variants (hover, active,
  pressed) are expressed as separate component entries with a related key name. Consumer
  Behavior for Unknown Content Scenario Behavior Unknown section heading Preserve;
  do not error Unknown color token name Accept if value is valid Unknown typography
  token name Accept as valid typography Unknown component property Accept with warning
  Duplicate section heading Error; reject the file CLI Reference Installation npm
  install @google/design.md On Windows, quote the package name if your shell treats
  @ specially (PowerShell, some terminals): npm install "@google/design.md" Or run
  directly (always resolves from the public npm registry): npx @google/design.md lint
  DESIGN.md On Windows/PowerShell, this direct form can produce no output (or open
  DESIGN.md in your Markdown editor) because the .md suffix in the design.md bin name
  collides with the Windows Markdown file association during command resolution. Run
  the dot-free designmd alias instead — point npx at the package with -p, then invoke
  designmd: npx -p @google/design.md designmd lint DESIGN.md The designmd shim resolves
  to the same entrypoint and works identically across all platforms. npm error ENOVERSIONS
  (“No versions available for @google/design.md”) The CLI is published as @google/design.md
  on npm. ENOVERSIONS almost always means npm is not querying the public registry
  (custom registry= in .npmrc, a corporate mirror that has not synced this package,
  or a misconfigured @google:registry for the @google scope). Check your effective
  registry: npm config get registry For a normal install from the internet it should
  be https://registry.npmjs.org/. After fixing config, retry with npm cache clean
  --force if a stale 404 was cached. All commands accept a file path or - for stdin.
  Output defaults to JSON. Windows tip: when invoking the CLI directly from a package.json
  script (rather than through npx), use the designmd alias instead of design.md. The
  .md suffix in the original bin name confuses Windows command resolution with the
  file association for Markdown files. The designmd shim resolves to the same entrypoint
  and works identically across all platforms. // package.json { "scripts": { "design:lint":
  "designmd lint DESIGN.md" } } lint Validate a DESIGN.md file for structural correctness.
  npx @google/design.md lint DESIGN.md npx @google/design.md lint --format json DESIGN.md
  cat DESIGN.md | npx @google/design.md lint - Option Type Default Description file
  positional required Path to DESIGN.md (or - for stdin) --format json json Output
  format Exit code 1 if errors are found, 0 otherwise. diff Compare two DESIGN.md
  files and report token-level changes. npx @google/design.md diff DESIGN.md DESIGN-v2.md
  Option Type Default Description before positional required Path to the "before"
  DESIGN.md after positional required Path to the "after" DESIGN.md --format json
  json Output format Exit code 1 if regressions are detected (more errors or warnings
  in the "after" file). export Export DESIGN.md tokens to other formats. npx @google/design.md
  export --format json-tailwind DESIGN.md > tailwind.theme.json npx @google/design.md
  export --format css-tailwind DESIGN.md > theme.css npx @google/design.md export
  --format dtcg DESIGN.md > tokens.json Option Type Default Description file positional
  required Path to DESIGN.md (or - for stdin) --format json-tailwind | css-tailwind
  | tailwind | dtcg required Output format Format Output Description json-tailwind
  JSON Tailwind v3 theme.extend config object css-tailwind CSS Tailwind v4 @theme
  { ... } block with CSS custom properties tailwind JSON Alias for json-tailwind dtcg
  JSON W3C Design Tokens Format Module spec Output the DESIGN.md format specification
  (useful for injecting spec context into agent prompts). npx @google/design.md spec
  npx @google/design.md spec --rules npx @google/design.md spec --rules-only --format
  json Option Type Default Description --rules boolean false Append the active linting
  rules table --rules-only boolean false Output only the linting rules table --format
  markdown | json markdown Output format Linting Rules The linter runs nine rules
  against a parsed DESIGN.md. Each rule produces findings at a fixed severity level.
  Rule Severity What it checks broken-ref error Token references ({colors.primary})
  that don''t resolve to any defined token missing-primary warning Colors are defined
  but no primary color exists — agents will auto-generate one contrast-ratio warning
  Component backgroundColor/textColor pairs below WCAG AA minimum (4.5:1) orphaned-tokens
  warning Color tokens defined but never referenced by any component token-summary
  info Summary of how many tokens are defined in each section missing-sections info
  Optional sections (spacing, rounded) absent when other tokens exist missing-typography
  warning Colors are defined but no typography tokens exist — agents will use default
  fonts section-order warning Sections appear out of the canonical order defined by
  the spec unknown-key warning A top-level YAML key looks like a typo of a known schema
  key (e.g. colours: → colors:); custom extension keys stay silent Programmatic API
  The linter is also available as a library: import { lint } from ''@google/design.md/linter'';
  const report = lint(markdownString); console.log(report.findings); // Finding[]
  console.log(report.summary); // { errors, warnings, info } console.log(report.designSystem);
  // Parsed DesignSystemState Design Token Interoperability DESIGN.md tokens are inspired
  by the W3C Design Token Format. The export command converts tokens to other formats:
  Tailwind v3 config (JSON) — npx @google/design.md export --format json-tailwind
  DESIGN.md — emits a theme.extend JSON object for tailwind.config.js. --format tailwind
  is a backwards-compatible alias. Tailwind v4 theme (CSS) — npx @google/design.md
  export --format css-tailwind DESIGN.md — emits a CSS @theme { ... } block using
  Tailwind v4''s CSS-variable token namespaces (--color-*, --font-*, --text-*, --leading-*,
  --tracking-*, --font-weight-*, --radius-*, --spacing-*). DTCG tokens.json (W3C Design
  Tokens Format Module) — npx @google/design.md export --format dtcg DESIGN.md Status
  The DESIGN.md format is at version alpha. The spec, token schema, and CLI are under
  active development. Expect changes to the format as it matures. Disclaimer This
  project is not eligible for the Google Open Source Software Vulnerability Rewards
  Program.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: c5d3fe2948643ba0
source_type: community_discussion
tldr: Google 发布了 DESIGN.md 格式规范及其 CLI 工具 @google/design.md，将设计令牌与设计原理说明合并为单个文件，支持
  lint、diff 和导出为 Tailwind 及 W3C DTCG 格式。
objective_summary: Google 通过其 google-labs-code 仓库发布了 DESIGN.md 格式规范，这是一种将 YAML 前置元数据定义的设计令牌与
  Markdown 正文描述的设计原理相结合的文件格式。Google 同时发布了 @google/design.md CLI 工具，支持对 DESIGN.md 文件执行
  lint（结构验证与 WCAG 对比度检查）、diff（版本间令牌级别对比）和 export（导出为 Tailwind v3 JSON、Tailwind v4
  CSS 和 W3C DTCG 格式）操作。该规范目前处于 alpha 阶段，定义了颜色、排版、圆角、间距和组件共五类设计令牌以及八个可选章节的规范顺序。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Google
  technologies:
  - DESIGN.md
  - Tailwind CSS
  - W3C Design Tokens
  - WCAG
  key_people: []
key_logic_flow:
- Google 发布了 DESIGN.md 格式规范，将机器可读的设计令牌（YAML 前置元数据）与人类可读的设计原理说明（Markdown 正文）合并为单个文件。
- Google 同时发布了 @google/design.md CLI 工具，支持 lint（结构验证）、diff（版本对比）和 export（格式导出）三种核心命令。
- DESIGN.md 定义了颜色、排版、圆角、间距和组件共五类设计令牌的标准结构，以及八个可选章节的规范顺序。
- lint 工具内置九条规则，涵盖令牌引用断裂检查、缺少主色警告、WCAG AA 对比度验证、孤立令牌检测和章节顺序合规性检查。
- export 命令支持将设计令牌转换为 Tailwind v3 JSON 配置、Tailwind v4 CSS @theme 块和 W3C DTCG 标准设计令牌格式。
- DESIGN.md 目前为 alpha 版本，规范、令牌模式和 CLI 仍在积极开发中，且该项目不参与 Google 开源软件漏洞奖励计划。
specialized_tags:
  github:
    projectName: google-labs-code/design.md
    projectUrl: https://github.com/google-labs-code/design.md
    primaryLanguage: TypeScript
    licenseType: null
    domain: ai_ml
    crossTags:
    - developer-tools
    - cli-tool
    - open-source
    aiDetail:
      primaryCategories:
      - agent_framework
      - prompt_engineering
      agentSubcategory:
      - general_framework
      techTags:
      - design-tokens
extract_result: success
object_mentions:
- object_type: project
  name: '@google/design.md'
  canonical_name: '@google/design.md'
  url: https://github.com/google-labs-code/design.md
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Google 发布了 @google/design.md 命令行工具，用于对 DESIGN.md 文件执行 lint、diff 和 export 操作。
  - 该工具支持将设计令牌导出为 Tailwind v3 JSON 配置、Tailwind v4 CSS @theme 块和 W3C DTCG 标准设计令牌格式。
  - lint 功能内置九条规则，可检测令牌引用断裂、WCAG AA 对比度违规和章节顺序错误等问题。
  article_id: c5d3fe2948643ba0
- object_type: project
  name: DESIGN.md
  canonical_name: DESIGN.md
  url: https://github.com/google-labs-code/design.md
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - DESIGN.md 是一种将机器可读的设计令牌（YAML 前置元数据）与人类可读的设计原理（Markdown 正文）相结合的格式规范。
  - 该规范定义了颜色、排版、圆角、间距和组件共五类设计令牌的标准结构以及八个可选章节的规范顺序。
  - 令牌值支持 CSS 颜色格式、带单位的尺寸、令牌引用表达式和排版对象等多种数据类型。
  article_id: c5d3fe2948643ba0
impact_score:
  score: 5.5
  reason: Google Labs 的 DESIGN.md 规格定位于'AI 编码代理的设计系统接口'这一细分且新兴的方向，概念上有价值——将设计令牌与人类可读的理念说明集成在单一文件中，并配套了
    lint、diff、export 等实用性 CLI 工具。但该项目存在三个制约因素：其一是 alpha 阶段，平台兼容性（Windows .md 后缀冲突）尚未完善；其二是设计令牌标准化领域已有
    W3C DTCG（Design Token Community Group）在推进，DESIGN.md 属于生态中的后来者；其三是仅覆盖视觉标识（color/typography/spacing）而缺少交互状态、动效、响应式断点等核心设计维度，能力边界有限。短期行业冲击力属于局部工具改进级别，不足以改变竞争格局。
sentiment: positive
developer_sentiment:
  tone: neutral
  primary_focus: 设计系统与 AI 代理之间的结构化接口标准能否被广泛采纳，以及是否会与现有 DTCG/Tailwind 生态产生碎片化
hype_assessment:
  level: low
  reason: 文章采用技术规格文档风格，未出现'颠覆''革命性'等 PR 滥用词汇。项目明确标注 alpha 版本，并坦诚列出了 Windows 平台因 .md
    后缀导致命令解析冲突的具体问题和 workaround。整体叙事务实而克制，几乎没有水分。
information_entropy: high
domain_disruption:
  technical_innovation: 提出 YAML 前端数据（机器可读设计令牌）+ Markdown 正文（人类可读设计理念）的双层文件结构，为 AI
    编码代理提供结构化的设计系统理解入口。配套 lint（含 WCAG 对比度检查）、diff（令牌级差异比对）、export（Tailwind v3/v4 及
    DTCG 格式互转）四个 CLI 命令，降低了设计系统与 AI 代理之间的集成门槛。
  business_model: 开源工具，当前无直接商业模式。但从 Google 战略角度看，该规范可能推动 Material Design 生态的 AI 友好化，并为
    Google 在 AI 辅助前端开发工具链中建立标准话语权。长期看，若被广泛采纳，可能通过生态锁定增强 Google Cloud/Vertex AI 对前端开发工作流的吸引力。
engineering_complexity: prototype
compound_value:
  score: 7.5
  reason: DESIGN.md 精准定位了 AI 编码代理时代的关键瓶颈：AI 能写代码，但缺乏对设计系统的结构化理解。该规范将设计系统拆解为两层——YAML
    前端数据提供机器可读的设计令牌，Markdown 正文提供人类和 AI 共同可读的设计理念与应用上下文，天然适配 LLM 上下文窗口。配套的 lint/diff/export/spec
    CLI 工具链完整，且内置 WCAG 对比度检查、Tailwind v3/v4 及 DTCG 格式导出，降低了从现有生态迁移的摩擦。从复利效应看：一旦 DESIGN.md
    成为 AI 编码代理读取设计系统的事实标准，将产生强网络效应——更多设计系统采用 → AI 输出质量更高 → 吸引更多团队接入，形成正向飞轮。但该项目仍处于
    alpha 阶段，且 Google 对非核心基础设施级项目的长期维护投入存疑（历史上 Google 弃置大量开发者工具），同时面临 W3C DTCG 标准、Tailwind
    配置等既有方案的竞争。结论：有潜力成为 AI 编码时代的"设计系统中间层"基础设施，复利效应中等偏强，但 adoption 速度和 Google 的持续投入是关键验证节点。
value_capture_layer: agent_middleware
moat_impact: creates_new_moat
key_beneficiaries:
- Google
- Cursor
- GitHub Copilot
- Vercel
- Figma
competitive_casualty:
- Zeplin
- Avocode
- 传统设计交付与标注工具
- 纯 DTCG 工具链
market_opportunities:
- 设计系统团队可基于 DESIGN.md 建立 AI 友好的设计令牌仓库，实现从 Figma 设计稿到 AI 编码代理的无缝设计系统传递，大幅减少前端实现中的样式偏差
- 前端工具链开发者可围绕 DESIGN.md 生态构建插件，如 Figma 插件一键导出 DESIGN.md、VS Code 实时预览令牌变更、CI/CD 中集成
  lint 检查设计一致性
- Tailwind 用户可直接利用 export 命令将 DESIGN.md 令牌转换为 Tailwind v4 CSS 变量块，节省手动维护主题配置的人力成本，适合中大型设计系统团队采纳
risk_matrix:
  regulatory: 无
  technological: 该格式当前为 alpha 版本，令牌规范可能在未来发生 breaking changes；若 W3C DTCG 标准正式成为行业规范，DESIGN.md
    的专有扩展字段可能面临兼容性压力；.md 后缀在 Windows 平台存在命令行冲突，跨平台开发体验存在缺陷
  competitive: Google 的品牌背书和 npm 发布可能加速该格式在 Google 生态内的采用，但开源社区可能涌现竞争性方案（如基于 DTCG
    纯 JSON 的工具链），导致生态分裂；若各大云厂商（AWS、Microsoft）推出自家设计系统描述格式，开发者面临选择成本
  ethical: lint 工具内置 WCAG 对比度检查有助于推动无障碍设计实践，这是积极的社会影响；但设计令牌的标准化可能隐性地限制创意探索，过度依赖代理自动应用令牌可能导致千篇一律的界面风格
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: '@google/design.md'
  canonical_name: '@google/design.md'
  url: https://github.com/google-labs-code/design.md
  positioning: Google 发布的 DESIGN.md 文件专用 CLI 工具，支持 lint、diff 和 export 三种核心命令，帮助开发者和
    AI 代理验证、对比和转换设计系统定义。
  technical_signal: CLI 工具内置九条 lint 规则，可检测令牌引用断裂、WCAG AA 对比度违规和章节顺序错误，并支持将设计令牌导出为
    Tailwind v3 JSON、Tailwind v4 CSS 和 W3C DTCG 标准格式。
  adoption_signal: 该项目以 npm 包形式发布，可通过 npx 直接运行，但当前为 alpha 版本且暴露了 Windows 平台 .md 后缀与系统文件关联冲突的问题。
  ecosystem_relevance: 填补了设计令牌文件标准化工具链的空白，直接对接 Tailwind CSS 和 W3C DTCG 两大主流设计系统生态，具备显著的互操作价值。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 作为 Google 官方推出的设计系统验证与转换工具，@google/design.md 直接服务于 AI 编码代理对设计令牌的结构化理解需求，其
    alpha 阶段的演进方向——包括规则扩展、生态集成深度和跨平台兼容性——值得持续跟踪。
  risk_notes:
  - 该工具目前为 alpha 版本，规范和功能仍在积极开发中，向后兼容性尚未承诺。
  - Windows 平台存在 .md 命令名与 Markdown 文件关联冲突的问题，需通过 designmd 别名解决，影响开箱即用体验。
  score: 7.0
  article_ids:
  - c5d3fe2948643ba0
  evidence_snippets:
  - Google 发布了 @google/design.md 命令行工具，用于对 DESIGN.md 文件执行 lint、diff 和 export 操作。
  - 该工具支持将设计令牌导出为 Tailwind v3 JSON 配置、Tailwind v4 CSS @theme 块和 W3C DTCG 标准设计令牌格式。
  - lint 功能内置九条规则，可检测令牌引用断裂、WCAG AA 对比度违规和章节顺序错误等问题。
- object_type: project
  name: DESIGN.md
  canonical_name: DESIGN.md
  url: https://github.com/google-labs-code/design.md
  positioning: Google 定义的一种将机器可读设计令牌（YAML 前置元数据）与人类可读设计原理（Markdown 正文）合并为单一文件的格式规范，旨在为编码
    AI 代理提供对设计系统的持久结构化理解。
  technical_signal: 规范定义了颜色、排版、圆角、间距和组件共五类设计令牌的标准结构以及八个可选章节的规范顺序，令牌值支持 CSS 颜色格式、带单位的尺寸、令牌引用表达式和排版对象等多种数据类型。
  adoption_signal: 该规范目前处于 alpha 阶段，作为 Google 实验室项目发布，尚未有公开的社区采用案例或企业落地证据。
  ecosystem_relevance: 为 AI 编码代理提供了标准化的设计系统接口，直接与 Tailwind CSS 和 W3C DTCG 生态互操作，有望成为
    AI 辅助 UI 开发领域的基础规范。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: DESIGN.md 为 AI 代理提供了一种标准化的设计系统理解方式，可能从根本上改变 AI 编码工具与设计系统之间的协作模式，作为
    Google 推动的开放规范具有显著的平台级影响力潜力，值得持续跟踪其社区采纳节奏和工具生态发展。
  risk_notes:
  - 该项目不参与 Google 开源软件漏洞奖励计划，长期安全维护策略尚不明确。
  - 规范目前为 alpha 版本，令牌模式和章节结构可能在正式发布前发生重大变更。
  score: 8.0
  article_ids:
  - c5d3fe2948643ba0
  evidence_snippets:
  - DESIGN.md 是一种将机器可读的设计令牌（YAML 前置元数据）与人类可读的设计原理（Markdown 正文）相结合的格式规范。
  - 该规范定义了颜色、排版、圆角、间距和组件共五类设计令牌的标准结构以及八个可选章节的规范顺序。
  - 令牌值支持 CSS 颜色格式、带单位的尺寸、令牌引用表达式和排版对象等多种数据类型。
---

A format specification for describing a visual identity to coding agents. DESIGN.md gives agents a persistent, structured understanding of a design system.

A DESIGN.md file combines machine-readable design tokens (YAML front matter) with human-readable design rationale (markdown prose). Tokens give agents exact values. Prose tells them *why* those values exist and how to apply them.

```
---
name: Heritage
colors:
primary: "#1A1C1E"
secondary: "#6C7278"
tertiary: "#B8422E"
neutral: "#F7F5F2"
typography:
h1:
fontFamily: Public Sans
fontSize: 3rem
body-md:
fontFamily: Public Sans
fontSize: 1rem
label-caps:
fontFamily: Space Grotesk
fontSize: 0.75rem
rounded:
sm: 4px
md: 8px
spacing:
sm: 8px
md: 16px
---
## Overview
Architectural Minimalism meets Journalistic Gravitas. The UI evokes a
premium matte finish — a high-end broadsheet or contemporary gallery.
## Colors
The palette is rooted in high-contrast neutrals and a single accent color.
- **Primary (#1A1C1E):** Deep ink for headlines and core text.
- **Secondary (#6C7278):** Sophisticated slate for borders, captions, metadata.
- **Tertiary (#B8422E):** "Boston Clay" — the sole driver for interaction.
- **Neutral (#F7F5F2):** Warm limestone foundation, softer than pure white.
```

An agent that reads this file will produce a UI with deep ink headlines in Public Sans, a warm limestone background, and Boston Clay call-to-action buttons.

Validate a DESIGN.md against the spec, catch broken token references, check WCAG contrast ratios, and surface structural findings — all as structured JSON that agents can act on.

`npx @google/design.md lint DESIGN.md`

```
{
"findings": [
{
"severity": "warning",
"path": "components.button-primary",
"message": "textColor (#ffffff) on backgroundColor (#1A1C1E) has contrast ratio 15.42:1 — passes WCAG AA."
}
],
"summary": { "errors": 0, "warnings": 1, "info": 1 }
}
```

Compare two versions of a design system to detect token-level and prose regressions:

`npx @google/design.md diff DESIGN.md DESIGN-v2.md`

```
{
"tokens": {
"colors": { "added": ["accent"], "removed": [], "modified": ["tertiary"] },
"typography": { "added": [], "removed": [], "modified": [] }
},
"regression": false
}
```

The full DESIGN.md spec lives at `docs/spec.md`

. What follows is a condensed reference.

A DESIGN.md file has two layers:

**YAML front matter**— Machine-readable design tokens, delimited by`---`

fences at the top of the file.**Markdown body**— Human-readable design rationale organized into`##`

sections.

The tokens are the normative values. The prose provides context for how to apply them.

```
version: <string> # optional, current: "alpha"
name: <string>
description: <string> # optional
colors:
<token-name>: <Color>
typography:
<token-name>: <Typography>
rounded:
<scale-level>: <Dimension>
spacing:
<scale-level>: <Dimension | number>
components:
<component-name>:
<token-name>: <string | token reference>
```

| Type | Format | Example |
|---|---|---|
| Color | Any CSS color (hex, `rgb()` , `oklch()` , named, etc.) |
`"#1A1C1E"` , `"oklch(62% 0.18 250)"` |
| Dimension | number + unit (`px` , `em` , `rem` ) |
`48px` , `-0.02em` |
| Token Reference | `{path.to.token}` |
`{colors.primary}` |
| Typography | object with `fontFamily` , `fontSize` , `fontWeight` , `lineHeight` , `letterSpacing` , `fontFeature` , `fontVariation` |
See example above |

Sections use `##`

headings. They can be omitted, but those present must appear in this order:

| # | Section | Aliases |
|---|---|---|
| 1 | Overview | Brand & Style |
| 2 | Colors | |
| 3 | Typography | |
| 4 | Layout | Layout & Spacing |
| 5 | Elevation & Depth | Elevation |
| 6 | Shapes | |
| 7 | Components | |
| 8 | Do's and Don'ts |

Components map a name to a group of sub-token properties:

```
components:
button-primary:
backgroundColor: "{colors.tertiary}"
textColor: "{colors.on-tertiary}"
rounded: "{rounded.sm}"
padding: 12px
button-primary-hover:
backgroundColor: "{colors.tertiary-container}"
```

Valid component properties: `backgroundColor`

, `textColor`

, `typography`

, `rounded`

, `padding`

, `size`

, `height`

, `width`

.

Variants (hover, active, pressed) are expressed as separate component entries with a related key name.

| Scenario | Behavior |
|---|---|
| Unknown section heading | Preserve; do not error |
| Unknown color token name | Accept if value is valid |
| Unknown typography token name | Accept as valid typography |
| Unknown component property | Accept with warning |
| Duplicate section heading | Error; reject the file |

`npm install @google/design.md`

On **Windows**, quote the package name if your shell treats `@`

specially (PowerShell, some terminals):

`npm install "@google/design.md"`

Or run directly (always resolves from the public npm registry):

`npx @google/design.md lint DESIGN.md`

On **Windows/PowerShell**, this direct form can produce no output (or open
`DESIGN.md`

in your Markdown editor) because the `.md`

suffix in the `design.md`

bin name collides with the Windows Markdown file association during command
resolution. Run the dot-free `designmd`

alias instead — point `npx`

at the
package with `-p`

, then invoke `designmd`

:

`npx -p @google/design.md designmd lint DESIGN.md`

The `designmd`

shim resolves to the same entrypoint and works identically across
all platforms.

The CLI is published as `@google/design.md`

on npm. `ENOVERSIONS`

almost always means npm is not querying the public registry (custom `registry=`

in `.npmrc`

, a corporate mirror that has not synced this package, or a misconfigured `@google:registry`

for the `@google`

scope).

Check your effective registry:

`npm config get registry`

For a normal install from the internet it should be `https://registry.npmjs.org/`

. After fixing config, retry with `npm cache clean --force`

if a stale 404 was cached.

All commands accept a file path or `-`

for stdin. Output defaults to JSON.


Windows tip: when invoking the CLI directly from a`package.json`

script (rather than through`npx`

), use the`designmd`

alias instead of`design.md`

. The`.md`

suffix in the original bin name confuses Windows command resolution with the file association for Markdown files. The`designmd`

shim resolves to the same entrypoint and works identically across all platforms.// package.json { "scripts": { "design:lint": "designmd lint DESIGN.md" } }

Validate a DESIGN.md file for structural correctness.

```
npx @google/design.md lint DESIGN.md
npx @google/design.md lint --format json DESIGN.md
cat DESIGN.md | npx @google/design.md lint -
```

| Option | Type | Default | Description |
|---|---|---|---|
`file` |
positional | required | Path to DESIGN.md (or `-` for stdin) |
`--format` |
`json` |
`json` |
Output format |

Exit code `1`

if errors are found, `0`

otherwise.

Compare two DESIGN.md files and report token-level changes.

`npx @google/design.md diff DESIGN.md DESIGN-v2.md`

| Option | Type | Default | Description |
|---|---|---|---|
`before` |
positional | required | Path to the "before" DESIGN.md |
`after` |
positional | required | Path to the "after" DESIGN.md |
`--format` |
`json` |
`json` |
Output format |

Exit code `1`

if regressions are detected (more errors or warnings in the "after" file).

Export DESIGN.md tokens to other formats.

```
npx @google/design.md export --format json-tailwind DESIGN.md > tailwind.theme.json
npx @google/design.md export --format css-tailwind DESIGN.md > theme.css
npx @google/design.md export --format dtcg DESIGN.md > tokens.json
```

| Option | Type | Default | Description |
|---|---|---|---|
`file` |
positional | required | Path to DESIGN.md (or `-` for stdin) |
`--format` |
`json-tailwind` | `css-tailwind` | `tailwind` | `dtcg` |
required | Output format |

| Format | Output | Description |
|---|---|---|
`json-tailwind` |
JSON | Tailwind v3 `theme.extend` config object |
`css-tailwind` |
CSS | Tailwind v4 `@theme { ... }` block with CSS custom properties |
`tailwind` |
JSON | Alias for `json-tailwind` |
`dtcg` |
JSON | W3C Design Tokens Format Module |

Exit code `0`

on a successful export (regardless of any lint findings in the source — run `lint`

to gate on those), `1`

on an invalid `--format`

or an emitter error, and `2`

if the input file cannot be read.

Output the DESIGN.md format specification (useful for injecting spec context into agent prompts).

```
npx @google/design.md spec
npx @google/design.md spec --rules
npx @google/design.md spec --rules-only --format json
```

| Option | Type | Default | Description |
|---|---|---|---|
`--rules` |
boolean | `false` |
Append the active linting rules table |
`--rules-only` |
boolean | `false` |
Output only the linting rules table |
`--format` |
`markdown` | `json` |
`markdown` |
Output format |

The linter runs nine rules against a parsed DESIGN.md. Each rule produces findings at a fixed severity level.

| Rule | Severity | What it checks |
|---|---|---|
`broken-ref` |
error | Token references (`{colors.primary}` ) that don't resolve to any defined token |
`missing-primary` |
warning | Colors are defined but no `primary` color exists — agents will auto-generate one |
`contrast-ratio` |
warning | Component `backgroundColor` /`textColor` pairs below WCAG AA minimum (4.5:1) |
`orphaned-tokens` |
warning | Color tokens defined but never referenced by any component |
`token-summary` |
info | Summary of how many tokens are defined in each section |
`missing-sections` |
info | Optional sections (spacing, rounded) absent when other tokens exist |
`missing-typography` |
warning | Colors are defined but no typography tokens exist — agents will use default fonts |
`section-order` |
warning | Sections appear out of the canonical order defined by the spec |
`unknown-key` |
warning | A top-level YAML key looks like a typo of a known schema key (e.g. `colours:` → `colors:` ); custom extension keys stay silent |

The linter is also available as a library:

```
import { lint } from '@google/design.md/linter';
const report = lint(markdownString);
console.log(report.findings); // Finding[]
console.log(report.summary); // { errors, warnings, info }
console.log(report.designSystem); // Parsed DesignSystemState
```

DESIGN.md tokens are inspired by the W3C Design Token Format. The `export`

command converts tokens to other formats:

**Tailwind v3 config (JSON)**—`npx @google/design.md export --format json-tailwind DESIGN.md`

— emits a`theme.extend`

JSON object for`tailwind.config.js`

.`--format tailwind`

is a backwards-compatible alias.**Tailwind v4 theme (CSS)**—`npx @google/design.md export --format css-tailwind DESIGN.md`

— emits a CSS`@theme { ... }`

block using Tailwind v4's CSS-variable token namespaces (`--color-*`

,`--font-*`

,`--text-*`

,`--leading-*`

,`--tracking-*`

,`--font-weight-*`

,`--radius-*`

,`--spacing-*`

).**DTCG tokens.json**(W3C Design Tokens Format Module) —`npx @google/design.md export --format dtcg DESIGN.md`


The DESIGN.md format is at version `alpha`

. The spec, token schema, and CLI are under active development. Expect changes to the format as it matures.

This project is not eligible for the Google Open Source Software Vulnerability Rewards Program.