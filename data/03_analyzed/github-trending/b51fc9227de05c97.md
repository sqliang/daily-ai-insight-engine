---
title: denoland/celld
source: https://github.com/denoland/celld
author: []
published: ''
created: '2026-08-08'
manifest_dates:
- '2026-08-08'
- '2026-08-09'
description: 'self-hosted, distributed Durable Objectscelld Self-hosted, distributed
  Durable Objects. celld is an open-source daemon that runs Cloudflare Workers and
  Durable Objects on your own machines. Each object is its own SQLite database, addressed
  by name and replicated to an S3-compatible bucket you own; nodes coordinate through
  that bucket alone, with no control plane or consensus. Because every object is its
  own small database, applications shard by construction — the contention and blast-radius
  failures of one shared database are designed out, not managed. Idle cells hibernate
  to nearly nothing. Learn more at celld.dev or read the documentation. How it works
  Every celld node embeds V8 and executes Wrangler bundles. The fleet shares an S3-compatible
  bucket containing deployments, cell state, and small ownership records. Object-storage
  compare-and-swap ensures that exactly one node owns a cell at a time, without a
  membership protocol, failure detector, or consensus service. celld continuously
  replicates each cell''s SQLite database to the bucket. When a cell moves or wakes
  up, its new owner restores that database and resumes execution. The bucket is the
  durable source of truth; nodes are replaceable. Install The installer downloads
  the celld binary (provenance is verifiable with gh attestation verify): curl -fsSL
  https://celld.dev/install.sh | sh Put ~/.local/bin on your PATH if the installer
  asks you to. Worker projects deployed with celld deploy need esbuild on PATH; asset-only
  projects do not. The installer keeps verified releases under ~/.local/lib/celld/releases
  and atomically switches one current pointer. To remove celld, use the guarded uninstaller:
  curl -fsSL https://celld.dev/uninstall.sh | sh Container The release image contains
  the celld binary and is published for Linux x86-64 and ARM64: docker run --rm ghcr.io/denoland/celld
  --version Persist the runtime''s local state and pass the standard AWS credential
  environment through: docker volume create celld-state docker run --rm --network
  host \ -e AWS_ACCESS_KEY_ID \ -e AWS_SECRET_ACCESS_KEY \ -e AWS_SESSION_TOKEN \
  -e CELLD_WATCH=/var/lib/celld/state \ -v celld-state:/var/lib/celld \ ghcr.io/denoland/celld
  \ --bucket s3://my-cells-bucket \ --endpoint https://ACCOUNT.r2.cloudflarestorage.com
  \ --region auto \ --listen 0.0.0.0:8080 \ --advertise node-a.internal:8080 Drop
  --endpoint/--region for real AWS S3. Behind a load balancer, give each node a distinct
  --advertise its peers can reach. Run it celld uses the standard AWS credential chain.
  Deploy to an S3-compatible bucket, then start celld against the same bucket: celld
  deploy . \ --bucket s3://my-cells-bucket celld \ --bucket s3://my-cells-bucket \
  --listen 0.0.0.0:8080 \ --advertise 10.0.0.12:8080 Use --endpoint for another S3-compatible
  service and --region when it cannot be inferred. A fleet runs one application, and
  every node loads its latest successfully committed deployment from deploy/current.json.
  Run celld --help for the complete command line. Deployment objects use the documented
  types in crates/celld/protocol.rs. celld deploy invokes esbuild from PATH for Worker
  code, accepts the supported Wrangler config subset—including co-deployed or asset-only
  static assets—and writes those objects directly. Every node discovers owners and
  peers from bucket leases; there is no account or join service. Peer HTTP does not
  terminate TLS. Put every advertised address on a trusted private network or an encrypted
  overlay such as WireGuard or Tailscale; do not publish the peer port directly. A
  literal public IP is rejected unless --unsafe-public-advertise is supplied explicitly.
  The first current node creates fleet/peer-auth.json in the bucket. All peer requests
  are protocol-versioned, body-bound, HMAC-authenticated, clock-bounded, and replay-protected
  with that fleet secret. Treat access to the bucket and its credentials as fleet
  administrator access. Operate a fleet celld diagnose enumerates every node lease
  by default, then performs a signed direct probe of each live peer: celld diagnose
  --bucket s3://my-cells-bucket The report keeps checking after an individual failure
  and distinguishes expired records, malformed or unsafe advertise addresses, unreachable
  peers, and incompatible protocols. It also prints each node''s coarse resident-cell,
  WebSocket, RSS, CPU, file-descriptor, pressure, and shedding sample. Pass one or
  more --peer NODE_ID options to restrict the check. Pressure shedding is opt-in while
  the first release''s safe defaults are being measured. Set a resident-cell high
  and low watermark on loaded nodes: CELLD_MAX_RESIDENT_CELLS=1000 \ CELLD_RESIDENT_LOW_WATER=800
  \ celld --bucket s3://my-cells-bucket --listen 0.0.0.0:8080 \ --advertise node-a.internal:8080
  On Linux, CELLD_MAX_RSS_MB and CELLD_MAX_CPU_PERCENT add process-memory and CPU
  triggers; the resident-cell watermark is portable. Under pressure, celld durably
  replicates and fences least-recently used idle cells, publishes them as unowned
  without resetting their epoch, and refuses to reacquire new unowned cells until
  the low watermark is reached. A spare receives no assignment: it acquires released
  cells through the same bucket protocol when normal traffic reaches it. Cells with
  active work or live host WebSockets are not shed. Build from source cargo build
  --locked cargo test --locked cargo clippy --all-targets --locked -- -D warnings
  The workspace builds the celld runtime. Its versioned object-storage protocol lives
  in crates/celld/protocol.rs. Small Wrangler projects under examples/ exercise the
  supported Worker and Durable Object surface. The runtime and compatibility surface
  are still evolving. Public tests cover the standalone engine smoke path; conformance
  against the Workers and Durable Objects reference behavior, and a deterministic
  simulation of the distributed protocol under fault injection, run before each release.
  Contributions Pull requests are disabled. Coding agents make it too easy to send
  a large, low-context change that costs maintainers more time than it saves. Thoughtful
  contributions are welcome; please understand the code, keep the patch focused, and
  respect the review time you are asking for. Send a git format-patch attachment to
  ry@deno.com. Contributor License Agreement: By emailing a patch, you certify that
  you have the right to submit it and assign to Deno Land Inc. all rights in the patch
  that you can assign. Where a right cannot be assigned, you grant Deno Land Inc.
  a perpetual, irrevocable, worldwide, royalty-free, transferable, sublicensable license
  to use, modify, combine, relicense, redistribute, or publish the patch, in whole
  or in part, with or without attribution. License Apache-2.0 See the limitations
  and security pages before operating a public fleet.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b51fc9227de05c97
source_type: community_discussion
tldr: Deno Land 发布开源守护进程 celld，让用户在自己机器上自托管运行 Cloudflare Workers 与 Durable Objects。每个对象是独立
  SQLite 数据库并复制到 S3 兼容桶，节点仅靠桶协调，无需控制平面或共识服务。
objective_summary: celld 是 Deno Land Inc. 开源的自托管分布式 Durable Objects 守护进程，支持在用户自有机器上运行
  Cloudflare Workers 与 Durable Objects。每个对象作为独立 SQLite 数据库按名称寻址，通过对象存储比较交换复制到用户拥有的
  S3 兼容存储桶，节点间无需控制平面或共识协议。项目提供可验证来源的安装脚本与 Docker 镜像，支持 celld deploy 部署和 celld diagnose
  诊断，并实现了可选的资源压力淘汰机制。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Deno Land Inc.
  - Cloudflare
  technologies:
  - Durable Objects
  - Cloudflare Workers
  - SQLite
  - S3
  - V8
  - Wrangler
  - esbuild
  - HMAC
  key_people:
  - Ryan Dahl
key_logic_flow:
- Deno Land 发布开源守护进程 celld，使用户能在自有机器上自托管运行 Cloudflare Workers 与 Durable Objects。
- 每个对象对应一个独立 SQLite 数据库，按名称寻址并持续复制到用户拥有的 S3 兼容存储桶，节点仅通过该桶协调。
- celld 用对象存储比较交换机制保证同一时刻只有一个节点拥有某个 cell，不需要成员协议、故障检测器或共识服务。
- celld 提供安装脚本、Docker 镜像与 celld deploy 命令，Worker 项目部署依赖 esbuild，并支持纯静态资源项目。
- celld diagnose 命令会枚举节点租约并对每个存活对等节点做签名直连探测，区分过期记录与各类异常。
- 压力淘汰为可选功能，celld 会持久复制并围栏最久未使用的空闲 cell，且不会淘汰有活跃工作或 WebSocket 的 cell。
object_mentions:
- object_type: project
  name: denoland/celld
  canonical_name: denoland/celld
  url: https://github.com/denoland/celld
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - celld 是 Deno Land 推出的开源守护进程，可在用户自有机器上运行 Cloudflare Workers 和 Durable Objects。
  - celld 的每个对象都是独立 SQLite 数据库，按名称寻址并复制到用户拥有的 S3 兼容存储桶，节点仅靠该桶协调。
  - celld 通过安装脚本与 Docker 镜像提供发布版本，并支持用 celld deploy 命令部署 Worker 项目与静态资源。
  article_id: b51fc9227de05c97
- object_type: product
  name: Cloudflare Workers
  canonical_name: Cloudflare Workers
  url: https://workers.cloudflare.com/
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - celld 内嵌 V8 并执行 Wrangler bundles，把 Cloudflare Workers 的运行时兼容面带到自托管环境。
  article_id: b51fc9227de05c97
- object_type: product
  name: Durable Objects
  canonical_name: Cloudflare Durable Objects
  url: https://developers.cloudflare.com/durable-objects/
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - celld 将 Cloudflare Durable Objects 实现为独立 SQLite 数据库，每个对象按名称寻址并复制到 S3 兼容存储桶。
  article_id: b51fc9227de05c97
extract_result: success
impact_score:
  score: 7.0
  reason: 评分依据：Deno Land 以开源方式落地了自托管的分布式 Durable Objects 守护进程，直接冲击 Cloudflare Workers/Durable
    Objects 的'平台锁定'叙事，为有状态边缘计算提供了一种无控制平面、无共识服务的分布式架构范本。该项目来自头部 JS 运行时厂商、含安装脚本/Docker/诊断工具等完整运维配套，属于能改变
    serverless 局部竞争格局的重要开源基础设施发布。但它尚未达到范式转移级别——Durable Objects 并非全新概念，且文档明确声明运行时与兼容面仍处演化期、压力淘汰的安全默认值还在实测中，短期采用面有限。综合评定为
    7.0 分。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 自托管 Durable Objects，摆脱 Cloudflare 平台锁定
hype_assessment:
  level: low
  reason: 判定依据：正文几乎全为可验证的工程细节——每个对象独立 SQLite、S3 比较交换协调、HMAC 鉴权与重放防护、租约枚举与直连探测、压力淘汰水位线、确定性故障注入模拟测试，且给出协议文件路径与构建命令，信息密度极高，未出现'颠覆''革命'等
    PR 滥用词汇。仅有的'designed out, not managed'略带宣传色彩，但整体属于实打实的干货，水分低。
information_entropy: high
domain_disruption:
  technical_innovation: 核心突破在于以对象存储比较交换（CAS）作为唯一协调原语，设计出无需成员协议、故障检测器或共识服务的分布式 Durable
    Objects 架构——每个对象即一个按名寻址的独立 SQLite 数据库，天然按对象分片，将共享数据库的争用与爆炸半径通过架构设计消除而非治理；空闲 cell
    休眠到近乎零开销，节点可替换、桶为持久事实源，这是对分布式有状态计算控制面的一次实质性简化。
  business_model: 开源自托管路线直接冲击 Cloudflare Durable Objects 的厂商锁定，为边缘有状态计算开辟'自带 S3 兼容桶即可运行
    Workers'的混合部署路径，显著降低 Durable Objects 工作负载的迁移成本；同时为 Deno Land 构建从开源运行时到 Deno Deploy
    托管服务的商业转化漏斗，并可能倒逼 Cloudflare 强化本地开发兼容层或调整平台策略。
engineering_complexity: production_ready
compound_value:
  score: 7.0
  reason: 投资逻辑链推演：第一步，celld 把 Cloudflare 已验证但闭源的 Durable Objects 编程模型开源为可自托管的守护进程，而
    Workers/Durable Objects 这套编程心智已被全球开发者大规模教育过，采用与迁移成本被大幅压低，这是'把巨头的验证资产变成开放基础设施'的典型路径。第二步，架构设计本身具备强复利基因——每个对象一个独立
    SQLite 数据库、通过 S3 对象存储比较交换完成协调、无控制平面与共识服务，意味着分布式状态管理被简化成对象存储原子操作，天然契合 AI Agent
    时代的持久化需求（每个 agent 会话/记忆状态可映射为一个 durable object），一旦成为事实标准将沉淀为 3-5 年后的运行时基座。第三步，Deno
    Land 有明确的战略意图：以开源 Runtime 拉新开发者、围绕 Workers 兼容层建立生态护城河，为其商业产品导流。但需扣分项：celld 自身明确标注运行时与兼容面仍
    evolving、分布式协议尚在确定性故障注入仿真验证阶段、生态规模未跑出；且 Cloudflare 掌握 workerd 开源实现与更成熟的平台，反制空间大。综合判断落在'有潜力成为细分赛道基础设施但需持续验证'区间上限，给予
    7.0 分。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Deno Land
- AWS S3
- Cloudflare R2
- Backblaze B2
- Tailscale
competitive_casualty:
- Cloudflare 专有平台锁定
- 托管型 Durable Objects/边缘状态服务商
- 闭源 Serverless 边缘平台
market_opportunities:
- 企业可在私有化或混合云环境中自托管运行类 Cloudflare Workers 与 Durable Objects 工作负载，满足数据主权与合规要求，降低对单一云厂商的锁定风险
- '''每对象独立 SQLite + S3 桶 CAS 协调、无需共识服务''的架构范式，可启发构建去中心化的有状态 AI Agent 运行时（如跨节点无缝迁移的会话状态与长期记忆存储）'
- 围绕 celld 可衍生托管运维服务（部署、诊断、监控、压力治理），并为 AI 实时协作应用（有状态 WebSocket/会话型产品）提供自托管基础设施方案
risk_matrix:
  regulatory: 自托管模式下数据合规责任完全转移给使用者，S3 桶与凭据等同管理员权限，需自行满足数据保护与跨境传输法规；与 Cloudflare 品牌及兼容层的商标/授权边界存在不确定性
  technological: 项目仍处早期，Workers/Durable Objects 兼容面持续演进，尚需通过 conformance 测试验证；运行依赖
    esbuild 与 S3 兼容服务的对象存储语义，存在被云厂商原生方案或更成熟开源方案替代的技术风险
  competitive: Cloudflare 可能收紧对兼容层的限制或推出竞争性自托管方案；Fly.io、Vercel（Deno 母公司）等边缘/无服务器平台构成生态挤压，巨头入场可能压缩独立生存空间
  ethical: 所有 cell 数据持续复制到 S3 桶，桶权限配置不当将导致敏感数据大规模泄露；自托管环境缺乏统一安全治理，存在数据投毒与隐私侵犯隐患
  additional:
  - 供应链风险：安装脚本与发布镜像的完整性验证、fleet 密钥（peer-auth.json）管理高度依赖用户侧运维能力
  - 运维复杂度：节点 advertise 地址、时钟同步与桶凭据管理不当可能导致集群级安全事件或数据可用性故障
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: denoland/celld
  canonical_name: denoland/celld
  url: https://github.com/denoland/celld
  positioning: celld 是 Deno Land 开源的自托管分布式 Durable Objects 守护进程，让开发者在自有机器上运行 Cloudflare
    Workers 与 Durable Objects，以对象存储替代控制平面完成节点协调。
  technical_signal: 每个对象是独立 SQLite 数据库，按名称寻址并持续复制到用户拥有的 S3 兼容桶，节点通过对象存储比较交换保证单节点独占，无需成员协议、故障检测器或共识服务。
  adoption_signal: Deno Land 官方开源发布，提供安装脚本、Docker 镜像与 gh attestation 可验证来源，支持 celld
    deploy 部署与 celld diagnose 诊断，覆盖 Linux x86-64 与 ARM64。
  ecosystem_relevance: 扩展 Deno 与 Wrangler 生态至自托管分布式场景，节点内嵌 V8 执行 Wrangler 构建产物，持续对齐
    Workers 与 Durable Objects 参考行为，形成 Cloudflare 平台之外的自托管路径。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: 相比依赖控制平面与共识服务的分布式运行时，celld 以对象存储比较交换协调节点，将共享数据库的争用与爆炸半径通过架构设计消除而非被动管理。
  watch_reason: celld 代表自托管分布式 Durable Objects 的新技术路线，由 Deno Land 官方推动，若被广泛采用可能改变
    Cloudflare Workers 的部署边界与多云部署格局；其运行时与兼容面仍在演进，公开测试覆盖有限，后续版本与社区采用值得持续跟踪。
  risk_notes:
  - celld 运行时与兼容面仍处于演进阶段，公开测试仅覆盖独立引擎冒烟路径，与 Workers 参考行为的一致性需发布前压测验证。
  - celld 明确要求对等地址置于可信网络或加密覆盖层，且默认拒绝公网直连，部署复杂度与安全边界要求较高。
  - 压力淘汰仍为可选功能，默认安全阈值尚未充分测量，大规模生产环境下的稳定性有待验证。
  score: 8.0
  article_ids:
  - b51fc9227de05c97
  evidence_snippets:
  - celld 是 Deno Land 推出的开源守护进程，可在用户自有机器上运行 Cloudflare Workers 和 Durable Objects。
  - celld 的每个对象都是独立 SQLite 数据库，按名称寻址并复制到用户拥有的 S3 兼容存储桶，节点仅靠该桶协调。
  - celld 通过安装脚本与 Docker 镜像提供发布版本，并支持用 celld deploy 命令部署 Worker 项目与静态资源。
---

Self-hosted, distributed **Durable Objects**.

celld is an open-source daemon that runs Cloudflare Workers and Durable Objects on your own machines. Each object is its own SQLite database, addressed by name and replicated to an S3-compatible bucket you own; nodes coordinate through that bucket alone, with no control plane or consensus. Because every object is its own small database, applications shard by construction — the contention and blast-radius failures of one shared database are designed out, not managed. Idle cells hibernate to nearly nothing. Learn more at celld.dev or read the documentation.

Every `celld`

node embeds V8 and executes Wrangler bundles. The fleet shares an
S3-compatible bucket containing deployments, cell state, and small ownership
records. Object-storage compare-and-swap ensures that exactly one node owns a
cell at a time, without a membership protocol, failure detector, or consensus
service.

celld continuously replicates each cell's SQLite database to the bucket. When a cell moves or wakes up, its new owner restores that database and resumes execution. The bucket is the durable source of truth; nodes are replaceable.

The installer downloads the `celld`

binary (provenance is verifiable with
`gh attestation verify`

):

`curl -fsSL https://celld.dev/install.sh | sh`

Put `~/.local/bin`

on your `PATH`

if the installer asks you to.

Worker projects deployed with `celld deploy`

need
esbuild on `PATH`

; asset-only projects do not.

The installer keeps verified releases under `~/.local/lib/celld/releases`

and
atomically switches one `current`

pointer. To remove celld, use the guarded
uninstaller:

`curl -fsSL https://celld.dev/uninstall.sh | sh`

The release image contains the `celld`

binary and is published for Linux
x86-64 and ARM64:

`docker run --rm ghcr.io/denoland/celld --version`

Persist the runtime's local state and pass the standard AWS credential environment through:

```
docker volume create celld-state
docker run --rm --network host \
-e AWS_ACCESS_KEY_ID \
-e AWS_SECRET_ACCESS_KEY \
-e AWS_SESSION_TOKEN \
-e CELLD_WATCH=/var/lib/celld/state \
-v celld-state:/var/lib/celld \
ghcr.io/denoland/celld \
--bucket s3://my-cells-bucket \
--endpoint https://ACCOUNT.r2.cloudflarestorage.com \
--region auto \
--listen 0.0.0.0:8080 \
--advertise node-a.internal:8080
```

Drop `--endpoint`

/`--region`

for real AWS S3. Behind a load balancer,
give each node a distinct `--advertise`

its peers can reach.

celld uses the standard AWS credential chain. Deploy to an S3-compatible bucket, then start celld against the same bucket:

```
celld deploy . \
--bucket s3://my-cells-bucket
celld \
--bucket s3://my-cells-bucket \
--listen 0.0.0.0:8080 \
--advertise 10.0.0.12:8080
```

Use `--endpoint`

for another S3-compatible service and `--region`

when it
cannot be inferred. A fleet runs one application, and every node loads its
latest successfully committed deployment from `deploy/current.json`

. Run
`celld --help`

for the complete command line.
Deployment objects use the documented types in `crates/celld/protocol.rs`

. `celld deploy`

invokes `esbuild`

from `PATH`

for Worker code, accepts the supported
Wrangler config subset—including co-deployed or asset-only static
assets—and writes those objects directly. Every node discovers owners and
peers from bucket leases; there is no account or join service.

Peer HTTP does not terminate TLS. Put every advertised address on a trusted
private network or an encrypted overlay such as WireGuard or Tailscale; do not
publish the peer port directly. A literal public IP is rejected unless
`--unsafe-public-advertise`

is supplied explicitly. The first current node creates
`fleet/peer-auth.json`

in the bucket. All peer requests are protocol-versioned,
body-bound, HMAC-authenticated, clock-bounded, and replay-protected with that
fleet secret. Treat access to the bucket and its credentials as fleet
administrator access.

`celld diagnose`

enumerates every node lease by default, then performs a signed
direct probe of each live peer:

`celld diagnose --bucket s3://my-cells-bucket`

The report keeps checking after an individual failure and distinguishes
expired records, malformed or unsafe advertise addresses, unreachable peers,
and incompatible protocols. It also prints each node's coarse resident-cell,
WebSocket, RSS, CPU, file-descriptor, pressure, and shedding sample. Pass one
or more `--peer NODE_ID`

options to restrict the check.

Pressure shedding is opt-in while the first release's safe defaults are being measured. Set a resident-cell high and low watermark on loaded nodes:

```
CELLD_MAX_RESIDENT_CELLS=1000 \
CELLD_RESIDENT_LOW_WATER=800 \
celld --bucket s3://my-cells-bucket --listen 0.0.0.0:8080 \
--advertise node-a.internal:8080
```

On Linux, `CELLD_MAX_RSS_MB`

and `CELLD_MAX_CPU_PERCENT`

add process-memory and
CPU triggers; the resident-cell watermark is portable. Under pressure, celld
durably replicates and fences least-recently used idle cells, publishes them as
unowned without resetting their epoch, and refuses to reacquire new unowned
cells until the low watermark is reached. A spare receives no assignment: it
acquires released cells through the same bucket protocol when normal traffic
reaches it. Cells with active work or live host WebSockets are not shed.

```
cargo build --locked
cargo test --locked
cargo clippy --all-targets --locked -- -D warnings
```

The workspace builds the `celld`

runtime. Its versioned object-storage
protocol lives in `crates/celld/protocol.rs`

. Small Wrangler projects under `examples/`

exercise the supported Worker and Durable Object surface.

The runtime and compatibility surface are still evolving. Public tests cover the standalone engine smoke path; conformance against the Workers and Durable Objects reference behavior, and a deterministic simulation of the distributed protocol under fault injection, run before each release.

Pull requests are disabled. Coding agents make it too easy to send a large, low-context change that costs maintainers more time than it saves. Thoughtful contributions are welcome; please understand the code, keep the patch focused, and respect the review time you are asking for.

Send a `git format-patch`

attachment to ry@deno.com.

Contributor License Agreement: By emailing a patch, you certify that you have the right to submit it and assign to Deno Land Inc. all rights in the patch that you can assign. Where a right cannot be assigned, you grant Deno Land Inc. a perpetual, irrevocable, worldwide, royalty-free, transferable, sublicensable license to use, modify, combine, relicense, redistribute, or publish the patch, in whole or in part, with or without attribution.

See the limitations and security pages before operating a public fleet.