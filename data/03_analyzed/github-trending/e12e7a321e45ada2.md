---
title: Osmantic/ODS
source: https://github.com/Osmantic/ODS
author: []
published: ''
created: '2026-08-30'
manifest_dates:
- '2026-08-30'
description: 'Turn your PC, Mac, or Linux box into an AI server. LLM inference, chat
  UI, voice, agents, workflows, RAG, and image generation.https://discord.gg/qGVygYada3
  ODS Osmantic Deployment System Turn your PC, Mac, or Linux box into a private AI
  server. AI server and homelab setup is rapidly becoming a solved problem. It should
  feel that way for everyone. ODS installs and wires together everything you need
  to run AI locally, so you do not have to assemble Ollama, Open WebUI, n8n, ComfyUI,
  and privacy tools by hand: Local model inference — run open models on your own hardware
  ChatGPT-style web UI — talk to your models from any browser Control dashboard —
  manage models, services, setup, GPU status, and extensions from one place Voice,
  agents, and workflows — build automations that can listen, speak, call tools, and
  get work done RAG and search — connect local documents, private search, and retrieval
  workflows Image generation — run local image tools without sending prompts to a
  hosted API Privacy and ops — keep service auth, secrets, observability, and diagnostics
  in one local stack No cloud required. No subscriptions required. Your prompts and
  data stay on your machine unless you choose otherwise. Cloud and hybrid API modes
  are optional when you want them. Release validation: Operational changes are checked
  with a release-grade fleet and distro lab: zero-prereq bootstrap, fresh installs,
  product flows, full-model capabilities, lifecycle recovery, and the final User Green
  gate. See Release Validation for what a green run proves. Repo layout: the repository
  root holds the public README, installers, security policy, GitHub workflows, and
  project coordination docs. The ods/ directory is the product runtime: services,
  installer phases, compose overlays, dashboard, CLI, tests, and operator docs. Stable
  consumption: v2.6.0 is the current stable release. main moves quickly; use it for
  active development and validation candidates. For forks, appliances, labs, or production-like
  installs, pin a tagged release or audited commit and keep your own validation receipt.
  Stable patch fixes land on release/2.6.x before being merged forward. See Release
  Channels, Installer Trust, and Forkability. Get Started Choose your system, copy
  the block, run it in a normal terminal. ODS installs the stack, picks a model for
  your hardware, starts the services, and gives you the local web UI. Linux or macOS
  curl -fsSL https://install.osmantic.com/ods.sh | bash Windows PowerShell $ProgressPreference
  = "SilentlyContinue" $odsSrc = Join-Path $env:TEMP ("ods-install-" + [guid]::NewGuid().ToString("N"))
  $odsZip = Join-Path $odsSrc "ods-main.zip" New-Item -ItemType Directory -Path $odsSrc
  | Out-Null Invoke-WebRequest "https://github.com/Osmantic/ODS/archive/refs/heads/main.zip"
  -OutFile $odsZip Expand-Archive -LiteralPath $odsZip -DestinationPath $odsSrc -Force
  cd (Get-ChildItem -LiteralPath $odsSrc -Directory | Select-Object -First 1).FullName
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass .\install.ps1 Prerequisites:
  Docker must be installed and running. On Windows, use Docker Desktop with the WSL2
  backend enabled and run the block in a normal, non-Administrator PowerShell window.
  The hosted Linux/macOS endpoint proxies the current bootstrap from repository main.
  Reviewed merges reach it automatically after edge-cache refresh. ODS_REF selects
  a compatible repository checkout. See Installer Trust to inspect the script or install
  a stable release or audited commit manually. Windows users should not run the curl
  ... | bash command from PowerShell. The PowerShell block above downloads the source
  ZIP and runs the same Windows installer used by the clone-based workflow. For more
  detail, see the Windows Quickstart. After install, open http://localhost:3000 and
  start chatting. Uninstall later with the matching platform command: cd ~/ods ./ods-uninstall.sh
  --force $installDir = "$env:USERPROFILE\ods" cd $installDir .\ods.ps1 uninstall
  --force Windows recovery note: if the runtime folder is partial and .\ods.ps1 is
  missing, run the same command from a source checkout as .\ods\installers\windows\ods.ps1
  uninstall --force. It removes Docker resources labelled as the ODS compose project
  before removing the runtime directory. API endpoint: Linux Docker installs expose
  llama-server on http://localhost:11434 by default (OLLAMA_PORT) while containers
  use llama-server:8080. macOS native Metal and Windows native/Lemonade paths use
  http://localhost:8080 unless overridden. Open WebUI stays on http://localhost:3000.
  No GPU? ODS also runs in cloud mode — same full stack, powered by OpenAI/Anthropic/Together
  APIs instead of local inference: ./install.sh --cloud Port conflicts? Every port
  is configurable via environment variables. See .env.example for the full list, or
  override at install time: WEBUI_PORT=9090 ./install.sh New here? Read the Friendly
  Guide or listen to the audio version — a complete walkthrough of what ODS is, how
  it works, and how to make it your own. No technical background needed. At A Glance
  Question Answer What is it? A local AI server stack for your own hardware, with
  a one-command Linux/macOS installer and a PowerShell installer for Windows. Who
  is it for? People who want private AI at home, in a lab, or on a workstation without
  hand-wiring a dozen services. What do I get? Local inference, Open WebUI chat, a
  control dashboard, voice, agents, workflows, RAG, search, image generation, privacy
  tools, observability, and developer tools. What does it run on? Linux, Windows with
  WSL2/Docker Desktop, and macOS Apple Silicon. Is cloud required? No. Local mode
  is the default; cloud and hybrid API modes are optional. If you know... ODS adds...
  Ollama / llama.cpp The surrounding server stack: chat, dashboard, voice, RAG, workflows,
  agents, privacy, and service management. Open WebUI A full installer and control
  plane around Open WebUI, plus pre-wired local services. AnythingLLM Broader local
  AI appliance behavior beyond RAG: inference, chat, voice, workflows, image generation,
  and ops. n8n self-hosted AI starter kits Workflow automation as one part of a larger
  private AI server. Current Platform Support Platform Status Linux (NVIDIA + AMD
  + Intel Arc) Supported — install and run today Windows (NVIDIA + AMD) Supported
  — install and run today macOS (Apple Silicon) Supported — install and run today
  Tested Linux distros: Ubuntu 24.04/22.04, Debian 12, Linux Mint 21.3, Fedora 41+,
  Rocky Linux 9, Arch Linux, Manjaro, CachyOS, and openSUSE Tumbleweed. Other distros
  using apt, dnf, pacman, or zypper should also work — open an issue if yours doesn''t.
  Release validation: Operational changes run through a release-grade gate that covers
  zero-prereq bootstrap, clean installs, product behavior, full-model capabilities,
  lifecycle recovery, and User Green. See Release Validation and the Validation Matrix.
  Windows: Requires Docker Desktop with WSL2 backend. NVIDIA GPUs use Docker GPU passthrough;
  AMD Strix Halo runs through the platform-specific accelerated path documented in
  the Windows installer and support matrix. macOS: Requires Apple Silicon (M1+) and
  Docker Desktop. llama-server runs natively with Metal GPU acceleration; all other
  services run in Docker. See the Support Matrix for supported platform claims and
  the Validation Matrix for the layered test surface used to test those claims. Why
  ODS? A handful of companies control the vast majority of global AI traffic — and
  with it, your data, your costs, and your uptime. Every query you send to a centralized
  provider is business intelligence you don’t own, running on infrastructure you don’t
  control, priced on terms you can’t negotiate. If AI is becoming critical infrastructure,
  it shouldn’t be rented. Self-hosting local AI should be a sovereign human right,
  not a career choice. Because running your own AI shouldn''t require a CS degree
  and a weekend of debugging CUDA drivers. Right now, setting up local AI means stitching
  together a dozen projects, writing Docker configs from scratch, and praying everything
  talks to each other. Most people give up and go back to paying OpenAI. We built
  ODS so you don''t have to. One command — detects your GPU, picks the right model,
  generates credentials, launches everything Chatting in under 2 minutes — bootstrap
  mode gives you a working model instantly while your full model downloads in the
  background Full service stack, pre-wired — chat, agents, voice, workflows, search,
  RAG, image generation, privacy tools, observability, and developer tools. All talking
  to each other out of the box Fully moddable — every service is an extension. Drop
  in a folder, run ods enable, done Manual install (Linux) git clone https://github.com/Osmantic/ODS.git
  cd ODS/ods ./install.sh Windows (PowerShell) Requires Docker Desktop with WSL2 backend
  enabled. Install Docker Desktop first and make sure it is running before you start.
  Open a normal PowerShell session and run: $ProgressPreference = "SilentlyContinue"
  $odsSrc = Join-Path $env:TEMP ("ods-install-" + [guid]::NewGuid().ToString("N"))
  $odsZip = Join-Path $odsSrc "ods-main.zip" New-Item -ItemType Directory -Path $odsSrc
  | Out-Null Invoke-WebRequest "https://github.com/Osmantic/ODS/archive/refs/heads/main.zip"
  -OutFile $odsZip Expand-Archive -LiteralPath $odsZip -DestinationPath $odsSrc -Force
  cd (Get-ChildItem -LiteralPath $odsSrc -Directory | Select-Object -First 1).FullName
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass .\install.ps1 The Set-ExecutionPolicy
  command allows the installer script to run in the current session. It does not change
  your system-wide policy. Running as Administrator is not recommended for the installer
  because user-level paths such as .opencode, data/, and .env can be created with
  admin-owned permissions. The installer detects your GPU, picks the right model,
  generates credentials, starts all services, and creates a Desktop shortcut to the
  Dashboard. Manage from the runtime directory with .\ods.ps1 status; uninstall with
  .\ods.ps1 uninstall --force. macOS (Apple Silicon) Requires Apple Silicon (M1+)
  and Docker Desktop. Install Docker Desktop first and make sure it is running before
  you start. git clone https://github.com/Osmantic/ODS.git cd ODS/ods ./install.sh
  The installer detects your chip, picks the right model for your unified memory,
  launches llama-server natively with Metal acceleration, and starts all other services
  in Docker. Manage with ./ods-macos.sh status. See the macOS Quickstart for details.
  What''s In The Box Chat & Inference Open WebUI — full-featured chat interface with
  conversation history, web search, document upload, and 30+ languages llama-server
  — high-performance LLM inference with continuous batching, auto-selected for your
  GPU; Linux Docker host API defaults to localhost:11434, native macOS/Windows paths
  use localhost:8080, and container API runs on 8080 LiteLLM — API gateway supporting
  local/cloud/hybrid modes TEI Embeddings — text embedding service for RAG and search
  workflows Voice Whisper — speech-to-text Kokoro — text-to-speech Agents & Automation
  Hermes Agent — default local-first autonomous/browser agent with memory, skills,
  and a magic-link-gated proxy OpenClaw — deprecated legacy autonomous agent, still
  opt-in during the migration window n8n — workflow automation with 400+ integrations
  (Slack, email, databases, APIs) APE — Agent Policy Engine for auditing and governing
  autonomous tool calls OpenCode — browser-based AI coding assistant wired to the
  local stack Memory Shepherd — host/systemd helper for agent memory lifecycle management
  Knowledge & Search Qdrant — vector database for retrieval-augmented generation (RAG)
  SearXNG — self-hosted web search (no tracking) Perplexica — deep research engine
  Brave Search — optional paid Brave Search API integration Creative ComfyUI — node-based
  image generation Privacy & Ops Privacy Shield — PII scrubbing proxy for API calls
  Dashboard — real-time GPU metrics, service health, model management Dashboard API
  — service health, setup, status, metrics, and management API behind the dashboard
  Token Spy — token usage monitor for local and proxied LLM traffic Langfuse — optional
  LLM observability and tracing Hardware Auto-Detection The installer detects your
  GPU and first assigns a deterministic hardware tier. Linux and macOS then run the
  versioned catalog selector (ods/scripts/select-model.py), while Windows uses the
  PowerShell catalog selector in ods/installers/windows/lib/tier-map.ps1; both read
  ods/config/model-library.json to choose the best installable GGUF for the detected
  memory envelope. The final choice is written to .env as LLM_MODEL, GGUF_FILE, MAX_CONTEXT,
  and MODEL_RECOMMENDATION_*. MODEL_PROFILE=qwen is the default non-Gemma catalog
  profile, so the effective pick can be Qwen, Phi, or DeepSeek depending on what fits
  best. MODEL_PROFILE=gemma4 forces Gemma 4 where available, and MODEL_PROFILE=auto
  uses Gemma 4 on NVIDIA, Apple Silicon, and Intel Arc tiers. Override tier selection
  with ./install.sh --tier 3; override the model family with MODEL_PROFILE=gemma4
  ./install.sh or MODEL_PROFILE=auto ./install.sh. When Hermes is enabled, which is
  the default agent path, installers keep the first-run bootstrap model at a 64K context
  floor and promote the full local model context to 128K where the selected model
  supports it. That avoids Hermes''s hard 64K minimum while preserving the under-2-minute
  first chat experience. The examples below are current catalog-selector outputs for
  common hardware envelopes; exact installs can differ with detected VRAM/RAM, host
  architecture, existing downloads, or explicit profile overrides. Throughput still
  needs a local benchmark after first launch. NVIDIA Tier / envelope Current default
  catalog pick Context Example hardware 0 / 8 GB CPU fallback Qwen3.5 2B (Q4_K_M)
  8K Low-RAM CPU-only 1 / 8 GB discrete VRAM Qwen3.5 9B (Q4_K_M) 32K RTX 4060, RTX
  3060 12GB 2 / 12 GB discrete VRAM Phi-4 14B (Q4_K_M) 16K RTX 4070-class cards 3
  / 24 GB discrete VRAM Qwen3.5 27B (Q4_K_M) 32K RTX 4090, A6000 4 / 48 GB discrete
  VRAM DeepSeek R1 Distill Llama 70B (Q4_K_M) 32K A6000 Ada, L40S NV_ULTRA / 90+ GB
  amd64 discrete VRAM Qwen3 Coder Next (Q4_K_M) 128K Multi-GPU A100/H100 NV_ULTRA
  / 90+ GB arm64 unified memory Qwen3.6 35B-A3B (UD-Q4_K_M) 128K DGX Spark / GB10-class
  hosts AMD Strix Halo (Unified Memory) Tier / envelope Current default catalog pick
  Context Hardware SH_COMPACT / 64 GB unified RAM Qwen3.6 35B-A3B (UD-Q4_K_M) 128K
  Ryzen AI MAX+ 395 (64GB) SH_LARGE / 96 GB unified RAM DeepSeek R1 Distill Llama
  70B (Q4_K_M) 32K Ryzen AI MAX+ 395 (96GB) SH_LARGE / 124 GB unified RAM Qwen3.6
  35B-A3B (UD-Q4_K_M) 128K Ryzen AI MAX+ 395 (128GB class) The selector routes unified-memory
  hosts away from Qwen3 Coder Next when that model would otherwise be selected, because
  current repo policy documents correctness issues on those backends. Apple Silicon
  (Unified Memory, Metal) Tier / envelope Current default catalog pick Context Example
  hardware 0 / 8 GB unified RAM Phi-4 Mini (Q4_K_M) 128K M1/M2 base (8GB) 1 / 16 GB
  unified RAM Qwen3.5 9B (Q4_K_M) 32K M4 Mac Mini (16GB) 2 / 32 GB unified RAM Phi-4
  14B (Q4_K_M) 16K M4 Pro Mac Mini, M3 Max MacBook Pro 3 / 48 GB unified RAM Qwen3.5
  27B (Q4_K_M) 32K M4 Pro (48GB), M2 Max (48GB) 4 / 64+ GB unified RAM Qwen3.6 35B-A3B
  (UD-Q4_K_M) 128K M2 Ultra Mac Studio, M4 Max (64GB+) Intel Arc (Linux, SYCL) Tier
  / envelope Current default catalog pick Context Example hardware ARC_LITE / 6 GB
  discrete VRAM Phi-4 Mini (Q4_K_M) 128K Arc A380 ARC_LITE / 8 GB discrete VRAM Qwen3.5
  9B (Q4_K_M) 32K Arc A750 ARC / 16 GB discrete VRAM Phi-4 14B (Q4_K_M) 16K Arc A770
  16GB, newer Arc GPUs Gemma 4 profile tiers remain in the installer tier maps: E2B
  on entry hardware, E4B on midrange hardware, 26B-A4B on pro hardware, and 31B on
  large/ultra hardware. Bootstrap Mode No waiting for large downloads. ODS uses bootstrap
  mode by default: Downloads a tiny 1.5B model in under a minute You start chatting
  immediately The full model downloads in the background Hot-swap to the full model
  when it''s ready — zero downtime The bootstrap model starts with a 64K context window
  so Hermes can work during the first session. After the background download finishes,
  ODS swaps to the full model and restores the Hermes/full-model context target. Skip
  bootstrap: ./install.sh --no-bootstrap Switching Models The installer picks a model
  for your hardware, but you can switch anytime: ods model current # What''s running
  now? ods model list # Show all available tiers ods model swap T3 # Switch to a different
  tier If the new model isn''t downloaded yet, pre-fetch it first: ./scripts/pre-download.sh
  --tier 3 # Download before switching ods model swap T3 # Then swap (restarts llama-server)
  Already have a GGUF you want to use? Drop the single .gguf file in data/models/,
  then open Dashboard -> Models and load the local entry. For older installs or headless
  maintenance, update GGUF_FILE and LLM_MODEL in .env, then restart with the CLI:
  ods restart llm Or restart the container directly from the installed ods directory:
  docker compose restart llama-server Rollback is automatic — if a new model fails
  to load, ODS reverts to your previous model. Extensibility ODS is designed to be
  modded. Every service is an extension — a folder with a manifest.yaml and a compose.yaml.
  The dashboard, CLI, health checks, and compose stack all discover extensions automatically.
  extensions/services/ my-service/ manifest.yaml # Metadata: name, port, health endpoint,
  GPU backends compose.yaml # Docker Compose fragment (auto-merged into the stack)
  ods enable my-service # Enable it ods disable my-service # Disable it ods list #
  See everything The installer itself is modular — 19 library modules, a shared service
  registry, and 13 ordered phases. Want to add a hardware tier, swap a default model,
  or skip a phase? Start with the installer architecture map so you update the Linux,
  macOS, Windows, upgrade, and host-agent writers together. Full extension guide |
  Installer architecture ods-cli The ods CLI manages your entire stack: ods status
  # Health checks + GPU status ods list # All services and their state ods logs llm
  # Tail logs (aliases: llm, stt, tts) ods restart [service] # Restart one or all
  services ods start / stop # Start or stop the stack ods mode cloud # Switch to cloud
  APIs via LiteLLM ods mode local # Switch back to local inference ods mode hybrid
  # Local primary, cloud fallback ods model swap T3 # Switch to a different hardware
  tier ods enable n8n # Enable an extension ods disable whisper # Disable one ods
  config show # View .env (secrets masked) ods preset save gaming # Snapshot current
  config ods preset load gaming # Restore it How It Compares Other tools get you part
  of the way. ODS gets you the whole way. ODS Ollama + Open WebUI LocalAI Scope Full
  AI stack — inference to agents to workflows LLM + chat LLM only One-command install
  Everything, auto-configured LLM + chat only LLM only Hardware auto-detect + model
  selection NVIDIA + AMD Strix Halo + Apple Silicon + Intel Arc + CPU/cloud fallback
  No No AMD APU unified memory support Platform-specific accelerated backend, selected
  by installer Partial (Vulkan) No Autonomous AI agents Hermes Agent default; OpenClaw
  legacy opt-in No No Workflow automation n8n (400+ integrations) No No Voice (STT
  + TTS) Whisper + Kokoro No No Image generation ComfyUI No No RAG pipeline Qdrant
  + embeddings No No Extension system Manifest-based, hot-pluggable No No Multi-GPU
  Yes (NVIDIA) Partial Partial Documentation Quickstart Step-by-step install guide
  with troubleshooting Docs Index Maintained map for operators, contributors, and
  reviewers Build On ODS Forking, custom editions, extension templates, and downstream
  validation Forkability How to fork, audit, customize, and independently operate
  ODS Maintainer Runbook Release, rollback, validation, and operator continuity guidance
  for maintainers and forks High-Risk Change Map Which changes require focused checks,
  fleet validation, or release-grade gates Headless Setup QR onboarding, first-boot
  setup, AP mode, mDNS, and local agent access Support Matrix Current platform and
  GPU support status Release Validation User Green gates and the release-grade fleet/distro
  validation policy 2.6.0 Release Notes Current stable release notes, validation receipt,
  and known validation boundaries Validation Matrix Sanitized CI, distro lab, and
  real-hardware fleet release-readiness evidence Validation Reproducibility How forks
  and operators can reproduce the validation story on their own hardware Offline And
  Mirroring Pinning, mirroring, and preserving release artifacts for independent operation
  Installer Trust Inspect-first install paths, ref pinning, and current provenance
  limits Model Management Curated and Hugging Face GGUF discovery, verified imports,
  switching, and recovery Hardware Guide What to buy, tier recommendations FAQ Common
  questions and configuration Extensions How to add custom services Installer Architecture
  Modular installer deep dive Installer Phase Contracts Phase ownership, idempotency,
  failure modes, and validation expectations Compose Resolver Contracts Rules for
  compose layers, extensions, backends, ports, and mode overlays Changelog Version
  history and release notes Contributing How to contribute Contributors And Recognition
  ODS is built by a growing group of contributors across installers, GPU support,
  dashboard, security, extensions, docs, and release validation. The README keeps
  the product overview focused; the long-form credits, upstream acknowledgements,
  and contributor history live in CONTRIBUTORS.md. ODS has been recognized by the
  local AI and developer community, including AMD Featured Developer recognition,
  selection as a May 2026 AMD Lemonade Developer Challenge winner, and a feature at
  (Co)nnect: Philly''s AI Ecosystem Summit at Pennovation Works. License Apache 2.0
  — Use it, modify it, ship it. See LICENSE. Built by Osmantic and the growing resistance
  that refuses to rent what should be owned.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: e12e7a321e45ada2
source_type: community_discussion
tldr: ODS（Osmantic Deployment System）是一个开源的私有 AI 服务器部署系统，通过一条命令把 PC、Mac 或 Linux 变成本地
  AI 服务器，自动整合 Ollama、Open WebUI、n8n、ComfyUI 等全套服务，当前稳定版本为 v2.6.0。
objective_summary: Osmantic 团队在 GitHub 发布开源项目 ODS（Osmantic Deployment System）v2.6.0，它通过单条命令将普通电脑变成私有
  AI 服务器。该系统自动检测 GPU、挑选合适模型并启动服务，整合本地推理、Open WebUI 聊天、控制台、语音、智能体、RAG、图像生成与隐私工具，支持
  Linux、Windows（WSL2/Docker Desktop）和 macOS Apple Silicon。系统默认本地运行无需云服务，数据和提示词保留在本机，云端与混合
  API 模式作为可选项提供。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Osmantic
  technologies:
  - Ollama
  - Open WebUI
  - llama-server
  - LiteLLM
  - RAG
  - Qdrant
  - ComfyUI
  - Hermes Agent
  - OpenClaw
  - n8n
  - SearXNG
  - Perplexica
  - Whisper
  - Kokoro
  - TEI Embeddings
  - APE
  - OpenCode
  - Memory Shepherd
  - Privacy Shield
  - Brave Search
  key_people: []
key_logic_flow:
- ODS 定位为解决本地 AI 部署碎片化问题，将 Ollama、Open WebUI、n8n、ComfyUI 等十多个服务预装并串联成一套开箱即用的私有 AI
  服务器。
- 安装提供 Linux/macOS 的 curl 一键脚本与 Windows PowerShell 安装器，前提是 Docker 已安装并运行，Windows 需启用
  WSL2 后端。
- 安装器会自动检测 GPU、为硬件选择模型、生成凭证并启动全部服务，bootstrap 模式可在两分钟内让用户开始聊天，同时后台下载完整模型。
- 核心组件包括 llama-server 推理引擎、Open WebUI 聊天界面、LiteLLM API 网关、Hermes Agent 智能体、Qdrant
  向量库与 ComfyUI 图像生成。
- 项目主张本地自托管 AI 应成为数字主权权利，默认本地模式不依赖云端，云端与混合模式通过 --cloud 参数可选启用。
- 当前稳定版本为 v2.6.0，main 分支快速迭代，官方通过发布级验证流程覆盖全新安装、产品流程、生命周期恢复与用户验收。
object_mentions:
- object_type: project
  name: Osmantic/ODS
  canonical_name: Osmantic/ODS
  url: https://github.com/Osmantic/ODS
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - ODS 是一个开源本地 AI 服务器部署系统，通过一条命令即可安装并串联推理、聊天、智能体、RAG 与图像生成等全套服务。
  - 当前稳定版本为 v2.6.0，main 分支快速迭代，官方提供发布级验证流程保证安装与生命周期可靠性。
  article_id: e12e7a321e45ada2
- object_type: product
  name: Open WebUI
  canonical_name: Open WebUI
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - ODS 内置 Open WebUI 作为 ChatGPT 风格聊天界面，支持对话历史、网页搜索、文档上传和 30 多种语言，默认运行在 3000 端口。
  article_id: e12e7a321e45ada2
- object_type: project
  name: llama-server
  canonical_name: llama-server
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - llama-server 是 ODS 的高性能 LLM 推理组件，支持连续批处理并自动适配 GPU，Linux Docker 默认 API 端口为 11434，原生
    macOS 与 Windows 路径使用 8080 端口。
  article_id: e12e7a321e45ada2
- object_type: project
  name: Ollama
  canonical_name: Ollama
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - ODS 免去用户手动组装 Ollama、Open WebUI、n8n、ComfyUI 和隐私工具，自动完成本地 AI 运行所需的整套环境搭建。
  article_id: e12e7a321e45ada2
- object_type: product
  name: n8n
  canonical_name: n8n
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - n8n 提供工作流自动化能力，支持 Slack、邮件、数据库和 API 等 400 多种集成，是 ODS 服务栈中的自动化组件。
  article_id: e12e7a321e45ada2
- object_type: project
  name: ComfyUI
  canonical_name: ComfyUI
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - ComfyUI 是 ODS 集成的节点式图像生成工具，可在本地完成图像生成而不将提示词发送到托管 API。
  article_id: e12e7a321e45ada2
- object_type: project
  name: Hermes Agent
  canonical_name: Hermes Agent
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Hermes Agent 是 ODS 默认的本地优先自主浏览器智能体，具备记忆、技能和魔法链接门控代理能力，可执行工具调用与自动化任务。
  article_id: e12e7a321e45ada2
- object_type: project
  name: LiteLLM
  canonical_name: LiteLLM
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - LiteLLM 作为 API 网关集成在 ODS 中，负责路由本地、云端与混合三种推理模式之间的请求。
  article_id: e12e7a321e45ada2
- object_type: project
  name: Qdrant
  canonical_name: Qdrant
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Qdrant 是 ODS 使用的向量数据库，为检索增强生成（RAG）与搜索工作流提供向量存储和检索能力。
  article_id: e12e7a321e45ada2
- object_type: project
  name: Perplexica
  canonical_name: Perplexica
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Perplexica 作为深度研究引擎集成在 ODS 的搜索检索体系中，与 SearXNG 自托管搜索共同构成本地检索能力。
  article_id: e12e7a321e45ada2
- object_type: project
  name: SearXNG
  canonical_name: SearXNG
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - SearXNG 是 ODS 集成的自托管网页搜索引擎，无追踪，用于提供本地私有搜索能力。
  article_id: e12e7a321e45ada2
- object_type: project
  name: OpenClaw
  canonical_name: OpenClaw
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - OpenClaw 是 ODS 中已弃用的旧版自主智能体，在迁移窗口期内仍可选择启用。
  article_id: e12e7a321e45ada2
extract_result: success
impact_score:
  score: 5.5
  reason: 评分依据：ODS 本质是工程整合型开源发布，将 Ollama、Open WebUI、n8n、ComfyUI、LiteLLM、Qdrant 等既有组件打包成一条命令可用的私有
    AI 服务器，未引入新模型或算法突破，不构成范式转移（8 分档）；但 v2.6.0 稳定版配合自动 GPU 探测与模型选型、统一控制面板、发布级验证流程，确实把本地自托管
    AI 的碎片化门槛拉低了一个量级，足以影响个人开发者与小团队的自托管竞争格局（4-7 分档）。考虑到 AnythingLLM、Pinokio 及手工 Docker
    栈等替代路径已存在，且本事件仅限 GitHub 社区讨论渠道、无资本与生态背书，冲击力打折，综合取 5.5 分。
sentiment: positive
developer_sentiment:
  tone: neutral
  primary_focus: 一键整合十余个服务的部署可靠性与版本锁定策略，以及它是否只是对 Docker Compose 生态的再包装
hype_assessment:
  level: low
  reason: 判定依据：README 开篇的'AI 服务器与 homelab 搭建正迅速成为已解决问题'带有适度包装色彩，但整体表述克制且可验证——给出了可复现的
    curl/PowerShell 安装命令、明确的稳定版本号 v2.6.0、安装信任链与 fork 策略说明、端口映射与平台兼容细节，全文未滥用'颠覆''革命性'等
    PR 词汇，项目本体可实测，属于实打实的工程交付。
information_entropy: high
domain_disruption:
  technical_innovation: 技术本质不是新算法或新推理引擎，而是部署编排工程上的整合式创新：安装器自动探测 GPU 并为硬件选配模型、基于 Compose
    overlays 组织十余个服务（llama-server、Open WebUI、LiteLLM、Qdrant、ComfyUI、Hermes Agent 等）、提供统一控制面板与
    CLI，并以发布级验证流程（零前置依赖引导、全新安装、产品流程、生命周期恢复、用户验收）保障整体可靠性。真正的技术含量在于把碎片化的自托管体验收敛为可复现、可升级、可卸载的单一系统。
  business_model: 作为开源项目暂无直接商业模式，但'本地优先、云/混合 API 可选'的定位会重塑个人与 SMB 的 AI 成本结构——降低对云端推理
    API 的依赖，可能蚕食商业本地 AI 一体机与托管服务的市场。潜在商业化路径包括企业级 appliance/支持服务、受管更新订阅，以及作为 Open WebUI
    等生态的流量入口，客观上推动本地 AI 数字主权叙事。
engineering_complexity: production_ready
compound_value:
  score: 4.5
  reason: 从资本视角评估，ODS 切中的'本地私有 AI 部署碎片化'是真实痛点，数据主权与隐私合规（GDPR、企业数据出境限制）构成长期结构性顺风，赛道本身具备复利属性。但价值捕获路径存在明显瓶颈：(1)
    本质是对 Ollama、Open WebUI、n8n、ComfyUI 等既有开源组件的打包与编排，核心价值层薄，难以沉淀专属技术壁垒；(2) 上游组件体量更大（Ollama
    已是事实上的本地模型运行时标准，Open WebUI 是主流 UI），随时可能向下吞噬安装编排层，ODS 面临'被上游内化'的生存风险；(3) 纯开源社区驱动，无明确商业化与收入模型，VC
    退出路径模糊；(4) '一键安装'类产品护城河天然偏弱，Docker、云厂商或 OS 厂商一旦内化同等能力即被稀释。因此判定为 4.5 分：具备成为'细分赛道基础设施'的候选资格，但需持续验证用户留存、扩展生态与商业化能力，3-5
    年后的行业基石地位确定性不足，够不到 8 分以上的复利区间。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- Ollama
- Open WebUI
- Docker
- NVIDIA
- ComfyUI
- n8n
competitive_casualty:
- LM Studio
- AnythingLLM（托管版）
- 商业 AI 服务器搭建/集成服务商
- 云端推理 API 的边缘场景需求
market_opportunities:
- 面向医疗、法律、金融等数据敏感行业，创业者可基于 ODS 类一键部署方案，推出私有 AI 服务器的交钥匙交付与长期运维托管服务，切中数字主权与合规诉求
- 个人开发者和中小企业可将 ODS 整合的本地 AI 栈（本地推理、RAG、智能体、图像生成）作为低成本原型验证与私有化部署底座，降低从零拼装十余个开源组件的门槛
- 可探索围绕本地 AI 服务器的增值变现路径，如模型精选市场、更新订阅、企业级技术支持与合规审计报告服务，弥补开源免费模式的商业化缺口
risk_matrix:
  regulatory: 本地部署虽规避云侧数据出境审查，但企业使用自托管 AI 栈仍需承担内部数据治理责任（如《数据安全法》/GDPR 下的数据保护义务）；内置
    ComfyUI 图像生成、语音等能力可能触碰生成内容合规红线
  technological: 整栈依赖 Docker 与十余个上游开源组件（Ollama、Open WebUI、n8n、ComfyUI 等），任一上游变更或弃坑都会影响稳定性；安装采用
    curl | bash 远程脚本模式，存在供应链劫持与版本漂移风险；Docker Desktop 对大企业有商业授权限制
  competitive: 本地 AI 赛道已有 Ollama、LM Studio、Jan.ai、AnythingLLM 等成熟玩家，NVIDIA/Apple/微软等巨头若下场打包原生方案将挤压该生态；开源免费模式导致商业化变现困难、同质化竞争激烈
  ethical: 本地图像生成与语音能力降低深度伪造和滥用门槛且无云端内容审查兜底；开源模型存在偏见与数据投毒风险，RAG 引入的私有文档在缺乏治理时可能被错误检索外泄
  additional:
  - 安装器信任链风险：一键脚本托管在独立域名，一旦被劫持影响面极大，需持续关注其安全策略、签名与审计承诺
  - 项目较新且 main 分支快速迭代，长期维护意愿与社区可持续性有待观察
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: monitor
object_insights:
- object_type: project
  name: Osmantic/ODS
  canonical_name: Osmantic/ODS
  url: https://github.com/Osmantic/ODS
  positioning: 开源私有 AI 服务器部署系统，通过一条命令将 PC、Mac 或 Linux 变成本地 AI 服务器，整合推理、聊天、智能体、RAG
    与图像生成等全套服务。
  technical_signal: 安装器自动检测 GPU 并为硬件挑选模型，以 Docker 为基础串联十多个开源服务，当前稳定版本为 v2.6.0，main
    分支持续快速迭代。
  adoption_signal: 官方提供发布级验证流程，覆盖全新安装、产品流程、生命周期恢复与用户验收门禁，并面向 Linux、Windows 与 macOS
    提供一键安装脚本。
  ecosystem_relevance: 整合 Open WebUI、n8n、ComfyUI、Qdrant、LiteLLM 等十余个开源组件，面向 Ollama
    生态用户提供完整服务栈替代方案，降低本地 AI 自托管门槛。
  target_users:
  - 希望在家、实验室或工作站私有部署 AI 的个人用户
  - 不想手工组装十余个服务的开发者
  - 重视数据隐私与数字主权的自托管爱好者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: ODS 将本地 AI 部署从手工组装十余个服务简化为一条命令，主张本地自托管应成为数字主权权利，其默认本地、可选云端与混合模式的架构设计值得持续跟踪，以观察本地
    AI 能否在主流部署方式中占据一席之地。
  risk_notes:
  - 本地推理依赖用户自有硬件算力，无 GPU 时推理体验受限，云模式兜底但会弱化本地优势。
  - 项目处于快速迭代期，main 分支变动频繁，生产环境需锁定稳定版本并自行验证，可靠性存在不确定性。
  - 本地自托管赛道已有 Ollama、AnythingLLM 等成熟方案，ODS 的差异化优势与长期维护能力仍需时间验证。
  score: 9.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - ODS 是一个开源本地 AI 服务器部署系统，通过一条命令即可安装并串联推理、聊天、智能体、RAG 与图像生成等全套服务。
  - 当前稳定版本为 v2.6.0，main 分支快速迭代，官方提供发布级验证流程保证安装与生命周期可靠性。
- object_type: product
  name: Open WebUI
  canonical_name: Open WebUI
  url: null
  positioning: 开源 ChatGPT 风格本地聊天界面，被 ODS 作为默认 Web 聊天组件集成，运行在 3000 端口。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - ODS 使用者
  - 本地 AI 自托管用户
  - 需要浏览器端对话界面的隐私敏感用户
  product_signal: 提供对话历史、网页搜索、文档上传和 30 多种语言支持，作为 ODS 服务栈中面向用户的聊天入口。
  market_signal: 作为成熟的本地聊天界面被 ODS 选为默认组件，反映其在自托管社区中的广泛采用与主流地位。
  differentiation: 以多模型兼容和丰富交互能力见长，被集成进 ODS 完整服务栈后可与推理、RAG、图像生成等模块联动使用。
  watch_reason: Open WebUI 是 ODS 面向用户的默认聊天入口，其功能迭代直接影响 ODS 的可用性，同时它本身是自托管社区广泛采用的开源产品，值得跟踪其与
    ODS 集成深度的变化。
  risk_notes:
  - 作为 ODS 内置组件，其演进方向受上游开源社区影响，版本变更可能带来兼容性问题。
  score: 6.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - ODS 内置 Open WebUI 作为 ChatGPT 风格聊天界面，支持对话历史、网页搜索、文档上传和 30 多种语言，默认运行在 3000 端口。
- object_type: project
  name: llama-server
  canonical_name: llama-server
  url: null
  positioning: 高性能 LLM 本地推理组件，在 ODS 中承担连续批处理推理职责，并自动适配 GPU 运行环境。
  technical_signal: 支持连续批处理并自动适配 GPU，Linux Docker 默认 API 端口为 11434，原生 macOS 与 Windows
    路径使用 8080 端口。
  adoption_signal: null
  ecosystem_relevance: 作为 ODS 的默认本地推理引擎，与 Open WebUI、LiteLLM 等组件协同构成完整的本地推理链路。
  target_users:
  - 需要本地 LLM 推理的开发者
  - ODS 服务栈使用者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: llama-server 是 ODS 本地推理能力的核心组件，其批处理性能与 GPU 适配能力决定本地部署体验的上限，值得跟踪其性能迭代与多平台运行表现。
  risk_notes:
  - 推理性能高度依赖硬件配置，无 GPU 环境下连续批处理能力受限，可能影响本地服务体验。
  score: 5.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - llama-server 是 ODS 的高性能 LLM 推理组件，支持连续批处理并自动适配 GPU，Linux Docker 默认 API 端口为 11434，原生
    macOS 与 Windows 路径使用 8080 端口。
- object_type: product
  name: n8n
  canonical_name: n8n
  url: null
  positioning: 开源工作流自动化平台，在 ODS 服务栈中承担自动化编排职责，连接 400 多种外部服务。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 需要工作流自动化的 ODS 用户
  - 本地 AI 应用构建者
  - 低代码自动化需求方
  product_signal: 支持 Slack、邮件、数据库和 API 等 400 多种集成，为 ODS 提供可编程的自动化工作流编排能力。
  market_signal: 作为成熟的自动化平台被 ODS 纳入默认服务栈，反映低代码工作流在本地 AI 应用场景中的需求增长。
  differentiation: 以 400 多种现成集成和可视化编排见长，是 ODS 服务栈中连接外部系统的主要自动化组件。
  watch_reason: n8n 是 ODS 自动化能力的核心载体，其与本地模型的结合方式决定智能体与工作流自动化的上限，值得跟踪其在 ODS 生态中的集成深度与本地化适配进展。
  risk_notes:
  - n8n 的 400 多种集成多面向云端服务，本地离线场景下可用集成范围受限。
  score: 5.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - n8n 提供工作流自动化能力，支持 Slack、邮件、数据库和 API 等 400 多种集成，是 ODS 服务栈中的自动化组件。
- object_type: project
  name: ComfyUI
  canonical_name: ComfyUI
  url: null
  positioning: 节点式本地图像生成工具，被 ODS 集成用于在本地完成图像生成，避免提示词发送到托管 API。
  technical_signal: 以节点化工作流实现本地图像生成，提示词不出本机，保障多模态生成场景的隐私安全。
  adoption_signal: null
  ecosystem_relevance: 作为 ODS 图像生成组件补全服务栈的多模态能力，与推理、聊天、RAG 等模块协同构成完整本地 AI 体验。
  target_users:
  - 本地图像生成需求用户
  - 重视提示词隐私的创作者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: ComfyUI 是 ODS 本地图像生成能力的核心，其节点式工作流与本地隐私特性契合数字主权理念，值得跟踪其与 ODS 的集成方式及模型生态发展。
  risk_notes:
  - 本地图像生成对显存要求较高，普通硬件难以获得理想出图速度，使用门槛高于推理组件。
  score: 5.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - ComfyUI 是 ODS 集成的节点式图像生成工具，可在本地完成图像生成而不将提示词发送到托管 API。
- object_type: project
  name: Hermes Agent
  canonical_name: Hermes Agent
  url: null
  positioning: 本地优先的自主浏览器智能体，被 ODS 设为默认智能体组件，具备记忆、技能与门控代理能力。
  technical_signal: 具备记忆、技能和魔法链接门控代理能力，可执行工具调用与自动化任务，是 ODS 智能体体系的新默认选择。
  adoption_signal: null
  ecosystem_relevance: 作为 ODS 默认智能体取代旧版 OpenClaw，与 n8n 工作流及本地工具链协同构成自动化能力体系。
  target_users:
  - 需要本地自动化智能体的 ODS 用户
  - 自主智能体应用开发者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Hermes Agent 作为 ODS 默认智能体决定本地 AI 自动化能力的走向，其记忆与技能机制的成熟度值得持续跟踪，是观察本地优先智能体趋势的重要样本。
  risk_notes:
  - 证据置信度为中等，且智能体自主执行浏览器操作存在安全与误操作风险，实际能力有待验证。
  score: 5.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - Hermes Agent 是 ODS 默认的本地优先自主浏览器智能体，具备记忆、技能和魔法链接门控代理能力，可执行工具调用与自动化任务。
- object_type: project
  name: LiteLLM
  canonical_name: LiteLLM
  url: null
  positioning: 开源 LLM API 网关，在 ODS 中负责路由本地、云端与混合三种推理模式的请求。
  technical_signal: 作为统一 API 网关集成在 ODS 中，负责在本地、云端与混合三种推理模式间路由请求，是模式切换的关键组件。
  adoption_signal: null
  ecosystem_relevance: 连接 llama-server 本地推理与 OpenAI、Anthropic 等云端 API，为 ODS 的混合部署模式提供统一接口层。
  target_users:
  - 需要多模式推理切换的 ODS 用户
  - API 网关集成开发者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: LiteLLM 是 ODS 实现本地、云端与混合模式灵活切换的枢纽，其网关稳定性直接影响多模式部署体验，而混合模式是 ODS 差异化能力的重要一环，值得持续跟踪。
  risk_notes:
  - 作为中间网关增加部署链路复杂度，多模式路由与 API 兼容问题可能在混合场景下暴露。
  score: 4.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - LiteLLM 作为 API 网关集成在 ODS 中，负责路由本地、云端与混合三种推理模式之间的请求。
- object_type: project
  name: Qdrant
  canonical_name: Qdrant
  url: null
  positioning: 开源向量数据库，在 ODS 中为本地 RAG 与搜索工作流提供向量存储和检索能力，是知识增强的基础组件。
  technical_signal: 为检索增强生成与搜索工作流提供向量存储和检索能力，支撑本地文档问答与知识库检索场景。
  adoption_signal: null
  ecosystem_relevance: 作为 ODS 的 RAG 基础设施，与本地文档连接和 Perplexica 深度检索共同构成知识增强与搜索链路。
  target_users:
  - 需要本地知识库问答的 ODS 用户
  - RAG 应用开发者
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Qdrant 支撑 ODS 的 RAG 与本地搜索能力，向量检索性能直接影响本地知识库问答体验，值得跟踪其在 ODS 中的集成深度与版本升级节奏。
  risk_notes:
  - RAG 效果依赖文档质量与分块策略，单纯部署向量数据库并不保证检索质量，需要整体调优。
  score: 4.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - Qdrant 是 ODS 使用的向量数据库，为检索增强生成（RAG）与搜索工作流提供向量存储和检索能力。
- object_type: project
  name: Perplexica
  canonical_name: Perplexica
  url: null
  positioning: 开源深度研究引擎，被 ODS 集成于搜索检索体系，与 SearXNG 共同构成本地化研究能力。
  technical_signal: 作为深度研究引擎集成在 ODS 的搜索检索体系中，与 SearXNG 自托管搜索协同提供本地化研究能力。
  adoption_signal: null
  ecosystem_relevance: 与 Qdrant、SearXNG 共同构成 ODS 的本地检索与 RAG 能力，支撑文档问答与深度研究场景。
  target_users:
  - 需要本地深度研究的 ODS 用户
  - 研究检索场景用户
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Perplexica 代表本地深度研究这一新兴方向，其与 ODS 搜索体系的整合效果值得跟踪，以观察本地化研究工具能否在隐私与质量之间达到可用平衡。
  risk_notes:
  - 本地深度研究的检索质量依赖底层搜索源与模型能力，与云端研究工具相比仍有差距，实际效果待验证。
  score: 4.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - Perplexica 作为深度研究引擎集成在 ODS 的搜索检索体系中，与 SearXNG 自托管搜索共同构成本地检索能力。
- object_type: project
  name: SearXNG
  canonical_name: SearXNG
  url: null
  positioning: 无追踪的自托管网页搜索引擎，被 ODS 集成用于提供本地私有搜索能力，保障搜索行为不出本机。
  technical_signal: 作为自托管搜索引擎集成于 ODS，无追踪特性保障本地搜索隐私，为 RAG 与深度研究提供数据来源。
  adoption_signal: null
  ecosystem_relevance: 与 Perplexica、Qdrant 共同构成本地检索体系，是 ODS 隐私搜索与 RAG 工作流的数据入口。
  target_users:
  - 重视搜索隐私的 ODS 用户
  - 自托管搜索需求方
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: SearXNG 为 ODS 提供无追踪的本地搜索能力，是隐私优先设计的关键组件，搜索质量直接影响 RAG 与深度研究效果，其维护活跃度与索引能力值得持续跟踪。
  risk_notes:
  - 自托管搜索的索引覆盖面与结果质量通常弱于商业搜索引擎，可能限制本地研究场景的检索效果。
  score: 4.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - SearXNG 是 ODS 集成的自托管网页搜索引擎，无追踪，用于提供本地私有搜索能力。
- object_type: project
  name: OpenClaw
  canonical_name: OpenClaw
  url: null
  positioning: ODS 中已弃用的旧版自主智能体，在迁移窗口期内仍可选择启用，属于过渡性组件。
  technical_signal: 作为已弃用的旧版智能体，官方保留迁移窗口期供用户选择启用，其后继者为新默认智能体 Hermes Agent。
  adoption_signal: null
  ecosystem_relevance: 反映 ODS 智能体组件的演进路径，从 OpenClaw 迁移到 Hermes Agent 体现本地智能体方案的迭代方向。
  target_users:
  - 仍在使用旧版智能体的 ODS 用户
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: OpenClaw 的弃用与迁移窗口反映 ODS 对智能体组件的策略调整，跟踪其迁移过程可观察 ODS 智能体路线的稳定性与向后兼容策略。
  risk_notes:
  - 已弃用组件将不再获得新功能迭代，长期使用面临维护缺失与兼容性风险，用户应尽快迁移至 Hermes Agent。
  score: 3.0
  article_ids:
  - e12e7a321e45ada2
  evidence_snippets:
  - OpenClaw 是 ODS 中已弃用的旧版自主智能体，在迁移窗口期内仍可选择启用。
---

**Osmantic Deployment System**

**Turn your PC, Mac, or Linux box into a private AI server.**

AI server and homelab setup is rapidly becoming a solved problem. It should feel that way for everyone.

ODS installs and wires together everything you need to run AI locally, so you do not have to assemble Ollama, Open WebUI, n8n, ComfyUI, and privacy tools by hand:

**Local model inference**— run open models on your own hardware**ChatGPT-style web UI**— talk to your models from any browser**Control dashboard**— manage models, services, setup, GPU status, and extensions from one place**Voice, agents, and workflows**— build automations that can listen, speak, call tools, and get work done**RAG and search**— connect local documents, private search, and retrieval workflows**Image generation**— run local image tools without sending prompts to a hosted API**Privacy and ops**— keep service auth, secrets, observability, and diagnostics in one local stack

No cloud required. No subscriptions required. Your prompts and data stay on your machine unless you choose otherwise. Cloud and hybrid API modes are optional when you want them.

**Release validation:** Operational changes are checked with a release-grade
fleet and distro lab: zero-prereq bootstrap, fresh installs, product flows,
full-model capabilities, lifecycle recovery, and the final User Green gate. See
Release Validation for what a green
run proves.

**Repo layout:** the repository root holds the public README, installers,
security policy, GitHub workflows, and project coordination docs. The
`ods/`

directory is the product runtime: services, installer phases,
compose overlays, dashboard, CLI, tests, and operator docs.

**Stable consumption:** `v2.6.0`

is the current stable release. `main`

moves
quickly; use it for active development and validation candidates. For forks,
appliances, labs, or production-like installs, pin a tagged release or audited
commit and keep your own validation receipt. Stable patch fixes land on
`release/2.6.x`

before being merged forward. See
Release Channels,
Installer Trust, and
Forkability.

Choose your system, copy the block, run it in a normal terminal. ODS installs the stack, picks a model for your hardware, starts the services, and gives you the local web UI.

**Linux or macOS**

`curl -fsSL https://install.osmantic.com/ods.sh | bash`

**Windows PowerShell**

```
$ProgressPreference = "SilentlyContinue"
$odsSrc = Join-Path $env:TEMP ("ods-install-" + [guid]::NewGuid().ToString("N"))
$odsZip = Join-Path $odsSrc "ods-main.zip"
New-Item -ItemType Directory -Path $odsSrc | Out-Null
Invoke-WebRequest "https://github.com/Osmantic/ODS/archive/refs/heads/main.zip" -OutFile $odsZip
Expand-Archive -LiteralPath $odsZip -DestinationPath $odsSrc -Force
cd (Get-ChildItem -LiteralPath $odsSrc -Directory | Select-Object -First 1).FullName
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\install.ps1
```

Prerequisites: Docker must be installed and running. On Windows, use Docker Desktop with the WSL2 backend enabled and run the block in a normal, non-Administrator PowerShell window.

The hosted Linux/macOS endpoint proxies the current bootstrap from repository `main`

.
Reviewed merges reach it automatically after edge-cache refresh. `ODS_REF`

selects a compatible repository checkout. See
Installer Trust to inspect the script or install
a stable release or audited commit manually.

Windows users should not run the `curl ... | bash`

command from PowerShell. The PowerShell block above downloads the source ZIP and runs the same Windows installer used by the clone-based workflow. For more detail, see the Windows Quickstart.

After install, open **http://localhost:3000** and start chatting.

Uninstall later with the matching platform command:

```
cd ~/ods
./ods-uninstall.sh --force
```

```
$installDir = "$env:USERPROFILE\ods"
cd $installDir
.\ods.ps1 uninstall --force
```

Windows recovery note: if the runtime folder is partial and `.\ods.ps1`

is missing, run the same command from a source checkout as `.\ods\installers\windows\ods.ps1 uninstall --force`

. It removes Docker resources labelled as the ODS compose project before removing the runtime directory.


API endpoint:Linux Docker installs expose llama-server onhttp://localhost:11434by default (`OLLAMA_PORT`

) while containers use`llama-server:8080`

. macOS native Metal and Windows native/Lemonade paths usehttp://localhost:8080unless overridden. Open WebUI stays onhttp://localhost:3000.


No GPU?ODS also runs in cloud mode — same full stack, powered by OpenAI/Anthropic/Together APIs instead of local inference:./install.sh --cloud


Port conflicts?Every port is configurable via environment variables. See`.env.example`

for the full list, or override at install time:WEBUI_PORT=9090 ./install.sh

**New here?** Read the Friendly Guide or listen to the audio version — a complete walkthrough of what ODS is, how it works, and how to make it your own. No technical background needed.

| Question | Answer |
|---|---|
What is it? |
A local AI server stack for your own hardware, with a one-command Linux/macOS installer and a PowerShell installer for Windows. |
Who is it for? |
People who want private AI at home, in a lab, or on a workstation without hand-wiring a dozen services. |
What do I get? |
Local inference, Open WebUI chat, a control dashboard, voice, agents, workflows, RAG, search, image generation, privacy tools, observability, and developer tools. |
What does it run on? |
Linux, Windows with WSL2/Docker Desktop, and macOS Apple Silicon. |
Is cloud required? |
No. Local mode is the default; cloud and hybrid API modes are optional. |

| If you know... | ODS adds... |
|---|---|
Ollama / llama.cpp |
The surrounding server stack: chat, dashboard, voice, RAG, workflows, agents, privacy, and service management. |
Open WebUI |
A full installer and control plane around Open WebUI, plus pre-wired local services. |
AnythingLLM |
Broader local AI appliance behavior beyond RAG: inference, chat, voice, workflows, image generation, and ops. |
n8n self-hosted AI starter kits |
Workflow automation as one part of a larger private AI server. |


Current Platform Support

Platform Status Linux(NVIDIA + AMD + Intel Arc)Supported— install and run todayWindows(NVIDIA + AMD)Supported— install and run todaymacOS(Apple Silicon)Supported— install and run today

Tested Linux distros:Ubuntu 24.04/22.04, Debian 12, Linux Mint 21.3, Fedora 41+, Rocky Linux 9, Arch Linux, Manjaro, CachyOS, and openSUSE Tumbleweed. Other distros using apt, dnf, pacman, or zypper should also work — open an issue if yours doesn't.

Release validation:Operational changes run through a release-grade gate that covers zero-prereq bootstrap, clean installs, product behavior, full-model capabilities, lifecycle recovery, and User Green. See Release Validation and the Validation Matrix.

Windows:Requires Docker Desktop with WSL2 backend. NVIDIA GPUs use Docker GPU passthrough; AMD Strix Halo runs through the platform-specific accelerated path documented in the Windows installer and support matrix.

macOS:Requires Apple Silicon (M1+) and Docker Desktop. llama-server runs natively with Metal GPU acceleration; all other services run in Docker.See the Support Matrix for supported platform claims and the Validation Matrix for the layered test surface used to test those claims.


A handful of companies control the vast majority of global AI traffic — and with it, your data, your costs, and your uptime. Every query you send to a centralized provider is business intelligence you don’t own, running on infrastructure you don’t control, priced on terms you can’t negotiate.

If AI is becoming critical infrastructure, it shouldn’t be rented. Self-hosting local AI should be a sovereign human right, not a career choice.

Because running your own AI shouldn't require a CS degree and a weekend of debugging CUDA drivers. Right now, setting up local AI means stitching together a dozen projects, writing Docker configs from scratch, and praying everything talks to each other. Most people give up and go back to paying OpenAI.

We built ODS so you don't have to.

**One command**— detects your GPU, picks the right model, generates credentials, launches everything**Chatting in under 2 minutes**— bootstrap mode gives you a working model instantly while your full model downloads in the background**Full service stack, pre-wired**— chat, agents, voice, workflows, search, RAG, image generation, privacy tools, observability, and developer tools. All talking to each other out of the box**Fully moddable**— every service is an extension. Drop in a folder, run`ods enable`

, done

**Manual install (Linux)**

```
git clone https://github.com/Osmantic/ODS.git
cd ODS/ods
./install.sh
```

**Windows (PowerShell)**

Requires Docker Desktop with WSL2 backend enabled.
**Install Docker Desktop first and make sure it is running before you start.**

Open a normal **PowerShell** session and run:

```
$ProgressPreference = "SilentlyContinue"
$odsSrc = Join-Path $env:TEMP ("ods-install-" + [guid]::NewGuid().ToString("N"))
$odsZip = Join-Path $odsSrc "ods-main.zip"
New-Item -ItemType Directory -Path $odsSrc | Out-Null
Invoke-WebRequest "https://github.com/Osmantic/ODS/archive/refs/heads/main.zip" -OutFile $odsZip
Expand-Archive -LiteralPath $odsZip -DestinationPath $odsSrc -Force
cd (Get-ChildItem -LiteralPath $odsSrc -Directory | Select-Object -First 1).FullName
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\install.ps1
```

The

`Set-ExecutionPolicy`

command allows the installer script to run in the current session. It does not change your system-wide policy. Running as Administrator is not recommended for the installer because user-level paths such as`.opencode`

,`data/`

, and`.env`

can be created with admin-owned permissions.

The installer detects your GPU, picks the right model, generates credentials, starts all services, and creates a Desktop shortcut to the Dashboard. Manage from the runtime directory with `.\ods.ps1 status`

; uninstall with `.\ods.ps1 uninstall --force`

.

**macOS (Apple Silicon)**

Requires Apple Silicon (M1+) and Docker Desktop.
**Install Docker Desktop first and make sure it is running before you start.**

```
git clone https://github.com/Osmantic/ODS.git
cd ODS/ods
./install.sh
```

The installer detects your chip, picks the right model for your unified memory, launches llama-server natively with Metal acceleration, and starts all other services in Docker. Manage with `./ods-macos.sh status`

.

See the macOS Quickstart for details.

**Open WebUI**— full-featured chat interface with conversation history, web search, document upload, and 30+ languages**llama-server**— high-performance LLM inference with continuous batching, auto-selected for your GPU; Linux Docker host API defaults to`localhost:11434`

, native macOS/Windows paths use`localhost:8080`

, and container API runs on`8080`

**LiteLLM**— API gateway supporting local/cloud/hybrid modes**TEI Embeddings**— text embedding service for RAG and search workflows

**Whisper**— speech-to-text**Kokoro**— text-to-speech

**Hermes Agent**— default local-first autonomous/browser agent with memory, skills, and a magic-link-gated proxy**OpenClaw**— deprecated legacy autonomous agent, still opt-in during the migration window**n8n**— workflow automation with 400+ integrations (Slack, email, databases, APIs)**APE**— Agent Policy Engine for auditing and governing autonomous tool calls**OpenCode**— browser-based AI coding assistant wired to the local stack**Memory Shepherd**— host/systemd helper for agent memory lifecycle management

**Qdrant**— vector database for retrieval-augmented generation (RAG)**SearXNG**— self-hosted web search (no tracking)**Perplexica**— deep research engine**Brave Search**— optional paid Brave Search API integration

**ComfyUI**— node-based image generation

**Privacy Shield**— PII scrubbing proxy for API calls**Dashboard**— real-time GPU metrics, service health, model management**Dashboard API**— service health, setup, status, metrics, and management API behind the dashboard**Token Spy**— token usage monitor for local and proxied LLM traffic**Langfuse**— optional LLM observability and tracing

The installer detects your GPU and first assigns a deterministic hardware tier. Linux and macOS then run the versioned catalog selector (`ods/scripts/select-model.py`

), while Windows uses the PowerShell catalog selector in `ods/installers/windows/lib/tier-map.ps1`

; both read `ods/config/model-library.json`

to choose the best installable GGUF for the detected memory envelope. The final choice is written to `.env`

as `LLM_MODEL`

, `GGUF_FILE`

, `MAX_CONTEXT`

, and `MODEL_RECOMMENDATION_*`

.

`MODEL_PROFILE=qwen`

is the default non-Gemma catalog profile, so the effective pick can be Qwen, Phi, or DeepSeek depending on what fits best. `MODEL_PROFILE=gemma4`

forces Gemma 4 where available, and `MODEL_PROFILE=auto`

uses Gemma 4 on NVIDIA, Apple Silicon, and Intel Arc tiers. Override tier selection with `./install.sh --tier 3`

; override the model family with `MODEL_PROFILE=gemma4 ./install.sh`

or `MODEL_PROFILE=auto ./install.sh`

.

When Hermes is enabled, which is the default agent path, installers keep the first-run bootstrap model at a 64K context floor and promote the full local model context to 128K where the selected model supports it. That avoids Hermes's hard 64K minimum while preserving the under-2-minute first chat experience. The examples below are current catalog-selector outputs for common hardware envelopes; exact installs can differ with detected VRAM/RAM, host architecture, existing downloads, or explicit profile overrides. Throughput still needs a local benchmark after first launch.

| Tier / envelope | Current default catalog pick | Context | Example hardware |
|---|---|---|---|
| 0 / 8 GB CPU fallback | Qwen3.5 2B (Q4_K_M) | 8K | Low-RAM CPU-only |
| 1 / 8 GB discrete VRAM | Qwen3.5 9B (Q4_K_M) | 32K | RTX 4060, RTX 3060 12GB |
| 2 / 12 GB discrete VRAM | Phi-4 14B (Q4_K_M) | 16K | RTX 4070-class cards |
| 3 / 24 GB discrete VRAM | Qwen3.5 27B (Q4_K_M) | 32K | RTX 4090, A6000 |
| 4 / 48 GB discrete VRAM | DeepSeek R1 Distill Llama 70B (Q4_K_M) | 32K | A6000 Ada, L40S |
| NV_ULTRA / 90+ GB amd64 discrete VRAM | Qwen3 Coder Next (Q4_K_M) | 128K | Multi-GPU A100/H100 |
| NV_ULTRA / 90+ GB arm64 unified memory | Qwen3.6 35B-A3B (UD-Q4_K_M) | 128K | DGX Spark / GB10-class hosts |

| Tier / envelope | Current default catalog pick | Context | Hardware |
|---|---|---|---|
| SH_COMPACT / 64 GB unified RAM | Qwen3.6 35B-A3B (UD-Q4_K_M) | 128K | Ryzen AI MAX+ 395 (64GB) |
| SH_LARGE / 96 GB unified RAM | DeepSeek R1 Distill Llama 70B (Q4_K_M) | 32K | Ryzen AI MAX+ 395 (96GB) |
| SH_LARGE / 124 GB unified RAM | Qwen3.6 35B-A3B (UD-Q4_K_M) | 128K | Ryzen AI MAX+ 395 (128GB class) |

The selector routes unified-memory hosts away from Qwen3 Coder Next when that model would otherwise be selected, because current repo policy documents correctness issues on those backends.

| Tier / envelope | Current default catalog pick | Context | Example hardware |
|---|---|---|---|
| 0 / 8 GB unified RAM | Phi-4 Mini (Q4_K_M) | 128K | M1/M2 base (8GB) |
| 1 / 16 GB unified RAM | Qwen3.5 9B (Q4_K_M) | 32K | M4 Mac Mini (16GB) |
| 2 / 32 GB unified RAM | Phi-4 14B (Q4_K_M) | 16K | M4 Pro Mac Mini, M3 Max MacBook Pro |
| 3 / 48 GB unified RAM | Qwen3.5 27B (Q4_K_M) | 32K | M4 Pro (48GB), M2 Max (48GB) |
| 4 / 64+ GB unified RAM | Qwen3.6 35B-A3B (UD-Q4_K_M) | 128K | M2 Ultra Mac Studio, M4 Max (64GB+) |

| Tier / envelope | Current default catalog pick | Context | Example hardware |
|---|---|---|---|
| ARC_LITE / 6 GB discrete VRAM | Phi-4 Mini (Q4_K_M) | 128K | Arc A380 |
| ARC_LITE / 8 GB discrete VRAM | Qwen3.5 9B (Q4_K_M) | 32K | Arc A750 |
| ARC / 16 GB discrete VRAM | Phi-4 14B (Q4_K_M) | 16K | Arc A770 16GB, newer Arc GPUs |

Gemma 4 profile tiers remain in the installer tier maps: E2B on entry hardware, E4B on midrange hardware, 26B-A4B on pro hardware, and 31B on large/ultra hardware.

No waiting for large downloads. ODS uses bootstrap mode by default:

- Downloads a tiny 1.5B model in under a minute
- You start chatting immediately
- The full model downloads in the background
- Hot-swap to the full model when it's ready — zero downtime

The bootstrap model starts with a 64K context window so Hermes can work during the first session. After the background download finishes, ODS swaps to the full model and restores the Hermes/full-model context target.

Skip bootstrap: `./install.sh --no-bootstrap`


The installer picks a model for your hardware, but you can switch anytime:

```
ods model current # What's running now?
ods model list # Show all available tiers
ods model swap T3 # Switch to a different tier
```

If the new model isn't downloaded yet, pre-fetch it first:

```
./scripts/pre-download.sh --tier 3 # Download before switching
ods model swap T3 # Then swap (restarts llama-server)
```

Already have a GGUF you want to use? Drop the single `.gguf`

file in
`data/models/`

, then open Dashboard -> Models and load the local entry. For
older installs or headless maintenance, update `GGUF_FILE`

and `LLM_MODEL`

in
`.env`

, then restart with the CLI:

`ods restart llm`

Or restart the container directly from the installed `ods`

directory:

`docker compose restart llama-server`

Rollback is automatic — if a new model fails to load, ODS reverts to your previous model.

ODS is designed to be modded. Every service is an extension — a folder with a `manifest.yaml`

and a `compose.yaml`

. The dashboard, CLI, health checks, and compose stack all discover extensions automatically.

```
extensions/services/
my-service/
manifest.yaml # Metadata: name, port, health endpoint, GPU backends
compose.yaml # Docker Compose fragment (auto-merged into the stack)
```


```
ods enable my-service # Enable it
ods disable my-service # Disable it
ods list # See everything
```

The installer itself is modular — 19 library modules, a shared service registry, and 13 ordered phases. Want to add a hardware tier, swap a default model, or skip a phase? Start with the installer architecture map so you update the Linux, macOS, Windows, upgrade, and host-agent writers together.

Full extension guide | Installer architecture

The `ods`

CLI manages your entire stack:

```
ods status # Health checks + GPU status
ods list # All services and their state
ods logs llm # Tail logs (aliases: llm, stt, tts)
ods restart [service] # Restart one or all services
ods start / stop # Start or stop the stack
ods mode cloud # Switch to cloud APIs via LiteLLM
ods mode local # Switch back to local inference
ods mode hybrid # Local primary, cloud fallback
ods model swap T3 # Switch to a different hardware tier
ods enable n8n # Enable an extension
ods disable whisper # Disable one
ods config show # View .env (secrets masked)
ods preset save gaming # Snapshot current config
ods preset load gaming # Restore it
```

Other tools get you part of the way. ODS gets you the whole way.

| ODS | Ollama + Open WebUI | LocalAI | |
|---|---|---|---|
Scope |
Full AI stack — inference to agents to workflows | LLM + chat | LLM only |
| One-command install | Everything, auto-configured | LLM + chat only | LLM only |
| Hardware auto-detect + model selection | NVIDIA + AMD Strix Halo + Apple Silicon + Intel Arc + CPU/cloud fallback | No | No |
| AMD APU unified memory support | Platform-specific accelerated backend, selected by installer | Partial (Vulkan) | No |
| Autonomous AI agents | Hermes Agent default; OpenClaw legacy opt-in | No | No |
| Workflow automation | n8n (400+ integrations) | No | No |
| Voice (STT + TTS) | Whisper + Kokoro | No | No |
| Image generation | ComfyUI | No | No |
| RAG pipeline | Qdrant + embeddings | No | No |
| Extension system | Manifest-based, hot-pluggable | No | No |
| Multi-GPU | Yes (NVIDIA) | Partial | Partial |

| Quickstart | Step-by-step install guide with troubleshooting |
| Docs Index | Maintained map for operators, contributors, and reviewers |
| Build On ODS | Forking, custom editions, extension templates, and downstream validation |
| Forkability | How to fork, audit, customize, and independently operate ODS |
| Maintainer Runbook | Release, rollback, validation, and operator continuity guidance for maintainers and forks |
| High-Risk Change Map | Which changes require focused checks, fleet validation, or release-grade gates |
| Headless Setup | QR onboarding, first-boot setup, AP mode, mDNS, and local agent access |
| Support Matrix | Current platform and GPU support status |
| Release Validation | User Green gates and the release-grade fleet/distro validation policy |
| 2.6.0 Release Notes | Current stable release notes, validation receipt, and known validation boundaries |
| Validation Matrix | Sanitized CI, distro lab, and real-hardware fleet release-readiness evidence |
| Validation Reproducibility | How forks and operators can reproduce the validation story on their own hardware |
| Offline And Mirroring | Pinning, mirroring, and preserving release artifacts for independent operation |
| Installer Trust | Inspect-first install paths, ref pinning, and current provenance limits |
| Model Management | Curated and Hugging Face GGUF discovery, verified imports, switching, and recovery |
| Hardware Guide | What to buy, tier recommendations |
| FAQ | Common questions and configuration |
| Extensions | How to add custom services |
| Installer Architecture | Modular installer deep dive |
| Installer Phase Contracts | Phase ownership, idempotency, failure modes, and validation expectations |
| Compose Resolver Contracts | Rules for compose layers, extensions, backends, ports, and mode overlays |
| Changelog | Version history and release notes |
| Contributing | How to contribute |

ODS is built by a growing group of contributors across installers, GPU support, dashboard, security, extensions, docs, and release validation. The README keeps the product overview focused; the long-form credits, upstream acknowledgements, and contributor history live in CONTRIBUTORS.md.

ODS has been recognized by the local AI and developer community, including AMD Featured Developer recognition, selection as a May 2026 AMD Lemonade Developer Challenge winner, and a feature at (Co)nnect: Philly's AI Ecosystem Summit at Pennovation Works.

Apache 2.0 — Use it, modify it, ship it. See LICENSE.