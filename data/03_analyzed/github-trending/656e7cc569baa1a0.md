---
title: handy-computer/transcribe.cpp
source: https://github.com/handy-computer/transcribe.cpp
author: []
published: ''
created: '2026-07-21'
manifest_dates:
- '2026-07-21'
description: 'ggml speech-to-text inference for 16+ model familiestranscribe.cpp C/C++
  speech-to-text inference library. Runs diverse STT model families via GGUF models
  on the ggml runtime, with Metal, Vulkan, and CUDA backends for fast GPU inference
  plus a tinyBLAS-accelerated CPU path. 16 model families and 60+ variants, streaming
  and batch. Every model we publish under handy-computer is numerically verified and
  WER-tested against its reference implementation Supported models: Family Variants
  Docs Parakeet 10 variants: TDT, RNN-T, CTC, TDT+CTC (110M–1.1B) docs/models/parakeet.md
  Canary canary-1b, canary-1b-v2, canary-1b-flash, canary-180m-flash docs/models/canary.md
  Canary-Qwen canary-qwen-2.5b (FastConformer + Qwen3-1.7B SALM) docs/models/canary-qwen-2.5b.md
  Whisper 12 variants (tiny through large-v3-turbo, plus .en siblings) docs/models/whisper.md
  GigaAM gigaam-v3-{e2e-rnnt,e2e-ctc,rnnt,ctc} docs/models/gigaam.md Moonshine moonshine-tiny,
  moonshine-base docs/models/moonshine.md Moonshine Streaming moonshine-streaming-{tiny,small,medium}
  docs/models/moonshine-streaming.md Qwen3-ASR qwen3-asr-0.6b, qwen3-asr-1.7b docs/models/qwen3-asr.md
  Cohere Transcribe cohere-transcribe-03-2026 docs/models/cohere-transcribe-03-2026.md
  SenseVoice sensevoice-small docs/models/sensevoice-small.md FunASR Nano fun-asr-nano-2512,
  fun-asr-mlt-nano-2512 docs/models/fun-asr-nano.md Nemotron Speech Streaming nemotron-speech-streaming-en-0.6b
  docs/models/nemotron-speech-streaming-en-0.6b.md Nemotron 3.5 ASR Streaming nemotron-3.5-asr-streaming-0.6b
  (multilingual, 40 locales) docs/models/nemotron-3.5-asr-streaming-0.6b.md Multitalker
  Parakeet Streaming multitalker-parakeet-streaming-0.6b-v1 (single-speaker ASR path
  only) docs/models/multitalker-parakeet-streaming-0.6b-v1.md Granite Speech 4 / 4.1
  granite-4.0-1b-speech, granite-speech-4.1-2b{,-plus,-nar} docs/models/granite-speech.md
  Voxtral voxtral-mini-3b-2507, voxtral-small-24b-2507 (audio-LLM; transcription +
  translation) docs/models/voxtral.md Voxtral Realtime voxtral-mini-4b-realtime-2602
  (streaming audio-LLM) docs/models/voxtral-realtime.md MedASR medasr (Conformer +
  CTC, English medical-dictation, gated) docs/models/medasr.md MOSS Transcribe-Diarize
  moss-transcribe-diarize (audio-LLM; English + Chinese ASR with inline speaker diarization)
  docs/models/moss-transcribe-diarize.md Per-variant model cards live under docs/models/.
  Build cmake -B build cmake --build build Metal is enabled automatically on Apple
  Silicon. For Vulkan (Linux/Windows): # Ubuntu/Debian sudo apt install build-essential
  cmake libvulkan-dev glslc libopenblas-dev cmake -B build -DTRANSCRIBE_VULKAN=ON
  cmake --build build On Windows, see the complete build guide for Vulkan SDK setup,
  Visual Studio commands, and the short-build-root fallback for unusually deep checkouts.
  For CUDA (Linux + NVIDIA GPU): # requires the CUDA toolkit (nvcc) on PATH cmake
  -B build -DTRANSCRIBE_CUDA=ON cmake --build build libopenblas-dev is optional but
  recommended. It accelerates the host-side decoder ~10-15x. Without it the build
  falls back to a scalar path automatically. tinyBLAS (Justine Tunney''s llamafile_sgemm
  kernels) is on by default. To build the quantization tool: cmake -B build -DTRANSCRIBE_BUILD_TOOLS=ON
  cmake --build build Models Pre-built GGUFs for all supported models are hosted on
  Hugging Face under handy-computer. Each per-model doc (linked in the table above)
  includes direct download links for every quant. Convert from source only if you
  need a different dtype or a checkpoint that isn''t pre-built. Convert to GGUF The
  converter loads directly from NVIDIA''s NeMo checkpoints via ASRModel.from_pretrained.
  Requires uv; the parakeet env ships NeMo and its deps. uv run --project scripts/envs/parakeet
  \ scripts/convert-parakeet.py nvidia/parakeet-tdt-0.6b-v2 This writes models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf
  following the llama.cpp-style <slug>-<QUANT>.gguf naming convention. Pass a local
  .nemo path or extracted directory for offline conversion. Quantize The transcribe-quantize
  tool produces smaller models from the reference GGUF. Available presets: F16, Q8_0,
  Q6_K, Q5_K_M, Q4_K_M. build/bin/transcribe-quantize \ models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf
  \ models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-Q4_K_M.gguf \ --quant Q4_K_M
  Usage build/bin/transcribe-cli -m models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf
  samples/jfk.wav Input must be 16 kHz mono WAV. Use ffmpeg or sox to convert other
  formats: ffmpeg -i input.mp3 -ar 16000 -ac 1 output.wav Bindings Official bindings
  wrap the C API for other languages: Language Path Python bindings/python TypeScript
  / JavaScript bindings/typescript Rust bindings/rust/transcribe-cpp Swift / ObjC
  bindings/swift See docs/bindings.md for how the bindings are generated and kept
  in sync with the header. Tests cd build && ctest Some tests require a real model
  file. Enable them with: cmake -B build -DTRANSCRIBE_BUILD_REAL_MODEL_TESTS=ON cmake
  --build build TRANSCRIBE_PARAKEET_GGUF=path/to/model.gguf ctest --test-dir build
  For the model-family smoke-test, numerical-validation, and benchmark pattern expected
  of new ports, see docs/model-family-testing.md. Sponsors & Supporting Organizations
  Mozilla AI & BiR Program A huge thanks to Mozilla AI and their BiR Program. This
  whole project started out as an idea, not even an implementation direction. It was
  a research project in how to accelerate transcription models across all platforms
  as easily as possible. The BiR program and Davide helped support the research, and
  my eventual direction to choose to implement and inference engine backed by ggml.
  And also experimenting with automated model porting using agentic programming tools.
  Hugging Face Hugging Face provided the project extra storage so we can host all
  of the models which we support. We want to provide canonical references for as many
  models as reasonably possible, the support from Hugging Face helps to enable this.
  Modal Modal helped to provide GPU credits so the project can test and validate the
  projects implementations match the transformers or nemo reference source. This is
  critical to ensuring that we have as close to a production grade inference engine
  that works everywhere. We believe it is critical to have accurate transcriptions
  and the only way to ensure this is through long running WER checks which Modal helps
  to provide. Every model published under handy-computer on hugggingface has had the
  WER checked, so you can trust the results. And if there are any regressions, you
  bet we will be fixing them. Blacksmith Blacksmith provides many of the CI runners
  for this project. That helps to keep transcribe.cpp well tested and ensure our releases
  are as smooth as possible. The CI is quick and a drop in replacement for the standard
  Github Actions runners. I ran into limits very fast with them and super happy upon
  reaching out to Blacksmith they were able to provide runners for the project. Project
  layout include/transcribe.h Public C API (single header) src/ Library internals
  (C++17) src/arch/parakeet/ Parakeet family implementation src/arch/cohere/ Cohere
  Transcribe family implementation examples/cli/ CLI binary source tools/transcribe-quantize/
  Quantization tool source bindings/ Python, TypeScript, Rust, and Swift bindings
  docs/ Porting and validation guidance scripts/ Python converter + test tooling ggml/
  Vendored ggml (see ggml/UPSTREAM for pinned SHA) src/third_party/miniz/ Vendored
  miniz deflate codec (see its UPSTREAM file) samples/ Test audio files tests/ Unit
  and smoke tests License transcribe.cpp is MIT-licensed. See LICENSE for details.
  Vendored third-party components (ggml, miniz — both MIT) are attributed in THIRD-PARTY-LICENSES.md.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 656e7cc569baa1a0
source_type: community_discussion
tldr: transcribe.cpp 是一个基于 ggml 运行时的 C/C++ 语音转文字推理库，支持 16 个模型家族和 60 多种变体，提供 Metal、Vulkan
  和 CUDA 后端 GPU 加速以及 CPU 路径。
objective_summary: handy-computer 团队发布了 transcribe.cpp，一个基于 ggml 运行时的 C/C++ 语音转文字推理库。该库支持
  Parakeet、Canary、Whisper、Qwen3-ASR 等 16 个模型家族共 60 多种变体，提供 Metal、Vulkan 和 CUDA 三种
  GPU 后端加速和 tinyBLAS 加速的 CPU 路径。项目使用 MIT 许可证开源，所有发布模型均经过数值验证和词错误率测试，并提供了 Python、TypeScript、Rust
  和 Swift 四种语言的官方绑定。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - handy-computer
  - Mozilla AI
  - Hugging Face
  - Modal
  - Blacksmith
  - NVIDIA
  technologies:
  - GGUF
  - ggml
  - STT
  - RNN-T
  - CTC
  - WER
  - Metal
  - Vulkan
  - CUDA
  - tinyBLAS
  key_people:
  - Davide
  - Justine Tunney
key_logic_flow:
- handy-computer 团队发布了 transcribe.cpp，一个基于 ggml 运行时的 C/C++ 语音转文字推理库，支持 16 个模型家族和 60
  多种变体。
- 该库提供 Metal（Apple Silicon）、Vulkan（Linux/Windows）和 CUDA（NVIDIA GPU）三种 GPU 后端加速，以及
  tinyBLAS 加速的 CPU 路径。
- 所有发布在 handy-computer 命名空间下的模型都经过数值验证和词错误率测试，确保与参考实现一致。
- 项目提供了 Python、TypeScript、Rust 和 Swift/ObjC 四种语言的官方绑定，以及 CLI 工具和量化工具。
- 项目由 Mozilla AI 的 BiR 计划资助起步，Hugging Face 提供模型存储支持，Modal 提供 GPU 算力用于 WER 验证。
- 项目使用 MIT 许可证开源，可从 Hugging Face 直接下载预构建的 GGUF 格式模型，也支持从 NVIDIA NeMo 检查点自行转换。
object_mentions:
- object_type: project
  name: handy-computer/transcribe.cpp
  canonical_name: transcribe.cpp
  url: https://github.com/handy-computer/transcribe.cpp
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - transcribe.cpp 是一个基于 ggml 运行时的 C/C++ 语音转文字推理库，支持 Metal、Vulkan 和 CUDA 后端进行 GPU
    推理加速。
  - 该库支持 16 个模型家族共 60 多种变体，涵盖 Parakeet、Canary、Whisper、Qwen3-ASR 等主流语音识别模型。
  - 项目提供了 Python、TypeScript、Rust 和 Swift 四种语言的官方绑定，所有模型均经过数值验证和 WER 测试。
  article_id: 656e7cc569baa1a0
- object_type: project
  name: ggml
  canonical_name: ggml
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - transcribe.cpp 基于 ggml 运行时构建，并内嵌了 ggml 的 vendored 副本作为底层张量计算引擎。
  - ggml 是 transcribe.cpp 的核心推理运行时，项目在 ggml/UPSTREAM 文件中标记了所定制的提交版本。
  article_id: 656e7cc569baa1a0
- object_type: project
  name: bindings/python
  canonical_name: transcribe.cpp Python bindings
  url: https://github.com/handy-computer/transcribe.cpp/tree/main/bindings/python
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - transcribe.cpp 提供了 Python 语言绑定，封装 C API 以便在 Python 环境中调用语音转文字推理功能。
  - 项目同时提供了 TypeScript、Rust 和 Swift 共四种语言的官方绑定，所有绑定通过自动化流程与核心头文件保持同步。
  article_id: 656e7cc569baa1a0
extract_result: success
impact_score:
  score: 7.0
  reason: transcribe.cpp 是语音转文字推理领域的 'llama.cpp 时刻'——它将 16 个模型家族、60+ 变体统一在 ggml 运行时之下，提供
    Metal/Vulkan/CUDA 三后端 GPU 加速和 CPU 路径。此前，Parakeet、Canary、Qwen3-ASR、SenseVoice、Granite
    Speech 等模型各自需要不同的推理框架和依赖，开发者无法在本地高效运行。该项目的意义在于：通过 GGUF 格式标准化和单一 C/C++ 运行时，大幅降低了
    STT 模型本地部署的门槛，类似于 llama.cpp 对 LLM 生态的冲击。虽然不是范式转移级别的突破（底层是 ggml，非全新架构），但它填补了 ggml
    生态在语音领域的空白，并且对所有发布模型做了数值验证和 WER 测试，工程严谨度远超同类项目。综合评分 7.0 分：重要基础设施发布，改变 STT 本地推理格局。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 单一推理引擎统一 16 个 STT 模型家族，本地部署门槛大幅降低
hype_assessment:
  level: low
  reason: 项目 README 和技术文档风格务实、数据翔实，没有出现 '颠覆'、'革命性' 等 PR 词汇。每个模型家族都有独立文档页，提供了精确的变体数量、参数量、链接和量化方案。项目明确标注了赞助方（Mozilla
    AI BiR）和合作方（Hugging Face、Modal），并说明了模型验证方法（数值验证 + WER 测试），无夸大宣传成分。
information_entropy: high
domain_disruption:
  technical_innovation: 在 ggml 运行时之上构建统一的 STT 推理引擎，支持 16 个模型家族的流式和批量推理，覆盖 RNN-T、CTC、TDT、音频
    LLM 等多种架构，通过 Metal/Vulkan/CUDA 三后端实现跨平台 GPU 加速，并提供 tinyBLAS 加速的 CPU 路径。所有模型经过数值精度验证和词错误率测试，确保与参考实现一致。
  business_model: MIT 许可证开源，降低 STT 技术的商业使用门槛；借助 Mozilla AI 的 BiR 计划资助起步，Hugging Face
    提供模型分发，Modal 提供 GPU 算力验证——这种 '基金会资助 + 社区托管 + 云商赞助' 的模式为 AI 基础设施项目提供了可持续的开源路径参考。
engineering_complexity: production_ready
compound_value:
  score: 7.5
  reason: 作为 ggml 生态中首个覆盖 16 个模型家族、60+ 变体的通用 STT 推理引擎，transcribe.cpp 有潜力成为语音转文字领域的
    'llama.cpp'。其核心投资逻辑有三层：(1) 平台锁定效应——一旦开发者基于该库构建应用，迁移成本极高，因为 4 种语言绑定和 3 种 GPU 后端的抽象层形成了技术粘性；(2)
    模型生态飞轮——支持从 Parakeet、Whisper 到 Voxtral、Granite Speech 的全谱系模型，且所有模型经过数值验证和 WER
    测试，这建立了质量信任壁垒；(3) 本地推理趋势——隐私合规和延迟敏感场景（医疗听写、会议转录、边缘设备）正推动 STT 从云端走向本地，transcribe.cpp
    是这一趋势的核心基础设施。但需注意两点风险：一是 whisper.cpp 已占据先发优势，二是 STT 整体市场规模远小于 LLM，限制了复利效应的天花板。综合评分
    7.5，具备成为细分赛道基础设施的潜力。
value_capture_layer: agent_middleware
moat_impact: democratizes_access
key_beneficiaries:
- handy-computer (项目团队)
- NVIDIA
- Apple
- Hugging Face
- ggml 社区
competitive_casualty:
- Deepgram
- AssemblyAI
- Rev.ai
- OpenAI Whisper API
- 云 STT API 提供商
market_opportunities:
- 开发者可基于 transcribe.cpp 构建完全离线的语音转文字桌面/移动应用，在隐私敏感场景（医疗、法律、金融）替代云端 API，显著降低延迟和合规成本
- 项目支持 16 个模型家族，医疗领域可聚焦 MedASR 构建专科听写方案，客服场景可借 MOSS Transcribe-Diarize 实现说话人分离的会议转写，长尾场景有精准落地空间
- 多语言官方绑定（Python/TypeScript/Rust/Swift）降低了跨平台集成门槛，可快速嵌入 Electron 桌面应用、React Native
  移动端或 Swift macOS 原生产品
risk_matrix:
  regulatory: 无
  technological: whisper.cpp 已在本地 STT 领域建立生态和用户习惯优势，transcribe.cpp 需以模型多样性和一键式体验争夺开发者心智；ggml
    生态与 llama.cpp 绑定的风险仍在，若 ggml 架构演进方向变化可能导致迁移成本
  competitive: OpenAI Whisper 生态（whisper.cpp、faster-whisper）已占据大部分本地推理份额，NVIDIA NeMo
    本身提供官方推理方案，transcribe.cpp 作为第三方推理引擎需持续维护模型兼容性以留住用户
  ethical: 本地语音转写降低了监控和窃听工具的开发门槛，易被滥用为未经同意的语音录音分析；跨语言和口音的 WER 差异若未充分披露，可能导致对非标准英语/方言群体的系统性识别偏差
  additional: []
confidence:
  impact: high
  compound: medium
  hype: medium
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: handy-computer/transcribe.cpp
  canonical_name: transcribe.cpp
  url: https://github.com/handy-computer/transcribe.cpp
  positioning: 基于 ggml 运行时的跨平台 C/C++ 语音转文字推理库，支持 16 个模型家族和 Metal/Vulkan/CUDA 三重 GPU
    加速后端。
  technical_signal: 支持 Parakeet、Canary、Whisper、Qwen3-ASR 等 16 个模型家族共 60 多种变体，覆盖从传统
    CTC/RNN-T 到音频 LLM 的全谱系。
  adoption_signal: 获 Mozilla AI BiR 计划资助起步，Hugging Face 提供模型分发，Modal 提供 GPU 算力用于 WER
    验证。
  ecosystem_relevance: 基于 ggml 运行时构建，与 llama.cpp 生态兼容，提供 Python/TypeScript/Rust/Swift
    四种官方绑定可嵌入多语言应用。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: 首个系统化支持十余种模型家族的 ggml 语音推理引擎，其模态化架构和数值验证流程可能成为本地语音推理领域的基础设施级项目。
  risk_notes:
  - 项目仍处于早期阶段，社区规模和贡献者活跃度有待观察。
  - 多模型家族维护成本高，部分模型可用性和许可条款存在不确定性。
  score: 8.0
  article_ids:
  - 656e7cc569baa1a0
  evidence_snippets:
  - transcribe.cpp 是一个基于 ggml 运行时的 C/C++ 语音转文字推理库，支持 Metal、Vulkan 和 CUDA 后端进行 GPU
    推理加速。
  - 该库支持 16 个模型家族共 60 多种变体，涵盖 Parakeet、Canary、Whisper、Qwen3-ASR 等主流语音识别模型。
  - 项目提供了 Python、TypeScript、Rust 和 Swift 四种语言的官方绑定，所有模型均经过数值验证和 WER 测试。
- object_type: project
  name: bindings/python
  canonical_name: transcribe.cpp Python bindings
  url: https://github.com/handy-computer/transcribe.cpp/tree/main/bindings/python
  positioning: transcribe.cpp 的 Python 官方绑定，封装 C API 以便在 Python 环境中调用语音转文字推理功能。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: 与 TypeScript、Rust、Swift 三种绑定通过自动化流程与核心头文件保持同步，降低多语言生态的维护成本。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: Python 绑定的完善程度直接决定 transcribe.cpp 在数据科学和 AI 应用开发者中的采用速度，是项目生态扩展的关键入口。
  risk_notes:
  - 作为子组件，其发展完全依赖主项目 transcribe.cpp 的 API 稳定性和演进节奏。
  score: 5.0
  article_ids:
  - 656e7cc569baa1a0
  evidence_snippets:
  - transcribe.cpp 提供了 Python 语言绑定，封装 C API 以便在 Python 环境中调用语音转文字推理功能。
  - 项目同时提供了 TypeScript、Rust 和 Swift 共四种语言的官方绑定，所有绑定通过自动化流程与核心头文件保持同步。
---

C/C++ speech-to-text inference library. Runs diverse STT model families via GGUF models on the ggml runtime, with Metal, Vulkan, and CUDA backends for fast GPU inference plus a tinyBLAS-accelerated CPU path.

16 model families and 60+ variants, streaming and batch. Every model we publish under `handy-computer`

is numerically verified and WER-tested against its reference implementation

**Supported models:**

| Family | Variants | Docs |
|---|---|---|
| Parakeet | 10 variants: TDT, RNN-T, CTC, TDT+CTC (110M–1.1B) | docs/models/parakeet.md |
| Canary | `canary-1b` , `canary-1b-v2` , `canary-1b-flash` , `canary-180m-flash` |
docs/models/canary.md |
| Canary-Qwen | `canary-qwen-2.5b` (FastConformer + Qwen3-1.7B SALM) |
docs/models/canary-qwen-2.5b.md |
| Whisper | 12 variants (`tiny` through `large-v3-turbo` , plus `.en` siblings) |
docs/models/whisper.md |
| GigaAM | `gigaam-v3-{e2e-rnnt,e2e-ctc,rnnt,ctc}` |
docs/models/gigaam.md |
| Moonshine | `moonshine-tiny` , `moonshine-base` |
docs/models/moonshine.md |
| Moonshine Streaming | `moonshine-streaming-{tiny,small,medium}` |
docs/models/moonshine-streaming.md |
| Qwen3-ASR | `qwen3-asr-0.6b` , `qwen3-asr-1.7b` |
docs/models/qwen3-asr.md |
| Cohere Transcribe | `cohere-transcribe-03-2026` |
docs/models/cohere-transcribe-03-2026.md |
| SenseVoice | `sensevoice-small` |
docs/models/sensevoice-small.md |
| FunASR Nano | `fun-asr-nano-2512` , `fun-asr-mlt-nano-2512` |
docs/models/fun-asr-nano.md |
| Nemotron Speech Streaming | `nemotron-speech-streaming-en-0.6b` |
docs/models/nemotron-speech-streaming-en-0.6b.md |
| Nemotron 3.5 ASR Streaming | `nemotron-3.5-asr-streaming-0.6b` (multilingual, 40 locales) |
docs/models/nemotron-3.5-asr-streaming-0.6b.md |
| Multitalker Parakeet Streaming | `multitalker-parakeet-streaming-0.6b-v1` (single-speaker ASR path only) |
docs/models/multitalker-parakeet-streaming-0.6b-v1.md |
| Granite Speech 4 / 4.1 | `granite-4.0-1b-speech` , `granite-speech-4.1-2b{,-plus,-nar}` |
docs/models/granite-speech.md |
| Voxtral | `voxtral-mini-3b-2507` , `voxtral-small-24b-2507` (audio-LLM; transcription + translation) |
docs/models/voxtral.md |
| Voxtral Realtime | `voxtral-mini-4b-realtime-2602` (streaming audio-LLM) |
docs/models/voxtral-realtime.md |
| MedASR | `medasr` (Conformer + CTC, English medical-dictation, gated) |
docs/models/medasr.md |
| MOSS Transcribe-Diarize | `moss-transcribe-diarize` (audio-LLM; English + Chinese ASR with inline speaker diarization) |
docs/models/moss-transcribe-diarize.md |

Per-variant model cards live under `docs/models/`

.

```
cmake -B build
cmake --build build
```

Metal is enabled automatically on Apple Silicon. For Vulkan (Linux/Windows):

```
# Ubuntu/Debian
sudo apt install build-essential cmake libvulkan-dev glslc libopenblas-dev
cmake -B build -DTRANSCRIBE_VULKAN=ON
cmake --build build
```

On Windows, see the complete build guide for Vulkan SDK setup, Visual Studio commands, and the short-build-root fallback for unusually deep checkouts.

For CUDA (Linux + NVIDIA GPU):

```
# requires the CUDA toolkit (nvcc) on PATH
cmake -B build -DTRANSCRIBE_CUDA=ON
cmake --build build
```

`libopenblas-dev`

is optional but recommended. It accelerates the host-side decoder ~10-15x. Without it the build falls back to a scalar path automatically.

tinyBLAS (Justine Tunney's `llamafile_sgemm`

kernels) is on by default.

To build the quantization tool:

```
cmake -B build -DTRANSCRIBE_BUILD_TOOLS=ON
cmake --build build
```

Pre-built GGUFs for all supported models are hosted on Hugging Face under
`handy-computer`

. Each per-model doc
(linked in the table above) includes direct download links for every quant.
Convert from source only if you need a different dtype or a checkpoint that
isn't pre-built.

The converter loads directly from NVIDIA's NeMo checkpoints via
`ASRModel.from_pretrained`

. Requires uv;
the parakeet env ships NeMo and its deps.

```
uv run --project scripts/envs/parakeet \
scripts/convert-parakeet.py nvidia/parakeet-tdt-0.6b-v2
```

This writes `models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf`

following
the llama.cpp-style `<slug>-<QUANT>.gguf`

naming convention. Pass a local
`.nemo`

path or extracted directory for offline conversion.

The `transcribe-quantize`

tool produces smaller models from the
reference GGUF. Available presets: `F16`

, `Q8_0`

, `Q6_K`

, `Q5_K_M`

,
`Q4_K_M`

.

```
build/bin/transcribe-quantize \
models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf \
models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-Q4_K_M.gguf \
--quant Q4_K_M
```

`build/bin/transcribe-cli -m models/parakeet-tdt-0.6b-v2/parakeet-tdt-0.6b-v2-F32.gguf samples/jfk.wav`

Input must be 16 kHz mono WAV. Use `ffmpeg`

or `sox`

to convert other formats:

`ffmpeg -i input.mp3 -ar 16000 -ac 1 output.wav`

Official bindings wrap the C API for other languages:

| Language | Path |
|---|---|
| Python | bindings/python |
| TypeScript / JavaScript | bindings/typescript |
| Rust | bindings/rust/transcribe-cpp |
| Swift / ObjC | bindings/swift |

See `docs/bindings.md`

for how the bindings are generated
and kept in sync with the header.

`cd build && ctest`

Some tests require a real model file. Enable them with:

```
cmake -B build -DTRANSCRIBE_BUILD_REAL_MODEL_TESTS=ON
cmake --build build
TRANSCRIBE_PARAKEET_GGUF=path/to/model.gguf ctest --test-dir build
```

For the model-family smoke-test, numerical-validation, and benchmark
pattern expected of new ports, see
`docs/model-family-testing.md`

.

A huge thanks to Mozilla AI and their BiR Program. This whole project started out as an idea, not even an implementation direction. It was a research project in how to accelerate transcription models across all platforms as easily as possible. The BiR program and Davide helped support the research, and my eventual direction to choose to implement and inference engine backed by ggml. And also experimenting with automated model porting using agentic programming tools.

Hugging Face provided the project extra storage so we can host all of the models which we support. We want to provide canonical references for as many models as reasonably possible, the support from Hugging Face helps to enable this.

Modal helped to provide GPU credits so the project can test and validate the projects implementations match the transformers or nemo reference source. This is critical to ensuring that we have as close to a production grade inference engine that works everywhere. We believe it is critical to have accurate transcriptions and the only way to ensure this is through long running WER checks which Modal helps to provide. Every model published under handy-computer on hugggingface has had the WER checked, so you can trust the results. And if there are any regressions, you bet we will be fixing them.

Blacksmith provides many of the CI runners for this project. That helps to keep transcribe.cpp well tested and ensure our releases are as smooth as possible. The CI is quick and a drop in replacement for the standard Github Actions runners. I ran into limits very fast with them and super happy upon reaching out to Blacksmith they were able to provide runners for the project.

```
include/transcribe.h Public C API (single header)
src/ Library internals (C++17)
src/arch/parakeet/ Parakeet family implementation
src/arch/cohere/ Cohere Transcribe family implementation
examples/cli/ CLI binary source
tools/transcribe-quantize/ Quantization tool source
bindings/ Python, TypeScript, Rust, and Swift bindings
docs/ Porting and validation guidance
scripts/ Python converter + test tooling
ggml/ Vendored ggml (see ggml/UPSTREAM for pinned SHA)
src/third_party/miniz/ Vendored miniz deflate codec (see its UPSTREAM file)
samples/ Test audio files
tests/ Unit and smoke tests
```


transcribe.cpp is MIT-licensed. See LICENSE for details. Vendored third-party components (ggml, miniz — both MIT) are attributed in THIRD-PARTY-LICENSES.md.