---
title: interviewstreet/hiring-agent
source: https://github.com/interviewstreet/hiring-agent
author: []
published: ''
created: '2026-06-25'
description: 'AI agent to evaluate and score resumes.Hiring Agent Resume-to-Score
  pipeline that extracts structured data from PDFs, enriches with GitHub signals,
  and outputs a fair, explainable evaluation. Contents Overview Architecture Installation
  and Setup Prerequisites Quick setup with pip Ollama models Configuration How it
  works CLI usage Directory layout Provider details Contributing License Overview
  Hiring Agent parses a resume PDF to Markdown, extracts sectioned JSON using a local
  or hosted LLM, augments the data with GitHub profile and repository signals, then
  produces an objective evaluation with category scores, evidence, bonus points, and
  deductions. You can run fully local with Ollama or use Google Gemini. Architecture
  Flow pymupdf_rag.py converts PDF pages to Markdown-like text. pdf.py calls the LLM
  per section using Jinja templates under prompts/templates. github.py fetches profile
  and repos, classifies projects, and asks the LLM to select the top 7. evaluator.py
  runs a strict-scored evaluation with fairness constraints. score.py orchestrates
  everything end to end and writes CSV when development mode is on. Key modules models.py
  Pydantic schemas and LLM provider interfaces. llm_utils.py Provider initialization
  and response cleanup. transform.py Normalization from loose LLM JSON to JSON Resume
  style. prompts/ All Jinja templates for extraction and scoring. Installation and
  Setup Prerequisites Python 3.11+ The repository pins .python-version to 3.11.13.
  One LLM backend (either of them) Ollama for local models Install from the official
  site, then run ollama serve. Google Gemini if you have an API key, get it from here.
  Quick setup with pip $ git clone https://github.com/interviewstreet/hiring-agent
  $ cd hiring-agent $ python -m venv .venv # Linux or macOS $ source .venv/bin/activate
  # Windows # .venv\Scripts\activate $ pip install -r requirements.txt Ollama Models
  Pull the model you want to use. For example: $ ollama pull gemma3:4b If you want
  different results, you can pull other models such as: # For higher system configuration
  $ ollama pull gemma3:12b # For lower system configuration $ ollama pull gemma3:1b
  Configuration Copy the template and set your environment variables. $ cp .env.example
  .env Environment variables Variable Values Description LLM_PROVIDER ollama or gemini
  Chooses provider. Defaults to Ollama. DEFAULT_MODEL for example gemma3:4b or gemini-2.5-pro
  Model name passed to the provider. GEMINI_API_KEY string Required when LLM_PROVIDER=gemini.
  GITHUB_TOKEN optional Inherits from your shell environment, improves GitHub API
  rate limits. Provider mapping lives in prompt.py and models.py. The config.py file
  has a single flag: # config.py DEVELOPMENT_MODE = True # enables caching and CSV
  export You can leave it on during iteration. See the next section for details. How
  it works 1) PDF extraction pymupdf_rag.py and pdf.py read the PDF using PyMuPDF
  and convert pages to Markdown-like text. The to_markdown routine handles headings,
  links, tables, and basic formatting. 2) Section parsing with templates prompts/templates/*.jinja
  define strict instructions for each section Basics, Work, Education, Skills, Projects,
  Awards. pdf.PDFHandler calls the LLM per section and assembles a JSONResume object
  (see models.py). 3) GitHub enrichment github.py extracts a username from the resume
  profiles, fetches profile and repos, and classifies each project. It asks the LLM
  to select exactly 7 unique projects with a minimum author commit threshold, favoring
  meaningful contributions. 4) Evaluation evaluator.py uses templates that encode
  fairness and scoring rules. Scores include open_source, self_projects, production,
  and technical_skills, plus bonus and deductions, then an explanation for evidence.
  5) Output and CSV export score.py prints a readable summary to stdout. When DEVELOPMENT_MODE=True
  it creates or appends a resume_evaluations.csv with key fields, and caches intermediate
  JSON under cache/. CLI usage End to end scoring Provide a path to a resume PDF.
  $ python score.py /path/to/resume.pdf What happens: If development mode is on, the
  PDF extraction result is cached to cache/resumecache_<basename>.json. If a GitHub
  profile is found in the resume, repositories are fetched and cached to cache/githubcache_<basename>.json.
  The evaluator prints a report and, in development mode, appends a CSV row to resume_evaluations.csv.
  Directory layout . ├── .env.example ├── .python-version ├── config.py ├── evaluator.py
  ├── github.py ├── llm_utils.py ├── models.py ├── pdf.py ├── prompt.py ├── prompts/
  │ ├── template_manager.py │ └── templates/ │ ├── awards.jinja │ ├── basics.jinja
  │ ├── education.jinja │ ├── github_project_selection.jinja │ ├── projects.jinja
  │ ├── resume_evaluation_criteria.jinja │ ├── resume_evaluation_system_message.jinja
  │ ├── skills.jinja │ ├── system_message.jinja │ └── work.jinja ├── pymupdf_rag.py
  ├── requirements.txt ├── score.py └── transform.py Provider details Ollama Set LLM_PROVIDER=ollama
  Set DEFAULT_MODEL to any pulled model, for example gemma3:4b The provider wrapper
  in models.OllamaProvider calls ollama.chat Gemini Set LLM_PROVIDER=gemini Set DEFAULT_MODEL
  to a supported Gemini model, for example gemini-2.0-flash Provide GEMINI_API_KEY
  The wrapper in models.GeminiProvider adapts responses to a unified format Contributing
  Please read the CONTRIBUTING.md for detailed guidelines on filing issues, proposing
  changes, and submitting pull requests. Key principles include: Keep prompts declarative
  and provider-agnostic. Validate changes with a couple of real resumes under different
  providers. Add or adjust unit-free smoke tests that call each stage with minimal
  inputs. License MIT © HackerRank'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 55bda1aed5a8659b
source_type: community_discussion
tldr: HackerRank 在 GitHub 上开源了 hiring-agent 简历评分流水线，可将 PDF 简历解析为结构化数据，结合 GitHub 信号进行多维度客观评估，支持
  Ollama 本地模型和 Google Gemini 两种 LLM 后端。
objective_summary: HackerRank（GitHub 组织 interviewstreet）开源了 hiring-agent 项目，这是一个从
  PDF 简历到评分的端到端流水线。该工具使用 PyMuPDF 将简历 PDF 转换为 Markdown 文本，通过 Ollama 本地模型或 Google Gemini
  等 LLM 按章节提取结构化 JSON 数据，再从简历中获取 GitHub 用户名并抓取仓库信号，最后基于开源贡献、自有项目、生产经验和技术能力四个维度给出可解释的评分结果，附带加分项和扣分项以及证据说明。项目采用
  MIT 许可证，支持开发模式下的缓存与 CSV 导出。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - HackerRank
  - interviewstreet
  technologies:
  - Ollama
  - Google Gemini
  - PyMuPDF
  - Jinja
  - LLM
  key_people: []
key_logic_flow:
- HackerRank 开源了 hiring-agent 项目，这是一个将 PDF 简历解析为结构化评分结果的端到端流水线工具。
- 系统使用 PyMuPDF 将简历 PDF 转换为 Markdown 格式文本，再通过 LLM 按基础信息、工作经历、教育背景、技能、项目、奖项等章节提取结构化
  JSON 数据。
- 工具支持 Ollama 本地模型和 Google Gemini 两种 LLM 后端，用户通过环境变量 LLM_PROVIDER 和 DEFAULT_MODEL
  切换配置。
- 从简历中提取 GitHub 用户名后，系统获取用户资料和仓库信息，并要求 LLM 选择恰好 7 个有意义的项目进行评分。
- 评估器基于开放源代码贡献、自有项目、生产经验和技术能力四个维度给出评分，并附带加分项和扣分项以及解释证据。
- 结果以可读摘要输出到控制台，在开发模式下还会追加 CSV 行并缓存 PDF 解析结果和 GitHub 数据。
extract_result: success
object_mentions:
- object_type: project
  name: interviewstreet/hiring-agent
  canonical_name: interviewstreet/hiring-agent
  url: https://github.com/interviewstreet/hiring-agent
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Hiring Agent 是一个将 PDF 简历解析为结构化 JSON 的端到端流水线，支持本地 Ollama 或云端 Google Gemini 作为 LLM
    后端。
  - 项目通过 GitHub 信号对候选人进行客观评估，基于开源贡献、自有项目、生产经验和技术能力四个维度输出可解释的评分结果。
  - 该项目由 HackerRank 在 GitHub 上以 MIT 许可证开源，仓库地址为 github.com/interviewstreet/hiring-agent。
  article_id: 55bda1aed5a8659b
- object_type: product
  name: Ollama
  canonical_name: Ollama
  url: https://ollama.ai
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Ollama 是 hiring-agent 支持的本地 LLM 后端之一，用户可通过设置 LLM_PROVIDER=ollama 并指定模型名称来使用本地模型。
  - 系统支持拉取 gemma3:4b、gemma3:12b 或 gemma3:1b 等多种本地模型，通过 OllamaProvider 封装调用 ollama.chat
    API。
  article_id: 55bda1aed5a8659b
- object_type: product
  name: Google Gemini
  canonical_name: Google Gemini
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - Google Gemini 是 hiring-agent 支持的云端 LLM 后端，用户需设置 LLM_PROVIDER=gemini 并提供 GEMINI_API_KEY
    来使用。
  - 支持的 Gemini 模型包括 gemini-2.0-flash 等，通过 GeminiProvider 适配器将响应统一为与 Ollama 一致的格式。
  article_id: 55bda1aed5a8659b
impact_score:
  score: 5.5
  reason: HackerRank 开源 hiring-agent 是一个实用工具，但并非行业范式转移。其价值在于将 AI 简历筛选流程透明化、可解释化，并且支持本地部署（Ollama）避免数据隐私问题。然而，AI
    简历解析和评分在商业领域已有成熟产品（如 HireVue、Pymetrics、甚至 HackerRank 自身平台），开源版本更多是填补透明度和可定制性的空白，而非开辟全新赛道。短期内可能吸引开发者社区关注和中小团队采用，但不足以撼动主流
    ATS 市场格局。
sentiment: positive
developer_sentiment:
  tone: excited
  primary_focus: 本地优先 + 开源透明，可审计的简历评分流水线
hype_assessment:
  level: low
  reason: 项目 README 以技术文档风格撰写，未使用'颠覆'、'革命性'等 PR 词汇。架构描述实事求是，包含明确的局限性和依赖说明（如 Python
    3.11+、Ollama 或 Gemini 后端）。评分机制公开透明且有模板定义，不存在夸大宣传。
information_entropy: high
domain_disruption:
  technical_innovation: 模板化提示词驱动（Jinja）+ 本地 LLM（Ollama）+ GitHub 信号富集的多阶段流水线设计，将简历评估从黑盒评分转变为可解释、可审计的结构化流程。核心创新在于通过声明式提示词模板解耦
    LLM 后端，并设计了开源贡献/个人项目/生产经验/技术技能四个维度的客观评分体系。
  business_model: HackerRank 作为技术招聘平台，将核心评估能力开源，可能通过生态锁定（开发者习惯其评估标准）反哺商业版服务。对中小企业而言，零成本获得可自托管的
    AI 筛查工具，可能降低商业 ATS（如 Greenhouse、Lever）的附加价值，推动招聘技术栈向开源+可审计方向演进。
engineering_complexity: production_ready
compound_value:
  score: 5.5
  reason: HackerRank 开源 hiring-agent 的核心战略意图是将其在编码评估领域的品牌影响力延伸到简历初筛环节，通过开放标准化评分流水线来塑造行业参考实现。短期看，MIT
    许可证和本地 Ollama 支持能迅速获取开发者社区的采用与贡献；中长期看，简历筛选赛道极度拥挤（数十家 AI 招聘初创 + 传统 ATS 内置功能），且
    LLM 驱动的简历评分可复制性极强——差异化主要来自评分质量、公平性设计和 GitHub 信号融合，而非底层技术壁垒。最大的复利瓶颈在于：简历数据是高度私密的，无法跨组织聚合形成数据网络效应，因此评分流水线本身难以产生持续提升的护城河。不过，如果
    hiring-agent 成为 AI 招聘领域的'事实标准流水线'，HackerRank 可借此构建从简历筛选到编码评估的完整工作流闭环，提升其商业产品的转化率和粘性。总体判断：有细分赛道基础设施潜力，但需持续验证能否突破'开源玩具到企业标准'的鸿沟。
value_capture_layer: end_application
moat_impact: democratizes_access
key_beneficiaries:
- HackerRank
- Ollama
- Google
competitive_casualty:
- 传统 ATS 供应商
- AI 简历解析 SaaS 初创公司
- 闭源简历评分工具
market_opportunities:
- HR Tech 创业公司可直接基于该开源管线搭建 AI 简历初筛 SaaS 服务，大幅降低从零开发 PDF 解析和 LLM 评分的成本
- 招聘平台和猎头公司可集成该工具实现候选人简历的自动化客观评分，减少人工筛选工作量，提升招聘效率
- 个人开发者可将该工具改造为简历优化助手，通过了解 AI 评分逻辑来针对性改进简历内容和 GitHub 项目展示
risk_matrix:
  regulatory: 欧盟 AI Act 将招聘筛选 AI 列为高风险应用，需满足透明度、偏见检测和人机协同等合规要求；中国《生成式人工智能服务管理暂行办法》也要求
    AI 招聘工具通过算法备案审查
  technological: PDF 解析质量依赖 PyMuPDF 和 LLM 对复杂排版简历的识别能力，LLM 幻觉可能导致结构化数据错漏；对无 GitHub
    活动或私仓项目的候选人评分偏低，可能产生系统性偏差
  competitive: 主流 ATS 系统（Workday、Greenhouse、Lever）已内建 AI 筛选功能；LinkedIn Recruiter 拥有天然数据优势；开源竞品（如
    OpenResume）也在快速发展
  ethical: AI 简历筛选存在性别、种族、年龄等偏见放大的伦理风险，尤其在评分权重设计不透明时；缺少 GitHub 公共仓库活动的候选人（如非技术岗、资深工程师、女性开发者）可能被系统不公平地低评
  additional: []
confidence:
  impact: medium
  compound: medium
  hype: low
actionable_insight: deep_dive
object_insights:
- object_type: project
  name: interviewstreet/hiring-agent
  canonical_name: interviewstreet/hiring-agent
  url: https://github.com/interviewstreet/hiring-agent
  positioning: HackerRank 开源的端到端简历评分流水线，将 PDF 简历解析为结构化 JSON，结合 GitHub 信号进行多维度客观评估，输出可解释的评分结果。
  technical_signal: 使用 PyMuPDF 将简历 PDF 转换为 Markdown 格式文本，再通过 LLM 按章节提取结构化 JSON 数据，支持
    Ollama 和 Google Gemini 两种 LLM 后端。
  adoption_signal: 由 HackerRank 在 GitHub 上以 MIT 许可证开源，面向技术招聘场景提供可复现、可定制的简历筛选流水线方案。
  ecosystem_relevance: 与 Ollama 和 Google Gemini 等多种 LLM 后端集成，通过 GitHub 公开仓库信号增强简历评估维度，降低简历筛选中的主观偏见。
  target_users: []
  product_signal: null
  market_signal: null
  differentiation: null
  watch_reason: HackerRank 将内部简历筛选逻辑开源为可复现流水线，结合 GitHub 信号和本地 LLM 实现公平、可解释的候选人评估，其对企业招聘流程的影响值得持续跟踪。
  risk_notes:
  - 评估质量高度依赖 LLM 提取和评分的准确性，不同模型输出可能存在偏差。
  - GitHub 信号仅覆盖公开仓库，无法反映闭源项目或企业内部贡献，对部分候选人可能不公。
  score: 7.0
  article_ids:
  - 55bda1aed5a8659b
  evidence_snippets:
  - Hiring Agent 是一个将 PDF 简历解析为结构化 JSON 的端到端流水线，支持本地 Ollama 或云端 Google Gemini 作为 LLM
    后端。
  - 项目通过 GitHub 信号对候选人进行客观评估，基于开源贡献、自有项目、生产经验和技术能力四个维度输出可解释的评分结果。
  - 该项目由 HackerRank 在 GitHub 上以 MIT 许可证开源，仓库地址为 github.com/interviewstreet/hiring-agent。
- object_type: product
  name: Ollama
  canonical_name: Ollama
  url: https://ollama.ai
  positioning: 支持本地部署的大语言模型运行平台，作为 hiring-agent 的本地 LLM 后端，为简历解析和评估提供完全离线的推理能力。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 需要本地运行 AI 模型以保障数据隐私的技术团队
  - 关注离线部署能力的招聘系统用户
  product_signal: 支持 gemma3:4b、gemma3:12b 和 gemma3:1b 等多种本地模型，通过 OllamaProvider 封装调用
    ollama.chat API 提供统一接口。
  market_signal: 作为开源 LLM 运行平台，在 hiring-agent 中作为本地部署的首选后端选项，满足企业对数据不离境的核心诉求。
  differentiation: 无需网络连接和 API Key，完全本地化运行，相比云端 API 方案显著降低使用门槛和隐私风险。
  watch_reason: Ollama 作为 hiring-agent 的本地 LLM 后端，为企业提供无需数据离境的 AI 简历评估能力，其在本地 AI 工具链中的生态位值得持续关注。
  risk_notes:
  - 本地模型能力受限于硬件配置，较小模型的评估精度和稳定性可能低于云端方案。
  score: 5.0
  article_ids:
  - 55bda1aed5a8659b
  evidence_snippets:
  - Ollama 是 hiring-agent 支持的本地 LLM 后端之一，用户可通过设置 LLM_PROVIDER=ollama 并指定模型名称来使用本地模型。
  - 系统支持拉取 gemma3:4b、gemma3:12b 或 gemma3:1b 等多种本地模型，通过 OllamaProvider 封装调用 ollama.chat
    API。
- object_type: product
  name: Google Gemini
  canonical_name: Google Gemini
  url: null
  positioning: Google 提供的云端大语言模型 API 服务，作为 hiring-agent 的云端 LLM 后端，为简历评估提供高性能云端推理能力。
  technical_signal: null
  adoption_signal: null
  ecosystem_relevance: null
  target_users:
  - 需要高性能 LLM 推理能力的招聘系统用户
  - 已具备 Google Cloud 基础设施或愿意支付 API 费用的团队
  product_signal: 支持 gemini-2.0-flash 等模型，通过 GeminiProvider 适配器将响应统一为与 Ollama 一致的格式，方便用户在不同后端间切换。
  market_signal: 作为 hiring-agent 的云端 LLM 后端选项，与本地 Ollama 形成互补，为需要更强模型能力的用户提供替代方案。
  differentiation: 相比 Ollama 的本地方案，Gemini 提供更强大的云端推理模型能力，但需要 API Key 并依赖网络连接，存在服务中断风险。
  watch_reason: Google Gemini 作为 hiring-agent 的云端 LLM 后端，提供比本地模型更强大的推理能力，其在 AI 招聘工具中的实际应用效果值得持续跟踪。
  risk_notes:
  - 依赖 API Key 和网络连接，存在服务中断和数据经第三方处理的隐私风险。
  score: 4.0
  article_ids:
  - 55bda1aed5a8659b
  evidence_snippets:
  - Google Gemini 是 hiring-agent 支持的云端 LLM 后端，用户需设置 LLM_PROVIDER=gemini 并提供 GEMINI_API_KEY
    来使用。
  - 支持的 Gemini 模型包括 gemini-2.0-flash 等，通过 GeminiProvider 适配器将响应统一为与 Ollama 一致的格式。
---

**Resume-to-Score pipeline** that extracts structured data from PDFs, enriches with GitHub signals, and outputs a fair, explainable evaluation.

- Overview
- Architecture
- Installation and Setup
- Configuration
- How it works
- CLI usage
- Directory layout
- Provider details
- Contributing
- License

Hiring Agent parses a resume PDF to Markdown, extracts sectioned JSON using a local or hosted LLM, augments the data with GitHub profile and repository signals, then produces an objective evaluation with category scores, evidence, bonus points, and deductions. You can run fully local with Ollama or use Google Gemini.

|
|
|

-
**Python 3.11+**The repository pins

`.python-version`

to 3.11.13. -
**One LLM backend**(either of them)**Ollama**for local models Install from the official site, then run`ollama serve`

.**Google Gemini**if you have an API key, get it from here.


```
$ git clone https://github.com/interviewstreet/hiring-agent
$ cd hiring-agent
$ python -m venv .venv
# Linux or macOS
$ source .venv/bin/activate
# Windows
# .venv\Scripts\activate
$ pip install -r requirements.txt
```

Pull the model you want to use. For example:

`$ ollama pull gemma3:4b`

If you want different results, you can pull other models such as:

```
# For higher system configuration
$ ollama pull gemma3:12b
# For lower system configuration
$ ollama pull gemma3:1b
```

Copy the template and set your environment variables.

`$ cp .env.example .env`

**Environment variables**

| Variable | Values | Description |
|---|---|---|
`LLM_PROVIDER` |
`ollama` or `gemini` |
Chooses provider. Defaults to Ollama. |
`DEFAULT_MODEL` |
for example `gemma3:4b` or `gemini-2.5-pro` |
Model name passed to the provider. |
`GEMINI_API_KEY` |
string | Required when `LLM_PROVIDER=gemini` . |
`GITHUB_TOKEN` |
optional | Inherits from your shell environment, improves GitHub API rate limits. |

Provider mapping lives in `prompt.py`

and `models.py`

. The `config.py`

file has a single flag:

```
# config.py
DEVELOPMENT_MODE = True # enables caching and CSV export
```

You can leave it on during iteration. See the next section for details.

**1) PDF extraction**

`pymupdf_rag.py`

and`pdf.py`

read the PDF using PyMuPDF and convert pages to Markdown-like text.- The
`to_markdown`

routine handles headings, links, tables, and basic formatting.

**2) Section parsing with templates**

`prompts/templates/*.jinja`

define strict instructions for each section Basics, Work, Education, Skills, Projects, Awards.`pdf.PDFHandler`

calls the LLM per section and assembles a`JSONResume`

object (see`models.py`

).

**3) GitHub enrichment**

`github.py`

extracts a username from the resume profiles, fetches profile and repos, and classifies each project.- It asks the LLM to select exactly 7 unique projects with a minimum author commit threshold, favoring meaningful contributions.

**4) Evaluation**

`evaluator.py`

uses templates that encode fairness and scoring rules.- Scores include
`open_source`

,`self_projects`

,`production`

, and`technical_skills`

, plus bonus and deductions, then an explanation for evidence.

**5) Output and CSV export**

`score.py`

prints a readable summary to stdout.- When
`DEVELOPMENT_MODE=True`

it creates or appends a`resume_evaluations.csv`

with key fields, and caches intermediate JSON under`cache/`

.

Provide a path to a resume PDF.

`$ python score.py /path/to/resume.pdf`

What happens:

- If development mode is on, the PDF extraction result is cached to
`cache/resumecache_<basename>.json`

. - If a GitHub profile is found in the resume, repositories are fetched and cached to
`cache/githubcache_<basename>.json`

. - The evaluator prints a report and, in development mode, appends a CSV row to
`resume_evaluations.csv`

.

```
.
├── .env.example
├── .python-version
├── config.py
├── evaluator.py
├── github.py
├── llm_utils.py
├── models.py
├── pdf.py
├── prompt.py
├── prompts/
│ ├── template_manager.py
│ └── templates/
│ ├── awards.jinja
│ ├── basics.jinja
│ ├── education.jinja
│ ├── github_project_selection.jinja
│ ├── projects.jinja
│ ├── resume_evaluation_criteria.jinja
│ ├── resume_evaluation_system_message.jinja
│ ├── skills.jinja
│ ├── system_message.jinja
│ └── work.jinja
├── pymupdf_rag.py
├── requirements.txt
├── score.py
└── transform.py
```


- Set
`LLM_PROVIDER=ollama`

- Set
`DEFAULT_MODEL`

to any pulled model, for example`gemma3:4b`

- The provider wrapper in
`models.OllamaProvider`

calls`ollama.chat`


- Set
`LLM_PROVIDER=gemini`

- Set
`DEFAULT_MODEL`

to a supported Gemini model, for example`gemini-2.0-flash`

- Provide
`GEMINI_API_KEY`

- The wrapper in
`models.GeminiProvider`

adapts responses to a unified format

Please read the CONTRIBUTING.md for detailed guidelines on filing issues, proposing changes, and submitting pull requests. Key principles include:

- Keep prompts declarative and provider-agnostic.
- Validate changes with a couple of real resumes under different providers.
- Add or adjust unit-free smoke tests that call each stage with minimal inputs.

MIT © HackerRank