---
title: Cloud Agents and Cursor Harness Improvements (2 minute read)
source: https://cursor.com/changelog/08-19-26?utm_source=tldrai
author: []
published: ''
created: '2026-08-21'
manifest_dates:
- '2026-08-21'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 250adbdf9f94f30c
source_type: news_media
tldr: Cursor 发布云代理与 harness 改进：云代理可订阅 PR、Slack 线程或定时任务等事件源自动唤醒，支持 /goal 长期目标、技能自定义模式、子代理独立虚拟机运行，使无人值守代理能自主构建和交付软件。
objective_summary: Cursor 于 2026 年 8 月发布云代理与 Cursor harness 的功能更新。云代理现在可以订阅事件源（包括
  PR、Slack 线程和定时任务），事件发生时自动唤醒，并自动将自己创建的 PR 驱动至完成。子代理可在各自独立的虚拟机上运行，获得隔离的项目副本；用户可通过
  /goal 设定长期目标，通过自定义模式固定技能，并在代理工作中随时发送跟进消息而不打断其操作。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies:
  - Cursor
  technologies:
  - Cloud Agents
  - Cursor Harness
  - Subagents
  - Custom Modes
  key_people: []
key_logic_flow:
- Cursor 发布云代理与 harness 的改进，使无人值守代理可以作为系统自主构建和交付软件，无需在每个循环进行人工干预。
- 云代理现在可以订阅事件源，包括 PR、Slack 线程和定时任务，在事件发生时自动唤醒并开始工作，该订阅功能目前仅对云代理开放。
- 云代理会自动订阅自己创建的 PR 并将其驱动到完成，包括修复 CI 失败和处理机器人评论。
- 任何 skill 都可以作为 Custom Mode 固定在聊天中使用，让代理持续专注于某个技能。
- 子代理现在可以运行在各自独立的虚拟机上，获得隔离的项目副本和干净的云端上下文，可用于测试父代理改动或并行独立修复。
- 用户可以通过 /goal 为代理设定长期目标，让其持续工作直到完全完成，也可搭配自定义模式或 /loop 进行周期性回访，同时可在不打断代理的情况下发送跟进消息。
object_mentions:
- object_type: product
  name: Cursor
  canonical_name: Cursor
  url: https://cursor.com
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章来自 Cursor 官方 changelog，介绍其云代理与 Cursor harness 的功能改进，目标是让代理在每个循环都无需人工干预的情况下自主构建和交付软件。
  article_id: 250adbdf9f94f30c
- object_type: product
  name: Cursor Cloud Agents
  canonical_name: Cursor Cloud Agents
  url: https://cursor.com
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 云代理现在可以订阅事件源（如 PR、Slack 线程或定时任务），在事件发生时自动唤醒，并自动将自己创建的 PR 驱动到完成。
  - 子代理现在可以运行在各自独立的虚拟机上，获得隔离的项目副本和干净上下文，用于并行测试或独立修复。
  article_id: 250adbdf9f94f30c
extract_result: success
---

We're continuing to improve cloud agents and the Cursor harness so always-on agents can operate as a system, building and shipping software on their own without the need for intervention at each loop.

With this release, cloud agents can automatically pick up work in response to events, hold a goal until it's met, and stay on course through long-running sessions.

Cursor can now monitor your PRs, watch a Slack thread, or run scheduled tasks. Cursor Agent subscribes to an event source (a thread or conversation) and wakes when something happens. Subscriptions are available for cloud agents only, for now.

Cloud agents automatically subscribe to PRs they create and drive them to completion, fixing CI and addressing bot comments. In Slack, ask `@cursor check back in an hour and keep going until that feedback is in`

.

Use any skill as a Custom Mode: a skill that stays pinned in the chat. Custom modes keep agents focused on a skill - you can think about it like "always on" skills.

From `/`

, pick a skill and press ⌥⏎ (Mac) or Alt+Enter (Windows), or choose **Use as Mode**.

Subagents can now run on their own virtual machines. Each gets an isolated copy of the project with clean context in its own cloud environment.

Have subagents test the parent agent's changes in fresh environments or swarm independent fixes without collisions. Try `run a swarm of subagents to test my app for bugs, each in its own environment`

.

Use `/goal`

to give the agent a long-lived objective to work towards until it's fully complete.

Try `/goal fix all flaky tests and make CI green`

in a new chat. Pair it with a custom mode to follow a playbook, or `/loop`

for recurring check-ins.

You can now send a message to steer the agent while it's working without interruption. Follow-ups wait for the next tool call instead of cutting the agent off mid-action.

Type a follow-up and hit Send now, or press ⏎ twice.