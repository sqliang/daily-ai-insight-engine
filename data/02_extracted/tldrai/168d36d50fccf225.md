---
title: FlashMemory DeepSeek-V4 Retriever (GitHub Repo)
source: https://github.com/libertywing/FlashMemory-Deepseek-V4?utm_source=tldrai
author: []
published: ''
created: '2026-06-11'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 168d36d50fccf225
source_type: news_media
tldr: FlashMemory DeepSeek-V4 Retriever 是一个轻量级检索器，通过预测 DeepSeek-V4 CSA KV-cache 中哪些块会被后续
  token 关注，将 GPU 上的 KV 缓存降至约 10-15%，同时保持与全注意力基准相当的性能。
objective_summary: libertywing 在 GitHub 上发布了 FlashMemory DeepSeek-V4 Retriever，这是一个针对
  DeepSeek-V4 压缩稀疏注意力（CSA）KV-cache 的轻量级检索器。该检索器根据解码 token 的隐藏状态，预测未来约 64 步会关注的 CSA
  块，仅将得分最高的块保留在 GPU 上，其余块可卸载到 CPU 或磁盘。评估结果显示，该方案仅需保留约 10-15% 的 KV 缓存即可匹配或超越全注意力基线性能。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - DeepSeek
  technologies:
  - CSA
  - KV-cache
  - RoPE
  - YaRN
  - LoRA
  - fp8
  - Walsh-Hadamard
  key_people: []
key_logic_flow:
- FlashMemory DeepSeek-V4 Retriever 是一个轻量级检索器，专门针对 DeepSeek-V4 的压缩稀疏注意力（CSA）KV-cache
  设计，用于优化解码时的显存占用。
- 该检索器根据解码 token 的隐藏状态，预测未来约 64 步会关注的 CSA KV-cache 块，仅将得分最高的块保留在 GPU 上，其余块可卸载到 CPU
  或磁盘。
- 评估结果显示该方案仅需保留约 10-15% 的 KV 缓存即可匹配或超越全注意力基线的性能表现。
- 检索器使用 LoRA 风格的低秩投影（Q_LORA_RANK=2048）将隐藏状态映射到查询空间，经过 RoPE（YaRN）编码和 Walsh-Hadamard
  变换后与压缩键计算得分。
- 模型包含三个独立的 CSA 层（l10、l12、l20），每层有独立权重，推理时通过 max 或 mean 集成产生最终的保留或丢弃决策。
- 项目提供了包含真实检索器权重的 demo 脚本和模拟稀疏解码循环的 toy 示例，但实际的 GPU 与 CPU 之间的 KV 交换引擎不包含在仓库中。
extract_result: success
object_mentions:
- object_type: project
  name: FlashMemory-DeepSeek-V4
  canonical_name: libertywing/FlashMemory-Deepseek-V4
  url: https://github.com/libertywing/FlashMemory-Deepseek-V4
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - FlashMemory DeepSeek-V4 Retriever 是一个轻量级检索器，能够根据解码 token 的隐藏状态预测 CSA KV-cache
    中哪些块会被后续 token 关注。
  - 在评估中该检索器仅需保留约 10-15% 的 KV 缓存即可匹配或超越全注意力基线的性能表现。
  - 项目提供了含真实检索器权重的 demo 脚本和模拟稀疏解码循环的 toy 示例，但实际的 KV 交换引擎不包含在仓库中。
  article_id: 168d36d50fccf225
- object_type: model
  name: DeepSeek-V4
  canonical_name: DeepSeek-V4
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 该检索器专门针对 DeepSeek-V4 的压缩稀疏注意力（CSA）KV-cache 设计，利用其量化后的 fp8 压缩键进行评分。
  - 检索器模型包含三个独立的 CSA 层（l10、l12、l20），对应 DeepSeek-V4 中的不同注意力层。
  article_id: 168d36d50fccf225
---

A lightweight retriever that sparsifies **DeepSeek-V4 Compressed-Sparse-Attention (CSA) KV-cache**.

Given the hidden state of a decode token, the retriever predicts which CSA
KV-cache chunks the next ~64 tokens will attend to. Only the top-scoring chunks
stay resident on the GPU; the rest can be offloaded to CPU/disk. In downstream
evaluation it matches or beats the full-attention baseline while keeping **~10–15%
of the KV cache** on-device.

```
pip install torch safetensors
# Demo with mock inputs
python demo.py --ckpt weights/flashmemory_ds_v4.safetensors
# Toy sparse-decode loop
python toy_flashmemory_inference.py --ckpt weights/flashmemory_ds_v4.safetensors
```

```
from retriever import FlashMemoryRetriever
model = FlashMemoryRetriever.from_checkpoint(
"weights/flashmemory_ds_v4.safetensors", device="cuda"
)
# hidden: [B, 4096] decode-token hidden state
# comp_k: [B, N, 132] uint8 compressed CSA keys
# positions: [B] int64 token positions
# Per-layer sigmoid scores: {"l10": [B,N], "l12": [B,N], "l20": [B,N]}
per_layer = model(hidden, comp_k, positions)
# Cross-layer ensemble (mode="max" or "mean")
scores = model.ensemble(hidden, comp_k, positions, mode="max") # [B, N]
# Boolean keep mask
keep = model.select_topk(hidden, comp_k, positions, top_k=512) # top-K
keep = model.select_topk(hidden, comp_k, positions, threshold=0.5) # threshold
```

Each chunk = `HEAD_DIM + 4 = 132`

`uint8`

bytes:

| Bytes | Type | Meaning |
|---|---|---|
`[:128]` |
`float8_e4m3` |
Quantized key values |
`[128:132]` |
`float32` |
Per-chunk dequant scale |

Dequant: `fp8_values.view(float8_e4m3).float() * scale`

.

See `make_mock_compressed_k()`

in `demo.py`

.

Per CSA layer, scores are computed as:

```
hidden [B, 4096]
→ wq_a (4096 → Q_LORA_RANK)
→ RMSNorm (q_norm_weight, eps=1e-6)
→ wq_b (Q_LORA_RANK → N_HEADS * HEAD_DIM)
→ reshape [B, N_HEADS, HEAD_DIM]
→ RoPE (YaRN, last ROPE_DIM=64 dims, base=160000)
→ Hadamard (normalized Walsh-Hadamard)
→ q [B, N_HEADS, HEAD_DIM]
hidden [B, 4096]
→ weights_proj (4096 → N_HEADS)
→ × weight_scale (= HEAD_DIM^-0.5 * N_HEADS^-0.5)
→ fused_w [B, N_HEADS]
compressed_k [B, N, HEAD_DIM + 4] (uint8)
→ bytes[:HEAD_DIM] viewed as float8_e4m3 → dequant
→ × bytes[HEAD_DIM:] viewed as float32 → k [B, N, HEAD_DIM]
score = sigmoid( sum_heads( relu(k @ q^T) * fused_w ) ) in [0, 1]
```


The checkpoint holds **three independent CSA layers** (`l10`

, `l12`

, `l20`

),
each with its own weights. At inference time per-layer sigmoid scores are
**ensembled per chunk** — `max`

(union, default) or `mean`

— to produce a
single keep/drop decision.

| Param | Value |
|---|---|
`N_HEADS` |
128 |
`HEAD_DIM` |
128 |
`Q_LORA_RANK` |
2048 |
`ROPE_DIM` |
64 (last 64 dims) |
`ROPE_BASE` |
160000 (YaRN) |
`ROPE_FACTOR` |
16 |
`ROPE_ORIGINAL_SEQ_LEN` |
65536 |
`ROPE_BETA_FAST` |
32 |
`ROPE_BETA_SLOW` |
1 |
`RMS_NORM_EPS` |
1e-6 |

A self-contained illustration of how the retriever drives memory recall during decode — the actual control flow used inside DeepSeek-V4-FlashMemory.

```
┌──────────┐ compress & store ┌────────────────────────────┐
│ PREFILL │ historical K/V │ CSA KV-cache (the memory) │
│ (dense │ ───────────────────► │ N compressed chunks, │
│ attn) │ │ each = [132] uint8 fp8-K │
└────┬─────┘ └──────────────┬─────────────┘
│ last hidden state │ scored every 64 steps
▼ │
┌──────────────────────── DECODE LOOP ──────────┼──────────────────────────┐
│ for each decode step t: │ │
│ hidden = toy_decoder.step(token, keep_mask) │ (sparse memory attn) │
│ │ │
│ every RETRIEVAL_INTERVAL (= 64) steps: ▼ │
│ scores[N] = retriever.ensemble(hidden, compressed_k, pos) │
│ keep_mask[N] = top-K (or sigmoid>thresh) of scores │
│ -> unselected chunks masked to -inf in next 64 steps │
└──────────────────────────────────────────────────────────────────────────┘
```


**Prefill (dense).**Short prompt runs through dense memory attention. Its last hidden state seeds the first retrieval cycle.**Decode loop.**Toy decoder produces a`[B, 4096]`

hidden state each step.**Retrieval cycle (every 64 steps).**The real`FlashMemoryRetriever`

scores all`N`

compressed-K chunks, ensembles per-layer scores, selects keep chunks.**Sparse attention.**Unselected chunks' attention logits are set to`-inf`

.

**This toy does NOT**perform real CPU↔GPU KV-cache transfer. The swap engine is internal FlashMemory infrastructure and is not included.**We simulate memory recall by masking attention logits**to`-inf`

. A masked chunk contributes nothing to attention — the same effect as not loading its KV.- The purpose is to make the
**decode-time control flow concrete**.

| IS | IS NOT |
|---|---|
| Minimal torch-only illustration of memory recall | A runnable DeepSeek-V4 |
| Uses the real retriever weights & scoring math | Production KV swap engine |
| Pedagogical: shows the control flow | Meaningful text generation |