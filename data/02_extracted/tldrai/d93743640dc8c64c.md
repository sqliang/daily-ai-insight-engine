---
title: Production Infrastructure for AI Agents (19 minute read)
source: https://vercel.com/blog/introducing-eve?utm_source=tldrai
author: []
published: ''
created: '2026-06-19'
description: AI 工程与研究
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: d93743640dc8c64c
source_type: news_media
tldr: Vercel 正式发布 eve，一个专为构建、部署和运维 AI Agent 的生产级基础设施平台。Agent 以目录结构定义，包含模型配置、指令、工具、技能、子代理、渠道和定时任务，支持本地开发、沙箱执行、人工审批、MCP
  连接和内置追踪，可通过 vercel deploy 一键部署。
objective_summary: 2026 年 7 月，Vercel 在其官方博客宣布推出 eve 产品。eve 将 AI Agent 定义为标准化目录结构（agent/），通过
  defineAgent 配置模型与指令，通过 defineTool 和 MCP 连接集成工具与外部服务，支持沙箱代码执行、人工审批（needsApproval）、子代理委托、多渠道部署（Slack
  等）以及基于 OpenTelemetry 的追踪。开发者可使用 eve dev 在本地运行 Agent、通过 defineEval 编写测试套件，最终以 vercel
  deploy 完成生产部署。
event_type: application_landing
epistemic_status: pr_statement
entities:
  companies:
  - Vercel
  technologies:
  - MCP
  - OpenTelemetry
  - Zod
  key_people: []
key_logic_flow:
- Vercel 推出了 eve，这是一个用于构建生产级 AI Agent 的完整基础设施平台，将 Agent 抽象为目录结构（agent/），包含模型配置、指令、工具、技能、子代理、渠道和定时任务等模块。
- Agent 的定义通过 defineAgent API 完成，开发者只需配置模型（如 anthropic/claude-opus-4.8）和指令文件即可启动，每个
  Agent 拥有持久化会话和独立沙箱环境。
- 工具集成采用文件即工具的设计理念，每个 defineTool 文件以文件名为工具名，支持 Zod 输入校验、只读 SQL 执行以及基于条件触发的人工审批（needsApproval）。
- Agent 可通过 defineMcpClientConnection 连接任意 MCP 服务器（如 Linear），实现与外部服务的标准化集成。
- eve 内置了基于 OpenTelemetry 的追踪系统和评估框架（defineEval），开发者可以编写断言测试来验证 Agent 行为，并通过 eve dev
  在本地交互式运行调试。
- 完成开发的 Agent 可通过 vercel deploy 一键部署到生产环境，并分发到多个通信渠道供团队使用。
extract_result: success
object_mentions:
- object_type: product
  name: eve
  canonical_name: Vercel eve
  url: https://vercel.com/blog/introducing-eve
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Vercel 正式推出 eve，这是一个面向 AI Agent 的生产级基础设施平台，提供从开发、测试到部署的全链路能力。
  - Agent 被设计为标准化目录结构（agent/），包含模型配置、指令、工具、技能、子代理、渠道和调度模块，整个 Agent 定义一目了然。
  - eve 内置持久化会话、沙箱执行、人工审批、MCP 连接、多渠道分发以及基于 OpenTelemetry 的追踪和评估系统。
  article_id: d93743640dc8c64c
- object_type: product
  name: eve dev
  canonical_name: eve dev CLI
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 开发者可以通过 eve dev 命令在本地启动 Agent，并在终端 UI 中与其交互，实时观察每一步的工具调用和推理过程。
  article_id: d93743640dc8c64c
- object_type: project
  name: defineAgent
  canonical_name: eve defineAgent API
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - defineAgent 是 eve 的核心 API，用于定义 Agent 的模型选择（如 anthropic/claude-opus-4.8）、指令规则和工具配置，整个
    Agent 定义在一个文件中完成。
  article_id: d93743640dc8c64c
- object_type: project
  name: defineTool
  canonical_name: eve defineTool API
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - defineTool 允许开发者以单个文件定义一个类型安全的工具，文件名即工具名，支持 Zod 输入模式校验和条件触发的人工审批。
  article_id: d93743640dc8c64c
- object_type: project
  name: defineEval
  canonical_name: eve defineEval testing framework
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - defineEval 是 eve 内置的评估框架，开发者可以编写断言测试来验证 Agent 是否调用了正确的工具并遵循了团队的定义规则。
  article_id: d93743640dc8c64c
- object_type: project
  name: defineMcpClientConnection
  canonical_name: eve defineMcpClientConnection API
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - defineMcpClientConnection 用于将 Agent 连接到外部 MCP 服务器（如 Linear），通过标准化协议实现工具和数据的安全访问。
  article_id: d93743640dc8c64c
---

## Link to headingAn agent is a directory

`agent/ agent.ts # the model it runs on instructions.md # who it is tools/ run_sql.ts # what it can do post_chart.ts skills/ revenue-definitions.md # what it knows subagents/ investigator/ # who it delegates to channels/ slack.ts # where it lives schedules/ monday-summary.ts # when it acts on its own`


A data analyst agent, readable at a glance

### Link to headingCreate an eve agent in minutes

```
import { defineAgent } from "eve";
export default defineAgent({ model: "anthropic/claude-opus-4.8",});
```


Configuring the agent and its model in one file

```
You are a senior data analyst. You answer questions about the team's data.
- Prefer exact numbers to hand-waving. If you can compute it, compute it.- State the assumptions behind any number you report (date range, filters, grain).- Use the tools available to you rather than guessing. If you cannot answer from the data, say so plainly.
```


The agent's identity and standing rules, prepended to every model call

## Link to headingWhy we built eve

## Link to headingBatteries included

### Link to headingA durable session for every conversation

### Link to headingA sandbox for every agent

### Link to headingHuman-in-the-loop approvals

### Link to headingSecure connections to tools, data, and services

```
import { defineMcpClientConnection } from "eve/connections";
export default defineMcpClientConnection({ url: "https://mcp.linear.app/sse", description: "Linear workspace: issues, projects, cycles, and comments.", auth: { getToken: async () => ({ token: process.env.LINEAR_API_TOKEN! }), },});
```


A connection to an MCP server, in one file

### Link to heading**The same agent on every channel**

### Link to headingTracing and evals built in

`ai.eve.turn # one span per turn├── ai.streamText # the model call│ └── ai.streamText.doStream└── ai.toolCall # run_sql, with inputs and outputs`


The OpenTelemetry span tree a single turn produces

## Link to heading**Extend an agent one file at a time**

```
import { defineTool } from "eve/tools";import { z } from "zod";import { runReadOnlySql } from "../lib/sample-db";
export default defineTool({ description: "Run a read-only SQL query against the orders and customers tables.", inputSchema: z.object({ sql: z.string().describe("A single read-only SELECT statement."), }), async execute({ sql }) { const { columns, rows } = await runReadOnlySql(sql); return { columns, rows: rows.slice(0, 500), truncated: rows.length > 500 }; },});
```


A typed tool in one file, where the filename becomes the tool name

```
---description: How this team defines revenue. Load before answering any revenue question.---
Revenue is recognized net of refunds, over the subscription term.Weeks are Monday-anchored, in UTC.Exclude trial and internal accounts from every number.
```


A skill in one markdown file, loaded only when the topic comes up

### Link to heading**Add human-in-the-loop approval**

`export default defineTool({ description: "Run a read-only SQL query against the warehouse.", inputSchema: z.object({ sql: z.string() }), needsApproval: ({ toolInput }) => estimateScanGb(toolInput.sql) > 50, async execute({ sql }) { // unchanged },});`


Requiring approval when a query would scan more than 50GB

### Link to heading**Let the agent write its own code**

```
> Break last week's revenue down by region and chart it
⦿ write_file analysis/by_region.py⦿ bash python analysis/by_region.py
Revenue by region for the week of June 1. AMER $2.1M, EMEA $1.6M, APAC$0.5M. Chart saved to analysis/by_region.png.
```


The agent writing and running its own code in its own sandbox

### Link to heading**Delegate work to a subagent**

```
import { defineAgent } from "eve";
export default defineAgent({ description: "Investigates anomalies in the data before the analyst reports them.", model: "anthropic/claude-opus-4.8",});
```


A subagent the analyst can hand work to

## Link to heading**Start and interact with your agent**

### Link to heading**Run the agent locally**

`eve dev`


Starting the agent locally, with a terminal UI to talk to it

```
> What was revenue last week?
⦿ load_skill revenue-definitions⦿ run_sql SELECT date_trunc('week', created_at) ...
Revenue for the week of June 1 was $4.2M net of refunds, up 6% from theprior week.
```


Every step of the run, visible as it happens

### Link to heading**Test the agent with evals**

```
import { defineEval } from "eve/evals";import { includes } from "eve/evals/expect";
export default defineEval({ description: "The analyst answers revenue questions by the team's rules.", async test(t) { await t.send("What was revenue last week?"); t.completed(); t.calledTool("run_sql"); t.check(t.reply, includes("net of refunds")); },});
```


A suite that checks whether the analyst used its tool and followed the team's definitions

## Link to heading**Ship it**

`vercel deploy`


Deploying the agent

## Link to heading**Introduce the agent to your team**