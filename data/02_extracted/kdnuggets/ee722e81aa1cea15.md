---
title: Run Muse Glimmer for Local Vibe Coding with llama.cpp, DFlash, and Pi
source: https://www.kdnuggets.com/run-muse-glimmer-for-local-vibe-coding-with-llama-cpp-dflash-and-pi
author:
- '[[Abid Ali Awan]]'
published: '2026-08-21'
created: '2026-08-22'
manifest_dates:
- '2026-08-22'
- '2026-08-23'
- '2026-08-24'
description: Run Muse Glimmer locally on an RTX 3090 GPU using llama.cpp, DFlash speculative
  decoding, and Pi for fast, private, agentic AI coding.
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: ee722e81aa1cea15
source_type: news_media
tldr: KDnuggets 教程指导在 RTX 3090 上本地运行 Meta 的 Muse Glimmer 30B 模型：用 llama.cpp 加载 DFlash
  草稿模型做投机解码，实测速度约 46–127 tokens/s，并接入 Pi 编码代理实现本地 vibe coding；但作者认为生成 HTML 游戏的质量仍不如
  Qwen3.8-27B。
objective_summary: 该教程介绍如何在配备 RTX 3090 GPU 的本地环境运行 Meta 的 Muse Glimmer 30B 开源模型，用于本地
  AI 编码与智能体工作流。作者从 Hugging Face 下载 16.8 GB 主模型和 1.63 GB 的 DFlash 草稿模型，编译带 CUDA 支持的
  llama.cpp 并启动 llama-server，通过 DFlash 投机解码将生成速度提升至约 46–127 tokens/s。随后安装 Pi 编码代理和
  pi-llama 扩展，使其自动连接本地服务器完成构建、测试与调试。实测中 Muse Glimmer 生成的 HTML 游戏质量不佳，作者认为 Qwen3.8-27B
  在生成可运行 HTML 应用方面仍明显更优。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Meta
  - Hugging Face
  technologies:
  - llama.cpp
  - GGUF
  - CUDA
  - DFlash
  - speculative decoding
  - vibe coding
  key_people: []
key_logic_flow:
- 文章介绍在配备 RTX 3090 GPU 的本地环境运行 Meta 的 Muse Glimmer 30B 开源模型，用于本地 AI 编码与智能体工作流。
- 先从 Hugging Face 下载 16.8 GB 的主模型 muse-glimmer-30B-kquant-17gb.gguf 和 1.63 GB 的 DFlash
  草稿模型 dflash-kquant.gguf。
- 编译带 CUDA 支持的 llama.cpp 后，用 llama-server 同时加载主模型与 DFlash 草稿模型，并启用投机解码参数（--spec-type
  draft-dflash、--spec-draft-n-max 15 等）。
- 作者实测 Muse Glimmer 初始生成速度约 46 tokens/s，长编码任务中可达约 127 tokens/s。
- 实测中 Muse Glimmer 生成的 HTML 游戏质量不佳，作者认为 Qwen3.8-27B 在生成可运行的 HTML 应用和游戏方面明显更好。
- 最后安装 Pi 编码代理与 Hugging Face 的 pi-llama 扩展，使其自动连接本地 llama.cpp 服务器，在终端中完成构建、测试与调试。
object_mentions:
- object_type: model
  name: Muse Glimmer
  canonical_name: meta-models/Muse-Glimmer-30B-GGUF
  url: https://huggingface.co/meta-models/Muse-Glimmer-30B-GGUF
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章指导用户从 Hugging Face 下载 16.8 GB 的 Muse Glimmer 主模型 muse-glimmer-30B-kquant-17gb.gguf，以及
    1.63 GB 的 DFlash 草稿模型 dflash-kquant.gguf，用于本地部署。
  - Muse Glimmer 在本地 AI 社区受到关注，常被拿来与 Qwen 的 27B 级模型对比，在本地编码和智能体工作流中表现更好。
  - 作者实测 Muse Glimmer 初始生成速度约 46 tokens/s，在较长编码任务中可达约 127 tokens/s，但生成的 HTML 游戏效果并不理想。
  article_id: ee722e81aa1cea15
- object_type: project
  name: ggml-org/llama.cpp
  canonical_name: ggml-org/llama.cpp
  url: https://github.com/ggml-org/llama.cpp
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 文章通过 git clone 克隆 ggml-org/llama.cpp 仓库，以 GGML_CUDA=ON 开启 CUDA 支持编译出 llama-server，并链接到
    PATH 供后续调用。
  article_id: ee722e81aa1cea15
- object_type: model
  name: DFlash drafter
  canonical_name: dflash-kquant.gguf
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - DFlash 投机解码通过加载独立的 dflash-kquant.gguf 草稿模型，并配合 --spec-type draft-dflash 与 --spec-draft-n-max
    15 参数，提升 Muse Glimmer 的生成速度。
  article_id: ee722e81aa1cea15
- object_type: product
  name: Pi
  canonical_name: Pi coding agent
  url: https://pi.dev
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - Pi 是文章推荐的本地编码智能体，安装后与 llama.cpp 服务器连接，可在终端中完成构建、测试和调试项目的 vibe coding 流程。
  - 文章通过 curl 安装 Pi 后，再安装 Hugging Face 的 pi-llama 扩展，使 Pi 自动连接到本地服务器的 OpenAI 兼容接口。
  article_id: ee722e81aa1cea15
- object_type: project
  name: huggingface/pi-llama
  canonical_name: huggingface/pi-llama
  url: https://github.com/huggingface/pi-llama
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 文章指导用户运行 pi install git:github.com/huggingface/pi-llama 安装 llama.cpp 扩展，重启终端后该扩展会自动连接到
    http://localhost:8080/v1。
  article_id: ee722e81aa1cea15
- object_type: model
  name: Qwen3.8-27B
  canonical_name: Qwen3.8-27B
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 作者在对比测试中指出，生成可运行的 HTML 应用和游戏方面 Qwen3.8-27B 仍明显优于 Muse Glimmer，尽管后者在本地智能体编码场景中表现不错。
  article_id: ee722e81aa1cea15
extract_result: success
---

# Run Muse Glimmer for Local Vibe Coding with llama.cpp, DFlash, and Pi

Run Muse Glimmer locally on an RTX 3090 GPU using llama.cpp, DFlash speculative decoding, and Pi for fast, private, agentic AI coding.

Muse Glimmer is gaining attention in the local AI community and is being compared with Qwen's 27B-class models. In many cases, it is performing better, especially for local coding and agentic workflows.

Meta looks strong in the open-model space, and with a few more iterations, models like this could start competing closely with proprietary systems. As an AI enthusiast, it's exciting to be able to run this level of AI locally.

In this guide, we will run Muse Glimmer with **llama.cpp**, speed it up with **DFlash**, and connect it to **Pi** for local vibe coding. It will be able to build, test, and debug a project directly from the terminal.

## 1. Downloading Muse Glimmer

First, download the main Muse Glimmer model and its DFlash drafter from Hugging Face.

Install the **Hugging Face CLI**:

```
curl -LsSf https://hf.co/cli/install.sh | bash
echo 'export PATH="/root/.local/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```


Log in:

`hf auth login`


Create a model directory:

`mkdir -p /workspace/muse-glimmer`


Download the **16.8 GB main model**:

```
hf download meta-models/Muse-Glimmer-30B-GGUF \
muse-glimmer-30B-kquant-17gb.gguf \
--local-dir /workspace/muse-glimmer
```


Download the **1.63 GB DFlash drafter**:

```
hf download meta-models/Muse-Glimmer-30B-GGUF \
dflash-kquant.gguf \
--local-dir /workspace/muse-glimmer
```


Both files will be saved in `/workspace/muse-glimmer`

.

## 2. Installing and Running llama.cpp

Next, install llama.cpp with CUDA support and use it to serve Muse Glimmer with the DFlash drafter.

Install and build llama.cpp:

```
cd /workspace
git clone https://github.com/ggml-org/llama.cpp.git
cd llama.cpp
git pull origin master
cmake -B build -DGGML_CUDA=ON -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release -j$(nproc)
ln -sf "$(pwd)/build/bin/llama-server" /root/.local/bin/llama-server
```


Check the installation:

```
git pull origin master
cmake -B build -DGGML_CUDA=ON -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release -j$(nproc)
ln -sf "$(pwd)/build/bin/llama-server" /root/.local/bin/llama-server
```


You should see the installed llama.cpp version and build information.

Now start Muse Glimmer with **DFlash speculative decoding**:

```
llama-server \
-m /workspace/muse-glimmer/muse-glimmer-30B-kquant-17gb.gguf \
-md /workspace/muse-glimmer/dflash-kquant.gguf \
--spec-type draft-dflash \
--spec-draft-n-max 15 \
-ngl all \
--spec-draft-ngl all \
-fa on \
--ctx-size 16384 \
--alias muse \
--host 0.0.0.0 \
--port 8080 \
--jinja
```


Here, llama.cpp loads the main model and the separate DFlash drafter onto the GPU, using speculative decoding to improve generation speed.

## 3. Testing Muse Glimmer

Once the server is running, you can test Muse Glimmer directly through the built-in **llama.cpp Web UI**.

Open:

`http://localhost:8080/`


In my initial testing, I was getting around **46 tokens/second**, which is already quite good.

During longer coding tasks, I saw speeds reach around **127 tokens/second**, making the model feel much faster for agentic coding workflows.

The results were mixed, though. Muse Glimmer created an HTML game for me, but it didn't work particularly well. For this kind of task, I still found **Qwen3.8-27B** noticeably better at producing working HTML apps and games.

## 4. Installing Pi Coding Agent

Next, install **Pi** and connect it to the llama.cpp server running Muse Glimmer.

Install Pi:

`curl -fsSL https://pi.dev/install.sh | sh`


Then install Hugging Face's llama.cpp extension:

`pi install git:github.com/huggingface/pi-llama`


Restart your terminal after installation.

The `pi-llama`

extension automatically connects to:

`http://localhost:8080/v1`