---
title: Software rendering in 500 lines of bare C++
source: https://haqr.eu/tinyrenderer/
author:
- '[[mpweiher]]'
published: '2026-07-23'
created: '2026-07-24'
manifest_dates:
- '2026-07-24'
description: 'Article URL: https://haqr.eu/tinyrenderer/ Comments URL: https://news.ycombinator.com/item?id=49022038
  Points: 284 # Comments: 64'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 31ded297382248be
source_type: community_discussion
tldr: 一篇教育性文章系列，通过从零编写约500行C++代码的纯软件渲染器，来揭示OpenGL、Vulkan、Metal和DirectX等3D图形API的内部工作原理，无需任何第三方图形库。
objective_summary: 该教程作者通过不依赖任何第三方图形库的方式，逐步构建一个完整的软件渲染器，旨在帮助学生理解3D图形API的内部机制。输入为三角化网格3D模型和纹理，输出为渲染图像，程序无图形界面。作者仅提供一个TGA文件处理类作为基础依赖，所有线段和三角形绘制功能均由学生手动实现。学生学习后通常需要10到20小时即可制作出功能完整的渲染器，最终代码约500行并托管在GitHub上。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - OpenGL
  - Vulkan
  - Metal
  - DirectX
  - TGA
  key_people: []
key_logic_flow:
- 该系列文章通过从零编写简化版软件渲染器来演示OpenGL、Vulkan、Metal和DirectX等3D图形API的工作原理。
- 教程要求学生不使用任何第三方图形库，所有绘制功能必须手动实现。
- 输入是三角化网格3D模型和纹理，输出是渲染图像，程序没有图形界面，仅生成图片文件。
- 作者提供的唯一外部依赖是一个TGAImage类，用于处理支持RGB、RGBA和灰度图像的TGA文件格式。
- 初始代码没有任何绘制线段或三角形的内置函数，所有几何绘制算法需学生自行实现。
- 最终完整代码约500行C++，托管在GitHub上供参考，学生通常需10到20小时完成功能渲染器。
object_mentions:
- object_type: project
  name: tinyrenderer
  canonical_name: ssloy/tinyrenderer
  url: https://github.com/ssloy/tinyrenderer
  confidence: medium
  article_role: primary_subject
  evidence_snippets:
  - 该系列文章通过从零编写约500行C++代码的软件渲染器来揭示OpenGL、Vulkan、Metal和DirectX等图形API的内部工作原理。
  - 完整的源代码托管在GitHub上，学生通常需要10到20小时就能制作出功能齐全的渲染器。
  - 该教程不使用任何第三方图形库，所有线段和三角形绘制功能均由学生手动实现。
  article_id: 31ded297382248be
- object_type: project
  name: TGAImage
  canonical_name: TGAImage
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 作者提供的唯一外部依赖是一个TGAImage类，用于处理TGA文件格式，这是支持RGB、RGBA和灰度图像的最简单图像格式之一。
  - 初始代码除了TGA图像的加载和保存功能以及设置单个像素颜色的能力之外，没有任何其他图形绘制函数。
  article_id: 31ded297382248be
extract_result: success
---

# Software rendering in 500 lines of bare C++

In this series of articles, I aim to demonstrate how OpenGL, Vulkan, Metal, and DirectX work by writing a simplified clone from scratch. Surprisingly, many people struggle with the initial hurdle of learning a 3D graphics API. To help with this, I have prepared a short series of lectures, after which my students are able to produce quite capable renderers.

The task is as follows: using no third-party libraries (especially graphics-related ones), we will generate an image like this:

*Warning: This is a training material that loosely follows the structure of modern 3D graphics libraries.
It is a software renderer.
I do not intend to show how to write GPU applications — I want to show how they work.
I firmly believe that understanding this is essential for writing efficient applications using 3D libraries.*

## The starting point

The final code consists of about 500 lines. My students typically require 10 to 20 hours of programming to start producing such renderers. The input is a 3D model composed of a triangulated mesh and textures. The output is a rendereding. There is no graphical interface, the program simply generates an image.

To minimize external dependencies, I provide my students with a single class for handling TGA files — one of the simplest formats supporting RGB, RGBA, and grayscale images. This serves as our foundation for image manipulation. At the beginning, the only available functionality (besides loading and saving images) is the ability to set the color of a single pixel.

There are no built-in functions for drawing line segments or triangles — we will implement all of this manually. While I provide my own source code, written alongside my students, I do not recommend using it directly, as doing the work yourself is essential to understanding the concepts. The complete code is available on github, and you can find the initial source code I provide to my students here. Behold, here is the starting point:

## main.cpp

```
#include "tgaimage.h"
constexpr TGAColor white = {255, 255, 255, 255}; // attention, BGRA order
constexpr TGAColor green = { 0, 255, 0, 255};
constexpr TGAColor red = { 0, 0, 255, 255};
constexpr TGAColor blue = {255, 128, 64, 255};
constexpr TGAColor yellow = { 0, 200, 255, 255};
int main(int argc, char** argv) {
constexpr int width = 64;
constexpr int height = 64;
TGAImage framebuffer(width, height, TGAImage::RGB);
int ax = 7, ay = 3;
int bx = 12, by = 37;
int cx = 62, cy = 53;
framebuffer.set(ax, ay, white);
framebuffer.set(bx, by, white);
framebuffer.set(cx, cy, white);
framebuffer.write_tga_file("framebuffer.tga");
return 0;
}
```


It produces the 64x64 image `framebuffer.tga`

, here I scaled it for better readability:

## Compilation

`framebuffer.tga`

.