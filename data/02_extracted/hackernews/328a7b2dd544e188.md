---
title: Codex Security
source: https://github.com/openai/codex-security
author:
- '[[bakigul]]'
published: '2026-07-28'
created: '2026-07-29'
manifest_dates:
- '2026-07-28'
- '2026-07-29'
description: 'Article URL: https://github.com/openai/codex-security Comments URL:
  https://news.ycombinator.com/item?id=49089755 Points: 495 # Comments: 164'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 328a7b2dd544e188
source_type: community_discussion
tldr: OpenAI 发布了 Codex Security，一款用于发现、验证和修复代码安全漏洞的 CLI 与 TypeScript SDK，支持仓库扫描、变更审查与
  CI 安全检查，需 Node.js 22 以上并拥有相应访问权限。
objective_summary: OpenAI 发布了 Codex Security，这是一个 CLI 与 TypeScript SDK，用于在代码中查找、验证并修复安全漏洞。该工具支持扫描代码仓库、审查变更、随时间追踪漏洞，并可在
  CI 流程中运行安全检查。使用前提是 Node.js 22 及以上、Python 3.10 及以上，并具备 Codex Security 的访问权限。用户可通过
  npm 安装后登录账号或设置 OPENAI_API_KEY 完成认证，扫描历史存放在可配置的工作台状态目录中。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - OpenAI
  technologies:
  - Codex Security
  - CLI
  - TypeScript SDK
  key_people: []
key_logic_flow:
- OpenAI 发布了 Codex Security，这是一款用于发现、验证和修复代码安全漏洞的 CLI 与 TypeScript SDK。
- 该工具支持扫描代码仓库、审查代码变更、随时间追踪漏洞发现，并可在 CI 流水线中运行安全检查。
- 使用该工具要求 Node.js 22 及以上、Python 3.10 及以上，并需要获得 Codex Security 的访问权限。
- 工具通过 npm 安装，交互式扫描可登录账号或将 API key 设为默认凭据，CI 等非交互场景优先使用 API key 认证。
- 扫描历史存储在 Codex Security 工作台状态目录中，若该目录不可写，可通过设置 CODEX_SECURITY_STATE_DIR 指向仓库外部的可写目录。
object_mentions:
- object_type: product
  name: '@openai/codex-security'
  canonical_name: Codex Security
  url: https://github.com/openai/codex-security
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - OpenAI 发布了 Codex Security，这是一个 CLI 与 TypeScript SDK，用于在代码中发现、验证和修复安全漏洞。
  - 该工具支持扫描代码仓库、审查代码变更、随时间追踪漏洞，并可在 CI 流程中运行安全检查。
  - 工具通过 npm 安装并支持 ChatGPT 登录或 API key 两种认证方式，扫描历史存储在可配置的工作台状态目录中。
  article_id: 328a7b2dd544e188
extract_result: success
---

`@openai/codex-security`

is a CLI and TypeScript SDK for finding, validating, and fixing security vulnerabilities in your code. Scan repositories, review changes, track findings over time, and run security checks in CI.

Requires Node.js 22 or later, Python 3.10 or later, and access to Codex Security.

```
npm install @openai/codex-security
npx codex-security login
npx codex-security scan .
```

For CI, set `OPENAI_API_KEY`

instead of signing in.

If both a ChatGPT sign-in and an API key are available, interactive scans ask which credential to use. CI and other noninteractive scans keep the existing API-key precedence. Select a credential explicitly when needed:

```
npx codex-security scan . --auth chatgpt
npx codex-security scan . --auth api-key
```

To make your ChatGPT sign-in the automatic default, unset any configured API keys:

`unset OPENAI_API_KEY CODEX_API_KEY`

Scan history is stored in the Codex Security workbench state directory. If that
directory cannot be written, set `CODEX_SECURITY_STATE_DIR`

to a writable
directory outside the repository.

```
import { CodexSecurity } from "@openai/codex-security";
const security = new CodexSecurity();
const result = await security.run(".");
console.log(result.reportPath);
await security.close();
```

For installation, authentication, scan options, and CI setup, see the official documentation.