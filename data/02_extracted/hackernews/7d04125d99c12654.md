---
title: 'Show HN: GlassBox – what the browser reveals, and how identifiable you are'
source: https://glassbox.codecanary.org
author:
- '[[DavidD]]'
published: '2026-08-24'
created: '2026-08-24'
manifest_dates:
- '2026-08-24'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 7d04125d99c12654
source_type: community_discussion
tldr: GlassBox 是一个浏览器指纹检测工具，实时运行网站可采集的各项测量并回显给用户，覆盖硬件环境、引擎、浏览器构建三个层级，支持粘贴 JSON 报告跨浏览器比对。绝大多数探针在本地执行，仅
  IP 情报调用公共地理定位 API。
objective_summary: GlassBox 是一款通过 Show HN 发布的浏览器指纹检测产品，它在浏览器内实时运行网站可采集的各类指纹测量并向用户回显结果。该工具提供
  audio-fp 探针、IP 解析以及三个层级的数据，分别覆盖硬件与环境（跨引擎）、引擎×硬件（canvas、audio、math、codecs）和浏览器构建信息。用户可粘贴来自其他浏览器或会话的
  JSON 报告，GlassBox 会按层级评分，判断是否为同一台机器、同一引擎家族或同一浏览器，模拟真实跨浏览器追踪器的工作方式。测量绝大部分在本地浏览器内完成且不发送分析数据，仅
  IP 情报会在加载时调用 ipwho.is、ipapi.is、geojs.io 等公共地理定位 API 查询地址、网络与 VPN 状态。
event_type: application_landing
epistemic_status: verified_fact
entities:
  companies:
  - GlassBox
  - ipwho.is
  - ipapi.is
  - geojs.io
  technologies:
  - browser fingerprinting
  - canvas fingerprinting
  - audio fingerprinting
  - geolocation
  - VPN detection
  key_people: []
key_logic_flow:
- GlassBox 在浏览器内实时运行网站可采集的各项指纹测量，并将结果直接展示给用户，让通常被隐藏的追踪与反欺诈信号显性化。
- 该工具提供 audio-fp 探针、IP 解析以及三个层级的数据，分别覆盖硬件与环境（跨引擎）、引擎×硬件（canvas、audio、math、codecs）与浏览器构建信息。
- 用户可粘贴来自另一浏览器或会话的 JSON 报告，GlassBox 为每一层级评分，判断是否为同一台机器、同一引擎家族或同一浏览器。
- 除 IP 情报外，所有指纹测量探针均在本地浏览器内运行，不发送分析数据或 beacon，用户可通过工具栏关闭 Geo 功能保持完全本地运行。
- IP 情报在页面加载时调用 ipwho.is、ipapi.is、geojs.io 等公共地理定位 API，查询地址、网络与 VPN 状态，其余逻辑可查看源码验证。
object_mentions:
- object_type: product
  name: GlassBox
  canonical_name: GlassBox
  url: https://glassbox.codecanary.org
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - GlassBox 在浏览器内实时运行网站可采集的各项指纹测量，将追踪与反欺诈脚本使用的信号直接展示给用户。
  - 用户可粘贴来自另一浏览器或会话的 JSON 报告，GlassBox 按层级评分判断是否为同一台机器、同一引擎家族或同一浏览器。
  - GlassBox 的测量绝大部分在本地浏览器内完成，仅 IP 情报会调用 ipwho.is、ipapi.is、geojs.io 等公共地理定位 API。
  article_id: 7d04125d99c12654
extract_result: success
---

Every measurement a website can take from your browser, run live and shown back to you. This is the same class of signals the tracking and anti-fraud scripts collect — surfaced instead of hidden.

audio-fp probe—

Your IP

resolving…

1 Hardware & environment
cross-engine · links you across different browsers

…

2 Engine × hardware
canvas · audio · math · codecs — links within one engine family

…

3 Browser build
which browser & version — not who you are

Paste a JSON report from another browser or session (Copy JSON report → paste here). GlassBox scores each tier and tells you whether it's the same machine, same engine family, or same browser — the way a real cross-browser tracker links you.

Mostly local. Every fingerprinting probe runs in your browser and stays here — no analytics, no beacon. The one exception is IP intelligence: on load, GlassBox queries public geolocation APIs (ipwho.is, ipapi.is, geojs.io) to look up your address, network and VPN status. Turn Geo off in the toolbar to stay fully local. View source to confirm the rest.