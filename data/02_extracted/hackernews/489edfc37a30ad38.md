---
title: 'Show HN: PicoMQ – Durable Streams over HTTP, on object storage'
source: https://picomq.com/
author:
- '[[adesh_nalpet]]'
published: '2026-08-24'
created: '2026-08-25'
manifest_dates:
- '2026-08-24'
- '2026-08-25'
description: 'PicoMQ is a Rust server for Durable Streams, built on Object Store.
  Cheap, URL-addressable, granular streams (create/append/read/long-poll/SSE), with
  Pico Protocol or Durable Streams Protocol as the facade.S3Stream is the stream storage
  primitive, used in AutoMQ, shipped as a Rust library. Coordination is a command
  log in Postgres. Comments URL: https://news.ycombinator.com/item?id=49421806 Points:
  118 # Comments: 22'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 489edfc37a30ad38
source_type: community_discussion
tldr: PicoMQ 是一个基于 S3 兼容对象存储构建的持久化实时流产品，通过 HTTP 对外提供流服务，主张为每个用例创建独立可寻址、容量无限的流。
objective_summary: PicoMQ 在 Show HN 上发布，是一款基于 S3 兼容对象存储构建的持久化实时流服务，通过 HTTP 对外提供数据流能力。其核心架构主张为每个用例单独创建流，而不是将同类记录集中打包进一个主题。每个流独立可寻址、容量无上限，可从容纳空闲状态平滑扩展到高吞吐场景。
event_type: application_landing
epistemic_status: pr_statement
entities:
  companies:
  - PicoMQ
  technologies:
  - S3
  - HTTP
  key_people: []
key_logic_flow:
- PicoMQ 是一款基于 S3 兼容对象存储构建的持久化实时流服务，数据流通过 HTTP 协议对外提供。
- 其架构建议为每个具体用例创建独立的流，而不是把同一类记录全部塞进单一主题。
- 每个流都独立可寻址、容量无限，并且能从容纳空闲状态平滑扩展到高吞吐场景。
object_mentions:
- object_type: product
  name: PicoMQ
  canonical_name: PicoMQ
  url: https://picomq.com/
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - PicoMQ 是一个基于 S3 兼容对象存储构建的持久化实时流系统，通过 HTTP 对外提供数据流服务。
  - 文章主张为每个用例单独创建流，而不是把同类记录全部打包进一个主题，每个流独立可寻址且容量无限。
  article_id: 489edfc37a30ad38
extract_result: success
---

# Durable streams on

object storage

PicoMQ is durable, real-time streams over HTTP,

built on S3-compatible object storage.

the architecture

Create a stream per use case instead of packing every record of a kind into one topic. Each stream is independently addressable, bottomless, and can scale from idle to high throughput.