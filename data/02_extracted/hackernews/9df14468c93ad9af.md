---
title: 'Show HN: ssh ssh.place'
source: https://ssh.place
author:
- '[[jeninh]]'
published: '2026-08-03'
created: '2026-08-03'
manifest_dates:
- '2026-08-03'
description: 'Article URL: https://ssh.place Comments URL: https://news.ycombinator.com/item?id=49149805
  Points: 112 # Comments: 66'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 9df14468c93ad9af
source_type: community_discussion
tldr: ssh.place 是一个通过 SSH 连接的共享画布，无需账号与安装，任何 SSH 密钥输入 ssh ssh.place 即可参与。画布为 200×60
  格，每位用户每 15 秒可放置一个色块，仅支持颜色，服务器拒绝含字符的输入。
objective_summary: ssh.place 由开发者在 Show HN 上发布，是一个通过 SSH 协议访问的公共协作画布。用户无需注册或安装客户端，使用任意
  SSH 密钥连接 ssh ssh.place 即可开始绘制。画布大小为 200×60 个单元格，支持方向键、wasd 或 hjkl 移动光标，数字键 0 至 9
  与 tab 键选择 16 种颜色，空格键放置色块，每次放置需等待 15 秒冷却，冷却时间与 SSH 密钥绑定。服务器会拒绝任何含字符的输入，画布仅接受纯色块，网页端只读显示画布内容。
event_type: application_landing
epistemic_status: pr_statement
entities:
  companies: []
  technologies:
  - SSH
  key_people: []
key_logic_flow:
- ssh.place 是一个通过 SSH 连接的公共共享画布，无需注册账号或安装软件即可使用。
- 画布尺寸为 200×60 个单元格，每位用户每 15 秒可以放置一个色块。
- 连接命令为 ssh ssh.place，任意 SSH 密钥均可通过验证，冷却时间绑定密钥，重连不会重置。
- 用户可通过方向键、wasd 或 hjkl 移动光标，shift 加左右方向键可整屏跳转。
- 颜色通过数字键 0 至 9 选择，tab 键可循环切换全部 16 种颜色，空格键放置色块。
- 服务器会拒绝任何包含字符的输入，画布仅支持颜色放置，网页端仅提供只读展示。
object_mentions:
- object_type: product
  name: ssh.place
  canonical_name: ssh.place
  url: https://ssh.place
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - ssh.place 是一个通过 SSH 访问的公共协作画布，无需账号和安装，使用任意 SSH 密钥即可连接绘制。
  - 画布大小为 200×60 个单元格，每位用户每 15 秒可放置一个色块，冷却时间与 SSH 密钥绑定。
  - 该画布仅接受颜色放置，服务器会拒绝包含字符的输入，因此用户无法在上面书写文字。
  article_id: 9df14468c93ad9af
extract_result: success
---

# ssh.place

**One canvas. Everyone draws on it over SSH.**

No account, no install · 200×60 cells · one placement every 15s

**Draw on it:**
`ssh ssh.place`


Any SSH key works. There is nothing to sign up for.

## How it works

- Move the cursor with the
**arrow keys**, `wasd`

or `hjkl`

- The canvas is wider than your terminal, so
**scroll** to pan around. `shift`+`←`/`→` jumps a whole screen.
- Pick a color with
`0`

to `9`

. `tab` cycles through all 16.
`space` puts down a solid block of that color
- Now wait 15 seconds, same as everyone else

This canvas is color only. The server turns down anything with a character in it, so you cannot write text here. Draw something instead.

Your cooldown is tied to your SSH key, so reconnecting will not reset it. This page only reads the canvas. It changes over SSH and nowhere else.