---
title: I wrote an bash enumerator because I was sick of xargs
source: https://numerlab.org/2025/07/20/bashumerate-enumerator/
author:
- '[[wallach-game]]'
published: '2026-07-20'
created: '2026-07-21'
manifest_dates:
- '2026-07-21'
description: 'Article URL: https://numerlab.org/2025/07/20/bashumerate-enumerator/
  Comments URL: https://news.ycombinator.com/item?id=48984270 Points: 126 # Comments:
  104'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: e7c54a804f65ab8c
source_type: community_discussion
tldr: bashumerate 是一个可编程的 Bash 迭代器工具，提供统一的 {} 占位符语法来遍历文件、行、范围和列表，取代 for 循环、find -exec
  和 xargs 等传统方式。核心仅约 140 行 Bash 代码，支持通过插件扩展自定义数据源。
objective_summary: 作者因厌倦 for 循环、find -exec 和 xargs 各自不同的语法和缺陷，开发了 enumerate 命令，通过
  -f（文件）、-l（行）、-r（范围）、-L（列表）四种参数配合统一的 {} 占位符实现一致的迭代风格。该工具核心约 140 行 Bash 代码，内部使用 NUL
  分隔保证文件名安全，支持 --include/--exclude 过滤、模板模式和 NUL 安全输出，用户可通过在 lib/enumerators/ 目录下放置脚本自定义源。项目托管在
  GitHub 上，可通过 basher 安装或直接克隆使用。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - bash
  - enumerate
  key_people: []
key_logic_flow:
- 作者认为 for 循环、find -exec 和 xargs 各自语法不同且存在缺陷，因此开发了一个统一的迭代工具 enumerate。
- enumerate 支持四种数据源：-f 文件、-l 行、-r 范围、-L 列表，全部使用 {} 作为占位符。
- 用户可以通过在 lib/enumerators/ 目录下定义 enumerate_source_<name> 函数来添加自定义数据源，无需注册或配置。
- 工具核心代码仅约 140 行 Bash，内部使用 NUL 分隔文件保证空格和特殊字符的安全性。
- 项目通过 basher 安装或直接从 GitHub 克隆并创建符号链接使用。
object_mentions:
- object_type: project
  name: wallach-game/bashumerate
  canonical_name: bashumerate
  url: https://github.com/wallach-game/bashumerate
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - bashumerate 是一个可编程的 Bash 枚举器，核心约 140 行代码，通过统一的 {} 占位符语法支持文件、行、范围和列表四种数据源。
  - 项目托管在 GitHub 上，可通过 basher install wallach-game/bashumerate 安装，或直接克隆仓库并创建符号链接。
  - 用户可以通过在 lib/enumerators/ 目录下定义 enumerate_source_<name> 函数来添加自定义数据源，无需注册或配置。
  article_id: e7c54a804f65ab8c
extract_result: success
---

Title: bashumerate — A programmable iterator for your shell

URL Source: https://numerlab.org/2025/07/20/bashumerate-enumerator/

Published Time: Mon, 20 Jul 2026 20:19:49 GMT

Markdown Content:
Ever found yourself writing this?

```
for f in *.txt; do
  wc -l "$f"
done
```

Or this?

```
find . -name '*.sh' -exec wc -l {} +
```

Or maybe you pipe into `xargs` and pray your filenames don’t have spaces:

```
find . -name '*.log' | xargs rm
```

These all work, but each has its own syntax, its own flags, its own quirks. I wanted something simpler — one consistent way to iterate over anything.

## Enter bashumerate

```
enumerate -f '*.sh' -- 'wc -l {}'
```

That’s it. `-f` for files, `{}` is the placeholder for each item, `--` separates source from command.

It supports four source types:

| Flag | Source | Example |
| --- | --- | --- |
| `-f` | Files | `enumerate -f '*.sh' -- wc -l {}` |
| `-l` | Lines | `enumerate -l /etc/hosts -- 'echo {}'` |
| `-r` | Range | `enumerate -r 1 10 -- 'printf "n=%d\n" {}'` |
| `-L` | List | `enumerate -L a b c -- 'echo item: {}'` |

If you omit the command, items are printed to stdout — pipe them anywhere:

```
enumerate -f '*.log' | xargs rm
cat urls.txt | enumerate -l - | while read url; do curl -s "$url"; done
```

## Why not just use a for loop?

You can. But `enumerate` gives you:

*   **Consistent syntax** — same `{}` placeholder for files, lines, ranges, or lists
*   **Template mode** — single-quoted commands work as shell templates: `enumerate -f '*' -- 'cat {} | head -5'`
*   **Filters** — `--include` and `--exclude` with glob patterns
*   **NUL-safe** — `-0` flag for NUL-delimited output
*   **Extensible** — drop a file in `lib/enumerators/` to add custom sources

## Adding your own source

Create `lib/enumerators/docker.sh`:

```
enumerate_source_docker() {
  docker ps --format '' | while read -r name; do
    printf '%s\0' "$name"
  done
}
```

Now you can do:

```
enumerate docker -- 'docker restart {}'
```

That’s it. No registration, no config — just name the function `enumerate_source_<name>` and it’s available.

## Under the hood

The core is about 140 lines of bash. Sources are NUL-delimited internally (safe for spaces and special chars), and the `{}` placeholder is replaced at the shell level — no sed, no eval tricks.

```
enumerate -f '*.md' -- 'wc -l {}'
# → wc -l README.md
# → wc -l docs/guide.md
```

## Install

```
basher install wallach-game/bashumerate

# or just clone and symlink
git clone https://github.com/wallach-game/bashumerate.git
ln -s "$PWD/bashumerate/bin/enumerate" ~/.local/bin/
```

## The name

“bash” + “enumerate” — a bash enumerator. Not creative, but it does what it says.

Check it out: [github.com/wallach-game/bashumerate](https://github.com/wallach-game/bashumerate)