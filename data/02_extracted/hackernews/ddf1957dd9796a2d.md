---
title: Shadcn/UI now defaults to Base UI instead of Radix
source: https://ui.shadcn.com/docs/changelog
author:
- '[[dabinat]]'
published: '2026-07-05'
created: '2026-07-05'
description: 'Article URL: https://ui.shadcn.com/docs/changelog Comments URL: https://news.ycombinator.com/item?id=48791328
  Points: 121 # Comments: 37'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: ddf1957dd9796a2d
manifest_dates:
- '2026-07-05'
source_type: community_discussion
tldr: shadcn/ui 官方宣布默认组件库从 Radix 切换为 Base UI，新项目初始化时 Base UI 成为默认选项，但 Radix 仍受支持且无需迁移已有项目。
objective_summary: 2026 年 7 月，shadcn/ui 团队基于社区采用数据（Base UI 以 2:1 比例超越 Radix）正式将 Base
  UI 设为新建项目的默认组件库。Base UI 已发布到 1.6.0 版本，周下载量超 600 万。团队为两个库同步维护更新，提供渐进式迁移工具（基于 Claude
  Code 或 Cursor 的迁移技能），并强调 Radix 不被弃用、现有项目无需迁移。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - shadcn/ui
  - Base UI
  - Radix
  key_people: []
key_logic_flow:
- shadcn/ui 从 2023 年 1 月发布以来一直基于 Radix 构建，Radix 以无样式、可访问的 headless 组件著称。
- Radix 原班团队后来打造了 Base UI，该库于 2025 年进入 beta 阶段，现已发布到 1.6.0 且周下载量超 600 万次。
- 社区已在 shadcn/create 中以 2:1 的比例选择 Base UI 而非 Radix，官方据此将 Base UI 设为默认选项。
- Radix 不会被弃用，shadcn/ui 仍为两个库同步提供所有更新和新组件。
- 团队提供了渐进式迁移技能（Claude Code/Cursor），每次迁移生成工作代码、组件报告和清晰的 Git 历史。
- 迁移工具采用逐组件迁移策略，两个库可共存，60 个组件的完整迁移实测约需 25 分钟、约每组件 10k tokens。
extract_result: success
object_mentions:
- object_type: project
  name: shadcn/ui
  canonical_name: shadcn/ui
  url: https://ui.shadcn.com
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - shadcn/ui 官方宣布从即日起将 Base UI 设为新建项目的默认组件库，取代此前长期使用的 Radix。
  - shadcn/ui 自 2023 年 1 月发布以来一直基于 Radix 构建，此次切换基于社区用户在 shadcn/create 中以 2:1 的比例选择
    Base UI 的数据。
  - 官方声明 Radix 不会被弃用，所有更新和新组件仍会为两个组件库同步发布。
  article_id: ddf1957dd9796a2d
- object_type: project
  name: Base UI
  canonical_name: Base UI
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Base UI 已发布到 1.6.0 版本，每周 npm 下载量超过 600 万次，达到生产级稳定状态。
  - Base UI 由原 Radix 团队打造，吸收了第一代组件的全部经验，团队持续定期推出新组件。
  article_id: ddf1957dd9796a2d
- object_type: project
  name: Radix
  canonical_name: Radix
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - Radix 是 shadcn/ui 自 2023 年 1 月上线以来一直使用的无样式 headless 组件库，以优秀的 API 和可访问性著称。
  - 官方明确表示 Radix 不会被弃用，现有生产项目无需迁移，shadcn/ui 团队仍会为其同步更新。
  article_id: ddf1957dd9796a2d
- object_type: project
  name: shadcn/create
  canonical_name: shadcn/create
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - shadcn/create 工具在创建新项目时展示 Base UI 为首选选项，用户选择 Base UI 的比例已达 Radix 的两倍。
  - 社区通过 shadcn/create 的实际选择数据是官方将 Base UI 设为默认的主要依据。
  article_id: ddf1957dd9796a2d
- object_type: project
  name: shadcn/ui Radix-to-Base-UI Migration Skill
  canonical_name: shadcn/ui migration skill
  url: null
  confidence: medium
  article_role: ecosystem_context
  evidence_snippets:
  - 官方为有意迁移的项目提供了专门的编码智能体迁移技能，支持逐组件渐进式迁移且两个库可共存。
  - 迁移工具实测可在约 25 分钟内完成 60 个组件的迁移（其中 36 个基于 Radix），每组件约消耗 10k tokens，构建通过且自定义内容保留。
  article_id: ddf1957dd9796a2d
---

## July 2026 - Base UI as the Default

Starting today, **Base UI is the default component library in shadcn/ui**.

First, a bit of history. When shadcn/ui launched in January 2023, it was built on Radix. At the time, nothing else came close. Unstyled headless components, great APIs, great accessibility, battle-tested in millions of apps.

Fast forward a few years and the same folks who built Radix are building something new: Base UI. They've done it once. Now they get to do it again, with everything they learned the first time.

Last year, Base UI tagged a beta and a lot of you asked if we are going to replace Radix with it. I said "the worst thing you can do for your production app is switch component libraries". I meant it, and it still holds. So instead of switching, we did the shadcn thing: we rebuilt every component for Base UI, kept the same abstraction, and let you choose. December brought `npx shadcn create`

with both libraries. January brought full Base UI docs.

Then we watched what you did with it.

**Base UI is stable.**It's at 1.6.0 with 6M+ weekly downloads.**It keeps getting better.**The team ships new and useful components regularly.**We use it.**Every new project we've started runs on Base UI.**You use it.**Projects created on shadcn/create now pick Base UI over Radix 2 to 1.

The community already made the call. We're making it official.

**New projects default to Base UI.**Run`npx shadcn init`

and Base UI is the default pick.**shadcn/create shows Base UI first.****The docs default to Base UI.**Component pages open on the Base UI tab. Radix docs are one click away.

**Radix is not being deprecated.** We still support it, and every update and new component will ship for both libraries (unless a component only exists in Base UI).

**You do not need to migrate.** Radix is a mature, tested library. We still run it in production today and we're not migrating. If your app works, keep shipping.

**Prefer Radix for new projects?** It's one flag away:

If you have scripts or CI running `shadcn init`

non-interactively and expecting Radix, add `-b radix`

to keep them on the same path.

**Building a registry?** Ship a `registry:base`

config if you want to pin a specific library. Items without one now init as Base UI.

**Starting something new?** We recommend Base UI.

You don't need to migrate. But if you want to, we built a skill for it:

Then ask your coding agent:

It's progressive by default: migrate one component and its usage at a time while your project stays green and shippable. Both libraries coexist while you work. Stop halfway, ship, come back next week and it picks up where you left off. Or ask for the whole project in one go.

Because you own the code. You've added variants, changed classes, threaded new props. A codemod handles the components you never touched and breaks on the ones you did.

So we shipped knowledge instead: every rename, every prop change, every behavior difference, hand-checked against both libraries. Your agent reads it, figures out what *you* changed, and carries those changes over.

Mechanical things get fixed everywhere (`asChild`

is now `render`

). Behavior changes get flagged, never silently patched. You decide.

Every run leaves three things:

**Working code.**Typechecked and built before it reports success.**A report per component**in`.migration/`

at your project root: what changed, what was left alone, and a short checklist of things to verify by hand.**Clean git history.**One commit per component, on a branch. Rollback is deleting the branch.

Here's what a report looks like:

No hidden state. Progress lives in your files and git history, so any agent, any session, any day picks up where the last one stopped.

It works with Claude Code, Cursor, or any agent that supports skills. We tested it on real projects: 60+ components, 36 of them on Radix. A full migration ran in about 25 minutes at roughly 10k tokens per component. Clean builds, customizations intact.