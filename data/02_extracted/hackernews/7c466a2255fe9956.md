---
title: Linux eliminates the strncpy API after six years of work, 360 patches
source: https://www.phoronix.com/news/Linux-7.2-Drops-strncpy
author:
- '[[simonpure]]'
published: '2026-06-20'
created: '2026-06-21'
description: 'https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/lin... Comments
  URL: https://news.ycombinator.com/item?id=48612943 Points: 200 # Comments: 172'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 7c466a2255fe9956
source_type: community_discussion
tldr: Linux 内核历时六年、经 362 次提交后，在 7.2 版本中彻底移除 strncpy API
objective_summary: Linux 内核在 7.2 版本合并窗口中移除了长期作为 Bug 来源的 strncpy API，该清理工作历时六年、涉及约
  362 次提交，同步移除了各 CPU 架构的 strncpy 实现，并推荐使用 strscpy() 等替代函数。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies:
  - Linux Foundation
  technologies:
  - strncpy
  - strscpy
  key_people: []
key_logic_flow:
- strncpy 函数因违反直觉的 NUL 终止语义和冗余零填充导致的性能问题，长期是 Linux 内核 Bug 的持续来源。
- Linux 内核社区历时六年、通过约 362 次提交，逐步清除了内核中所有 strncpy 的使用场景。
- 该合并操作于某个周五完成，彻底移除了 strncpy API 及各 CPU 架构的底层 strncpy 实现。
- 替代方案包括：strscpy()（NUL 终止目标）、strscpy_pad()（零填充 NUL 终止）、strtomem_pad()（定宽非 NUL 终止字段）、memcpy_and_pad()（带填充的边界拷贝）以及
  memcpy()（已知长度拷贝）。
extract_result: success
---

# Linux Finally Eliminates The strncpy API After Six Years Of Work, 360+ Patches

The strncpy function within the Linux kernel has been a "persistent source of bugs" for years due to counter-intuitive semantics and behavior around NUL termination along with performance issues due to redundant zero-filling of the destination. It took work over the last six years with around 362 commits to eliminate users of strncpy code within the kernel, but they are over the finish line for Linux 7.2.

This merge on Friday eliminated the strncpy API and the last per CPU architecture strncpy implementations.

In place of strncpy, Linux kernel code should use strscpy() for NUL terminated destinations, strscpy_pad() for NUl-terminated destinations with zero-padding, strtomem_pad() for non-NUL-terminated fixed-width fields, memcpy_and_pad() for bounded copies with explicit padding, or memcpy() for known-length memory copies.