---
title: 'Caddy compatibility for zeroserve: 3x throughput and 70% lower latency'
source: https://su3.io/posts/zeroserve-caddy-compat
author:
- '[[losfair]]'
published: '2026-06-14'
created: '2026-06-15'
description: 'Article URL: https://su3.io/posts/zeroserve-caddy-compat Comments URL:
  https://news.ycombinator.com/item?id=48527145 Points: 186 # Comments: 53'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b17c0458a0c4d4f6
source_type: community_discussion
tldr: zeroserve 是一款基于 eBPF 的用户态高性能 HTTPS 服务器，最新 v0.2.11 版本新增 Caddyfile 兼容模式，可将 Caddy
  配置 JIT 编译为 eBPF 再编译为原生机器码在 io_uring 中执行。基准测试显示，相比原生 Caddy，zeroserve 实现了约 3 倍吞吐量和
  70% 的延迟降低。
objective_summary: zeroserve 项目于 2026 年 7 月发布 v0.2.11 版本，新增 Caddyfile 兼容模式，可将 Caddy
  配置依次 JIT 编译为 eBPF 和原生 x86_64/ARM64 机器码，并在 io_uring 事件循环中执行。在 AMD Ryzen 7 3700X 双线程
  HTTPS 反向代理基准测试中，zeroserve（clang 编译）达到 38,948 req/s 吞吐量和 1.45ms p50 延迟，而原生 Caddy
  为 12,529 req/s 和 4.74ms p50 延迟，nginx 为 37,424 req/s 和 1.57ms p50 延迟。zeroserve 支持图灵完备的
  eBPF，用户可通过 Caddyfile 调用自定义 eBPF 中间件，例如为 S3 兼容存储桶添加 AWS SigV4 认证。
event_type: infrastructure_update
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - eBPF
  - io_uring
  - JIT
  - Caddyfile
  - AWS SigV4
  - x86_64
  - ARM64
  key_people: []
key_logic_flow:
- zeroserve 是一个在用户态运行 eBPF 脚本的高性能 HTTPS 服务器，现已新增 Caddyfile 兼容模式。
- 用户提供 Caddyfile 后，zeroserve 将其 JIT 编译为 eBPF，再编译为原生 x86_64/ARM64 机器码，并在 io_uring 事件循环中执行。
- HTTPS 反向代理基准测试显示 zeroserve-clang 达到 38,948 req/s 吞吐量（原生 Caddy 为 12,529 req/s），p50
  延迟为 1.45ms（原生 Caddy 为 4.74ms），峰值内存 30.9 MiB（Caddy 为 67.4 MiB）。
- zeroserve 支持用户通过 Caddyfile 调用自定义 eBPF 代码，例如使用 io.su3.aws-sigv4 中间件为 S3 兼容存储桶添加 AWS
  SigV4 认证。
- 用户可通过 curl 下载 zeroserve v0.2.11 的 Linux 二进制文件，使用 --caddy 参数指定配置路径即可直接运行。
extract_result: success
object_mentions:
- object_type: project
  name: zeroserve
  canonical_name: losfair/zeroserve
  url: https://github.com/losfair/zeroserve
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - zeroserve 是一个在用户态运行 eBPF 脚本的高性能 HTTPS 服务器，现已新增 Caddyfile 兼容模式，可将 Caddy 配置 JIT
    编译为 eBPF 后执行。
  - 用户可通过 curl 下载 zeroserve v0.2.11 的 Linux 二进制文件，使用 --caddy 参数指定 Caddyfile 路径即可启动，支持
    --plugin 参数加载自定义 eBPF 中间件。
  - 基准测试显示 zeroserve-clang 的 HTTPS 反向代理吞吐量达到 38,948 req/s，约为原生 Caddy 12,529 req/s
    的 3 倍，同时峰值内存仅 30.9 MiB 远低于 Caddy 的 67.4 MiB。
  article_id: b17c0458a0c4d4f6
- object_type: product
  name: Caddy
  canonical_name: Caddy
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章将 zeroserve 与原生 Caddy 进行 HTTPS 反向代理基准对比，Caddy 的吞吐量为 12,529 req/s，p50 延迟为 4.74ms，p99
    延迟为 13.11ms。
  - zeroserve 新增的兼容模式可直接接受 Caddyfile 作为配置输入，并将其即时编译为 eBPF 和原生机器码后执行，无需修改现有 Caddy 配置。
  article_id: b17c0458a0c4d4f6
- object_type: product
  name: nginx
  canonical_name: nginx
  url: null
  confidence: low
  article_role: mentioned_reference
  evidence_snippets:
  - 文章在同一基准测试中将 nginx 作为对照，nginx 的 HTTPS 反向代理吞吐量为 37,424 req/s，p50 延迟为 1.57ms，峰值内存
    25.7 MiB。
  article_id: b17c0458a0c4d4f6
---

zeroserve is a high-performance HTTPS server that runs eBPF scripts in userspace (intro). Now it's got a Caddy-compat mode - when provided a Caddyfile, zeroserve JIT-compiles it to eBPF and then to native x86_64/ARM64 machine code, and runs it in an `io_uring`

event loop.

| protocol | server | throughput | p50 | p99 | peak RSS |
|---|---|---|---|---|---|
| https | zeroserve-clang | 38,948 req/s | 1.45ms | 3.91ms | 30.9 MiB |
| https | zeroserve-tcc | 36,653 req/s | 1.67ms | 4.00ms | 34.2 MiB |
| https | caddy | 12,529 req/s | 4.74ms | 13.11ms | 67.4 MiB |
| https | nginx | 37,424 req/s | 1.57ms | 4.24ms | 25.7 MiB |

*HTTPS reverse proxy, 2 threads, AMD Ryzen 7 3700X. Check CI for original run result.*

Try it with your Caddyfile:

```
curl -fL -o zeroserve https://github.com/losfair/zeroserve/releases/download/v0.2.11/zeroserve-$(uname -m)-linux
chmod +x zeroserve
./zeroserve --caddy /etc/caddy/Caddyfile
curl http://127.0.0.1:8080
```


zeroserve runs turing-complete eBPF and you can call custom code from your Caddyfile. For example, to reverse-proxy a path to an S3-compatible bucket with AWS SigV4 auth, grab io.su3.aws-sigv4.c and then:

```
# zeroserve --plugin io.su3.aws-sigv4.c --caddy Caddyfile
example.com {
route /s3/* {
uri strip_prefix /s3
rewrite * /my-bucket{uri}
# Call the `sign_request` method in the eBPF middleware `io.su3.aws-sigv4.o`
zeroserve_call io.su3.aws-sigv4 sign_request {
access_key_id "minioadmin"
secret_access_key "minioadmin"
}
reverse_proxy http://127.0.0.1:9000
}
}
```