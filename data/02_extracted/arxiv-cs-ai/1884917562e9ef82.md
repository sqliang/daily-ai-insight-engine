---
title: 'DeepInsight: A Unified Evaluation Infrastructure Across the Physical AI Stack'
source: https://arxiv.org/abs/2606.17574
author:
- '[[Siyi Li, Chunyu Sun, Jiahao Zhang, Yuchen Kang, Wuliang Wang, Yu Qiu, Rui Jiang,
  Haitao Cui, Jie Chen]]'
published: '2026-06-17'
created: '2026-06-17'
description: 'arXiv:2606.17574v1 Announce Type: new Abstract: Evaluating a Physical
  AI stack spans operators that differ by more than three orders of magnitude -- from
  a single foundation-model decoding step to thousands of physics ticks of whole-body
  control -- varying orthogonally in modality, reward semantics, and resource profile.
  No existing framework spans this range, so the stack is evaluated today by stitching
  together separate harnesses that share neither runtime nor scoring, preserving each
  segment''s local validity but losing the shared identity needed to diagnose cross-layer
  regressions. We present DeepInsight, an evaluation infrastructure that serves this
  full spectrum on a single runtime. Rather than homogenize the regimes, it preserves
  their heterogeneity behind three narrow abstractions -- task, resource, and result
  -- each realized as one invariant shared by every subsystem: one episode driver,
  one resource-handle protocol implemented by every expensive backend (LLM inference
  and sandboxed runtimes alike), and one trace identity scheme under which every event
  is written. Deployed in production across all three layers of an embodied humanoid
  stack, this single set of invariants onboards new benchmarks largely by configuration.
  Where mature peer orchestrators exist -- at the foundation-model end -- it reproduces
  published references and peer-framework readings within their own spread, runs the
  same suites faster on a single node, and scales near-linearly across nodes. Its
  distinctive return is diagnostic: because every layer writes into one shared trace,
  a regression that begins in one layer and surfaces in another stays localizable
  on that trace -- a cross-layer payoff no federation of per-segment harnesses can
  reproduce.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 1884917562e9ef82
source_type: academic_paper
tldr: DeepInsight 是一个面向物理 AI 栈的统一评估基础设施，通过三个抽象（任务、资源、结果）在单一运行时上覆盖从基础模型解码到全身控制的完整评估光谱，已在具身人形机器人生产部署中验证其跨层诊断能力。
objective_summary: 该论文提出了 DeepInsight，一种能够在单一运行时上覆盖物理 AI 栈完整评估范围（从基础模型解码步骤到全身控制的数千物理滴答）的统一评估基础设施。当前物理
  AI 栈的评估依赖多个互不共享运行时和评分标准的独立框架，导致无法诊断跨层回归问题。DeepInsight 通过任务、资源和结果三个统一抽象保留了各评估领域的异质性，已在具身人形机器人的三个层级上生产部署，能复现已有基准测试结果并在单节点上更快运行。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - Physical AI
  - LLM
  key_people: []
key_logic_flow:
- 物理 AI 栈的评估范围跨越三个数量级，从单个基础模型解码步骤到全身控制的数千物理滴答，现有框架无法覆盖完整评估光谱。
- 当前评估方式通过拼凑多个独立评测框架实现，各段虽保持局部有效性，但丧失了跨层回归问题的诊断能力。
- DeepInsight 通过任务、资源和结果三个统一抽象，在单一运行时上服务于物理 AI 栈的完整评估光谱。
- 该框架已在具身人形机器人的三个层级（基础模型、中间层、全身控制）上生产部署，新基准测试大多只需通过配置即可接入。
- 在基础模型端，DeepInsight 能复现已发表参考结果与同行框架读数，在单节点上运行速度更快，并能在多节点间近线性扩展。
- DeepInsight 的核心价值在于跨层诊断：因所有层级写入同一共享跟踪，一个层级的回归问题可在该跟踪上被准确定位。
extract_result: success
object_mentions:
- object_type: project
  name: DeepInsight
  canonical_name: DeepInsight
  url: https://arxiv.org/abs/2606.17574
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - DeepInsight 是一个统一的评估基础设施，能够在一个运行时上服务于从基础模型解码到全身控制的物理 AI 栈完整评估光谱。
  - 该框架通过任务、资源和结果三个统一抽象保留各评估领域的异质性，并已在具身人形机器人的三层堆栈上生产部署。
  - DeepInsight 的独特价值在于跨层诊断能力：因每层写入同一个共享跟踪，一个层级的回归问题可在该跟踪上被准确定位。
  article_id: 1884917562e9ef82
---

# Computer Science > Artificial Intelligence

# Title:DeepInsight: A Unified Evaluation Infrastructure Across the Physical AI Stack

View PDF HTML (experimental)Abstract:Evaluating a Physical AI stack spans operators that differ by more than three orders of magnitude -- from a single foundation-model decoding step to thousands of physics ticks of whole-body control -- varying orthogonally in modality, reward semantics, and resource profile. No existing framework spans this range, so the stack is evaluated today by stitching together separate harnesses that share neither runtime nor scoring, preserving each segment's local validity but losing the shared identity needed to diagnose cross-layer regressions. We present DeepInsight, an evaluation infrastructure that serves this full spectrum on a single runtime. Rather than homogenize the regimes, it preserves their heterogeneity behind three narrow abstractions -- task, resource, and result -- each realized as one invariant shared by every subsystem: one episode driver, one resource-handle protocol implemented by every expensive backend (LLM inference and sandboxed runtimes alike), and one trace identity scheme under which every event is written. Deployed in production across all three layers of an embodied humanoid stack, this single set of invariants onboards new benchmarks largely by configuration. Where mature peer orchestrators exist -- at the foundation-model end -- it reproduces published references and peer-framework readings within their own spread, runs the same suites faster on a single node, and scales near-linearly across nodes. Its distinctive return is diagnostic: because every layer writes into one shared trace, a regression that begins in one layer and surfaces in another stays localizable on that trace -- a cross-layer payoff no federation of per-segment harnesses can reproduce.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.