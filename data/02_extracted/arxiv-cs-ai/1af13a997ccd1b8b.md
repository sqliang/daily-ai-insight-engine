---
title: 'KVBoost: Chunk-Level Key-Value Cache Reuse with Deviation-Guided Recomputation
  for Efficient Large Language Model Inference'
source: https://arxiv.org/abs/2608.21362
author:
- '[[Srihari Unnikrishnan]]'
published: '2026-08-25'
created: '2026-08-25'
manifest_dates:
- '2026-08-25'
description: 'arXiv:2608.21362v1 Announce Type: new Abstract: Transformer-based large
  language models (LLMs) incur high prefill latency because key-value (KV) tensors
  must be recomputed for each request. Existing prefix-caching systems reduce this
  cost but require prompts to share a leading contiguous prefix, limiting effectiveness
  when shared content appears at arbitrary positions. We present KVBoost, a chunk-level
  KV cache reuse system for HuggingFace-compatible decoder models that enables reuse
  regardless of content position. KVBoost introduces a dual-hash keying scheme that
  separates positional identity (prefix hash) from content identity (content hash),
  supporting both exact and approximate cache matches. To address attention boundary
  errors from independently cached chunks, KVBoost employs two repair strategies:
  SelectiveRecompute, which re-encodes boundary regions, and CacheBlendRecompute,
  which identifies and recomputes high-deviation tokens after a probe pass. The system
  further incorporates asymmetric KV quantization (int8/int4), adaptive chunk boundary
  splitting, and importance-weighted eviction under a fixed memory budget. Evaluated
  on Qwen/Qwen2.5-3B over 1,000 bug-localization samples, KVBoost achieves a 4.49x
  reduction in time-to-first-token (142.4 ms vs.\ 639.1 ms) and outperforms prefix
  caching by 16%, with no loss in accuracy (99.2% vs.\ 99.1%). KVBoost provides a
  practical, memory-bounded inference acceleration layer compatible with RoPE-based
  models without architectural modification.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 1af13a997ccd1b8b
source_type: academic_paper
tldr: KVBoost 提出分块级 KV 缓存复用系统，通过双哈希键控与偏差引导重算实现任意位置的缓存复用，在 Qwen2.5-3B 上将首 token 时延从
  639.1 毫秒降至 142.4 毫秒，实现 4.49 倍加速并较前缀缓存提升 16%，准确率保持 99.2% 无损。
objective_summary: KVBoost 是面向 HuggingFace 兼容解码器模型的推理加速系统，通过分块级 KV 缓存复用实现与内容位置无关的缓存命中。系统采用双哈希键控方案，以前缀哈希区分位置身份、内容哈希区分内容身份，并使用
  SelectiveRecompute 与 CacheBlendRecompute 修复独立缓存分块带来的注意力边界误差，同时集成 int8/int4 非对称量化、自适应分块边界切分和基于重要度的淘汰机制。在
  Qwen/Qwen2.5-3B 模型的 1000 个缺陷定位样本评测中，KVBoost 将首 token 时延从 639.1 毫秒降至 142.4 毫秒，实现
  4.49 倍加速并较前缀缓存提升 16%，准确率几乎无损（99.2% 对比 99.1%）。
event_type: infrastructure_update
epistemic_status: theoretical_claim
entities:
  companies:
  - Alibaba
  - HuggingFace
  technologies:
  - KV cache
  - RoPE
  - prefix caching
  - dual-hash keying
  - int8/int4 quantization
  - TTFT
  key_people: []
key_logic_flow:
- KVBoost 提出一种面向 HuggingFace 兼容解码器模型的分块级 KV 缓存复用系统，允许共享内容出现在任意位置时都能复用缓存。
- 系统引入双哈希键控方案，用前缀哈希标识位置身份、内容哈希标识内容身份，从而同时支持精确匹配与近似匹配两种缓存命中方式。
- 针对独立缓存分块引起的注意力边界误差，KVBoost 提出 SelectiveRecompute 与 CacheBlendRecompute 两种修复策略，前者重编码边界区域，后者在探测后重算高偏差
  token。
- 系统还整合了非对称 KV 量化（int8/int4）、自适应分块边界切分以及固定内存预算下基于重要度的淘汰机制。
- 在 Qwen/Qwen2.5-3B 模型的 1000 个缺陷定位样本上，KVBoost 将首 token 时延从 639.1 毫秒降至 142.4 毫秒，实现
  4.49 倍加速。
- 相比前缀缓存方案，KVBoost 提升 16% 且准确率几乎无损（99.2% 对比 99.1%），可作为无需修改架构的内存有界推理加速层。
object_mentions:
- object_type: project
  name: KVBoost
  canonical_name: KVBoost
  url: https://arxiv.org/abs/2608.21362
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - KVBoost 是一种面向 HuggingFace 兼容解码器模型的分块级 KV 缓存复用系统，能够不受内容位置限制地实现缓存复用。
  - KVBoost 通过双哈希键控方案将位置身份与内容身份分离，同时支持精确匹配与近似匹配两种缓存命中方式。
  - 在 Qwen2.5-3B 的评测中，KVBoost 将首 token 时延从 639.1 毫秒降至 142.4 毫秒，实现 4.49 倍加速且准确率几乎无损。
  article_id: 1af13a997ccd1b8b
- object_type: model
  name: Qwen/Qwen2.5-3B
  canonical_name: Qwen2.5-3B
  url: null
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 评测在 Qwen/Qwen2.5-3B 模型上基于 1000 个缺陷定位样本展开，用于验证 KVBoost 的推理加速效果。
  - KVBoost 在 Qwen2.5-3B 上相比前缀缓存方案提速 16%，准确率保持在 99.2% 对比 99.1% 的几乎无损水平。
  article_id: 1af13a997ccd1b8b
extract_result: success
---

# Computer Science > Artificial Intelligence

# Title:KVBoost: Chunk-Level Key-Value Cache Reuse with Deviation-Guided Recomputation for Efficient Large Language Model Inference

View PDF HTML (experimental)Abstract:Transformer-based large language models (LLMs) incur high prefill latency because key-value (KV) tensors must be recomputed for each request. Existing prefix-caching systems reduce this cost but require prompts to share a leading contiguous prefix, limiting effectiveness when shared content appears at arbitrary positions. We present KVBoost, a chunk-level KV cache reuse system for HuggingFace-compatible decoder models that enables reuse regardless of content position. KVBoost introduces a dual-hash keying scheme that separates positional identity (prefix hash) from content identity (content hash), supporting both exact and approximate cache matches. To address attention boundary errors from independently cached chunks, KVBoost employs two repair strategies: SelectiveRecompute, which re-encodes boundary regions, and CacheBlendRecompute, which identifies and recomputes high-deviation tokens after a probe pass. The system further incorporates asymmetric KV quantization (int8/int4), adaptive chunk boundary splitting, and importance-weighted eviction under a fixed memory budget. Evaluated on Qwen/Qwen2.5-3B over 1,000 bug-localization samples, KVBoost achieves a 4.49x reduction in time-to-first-token (142.4 ms vs.\ 639.1 ms) and outperforms prefix caching by 16%, with no loss in accuracy (99.2% vs.\ 99.1%). KVBoost provides a practical, memory-bounded inference acceleration layer compatible with RoPE-based models without architectural modification.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.