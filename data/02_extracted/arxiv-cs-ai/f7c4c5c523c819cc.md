---
title: 'AgentKVShift: Efficient KV Cache Reuse for Agentic Memory Systems'
source: https://arxiv.org/abs/2607.21604
author:
- '[[Nilesh Prasad Pandey, Jason Kong, Lanxiang Hu, Quanling Zhao, Yujie Zhao, Onat
  Gungor, Hao Zhang, Tajana Rosing]]'
published: '2026-07-27'
created: '2026-07-27'
manifest_dates:
- '2026-07-27'
description: 'arXiv:2607.21604v1 Announce Type: new Abstract: Memory-augmented LLM
  agents maintain context across hundreds of interactions through agentic memory systems
  that actively curate retrieved content with LLM-generated metadata such as summaries,
  keywords, and tags. From an inference cost standpoint, every retrieval triggers
  a full re-encoding of these structured memory units into Key-Value (KV) states,
  which dominates prefill latency. Existing training-free KV reuse methods mitigate
  this by selectively recomputing a small fraction of tokens, but were designed for
  RAG-style raw passages and degrade on structured agentic memories. We present AgentKVShift,
  a training-free, probe-guided KV residual correction method that operates per retrieved
  memory unit. One of the crucial insights we demonstrate is that the per-memory KV
  reuse residual decomposes into a shared memory-level offset plus small token-wise
  fluctuations. Estimating this offset from a small probe set allows us to correct
  every reused token by a single weighted correction. Unlike prior reuse methods which
  decide which tokens to recompute and leave the rest of the cache stale, AgentKVShift
  also corrects the tokens it does not recompute, turning the refresh budget into
  useful signal across the entire chunk. Across four open-source LLMs spanning 3B
  to 32B parameters and two long-horizon agentic memory benchmarks (long-term dialogue
  and agentic applications), AgentKVShift achieves near full recompute performance
  while refreshing only 10-30% of the cache, outperforming baselines at the same recompute
  ratio. It requires up to 5x lower recompute to reach this near-full performance,
  which prior reuse methods only attain at 45-55% refresh. In this regime, AgentKVShift
  delivers prefill speedups of 2-3.5x over no-KV-reuse on a single A100. AgentKVShift
  orthogonally composes with KV cache quantization, retaining over 2x the F1 of prior
  reuse methods under aggressive 2- and 4-bit settings.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: f7c4c5c523c819cc
source_type: academic_paper
tldr: AgentKVShift 是一种无需训练的 KV 缓存重用方法，通过探针引导的残差校正机制，在仅刷新 10-30% 缓存的条件下使智能体记忆系统达到接近完全重计算的性能，预填充速度提升
  2-3.5 倍。
objective_summary: 研究者提出了 AgentKVShift，一种无需训练、探针引导的 KV 残差校正方法，专门优化智能体记忆系统中结构化记忆单元的
  KV 状态重用。该方法发现每条记忆的 KV 重用残差可分解为共享的记忆级偏移加上微小的逐词元波动，通过小探针集估计该偏移量后一次性校正所有重用词元。在 3B 到
  32B 参数的四个开源 LLM 以及两个长周期智能体记忆基准测试（长期对话和智能体应用）上，AgentKVShift 以 10-30% 缓存刷新率达到接近完全重计算的性能，所需重计算量仅为现有方法的五分之一，在单个
  A100 上实现了 2-3.5 倍的预填充加速。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - KV Cache
  - AgentKVShift
  - LLM
  - RAG
  - KV Cache Quantization
  key_people: []
key_logic_flow:
- AgentKVShift 是一种无需训练的、基于探针引导的 KV 残差校正方法，专门针对智能体记忆系统中的结构化记忆单元进行优化，对每个检索到的记忆单元独立操作。
- 该方法的核心发现是每条记忆的 KV 重用残差可分解为一个共享的记忆级偏移加上微小的逐词元波动，通过小探针集估计该偏移量即可一次性校正所有重用词元。
- 与现有 KV 重用方法仅决定哪些词元需重计算而让其余缓存保持过期不同，AgentKVShift 还对未重计算的词元进行校正，使刷新预算在整个块中转化为有效信号。
- 在 3B 到 32B 参数的四个开源 LLM 以及两个长周期智能体记忆基准测试（长期对话和智能体应用）上，AgentKVShift 在仅刷新 10-30% 缓存的条件下达到接近完全重计算的性能。
- AgentKVShift 达到完全重计算性能所需的重计算量仅为现有方法（需 45-55% 刷新率）的五分之一，在单个 A100 上实现了 2-3.5 倍的预填充速度提升。
- AgentKVShift 可与 KV 缓存量化正交组合使用，在激进的 2-bit 和 4-bit 量化设置下，F1 值仍达到现有重用方法的两倍以上。
object_mentions:
- object_type: paper
  name: 'AgentKVShift: Efficient KV Cache Reuse for Agentic Memory Systems'
  canonical_name: AgentKVShift
  url: https://arxiv.org/abs/2607.21604
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - AgentKVShift 是一种无需训练的、探针引导的 KV 残差校正方法，专门针对智能体记忆系统中结构化记忆单元的 KV 重用进行优化，对每个检索到的记忆单元独立操作。
  - 该方法在仅刷新 10% 到 30% 缓存的条件下，使长周期智能体记忆任务（长期对话和智能体应用）达到接近完全重计算的性能水平。
  - AgentKVShift 在单个 A100 GPU 上实现了 2 到 3.5 倍的预填充加速效果，并且可与 KV 缓存量化正交组合，在 2-bit 和 4-bit
    设置下 F1 值达到现有方法的两倍以上。
  article_id: f7c4c5c523c819cc
extract_result: success
---

# Computer Science > Artificial Intelligence

# Title:AgentKVShift: Efficient KV Cache Reuse for Agentic Memory Systems

View PDF HTML (experimental)Abstract:Memory-augmented LLM agents maintain context across hundreds of interactions through agentic memory systems that actively curate retrieved content with LLM-generated metadata such as summaries, keywords, and tags. From an inference cost standpoint, every retrieval triggers a full re-encoding of these structured memory units into Key-Value (KV) states, which dominates prefill latency. Existing training-free KV reuse methods mitigate this by selectively recomputing a small fraction of tokens, but were designed for RAG-style raw passages and degrade on structured agentic memories. We present AgentKVShift, a training-free, probe-guided KV residual correction method that operates per retrieved memory unit. One of the crucial insights we demonstrate is that the per-memory KV reuse residual decomposes into a shared memory-level offset plus small token-wise fluctuations. Estimating this offset from a small probe set allows us to correct every reused token by a single weighted correction. Unlike prior reuse methods which decide which tokens to recompute and leave the rest of the cache stale, AgentKVShift also corrects the tokens it does not recompute, turning the refresh budget into useful signal across the entire chunk. Across four open-source LLMs spanning 3B to 32B parameters and two long-horizon agentic memory benchmarks (long-term dialogue and agentic applications), AgentKVShift achieves near full recompute performance while refreshing only 10-30% of the cache, outperforming baselines at the same recompute ratio. It requires up to 5x lower recompute to reach this near-full performance, which prior reuse methods only attain at 45-55% refresh. In this regime, AgentKVShift delivers prefill speedups of 2-3.5x over no-KV-reuse on a single A100. AgentKVShift orthogonally composes with KV cache quantization, retaining over 2x the F1 of prior reuse methods under aggressive 2- and 4-bit settings.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.