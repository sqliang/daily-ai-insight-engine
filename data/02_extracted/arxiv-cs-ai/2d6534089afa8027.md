---
title: 'EntropyMoE: Entropy-Aware Sparse Expert Routing for Tokenizer-Free LLMs'
source: https://arxiv.org/abs/2608.06398
author:
- '[[Bo Liu, Muxuab Yu, Yu Zhang, Pengfei Gao, Yongping Zhang]]'
published: '2026-08-10'
created: '2026-08-10'
manifest_dates:
- '2026-08-10'
description: 'arXiv:2608.06398v1 Announce Type: new Abstract: Recent byte-level large
  language models (LLMs) have made tokenizer-free modeling increasingly competitive
  by grouping bytes into dynamically sized patches. However, existing byte-patch architectures
  still apply the same dense feed-forward computation to every patch. This uniform
  computation cannot adapt model capacity to variations in patch semantics and granularity.
  We address this limitation with EntropyMoE, a Mixture-of-Experts (MoE) architecture
  designed for dynamic byte patches. EntropyMoE replaces the dense feed-forward modules
  in the global patch Transformer with Top-K expert layers. Each dynamic patch serves
  as the basic unit of expert routing, and its byte coverage determines its contribution
  to workload accounting. The router selects experts directly from patch entropy,
  using the same granularity signal that underlies dynamic patch construction to organize
  sparse computation. Patch entropy and length jointly define the feature space for
  regulating expert specialization. Experiments show that EntropyMoE achieves the
  lowest held-out bits-per-byte among matched dense and sparse baselines while maintaining
  comparable downstream accuracy. These results establish patch entropy as an effective
  routing coordinate for sparse conditional computation and extend Mixture-of-Experts
  modeling beyond tokenizer-based representations.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 2d6534089afa8027
source_type: academic_paper
tldr: EntropyMoE 是一种面向无分词器大语言模型设计的混合专家架构，以动态字节补丁为基本路由单元，直接用补丁熵作为路由坐标执行 Top-K 专家路由。实验表明其取得最低的留出
  bits-per-byte，同时保持与稠密和稀疏基线相当的下游准确率。
objective_summary: arXiv 论文提出 EntropyMoE，针对现有字节级大语言模型对每个动态字节补丁仍执行相同稠密前馈计算、无法适应补丁语义与粒度变化的局限，用混合专家层替换全局补丁
  Transformer 中的稠密前馈模块。每个动态补丁作为专家路由的基本单元，其字节覆盖范围决定工作量核算贡献，路由器直接根据补丁熵选择专家，补丁熵与长度共同定义特征空间以规范专家特化。实验结果显示，EntropyMoE
  在留出数据上取得最低的 bits-per-byte，同时保持与匹配的稠密和稀疏基线相当的下游准确率。
event_type: infrastructure_update
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - EntropyMoE
  - Mixture-of-Experts
  - Top-K expert routing
  - byte-patch architecture
  - tokenizer-free LLM
  key_people: []
key_logic_flow:
- 现有字节级大语言模型通过将字节分组为动态大小的补丁实现无分词器建模，但所有补丁仍采用相同的稠密前馈计算，无法根据补丁语义和粒度调整模型容量。
- EntropyMoE 用混合专家架构替换全局补丁 Transformer 中的稠密前馈模块，每个动态补丁作为专家路由的基本单元，其字节覆盖范围决定在工作量核算中的贡献。
- 路由器直接利用补丁熵作为路由坐标选择专家，以构建动态补丁的同一粒度信号来组织稀疏计算，补丁熵与长度共同定义规范专家特化的特征空间。
- 实验表明 EntropyMoE 在留出数据上取得最低的 bits-per-byte，优于匹配的稠密和稀疏基线，同时保持相当的下游准确率。
- 该工作将补丁熵确立为稀疏条件计算的有效路由坐标，并把混合专家建模扩展到基于分词器表示之外。
object_mentions:
- object_type: model
  name: EntropyMoE
  canonical_name: EntropyMoE
  url: https://arxiv.org/abs/2608.06398
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - EntropyMoE 是一种面向动态字节补丁设计的混合专家架构，用 Top-K 专家层替换全局补丁 Transformer 中的稠密前馈模块。
  - 路由器直接根据补丁熵选择专家，补丁熵与长度共同定义特征空间来规范专家特化，实现稀疏条件计算。
  - 实验显示 EntropyMoE 在留出数据上取得最低的 bits-per-byte，同时保持与匹配稠密和稀疏基线相当的下游准确率。
  article_id: 2d6534089afa8027
extract_result: success
---

# Computer Science > Artificial Intelligence

# Title:EntropyMoE: Entropy-Aware Sparse Expert Routing for Tokenizer-Free LLMs

View PDF HTML (experimental)Abstract:Recent byte-level large language models (LLMs) have made tokenizer-free modeling increasingly competitive by grouping bytes into dynamically sized patches. However, existing byte-patch architectures still apply the same dense feed-forward computation to every patch. This uniform computation cannot adapt model capacity to variations in patch semantics and granularity. We address this limitation with EntropyMoE, a Mixture-of-Experts (MoE) architecture designed for dynamic byte patches. EntropyMoE replaces the dense feed-forward modules in the global patch Transformer with Top-K expert layers. Each dynamic patch serves as the basic unit of expert routing, and its byte coverage determines its contribution to workload accounting. The router selects experts directly from patch entropy, using the same granularity signal that underlies dynamic patch construction to organize sparse computation. Patch entropy and length jointly define the feature space for regulating expert specialization. Experiments show that EntropyMoE achieves the lowest held-out bits-per-byte among matched dense and sparse baselines while maintaining comparable downstream accuracy. These results establish patch entropy as an effective routing coordinate for sparse conditional computation and extend Mixture-of-Experts modeling beyond tokenizer-based representations.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.