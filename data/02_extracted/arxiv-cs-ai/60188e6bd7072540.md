---
title: Physics-Audited Agentic Discovery in Scientific Machine Learning
source: https://arxiv.org/abs/2607.07379
author:
- '[[Diab W. Abueidda, Bilal Ahmed, Panos Pantidis, Mostafa E. Mobasher]]'
published: '2026-07-09'
created: '2026-07-09'
description: 'arXiv:2607.07379v1 Announce Type: new Abstract: In agentic scientific
  machine learning (SciML), large language model (LLM) agents can discover surrogate
  models and select one by an automated score, typically an error metric. A low error,
  however, does not establish that the predicted fields satisfy the physics that matter
  for mechanics, such as boundary conditions, superposition, stiffness scaling, or
  causality. We introduce Physics-Audited Agentic SciML (PA-SciML), a verification-first
  workflow for agentic SciML discovery. The workflow fixes a scoring evaluator before
  search, derives reviewable machine-checkable physics requirements, checks each trained
  candidate on its outputs, and separately searches prescribed input ranges or measured
  load-history spans for high-violation cases without reference solution fields. A
  surrogate is reported as verified only under the stated checks. When enabled, the
  workflow also adds advisory numerical probes before training and tests one modeling
  change at a time to record which isolated edits are associated with score gains
  before reuse. In the reported computational-solid-mechanics numerical examples,
  the static elasticity run selects a surrogate with lower validation error than the
  error-only baseline while both selected models pass the common linear-elastic checks.
  In the transient elastodynamics run, an error-only baseline with similar mean error
  fails a stricter causality check by responding to future parts of the loading history,
  while the selected surrogate passes the stated checks. The main distinction is per-candidate
  physics evidence on predicted fields, not a richer aggregate score.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 60188e6bd7072540
source_type: academic_paper
tldr: PA-SciML（Physics-Audited Agentic SciML）提出一种验证优先的代理科学机器学习工作流，在发现代理模型时不仅依赖误差指标，还要求模型通过边界条件、因果性等物理约束检查，并在静弹性与瞬态弹性动力学数值实验中验证了该方法优于纯误差基线。
objective_summary: arXiv 论文《Physics-Audited Agentic Discovery in Scientific Machine
  Learning》提出了 PA-SciML 工作流，该方法在代理模型搜索前固定评分评估器，推导可审查的机器可检查物理需求，对每个候选模型输出进行物理约束验证，无需参考解即可搜索高违规案例。在静弹性力学实验中，PA-SciML
  选出的代理模型验证误差低于纯误差基线；在瞬态弹性动力学实验中，纯误差基线因未能通过因果性检查而失败，PA-SciML 选出的模型则通过了所有物理约束检查。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - PA-SciML
  - SciML
  - LLM
  - surrogate model
  key_people: []
key_logic_flow:
- 代理科学机器学习使用 LLM 代理自动发现代理模型，传统方法仅依赖误差指标进行模型选择。
- 低误差并不能保证模型满足力学相关的物理约束，如边界条件、叠加原理、刚度缩放和因果性。
- PA-SciML 在搜索前固定评分评估器，推导可审查的机器可检查物理需求，并对每个候选模型输出进行独立物理验证。
- 该工作流可在不需要参考解的情况下，在规定的输入范围或载荷历史跨度内自动搜索高违规案例。
- 静弹性力学实验中，PA-SciML 选出的代理模型验证误差低于纯误差基线，且两者均通过线性弹性检查。
- 瞬态弹性动力学实验中，纯误差基线因对未来载荷历史做出响应而未能通过因果性检查，PA-SciML 选出的模型则通过了所有既定检查。
specialized_tags:
  paper:
    paperTitle: Physics-Audited Agentic Discovery in Scientific Machine Learning
    authors: []
    affiliations: []
    venue: arXiv preprint
    codeUrl: null
    datasetUrl: null
    researchArea: Other
    methodType: LLM-based
extract_result: success
object_mentions: []
---

# Computer Science > Artificial Intelligence

# Title:Physics-Audited Agentic Discovery in Scientific Machine Learning

View PDF HTML (experimental)Abstract:In agentic scientific machine learning (SciML), large language model (LLM) agents can discover surrogate models and select one by an automated score, typically an error metric. A low error, however, does not establish that the predicted fields satisfy the physics that matter for mechanics, such as boundary conditions, superposition, stiffness scaling, or causality. We introduce Physics-Audited Agentic SciML (PA-SciML), a verification-first workflow for agentic SciML discovery. The workflow fixes a scoring evaluator before search, derives reviewable machine-checkable physics requirements, checks each trained candidate on its outputs, and separately searches prescribed input ranges or measured load-history spans for high-violation cases without reference solution fields. A surrogate is reported as verified only under the stated checks. When enabled, the workflow also adds advisory numerical probes before training and tests one modeling change at a time to record which isolated edits are associated with score gains before reuse. In the reported computational-solid-mechanics numerical examples, the static elasticity run selects a surrogate with lower validation error than the error-only baseline while both selected models pass the common linear-elastic checks. In the transient elastodynamics run, an error-only baseline with similar mean error fails a stricter causality check by responding to future parts of the loading history, while the selected surrogate passes the stated checks. The main distinction is per-candidate physics evidence on predicted fields, not a richer aggregate score.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.