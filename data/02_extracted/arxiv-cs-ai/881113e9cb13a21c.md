---
title: 'LaCache: Exact Caching and Precision-Adaptive Inference for Diffusion Large
  Language Models'
source: https://arxiv.org/abs/2607.16339
author:
- '[[Xingru Chen, Zelang Liang, Yongjia Ma, Jiqing Zhan, Shuling Yang, Lian Wen, Kun
  Zhan]]'
published: '2026-07-21'
created: '2026-07-21'
manifest_dates:
- '2026-07-21'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 881113e9cb13a21c
source_type: academic_paper
tldr: LaCache 是一个针对扩散大语言模型（DLLM）的无训练加速框架，通过无损状态记忆缓存（EmbedCache、RoPECache、FACache）和逐组
  FP8 量化，在不改变输出的前提下跳过冗余计算，单独实现约 1.3 倍加速，结合现有方法最高可达 40.2 倍端到端加速。
objective_summary: 该论文提出 LaCache 框架，用于加速扩散大语言模型的半自回归解码过程。LaCache 通过三种缓存机制（EmbedCache
  缓存嵌入输出、RoPECache 缓存逐词预注意力状态、FACache 缓存 FlashAttention 在线 softmax 统计量）实现无损状态记忆化，跳过不变标记的重复计算。同时针对
  FFN 层引入逐组 FP8 量化策略，缓解内存带宽瓶颈。实验结果显示，LaCache 单独使用可实现约 1.3 倍端到端加速，与现有加速方法结合可达 40.2
  倍，且保持任务精度相当。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - DLLM
  - LaCache
  - Semi-Autoregressive (SAR)
  - Lossless State Memoization (LSM)
  - FlashAttention
  - FP8
  - EmbedCache
  - RoPECache
  - FACache
  key_people: []
key_logic_flow:
- 扩散大语言模型（DLLM）通过半自回归（SAR）解码实现并行文本生成，但现有方法在去噪步骤中重复计算整个序列，存在严重的算子级冗余。
- LaCache 提出无损状态记忆化（LSM），缓存三类中间结果：嵌入层输出的 EmbedCache、逐词预注意力状态的 RoPECache、以及 FlashAttention
  内在线 softmax 统计量的 FACache。
- 缓存机制使模型在去噪过程中直接跳过不变标记的冗余计算，且保证输出结果完全不变。
- 针对 FFN 层引入逐组 FP8 量化策略，根据扩散过程中逐步骤的激活分布特征进行适配，以缓解内存带宽瓶颈。
- 实验表明 LaCache 单独使用可实现约 1.3 倍端到端加速，与现有加速方法结合后最高达到 40.2 倍端到端加速。
- LaCache 是一个无需训练即可直接应用的加速框架，在加速的同时保持与原始模型相当的任务精度。
object_mentions:
- object_type: project
  name: LaCache
  canonical_name: LaCache
  url: https://arxiv.org/abs/2607.16339
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - LaCache 是一个针对扩散大语言模型的无训练加速框架，通过无损状态记忆化和混合精度技术缓解算子级冗余。
  - LaCache 单独实现约 1.3 倍端到端加速，与现有方法结合最高可达 40.2 倍端到端加速，且保持任务精度相当。
  - LaCache 采用三类缓存机制：EmbedCache、RoPECache 和 FACache，分别缓存嵌入输出、预注意力状态和 FlashAttention
    在线 softmax 统计量。
  article_id: 881113e9cb13a21c
extract_result: success
---

# Computer Science > Artificial Intelligence

# Title:LaCache: Exact Caching and Precision-Adaptive Inference for Diffusion Large Language Models

View PDF HTML (experimental)Abstract:Diffusion-based Large Language Models(DLLMs) enable parallel generation via Semi-Autoregressive (SAR) decoding in text generation. However, current methods suffer from severe operator-level redundancy: they recompute the entire sequence during denoising steps, ignoring that the prefix and masked suffix remain invariant within a block. We propose LaCache, a training-free acceleration framework that alleviates this redundancy through lossless caching and mixed precision. Specifically, LaCache employs Lossless State Memoization (LSM) by caching three types of intermediate results: (i) EmbedCache for embedding outputs, (ii) RoPECache for token-wise pre-attention states, and (iii) FACache for the online softmax statistics within FlashAttention. These caches allow the model to skip redundant computation on unchanged tokens without altering the output. To further alleviate memory-bandwidth bottlenecks, LaCache inegrates a per-group FP8 quantization strategy for FFN layers, tailored to step-dependent activation distributions across the diffusion process. Experiments demonstrate that LaCache alone achieves approximately 1.3X end-to-end speedup over vanilla DLLM. When combined with existing acceleration methods, LaCache reaches up to 40.2X end-to-end speedup while maintaining comparable task accuracy.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.