---
title: 'ROZA Graphs: Self-Improving Near-Deterministic RAG through Evidence-Centric
  Feedback'
source: https://arxiv.org/abs/2604.07595
author:
- '[[Matthew Penaroza]]'
published: '2026-05-07'
created: '2026-05-08'
description: 'arXiv:2604.07595v3 Announce Type: replace Abstract: Language model agents
  reason from scratch on every query, discarding their chain of thought after each
  run. The result is lower accuracy and high run-to-run variance. We introduce reasoning
  graphs, which persist the per-evidence chain of thought as structured edges. Unlike
  prior memory that retrieves distilled strategies by query similarity, reasoning
  graphs enable evidence-centric feedback: for every candidate item, the system traverses
  all incoming evaluation edges across prior runs to surface how that specific item
  has been judged before. We further introduce retrieval graphs, which feed a planner
  that prunes consistently-rejected candidates over successive runs. Together they
  form a ROZA graph: a self-improving feedback loop in which accuracy gains scale
  with gold-passage reuse (reasoning graph) and efficiency gains scale with candidate-pool
  overlap (retrieval graph). The base model remains frozen; all gains come from context
  engineering via graph traversal. We evaluate on MuSiQue and HotpotQA, plus a high-reuse
  deployment subset. Four findings stand out. (1) Dose-response: accuracy improves
  monotonically with evidence-profile coverage, reaching +10.6pp over Vanilla RAG
  at 50%+ coverage on the same questions (47% error reduction, $p<0.0001$; per-question
  Spearman $\rho=+0.144$, $p<10^{-6}$, $n=1{,}100$). (2) Multi-hop scaling: 4-hop
  accuracy improves by +11.0pp ($p=0.0001$). (3) Cross-cluster prediction: the cluster-level
  gain is predicted by gold-passage reuse density ($r=0.604$, $p=0.001$, $n=26$ clusters).
  (4) High-reuse Pareto dominance: highest or tied-for-highest accuracy alongside
  46% lower cost and 46% lower latency. Per-passage decision consistency across repeated
  runs ($N=73$ paired probes, $K=10$ runs each, two model families, three temperatures)
  rises by +8 to +13pp on a fixed 20-passage context and by +12 to +21pp when the
  retrieval graph also prunes (all $p<0.005$).'
tags:
- clippings
id: 78df007cc0572058
source_type: academic_paper
tldr: ROZA Graphs 通过证据中心反馈和检索图剪枝，实现自改进的近确定性 RAG，在多项基准上提升准确率并降低成本和延迟。
objective_summary: 该论文提出 ROZA Graphs，将推理链持久化为结构化边（推理图）并通过检索图剪枝被拒绝的候选项。在 MuSiQue 和
  HotpotQA 上的评估显示，50%+覆盖率下准确率比 Vanilla RAG 提升 10.6 个百分点（误差减少 47%），4 跳准确率提升 11.
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - RAG
  - ROZA Graphs
  - reasoning graphs
  - retrieval graphs
  - evidence-centric feedback
  - MuSiQue
  - HotpotQA
  key_people: []
key_logic_flow:
- ROZA Graphs 由推理图（reasoning graph）和检索图（retrieval graph）组成：推理图持久化每条证据的推理链作为结构化边，使新查询可检索到该证据过往所有评估记录；检索图将信息反馈给规划器，连续剪枝被一致拒绝的候选项。
- 基础模型保持冻结，所有性能提升来自通过图遍历实现的上下文工程（context engineering）。
- 在 MuSiQue 和 HotpotQA 上的评估显示，准确率随证据画像覆盖率单调提升，50%+覆盖率下比 Vanilla RAG 高 10.6 个百分点，误差减少
  47%。
- 多跳推理场景中，4 跳问题准确率提升 11.0 个百分点。
- 高复用场景下，ROZA Graphs 在保持最高或并列最高准确率的同时，实现成本降低 46%、延迟降低 46%，呈现帕累托最优。
- 单次问答的逐证据决策一致性提升 8 到 21 个百分点，检索图进一步剪枝时可额外提升 12 到 21 个百分点。
---

# Computer Science > Artificial Intelligence

# Title:ROZA Graphs: Self-Improving Near-Deterministic RAG through Evidence-Centric Feedback

View PDF HTML (experimental)Abstract:Language model agents reason from scratch on every query, discarding their chain of thought after each run. The result is lower accuracy and high run-to-run variance. We introduce reasoning graphs, which persist the per-evidence chain of thought as structured edges. Unlike prior memory that retrieves distilled strategies by query similarity, reasoning graphs enable evidence-centric feedback: for every candidate item, the system traverses all incoming evaluation edges across prior runs to surface how that specific item has been judged before. We further introduce retrieval graphs, which feed a planner that prunes consistently-rejected candidates over successive runs. Together they form a ROZA graph: a self-improving feedback loop in which accuracy gains scale with gold-passage reuse (reasoning graph) and efficiency gains scale with candidate-pool overlap (retrieval graph). The base model remains frozen; all gains come from context engineering via graph traversal. We evaluate on MuSiQue and HotpotQA, plus a high-reuse deployment subset. Four findings stand out. (1) Dose-response: accuracy improves monotonically with evidence-profile coverage, reaching +10.6pp over Vanilla RAG at 50%+ coverage on the same questions (47% error reduction, $p<0.0001$; per-question Spearman $\rho=+0.144$, $p<10^{-6}$, $n=1{,}100$). (2) Multi-hop scaling: 4-hop accuracy improves by +11.0pp ($p=0.0001$). (3) Cross-cluster prediction: the cluster-level gain is predicted by gold-passage reuse density ($r=0.604$, $p=0.001$, $n=26$ clusters). (4) High-reuse Pareto dominance: highest or tied-for-highest accuracy alongside 46% lower cost and 46% lower latency. Per-passage decision consistency across repeated runs ($N=73$ paired probes, $K=10$ runs each, two model families, three temperatures) rises by +8 to +13pp on a fixed 20-passage context and by +12 to +21pp when the retrieval graph also prunes (all $p<0.005$).

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.