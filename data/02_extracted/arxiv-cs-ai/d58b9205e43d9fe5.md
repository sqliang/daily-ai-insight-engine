---
title: 'Second Thought: Reasoning in Parallel as LLM Agents Act and Observe'
source: https://arxiv.org/abs/2608.13667
author:
- '[[Zhensu Sun, Chengran Yang, Yunbo Lyu, Jieke Shi, David Lo]]'
published: '2026-08-17'
created: '2026-08-17'
manifest_dates:
- '2026-08-17'
description: 'arXiv:2608.13667v1 Announce Type: new Abstract: LLM agents in the ReAct
  paradigm alternate between reasoning, acting, and observing, but deliberate reasoning
  is confined to the Thought phase: while the agent serializes an action and waits
  for the environment, its reasoning is frozen. We identify this recurring interval
  for Action and Observation as a reasoning idle window and ask whether it can host
  additional reasoning in parallel that serves future turns. Therefore, we propose
  Second Thought, a training-free inference framework that forks four auxiliary branches
  the instant each Thought phase concludes, decodes them concurrently with the main
  loop, and merges the generated thoughts back when the environment observation arrives.
  In this way, Second Thought relocates the added reasoning off the main thread''s
  sequential decoding path. Across three agentic benchmarks and three reasoning LLMs,
  Second Thought lowers the average turn count in all nine (model,benchmark) pairs
  and reduces main thread decoding in six of them by up to 43% (roughly 20% on average
  among those settings), while leaving it essentially unchanged in a seventh; Pass@1
  shows no significant change in seven of nine pairs and the two significant differences
  are +12.4 and +10.2 points. Against a compute-matched control that forces an equivalent
  budget onto the main thread''s own reasoning, it attains strictly higher Pass@1
  with 1.3 to 3.2 less sequential decoding in all four settings where the control
  applies.'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: d58b9205e43d9fe5
source_type: academic_paper
tldr: arXiv 论文提出 Second Thought，一个无需训练的推理框架：在 ReAct 范式的 LLM 智能体行动与观察等待期间，并行派生四个辅助分支进行思考，待环境观察返回时合并。在九个（模型×基准）组合中均降低平均轮次，六个组合的主线程解码最高减少
  43%。
objective_summary: arXiv 论文 2608.13667 提出 Second Thought，一个针对 ReAct 范式 LLM 智能体的免训练推理框架。其思路是在每个思考阶段结束的瞬间派生四个辅助分支，与主循环并发解码，并在环境观察返回时合并生成的想法，从而把额外推理移出主线程的顺序解码路径。实验覆盖三个智能体基准与三个推理
  LLM，结果显示所有九个（模型、基准）组合的平均轮次均下降，六个组合的主线程解码最多减少 43%（平均约 20%），七个组合的 Pass@1 无显著变化，两个显著提升为
  +12.4 与 +10.2 分。在与计算量匹配的对照组比较中，四个适用场景里 Second Thought 均以更少的顺序解码取得严格更高的 Pass@1。
event_type: framework_tools
epistemic_status: theoretical_claim
entities:
  companies: []
  technologies:
  - ReAct
  - Second Thought
  - LLM agents
  - Pass@1
  key_people: []
key_logic_flow:
- ReAct 范式的 LLM 智能体在思考、行动与观察之间交替执行，但在行动序列化和等待环境反馈期间推理被冻结，形成推理空闲窗口。
- Second Thought 是一个无需训练的推理框架，在每个思考阶段结束时派生四个辅助分支，与主循环并发解码，并在环境观察返回时合并生成的想法。
- 该框架将新增的推理从主线程的顺序解码路径上移开，从而在不阻塞主循环的前提下利用空闲窗口进行额外思考。
- 在三个智能体基准和三个推理 LLM 上，Second Thought 在所有九个（模型、基准）组合中均降低了平均轮次。
- 主线程解码在九个组合中的六个上最多减少 43%（这些设置下平均约 20%），第七个组合基本不变；Pass@1 在七个组合中无显著变化，两个显著差异为 +12.4
  和 +10.2 分。
- 与计算量匹配的对照组相比，Second Thought 在全部四个适用场景中以少 1.3 到 3.2 的顺序解码取得了严格更高的 Pass@1。
object_mentions:
- object_type: project
  name: Second Thought
  canonical_name: Second Thought
  url: https://arxiv.org/abs/2608.13667
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Second Thought 是一个无需训练的推理框架，在每个思考阶段结束时派生四个辅助分支，与主循环并发解码，并在环境观察返回时合并生成的想法。
  - 在三个智能体基准和三个推理 LLM 上，Second Thought 在所有九个（模型、基准）组合中均降低了平均轮次，并在六个组合中减少了主线程解码。
  - 在与计算量匹配的对照组比较中，Second Thought 在四个适用场景里均以更少的顺序解码取得了严格更高的 Pass@1。
  article_id: d58b9205e43d9fe5
extract_result: success
---

# Computer Science > Artificial Intelligence

# Title:Second Thought: Reasoning in Parallel as LLM Agents Act and Observe

View PDF HTML (experimental)Abstract:LLM agents in the ReAct paradigm alternate between reasoning, acting, and observing, but deliberate reasoning is confined to the Thought phase: while the agent serializes an action and waits for the environment, its reasoning is frozen. We identify this recurring interval for Action and Observation as a reasoning idle window and ask whether it can host additional reasoning in parallel that serves future turns. Therefore, we propose Second Thought, a training-free inference framework that forks four auxiliary branches the instant each Thought phase concludes, decodes them concurrently with the main loop, and merges the generated thoughts back when the environment observation arrives. In this way, Second Thought relocates the added reasoning off the main thread's sequential decoding path. Across three agentic benchmarks and three reasoning LLMs, Second Thought lowers the average turn count in all nine (model,benchmark) pairs and reduces main thread decoding in six of them by up to 43% (roughly 20% on average among those settings), while leaving it essentially unchanged in a seventh; Pass@1 shows no significant change in seven of nine pairs and the two significant differences are +12.4 and +10.2 points. Against a compute-matched control that forces an equivalent budget onto the main thread's own reasoning, it attains strictly higher Pass@1 with 1.3 to 3.2 less sequential decoding in all four settings where the control applies.

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.