---
title: esengine/DeepSeek-Reasonix
source: https://github.com/esengine/DeepSeek-Reasonix
author: []
published: ''
created: '2026-08-03'
manifest_dates:
- '2026-08-03'
description: 'DeepSeek-native AI coding agent for your terminal. Engineered around
  prefix-cache stability — leave it running. English &nbsp;·&nbsp; 简体中文 &nbsp;·&nbsp;
  Guide &nbsp;·&nbsp; ACP &nbsp;·&nbsp; Spec &nbsp;·&nbsp; Website &nbsp;·&nbsp; Discord
  A DeepSeek-native AI coding agent for your terminal. A config- and plugin-driven
  harness — a single static Go binary, tuned around DeepSeek''s prefix cache so token
  costs stay low across long sessions. Important Community · 加入社区 — bilingual Discord
  for setup help (#help / #求助), workflow showcases, and feature ideas. → https://discord.gg/XF78rEME2D
  Features Config-driven. Providers, the agent, enabled tools, and plugins are all
  declared in reasonix.toml. No hardcoded models. Multi-model & composable. DeepSeek
  ships as a preset; any OpenAI-compatible endpoint is a config entry, not new code.
  Optionally run two models together (executor + planner) in separate, cache-stable
  sessions. Plugin-driven. External tools run as subprocesses over stdio JSON-RPC
  (MCP-compatible). Built-in tools self-register at compile time. Cache-aware context
  maintenance. Startup injects a small stable environment summary, stale tool output
  is snipped/pruned before summary compaction, and the built-in tool schema contract
  is documented for regression review. Zero-friction distribution. CGO_ENABLED=0 single
  binary; cross-compile to six targets with one command. The only dependency is a
  TOML parser. Install Choose the path that matches how you want to use Reasonix.
  The CLI/TUI, desktop app, and VS Code extension all use the same local Reasonix
  engine. Path A: CLI / TUI Install the native binary through npm on any supported
  platform, or use Homebrew on macOS: npm i -g reasonix # any OS; pulls the prebuilt
  native binary brew install esengine/reasonix/reasonix # macOS Prebuilt archives
  (darwin|linux|windows × amd64|arm64) and SHA256SUMS are on every GitHub release.
  Path B: Desktop app Use the official download page for the latest desktop build.
  Platform Package Architecture macOS Universal .dmg or .zip Apple Silicon / Intel
  Windows Installer .exe or portable .zip x64 / ARM64 Linux .deb or .tar.gz x64 Windows
  installers are code-signed through SignPath.io with a free certificate provided
  by the SignPath Foundation. Path C: VS Code extension Complete Path A first. The
  extension does not bundle the CLI; it starts your local reasonix acp backend and
  adds native chat, editor context, tool-call approvals, model selection, and workspace
  sessions. VS Code: install from Visual Studio Marketplace VSCodium / Eclipse Theia:
  install from Open VSX Registry Extension ID: SivanLiu.reasonix-agent · source and
  usage guide Path D: Build from source git clone https://github.com/esengine/DeepSeek-Reasonix.git
  cd DeepSeek-Reasonix make build # -> bin/reasonix(.exe) make cross # -> dist/ (darwin|linux|windows
  × amd64|arm64) Quick start CLI / TUI These commands are for the CLI/TUI installed
  through Path A: reasonix setup # configure a provider and model reasonix # start
  an interactive session reasonix run "implement the TODOs in main.go" In an interactive
  session, run /init when you want Reasonix to create project instructions. Desktop
  app Download the installer for your platform from the official download page, install
  and launch Reasonix, then configure a provider and model in the app. The CLI commands
  above are not required for the desktop app. For advanced CLI usage and configuration,
  see the CLI reference, Guide, and configuration paths. Documentation Getting started:
  Guide · CLI reference · Configuration paths · ACP editor integration Features &
  troubleshooting: Subagent profiles · Context Engine v2 · Capability diagnostics
  · Recovery and updates · Bot guide · Checkpoints & rewind Engineering & migration:
  Spec · Task contracts & pause policy · Tool contract · Migrating from 0.x Star History
  Acknowledgments A small list of folks whose work has shaped Reasonix the most —
  the current top 20 contributors by commit count. The full contributor graph is on
  GitHub. Contributor Contributor Contributor Contributor SivanCola esengine ttmouse
  lifu963 reasonix (anonymous) HUQIANTAO GTC2080 light-front-theory merge-order-check
  (anonymous) Li-Charles-One eghrhegpe wufengfan (anonymous) CVEngineer66 dependabot[bot]
  lanshi17 SuMuxi66 CnsMaple cyq1017 JesonChou XTLine Also a separate thank-you to
  Bernardxu123 for designing the project logo, and to AIGC Link for promoting the
  project on XiaoHongShu. MIT — see LICENSE Built by the community at esengine/DeepSeek-Reasonix
  Support this project If Reasonix has been useful and you''d like to say thanks,
  you can. It stays a coffee, not a contract — donations don''t buy feature priority
  or change how issues get triaged. International — PayPal: paypal.me/yuhuahui 国内
  — 微信支付（扫码）'
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: b5590ec052a4f771
source_type: community_discussion
tldr: esengine 开源的配置与插件驱动智能体运行框架，以单个静态 Go 二进制分发，围绕 DeepSeek 前缀缓存降低长会话 token 成本，提供
  CLI/TUI、桌面应用与 VS Code 扩展三种形态，支持 MCP 兼容插件与 OpenAI 兼容端点。
objective_summary: Reasonix（esengine/DeepSeek-Reasonix）是社区以 MIT 协议开源的 Go 智能体运行框架，核心设计是配置驱动，通过
  reasonix.toml 管理提供商、模型、工具与插件，并以 DeepSeek 为预设模型。插件以子进程方式通过 stdio JSON-RPC（兼容 MCP）运行，上下文维护采用缓存感知策略。项目提供
  npm 预编译二进制、Homebrew、macOS/Windows/Linux 桌面安装包以及 VS Code/VSCodium 扩展等分发渠道，Windows
  安装包经 SignPath.io 签名，并设立双语 Discord 社区。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - DeepSeek
  - esengine
  - SignPath.io
  - SignPath Foundation
  - AIGC Link
  technologies:
  - MCP
  - ACP
  - JSON-RPC
  - Go
  - TOML
  - prefix caching
  key_people:
  - SivanLiu
  - SivanCola
  - Bernardxu123
key_logic_flow:
- Reasonix 是一个配置与插件驱动的智能体运行框架，以单个静态 Go 二进制分发，并针对 DeepSeek 前缀缓存优化，以降低长会话的 token 开销。
- 项目采用 reasonix.toml 声明式配置管理提供商、模型、工具与插件，DeepSeek 作为预设模型，任何 OpenAI 兼容端点均可通过配置接入，并支持执行器与规划器双模型组合运行。
- 外部工具以子进程方式通过 stdio JSON-RPC（兼容 MCP）运行，内置工具在编译期自注册，上下文维护采用缓存感知策略，启动时注入稳定的环境摘要并裁剪过期的工具输出。
- Reasonix 提供 CLI/TUI、桌面应用和 VS Code 扩展三种使用方式，可通过 npm、Homebrew 或 GitHub Release 预编译包安装，桌面端覆盖
  macOS、Windows 和 Linux 三平台。
- 项目以 MIT 协议开源，Windows 安装器通过 SignPath.io 使用 SignPath Foundation 提供的免费证书进行代码签名，并设有双语
  Discord 社区供用户获取帮助。
object_mentions:
- object_type: project
  name: esengine/DeepSeek-Reasonix
  canonical_name: esengine/DeepSeek-Reasonix
  url: https://github.com/esengine/DeepSeek-Reasonix
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - DeepSeek-Reasonix 是一个配置与插件驱动的智能体运行框架，以单个静态 Go 二进制分发，围绕 DeepSeek 前缀缓存优化以降低长会话的
    token 成本。
  - 项目以 MIT 协议开源，支持通过 reasonix.toml 声明式配置管理提供商、模型、工具与插件，任何 OpenAI 兼容端点都可作为配置项接入。
  article_id: b5590ec052a4f771
- object_type: product
  name: reasonix
  canonical_name: reasonix
  url: https://www.npmjs.com/package/reasonix
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - CLI/TUI 可通过 npm（npm i -g reasonix）或 Homebrew（brew install esengine/reasonix/reasonix）安装，预编译包覆盖
    darwin、linux、windows 三种系统的 amd64 与 arm64 架构。
  - CLI 提供 reasonix setup 配置提供商与模型、reasonix 启动交互式会话、reasonix run 执行任务等命令，并在会话中用 /init
    生成项目指令。
  article_id: b5590ec052a4f771
- object_type: product
  name: Reasonix Desktop
  canonical_name: reasonix-desktop
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 桌面应用覆盖 macOS、Windows、Linux 三平台，macOS 提供通用 .dmg 或 .zip，Windows 提供安装器 .exe 或便携 .zip，Linux
    提供 .deb 或 .tar.gz。
  - Windows 安装器通过 SignPath.io 使用 SignPath Foundation 提供的免费证书进行代码签名。
  article_id: b5590ec052a4f771
- object_type: product
  name: reasonix-agent
  canonical_name: SivanLiu.reasonix-agent
  url: https://marketplace.visualstudio.com/items?itemName=SivanLiu.reasonix-agent
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - VS Code 扩展从 Visual Studio Marketplace 安装，扩展 ID 为 SivanLiu.reasonix-agent，VSCodium
    与 Eclipse Theia 用户可从 Open VSX Registry 安装。
  - 该扩展不捆绑 CLI，而是启动本地 reasonix acp 后端，并原生提供聊天、编辑器上下文、工具调用审批、模型选择与工作区会话功能。
  article_id: b5590ec052a4f771
extract_result: success
---

**English**
·
简体中文
·
Guide
·
ACP
·
Spec
·
Website
·
**Discord**

A config- and plugin-driven harness — a single static Go binary, tuned around DeepSeek's prefix cache so token costs stay low across long sessions.

Important

**Community · 加入社区** — bilingual Discord for setup help (`#help`

/ `#求助`

), workflow showcases, and feature ideas. → **https://discord.gg/XF78rEME2D**

**Config-driven.**Providers, the agent, enabled tools, and plugins are all declared in`reasonix.toml`

. No hardcoded models.**Multi-model & composable.**DeepSeek ships as a preset; any OpenAI-compatible endpoint is a config entry, not new code. Optionally run two models together (executor + planner) in separate, cache-stable sessions.**Plugin-driven.**External tools run as subprocesses over stdio JSON-RPC (MCP-compatible). Built-in tools self-register at compile time.**Cache-aware context maintenance.**Startup injects a small stable environment summary, stale tool output is snipped/pruned before summary compaction, and the built-in tool schema contract is documented for regression review.**Zero-friction distribution.**`CGO_ENABLED=0`

single binary; cross-compile to six targets with one command. The only dependency is a TOML parser.

Choose the path that matches how you want to use Reasonix. The CLI/TUI, desktop app, and VS Code extension all use the same local Reasonix engine.

Install the native binary through npm on any supported platform, or use Homebrew on macOS:

```
npm i -g reasonix # any OS; pulls the prebuilt native binary
brew install esengine/reasonix/reasonix # macOS
```

Prebuilt archives (`darwin|linux|windows × amd64|arm64`

) and `SHA256SUMS`

are on
every GitHub release.

Use the official download page for the latest desktop build.

| Platform | Package | Architecture |
|---|---|---|
| macOS | Universal `.dmg` or `.zip` |
Apple Silicon / Intel |
| Windows | Installer `.exe` or portable `.zip` |
x64 / ARM64 |
| Linux | `.deb` or `.tar.gz` |
x64 |

Windows installers are code-signed through SignPath.io with a free certificate provided by the SignPath Foundation.

Complete Path A first. The extension does not bundle the CLI; it starts your
local `reasonix acp`

backend and adds native chat, editor context, tool-call
approvals, model selection, and workspace sessions.

**VS Code:**install from Visual Studio Marketplace**VSCodium / Eclipse Theia:**install from Open VSX Registry**Extension ID:**`SivanLiu.reasonix-agent`

· source and usage guide

```
git clone https://github.com/esengine/DeepSeek-Reasonix.git
cd DeepSeek-Reasonix
make build # -> bin/reasonix(.exe)
make cross # -> dist/ (darwin|linux|windows × amd64|arm64)
```

These commands are for the CLI/TUI installed through Path A:

```
reasonix setup # configure a provider and model
reasonix # start an interactive session
reasonix run "implement the TODOs in main.go"
```

In an interactive session, run `/init`

when you want Reasonix to create project
instructions.

Download the installer for your platform from the official download page, install and launch Reasonix, then configure a provider and model in the app. The CLI commands above are not required for the desktop app.

For advanced CLI usage and configuration, see the **CLI reference**,
**Guide**, and
**configuration paths**.

**Getting started:**Guide · CLI reference · Configuration paths · ACP editor integration**Features & troubleshooting:**Subagent profiles · Context Engine v2 · Capability diagnostics · Recovery and updates · Bot guide · Checkpoints & rewind**Engineering & migration:**Spec · Task contracts & pause policy · Tool contract · Migrating from 0.x

A small list of folks whose work has shaped Reasonix the most — the current top 20 contributors by commit count. The full contributor graph is on GitHub.

| Contributor | Contributor | Contributor | Contributor |
|---|---|---|---|
SivanCola |
esengine |
ttmouse |
lifu963 |
reasonix (anonymous) |
HUQIANTAO |
GTC2080 |
light-front-theory |
merge-order-check (anonymous) |
Li-Charles-One |
eghrhegpe |
wufengfan (anonymous) |
CVEngineer66 |
dependabot[bot] |
lanshi17 |
SuMuxi66 |
CnsMaple |
cyq1017 |
JesonChou |
XTLine |

Also a separate thank-you to **Bernardxu123**
for designing the project logo, and to
AIGC Link for promoting the project on XiaoHongShu.

MIT — see LICENSE

Built by the community at esengine/DeepSeek-Reasonix

**Support this project**

If Reasonix has been useful and you'd like to say thanks, you can. It stays a coffee, not a contract — donations don't buy feature priority or change how issues get triaged.

**International**— PayPal: paypal.me/yuhuahui**国内**— 微信支付（扫码）