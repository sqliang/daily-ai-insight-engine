---
title: 'Probe-Geometry Alignment: Erasing the Cross-Sequence Memorization Signature
  Below Chance'
source: https://arxiv.org/abs/2605.01699
author:
- '[[Anamika Paul Rupa, Anietie Andy]]'
published: '2026-05-07'
created: '2026-05-08'
description: 'arXiv:2605.01699v2 Announce Type: replace-cross Abstract: Recent attacks
  show that behavioural unlearning of large language models leaves internal traces
  recoverable by adversarial probes. We characterise where this retention lives and
  show it can be surgically removed without measurable capability cost. Our central
  protocol is a leave-one-out cross-sequence probe that tests whether a memorisation
  signature generalises across held-out sequences. The signature is real and consistent
  across scale: memorisation-specific gaps of +0.32, +0.19, +0.30 on Pythia-70M, GPT-2
  medium, and Mistral-7B; on Pythia-70M, the random-initialisation control collapses
  to -0.04 at the deepest layer where the pretrained signature peaks. The probe direction
  is causally separable from recall -- projecting it out collapses the signature locally
  (+0.44 -> -0.19) while behavioural recall barely changes -- and a probe trained
  on naturally memorised content does not classify fine-tuning-injected secrets, marking
  two representationally distinct regimes. We then introduce probe-geometry alignment
  (PGA), a surgical erasure that aligns activations along the probe''s live readout
  direction at each depth. PGA drives the cross-sequence probe below random chance
  at all four scales tested (toy depth-4: 0.17; Pythia-70M: 0.07; Mistral-7B: 0.45;
  GPT-2 medium: 0.06 via MD-PGA k=2) and remains robust to six adversarial probe variants.
  Against a re-fitting attacker who trains a fresh probe on PGA-treated activations,
  we extend PGA adversarially, defeating the re-fit probe at every memorisation-relevant
  depth while preserving five zero-shot capability benchmarks within 2.8 percentage
  points per task (mean {\Delta}acc = +0.2pp). The cross-sequence signature is a real,
  causally separable, regime-specific property of pretrained representations -- removable
  below chance with a single rank-one intervention per depth at no measurable capability
  cost.'
tags:
- clippings
id: e97918b989005230
source_type: academic_paper
tldr: 提出PGA方法，可在不损失模型能力的前提下手术级擦除LLM行为去遗忘后残留的记忆化痕迹。
objective_summary: 研究人员发现LLM行为去遗忘后内部仍留有可被探针恢复的痕迹，并提出探针-几何对齐方法，通过在每层沿探针方向对齐激活值，将跨序列记忆化签名降至随机水平以下，且不损害模型能力。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies: []
  technologies:
  - PGA (Probe-Geometry Alignment)
  - cross-sequence probing
  - adversarial probing
  - LLM unlearning
  key_people: []
key_logic_flow:
- 研究发现大型语言模型的行为级去遗忘会在内部表征中留下可被对抗性探针恢复的痕迹。
- 作者提出留一法跨序列探针，能够检测记忆化签名是否泛化到保留序列，并在Pythia-70M、GPT-2 medium和Mistral-7B等多个规模模型上验证了该签名的存在。
- 探针方向与召回功能在因果上可分离——局部投射掉探针方向后记忆化签名消失（+0.44降至-0.19），而行为级召回几乎不变。
- 作者提出探针-几何对齐方法，通过在每一深度沿探针实时读出方向对齐激活值，实现对记忆化痕迹的手术级擦除。
- PGA将跨序列探针性能在全部四个测试规模上降至随机水平以下，并对六种对抗性探针变体保持鲁棒性。
- 扩展后的对抗性PGA能在所有记忆相关深度击败重新训练的探针，同时将五项零样本能力基准测试的性能损失控制在每个任务2.8个百分点以内。
---

# Computer Science > Machine Learning

# Title:Probe-Geometry Alignment: Erasing the Cross-Sequence Memorization Signature Below Chance

View PDF HTML (experimental)Abstract:Recent attacks show that behavioural unlearning of large language models leaves internal traces recoverable by adversarial probes. We characterise where this retention lives and show it can be surgically removed without measurable capability cost. Our central protocol is a leave-one-out cross-sequence probe that tests whether a memorisation signature generalises across held-out sequences. The signature is real and consistent across scale: memorisation-specific gaps of +0.32, +0.19, +0.30 on Pythia-70M, GPT-2 medium, and Mistral-7B; on Pythia-70M, the random-initialisation control collapses to -0.04 at the deepest layer where the pretrained signature peaks. The probe direction is causally separable from recall -- projecting it out collapses the signature locally (+0.44 -> -0.19) while behavioural recall barely changes -- and a probe trained on naturally memorised content does not classify fine-tuning-injected secrets, marking two representationally distinct regimes. We then introduce probe-geometry alignment (PGA), a surgical erasure that aligns activations along the probe's live readout direction at each depth. PGA drives the cross-sequence probe below random chance at all four scales tested (toy depth-4: 0.17; Pythia-70M: 0.07; Mistral-7B: 0.45; GPT-2 medium: 0.06 via MD-PGA k=2) and remains robust to six adversarial probe variants. Against a re-fitting attacker who trains a fresh probe on PGA-treated activations, we extend PGA adversarially, defeating the re-fit probe at every memorisation-relevant depth while preserving five zero-shot capability benchmarks within 2.8 percentage points per task (mean {\Delta}acc = +0.2pp). The cross-sequence signature is a real, causally separable, regime-specific property of pretrained representations -- removable below chance with a single rank-one intervention per depth at no measurable capability cost.

### Current browse context:

# Bibliographic and Citation Tools

# Code, Data and Media Associated with this Article

# Demos

# Recommenders and Search Tools

# arXivLabs: experimental projects with community collaborators

arXivLabs is a framework that allows collaborators to develop and share new arXiv features directly on our website.

Both individuals and organizations that work with arXivLabs have embraced and accepted our values of openness, community, excellence, and user data privacy. arXiv is committed to these values and only works with partners that adhere to them.

Have an idea for a project that will add value for arXiv's community? **Learn more about arXivLabs**.