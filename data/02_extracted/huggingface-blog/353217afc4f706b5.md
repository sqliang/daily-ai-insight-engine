---
title: Record, train, and deploy from one place with Strands Agents, LeRobot, and
  Hugging Face Storage Buckets
source: https://huggingface.co/blog/amazon/strands-lerobot-streaming-data-loop
author: []
published: '2026-08-13'
created: '2026-08-15'
manifest_dates:
- '2026-08-15'
- '2026-08-16'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 353217afc4f706b5
source_type: tech_blog
tldr: Strands Robots 是 AWS 开源的机器人 SDK。本文讲解其流式数据闭环：用同一 Strands Agent 记录机器人演示、直接从 Hugging
  Face Hub 读取数据训练策略，再部署回实体机器人，全程保持 LeRobot 数据集格式不变，并由 Hugging Face Storage Buckets
  充当记录与训练之间的工作层。
objective_summary: AWS 的 Strands Robots 开源 SDK（Apache 2.0）在本系列第二篇文章中展示了完整流式数据闭环。该
  SDK 通过 AgentTools 暴露机器人抽象、仿真与 LeRobot 栈，开发者可记录演示数据、从 Hub 直读数据训练策略，并部署回 SO-101 等实体机器人，数据集全程保持
  LeRobot 格式。LeRobot 格式已被 Hub 上超过 9 万个数据集与模型、8 千多家发布者使用。文章还介绍了 Hugging Face Storage
  Buckets，它是 2026 年 3 月发布的可变、非版本化、基于 Xet 的对象存储库类型，作为记录与训练之间的工作层。
event_type: framework_tools
epistemic_status: pr_statement
entities:
  companies:
  - AWS
  - Hugging Face
  technologies:
  - Strands Robots
  - LeRobot
  - AgentTools
  - Hugging Face Storage Buckets
  - Xet
  key_people: []
key_logic_flow:
- Strands Robots 是 AWS 开源（Apache 2.0）的机器人 SDK，将机器人抽象、仿真与 LeRobot 栈封装为 AgentTools，可组合成单个
  Strands Agent。
- 本系列第一篇介绍了 Robot() 工厂、在仿真中记录演示、运行策略，以及把同一 agent 代码部署到实体 SO-101 机器人；本文转而聚焦数据从首个录制帧回到已部署策略的反向流动。
- LeRobot 数据集格式已被 Hub 上超过 9 万个数据集与模型、来自 8 千多家发布者使用，Strands Robots 录制的数据无需转换即可被所有读取
  LeRobot 数据的工具读取。
- 持续运行数据循环时，录制的数据不断增长、每次训练都要把整套数据集复制到 GPU，重复的字节传输会产生高昂成本，因此需要 Storage Buckets 作为中间工作层。
- Hugging Face Storage Buckets 是 2026 年 3 月发布的可变、非版本化、基于 Xet 的对象存储库类型，与数据集仓库同处 hf://
  命名空间，可使用用户已有的 hf CLI 操作。
object_mentions:
- object_type: project
  name: Strands Robots
  canonical_name: Strands Robots
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - Strands Robots 是 AWS 开源（Apache 2.0）的机器人 SDK，把机器人抽象、仿真和 LeRobot 栈作为 AgentTools
    暴露出来，可组合成单个 Strands Agent。
  - 本文围绕 Strands Robots 的流式数据循环展开，即用同一 agent 记录演示、从 Hub 直读数据训练，再把策略部署回实体机器人。
  article_id: 353217afc4f706b5
- object_type: project
  name: LeRobot
  canonical_name: LeRobot
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - LeRobot 的数据集格式已被 Hub 上超过 9 万个数据集与模型、来自 8 千多家发布者使用，Strands Robots 的录制数据无需转换即可被兼容工具读取。
  article_id: 353217afc4f706b5
- object_type: product
  name: Hugging Face Storage Buckets
  canonical_name: Hugging Face Storage Buckets
  url: null
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - Hugging Face Storage Buckets 是 2026 年 3 月发布的可变、非版本化、基于 Xet 的对象存储库类型，作为记录与训练之间的工作层。
  - 存储桶与数据集仓库同处 hf:// 命名空间，并使用用户已有的 hf CLI，可承载从录制到训练之间的全部数据。
  article_id: 353217afc4f706b5
- object_type: product
  name: SO-101 / SO-100
  canonical_name: SO-101
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 第一篇教程把同一套 Strands agent 代码部署到实体 SO-101 机器人上运行策略，而 Robot() 工厂可解析的 SO-100 是受支持的多类机器人实施例之一。
  article_id: 353217afc4f706b5
extract_result: success
---

# Record, train, and deploy from one place with Strands Agents, LeRobot, and Hugging Face Storage Buckets

*A walkthrough of the streaming data loop in Strands Robots, one agent loop that records robot demonstrations, trains on them by reading straight from the Hub, and deploys the policy back to hardware, with the dataset in the same on-disk LeRobot format the whole way through.*

You have an agent that can already record a demonstration and push it to the Hugging Face Hub. Now you want to run that loop continuously: collect episodes through the day, train a policy on the growing dataset, deploy it, and pull the next batch back to improve it. Run that loop once and every piece works. Run it every day and you start paying for the same byte transfers over and over. The recordings you upload keep growing, each training run copies the whole dataset to the GPUs before it starts, and every new checkpoint ships out while the next batch of recordings comes back.

The first post in this series introduced Strands Robots, an open source SDK from AWS (Apache 2.0) that exposes robot abstractions, simulation, and the LeRobot stack as AgentTools you compose into a single Strands agent. It covered the `Robot()`

factory, recording a demonstration in simulation, running a policy, and deploying the same agent code to a physical SO-101. That factory resolves a name against a registry of arms, humanoids, mobile bases, and hands, so the SO-100 used throughout this post is one of many supported embodiments. The robot catalog lists every robot the factory knows about. LeRobot's dataset format is already used by over 90,000 datasets and models on the Hub from more than 8,000 publishers (LeRobot Project Pulse). A Strands Robots recording is one more of them, so anything built to read LeRobot data can read it without conversion. If you are new to Strands Robots, start there; this post assumes that setup.

That post followed the agent loop in one direction, from a Hub dataset to a physical robot. This one follows the data the other way, from the first recorded frame back to the deployed policy, over Hugging Face Storage Buckets - a mutable, non-versioned, Xet-backed object-storage repository type announced in March 2026. A bucket sits beside your dataset repositories in the same `hf://`

namespace and uses the `hf`

CLI you already have, so it becomes the working layer that holds your data between the day you record it and the day you train on it.