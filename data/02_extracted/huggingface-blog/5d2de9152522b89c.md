---
title: Run a vLLM Server on HF Jobs in One Command
source: https://huggingface.co/blog/vllm-jobs
author: []
published: '2026-06-26'
created: '2026-06-26'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 5d2de9152522b89c
source_type: tech_blog
tldr: Hugging Face 推出通过一条命令在 HF Jobs 上运行 vLLM 推理服务器的功能，用户可使用 `hf jobs run` 指定 GPU
  型号、暴露端口，快速启动 OpenAI 兼容的模型推理端点，适合测试、评估和批量生成场景。
objective_summary: Hugging Face 在 2026 年 7 月发布了 `hf jobs run` 命令，允许用户通过单条命令在 HF Jobs
  基础设施上启动 vLLM 推理服务器。用户需安装 `huggingface_hub>=1.20.0` 并登录，指定 `--flavor`（如 a10g-large）和
  `--expose 8000` 参数即可运行官方 `vllm/vllm-openai` 镜像。启动后服务通过 HF 公共 Jobs 代理暴露，客户端使用 Hugging
  Face token 作为 Bearer token 即可通过 OpenAI 兼容 API 调用模型。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  technologies:
  - vLLM
  - OpenAI API
  key_people: []
key_logic_flow:
- Hugging Face 推出了 `hf jobs run` 命令，使用户能够以类似 `docker run` 的方式在 HF Jobs 基础设施上快速启动模型服务。
- 用户需要安装 `huggingface_hub>=1.20.0` 并通过 `hf auth login` 完成本地登录，同时需要有效的付款方式或预付费余额。
- 通过指定 `--flavor a10g-large`、`--expose 8000` 和 `--timeout 2h` 参数，可使用官方 `vllm/vllm-openai`
  镜像运行 `vllm serve` 命令启动推理服务器。
- 启动后服务会通过 HF 的公共 Jobs 代理暴露，用户获得一个格式为 `https://<job_id>--8000.hf.jobs` 的可访问 URL。
- vLLM 服务器提供 OpenAI 兼容的 API 接口，客户端使用 Hugging Face token 作为 Bearer token 即可发起请求，支持
  curl 和 OpenAI Python SDK 两种调用方式。
- 该功能适用于测试、评估和批量生成等非生产场景，生产级需求应使用 Hugging Face Inference Endpoints。
extract_result: success
object_mentions:
- object_type: project
  name: vLLM
  canonical_name: vllm-project/vllm
  url: https://github.com/vllm-project/vllm
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 文章使用官方 `vllm/vllm-openai` 镜像，通过 `vllm serve Qwen/Qwen3-4B --host 0.0.0.0 --port
    8000` 命令启动推理服务器。
  - vLLM 服务器提供 OpenAI 兼容的 API 接口，客户端可通过 curl 或 OpenAI Python SDK 调用模型的 chat completions
    功能。
  article_id: 5d2de9152522b89c
- object_type: product
  name: HF Jobs
  canonical_name: Hugging Face Jobs
  url: https://huggingface.co/jobs
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - '`hf jobs run` 命令允许用户在 HF Jobs 基础设施上启动容器化任务，按硬件使用分钟计费，需要付款方式或预付费余额。'
  - 通过 `--flavor a10g-large` 指定 GPU 型号、`--expose 8000` 暴露端口、`--timeout 2h` 设置超时时间，即可在
    Jobs 上运行 vLLM 推理服务。
  - 启动后服务通过 HF 公共 Jobs 代理暴露，URL 格式为 `https://<job_id>--8000.hf.jobs`，需使用 HF token
    进行认证访问。
  article_id: 5d2de9152522b89c
- object_type: project
  name: huggingface_hub
  canonical_name: huggingface/huggingface-hub
  url: https://github.com/huggingface/huggingface-hub
  confidence: high
  article_role: ecosystem_context
  evidence_snippets:
  - 使用 `hf jobs run` 命令需要安装 `huggingface_hub>=1.20.0` 版本，通过 `pip install -U "huggingface_hub>=1.20.0"`
    安装。
  - 用户需要通过 `hf auth login` 完成本地登录认证，后续 API 调用使用 `hf auth token` 获取的 token 进行鉴权。
  article_id: 5d2de9152522b89c
---

It's the quickest way to stand up a model for tests, evals, or batch generation. (If you're after a managed, production-ready service instead, that's what Inference Endpoints are for — more on when to pick which at the end.)

Here's the whole thing end to end.

- A payment method or a positive prepaid credit balance (Jobs is billed per‑minute by hardware usage).
`huggingface_hub >= 1.20.0`

:`pip install -U "huggingface_hub>=1.20.0"`

.- Logged in locally:
`hf auth login`

.

`hf jobs run`

is `docker run`

for HF infrastructure. We use the official `vllm/vllm-openai`

image, ask for a GPU with `--flavor`

, and expose vLLM's port with `--expose`

:

```
hf jobs run --flavor a10g-large --expose 8000 --timeout 2h \
vllm/vllm-openai:latest \
vllm serve Qwen/Qwen3-4B --host 0.0.0.0 --port 8000
```


`--expose 8000`

routes the container's port through HF's public jobs proxy (see the Serve Models guide for the full reference). The command prints the URL your server is reachable at:

```
✓ Job started
id: 6a381ca1953ed90bfb947332
url: https://huggingface.co/jobs/qgallouedec/6a381ca1953ed90bfb947332
Hint: Exposed ports are reachable at (requires an HF token with read access to the job):
https://6a381ca1953ed90bfb947332--8000.hf.jobs
```


`6a381ca1953ed90bfb947332`

is your job ID. Keep track of it, we'll need it. We'll use `<job_id>`

as a placeholder for it in the rest of the post.

Give it a couple of minutes to download weights and boot. When the logs show `Application startup complete`

, you're live.

vLLM speaks the OpenAI API, and every request just needs your HF token as a bearer token. The quickest way to hit it is curl:

```
curl https://<job_id>--8000.hf.jobs/v1/chat/completions \
-H "Authorization: Bearer $(hf auth token)" \
-H "Content-Type: application/json" \
-d '{
"model": "Qwen/Qwen3-4B",
"messages": [{"role": "user", "content": "Hello!"}],
"chat_template_kwargs": {"enable_thinking": false}
}'
```


which returns the usual OpenAI-style JSON, with `choices[0].message.content`

holding `"Hello! How can I assist you today? 😊"`

.

Or, from Python, point the OpenAI client at the exposed URL and pass the token as the API key:

```
from huggingface_hub import get_token
from openai import OpenAI
client = OpenAI(
base_url="https://<job_id>--8000.hf.jobs/v1",
api_key=get_token(),
)
resp = client.chat.completions.create(
model="Qwen/Qwen3-4B",
messages=[{"role": "user", "content": "Hello!"}],
extra_body={"chat_template_kwargs": {"enable_thinking": False}},
)
print(resp.choices[0].message.content)
```


```
Hello! How can I assist you today? 😊
```


Quick health check before you start: `curl https://<job_id>--8000.hf.jobs/v1/models -H "Authorization: Bearer $(hf auth token)"`

should list the model.