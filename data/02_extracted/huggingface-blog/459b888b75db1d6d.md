---
title: How an Agent Built a 3D Paris Gallery by Chaining Two Hugging Face Spaces
source: https://huggingface.co/blog/mishig/spaces-agents-md
author: []
published: '2026-06-09'
created: '2026-06-09'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: 459b888b75db1d6d
source_type: tech_blog
tldr: 一个编码智能体通过链式调用两个 Hugging Face Space（图像生成 + 3D 重建），自动构建了一个巴黎地标的 3D 高斯泼溅展示网站，全程依赖
  Spaces 的 agents.md API 文档而非手动操作。
objective_summary: Hugging Face 官方博客展示了一种新的 AI 应用构建范式：编码智能体利用 Gradio Space 自动暴露的 agents.md
  文件，先后调用 ideogram-ai/ideogram4（文本生成图像）和 VAST-AI/TripoSplat（单图转 3D 高斯泼溅）两个 Space，无需任何人工图像处理或
  3D 重建操作即生成了一座巴黎地标 3D 画廊。该演示印证了 Mitchell Hashimoto 提出的 "积木经济" 理论——AI 最强的能力不是从零构建，而是将经过验证的小型组件拼接在一起。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  - Ideogram
  - VAST-AI
  technologies:
  - Gaussian Splat
  - agents.md
  - Gradio
  - REST API
  key_people:
  - Mitchell Hashimoto
key_logic_flow:
- 一个编码智能体通过链式调用两个 Hugging Face Spaces，自动构建了巴黎地标的 3D 高斯泼溅展示网站，全程未使用图像生成或 3D 重建工具。
- 每个 Gradio Space 自动暴露一个 agents.md 文件，包含 API schema URL、调用和轮询端点、文件上传方式及认证信息，智能体可据此直接驱动
  Space。
- 智能体先调用 ideogram-ai/ideogram4 生成六张黑色背景的地标图像，再调用 VAST-AI/TripoSplat 将每张图像重建为 3D 高斯泼溅模型（.ply
  格式）。
- Mitchell Hashimoto 提出的 "积木经济" 理论认为，AI 最擅长的不是从零构建软件，而是将经过验证的小型组件拼接在一起。
- Hugging Face Spaces 已发展成为数千个开放权重模型的交互式部署平台，每个 Space 都可被智能体作为可调用的独立模块使用。
extract_result: success
object_mentions:
- object_type: project
  name: 3D Paris Gallery
  canonical_name: 3D Paris Gallery (Hugging Face Space)
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 一个编码智能体通过链式调用两个 Hugging Face Spaces，自动生成了巴黎地标的 3D 高斯泼溅展示网站，全程未使用图像生成或 3D 重建工具。
  - 该画廊使用 ideogram-ai/ideogram4 生成六张黑色背景的地标图像，再由 VAST-AI/TripoSplat 将每张图像重建为 3D 高斯泼溅模型。
  article_id: 459b888b75db1d6d
- object_type: product
  name: agents.md
  canonical_name: agents.md (Hugging Face Spaces specification)
  url: null
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 每个 Gradio Space 自动暴露一个 agents.md 文件，包含 API schema URL、调用和轮询端点、文件上传方式及认证信息。
  - 智能体通过读取 agents.md 即可直接驱动 Space 完成端到端调用，无需客户端库或硬编码集成。
  article_id: 459b888b75db1d6d
- object_type: model
  name: VAST-AI/TripoSplat
  canonical_name: VAST-AI/TripoSplat
  url: https://huggingface.co/spaces/VAST-AI/TripoSplat
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - VAST-AI/TripoSplat 能够从单张图像重建出 3D 高斯泼溅模型（.ply 格式），是智能体链式管道的第二个环节。
  - 该 Space 接受单张图像作为输入，输出三维高斯泼溅重建结果，无需额外的 SDK 或 GPU 配置。
  article_id: 459b888b75db1d6d
- object_type: model
  name: ideogram-ai/ideogram4
  canonical_name: ideogram-ai/ideogram4
  url: https://huggingface.co/spaces/ideogram-ai/ideogram4
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - ideogram-ai/ideogram4 将文字提示转换为黑色背景的地标图像，例如将埃菲尔铁塔渲染为基座上的立体模型展示。
  - 该 Space 是智能体管道的第一环，将文字提示转化为可供 3D 重建使用的干净、单一主题图像。
  article_id: 459b888b75db1d6d
---

#### TripoSplat

Generate 3D models from a single image

I asked a coding agent to build a beautiful website showcasing the monuments of
Paris as 3D Gaussian splats. I never opened an image generator. I never touched a
3D reconstruction tool. The agent produced every asset (the images **and** the 3D
splats) by calling two Hugging Face Spaces directly, then wired them into a
cinematic viewer.

Here's the result, live as a static Space:

This post is about *how* that's possible now, and why I think it's a preview of
how a lot of multimedia software gets built from here on.

Mitchell Hashimoto recently described a shift he calls the
building block economy:
the most effective path to software is no longer a polished monolith, but small,
well-documented components that others (increasingly *agents*) can assemble.
His key observation: AI is okay at building everything from scratch, but it is
**really good at gluing together** proven pieces.

That thesis has mostly been told with *code* libraries. But the same forces are
hitting **multimedia AI**. The hard part of using a state-of-the-art image model,
a video model, a TTS model, or a 3D reconstruction model was never the model. It
was the integration: SDKs, weights, GPUs, input formats, polling. If each model
were instead a documented, callable block, an agent could glue them together the
same way it globs together npm packages.

That's exactly what Hugging Face Spaces have quietly become.

The Hub hosts thousands of state-of-the-art models (a huge share of them
**open-weights**), and most are deployed as interactive **Spaces**. As of now,
every Gradio Space also exposes a plain-text
`agents.md`

that tells an agent
*exactly* how to call it:

```
curl https://huggingface.co/spaces/VAST-AI/TripoSplat/agents.md
```


returns everything needed in one shot: the schema URL, the call and poll templates, how to upload files, and the auth hint:

```
API schema: GET .../gradio_api/info
Call endpoint: POST .../gradio_api/call/v2/{endpoint} {"param_name": value, ...}
Poll result: GET .../gradio_api/call/{endpoint}/{event_id}
File inputs: POST .../gradio_api/upload -F "files=@file.ext"
Auth: Bearer $HF_TOKEN
```


No client library. No hardcoded integration. An agent reads that, and it can drive
the Space end to end. Set an `HF_TOKEN`

and you're going.

The real unlock is **chaining**: the output of one Space becomes the input to the
next. Prompt → image → 3D. That's the whole pipeline behind this gallery.

The agent chained two Spaces:

`ideogram-ai/ideogram4`

turned each monument into a clean,
dark-background "specimen" shot (and the Eiffel Tower into a little diorama on a
plinth). Prompt in, image out.`VAST-AI/TripoSplat`

reconstructed a 3D Gaussian splat (`.ply`

) from each single image. Image in,
3D out.Generated image

Reconstructed splat

The six source images the agent generated, all isolated on black, ready for single-image 3D reconstruction: