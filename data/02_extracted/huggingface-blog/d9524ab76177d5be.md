---
title: Multi-Vector (Late Interaction) Embedding Models with Sentence Transformers
source: https://huggingface.co/blog/multi-vector-encoder
author: []
published: '2026-08-18'
created: '2026-08-19'
manifest_dates:
- '2026-08-19'
- '2026-08-20'
- '2026-08-21'
description: ''
tags:
- clippings
extraction_status: success
pipeline_stage: fact_extracted
id: d9524ab76177d5be
source_type: tech_blog
tldr: Hugging Face 推出 sentence-transformers 的 MultiVectorEncoder 类，用于 ColBERT 式多向量（后期交互）检索。它可加载
  PyLate、Stanford NLP ColBERT 与 colpali-engine 检查点，用 MaxSim 打分，并支持无需 OCR 的视觉文档检索。
objective_summary: Hugging Face 在官方博客介绍 sentence-transformers 库新增的 MultiVectorEncoder
  类，用于 ColBERT 风格的后期交互检索。该类可直接加载任何 PyLate 检查点、Stanford NLP 的 ColBERT 检查点以及 colpali-engine
  视觉文档检索模型。多向量模型为每个 token 保留一个低维向量，用 MaxSim 算子计算查询与文档的相似度，相比单向量模型保留更多 token 级匹配信息，检索效果通常更强但索引体积更大。
event_type: framework_tools
epistemic_status: verified_fact
entities:
  companies:
  - Hugging Face
  - Stanford NLP
  technologies:
  - MultiVectorEncoder
  - ColBERT
  - MaxSim
  - sentence-transformers
  - PyLate
  - colpali-engine
  - late interaction
  key_people: []
key_logic_flow:
- 单向量模型将整段文本压缩为一个固定大小的向量，相似度通过一次点积计算，稀有实体、精确标识符或长段落中的关键子句会在压缩中互相竞争空间。
- 多向量模型又被称为后期交互或 ColBERT 式模型，它不做 token 池化，而是把每个 token 嵌入投影到低维（经典为 128 维）并全部保留，例如 9
  个 token 的文档变成 9x128 矩阵。
- Hugging Face 在 sentence-transformers 中新增 MultiVectorEncoder 类，任何 PyLate 检查点、Stanford
  NLP ColBERT 检查点以及 colpali-engine 模型都能通过同一套 API 直接加载。
- 查询与文档之间使用 MaxSim 算子按 token 级匹配进行打分，通常带来更强的检索效果，但代价是索引体积更大。
- 多向量模型是视觉文档检索的当前最先进方法，文本查询可直接匹配页面图像而无需中间的 OCR 步骤。
- 上述全部功能只需执行 pip install -U sentence-transformers 即可使用，无需额外安装其它依赖。
object_mentions:
- object_type: project
  name: sentence-transformers
  canonical_name: sentence-transformers
  url: https://github.com/UKPLab/sentence-transformers
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - 博客介绍 sentence-transformers 库新增的 MultiVectorEncoder 类，用于 ColBERT 式后期交互检索。
  - 文章称全部功能只需执行 pip install -U sentence-transformers 即可使用，无需其它额外依赖。
  article_id: d9524ab76177d5be
- object_type: product
  name: MultiVectorEncoder
  canonical_name: MultiVectorEncoder
  url: https://huggingface.co/blog/multi-vector-encoder
  confidence: high
  article_role: primary_subject
  evidence_snippets:
  - MultiVectorEncoder 是新增的用于 ColBERT 式后期交互检索的接口，可加载 PyLate、Stanford NLP ColBERT 以及
    colpali-engine 检查点。
  - 文章展示如何用 MultiVectorEncoder 进行模型加载、编码与 MaxSim 打分，并接入搜索栈与页面图像检索。
  article_id: d9524ab76177d5be
- object_type: paper
  name: ColBERT
  canonical_name: ColBERT
  url: https://arxiv.org/abs/2004.12832
  confidence: high
  article_role: mentioned_reference
  evidence_snippets:
  - 多向量模型也被称为后期交互或 ColBERT 式模型，其命名源自 ColBERT 论文。
  - 文章指出 Stanford NLP 的 ColBERT 检查点可以直接加载进 MultiVectorEncoder 使用。
  article_id: d9524ab76177d5be
- object_type: project
  name: PyLate
  canonical_name: PyLate
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - 文章称任何 PyLate 检查点都能直接载入 MultiVectorEncoder，通过与稠密、稀疏和重排模型相同的 API 使用。
  article_id: d9524ab76177d5be
- object_type: project
  name: colpali-engine
  canonical_name: colpali-engine
  url: null
  confidence: medium
  article_role: mentioned_reference
  evidence_snippets:
  - colpali-engine 模型用于视觉文档检索，可通过与稠密、稀疏和重排模型相同的 API 在 MultiVectorEncoder 中使用。
  - 多向量模型在视觉文档检索中为最先进方法，文本查询可直接匹配页面图像而无需 OCR 步骤。
  article_id: d9524ab76177d5be
extract_result: success
---

`MultiVectorEncoder`

, for ColBERT-style late interaction retrieval. Any PyLate checkpoint and any Stanford-NLP ColBERT checkpoint loads straight into it, and colpali-engine models for visual document retrieval can be used too, through the same familiar API you already use for dense, sparse, and reranker models.
Where a regular embedding model compresses a whole text into one vector, a multi-vector model keeps **one vector per token** and scores query against document with the MaxSim operator. That preserves token-level matching information that a single vector has to average away, which usually means stronger retrieval at the cost of a bigger index. It's also the state of the art for visual document retrieval, where a text query is matched against page images directly, with no OCR step in between.

In this blogpost, we'll show you how to use these models: loading the various checkpoint formats, encoding and scoring, plugging them into a search stack, running them on page images, and keeping the index affordable. Everything below runs on a plain `pip install -U sentence-transformers`

.

- What are Multi-Vector Models?
- Installation
- Loading a Model
- Encoding Queries and Documents
- Scoring with MaxSim
- Semantic Search
- Retrieve and Rerank
- Indexing
- Visual Document Retrieval
- Audio Retrieval
- Video Retrieval
- Interpretability
- Token Pooling
- Speeding Up Inference
- Evaluating a Model
- Coming from PyLate or colpali-engine
- Supported Models
- Acknowledgements
- Additional Resources

A dense embedding model reads a text and returns a single fixed-size vector. Everything the model noticed has to fit in those 384, 768, or 1024 numbers, and similarity is one dot product between two such summaries. This works remarkably well, but the compression is lossy in a specific way: a rare entity, an exact identifier, or one crucial clause in a long passage all have to compete for room in the same vector. A query with several requirements at once runs into the same wall. For "green sofa with wooden legs and rounded cushions", a single vector has to blend all four into one point, so a green sofa with the wrong legs ends up sitting close to the one you actually asked for.

A multi-vector model (also called a late-interaction or ColBERT-style model, after the ColBERT paper) skips that compression. It runs the same transformer, but instead of pooling the token embeddings into one vector, it projects each token embedding down to a small dimension (classically 128) and keeps all of them. A 9-token document becomes a 9x128 matrix, not a 1x128 vector.