// ============================================================================
// types.ts — 数据层共享类型
//
// 被 sources、reports 等多个数据模块共用。
// ============================================================================

/** 通用日期范围筛选，格式 YYYY-MM-DD */
export interface DateRange {
  from?: string;
  to?: string;
}
