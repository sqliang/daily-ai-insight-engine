# Python 依赖

# 初始化（pipeline/run.py 入口）
python-dotenv>=1.0          # .env 环境变量加载

# Claude Agent SDK (统一入口 run.py)
claude-agent-sdk>=0.1.0

# Stage 1: 数据获取 (Ingestion)
pyyaml>=6.0            # YAML 配置解析 & frontmatter 序列化
feedparser>=6.0        # RSS/Atom feed 解析
trafilatura>=1.12      # 网页正文抽取 (HTML → Markdown)
playwright>=1.48       # Headless browser for JS-rendered pages

# Schema 定义 (所有阶段)
pydantic>=2.0

# Testing
pytest>=7.0.0

# Type checking
mypy>=1.0.0

# 代码风格检查（可选）
ruff>=0.1.0
